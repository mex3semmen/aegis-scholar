use crate::corpus_paths::CorpusPaths;
use crate::errors::{AegisError, AegisResult};
use crate::locators::CitationLocator;
use crate::source_registry::SourceRegistry;
use crate::retrieval::{RetrievalResponse, RetrievalService};
use crate::local_runtime::{
    preview_local_model_runtime_health,
    preview_local_runtime_invocation_plan,
    smoke_test_local_runtime_inference,
    LocalModelRuntimeConfig,
    LocalRuntimeAdapterContractStatus,
    LocalRuntimeAdapterKind,
    LocalModelRuntimeHealthStatus,
    LocalRuntimeCapabilityStatus,
    LocalRuntimeInvocationPlanRequest,
    LocalRuntimeInvocationPlanStatus,
    LocalRuntimeProbeReadinessStatus,
    LocalRuntimeSmokeInferenceRequest,
    LocalRuntimeSmokeInferenceStatus,
    LocalRuntimeProbeWarning,
    LocalRuntimeSmokeExecutionPlanPreviewRequest,
    LocalRuntimeSmokeExecutionPlanStatus,
    LocalRuntimeSmokeDiagnosticPreview,
    LocalRuntimeSmokeDiagnosticStatus,
    LocalRuntimeSmokeReadinessStatus,
    LocalRuntimeValidationStatus,
    LocalRuntimeVersionProbeStatus,
    preview_llama_runtime_smoke_execution_plan,
};
use serde::{Deserialize, Serialize};
use std::fs;
use std::collections::{BTreeMap, BTreeSet};
use std::path::PathBuf;

const SCHOLAR_CHAT_RETRIEVAL_PREVIEW_LIMIT: usize = 5;

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatMode {
    LectureLearning,
    ThesisWriting,
    LiteratureReview,
    Flashcards,
    StatisticsMethods,
    GeneralScholar,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum GroundingPolicy {
    LocalOnly,
    LocalFirst,
    AllowMarkedModelKnowledge,
    ExternalAdaptersLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatStatus {
    PreviewOnly,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatRequest {
    pub prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_ids: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundingPlan {
    pub selected_source_count: usize,
    pub local_corpus_required: bool,
    pub retrieval_would_run: bool,
    pub evidence_pack_would_be_required: bool,
    pub model_knowledge_allowed: bool,
    pub external_adapters_available: bool,
    pub summary: String,
    pub steps: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatResponse {
    pub status: ScholarChatStatus,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub grounding_plan: ScholarChatGroundingPlan,
    pub warnings: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificDisciplineRegistryStatus {
    Blocked,
    ConceptMapped,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificDisciplineScienceClass {
    CoreScience,
    MethodScience,
    AppliedDomain,
    CurriculumLayer,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificDisciplineRegistryPreviewRequest {
    pub topic: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificDisciplineRegistryPreview {
    pub status: ScholarChatScientificDisciplineRegistryStatus,
    pub normalized_topic: String,
    pub normalized_mode: String,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub discipline_path: Vec<String>,
    pub science_class: Option<ScholarChatScientificDisciplineScienceClass>,
    pub parent_path: Vec<String>,
    pub related_methods: Vec<String>,
    pub appears_in: Vec<String>,
    pub preferred_sources: Vec<String>,
    pub curriculum_sources: Vec<String>,
    pub canonical_mappings: Vec<String>,
    pub planned_queries: Vec<String>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub registry_preview_only: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_bm25_index: bool,
    pub no_vector_index: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSourceRegistryStatus {
    Blocked,
    SourcePlanReady,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSourceAccessClass {
    OpenMetadata,
    OpenFullText,
    UserProvidedLocalFile,
    UserAuthenticatedAccess,
    RestrictedNoIngest,
    CatalogOnly,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSourcePriority {
    Primary,
    Supporting,
    Conditional,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSourceRegistryPreviewRequest {
    pub topic: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSourceRegistrySourceFamily {
    pub id: String,
    pub label: String,
    pub domain: String,
    pub access_class: ScholarChatScientificSourceAccessClass,
    pub priority: ScholarChatScientificSourcePriority,
    pub applies_when: String,
    pub active_for_current_context: bool,
    pub planned_use: String,
    pub query_roles: Vec<String>,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSourceRegistrySourcePlan {
    pub source_family_count: usize,
    pub active_source_family_count: usize,
    pub conditional_source_family_count: usize,
    pub planned_metadata_query_count: usize,
    pub summary: String,
    pub steps: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSourceRegistryPreview {
    pub status: ScholarChatScientificSourceRegistryStatus,
    pub normalized_topic: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub discipline_status: ScholarChatScientificDisciplineRegistryStatus,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub source_plan: ScholarChatScientificSourceRegistrySourcePlan,
    pub source_families: Vec<ScholarChatScientificSourceRegistrySourceFamily>,
    pub preferred_source_ids: Vec<String>,
    pub conditional_source_ids: Vec<String>,
    pub excluded_source_ids: Vec<String>,
    pub access_classes: Vec<ScholarChatScientificSourceAccessClass>,
    pub planned_metadata_queries: Vec<String>,
    pub ranking_hints: Vec<String>,
    pub deduplication_hints: Vec<String>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub source_registry_preview_only: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_bm25_index: bool,
    pub no_vector_index: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificQueryUnderstandingStatus {
    Blocked,
    Understood,
    Ambiguous,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificQueryIntent {
    ConceptExplanation,
    MethodApplication,
    LiteratureSearch,
    CourseLearning,
    Comparison,
    Unknown,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificAmbiguityLevel {
    None,
    Low,
    Medium,
    High,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificQueryDetectedAlias {
    pub alias: String,
    pub concept: String,
    pub language: String,
    pub start_index: usize,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificQueryUnderstandingPreviewRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificQueryUnderstandingPreview {
    pub status: ScholarChatScientificQueryUnderstandingStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub ambiguity_level: ScholarChatScientificAmbiguityLevel,
    pub ambiguity_warnings: Vec<String>,
    pub language_hints: Vec<String>,
    pub detected_aliases: Vec<ScholarChatScientificQueryDetectedAlias>,
    pub discipline_status: ScholarChatScientificDisciplineRegistryStatus,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub source_registry_status: ScholarChatScientificSourceRegistryStatus,
    pub preferred_source_ids: Vec<String>,
    pub conditional_source_ids: Vec<String>,
    pub excluded_source_ids: Vec<String>,
    pub planned_metadata_queries: Vec<String>,
    pub planned_local_search_queries: Vec<String>,
    pub planned_expanded_queries: Vec<String>,
    pub evidence_requirements: Vec<String>,
    pub ranking_hints: Vec<String>,
    pub deduplication_hints: Vec<String>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub query_understanding_preview_only: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_bm25_index: bool,
    pub no_vector_index: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSearchPlanStatus {
    Blocked,
    SearchPlanReady,
    NeedsDisambiguation,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSearchStrategy {
    LocalFirst,
    MetadataFirst,
    CourseLocalFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificSearchPlanStepKind {
    LocalSourceSearch,
    LocalCourseMaterialSearch,
    MetadataSourceSearch,
    QueryExpansion,
    SourceFamilyRouting,
    RankingPlan,
    DeduplicationPlan,
    EvidenceRequirementCheck,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSearchPlanStep {
    pub kind: ScholarChatScientificSearchPlanStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_queries: Vec<String>,
    pub source_ids: Vec<String>,
    pub depends_on: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificLocalSearchPlan {
    pub local_source_count: usize,
    pub selected_local_source_ids: Vec<String>,
    pub planned_queries: Vec<String>,
    pub local_first: bool,
    pub requires_local_evidence_before_answer: bool,
    pub will_read_files: bool,
    pub will_build_index: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataSearchPlan {
    pub source_family_count: usize,
    pub preferred_source_ids: Vec<String>,
    pub conditional_source_ids: Vec<String>,
    pub excluded_source_ids: Vec<String>,
    pub planned_queries: Vec<String>,
    pub will_call_connectors: bool,
    pub will_make_web_requests: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSourceRoutingPlan {
    pub route_count: usize,
    pub active_routes: Vec<String>,
    pub conditional_routes: Vec<String>,
    pub excluded_routes: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSearchPlanRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificSearchPlanPreview {
    pub status: ScholarChatScientificSearchPlanStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub ambiguity_level: ScholarChatScientificAmbiguityLevel,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub source_registry_status: ScholarChatScientificSourceRegistryStatus,
    pub search_strategy: ScholarChatScientificSearchStrategy,
    pub local_search_plan: ScholarChatScientificLocalSearchPlan,
    pub metadata_search_plan: ScholarChatScientificMetadataSearchPlan,
    pub source_routing_plan: ScholarChatScientificSourceRoutingPlan,
    pub planned_search_steps: Vec<ScholarChatScientificSearchPlanStep>,
    pub planned_local_queries: Vec<String>,
    pub planned_metadata_queries: Vec<String>,
    pub planned_expanded_queries: Vec<String>,
    pub preferred_source_ids: Vec<String>,
    pub conditional_source_ids: Vec<String>,
    pub excluded_source_ids: Vec<String>,
    pub evidence_requirements: Vec<String>,
    pub ranking_hints: Vec<String>,
    pub deduplication_hints: Vec<String>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub scientific_search_plan_preview_only: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_bm25_index: bool,
    pub no_vector_index: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatLocalLiteratureIndexStatus {
    Blocked,
    IndexPlanReady,
    NeedsLocalSources,
    NeedsDisambiguation,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatLocalLiteratureIndexStrategy {
    ScholarChatLocalFirst,
    ScientificPaperCitationLocalFirst,
    CourseMaterialLocalFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatLocalLiteratureIngestionReadiness {
    Blocked,
    PreviewReady,
    NeedsSources,
    NeedsDisambiguation,
    UnknownConceptMappingNeeded,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatLocalLiteratureIndexStepKind {
    SourceSelectionReview,
    MetadataRequirementCheck,
    CorpusManifestPlan,
    ExtractionPlan,
    ChunkingPolicyPlan,
    LexicalIndexPlan,
    VectorIndexPlan,
    DeduplicationPlan,
    RetrievalReadinessCheck,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatLocalLiteratureCorpusPlan {
    pub selected_source_count: usize,
    pub selected_local_source_ids: Vec<String>,
    pub expected_source_kinds: Vec<String>,
    pub corpus_manifest_would_be_required: bool,
    pub will_create_corpus: bool,
    pub will_read_files: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatLocalLiteratureIndexArtifactPlan {
    pub planned_artifact_ids: Vec<String>,
    pub planned_artifact_descriptions: Vec<String>,
    pub will_create_artifacts: bool,
    pub will_create_bm25_index: bool,
    pub will_create_vector_index: bool,
    pub will_generate_embeddings: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatLocalLiteratureIndexStep {
    pub kind: ScholarChatLocalLiteratureIndexStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatLocalLiteratureIndexRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatLocalLiteratureIndexPreview {
    pub status: ScholarChatLocalLiteratureIndexStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub expected_source_kinds: Vec<String>,
    pub search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub local_index_strategy: ScholarChatLocalLiteratureIndexStrategy,
    pub local_corpus_plan: ScholarChatLocalLiteratureCorpusPlan,
    pub index_artifact_plan: ScholarChatLocalLiteratureIndexArtifactPlan,
    pub ingestion_readiness: ScholarChatLocalLiteratureIngestionReadiness,
    pub planned_index_fields: Vec<String>,
    pub planned_chunking_policy: Vec<String>,
    pub planned_metadata_requirements: Vec<String>,
    pub planned_local_queries: Vec<String>,
    pub planned_index_steps: Vec<ScholarChatLocalLiteratureIndexStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub local_literature_index_preview_only: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_bm25_index: bool,
    pub no_vector_index: bool,
    pub no_retrieval_execution: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatCourseLiteratureRegistryStatus {
    Blocked,
    CourseRegistryPlanReady,
    NeedsCourseContext,
    NeedsLocalSources,
    NeedsDisambiguation,
    UnknownConcept,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatCourseLiteratureRegistryStrategy {
    CourseMaterialAlignmentFirst,
    ModuleContextFirst,
    LocalSourceAlignmentFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatCourseLiteratureRegistryStepKind {
    CourseIdentityReview,
    ModuleContextReview,
    CourseMaterialKindPlan,
    LocalSourceAlignmentPlan,
    CurriculumMetadataRequirementCheck,
    LocalLiteratureIndexAlignment,
    RetrievalReadinessCheck,
    LearningPathAlignmentPlan,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCourseIdentityPreview {
    pub course_context: Option<String>,
    pub module_code: Option<String>,
    pub course_title: Option<String>,
    pub instructor: Option<String>,
    pub semester: Option<String>,
    pub identity_key: Option<String>,
    pub has_course_context: bool,
    pub has_module_code: bool,
    pub has_course_title: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCourseMaterialPlan {
    pub selected_source_count: usize,
    pub selected_local_source_ids: Vec<String>,
    pub expected_course_material_kinds: Vec<String>,
    pub known_material_kinds: Vec<String>,
    pub unknown_material_kinds: Vec<String>,
    pub will_read_files: bool,
    pub will_import_sources: bool,
    pub will_create_registry: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCurriculumAlignmentPlan {
    pub requires_course_context: bool,
    pub requires_module_metadata: bool,
    pub requires_learning_objectives_later: bool,
    pub requires_prerequisites_later: bool,
    pub requires_session_or_week_mapping_later: bool,
    pub will_scrape_curriculum_sources: bool,
    pub will_call_connectors: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCourseLiteratureRegistryStep {
    pub kind: ScholarChatCourseLiteratureRegistryStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCourseLiteratureRegistryPreviewRequest {
    pub query: String,
    pub course_context: Option<String>,
    pub module_code: Option<String>,
    pub course_title: Option<String>,
    pub instructor: Option<String>,
    pub semester: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_course_material_kinds: Option<Vec<String>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatCourseLiteratureRegistryPreview {
    pub status: ScholarChatCourseLiteratureRegistryStatus,
    pub normalized_query: String,
    pub normalized_course_context: Option<String>,
    pub normalized_module_code: Option<String>,
    pub normalized_course_title: Option<String>,
    pub normalized_instructor: Option<String>,
    pub normalized_semester: Option<String>,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub expected_course_material_kinds: Vec<String>,
    pub local_literature_index_status: ScholarChatLocalLiteratureIndexStatus,
    pub search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub course_registry_strategy: ScholarChatCourseLiteratureRegistryStrategy,
    pub course_identity: ScholarChatCourseIdentityPreview,
    pub course_material_plan: ScholarChatCourseMaterialPlan,
    pub curriculum_alignment_plan: ScholarChatCurriculumAlignmentPlan,
    pub planned_course_metadata_requirements: Vec<String>,
    pub planned_course_material_queries: Vec<String>,
    pub planned_registry_steps: Vec<ScholarChatCourseLiteratureRegistryStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub course_literature_registry_preview_only: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_web_request: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_local_file_indexing: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatMetadataConnectorPlanStatus {
    Blocked,
    ConnectorPlanReady,
    NeedsDisambiguation,
    UnknownConcept,
    NeedsMetadataSource,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatMetadataConnectorStrategy {
    OpenalexAndCrossref,
    OpenalexFirst,
    CrossrefFirst,
    MetadataSourceSelectionNeeded,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatMetadataConnectorStepKind {
    QueryUnderstandingAlignment,
    MetadataSourceSelection,
    OpenalexQueryPlan,
    CrossrefQueryPlan,
    ResultShapeMappingPlan,
    DoiFilterPlan,
    OpenAccessFilterPlan,
    YearFilterPlan,
    DeduplicationPlan,
    AttributionRequirementCheck,
    ComplianceBoundaryCheck,
    DownstreamEvidencePackAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataConnectorSourceSelectionPlan {
    pub requested_sources: Vec<String>,
    pub supported_sources: Vec<String>,
    pub unknown_sources: Vec<String>,
    pub selected_sources: Vec<String>,
    pub needs_source_selection: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataConnectorQueryPlan {
    pub source_id: String,
    pub enabled: bool,
    pub endpoint_family: String,
    pub planned_query_terms: Vec<String>,
    pub planned_filters: Vec<String>,
    pub max_results: u32,
    pub requires_api_key: bool,
    pub will_call_api: bool,
    pub will_fetch_url: bool,
    pub will_scrape: bool,
    pub expected_result_fields: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataDeduplicationPlan {
    pub key_fields: Vec<String>,
    pub preferred_source_order: Vec<String>,
    pub doi_required: bool,
    pub title_year_fallback_allowed: bool,
    pub will_deduplicate_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataAttributionPlan {
    pub required_attribution_notes: Vec<String>,
    pub planned_user_agent_note: String,
    pub citation_safety_notes: Vec<String>,
    pub will_emit_attribution_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataCompliancePlan {
    pub external_metadata_only: bool,
    pub no_fulltext_download: bool,
    pub no_scraping: bool,
    pub respects_rate_limits_later: bool,
    pub requires_terms_review_later: bool,
    pub will_call_external_services: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataConnectorStep {
    pub kind: ScholarChatMetadataConnectorStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataConnectorPlanRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub max_results_per_source: Option<u32>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatMetadataConnectorPlanPreview {
    pub status: ScholarChatMetadataConnectorPlanStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub expected_source_kinds: Vec<String>,
    pub preferred_metadata_sources: Vec<String>,
    pub unknown_metadata_sources: Vec<String>,
    pub normalized_max_results_per_source: u32,
    pub require_open_access: bool,
    pub require_doi: bool,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub connector_strategy: ScholarChatMetadataConnectorStrategy,
    pub source_selection_plan: ScholarChatMetadataConnectorSourceSelectionPlan,
    pub openalex_plan: ScholarChatMetadataConnectorQueryPlan,
    pub crossref_plan: ScholarChatMetadataConnectorQueryPlan,
    pub deduplication_plan: ScholarChatMetadataDeduplicationPlan,
    pub attribution_plan: ScholarChatMetadataAttributionPlan,
    pub compliance_plan: ScholarChatMetadataCompliancePlan,
    pub planned_metadata_queries: Vec<String>,
    pub planned_connector_steps: Vec<ScholarChatMetadataConnectorStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub metadata_connector_plan_preview_only: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPsychologySourceConnectorPlanStatus {
    Blocked,
    SourceConnectorPlanReady,
    NeedsPsychologyScope,
    NeedsMetadataConnectorPlan,
    NeedsDisambiguation,
    UnknownConcept,
    NeedsSourceFamily,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPsychologySourceFamilyStrategy {
    PsychologyMetadataBalanced,
    OpenMetadataFirst,
    ScholarlyIndexFirst,
    CourseLiteratureFirst,
    SourceFamilySelectionNeeded,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPsychologyScopeStatus {
    Blocked,
    NeedsPsychologyScope,
    PsychologyScopeReady,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPsychologySourceConnectorStepKind {
    MetadataConnectorAlignment,
    PsychologyScopeClassification,
    SourceFamilySelection,
    OpenalexPsychologyPlan,
    CrossrefPsychologyPlan,
    PubmedPsychologyPlan,
    EricPsychologyPlan,
    ApaPsycnetDiscoveryPlan,
    LocalCourseLiteraturePlan,
    MethodologyRouting,
    PopulationRouting,
    TopicAreaRouting,
    EvidenceRequirementMapping,
    ComplianceBoundaryCheck,
    DownstreamEvidencePackAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologySourceFamilySelectionPlan {
    pub requested_families: Vec<String>,
    pub supported_families: Vec<String>,
    pub unknown_families: Vec<String>,
    pub selected_families: Vec<String>,
    pub needs_source_family_selection: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologySourceFamilyPlan {
    pub family_id: String,
    pub enabled: bool,
    pub source_family_label: String,
    pub connector_kind: String,
    pub planned_query_terms: Vec<String>,
    pub planned_filters: Vec<String>,
    pub planned_metadata_dependencies: Vec<String>,
    pub expected_result_types: Vec<String>,
    pub evidence_relevance: Vec<String>,
    pub access_boundary_notes: Vec<String>,
    pub will_call_connector: bool,
    pub will_fetch_url: bool,
    pub will_scrape: bool,
    pub will_authenticate: bool,
    pub will_bypass_paywall: bool,
    pub will_write_metadata: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologyMethodologyRoutingPlan {
    pub methodology_hints: Vec<String>,
    pub unknown_methodology_hints: Vec<String>,
    pub inferred_methodology_routes: Vec<String>,
    pub recommended_source_families: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologyPopulationRoutingPlan {
    pub population_hints: Vec<String>,
    pub recommended_filters_later: Vec<String>,
    pub ethics_or_safety_notes: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologyTopicAreaRoutingPlan {
    pub topic_area_hints: Vec<String>,
    pub inferred_topic_areas: Vec<String>,
    pub recommended_source_families: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologyEvidenceRequirementsPlan {
    pub evidence_requirements: Vec<String>,
    pub preferred_metadata_fields: Vec<String>,
    pub quality_signals: Vec<String>,
    pub exclusion_notes: Vec<String>,
    pub downstream_evidence_pack_requirements: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologyConnectorCompliancePlan {
    pub external_metadata_only: bool,
    pub no_fulltext_download: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub respects_rate_limits_later: bool,
    pub requires_terms_review_later: bool,
    pub requires_institutional_access_review_later: bool,
    pub will_call_external_services: bool,
    pub will_write_metadata: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologySourceConnectorStep {
    pub kind: ScholarChatPsychologySourceConnectorStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologySourceConnectorPlanRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub methodology_hints: Option<Vec<String>>,
    pub population_hints: Option<Vec<String>>,
    pub topic_area_hints: Option<Vec<String>>,
    pub max_results_per_source: Option<u32>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPsychologySourceConnectorPlanPreview {
    pub status: ScholarChatPsychologySourceConnectorPlanStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub expected_source_kinds: Vec<String>,
    pub preferred_metadata_sources: Vec<String>,
    pub preferred_psychology_source_families: Vec<String>,
    pub unknown_psychology_source_families: Vec<String>,
    pub methodology_hints: Vec<String>,
    pub unknown_methodology_hints: Vec<String>,
    pub population_hints: Vec<String>,
    pub topic_area_hints: Vec<String>,
    pub normalized_max_results_per_source: u32,
    pub require_open_access: bool,
    pub require_doi: bool,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub metadata_connector_plan_status: ScholarChatMetadataConnectorPlanStatus,
    pub metadata_connector_strategy: ScholarChatMetadataConnectorStrategy,
    pub scientific_search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub psychology_scope_status: ScholarChatPsychologyScopeStatus,
    pub source_family_strategy: ScholarChatPsychologySourceFamilyStrategy,
    pub source_family_selection_plan: ScholarChatPsychologySourceFamilySelectionPlan,
    pub psychology_source_family_plans: Vec<ScholarChatPsychologySourceFamilyPlan>,
    pub methodology_routing_plan: ScholarChatPsychologyMethodologyRoutingPlan,
    pub population_routing_plan: ScholarChatPsychologyPopulationRoutingPlan,
    pub topic_area_routing_plan: ScholarChatPsychologyTopicAreaRoutingPlan,
    pub evidence_requirements_plan: ScholarChatPsychologyEvidenceRequirementsPlan,
    pub compliance_plan: ScholarChatPsychologyConnectorCompliancePlan,
    pub planned_source_connector_steps: Vec<ScholarChatPsychologySourceConnectorStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub psychology_source_connector_preview_only: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificEvidencePackPlanStatus {
    Blocked,
    EvidencePackPlanReady,
    NeedsPsychologySourceConnectorPlan,
    NeedsEvidenceSources,
    NeedsCitationMetadata,
    NeedsDisambiguation,
    UnknownConcept,
    NeedsPsychologyScope,
    NeedsSourceFamily,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificEvidencePackStrategy {
    BalancedLocalAndMetadata,
    LocalContextFirst,
    MetadataCitationFirst,
    PsychologySourceFamilyFirst,
    EvidenceSourceSelectionNeeded,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificEvidencePackStepKind {
    PsychologyConnectorAlignment,
    EvidenceScopeReview,
    LocalEvidenceContextPlan,
    MetadataEvidencePlan,
    SourceFamilyEvidencePlan,
    ClaimCoveragePlan,
    CitationAttributionPlan,
    EvidenceQualityPlan,
    ComplianceBoundaryCheck,
    DownstreamAnswerReadinessAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackLocalContextPlan {
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub requires_local_sources: bool,
    pub planned_local_source_roles: Vec<String>,
    pub planned_local_evidence_requirements: Vec<String>,
    pub will_read_files: bool,
    pub will_index_files: bool,
    pub will_run_retrieval: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackMetadataPlan {
    pub required_metadata_fields: Vec<String>,
    pub required_identifier_fields: Vec<String>,
    pub preferred_metadata_sources: Vec<String>,
    pub citation_metadata_notes: Vec<String>,
    pub require_open_access: bool,
    pub require_doi: bool,
    pub will_call_metadata_connectors: bool,
    pub will_write_metadata: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackSourceFamilyPlan {
    pub selected_families: Vec<String>,
    pub supported_families: Vec<String>,
    pub family_requirements: Vec<String>,
    pub access_boundary_notes: Vec<String>,
    pub will_call_source_connectors: bool,
    pub will_authenticate: bool,
    pub will_bypass_paywall: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackClaimCoveragePlan {
    pub claim_scope: Vec<String>,
    pub planned_claim_kinds: Vec<String>,
    pub unknown_claim_scope_items: Vec<String>,
    pub minimum_evidence_requirements: Vec<String>,
    pub unsupported_claim_handling: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackCitationAttributionPlan {
    pub supported_citation_styles: Vec<String>,
    pub normalized_citation_style: String,
    pub required_citation_fields: Vec<String>,
    pub required_provider_attribution_notes: Vec<String>,
    pub citation_safety_notes: Vec<String>,
    pub will_emit_citations_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackQualityPlan {
    pub quality_signals: Vec<String>,
    pub ranking_inputs_later: Vec<String>,
    pub exclusion_rules: Vec<String>,
    pub review_requirements: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackCompliancePlan {
    pub external_metadata_only: bool,
    pub no_fulltext_download: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub respects_rate_limits_later: bool,
    pub requires_terms_review_later: bool,
    pub requires_institutional_access_review_later: bool,
    pub will_call_external_services: bool,
    pub will_write_metadata: bool,
    pub will_create_evidence_pack: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidenceItemPlan {
    pub item_id: String,
    pub kind: String,
    pub source_channel: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub enabled: bool,
    pub preview_only: bool,
    pub will_read_file: bool,
    pub will_call_connector: bool,
    pub will_write_artifact: bool,
    pub source_ids: Vec<String>,
    pub required_fields: Vec<String>,
    pub quality_requirements: Vec<String>,
    pub citation_requirements: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackStep {
    pub kind: ScholarChatScientificEvidencePackStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackPlanRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub methodology_hints: Option<Vec<String>>,
    pub population_hints: Option<Vec<String>>,
    pub topic_area_hints: Option<Vec<String>>,
    pub evidence_goal: Option<String>,
    pub claim_scope: Option<Vec<String>>,
    pub citation_style: Option<String>,
    pub max_results_per_source: Option<u32>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificEvidencePackPlanPreview {
    pub status: ScholarChatScientificEvidencePackPlanStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub normalized_expected_source_kinds: Vec<String>,
    pub normalized_preferred_metadata_sources: Vec<String>,
    pub normalized_preferred_psychology_source_families: Vec<String>,
    pub normalized_methodology_hints: Vec<String>,
    pub normalized_population_hints: Vec<String>,
    pub normalized_topic_area_hints: Vec<String>,
    pub normalized_evidence_goal: Option<String>,
    pub normalized_claim_scope: Vec<String>,
    pub normalized_citation_style: String,
    pub normalized_max_results_per_source: u32,
    pub require_open_access: bool,
    pub require_doi: bool,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub psychology_source_connector_status: ScholarChatPsychologySourceConnectorPlanStatus,
    pub metadata_connector_plan_status: ScholarChatMetadataConnectorPlanStatus,
    pub scientific_search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub psychology_scope_status: ScholarChatPsychologyScopeStatus,
    pub source_family_strategy: ScholarChatPsychologySourceFamilyStrategy,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub evidence_pack_strategy: ScholarChatScientificEvidencePackStrategy,
    pub unknown_claim_scope: Vec<String>,
    pub local_evidence_context_plan: ScholarChatScientificEvidencePackLocalContextPlan,
    pub metadata_evidence_plan: ScholarChatScientificEvidencePackMetadataPlan,
    pub source_family_evidence_plan: ScholarChatScientificEvidencePackSourceFamilyPlan,
    pub claim_coverage_plan: ScholarChatScientificEvidencePackClaimCoveragePlan,
    pub citation_attribution_plan: ScholarChatScientificEvidencePackCitationAttributionPlan,
    pub evidence_quality_plan: ScholarChatScientificEvidencePackQualityPlan,
    pub evidence_compliance_plan: ScholarChatScientificEvidencePackCompliancePlan,
    pub planned_evidence_items: Vec<ScholarChatScientificEvidenceItemPlan>,
    pub planned_evidence_pack_steps: Vec<ScholarChatScientificEvidencePackStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub scientific_evidence_pack_preview_only: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificPaperLiteratureReviewPlanStatus {
    Blocked,
    LiteratureReviewPlanReady,
    NeedsEvidencePackPlan,
    NeedsResearchQuestion,
    NeedsEvidenceSources,
    NeedsCitationMetadata,
    NeedsDisambiguation,
    UnknownConcept,
    NeedsPsychologyScope,
    NeedsSourceFamily,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificPaperLiteratureReviewStrategy {
    EvidencePackFirst,
    ResearchQuestionFirst,
    CitationMetadataFirst,
    PsychologyScopeFirst,
    SourceFamilyFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificPaperLiteratureReviewStepKind {
    ScientificPaperModeAlignment,
    EvidencePackPlanAlignment,
    ResearchQuestionPlanning,
    SearchStrategyPlanning,
    EvidenceMapPlanning,
    ReviewSectionPlanning,
    ClaimSynthesisPlanning,
    CitationPlanning,
    QualityReviewPlanning,
    ComplianceBoundaryCheck,
    DownstreamPaperGenerationAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperResearchQuestionPlan {
    pub research_question_present: bool,
    pub normalized_research_question: Option<String>,
    pub will_derive_research_question_later: bool,
    pub required_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperSearchStrategyPlan {
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub scientific_search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub planned_search_inputs: Vec<String>,
    pub planned_search_filters: Vec<String>,
    pub planned_source_channels: Vec<String>,
    pub will_run_search: bool,
    pub will_call_connectors: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperEvidencePackDependencyPlan {
    pub evidence_pack_plan_status: ScholarChatScientificEvidencePackPlanStatus,
    pub evidence_pack_strategy: ScholarChatScientificEvidencePackStrategy,
    pub required_evidence_pack_fields: Vec<String>,
    pub required_evidence_item_kinds: Vec<String>,
    pub will_create_evidence_pack: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperReviewSectionPlan {
    pub requested_sections: Vec<String>,
    pub planned_sections: Vec<String>,
    pub unknown_sections: Vec<String>,
    pub section_requirements: Vec<String>,
    pub will_generate_section_text: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperClaimSynthesisPlan {
    pub claim_scope: Vec<String>,
    pub unknown_claim_scope: Vec<String>,
    pub synthesis_units: Vec<String>,
    pub evidence_requirements: Vec<String>,
    pub unsupported_claim_handling: Vec<String>,
    pub will_generate_claims: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperCitationPlan {
    pub citation_style: String,
    pub required_citation_fields: Vec<String>,
    pub attribution_requirements: Vec<String>,
    pub citation_safety_notes: Vec<String>,
    pub will_emit_citations_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperQualityReviewPlan {
    pub quality_signals: Vec<String>,
    pub review_checks: Vec<String>,
    pub exclusion_rules: Vec<String>,
    pub required_manual_checks_later: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperComplianceReviewPlan {
    pub no_fulltext_download: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub external_metadata_only: bool,
    pub will_call_external_services: bool,
    pub will_write_metadata: bool,
    pub will_create_literature_review: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperLiteratureReviewStep {
    pub kind: ScholarChatScientificPaperLiteratureReviewStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperLiteratureReviewPlanRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub methodology_hints: Option<Vec<String>>,
    pub population_hints: Option<Vec<String>>,
    pub topic_area_hints: Option<Vec<String>>,
    pub evidence_goal: Option<String>,
    pub claim_scope: Option<Vec<String>>,
    pub citation_style: Option<String>,
    pub review_goal: Option<String>,
    pub research_question: Option<String>,
    pub review_section_preferences: Option<Vec<String>>,
    pub max_results_per_source: Option<u32>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificPaperLiteratureReviewPlanPreview {
    pub status: ScholarChatScientificPaperLiteratureReviewPlanStatus,
    pub normalized_query: String,
    pub requested_mode: Option<String>,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub normalized_expected_source_kinds: Vec<String>,
    pub normalized_preferred_metadata_sources: Vec<String>,
    pub normalized_preferred_psychology_source_families: Vec<String>,
    pub normalized_methodology_hints: Vec<String>,
    pub normalized_population_hints: Vec<String>,
    pub normalized_topic_area_hints: Vec<String>,
    pub normalized_evidence_goal: Option<String>,
    pub normalized_claim_scope: Vec<String>,
    pub unknown_claim_scope: Vec<String>,
    pub normalized_citation_style: String,
    pub normalized_review_goal: Option<String>,
    pub normalized_research_question: Option<String>,
    pub normalized_review_section_preferences: Vec<String>,
    pub unknown_review_section_preferences: Vec<String>,
    pub normalized_max_results_per_source: u32,
    pub require_open_access: bool,
    pub require_doi: bool,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub evidence_pack_plan_status: ScholarChatScientificEvidencePackPlanStatus,
    pub psychology_source_connector_status: ScholarChatPsychologySourceConnectorPlanStatus,
    pub metadata_connector_plan_status: ScholarChatMetadataConnectorPlanStatus,
    pub scientific_search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub psychology_scope_status: ScholarChatPsychologyScopeStatus,
    pub source_family_strategy: ScholarChatPsychologySourceFamilyStrategy,
    pub evidence_pack_strategy: ScholarChatScientificEvidencePackStrategy,
    pub inferred_topic: Option<String>,
    pub query_intent: ScholarChatScientificQueryIntent,
    pub recognized_concept: Option<String>,
    pub label: Option<String>,
    pub literature_review_strategy: ScholarChatScientificPaperLiteratureReviewStrategy,
    pub research_question_plan: ScholarChatScientificPaperResearchQuestionPlan,
    pub search_strategy_plan: ScholarChatScientificPaperSearchStrategyPlan,
    pub evidence_pack_dependency_plan: ScholarChatScientificPaperEvidencePackDependencyPlan,
    pub review_section_plan: ScholarChatScientificPaperReviewSectionPlan,
    pub claim_synthesis_plan: ScholarChatScientificPaperClaimSynthesisPlan,
    pub citation_plan: ScholarChatScientificPaperCitationPlan,
    pub quality_review_plan: ScholarChatScientificPaperQualityReviewPlan,
    pub compliance_review_plan: ScholarChatScientificPaperComplianceReviewPlan,
    pub planned_literature_review_steps: Vec<ScholarChatScientificPaperLiteratureReviewStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub scientific_paper_literature_review_preview_only: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_literature_review_created: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRetrievalCandidate {
    pub source_id: String,
    pub version_id: String,
    pub chunk_id: String,
    pub score: f32,
    pub matched_terms: Vec<String>,
    pub preview: String,
    pub locator: CitationLocator,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRetrievalPreviewResponse {
    pub status: ScholarChatStatus,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub candidate_count: usize,
    pub candidates: Vec<ScholarChatRetrievalCandidate>,
    pub warnings: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatEvidencePlanStatus {
    EvidencePlanPreview,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatEvidenceCandidate {
    pub source_id: String,
    pub version_id: String,
    pub chunk_id: String,
    pub score: f32,
    pub matched_terms: Vec<String>,
    pub preview: String,
    pub locator: CitationLocator,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatEvidencePlan {
    pub retrieval_candidate_count: usize,
    pub evidence_candidate_count: usize,
    pub evidence_required: bool,
    pub evidence_pack_would_be_built_later: bool,
    pub summary: String,
    pub steps: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatEvidencePlanResponse {
    pub status: ScholarChatEvidencePlanStatus,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub retrieval_candidate_count: usize,
    pub evidence_candidate_count: usize,
    pub evidence_plan: ScholarChatEvidencePlan,
    pub candidates: Vec<ScholarChatEvidenceCandidate>,
    pub warnings: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPromptPackStatus {
    PromptPackPreview,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatPromptPackSectionKind {
    SystemOrPolicyInstructions,
    ModeInstructions,
    GroundingInstructions,
    SourceContext,
    UserPrompt,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPromptPackSection {
    pub kind: ScholarChatPromptPackSectionKind,
    pub title: String,
    pub lines: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatPromptContextItem {
    pub source_id: String,
    pub version_id: String,
    pub chunk_id: String,
    pub score: f32,
    pub matched_terms: Vec<String>,
    pub preview: String,
    pub locator: CitationLocator,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatPromptPack {
    pub section_count: usize,
    pub context_item_count: usize,
    pub estimated_input_char_count: usize,
    pub sections: Vec<ScholarChatPromptPackSection>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatPromptPackPreviewResponse {
    pub status: ScholarChatPromptPackStatus,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub prompt_pack: ScholarChatPromptPack,
    pub context_items: Vec<ScholarChatPromptContextItem>,
    pub warnings: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatAnswerReadinessStatus {
    Blocked,
    NeedsSources,
    NeedsRetrievalIndex,
    NeedsEvidenceCandidates,
    NeedsRuntimeConfig,
    NeedsExecutionConsent,
    ReadyForDraftInferenceLater,
    ReadyForGroundedDraftLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatAnswerReadinessOutputClassification {
    Blocked,
    UngroundedDraft,
    SourceContextDraft,
    GroundedDraftCandidate,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatAnswerReadinessBlocker {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatAnswerReadinessWarning {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatAnswerReadinessRequest {
    pub scholar_chat_request: ScholarChatRequest,
    pub runtime_config: LocalModelRuntimeConfig,
    pub allow_model_execution: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatAnswerReadinessPreview {
    pub status: ScholarChatAnswerReadinessStatus,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_count: usize,
    pub retrieval_candidate_count: usize,
    pub evidence_candidate_count: usize,
    pub prompt_pack_ready: bool,
    pub runtime_health_status: LocalModelRuntimeHealthStatus,
    pub invocation_plan_status: LocalRuntimeInvocationPlanStatus,
    pub allow_model_execution: bool,
    pub would_generate_answer_now: bool,
    pub would_build_evidence_pack_now: bool,
    pub would_create_final_answer_now: bool,
    pub future_output_classification: ScholarChatAnswerReadinessOutputClassification,
    pub blockers: Vec<ScholarChatAnswerReadinessBlocker>,
    pub warnings: Vec<ScholarChatAnswerReadinessWarning>,
    pub next_required_actions: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatDraftInferenceStatus {
    Blocked,
    NeedsSources,
    NeedsEvidence,
    NeedsRuntimeConfig,
    NeedsExecutionConsent,
    InferenceSucceeded,
    InferenceFailed,
    TimedOut,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatDraftOutputClassification {
    Blocked,
    UngroundedModelDraft,
    SourceContextDraft,
    GroundedDraftCandidate,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftInferenceBlocker {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftInferenceWarning {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatDraftInferenceRequest {
    pub scholar_chat_request: ScholarChatRequest,
    pub runtime_config: LocalModelRuntimeConfig,
    pub allow_model_execution: bool,
    pub timeout_ms: Option<u64>,
    pub max_output_tokens: Option<u32>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatDraftInferencePreview {
    pub status: ScholarChatDraftInferenceStatus,
    pub output_classification: ScholarChatDraftOutputClassification,
    pub normalized_prompt: String,
    pub mode: ScholarChatMode,
    pub grounding_policy: GroundingPolicy,
    pub selected_source_count: usize,
    pub retrieval_candidate_count: usize,
    pub evidence_candidate_count: usize,
    pub prompt_pack_section_count: usize,
    pub prompt_char_count: usize,
    pub runtime_health_status: LocalModelRuntimeHealthStatus,
    pub invocation_plan_status: LocalRuntimeInvocationPlanStatus,
    pub allow_model_execution: bool,
    pub execution_attempted: bool,
    pub safe_model_file_name: Option<String>,
    pub safe_executable_file_name: Option<String>,
    pub stdout_preview: String,
    pub stderr_preview: String,
    pub duration_ms: u64,
    pub exit_code: Option<i32>,
    pub draft_only: bool,
    pub preview_only: bool,
    pub not_final_answer: bool,
    pub not_grounded_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub blockers: Vec<ScholarChatDraftInferenceBlocker>,
    pub warnings: Vec<ScholarChatDraftInferenceWarning>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatDraftGroundingInspectionStatus {
    Blocked,
    NoDraftText,
    NoEvidenceCandidates,
    Inspected,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatDraftGroundingSupportStatus {
    NotInspected,
    Unsupported,
    WeaklySupported,
    SupportedByLocalEvidence,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftGroundingInspectionBlocker {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftGroundingInspectionWarning {
    pub kind: String,
    pub message: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftGroundingInspectionRequest {
    pub scholar_chat_request: ScholarChatRequest,
    pub draft_text: Option<String>,
    pub max_items: Option<usize>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildIntentRequest {
    pub grounding_request: ScholarChatDraftGroundingInspectionRequest,
    pub answer_draft_id: Option<String>,
    pub explicit_user_intent: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftGroundingInspectionItem {
    pub item_index: usize,
    pub text_preview: String,
    pub support_status: ScholarChatDraftGroundingSupportStatus,
    pub matched_evidence_count: usize,
    pub source_ids: Vec<String>,
    pub locator_previews: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatDraftGroundingInspectionPreview {
    pub status: ScholarChatDraftGroundingInspectionStatus,
    pub normalized_prompt: String,
    pub draft_char_count: usize,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub unsupported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub supported_item_count: usize,
    pub items: Vec<ScholarChatDraftGroundingInspectionItem>,
    pub inspection_only: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_evidence_pack_built: bool,
    pub no_answer_artifact_created: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedDraftReadinessStatus {
    Blocked,
    NeedsReview,
    ReadyForGroundedDraftLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedDraftReadinessPreview {
    pub status: ScholarChatGroundedDraftReadinessStatus,
    pub inspection_status: ScholarChatDraftGroundingInspectionStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub summary: String,
    pub preview_only: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerBuildPlanStatus {
    Blocked,
    NeedsReview,
    PlanReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildPlanPreview {
    pub status: ScholarChatGroundedAnswerBuildPlanStatus,
    pub readiness_status: ScholarChatGroundedDraftReadinessStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub summary: String,
    pub planned_steps: Vec<String>,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerCandidateStatus {
    Blocked,
    NeedsReview,
    CandidateReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerCandidateItem {
    pub item_index: usize,
    pub statement_preview: String,
    pub support_status: ScholarChatDraftGroundingSupportStatus,
    pub source_ids: Vec<String>,
    pub locator_previews: Vec<String>,
    pub matched_evidence_count: usize,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerCandidatePreview {
    pub status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub summary: String,
    pub candidate_items: Vec<ScholarChatGroundedAnswerCandidateItem>,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerWriteEligibilityStatus {
    Blocked,
    NeedsReview,
    WriteEligibleLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerWriteEligibilityPreview {
    pub status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub eligibility_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerBuildIntentStatus {
    Blocked,
    NeedsReview,
    IntentReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildIntentPreview {
    pub status: ScholarChatGroundedAnswerBuildIntentStatus,
    pub write_eligibility_status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub required_inputs: Vec<String>,
    pub missing_inputs: Vec<String>,
    pub intent_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
    pub no_grounded_answer_service_call: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerBuildRequestStatus {
    Blocked,
    NeedsReview,
    RequestReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildRequestPreviewRequest {
    pub build_intent_request: ScholarChatGroundedAnswerBuildIntentRequest,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildRequestPreview {
    pub status: ScholarChatGroundedAnswerBuildRequestStatus,
    pub build_intent_status: ScholarChatGroundedAnswerBuildIntentStatus,
    pub write_eligibility_status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub answer_draft_id: Option<String>,
    pub selected_source_ids: Vec<String>,
    pub required_inputs: Vec<String>,
    pub missing_inputs: Vec<String>,
    pub request_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
    pub no_grounded_answer_service_call: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerBuildPreflightStatus {
    Blocked,
    NeedsReview,
    PreflightReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildPreflightPreviewRequest {
    pub build_request_preview_request: ScholarChatGroundedAnswerBuildRequestPreviewRequest,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerBuildPreflightPreview {
    pub status: ScholarChatGroundedAnswerBuildPreflightStatus,
    pub build_request_status: ScholarChatGroundedAnswerBuildRequestStatus,
    pub build_intent_status: ScholarChatGroundedAnswerBuildIntentStatus,
    pub write_eligibility_status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub answer_draft_id: Option<String>,
    pub selected_source_ids: Vec<String>,
    pub answer_draft_present: bool,
    pub answer_draft_readable: bool,
    pub answer_draft_claim_count: usize,
    pub required_inputs: Vec<String>,
    pub missing_inputs: Vec<String>,
    pub preflight_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
    pub no_grounded_answer_service_call: bool,
    pub no_grounded_answer_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerExecutionReadinessStatus {
    Blocked,
    NeedsReview,
    ExecutionReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerExecutionReadinessPreviewRequest {
    pub build_preflight_preview_request: ScholarChatGroundedAnswerBuildPreflightPreviewRequest,
    pub execution_consent: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerExecutionReadinessPreview {
    pub status: ScholarChatGroundedAnswerExecutionReadinessStatus,
    pub build_preflight_status: ScholarChatGroundedAnswerBuildPreflightStatus,
    pub build_request_status: ScholarChatGroundedAnswerBuildRequestStatus,
    pub build_intent_status: ScholarChatGroundedAnswerBuildIntentStatus,
    pub write_eligibility_status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub answer_draft_id: Option<String>,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub answer_draft_present: bool,
    pub answer_draft_readable: bool,
    pub answer_draft_claim_count: usize,
    pub execution_consent: bool,
    pub required_inputs: Vec<String>,
    pub missing_inputs: Vec<String>,
    pub readiness_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
    pub no_grounded_answer_service_call: bool,
    pub no_grounded_answer_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatGroundedAnswerExecutionPlanStatus {
    Blocked,
    NeedsReview,
    PlanReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerExecutionPlanPreviewRequest {
    pub execution_readiness_preview_request: ScholarChatGroundedAnswerExecutionReadinessPreviewRequest,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatGroundedAnswerExecutionPlanPreview {
    pub status: ScholarChatGroundedAnswerExecutionPlanStatus,
    pub readiness_status: ScholarChatGroundedAnswerExecutionReadinessStatus,
    pub build_preflight_status: ScholarChatGroundedAnswerBuildPreflightStatus,
    pub build_request_status: ScholarChatGroundedAnswerBuildRequestStatus,
    pub build_intent_status: ScholarChatGroundedAnswerBuildIntentStatus,
    pub write_eligibility_status: ScholarChatGroundedAnswerWriteEligibilityStatus,
    pub candidate_status: ScholarChatGroundedAnswerCandidateStatus,
    pub normalized_prompt: String,
    pub answer_draft_id: Option<String>,
    pub selected_source_ids: Vec<String>,
    pub selected_source_count: usize,
    pub evidence_candidate_count: usize,
    pub inspected_item_count: usize,
    pub supported_item_count: usize,
    pub weakly_supported_item_count: usize,
    pub unsupported_item_count: usize,
    pub candidate_statement_count: usize,
    pub answer_draft_present: bool,
    pub answer_draft_readable: bool,
    pub answer_draft_claim_count: usize,
    pub execution_consent: bool,
    pub planned_operation: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub planned_write_targets: Vec<String>,
    pub required_inputs: Vec<String>,
    pub missing_inputs: Vec<String>,
    pub plan_reasons: Vec<String>,
    pub blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    pub warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub not_answer_draft: bool,
    pub not_grounded_answer: bool,
    pub not_final_answer: bool,
    pub no_answer_artifact_created: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_llm_call: bool,
    pub no_runtime_execution: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
    pub no_grounded_answer_service_call: bool,
    pub no_grounded_answer_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatRuntimeDiagnosticBridgeStatus {
    Blocked,
    NeedsReview,
    RuntimeDiagnosticReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeDiagnosticBridgePreviewRequest {
    pub scholar_chat_request: ScholarChatRequest,
    pub smoke_execution_plan_preview_request: LocalRuntimeSmokeExecutionPlanPreviewRequest,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeDiagnosticBridgePreview {
    pub status: ScholarChatRuntimeDiagnosticBridgeStatus,
    pub normalized_prompt: String,
    pub selected_source_count: usize,
    pub smoke_execution_plan_status: LocalRuntimeSmokeExecutionPlanStatus,
    pub smoke_readiness_status: LocalRuntimeSmokeReadinessStatus,
    pub capability_status: LocalRuntimeCapabilityStatus,
    pub version_probe_status: LocalRuntimeVersionProbeStatus,
    pub probe_readiness_status: LocalRuntimeProbeReadinessStatus,
    pub validation_status: LocalRuntimeValidationStatus,
    pub adapter_contract_status: LocalRuntimeAdapterContractStatus,
    pub adapter_kind: LocalRuntimeAdapterKind,
    pub normalized_model_family: Option<String>,
    pub normalized_model_format: String,
    pub safe_executable_file_name: Option<String>,
    pub safe_model_file_name: Option<String>,
    pub diagnostic_prompt_char_count: usize,
    pub max_output_tokens: u32,
    pub timeout_ms: u64,
    pub runtime_diagnostic_reasons: Vec<String>,
    pub blockers: Vec<LocalRuntimeProbeWarning>,
    pub warnings: Vec<LocalRuntimeProbeWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub no_smoke_execution: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_answer_draft_created: bool,
    pub no_grounded_answer_created: bool,
    pub no_final_answer_created: bool,
    pub no_grounding_applied: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_artifact_write: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatRuntimeDiagnosticResultStatus {
    Blocked,
    NeedsReview,
    RuntimeDiagnosticFailed,
    RuntimeDiagnosticSucceededLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeDiagnosticResultPreviewRequest {
    pub bridge_preview_request: ScholarChatRuntimeDiagnosticBridgePreviewRequest,
    pub diagnostic_preview: LocalRuntimeSmokeDiagnosticPreview,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeDiagnosticResultPreview {
    pub status: ScholarChatRuntimeDiagnosticResultStatus,
    pub bridge_status: ScholarChatRuntimeDiagnosticBridgeStatus,
    pub smoke_diagnostic_status: LocalRuntimeSmokeDiagnosticStatus,
    pub smoke_execution_plan_status: LocalRuntimeSmokeExecutionPlanStatus,
    pub smoke_readiness_status: LocalRuntimeSmokeReadinessStatus,
    pub capability_status: LocalRuntimeCapabilityStatus,
    pub version_probe_status: LocalRuntimeVersionProbeStatus,
    pub probe_readiness_status: LocalRuntimeProbeReadinessStatus,
    pub validation_status: LocalRuntimeValidationStatus,
    pub adapter_contract_status: LocalRuntimeAdapterContractStatus,
    pub adapter_kind: LocalRuntimeAdapterKind,
    pub normalized_model_family: Option<String>,
    pub normalized_model_format: String,
    pub safe_executable_file_name: Option<String>,
    pub safe_model_file_name: Option<String>,
    pub diagnostic_prompt_char_count: usize,
    pub max_output_tokens: u32,
    pub timeout_ms: u64,
    pub exit_code: Option<i32>,
    pub stdout_preview: String,
    pub stderr_preview: String,
    pub stdout_truncated: bool,
    pub stderr_truncated: bool,
    pub runtime_result_reasons: Vec<String>,
    pub blockers: Vec<LocalRuntimeProbeWarning>,
    pub warnings: Vec<LocalRuntimeProbeWarning>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub diagnostic_result_only: bool,
    pub no_smoke_execution: bool,
    pub no_runtime_inference: bool,
    pub no_new_process_spawn: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_answer_draft_created: bool,
    pub no_grounded_answer_created: bool,
    pub no_final_answer_created: bool,
    pub no_grounding_applied: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_artifact_write: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatRuntimeAnswerPipelineGateStatus {
    Blocked,
    NeedsReview,
    ReadyLater,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeAnswerPipelineGatePreviewRequest {
    pub grounded_answer_execution_plan_preview_request: ScholarChatGroundedAnswerExecutionPlanPreviewRequest,
    pub runtime_diagnostic_result_preview_request: ScholarChatRuntimeDiagnosticResultPreviewRequest,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatRuntimeAnswerPipelineGatePreview {
    pub status: ScholarChatRuntimeAnswerPipelineGateStatus,
    pub grounded_answer_execution_plan_status: ScholarChatGroundedAnswerExecutionPlanStatus,
    pub grounded_answer_execution_readiness_status: ScholarChatGroundedAnswerExecutionReadinessStatus,
    pub runtime_diagnostic_result_status: ScholarChatRuntimeDiagnosticResultStatus,
    pub runtime_diagnostic_bridge_status: ScholarChatRuntimeDiagnosticBridgeStatus,
    pub smoke_diagnostic_status: LocalRuntimeSmokeDiagnosticStatus,
    pub smoke_execution_plan_status: LocalRuntimeSmokeExecutionPlanStatus,
    pub smoke_readiness_status: LocalRuntimeSmokeReadinessStatus,
    pub capability_status: LocalRuntimeCapabilityStatus,
    pub version_probe_status: LocalRuntimeVersionProbeStatus,
    pub probe_readiness_status: LocalRuntimeProbeReadinessStatus,
    pub validation_status: LocalRuntimeValidationStatus,
    pub adapter_contract_status: LocalRuntimeAdapterContractStatus,
    pub adapter_kind: LocalRuntimeAdapterKind,
    pub selected_source_count: usize,
    pub normalized_model_family: Option<String>,
    pub normalized_model_format: String,
    pub safe_executable_file_name: Option<String>,
    pub safe_model_file_name: Option<String>,
    pub diagnostic_prompt_char_count: usize,
    pub max_output_tokens: u32,
    pub timeout_ms: u64,
    pub pipeline_gate_reasons: Vec<String>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub gate_only: bool,
    pub no_smoke_execution: bool,
    pub no_runtime_inference: bool,
    pub no_new_process_spawn: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_answer_draft_created: bool,
    pub no_grounded_answer_created: bool,
    pub no_final_answer_created: bool,
    pub no_grounding_applied: bool,
    pub no_evidence_pack_built: bool,
    pub no_persistence: bool,
    pub no_artifact_write: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

enum ScholarChatPreviewKind {
    Request,
    Retrieval,
}

pub fn preview_scholar_chat_request(
    _root: impl Into<PathBuf>,
    request: ScholarChatRequest,
) -> AegisResult<ScholarChatResponse> {
    let normalized_prompt = normalized_prompt_or_err(request.prompt)?;
    let (selected_source_ids, selected_source_count) = normalize_selected_source_ids(request.selected_source_ids)?;
    let warnings = preview_warnings(&request.grounding_policy, selected_source_count, ScholarChatPreviewKind::Request);
    let grounding_plan = grounding_plan(&request.mode, &request.grounding_policy, selected_source_count);

    Ok(ScholarChatResponse {
        status: ScholarChatStatus::PreviewOnly,
        normalized_prompt,
        mode: request.mode,
        grounding_policy: request.grounding_policy,
        selected_source_ids,
        selected_source_count,
        grounding_plan,
        warnings,
    })
}

pub fn preview_scholar_chat_retrieval(
    root: impl Into<PathBuf>,
    request: ScholarChatRequest,
) -> AegisResult<ScholarChatRetrievalPreviewResponse> {
    let normalized_prompt = normalized_prompt_or_err(request.prompt)?;
    let (selected_source_ids, selected_source_count) = normalize_selected_source_ids(request.selected_source_ids)?;
    let mut warnings = preview_warnings(&request.grounding_policy, selected_source_count, ScholarChatPreviewKind::Retrieval);

    let retrieval_service = RetrievalService::new(root);
    let mut candidates = Vec::new();

    if selected_source_count > 0 {
        for source_id in &selected_source_ids {
            match retrieval_service.preview_search_source(source_id, &normalized_prompt, SCHOLAR_CHAT_RETRIEVAL_PREVIEW_LIMIT) {
                Ok(response) => {
                    if response.results.is_empty() {
                        warnings.push(format!("No retrieval candidates matched selected source {source_id}."));
                    }
                    candidates.extend(convert_retrieval_response(response));
                }
                Err(AegisError::RetrievalIndexMissing)
                | Err(AegisError::RetrievalIndexReadFailed)
                | Err(AegisError::ChunkingReportMissing)
                | Err(AegisError::ChunkingReportReadFailed) => {
                    warnings.push(format!("Retrieval data is not ready for selected source {source_id}; skipping this source."));
                }
                Err(error) => return Err(error),
            }
        }
    }

    candidates.sort_by(|left, right| {
        right
            .score
            .partial_cmp(&left.score)
            .unwrap_or(std::cmp::Ordering::Equal)
            .then_with(|| left.source_id.cmp(&right.source_id))
            .then_with(|| left.chunk_id.cmp(&right.chunk_id))
    });

    if selected_source_count > 0 && candidates.is_empty() && !warnings.iter().any(|warning| warning.contains("Retrieval data is not ready")) {
        warnings.push("No retrieval candidates matched the selected sources.".to_string());
    }

    Ok(ScholarChatRetrievalPreviewResponse {
        status: ScholarChatStatus::PreviewOnly,
        normalized_prompt,
        mode: request.mode,
        grounding_policy: request.grounding_policy,
        selected_source_ids,
        selected_source_count,
        candidate_count: candidates.len(),
        candidates,
        warnings,
    })
}

pub fn preview_scholar_chat_evidence_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatRequest,
) -> AegisResult<ScholarChatEvidencePlanResponse> {
    let retrieval_preview = preview_scholar_chat_retrieval(root, request)?;
    let mut warnings = retrieval_preview.warnings.clone();
    if retrieval_preview.selected_source_count == 0 && !warnings.iter().any(|warning| warning.contains("No selected sources")) {
        warnings.push("No selected sources; evidence plan preview is unscoped.".to_string());
    }
    if retrieval_preview.candidate_count == 0 {
        warnings.push("No retrieval candidates were eligible for Evidence Pack assembly yet.".to_string());
    }
    warnings.push("This is a preview only; no Evidence Pack was built.".to_string());

    let evidence_candidates = convert_retrieval_candidates_from_preview(&retrieval_preview.candidates);
    let evidence_candidate_count = evidence_candidates.len();
    let evidence_plan = evidence_plan(
        &retrieval_preview.mode,
        &retrieval_preview.grounding_policy,
        retrieval_preview.selected_source_count,
        retrieval_preview.candidate_count,
        evidence_candidate_count,
    );

    Ok(ScholarChatEvidencePlanResponse {
        status: ScholarChatEvidencePlanStatus::EvidencePlanPreview,
        normalized_prompt: retrieval_preview.normalized_prompt,
        mode: retrieval_preview.mode,
        grounding_policy: retrieval_preview.grounding_policy,
        selected_source_ids: retrieval_preview.selected_source_ids,
        selected_source_count: retrieval_preview.selected_source_count,
        retrieval_candidate_count: retrieval_preview.candidate_count,
        evidence_candidate_count,
        evidence_plan,
        candidates: evidence_candidates,
        warnings,
    })
}

pub fn preview_scholar_chat_prompt_pack(
    root: impl Into<PathBuf>,
    request: ScholarChatRequest,
) -> AegisResult<ScholarChatPromptPackPreviewResponse> {
    let evidence_plan = preview_scholar_chat_evidence_plan(root, request)?;
    let context_items = convert_evidence_candidates_to_prompt_context_items(&evidence_plan.candidates);
    let prompt_pack = build_prompt_pack(
        &evidence_plan.mode,
        &evidence_plan.grounding_policy,
        &evidence_plan.normalized_prompt,
        &evidence_plan.selected_source_ids,
        &context_items,
        evidence_plan.evidence_candidate_count,
    );
    let mut warnings = evidence_plan.warnings.clone();
    if evidence_plan.selected_source_count == 0 {
        push_warning(&mut warnings, "No selected sources; prompt pack preview is unscoped.");
    }
    if evidence_plan.evidence_candidate_count == 0 {
        push_warning(&mut warnings, "No evidence candidates were eligible for prompt-pack assembly yet.");
    }
    push_warning(&mut warnings, "This is a prompt pack preview only; no model inference was run.");
    if matches!(evidence_plan.grounding_policy, GroundingPolicy::LocalOnly) {
        push_warning(&mut warnings, "local_only requires local evidence before a prompt pack can be turned into an answer.");
    }
    if matches!(evidence_plan.grounding_policy, GroundingPolicy::AllowMarkedModelKnowledge) {
        push_warning(&mut warnings, "Model knowledge would need to be clearly marked later.");
    }

    Ok(ScholarChatPromptPackPreviewResponse {
        status: ScholarChatPromptPackStatus::PromptPackPreview,
        normalized_prompt: evidence_plan.normalized_prompt,
        mode: evidence_plan.mode,
        grounding_policy: evidence_plan.grounding_policy,
        selected_source_ids: evidence_plan.selected_source_ids,
        selected_source_count: evidence_plan.selected_source_count,
        evidence_candidate_count: evidence_plan.evidence_candidate_count,
        prompt_pack,
        context_items,
        warnings,
    })
}

pub fn preview_scholar_chat_scientific_discipline_registry(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificDisciplineRegistryPreviewRequest,
) -> AegisResult<ScholarChatScientificDisciplineRegistryPreview> {
    let _root = root.into();
    let normalized_topic = normalize_scientific_topic_text(&request.topic);
    let normalized_mode = normalize_scientific_mode(request.mode);
    let topic_key = normalize_scientific_topic_key(&normalized_topic);
    let mapped_entry = scientific_discipline_registry_entry(&topic_key);

    let status = if normalized_topic.is_empty() {
        ScholarChatScientificDisciplineRegistryStatus::Blocked
    } else if mapped_entry.is_some() {
        ScholarChatScientificDisciplineRegistryStatus::ConceptMapped
    } else {
        ScholarChatScientificDisciplineRegistryStatus::UnknownConcept
    };

    let mut blockers = Vec::new();
    let mut warnings = vec![
        "This is a scientific discipline registry preview only; no web requests, scraping, connectors, or local indexing were run.".to_string(),
    ];
    let mut next_required_actions = Vec::new();

    match normalized_mode.as_str() {
        "scientific_paper" => {
            push_unique_text(
                &mut warnings,
                "Scientific Paper Mode will later prioritize research question decomposition, literature search, deduplication, and no fabricated citations.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Future Scientific Paper Mode phases should prioritize research question decomposition, literature search, deduplication, and citation-safe planning.",
            );
        }
        "course" => {
            push_unique_text(
                &mut warnings,
                "Course Mode will later prioritize local course materials, module context, prerequisites, and learning path support.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Future Course Mode phases should prioritize local course materials, module context, prerequisites, and learning path support.",
            );
        }
        _ => {
            push_unique_text(
                &mut next_required_actions,
                "Plan local evidence lookup first before later Scholar Chat answering.",
            );
        }
    }

    if normalized_topic.is_empty() {
        blockers.push("topic_missing: Provide a scientific topic to preview.".to_string());
        next_required_actions.push("Provide a scientific topic to preview the discipline registry.".to_string());
    }

    if normalized_topic.is_empty() {
        // handled below
    } else if mapped_entry.is_none() {
        push_unique_text(
            &mut warnings,
            "The topic is not yet in the local preview registry.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Add a discipline registry mapping in a later phase.",
        );
    }

    if let Some(entry) = &mapped_entry {
        if request.course_context.as_deref().map(str::trim).filter(|value| !value.is_empty()).is_some()
            && matches!(normalized_mode.as_str(), "course")
        {
            push_unique_text(
                &mut warnings,
                "Course context is preview-only and will later be mapped to curriculum metadata without local file indexing.",
            );
        }

        let summary = format!(
            "Scientific discipline preview mapped {} in {} mode.",
            entry.label, normalized_mode
        );

        return Ok(ScholarChatScientificDisciplineRegistryPreview {
            status,
            normalized_topic,
            normalized_mode,
            recognized_concept: Some(entry.recognized_concept.to_string()),
            label: Some(entry.label.to_string()),
            discipline_path: entry
                .discipline_path
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            science_class: Some(entry.science_class.clone()),
            parent_path: entry
                .parent_path
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            related_methods: entry
                .related_methods
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            appears_in: entry
                .appears_in
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            preferred_sources: entry
                .preferred_sources
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            curriculum_sources: entry
                .curriculum_sources
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            canonical_mappings: entry
                .canonical_mappings
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            planned_queries: entry
                .planned_queries
                .iter()
                .map(|value| (*value).to_string())
                .collect(),
            blockers,
            warnings,
            next_required_actions,
            summary,
            preview_only: true,
            registry_preview_only: true,
            no_web_request: true,
            no_scraping: true,
            no_connector_call: true,
            no_source_import: true,
            no_local_file_indexing: true,
            no_bm25_index: true,
            no_vector_index: true,
            no_model_loading: true,
            no_runtime_inference: true,
            no_llm_call: true,
            no_answer_generated: true,
            no_evidence_pack_created: true,
            no_artifact_write: true,
            no_persistence: true,
            no_registry_status_change: true,
            no_audit_write: true,
        });
    }

    if !normalized_topic.is_empty() {
        push_unique_text(
            &mut warnings,
            "The topic is not yet in the local preview registry.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Add a discipline registry mapping in a later phase.",
        );
    }

    let mut planned_queries = Vec::new();
    if !normalized_topic.is_empty() {
        planned_queries.push(normalized_topic.clone());
    }

    let summary = if normalized_topic.is_empty() {
        "Scientific discipline preview blocked because the topic is blank.".to_string()
    } else {
        format!(
            "Scientific discipline preview did not yet recognize the topic '{}'.",
            normalized_topic
        )
    };

    Ok(ScholarChatScientificDisciplineRegistryPreview {
        status,
        normalized_topic,
        normalized_mode,
        recognized_concept: None,
        label: None,
        discipline_path: Vec::new(),
        science_class: None,
        parent_path: Vec::new(),
        related_methods: Vec::new(),
        appears_in: Vec::new(),
        preferred_sources: Vec::new(),
        curriculum_sources: Vec::new(),
        canonical_mappings: Vec::new(),
        planned_queries,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        registry_preview_only: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_bm25_index: true,
        no_vector_index: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_scientific_source_registry(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificSourceRegistryPreviewRequest,
) -> AegisResult<ScholarChatScientificSourceRegistryPreview> {
    let root = root.into();
    let normalized_topic = normalize_scientific_topic_text(&request.topic);
    let normalized_mode = normalize_scientific_mode(request.mode);
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags);
    let discipline_preview = preview_scholar_chat_scientific_discipline_registry(
        &root,
        ScholarChatScientificDisciplineRegistryPreviewRequest {
            topic: normalized_topic.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
        },
    )?;

    let discipline_status = discipline_preview.status.clone();
    let recognized_concept = discipline_preview.recognized_concept.clone();
    let label = discipline_preview.label.clone();
    let status = match discipline_status {
        ScholarChatScientificDisciplineRegistryStatus::Blocked => ScholarChatScientificSourceRegistryStatus::Blocked,
        ScholarChatScientificDisciplineRegistryStatus::ConceptMapped => {
            ScholarChatScientificSourceRegistryStatus::SourcePlanReady
        }
        ScholarChatScientificDisciplineRegistryStatus::UnknownConcept => {
            ScholarChatScientificSourceRegistryStatus::UnknownConcept
        }
    };

    let mut source_families = Vec::new();
    let mut preferred_source_ids = Vec::new();
    let mut conditional_source_ids = Vec::new();
    let mut excluded_source_ids = Vec::new();
    let mut blockers = discipline_preview.blockers.clone();
    let mut warnings = discipline_preview.warnings.clone();
    let mut next_required_actions = discipline_preview.next_required_actions.clone();

    if normalized_topic.is_empty() {
        blockers.push("topic_missing: Provide a scientific topic to preview.".to_string());
        next_required_actions.push("Provide a scientific topic to preview the source registry.".to_string());
    } else if matches!(normalized_mode.as_str(), "scholar_chat") {
        push_unique_text(
            &mut next_required_actions,
            "Later Scholar Chat should plan local evidence first before answering.",
        );
    }

    if normalized_topic.is_empty() {
        let summary = scientific_source_registry_plan_summary(&status, label.as_deref(), &normalized_mode);
        let source_plan = ScholarChatScientificSourceRegistrySourcePlan {
            source_family_count: 0,
            active_source_family_count: 0,
            conditional_source_family_count: 0,
            planned_metadata_query_count: 0,
            summary: summary.clone(),
            steps: vec![
                "Normalize topic, mode, and context tags.".to_string(),
                "Ask the user to provide a scientific topic.".to_string(),
            ],
        };

        return Ok(ScholarChatScientificSourceRegistryPreview {
            status,
            normalized_topic,
            normalized_mode,
            normalized_context_tags,
            discipline_status,
            recognized_concept: None,
            label: None,
            source_plan,
            source_families,
            preferred_source_ids,
            conditional_source_ids,
            excluded_source_ids,
            access_classes: Vec::new(),
            planned_metadata_queries: discipline_preview.planned_queries,
            ranking_hints: Vec::new(),
            deduplication_hints: Vec::new(),
            blockers,
            warnings,
            next_required_actions,
            summary,
            preview_only: true,
            source_registry_preview_only: true,
            no_web_request: true,
            no_scraping: true,
            no_connector_call: true,
            no_source_import: true,
            no_local_file_indexing: true,
            no_bm25_index: true,
            no_vector_index: true,
            no_model_loading: true,
            no_runtime_inference: true,
            no_llm_call: true,
            no_answer_generated: true,
            no_evidence_pack_created: true,
            no_artifact_write: true,
            no_persistence: true,
            no_registry_status_change: true,
            no_audit_write: true,
        });
    }

    let course_context_mentions_psychology = scientific_context_mentions(&request.course_context, &["psychology", "psychologie"]);
    let biomedical_context = scientific_context_tags_contain(
        &normalized_context_tags,
        &["biomedical", "medical", "neuroscience", "clinical", "diagnostics", "medicine"],
    ) || scientific_context_mentions(&request.course_context, &["biomedical", "medical", "neuroscience", "clinical", "diagnostics", "medicine"]);
    let psychology_context = scientific_context_tags_contain(&normalized_context_tags, &["psychology", "psychologie"]) || course_context_mentions_psychology;
    let theory_context = scientific_context_tags_contain(
        &normalized_context_tags,
        &["theory", "mathematics", "math", "statistics", "statistical_theory"],
    ) || scientific_context_mentions(&request.course_context, &["theory", "mathematics", "math", "statistics", "statistical_theory"]);

    let (planned_metadata_queries, _ranking_hints, _deduplication_hints) = match recognized_concept.as_deref() {
        Some("signal_detection_theory") => {
            let pubmed_active = biomedical_context;
            source_families = vec![
                scientific_source_registry_family(
                    "pubpsych",
                    "PubPsych",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active for signal detection theory psychology metadata planning.",
                    true,
                    "psychology literature metadata planning",
                    &["psychology metadata", "literature planning"],
                    &["preview-only", "no scraping", "no connector call"],
                ),
                scientific_source_registry_family(
                    "psycharchives",
                    "PsychArchives",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active for signal detection theory psychology repository planning.",
                    true,
                    "psychology repository and source-family planning",
                    &["repository planning", "psychology metadata"],
                    &["preview-only", "no scraping", "no connector call"],
                ),
                scientific_source_registry_family(
                    "openalex",
                    "OpenAlex",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Supporting,
                    "active as multidisciplinary source-family metadata planning.",
                    true,
                    "broad multidisciplinary metadata planning",
                    &["multidisciplinary metadata", "cross-domain mapping"],
                    &["preview-only", "no web requests"],
                ),
                scientific_source_registry_family(
                    "crossref",
                    "Crossref",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Supporting,
                    "active as citation and DOI metadata planning.",
                    true,
                    "citation and DOI metadata planning",
                    &["doi metadata", "citation metadata"],
                    &["preview-only", "no web requests"],
                ),
                scientific_source_registry_family(
                    "pubmed_if_biomedical_context",
                    "PubMed if biomedical context",
                    "medicine_biomedical_science",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active only when biomedical, medical, neuroscience, clinical, diagnostics, or medicine context is explicit.",
                    pubmed_active,
                    "biomedical metadata planning when context is explicit",
                    &["biomedical context", "clinical metadata"],
                    &["preview-only", "conditional on explicit biomedical context"],
                ),
            ];
            preferred_source_ids = vec![
                "pubpsych".to_string(),
                "psycharchives".to_string(),
                "openalex".to_string(),
                "crossref".to_string(),
            ];
            conditional_source_ids = vec!["pubmed_if_biomedical_context".to_string()];
            if !pubmed_active {
                excluded_source_ids.push("pubmed_if_biomedical_context".to_string());
            } else {
                push_unique_text(
                    &mut warnings,
                    "PubMed is active because biomedical, medical, neuroscience, clinical, diagnostics, or medicine context is explicit.",
                );
            }
            let planned_metadata_queries = vec![
                "Signalentdeckungstheorie".to_string(),
                "signal detection theory".to_string(),
                "psychophysics signal detection".to_string(),
                "d prime criterion ROC".to_string(),
            ];
            (
                planned_metadata_queries.clone(),
                vec![
                    "Prefer psychology and psychophysics sources first.".to_string(),
                    "Use biomedical sources only when biomedical context is explicit.".to_string(),
                    "Prefer method and review sources later where available.".to_string(),
                ],
                vec![
                    "Deduplicate later by DOI, title, and source identifiers.".to_string(),
                    "Keep psychophysics records distinct from broader psychology metadata until later consolidation.".to_string(),
                ],
            )
        }
        Some("analysis_of_variance") => {
            let psychology_context_active = psychology_context;
            let theory_context_active = theory_context;
            source_families = vec![
                scientific_source_registry_family(
                    "openalex",
                    "OpenAlex",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active as a broad statistics and methods metadata source.",
                    true,
                    "broad statistics and methods metadata planning",
                    &["methods metadata", "broad literature planning"],
                    &["preview-only", "no scraping", "no connector call"],
                ),
                scientific_source_registry_family(
                    "crossref",
                    "Crossref",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active as a citation and DOI metadata source.",
                    true,
                    "citation and DOI metadata planning",
                    &["doi metadata", "citation metadata"],
                    &["preview-only", "no web requests"],
                ),
                scientific_source_registry_family(
                    "pubpsych_if_psychology_context",
                    "PubPsych if psychology context",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when psychology or psychologies context is explicit.",
                    psychology_context_active,
                    "psychology methods planning when psychology context is explicit",
                    &["psychology context", "methods planning"],
                    &["preview-only", "conditional on explicit psychology context"],
                ),
                scientific_source_registry_family(
                    "psycharchives_if_psychology_context",
                    "PsychArchives if psychology context",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when psychology or psychologies context is explicit.",
                    psychology_context_active,
                    "psychology repository planning when psychology context is explicit",
                    &["psychology context", "repository planning"],
                    &["preview-only", "conditional on explicit psychology context"],
                ),
                scientific_source_registry_family(
                    "zbmath_if_theory_context",
                    "zbMATH if theory context",
                    "mathematics_statistics",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when theory, mathematics, math, statistics, or statistical_theory context is explicit.",
                    theory_context_active,
                    "mathematics and statistics theory planning when theory context is explicit",
                    &["theory context", "mathematics statistics"],
                    &["preview-only", "conditional on explicit theory or statistics context"],
                ),
                scientific_source_registry_family(
                    "arxiv_if_theory_context",
                    "arXiv if theory context",
                    "preprint",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when theory, mathematics, math, statistics, or statistical_theory context is explicit.",
                    theory_context_active,
                    "preprint planning when theory context is explicit",
                    &["theory context", "preprint planning"],
                    &["preview-only", "conditional on explicit theory or statistics context"],
                ),
            ];
            preferred_source_ids = vec!["openalex".to_string(), "crossref".to_string()];
            conditional_source_ids = vec![
                "pubpsych_if_psychology_context".to_string(),
                "psycharchives_if_psychology_context".to_string(),
                "zbmath_if_theory_context".to_string(),
                "arxiv_if_theory_context".to_string(),
            ];
            if !psychology_context_active {
                excluded_source_ids.extend([
                    "pubpsych_if_psychology_context".to_string(),
                    "psycharchives_if_psychology_context".to_string(),
                ]);
            }
            if !theory_context_active {
                excluded_source_ids.extend([
                    "zbmath_if_theory_context".to_string(),
                    "arxiv_if_theory_context".to_string(),
                ]);
            }
            let planned_metadata_queries = vec![
                "ANOVA".to_string(),
                "Varianzanalyse".to_string(),
                "analysis of variance".to_string(),
                "factorial ANOVA".to_string(),
                "repeated measures ANOVA".to_string(),
            ];
            (
                planned_metadata_queries.clone(),
                vec![
                    "Prefer statistics and methods sources first.".to_string(),
                    "Activate psychology-specific sources only when psychology context is explicit.".to_string(),
                    "Activate theory sources only when theory or statistics context is explicit.".to_string(),
                ],
                vec![
                    "Deduplicate later by DOI, title, and source identifiers.".to_string(),
                    "Keep method and psychology records distinct until later consolidation.".to_string(),
                ],
            )
        }
        Some("hypothesis_testing") => {
            let psychology_context_active = psychology_context;
            source_families = vec![
                scientific_source_registry_family(
                    "openalex",
                    "OpenAlex",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active as a broad methods and statistics metadata source.",
                    true,
                    "broad methods and statistics metadata planning",
                    &["methods metadata", "broad literature planning"],
                    &["preview-only", "no scraping", "no connector call"],
                ),
                scientific_source_registry_family(
                    "crossref",
                    "Crossref",
                    "multidisciplinary",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Primary,
                    "active as a citation and DOI metadata source.",
                    true,
                    "citation and DOI metadata planning",
                    &["doi metadata", "citation metadata"],
                    &["preview-only", "no web requests"],
                ),
                scientific_source_registry_family(
                    "zbmath",
                    "zbMATH Open",
                    "mathematics_statistics",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Supporting,
                    "active as a mathematics and statistics theory metadata source.",
                    true,
                    "math and statistics theory metadata planning",
                    &["statistics theory", "math metadata"],
                    &["preview-only", "no scraping"],
                ),
                scientific_source_registry_family(
                    "arxiv",
                    "arXiv",
                    "preprint",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Supporting,
                    "active as a preprint and methods metadata source.",
                    true,
                    "preprint methods metadata planning",
                    &["preprint metadata", "methods planning"],
                    &["preview-only", "no scraping"],
                ),
                scientific_source_registry_family(
                    "pubpsych_if_psychology_context",
                    "PubPsych if psychology context",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when psychology context is explicit.",
                    psychology_context_active,
                    "psychology methods planning when psychology context is explicit",
                    &["psychology context", "methods planning"],
                    &["preview-only", "conditional on explicit psychology context"],
                ),
                scientific_source_registry_family(
                    "psycharchives_if_psychology_context",
                    "PsychArchives if psychology context",
                    "psychology",
                    ScholarChatScientificSourceAccessClass::OpenMetadata,
                    ScholarChatScientificSourcePriority::Conditional,
                    "active when psychology context is explicit.",
                    psychology_context_active,
                    "psychology repository planning when psychology context is explicit",
                    &["psychology context", "repository planning"],
                    &["preview-only", "conditional on explicit psychology context"],
                ),
            ];
            preferred_source_ids = vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "zbmath".to_string(),
                "arxiv".to_string(),
            ];
            conditional_source_ids = vec![
                "pubpsych_if_psychology_context".to_string(),
                "psycharchives_if_psychology_context".to_string(),
            ];
            if !psychology_context_active {
                excluded_source_ids.extend([
                    "pubpsych_if_psychology_context".to_string(),
                    "psycharchives_if_psychology_context".to_string(),
                ]);
            }
            let planned_metadata_queries = vec![
                "Hypothesentests".to_string(),
                "hypothesis testing".to_string(),
                "null hypothesis p value".to_string(),
                "statistical power type I error type II error".to_string(),
                "confidence intervals hypothesis testing".to_string(),
            ];
            (
                planned_metadata_queries.clone(),
                vec![
                    "Prefer methods and statistics sources first.".to_string(),
                    "Use psychology sources when applied psychology context is explicit.".to_string(),
                    "Later phases should deduplicate by DOI, title, and source identifiers.".to_string(),
                ],
                vec![
                    "Deduplicate later by DOI, title, and source identifiers.".to_string(),
                    "Keep methods and psychology records distinct until later consolidation.".to_string(),
                ],
            )
        }
        _ => (
            Vec::new(),
            Vec::new(),
            Vec::new(),
        ),
    };

    let ranking_hints = scientific_source_registry_ranking_hints(
        recognized_concept.as_deref(),
        &normalized_mode,
        &normalized_context_tags,
        &request.course_context,
    );
    let deduplication_hints = scientific_source_registry_deduplication_hints(
        recognized_concept.as_deref(),
        &normalized_mode,
    );

    if matches!(normalized_mode.as_str(), "course") {
        warnings.push("Course Mode will later prioritize local course materials, module context, prerequisites, and learning path support.".to_string());
        next_required_actions.push("Future Course Mode phases should prioritize local course materials, module context, prerequisites, and learning path support.".to_string());
    } else if matches!(normalized_mode.as_str(), "scientific_paper") {
        warnings.push("Scientific Paper Mode will later prioritize literature search planning, deduplication, ranking, review/meta-analysis prioritization where appropriate, and citation-safe planning.".to_string());
        next_required_actions.push("Future Scientific Paper Mode phases should prioritize literature search planning, deduplication, ranking, review/meta-analysis prioritization where appropriate, and citation-safe planning.".to_string());
    }

    let planned_metadata_queries = if recognized_concept.is_none() && !normalized_topic.is_empty() {
        push_unique_text(
            &mut warnings,
            "The topic is not yet mapped through the discipline registry.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Consider adding discipline mapping before expanding the source registry preview.",
        );
        vec![normalized_topic.clone()]
    } else {
        planned_metadata_queries
    };
    let access_classes = scientific_source_registry_access_classes(&source_families);
    let source_family_count = source_families.len();
    let active_source_family_count = source_families.iter().filter(|family| family.active_for_current_context).count();
    let conditional_source_family_count = source_families
        .iter()
        .filter(|family| matches!(family.priority, ScholarChatScientificSourcePriority::Conditional))
        .count();
    let summary = scientific_source_registry_plan_summary(&status, label.as_deref(), &normalized_mode);
    let source_plan = ScholarChatScientificSourceRegistrySourcePlan {
        source_family_count,
        active_source_family_count,
        conditional_source_family_count,
        planned_metadata_query_count: planned_metadata_queries.len(),
        summary: summary.clone(),
        steps: scientific_source_registry_plan_steps(&normalized_mode),
    };

    Ok(ScholarChatScientificSourceRegistryPreview {
        status,
        normalized_topic,
        normalized_mode,
        normalized_context_tags,
        discipline_status,
        recognized_concept,
        label,
        source_plan,
        source_families,
        preferred_source_ids,
        conditional_source_ids,
        excluded_source_ids,
        access_classes,
        planned_metadata_queries,
        ranking_hints,
        deduplication_hints,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        source_registry_preview_only: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_bm25_index: true,
        no_vector_index: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_scientific_query_understanding(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificQueryUnderstandingPreviewRequest,
) -> AegisResult<ScholarChatScientificQueryUnderstandingPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let query_lower = normalized_query.to_lowercase();
    let detected_aliases = scientific_query_detected_aliases(&normalized_query);
    let detected_concepts = scientific_query_detected_concepts(&normalized_query);
    let ambiguity_warnings = scientific_query_ambiguity_warnings(&detected_concepts);
    let query_intent = scientific_query_intent(&normalized_mode, &query_lower);
    let language_hints = scientific_query_language_hints(&query_lower, &detected_aliases);

    let status = if normalized_query.is_empty() {
        ScholarChatScientificQueryUnderstandingStatus::Blocked
    } else if detected_concepts.len() > 1 {
        ScholarChatScientificQueryUnderstandingStatus::Ambiguous
    } else if detected_concepts.is_empty() {
        ScholarChatScientificQueryUnderstandingStatus::UnknownConcept
    } else {
        ScholarChatScientificQueryUnderstandingStatus::Understood
    };

    let ambiguity_level = if matches!(status, ScholarChatScientificQueryUnderstandingStatus::Ambiguous) {
        ScholarChatScientificAmbiguityLevel::Medium
    } else {
        ScholarChatScientificAmbiguityLevel::None
    };

    let inferred_topic = if normalized_query.is_empty() {
        None
    } else if let Some(first_concept) = detected_concepts.first() {
        Some(first_concept.topic_label.to_string())
    } else {
        Some(normalized_query.clone())
    };

    let preview_topic = inferred_topic.clone().unwrap_or_else(|| normalized_query.clone());
    let discipline_preview = preview_scholar_chat_scientific_discipline_registry(
        &root,
        ScholarChatScientificDisciplineRegistryPreviewRequest {
            topic: preview_topic.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
        },
    )?;
    let source_preview = preview_scholar_chat_scientific_source_registry(
        &root,
        ScholarChatScientificSourceRegistryPreviewRequest {
            topic: preview_topic.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
        },
    )?;

    let recognized_concept = discipline_preview.recognized_concept.clone();
    let label = discipline_preview.label.clone();
    let discipline_status = discipline_preview.status.clone();
    let source_registry_status = source_preview.status.clone();

    let mut blockers = Vec::new();
    let mut warnings = vec![
        "This is a scientific query understanding preview only; no web requests, scraping, connectors, local indexing, model loading, runtime inference, or answer generation were run.".to_string(),
    ];
    let mut next_required_actions = Vec::new();

    if normalized_query.is_empty() {
        blockers.push("query_missing: Provide a scientific query to preview.".to_string());
        next_required_actions.push("Provide a scientific query to preview the understanding plan.".to_string());
    }

    if matches!(status, ScholarChatScientificQueryUnderstandingStatus::Ambiguous) {
        if !ambiguity_warnings.is_empty() {
            warnings.extend(ambiguity_warnings.iter().cloned());
        }
        next_required_actions.push(
            "Narrow the topic or accept the first inferred concept before expanding retrieval planning."
                .to_string(),
        );
    } else if matches!(status, ScholarChatScientificQueryUnderstandingStatus::UnknownConcept) && !normalized_query.is_empty() {
        warnings.push(
            "The query does not yet map to a known scientific concept.".to_string(),
        );
        next_required_actions.push(
            "Add discipline and source registry mappings before expanding retrieval planning."
                .to_string(),
        );
    }

    match normalized_mode.as_str() {
        "scientific_paper" => {
            warnings.push(
                "Scientific Paper Mode will later prioritize metadata search planning, deduplication, ranking, and citation-safe evidence preparation."
                    .to_string(),
            );
            next_required_actions.push(
                "Future Scientific Paper Mode phases should prioritize metadata search planning, deduplication, ranking, and citation-safe evidence preparation."
                    .to_string(),
            );
        }
        "course" => {
            warnings.push(
                "Course Mode will later prioritize local course materials, module context, prerequisites, and learning path support."
                    .to_string(),
            );
            next_required_actions.push(
                "Future Course Mode phases should prioritize local course materials, module context, prerequisites, and learning path support."
                    .to_string(),
            );
        }
        _ => {
            warnings.push(
                "Later Scholar Chat phases should retrieve local evidence before answering."
                    .to_string(),
            );
            next_required_actions.push(
                "Later Scholar Chat phases should retrieve local evidence before answering."
                    .to_string(),
            );
        }
    }

    if !matches!(source_registry_status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady) {
        warnings.push(
            "The source registry preview is not yet ready later for this inferred topic."
                .to_string(),
        );
        next_required_actions.push(
            "Expand discipline and source registry coverage before retrieval planning."
                .to_string(),
        );
    }

    let planned_metadata_queries = if normalized_query.is_empty() {
        Vec::new()
    } else {
        source_preview.planned_metadata_queries.clone()
    };
    let planned_local_search_queries = scientific_query_planned_local_search_queries(
        recognized_concept.as_deref(),
        &normalized_query,
    );
    let planned_expanded_queries = scientific_query_planned_expanded_queries(recognized_concept.as_deref());
    let evidence_requirements = scientific_query_evidence_requirements(
        &normalized_mode,
        &query_intent,
        &source_registry_status,
    );

    let summary = match status {
        ScholarChatScientificQueryUnderstandingStatus::Blocked => {
            "Scientific query understanding preview blocked because the query is blank.".to_string()
        }
        ScholarChatScientificQueryUnderstandingStatus::Ambiguous => format!(
            "Scientific query understanding preview found multiple scientific concepts and prefers {} first.",
            inferred_topic.as_deref().unwrap_or("the first inferred concept")
        ),
        ScholarChatScientificQueryUnderstandingStatus::Understood => format!(
            "Scientific query understanding preview understood '{}' as {} in {} mode.",
            normalized_query,
            inferred_topic.as_deref().unwrap_or("the inferred topic"),
            normalized_mode
        ),
        ScholarChatScientificQueryUnderstandingStatus::UnknownConcept => format!(
            "Scientific query understanding preview could not yet map '{}' to a known scientific concept.",
            normalized_query
        ),
    };

    Ok(ScholarChatScientificQueryUnderstandingPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        inferred_topic,
        query_intent,
        ambiguity_level,
        ambiguity_warnings,
        language_hints,
        detected_aliases,
        discipline_status,
        recognized_concept,
        label,
        source_registry_status,
        preferred_source_ids: source_preview.preferred_source_ids,
        conditional_source_ids: source_preview.conditional_source_ids,
        excluded_source_ids: source_preview.excluded_source_ids,
        planned_metadata_queries,
        planned_local_search_queries,
        planned_expanded_queries,
        evidence_requirements,
        ranking_hints: source_preview.ranking_hints,
        deduplication_hints: source_preview.deduplication_hints,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        query_understanding_preview_only: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_bm25_index: true,
        no_vector_index: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_scientific_search_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificSearchPlanRequest,
) -> AegisResult<ScholarChatScientificSearchPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let query_understanding_request = ScholarChatScientificQueryUnderstandingPreviewRequest {
        query: normalized_query.clone(),
        mode: Some(normalized_mode.clone()),
        course_context: request.course_context.clone(),
        context_tags: Some(normalized_context_tags.clone()),
    };

    let query_understanding_preview = preview_scholar_chat_scientific_query_understanding(
        &root,
        query_understanding_request,
    )?;
    let source_registry_status = query_understanding_preview.source_registry_status.clone();
    let query_understanding_status = query_understanding_preview.status.clone();
    let inferred_topic = query_understanding_preview.inferred_topic.clone();
    let recognized_concept = query_understanding_preview.recognized_concept.clone();
    let label = query_understanding_preview.label.clone();
    let query_intent = query_understanding_preview.query_intent.clone();
    let ambiguity_level = query_understanding_preview.ambiguity_level.clone();

    let status = if normalized_query.is_empty() {
        ScholarChatScientificSearchPlanStatus::Blocked
    } else {
        match query_understanding_status {
            ScholarChatScientificQueryUnderstandingStatus::Blocked => {
                ScholarChatScientificSearchPlanStatus::Blocked
            }
            ScholarChatScientificQueryUnderstandingStatus::Ambiguous => {
                ScholarChatScientificSearchPlanStatus::NeedsDisambiguation
            }
            ScholarChatScientificQueryUnderstandingStatus::UnknownConcept => {
                ScholarChatScientificSearchPlanStatus::UnknownConcept
            }
            ScholarChatScientificQueryUnderstandingStatus::Understood => {
                ScholarChatScientificSearchPlanStatus::SearchPlanReady
            }
        }
    };

    let search_strategy = scientific_search_plan_strategy(&status, &normalized_mode, &query_intent);
    let preferred_source_ids = query_understanding_preview.preferred_source_ids.clone();
    let conditional_source_ids = query_understanding_preview.conditional_source_ids.clone();
    let excluded_source_ids = query_understanding_preview.excluded_source_ids.clone();
    let planned_local_queries = query_understanding_preview.planned_local_search_queries.clone();
    let planned_metadata_queries = query_understanding_preview.planned_metadata_queries.clone();
    let planned_expanded_queries = query_understanding_preview.planned_expanded_queries.clone();
    let evidence_requirements = scientific_search_plan_evidence_requirements(
        &normalized_mode,
        &query_intent,
        &source_registry_status,
    );
    let source_routing_plan = scientific_search_plan_source_routing(
        &preferred_source_ids,
        &conditional_source_ids,
        &excluded_source_ids,
    );
    let local_search_plan = scientific_search_plan_local_search(
        &normalized_mode,
        if matches!(status, ScholarChatScientificSearchPlanStatus::Blocked) {
            Vec::new()
        } else {
            selected_local_source_ids.clone()
        },
        planned_local_queries.clone(),
    );
    let metadata_search_plan = scientific_search_plan_metadata(
        &source_registry_status,
        preferred_source_ids.clone(),
        conditional_source_ids.clone(),
        excluded_source_ids.clone(),
        planned_metadata_queries.clone(),
    );
    let planned_search_steps = scientific_search_plan_steps(
        &normalized_mode,
        &status,
        &local_search_plan.selected_local_source_ids,
        &preferred_source_ids,
        &conditional_source_ids,
        &excluded_source_ids,
        &planned_local_queries,
        &planned_metadata_queries,
        &planned_expanded_queries,
        &evidence_requirements,
    );
    let mut blockers = Vec::new();
    let mut warnings = query_understanding_preview.warnings.clone();
    let mut next_required_actions = query_understanding_preview.next_required_actions.clone();

    if normalized_query.is_empty() {
        blockers.push("query_missing: Provide a scientific query to preview the search plan.".to_string());
        next_required_actions.push("Provide a scientific query to preview the scientific search plan.".to_string());
    }

    if matches!(status, ScholarChatScientificSearchPlanStatus::NeedsDisambiguation) {
        warnings.push(
            "The search plan is still preview-only and prefers the first inferred concept until the query is narrowed."
                .to_string(),
        );
        next_required_actions.push(
            "Narrow the scientific concept before execution phases can plan retrieval in more detail."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatScientificSearchPlanStatus::UnknownConcept) {
        warnings.push(
            "The query still does not map to a known scientific concept for full search planning."
                .to_string(),
        );
        next_required_actions.push(
            "Add discipline and source registry mappings before metadata routing can be refined."
                .to_string(),
        );
    }

    if !normalized_query.is_empty() && matches!(normalized_mode.as_str(), "scholar_chat" | "course") {
        if local_search_plan.selected_local_source_ids.is_empty() {
            warnings.push("No local sources selected.".to_string());
            next_required_actions.push(
                "Select or import local sources in a later phase before answering."
                    .to_string(),
            );
        } else {
            warnings.push(
                "Local source selection is preview-only; no files are read and no indexes are built."
                    .to_string(),
            );
        }
    }

    if !normalized_query.is_empty()
        && matches!(source_registry_status, ScholarChatScientificSourceRegistryStatus::Blocked)
    {
        blockers.push(
            "source_family_plan_required: The source registry preview must be ready before metadata routing can be executed later."
                .to_string(),
        );
    } else if matches!(source_registry_status, ScholarChatScientificSourceRegistryStatus::UnknownConcept) {
        warnings.push(
            "The source registry preview is not yet ready later for this inferred concept.".to_string(),
        );
        next_required_actions.push(
            "Add discipline and source registry mappings before metadata routing is planned in later phases."
                .to_string(),
        );
    }

    let summary = match status {
        ScholarChatScientificSearchPlanStatus::Blocked => {
            "Scientific search plan preview blocked because the query is blank or a required prerequisite is missing.".to_string()
        }
        ScholarChatScientificSearchPlanStatus::NeedsDisambiguation => {
            "Scientific search plan preview needs disambiguation before later retrieval planning can narrow the route.".to_string()
        }
        ScholarChatScientificSearchPlanStatus::UnknownConcept => {
            "Scientific search plan preview found an unknown concept and can only outline later planning steps.".to_string()
        }
        ScholarChatScientificSearchPlanStatus::SearchPlanReady => {
            "Scientific search plan preview is ready later and only describes future local and metadata search planning.".to_string()
        }
    };

    Ok(ScholarChatScientificSearchPlanPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids: local_search_plan.selected_local_source_ids.clone(),
        query_understanding_status,
        inferred_topic,
        query_intent,
        ambiguity_level,
        recognized_concept,
        label,
        source_registry_status,
        search_strategy,
        local_search_plan,
        metadata_search_plan,
        source_routing_plan,
        planned_search_steps,
        planned_local_queries,
        planned_metadata_queries,
        planned_expanded_queries,
        preferred_source_ids,
        conditional_source_ids,
        excluded_source_ids,
        evidence_requirements,
        ranking_hints: query_understanding_preview.ranking_hints.clone(),
        deduplication_hints: query_understanding_preview.deduplication_hints.clone(),
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        scientific_search_plan_preview_only: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_bm25_index: true,
        no_vector_index: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_local_literature_index(
    root: impl Into<PathBuf>,
    request: ScholarChatLocalLiteratureIndexRequest,
) -> AegisResult<ScholarChatLocalLiteratureIndexPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let expected_source_kinds = normalize_scientific_expected_source_kinds(
        request.expected_source_kinds.clone(),
    );
    let search_plan_preview = preview_scholar_chat_scientific_search_plan(
        &root,
        ScholarChatScientificSearchPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
        },
    )?;
    let search_plan_status = search_plan_preview.status.clone();
    let query_understanding_status = search_plan_preview.query_understanding_status.clone();
    let inferred_topic = search_plan_preview.inferred_topic.clone();
    let query_intent = search_plan_preview.query_intent.clone();
    let recognized_concept = search_plan_preview.recognized_concept.clone();
    let label = search_plan_preview.label.clone();
    let planned_local_queries = scientific_local_literature_index_local_queries(
        &search_plan_preview.planned_local_queries,
        &normalized_query,
    );
    let planned_metadata_requirements = scientific_local_literature_index_metadata_requirements(
        &normalized_mode,
    );
    let status = if normalized_query.is_empty() {
        ScholarChatLocalLiteratureIndexStatus::Blocked
    } else {
        scientific_local_literature_index_status(&search_plan_status, &selected_local_source_ids)
    };
    let local_index_strategy =
        scientific_local_literature_index_strategy(&status, &normalized_mode);
    let local_corpus_plan = ScholarChatLocalLiteratureCorpusPlan {
        selected_source_count: selected_local_source_ids.len(),
        selected_local_source_ids: if matches!(status, ScholarChatLocalLiteratureIndexStatus::Blocked) {
            Vec::new()
        } else {
            selected_local_source_ids.clone()
        },
        expected_source_kinds: expected_source_kinds.clone(),
        corpus_manifest_would_be_required: !selected_local_source_ids.is_empty(),
        will_create_corpus: false,
        will_read_files: false,
        summary: if selected_local_source_ids.is_empty() {
            "Local corpus planning is preview-only; no local files are read and no corpus is created.".to_string()
        } else {
            "Local corpus planning is preview-only; selected local sources are noted without reading files or creating a corpus."
                .to_string()
        },
    };
    let index_artifact_plan = ScholarChatLocalLiteratureIndexArtifactPlan {
        planned_artifact_ids: scientific_local_literature_index_planned_artifact_ids(),
        planned_artifact_descriptions: scientific_local_literature_index_planned_artifact_descriptions(),
        will_create_artifacts: false,
        will_create_bm25_index: false,
        will_create_vector_index: false,
        will_generate_embeddings: false,
        summary: "Index artifacts are preview-only; no artifacts, BM25 index, vector index, or embeddings are created."
            .to_string(),
    };
    let ingestion_readiness = match status {
        ScholarChatLocalLiteratureIndexStatus::Blocked => {
            ScholarChatLocalLiteratureIngestionReadiness::Blocked
        }
        ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation => {
            ScholarChatLocalLiteratureIngestionReadiness::NeedsDisambiguation
        }
        ScholarChatLocalLiteratureIndexStatus::UnknownConcept => {
            ScholarChatLocalLiteratureIngestionReadiness::UnknownConceptMappingNeeded
        }
        ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources => {
            ScholarChatLocalLiteratureIngestionReadiness::NeedsSources
        }
        ScholarChatLocalLiteratureIndexStatus::IndexPlanReady => {
            ScholarChatLocalLiteratureIngestionReadiness::PreviewReady
        }
    };
    let _source_registry_status = search_plan_preview.source_registry_status.clone();
    let mut blockers = Vec::new();
    let mut warnings = search_plan_preview.warnings.clone();
    let mut next_required_actions = search_plan_preview.next_required_actions.clone();

    if normalized_query.is_empty() {
        blockers.push("query_missing: Provide a scientific query to preview the local literature index.".to_string());
        next_required_actions.push("Provide a scientific query to preview the local literature index.".to_string());
    }

    if matches!(status, ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources) {
        warnings.push("No local sources selected.".to_string());
        next_required_actions.push(
            "Select or import local sources in a later phase before indexing."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation) {
        warnings.push(
            "The local literature index preview still needs disambiguation before later corpus planning can narrow the route."
                .to_string(),
        );
        next_required_actions.push(
            "Narrow the scientific concept before local index planning can continue."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatLocalLiteratureIndexStatus::UnknownConcept) {
        warnings.push(
            "The query still does not map to a known scientific concept for local literature indexing."
                .to_string(),
        );
        next_required_actions.push(
            "Add discipline and source registry mappings before local index planning can continue."
                .to_string(),
        );
    }

    let known_source_kinds = [
        "pdf",
        "markdown",
        "text",
        "course_material",
        "lecture_slide",
        "article",
        "book_chapter",
        "notes",
        "unknown",
    ]
    .iter()
    .copied()
    .collect::<BTreeSet<_>>();
    let unknown_expected_source_kinds = expected_source_kinds
        .iter()
        .filter(|kind| !known_source_kinds.contains(kind.as_str()))
        .cloned()
        .collect::<Vec<_>>();
    if !unknown_expected_source_kinds.is_empty() {
        warnings.push(format!(
            "Unknown expected source kinds are preserved as preview-only hints: {}.",
            unknown_expected_source_kinds.join(", ")
        ));
    }

    let planned_index_fields = scientific_local_literature_index_planned_index_fields();
    let planned_chunking_policy = scientific_local_literature_index_chunking_policy();
    let local_corpus_plan_inputs = local_corpus_plan
        .selected_local_source_ids
        .iter()
        .chain(local_corpus_plan.expected_source_kinds.iter())
        .cloned()
        .collect::<Vec<_>>();
    let local_corpus_plan_outputs = vec![
        "local_corpus_manifest_preview".to_string(),
        "local_literature_metadata_map_preview".to_string(),
    ];
    let step_active = !matches!(status, ScholarChatLocalLiteratureIndexStatus::Blocked);
    let planned_index_steps = vec![
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::SourceSelectionReview,
            "source_selection_review",
            "Source selection review",
            "Review local source selections only; no filesystem validation is performed.",
            vec![
                normalized_query.clone(),
                normalized_mode.clone(),
            ],
            local_corpus_plan.selected_local_source_ids.clone(),
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::MetadataRequirementCheck,
            "metadata_requirement_check",
            "Metadata requirement check",
            "Review metadata requirements only; no files are read and no corpus metadata is written.",
            vec![normalized_mode.clone()],
            planned_metadata_requirements.clone(),
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::CorpusManifestPlan,
            "corpus_manifest_plan",
            "Corpus manifest plan",
            "Plan a future corpus manifest only; no corpus is created and no files are read.",
            local_corpus_plan_inputs.clone(),
            local_corpus_plan_outputs.clone(),
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::ExtractionPlan,
            "extraction_plan",
            "Extraction plan",
            "Plan future extraction only; no PDF extraction or file reading occurs.",
            local_corpus_plan.selected_local_source_ids.clone(),
            vec!["extracted_text_preview".to_string()],
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::ChunkingPolicyPlan,
            "chunking_policy_plan",
            "Chunking policy plan",
            "Plan future chunking only; no chunking run, OCR, or extraction occurs.",
            planned_chunking_policy.clone(),
            vec!["chunk_policy_preview".to_string()],
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::LexicalIndexPlan,
            "lexical_index_plan",
            "Lexical index plan",
            "Plan future lexical indexing only; no BM25 index is created.",
            planned_local_queries.clone(),
            vec!["bm25_index_plan_preview".to_string()],
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::VectorIndexPlan,
            "vector_index_plan",
            "Vector index plan",
            "Plan future vector indexing only; no embeddings are generated.",
            planned_local_queries.clone(),
            vec!["vector_index_plan_preview".to_string()],
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::DeduplicationPlan,
            "deduplication_plan",
            "Deduplication plan",
            "Plan future deduplication only; no retrieval or evidence artifacts are created.",
            planned_metadata_requirements.clone(),
            vec!["deduplicated_literature_preview".to_string()],
            step_active,
        ),
        scientific_local_literature_index_step(
            ScholarChatLocalLiteratureIndexStepKind::RetrievalReadinessCheck,
            "retrieval_readiness_check",
            "Retrieval readiness check",
            "Plan later retrieval readiness only; no retrieval is executed.",
            vec![
                "search_plan_ready".to_string(),
                format!("search_plan_status={:?}", search_plan_status),
            ],
            vec!["retrieval_readiness_preview".to_string()],
            step_active,
        ),
    ];

    let summary = match status {
        ScholarChatLocalLiteratureIndexStatus::Blocked => {
            "Local literature index preview blocked because the query is blank or a prerequisite is missing."
                .to_string()
        }
        ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation => {
            "Local literature index preview needs disambiguation before later corpus planning can continue."
                .to_string()
        }
        ScholarChatLocalLiteratureIndexStatus::UnknownConcept => {
            "Local literature index preview found an unknown concept and can only outline later indexing steps."
                .to_string()
        }
        ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources => {
            "Local literature index preview is ready later but still needs local sources before indexing can proceed."
                .to_string()
        }
        ScholarChatLocalLiteratureIndexStatus::IndexPlanReady => {
            "Local literature index preview is ready later and only describes future corpus, chunking, and indexing planning."
                .to_string()
        }
    };

    Ok(ScholarChatLocalLiteratureIndexPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids: local_corpus_plan.selected_local_source_ids.clone(),
        expected_source_kinds,
        search_plan_status,
        query_understanding_status,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        local_index_strategy,
        local_corpus_plan,
        index_artifact_plan,
        ingestion_readiness,
        planned_index_fields,
        planned_chunking_policy,
        planned_metadata_requirements,
        planned_local_queries,
        planned_index_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        local_literature_index_preview_only: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_bm25_index: true,
        no_vector_index: true,
        no_retrieval_execution: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_course_literature_registry(
    root: impl Into<PathBuf>,
    request: ScholarChatCourseLiteratureRegistryPreviewRequest,
) -> AegisResult<ScholarChatCourseLiteratureRegistryPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_course_context = normalize_course_optional_text(request.course_context);
    let normalized_module_code = normalize_course_optional_text(request.module_code);
    let normalized_course_title = normalize_course_optional_text(request.course_title);
    let normalized_instructor = normalize_course_optional_text(request.instructor);
    let normalized_semester = normalize_course_optional_text(request.semester);
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let expected_course_material_kinds = normalize_course_material_kinds(
        request.expected_course_material_kinds.clone(),
    );
    let local_literature_index_preview = preview_scholar_chat_local_literature_index(
        &root,
        ScholarChatLocalLiteratureIndexRequest {
            query: normalized_query.clone(),
            mode: Some("course".to_string()),
            course_context: normalized_course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
            expected_source_kinds: Some(expected_course_material_kinds.clone()),
        },
    )?;

    let local_literature_index_status = local_literature_index_preview.status.clone();
    let search_plan_status = local_literature_index_preview.search_plan_status.clone();
    let query_understanding_status = local_literature_index_preview.query_understanding_status.clone();
    let inferred_topic = local_literature_index_preview.inferred_topic.clone();
    let query_intent = local_literature_index_preview.query_intent.clone();
    let recognized_concept = local_literature_index_preview.recognized_concept.clone();
    let label = local_literature_index_preview.label.clone();
    let course_identity = course_literature_registry_course_identity(
        normalized_course_context.clone(),
        normalized_module_code.clone(),
        normalized_course_title.clone(),
        normalized_instructor.clone(),
        normalized_semester.clone(),
    );
    let course_material_plan = course_literature_registry_course_material_plan(
        selected_local_source_ids.clone(),
        expected_course_material_kinds.clone(),
    );
    let curriculum_alignment_plan = course_literature_registry_curriculum_alignment_plan(
        &normalized_course_context,
        &normalized_module_code,
    );
    let planned_course_metadata_requirements =
        course_literature_registry_planned_course_metadata_requirements(
            &course_identity,
            &course_material_plan,
        );
    let planned_course_material_queries = course_literature_registry_planned_course_material_queries(
        &normalized_query,
        &normalized_module_code,
        &normalized_course_title,
        &local_literature_index_preview.planned_local_queries,
    );
    let status = course_literature_registry_status(
        &local_literature_index_status,
        &normalized_course_context,
        &normalized_module_code,
        &normalized_course_title,
        &selected_local_source_ids,
    );
    let course_registry_strategy = course_literature_registry_strategy(
        &status,
        &normalized_module_code,
        &selected_local_source_ids,
    );
    let step_active = !matches!(status, ScholarChatCourseLiteratureRegistryStatus::Blocked);
    let planned_registry_steps = vec![
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::CourseIdentityReview,
            "course_identity_review",
            "Course identity review",
            "Review course context, module code, title, instructor, and semester only; no course files are read.",
            vec![
                normalized_course_context.clone().unwrap_or_default(),
                normalized_module_code.clone().unwrap_or_default(),
                normalized_course_title.clone().unwrap_or_default(),
                normalized_instructor.clone().unwrap_or_default(),
                normalized_semester.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["course_identity_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::ModuleContextReview,
            "module_context_review",
            "Module context review",
            "Review module context only; no curriculum scraping or local file inspection occurs.",
            vec![
                normalized_module_code.clone().unwrap_or_default(),
                normalized_course_title.clone().unwrap_or_default(),
                normalized_semester.clone().unwrap_or_default(),
                normalized_instructor.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["module_context_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::CourseMaterialKindPlan,
            "course_material_kind_plan",
            "Course material kind plan",
            "Plan course material kinds only; no files are read, imported, or registered.",
            vec![
                course_material_plan.expected_course_material_kinds.join(", "),
                course_material_plan.known_material_kinds.join(", "),
                course_material_plan.unknown_material_kinds.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["course_material_kind_plan_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::LocalSourceAlignmentPlan,
            "local_source_alignment_plan",
            "Local source alignment plan",
            "Plan local course-source alignment only; no source validation or registry creation occurs.",
            vec![
                course_material_plan.selected_local_source_ids.join(", "),
                format!("selected_source_count={}", course_material_plan.selected_source_count),
            ],
            vec!["local_source_alignment_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::CurriculumMetadataRequirementCheck,
            "curriculum_metadata_requirement_check",
            "Curriculum metadata requirement check",
            "Review curriculum metadata requirements only; no scraping or connector calls occur.",
            planned_course_metadata_requirements.clone(),
            vec!["curriculum_alignment_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::LocalLiteratureIndexAlignment,
            "local_literature_index_alignment",
            "Local literature index alignment",
            "Align course planning with the local literature index preview only; no index creation or retrieval execution occurs.",
            vec![
                format!("local_literature_index_status={:?}", local_literature_index_status),
                format!("search_plan_status={:?}", search_plan_status),
            ],
            planned_course_material_queries.clone(),
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::RetrievalReadinessCheck,
            "retrieval_readiness_check",
            "Retrieval readiness check",
            "Check later retrieval readiness only; no retrieval is executed.",
            vec![
                format!("search_plan_status={:?}", search_plan_status),
                format!("selected_source_count={}", selected_local_source_ids.len()),
            ],
            vec!["retrieval_readiness_preview".to_string()],
            step_active,
        ),
        course_literature_registry_step(
            ScholarChatCourseLiteratureRegistryStepKind::LearningPathAlignmentPlan,
            "learning_path_alignment_plan",
            "Learning path alignment plan",
            "Plan learning-path alignment only; no answer generation or curriculum execution occurs.",
            vec![
                normalized_course_context.clone().unwrap_or_default(),
                normalized_course_title.clone().unwrap_or_default(),
                normalized_module_code.clone().unwrap_or_default(),
                normalized_instructor.clone().unwrap_or_default(),
                normalized_semester.clone().unwrap_or_default(),
                format!("{:?}", query_intent),
                recognized_concept.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["learning_path_alignment_preview".to_string()],
            step_active,
        ),
    ];

    let mut blockers = local_literature_index_preview.blockers.clone();
    let mut warnings = local_literature_index_preview.warnings.clone();
    let mut next_required_actions = local_literature_index_preview.next_required_actions.clone();

    if normalized_query.is_empty() {
        push_unique_string(
            &mut blockers,
            "query_missing: Provide a course or scientific query to preview the course literature registry."
                .to_string(),
        );
        push_unique_string(
            &mut next_required_actions,
            "Provide a course or scientific query to preview the course literature registry."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatCourseLiteratureRegistryStatus::NeedsCourseContext) {
        push_unique_string(
            &mut warnings,
            "No course context, module code, or course title was provided.".to_string(),
        );
        push_unique_string(
            &mut next_required_actions,
            "Provide course context, module code, or course title.".to_string(),
        );
    }

    if matches!(status, ScholarChatCourseLiteratureRegistryStatus::NeedsLocalSources) {
        push_unique_string(
            &mut warnings,
            "No local course sources selected.".to_string(),
        );
        push_unique_string(
            &mut next_required_actions,
            "Select or import local course material sources later before registry creation."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatCourseLiteratureRegistryStatus::NeedsDisambiguation) {
        push_unique_string(
            &mut warnings,
            "The local literature index preview still needs disambiguation before course registry planning can continue."
                .to_string(),
        );
        push_unique_string(
            &mut next_required_actions,
            "Narrow the scientific concept before course registry planning can continue."
                .to_string(),
        );
    }

    if matches!(status, ScholarChatCourseLiteratureRegistryStatus::UnknownConcept) {
        push_unique_string(
            &mut warnings,
            "The query still does not map to a known scientific concept for course registry planning."
                .to_string(),
        );
        push_unique_string(
            &mut next_required_actions,
            "Add discipline and source registry mappings before course registry planning can continue."
                .to_string(),
        );
    }

    let unknown_expected_material_kinds = course_material_plan
        .unknown_material_kinds
        .iter()
        .cloned()
        .collect::<Vec<_>>();
    if !unknown_expected_material_kinds.is_empty() {
        push_unique_string(
            &mut warnings,
            format!(
                "Unknown course material kinds are preserved as preview-only hints: {}.",
                unknown_expected_material_kinds.join(", ")
            ),
        );
    }

    let summary = match status {
        ScholarChatCourseLiteratureRegistryStatus::Blocked => {
            "Course literature registry preview blocked because the query is blank or the local literature index preview is blocked.".to_string()
        }
        ScholarChatCourseLiteratureRegistryStatus::NeedsCourseContext => {
            "Course literature registry preview needs course context, module code, or course title before later registry planning can continue.".to_string()
        }
        ScholarChatCourseLiteratureRegistryStatus::NeedsLocalSources => {
            "Course literature registry preview is ready later but still needs local course sources before registry alignment can proceed.".to_string()
        }
        ScholarChatCourseLiteratureRegistryStatus::NeedsDisambiguation => {
            "Course literature registry preview needs disambiguation before later course registry planning can continue.".to_string()
        }
        ScholarChatCourseLiteratureRegistryStatus::UnknownConcept => {
            "Course literature registry preview found an unknown concept and can only outline later course registry planning steps.".to_string()
        }
        ScholarChatCourseLiteratureRegistryStatus::CourseRegistryPlanReady => {
            "Course literature registry preview is ready later and only describes future course identity, course-material alignment, curriculum metadata, and learning-path planning.".to_string()
        }
    };

    Ok(ScholarChatCourseLiteratureRegistryPreview {
        status,
        normalized_query,
        normalized_course_context,
        normalized_module_code,
        normalized_course_title,
        normalized_instructor,
        normalized_semester,
        normalized_context_tags,
        selected_local_source_ids: selected_local_source_ids.clone(),
        expected_course_material_kinds: expected_course_material_kinds.clone(),
        local_literature_index_status,
        search_plan_status,
        query_understanding_status,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        course_registry_strategy,
        course_identity,
        course_material_plan,
        curriculum_alignment_plan,
        planned_course_metadata_requirements,
        planned_course_material_queries,
        planned_registry_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        course_literature_registry_preview_only: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_web_request: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_local_file_indexing: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

fn normalize_metadata_connector_sources(sources: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = sources
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn metadata_connector_supported_sources() -> Vec<String> {
    vec!["openalex".to_string(), "crossref".to_string()]
}

fn metadata_connector_step_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no connector call".to_string(),
        "no metadata record write".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ]
}

fn metadata_connector_step(
    kind: ScholarChatMetadataConnectorStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatMetadataConnectorStep {
    ScholarChatMetadataConnectorStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: metadata_connector_step_notes(),
    }
}

fn metadata_connector_source_selection_plan(
    requested_sources: Vec<String>,
    selected_sources: Vec<String>,
    unknown_sources: Vec<String>,
) -> ScholarChatMetadataConnectorSourceSelectionPlan {
    let needs_source_selection = !requested_sources.is_empty() && selected_sources.is_empty();
    let summary = if requested_sources.is_empty() {
        "OpenAlex and Crossref are selected by default later; no connector calls are made now.".to_string()
    } else if selected_sources.is_empty() {
        "No supported metadata source remains after normalization; choose OpenAlex or Crossref later.".to_string()
    } else if unknown_sources.is_empty() {
        "Metadata source preferences were normalized and selected later without connector calls.".to_string()
    } else {
        "Supported metadata sources are selected later while unknown source hints remain preview-only.".to_string()
    };
    ScholarChatMetadataConnectorSourceSelectionPlan {
        requested_sources,
        supported_sources: selected_sources.clone(),
        unknown_sources,
        selected_sources,
        needs_source_selection,
        summary,
    }
}

fn metadata_connector_query_plan(
    source_id: &str,
    enabled: bool,
    endpoint_family: &str,
    planned_query_terms: Vec<String>,
    planned_filters: Vec<String>,
    max_results: u32,
    expected_result_fields: Vec<String>,
) -> ScholarChatMetadataConnectorQueryPlan {
    let summary = if enabled {
        format!(
            "{} connector planning is preview-only and does not call an API, fetch URLs, scrape, or write metadata.",
            source_id.to_uppercase()
        )
    } else {
        format!(
            "{} connector planning is preview-only and remains disabled until the source is selected.",
            source_id.to_uppercase()
        )
    };
    ScholarChatMetadataConnectorQueryPlan {
        source_id: source_id.to_string(),
        enabled,
        endpoint_family: endpoint_family.to_string(),
        planned_query_terms,
        planned_filters,
        max_results,
        requires_api_key: false,
        will_call_api: false,
        will_fetch_url: false,
        will_scrape: false,
        expected_result_fields,
        summary,
    }
}

fn metadata_connector_deduplication_plan(
    preferred_source_order: Vec<String>,
    doi_required: bool,
) -> ScholarChatMetadataDeduplicationPlan {
    ScholarChatMetadataDeduplicationPlan {
        key_fields: vec![
            "doi".to_string(),
            "normalized_title".to_string(),
            "publication_year".to_string(),
            "first_author".to_string(),
            "source_id".to_string(),
        ],
        preferred_source_order,
        doi_required,
        title_year_fallback_allowed: !doi_required,
        will_deduplicate_now: false,
        summary: if doi_required {
            "Deduplication is preview-only and DOI-required planning disables title/year fallback later.".to_string()
        } else {
            "Deduplication is preview-only and will later prefer DOI, normalized title, publication year, first author, and source id.".to_string()
        },
    }
}

fn metadata_connector_attribution_plan() -> ScholarChatMetadataAttributionPlan {
    ScholarChatMetadataAttributionPlan {
        required_attribution_notes: vec![
            "cite_metadata_provider_later".to_string(),
            "preserve_provider_record_ids_later".to_string(),
            "preserve_doi_and_source_url_later".to_string(),
            "include_access_date_later".to_string(),
            "no_fabricated_citations".to_string(),
        ],
        planned_user_agent_note: "later_connector_should_identify_aegis_scholar_and_contact_when_configured"
            .to_string(),
        citation_safety_notes: vec![
            "metadata_preview_not_citation_evidence".to_string(),
            "full_citation_requires_verified_metadata_later".to_string(),
            "no_generated_sources".to_string(),
        ],
        will_emit_attribution_now: false,
        summary: "Metadata attribution is preview-only and will later preserve provider record ids, DOIs, source URLs, and access dates without fabricating citations."
            .to_string(),
    }
}

fn metadata_connector_compliance_plan() -> ScholarChatMetadataCompliancePlan {
    ScholarChatMetadataCompliancePlan {
        external_metadata_only: true,
        no_fulltext_download: true,
        no_scraping: true,
        respects_rate_limits_later: true,
        requires_terms_review_later: true,
        will_call_external_services: false,
        summary: "Metadata connector compliance is preview-only and does not call external services.".to_string(),
    }
}

fn normalize_psychology_connector_values(values: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = values
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn normalize_scientific_claim_scope(values: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = values
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn normalize_scientific_citation_style(style: Option<String>) -> (String, Vec<String>) {
    let normalized = style
        .as_deref()
        .map(|value| normalize_scientific_tag_text(value))
        .filter(|value| !value.is_empty())
        .unwrap_or_else(|| "neutral".to_string());
    let style = match normalized.as_str() {
        "apa" | "doi_first" | "neutral" => normalized,
        other => {
            let warning = format!("Unknown citation style '{other}' normalized to neutral.");
            return ("neutral".to_string(), vec![warning]);
        }
    };
    (style, Vec::new())
}

fn scientific_evidence_pack_supported_citation_styles() -> Vec<String> {
    vec![
        "apa".to_string(),
        "doi_first".to_string(),
        "neutral".to_string(),
    ]
}

fn psychology_supported_source_families() -> Vec<String> {
    vec![
        "openalex_psychology".to_string(),
        "crossref_psychology".to_string(),
        "pubmed_psychology".to_string(),
        "eric_psychology".to_string(),
        "apa_psycnet_discovery".to_string(),
        "local_course_literature".to_string(),
    ]
}

fn psychology_default_source_families(normalized_mode: &str) -> Vec<String> {
    match normalized_mode {
        "scientific_paper" => vec![
            "openalex_psychology".to_string(),
            "crossref_psychology".to_string(),
            "pubmed_psychology".to_string(),
            "apa_psycnet_discovery".to_string(),
        ],
        "course" => vec![
            "openalex_psychology".to_string(),
            "crossref_psychology".to_string(),
            "eric_psychology".to_string(),
            "local_course_literature".to_string(),
        ],
        _ => vec![
            "openalex_psychology".to_string(),
            "crossref_psychology".to_string(),
            "pubmed_psychology".to_string(),
        ],
    }
}

fn psychology_source_family_label(family_id: &str) -> &'static str {
    match family_id {
        "openalex_psychology" => "OpenAlex Psychology",
        "crossref_psychology" => "Crossref Psychology",
        "pubmed_psychology" => "PubMed Psychology",
        "eric_psychology" => "ERIC Psychology",
        "apa_psycnet_discovery" => "APA PsycNet / PsycINFO Discovery",
        "local_course_literature" => "Local Course Literature",
        _ => "Unknown Psychology Source Family",
    }
}

fn psychology_source_family_connector_kind(family_id: &str) -> &'static str {
    match family_id {
        "openalex_psychology" => "open_metadata",
        "crossref_psychology" => "doi_metadata",
        "pubmed_psychology" => "biomedical_metadata",
        "eric_psychology" => "education_psychology_metadata",
        "apa_psycnet_discovery" => "subscription_discovery_boundary",
        "local_course_literature" => "local_course_context",
        _ => "unknown_connector_kind",
    }
}

fn psychology_source_family_metadata_dependencies(family_id: &str) -> Vec<String> {
    match family_id {
        "openalex_psychology" => vec!["openalex".to_string()],
        "crossref_psychology" => vec!["crossref".to_string()],
        "pubmed_psychology" => vec!["later_public_metadata_connector".to_string()],
        "eric_psychology" => vec!["later_public_metadata_connector".to_string()],
        "apa_psycnet_discovery" => vec![
            "manual_or_institutional_access_later".to_string(),
            "no_automated_authenticated_scraping".to_string(),
        ],
        "local_course_literature" => vec!["local_course_literature_registry_later".to_string()],
        _ => vec!["unknown_dependency".to_string()],
    }
}

fn psychology_source_family_expected_result_types(family_id: &str) -> Vec<String> {
    match family_id {
        "openalex_psychology" => vec![
            "scholarly_work_metadata".to_string(),
            "authorship_metadata".to_string(),
            "open_access_metadata".to_string(),
            "citation_count_metadata".to_string(),
            "concept_metadata".to_string(),
        ],
        "crossref_psychology" => vec![
            "doi_metadata".to_string(),
            "bibliographic_metadata".to_string(),
            "publisher_metadata".to_string(),
            "journal_container_metadata".to_string(),
        ],
        "pubmed_psychology" => vec![
            "biomedical_citation_metadata".to_string(),
            "mesh_or_keyword_metadata_later".to_string(),
            "abstract_metadata_later_if_available_from_public_metadata".to_string(),
        ],
        "eric_psychology" => vec![
            "education_research_metadata".to_string(),
            "learning_context_metadata".to_string(),
            "pedagogy_metadata".to_string(),
        ],
        "apa_psycnet_discovery" => vec![
            "psychology_index_discovery_metadata".to_string(),
            "manual_access_requirement".to_string(),
            "no_automated_authenticated_scraping".to_string(),
        ],
        "local_course_literature" => vec![
            "course_reading_alignment".to_string(),
            "syllabus_or_module_context_later".to_string(),
            "local_source_context_later".to_string(),
        ],
        _ => vec!["unknown_result_type".to_string()],
    }
}

fn psychology_source_family_evidence_relevance(family_id: &str) -> Vec<String> {
    match family_id {
        "openalex_psychology" => vec![
            "scholarly_work_metadata".to_string(),
            "citation_count_metadata".to_string(),
            "concept_metadata".to_string(),
        ],
        "crossref_psychology" => vec![
            "doi_metadata".to_string(),
            "bibliographic_metadata".to_string(),
            "journal_container_metadata".to_string(),
        ],
        "pubmed_psychology" => vec![
            "biomedical_citation_metadata".to_string(),
            "abstract_metadata_later_if_available_from_public_metadata".to_string(),
        ],
        "eric_psychology" => vec![
            "education_research_metadata".to_string(),
            "learning_context_metadata".to_string(),
        ],
        "apa_psycnet_discovery" => vec![
            "psychology_index_discovery_metadata".to_string(),
            "manual_access_requirement".to_string(),
        ],
        "local_course_literature" => vec![
            "course_reading_alignment".to_string(),
            "local_source_context_later".to_string(),
        ],
        _ => vec!["unknown_evidence_relevance".to_string()],
    }
}

fn psychology_source_family_access_boundary_notes(family_id: &str) -> Vec<String> {
    match family_id {
        "openalex_psychology" | "crossref_psychology" => vec![
            "no API call now".to_string(),
            "no web request".to_string(),
            "no scraping".to_string(),
        ],
        "pubmed_psychology" | "eric_psychology" => vec![
            "no API call now".to_string(),
            "no web request".to_string(),
            "no scraping".to_string(),
        ],
        "apa_psycnet_discovery" => vec![
            "planning boundary only".to_string(),
            "no automated authenticated scraping".to_string(),
            "no paywall bypass".to_string(),
            "later manual or institutional access review required".to_string(),
        ],
        "local_course_literature" => vec![
            "no file read now".to_string(),
            "no local index access now".to_string(),
        ],
        _ => vec!["preview-only".to_string()],
    }
}

fn psychology_source_connector_step_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ]
}

fn psychology_source_connector_step(
    kind: ScholarChatPsychologySourceConnectorStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatPsychologySourceConnectorStep {
    ScholarChatPsychologySourceConnectorStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: psychology_source_connector_step_notes(),
    }
}

pub fn preview_scholar_chat_metadata_connector_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatMetadataConnectorPlanRequest,
) -> AegisResult<ScholarChatMetadataConnectorPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let expected_source_kinds = normalize_scientific_expected_source_kinds(
        request.expected_source_kinds.clone(),
    );
    let normalized_requested_metadata_sources =
        normalize_metadata_connector_sources(request.preferred_metadata_sources.clone());
    let supported_metadata_sources = metadata_connector_supported_sources();
    let selected_metadata_sources = if normalized_requested_metadata_sources.is_empty() {
        supported_metadata_sources.clone()
    } else {
        supported_metadata_sources
            .iter()
            .filter(|source| normalized_requested_metadata_sources.contains(source))
            .cloned()
            .collect::<Vec<_>>()
    };
    let unknown_metadata_sources = if normalized_requested_metadata_sources.is_empty() {
        Vec::new()
    } else {
        normalized_requested_metadata_sources
            .iter()
            .filter(|source| !supported_metadata_sources.contains(source))
            .cloned()
            .collect::<Vec<_>>()
    };
    let search_plan_preview = preview_scholar_chat_scientific_search_plan(
        &root,
        ScholarChatScientificSearchPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
        },
    )?;
    let search_plan_status = search_plan_preview.status.clone();
    let query_understanding_status = search_plan_preview.query_understanding_status.clone();
    let inferred_topic = search_plan_preview.inferred_topic.clone();
    let query_intent = search_plan_preview.query_intent.clone();
    let recognized_concept = search_plan_preview.recognized_concept.clone();
    let label = search_plan_preview.label.clone();

    let mut warnings = search_plan_preview.warnings.clone();
    let mut blockers = search_plan_preview.blockers.clone();
    let mut next_required_actions = search_plan_preview.next_required_actions.clone();

    let year_range_invalid = matches!(
        (request.year_from, request.year_to),
        (Some(year_from), Some(year_to)) if year_from > year_to
    );
    if year_range_invalid {
        push_unique_text(
            &mut blockers,
            "invalid_year_range: year_from must be less than or equal to year_to.",
        );
        push_unique_text(
            &mut warnings,
            "invalid_year_range: year_from is after year_to.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Correct the year range before connector planning can continue.",
        );
    }

    let mut normalized_max_results_per_source = request.max_results_per_source.unwrap_or(25);
    if normalized_max_results_per_source == 0 {
        normalized_max_results_per_source = 1;
        push_unique_text(
            &mut warnings,
            "max_results_per_source=0 was clamped to 1.",
        );
    } else if normalized_max_results_per_source > 200 {
        normalized_max_results_per_source = 200;
        push_unique_text(
            &mut warnings,
            "max_results_per_source above 200 was clamped to 200.",
        );
    }

    if !unknown_metadata_sources.is_empty() {
        push_unique_text(
            &mut warnings,
            &format!(
                "Unknown metadata source hints are preserved as preview-only hints: {}.",
                unknown_metadata_sources.join(", ")
            ),
        );
    }

    let status = if normalized_query.is_empty()
        || year_range_invalid
        || matches!(search_plan_status, ScholarChatScientificSearchPlanStatus::Blocked)
    {
        ScholarChatMetadataConnectorPlanStatus::Blocked
    } else if matches!(
        search_plan_status,
        ScholarChatScientificSearchPlanStatus::NeedsDisambiguation
    ) {
        ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation
    } else if matches!(
        search_plan_status,
        ScholarChatScientificSearchPlanStatus::UnknownConcept
    ) {
        ScholarChatMetadataConnectorPlanStatus::UnknownConcept
    } else if selected_metadata_sources.is_empty() {
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    } else {
        ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady
    };

    if matches!(status, ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation) {
        push_unique_text(
            &mut warnings,
            "The scientific search plan still needs disambiguation before metadata connector planning can continue.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Narrow the scientific concept before metadata connector planning can continue.",
        );
    }

    if matches!(status, ScholarChatMetadataConnectorPlanStatus::UnknownConcept) {
        push_unique_text(
            &mut warnings,
            "The query still does not map to a known scientific concept for metadata connector planning.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Add discipline and source registry mappings before metadata connector planning can continue.",
        );
    }

    if matches!(status, ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource) {
        push_unique_text(
            &mut warnings,
            "No supported metadata source selected.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Choose OpenAlex or Crossref before later metadata connector planning can continue.",
        );
    }

    match normalized_mode.as_str() {
        "scientific_paper" => {
            push_unique_text(
                &mut warnings,
                "Scientific Paper Mode will later prioritize citation-safe metadata, DOI deduplication, and result-shape mapping.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Future Scientific Paper Mode phases should prioritize citation-safe metadata, DOI deduplication, and result-shape mapping.",
            );
        }
        "course" => {
            push_unique_text(
                &mut warnings,
                "Course Mode will later prioritize course-context metadata and reading alignment.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Future Course Mode phases should prioritize course-context metadata and local reading alignment.",
            );
        }
        _ => {
            push_unique_text(
                &mut warnings,
                "Scholar Chat metadata planning stays balanced and local evidence will still be required later.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Later Scholar Chat phases should still gather local evidence before answering.",
            );
        }
    }

    if !unknown_metadata_sources.is_empty() && !selected_metadata_sources.is_empty() {
        push_unique_text(
            &mut warnings,
            "Unknown metadata sources are preserved as preview-only hints while supported sources remain selected.",
        );
    }

    let mut planned_metadata_queries = if search_plan_preview.planned_metadata_queries.is_empty() {
        vec![normalized_query.clone()]
    } else {
        search_plan_preview.planned_metadata_queries.clone()
    };
    planned_metadata_queries.extend(search_plan_preview.planned_expanded_queries.iter().cloned());
    planned_metadata_queries.retain(|value| !value.trim().is_empty());
    planned_metadata_queries.sort();
    planned_metadata_queries.dedup();

    let require_doi = request.require_doi.unwrap_or(false);
    let require_open_access = request.require_open_access.unwrap_or(false);
    let openalex_filters = {
        let mut filters = Vec::new();
        if require_open_access {
            filters.push("open_access=true".to_string());
        }
        if require_doi {
            filters.push("has_doi=true".to_string());
        }
        if let Some(year_from) = request.year_from {
            filters.push(format!("from_publication_year={year_from}"));
        }
        if let Some(year_to) = request.year_to {
            filters.push(format!("to_publication_year={year_to}"));
        }
        filters.push(format!("mode={normalized_mode}"));
        if let Some(recognized_concept) = recognized_concept.as_deref() {
            filters.push(format!("inferred_concept={recognized_concept}"));
        }
        filters.sort();
        filters.dedup();
        filters
    };
    let crossref_filters = {
        let mut filters = Vec::new();
        if require_doi {
            filters.push("has_doi=true".to_string());
        }
        if let Some(year_from) = request.year_from {
            filters.push(format!("from_pub_date={year_from}"));
        }
        if let Some(year_to) = request.year_to {
            filters.push(format!("until_pub_date={year_to}"));
        }
        filters.push(format!("mode={normalized_mode}"));
        if let Some(recognized_concept) = recognized_concept.as_deref() {
            filters.push(format!("inferred_concept={recognized_concept}"));
        }
        if require_open_access {
            filters.push("open_access_filter_not_native".to_string());
        }
        filters.sort();
        filters.dedup();
        filters
    };

    let openalex_enabled = selected_metadata_sources.iter().any(|source| source == "openalex");
    let crossref_enabled = selected_metadata_sources.iter().any(|source| source == "crossref");
    let preferred_source_order = selected_metadata_sources.clone();
    let connector_strategy = if matches!(status, ScholarChatMetadataConnectorPlanStatus::Blocked) {
        ScholarChatMetadataConnectorStrategy::Blocked
    } else if matches!(
        status,
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    ) || selected_metadata_sources.is_empty()
    {
        ScholarChatMetadataConnectorStrategy::MetadataSourceSelectionNeeded
    } else if selected_metadata_sources.len() == 2 {
        ScholarChatMetadataConnectorStrategy::OpenalexAndCrossref
    } else if openalex_enabled {
        ScholarChatMetadataConnectorStrategy::OpenalexFirst
    } else {
        ScholarChatMetadataConnectorStrategy::CrossrefFirst
    };

    let openalex_plan = metadata_connector_query_plan(
        "openalex",
        openalex_enabled,
        "openalex_works",
        planned_metadata_queries.clone(),
        openalex_filters,
        normalized_max_results_per_source,
        vec![
            "id".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "display_name".to_string(),
            "publication_year".to_string(),
            "authorships".to_string(),
            "primary_location".to_string(),
            "open_access".to_string(),
            "cited_by_count".to_string(),
            "concepts".to_string(),
        ],
    );
    let crossref_plan = metadata_connector_query_plan(
        "crossref",
        crossref_enabled,
        "crossref_works",
        planned_metadata_queries.clone(),
        crossref_filters,
        normalized_max_results_per_source,
        vec![
            "DOI".to_string(),
            "title".to_string(),
            "author".to_string(),
            "issued".to_string(),
            "container-title".to_string(),
            "publisher".to_string(),
            "type".to_string(),
            "URL".to_string(),
            "is-referenced-by-count".to_string(),
        ],
    );
    let source_selection_plan = metadata_connector_source_selection_plan(
        normalized_requested_metadata_sources.clone(),
        selected_metadata_sources.clone(),
        unknown_metadata_sources.clone(),
    );
    let deduplication_plan = metadata_connector_deduplication_plan(
        preferred_source_order.clone(),
        require_doi,
    );
    let attribution_plan = metadata_connector_attribution_plan();
    let compliance_plan = metadata_connector_compliance_plan();
    let step_active = !matches!(status, ScholarChatMetadataConnectorPlanStatus::Blocked);
    let planned_connector_steps = vec![
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::QueryUnderstandingAlignment,
            "query_understanding_alignment",
            "Query understanding alignment",
            "Align the metadata connector plan with the existing scientific search plan preview only.",
            vec![
                normalized_query.clone(),
                normalized_mode.clone(),
                normalized_context_tags.join(", "),
                selected_local_source_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                format!("{:?}", search_plan_status),
                format!("{:?}", query_understanding_status),
                inferred_topic.clone().unwrap_or_default(),
                format!("{:?}", query_intent),
                recognized_concept.clone().unwrap_or_default(),
                label.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::MetadataSourceSelection,
            "metadata_source_selection",
            "Metadata source selection",
            "Select OpenAlex and Crossref later without connector calls or metadata writes.",
            vec![
                normalized_requested_metadata_sources.join(", "),
                selected_metadata_sources.join(", "),
                unknown_metadata_sources.join(", "),
                expected_source_kinds.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                source_selection_plan.selected_sources.join(", "),
                format!("needs_source_selection={}", source_selection_plan.needs_source_selection),
            ],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::OpenalexQueryPlan,
            "openalex_query_plan",
            "OpenAlex query plan",
            "Plan OpenAlex metadata queries only; no API call, URL fetch, scraping, or metadata write occurs.",
            vec![
                openalex_plan.planned_query_terms.join(", "),
                openalex_plan.planned_filters.join(", "),
                openalex_plan.max_results.to_string(),
            ],
            vec![
                "openalex_query_plan_preview".to_string(),
                "openalex_works_result_shape_preview".to_string(),
            ],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::CrossrefQueryPlan,
            "crossref_query_plan",
            "Crossref query plan",
            "Plan Crossref metadata queries only; no API call, URL fetch, scraping, or metadata write occurs.",
            vec![
                crossref_plan.planned_query_terms.join(", "),
                crossref_plan.planned_filters.join(", "),
                crossref_plan.max_results.to_string(),
            ],
            vec![
                "crossref_query_plan_preview".to_string(),
                "crossref_works_result_shape_preview".to_string(),
            ],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::ResultShapeMappingPlan,
            "result_shape_mapping_plan",
            "Result shape mapping plan",
            "Plan how OpenAlex and Crossref result shapes would later be aligned for downstream evidence use.",
            vec![
                openalex_plan.expected_result_fields.join(", "),
                crossref_plan.expected_result_fields.join(", "),
                expected_source_kinds.join(", "),
            ],
            vec!["metadata_result_shape_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::DoiFilterPlan,
            "doi_filter_plan",
            "DOI filter plan",
            "Plan DOI handling only; no connector call or metadata record write occurs.",
            vec![
                format!("require_doi={require_doi}"),
                openalex_plan.planned_filters.join(", "),
                crossref_plan.planned_filters.join(", "),
            ],
            vec!["doi_filter_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::OpenAccessFilterPlan,
            "open_access_filter_plan",
            "Open access filter plan",
            "Plan open-access handling only; no API call or web request occurs.",
            vec![
                format!("require_open_access={require_open_access}"),
                openalex_plan.planned_filters.join(", "),
                crossref_plan.planned_filters.join(", "),
            ],
            vec!["open_access_filter_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::YearFilterPlan,
            "year_filter_plan",
            "Year filter plan",
            "Plan publication-year filtering only; no date-based web request occurs.",
            vec![
                request.year_from.map(|value| value.to_string()).unwrap_or_default(),
                request.year_to.map(|value| value.to_string()).unwrap_or_default(),
                openalex_plan.planned_filters.join(", "),
                crossref_plan.planned_filters.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["year_filter_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::DeduplicationPlan,
            "deduplication_plan",
            "Deduplication plan",
            "Plan deduplication only; no connector call, evidence pack build, or artifact write occurs.",
            vec![
                deduplication_plan.key_fields.join(", "),
                deduplication_plan.preferred_source_order.join(", "),
                format!("doi_required={}", deduplication_plan.doi_required),
                format!(
                    "title_year_fallback_allowed={}",
                    deduplication_plan.title_year_fallback_allowed
                ),
            ],
            vec!["deduplication_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::AttributionRequirementCheck,
            "attribution_requirement_check",
            "Attribution requirement check",
            "Plan metadata attribution only; no fabricated citations are emitted in this preview.",
            vec![
                attribution_plan.required_attribution_notes.join(", "),
                attribution_plan.planned_user_agent_note.clone(),
                attribution_plan.citation_safety_notes.join(", "),
            ],
            vec!["attribution_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::ComplianceBoundaryCheck,
            "compliance_boundary_check",
            "Compliance boundary check",
            "Confirm external-metadata-only boundaries without any external call.",
            vec![
                format!(
                    "external_metadata_only={}",
                    compliance_plan.external_metadata_only
                ),
                format!("no_fulltext_download={}", compliance_plan.no_fulltext_download),
                format!("no_scraping={}", compliance_plan.no_scraping),
                format!(
                    "respects_rate_limits_later={}",
                    compliance_plan.respects_rate_limits_later
                ),
                format!(
                    "requires_terms_review_later={}",
                    compliance_plan.requires_terms_review_later
                ),
            ],
            vec!["compliance_preview".to_string()],
            step_active,
        ),
        metadata_connector_step(
            ScholarChatMetadataConnectorStepKind::DownstreamEvidencePackAlignment,
            "downstream_evidence_pack_alignment",
            "Downstream Evidence Pack alignment",
            "Plan downstream Evidence Pack alignment only; no Evidence Pack is created now.",
            vec![
                format!("{:?}", search_plan_status),
                format!("{:?}", query_intent),
                recognized_concept.clone().unwrap_or_default(),
                expected_source_kinds.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_evidence_pack_alignment_preview".to_string()],
            step_active,
        ),
    ];

    let summary = match status {
        ScholarChatMetadataConnectorPlanStatus::Blocked => {
            "OpenAlex/Crossref metadata connector preview blocked because the query is blank, the year range is invalid, or the scientific search plan is blocked."
                .to_string()
        }
        ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation => {
            "OpenAlex/Crossref metadata connector preview needs disambiguation before later connector planning can continue."
                .to_string()
        }
        ScholarChatMetadataConnectorPlanStatus::UnknownConcept => {
            "OpenAlex/Crossref metadata connector preview found an unknown concept and can only outline later metadata planning steps."
                .to_string()
        }
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource => {
            "OpenAlex/Crossref metadata connector preview is waiting for a supported metadata source selection."
                .to_string()
        }
        ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady => {
            "OpenAlex/Crossref metadata connector preview is ready later and only describes future metadata query planning, deduplication, attribution, and compliance."
                .to_string()
        }
    };

    Ok(ScholarChatMetadataConnectorPlanPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids,
        expected_source_kinds,
        preferred_metadata_sources: selected_metadata_sources.clone(),
        unknown_metadata_sources,
        normalized_max_results_per_source,
        require_open_access,
        require_doi,
        year_from: request.year_from,
        year_to: request.year_to,
        search_plan_status,
        query_understanding_status,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        connector_strategy,
        source_selection_plan,
        openalex_plan,
        crossref_plan,
        deduplication_plan,
        attribution_plan,
        compliance_plan,
        planned_metadata_queries,
        planned_connector_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        metadata_connector_plan_preview_only: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_psychology_source_connector_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatPsychologySourceConnectorPlanRequest,
) -> AegisResult<ScholarChatPsychologySourceConnectorPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let expected_source_kinds = normalize_scientific_expected_source_kinds(
        request.expected_source_kinds.clone(),
    );
    let preferred_metadata_sources =
        normalize_metadata_connector_sources(request.preferred_metadata_sources.clone());
    let normalized_preferred_psychology_source_families = normalize_psychology_connector_values(
        request.preferred_psychology_source_families.clone(),
    );
    let methodology_hints = normalize_psychology_connector_values(request.methodology_hints.clone());
    let population_hints = normalize_psychology_connector_values(request.population_hints.clone());
    let topic_area_hints = normalize_psychology_connector_values(request.topic_area_hints.clone());

    let metadata_connector_plan_preview = preview_scholar_chat_metadata_connector_plan(
        &root,
        ScholarChatMetadataConnectorPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
            expected_source_kinds: Some(expected_source_kinds.clone()),
            preferred_metadata_sources: Some(preferred_metadata_sources.clone()),
            max_results_per_source: request.max_results_per_source,
            require_open_access: request.require_open_access,
            require_doi: request.require_doi,
            year_from: request.year_from,
            year_to: request.year_to,
        },
    )?;

    let metadata_connector_plan_status = metadata_connector_plan_preview.status.clone();
    let metadata_connector_strategy = metadata_connector_plan_preview.connector_strategy.clone();
    let scientific_search_plan_status = metadata_connector_plan_preview.search_plan_status.clone();
    let query_understanding_status = metadata_connector_plan_preview.query_understanding_status.clone();
    let inferred_topic = metadata_connector_plan_preview.inferred_topic.clone();
    let query_intent = metadata_connector_plan_preview.query_intent.clone();
    let recognized_concept = metadata_connector_plan_preview.recognized_concept.clone();
    let label = metadata_connector_plan_preview.label.clone();

    let combined_scope_text = [
        normalized_query.as_str(),
        normalized_mode.as_str(),
        request.course_context.as_deref().unwrap_or(""),
        &normalized_context_tags.join(" "),
        &methodology_hints.join(" "),
        &population_hints.join(" "),
        &topic_area_hints.join(" "),
        recognized_concept.as_deref().unwrap_or(""),
        label.as_deref().unwrap_or(""),
    ]
    .join(" ")
    .to_lowercase();

    let psychology_scope_found = [
        "psychology",
        "psychophysics",
        "signal_detection",
        "signalentdeckung",
        "hypothesentests",
        "anova",
        "perception",
        "cognitive",
        "clinical",
        "developmental",
        "social",
        "educational",
        "neuroscience",
        "behavior",
        "behaviour",
        "experiment",
        "survey",
        "hypothesis",
    ]
    .iter()
    .any(|marker| combined_scope_text.contains(marker));

    let requested_psychology_source_families = normalized_preferred_psychology_source_families;
    let supported_psychology_source_families = psychology_supported_source_families();
    let selected_psychology_source_families = if requested_psychology_source_families.is_empty() {
        psychology_default_source_families(&normalized_mode)
    } else {
        supported_psychology_source_families
            .iter()
            .filter(|family| requested_psychology_source_families.contains(family))
            .cloned()
            .collect::<Vec<_>>()
    };
    let unknown_psychology_source_families = if requested_psychology_source_families.is_empty() {
        Vec::new()
    } else {
        requested_psychology_source_families
            .iter()
            .filter(|family| !supported_psychology_source_families.contains(family))
            .cloned()
            .collect::<Vec<_>>()
    };

    let psychology_scope_status = if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::Blocked
    ) || normalized_query.is_empty() {
        ScholarChatPsychologyScopeStatus::Blocked
    } else if psychology_scope_found {
        ScholarChatPsychologyScopeStatus::PsychologyScopeReady
    } else {
        ScholarChatPsychologyScopeStatus::NeedsPsychologyScope
    };

    let warnings = metadata_connector_plan_preview.warnings.clone();
    let blockers = metadata_connector_plan_preview.blockers.clone();
    let next_required_actions = metadata_connector_plan_preview.next_required_actions.clone();

    let mut psychology_scope_warnings = Vec::new();
    let mut psychology_scope_actions = Vec::new();

    if matches!(psychology_scope_status, ScholarChatPsychologyScopeStatus::NeedsPsychologyScope) {
        psychology_scope_warnings.push(
            "Add psychology topic, method, population, or course context before psychology source connector planning can continue."
                .to_string(),
        );
        psychology_scope_actions.push(
            "Provide a psychology topic, method, population, or course context so the psychology source connector preview can become ready later."
                .to_string(),
        );
    }

    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation
    ) {
        psychology_scope_warnings.push(
            "The scientific search plan still needs disambiguation before psychology source connector planning can continue."
                .to_string(),
        );
        psychology_scope_actions.push(
            "Narrow the scientific concept before psychology source connector planning can continue."
                .to_string(),
        );
    }

    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::UnknownConcept
    ) {
        psychology_scope_warnings.push(
            "The query still does not map to a known scientific concept for psychology source connector planning."
                .to_string(),
        );
        psychology_scope_actions.push(
            "Add discipline and source registry mappings before psychology source connector planning can continue."
                .to_string(),
        );
    }

    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    ) {
        psychology_scope_warnings.push(
            "Choose OpenAlex or Crossref before psychology source connector planning can continue."
                .to_string(),
        );
        psychology_scope_actions.push(
            "Select a supported metadata connector source before psychology source connector planning can continue."
                .to_string(),
        );
    }

    if !requested_psychology_source_families.is_empty() && selected_psychology_source_families.is_empty() {
        psychology_scope_warnings.push(
            "No supported psychology source family remains after normalization.".to_string(),
        );
        psychology_scope_actions.push(
            "Choose one of the supported psychology source families before connector planning can continue."
                .to_string(),
        );
    }

    let supported_methodology_hints = [
        "experiment",
        "survey",
        "meta_analysis",
        "systematic_review",
        "psychophysics",
        "signal_detection",
        "quantitative",
        "qualitative",
        "mixed_methods",
        "longitudinal",
        "clinical_trial",
        "observational",
    ];
    let unknown_methodology_hints = methodology_hints
        .iter()
        .filter(|hint| !supported_methodology_hints.contains(&hint.as_str()))
        .cloned()
        .collect::<Vec<_>>();
    let mut inferred_methodology_routes = BTreeSet::new();
    let mut recommended_source_families = BTreeSet::new();

    let has_signal_detection_scope = [
        normalized_query.as_str(),
        normalized_mode.as_str(),
        request.course_context.as_deref().unwrap_or(""),
        recognized_concept.as_deref().unwrap_or(""),
        label.as_deref().unwrap_or(""),
        &normalized_context_tags.join(" "),
        &topic_area_hints.join(" "),
        &methodology_hints.join(" "),
    ]
    .join(" ")
    .contains("signal");
    let has_hypothesis_or_anova_scope = [
        normalized_query.as_str(),
        normalized_mode.as_str(),
        request.course_context.as_deref().unwrap_or(""),
        recognized_concept.as_deref().unwrap_or(""),
        label.as_deref().unwrap_or(""),
        &normalized_context_tags.join(" "),
        &topic_area_hints.join(" "),
        &methodology_hints.join(" "),
    ]
    .join(" ")
    .contains("anova")
        || [
            normalized_query.as_str(),
            normalized_mode.as_str(),
            request.course_context.as_deref().unwrap_or(""),
            recognized_concept.as_deref().unwrap_or(""),
            label.as_deref().unwrap_or(""),
            &normalized_context_tags.join(" "),
            &topic_area_hints.join(" "),
            &methodology_hints.join(" "),
        ]
        .join(" ")
        .contains("hypothes");

    if methodology_hints.is_empty() && has_signal_detection_scope {
        inferred_methodology_routes.insert("psychophysics".to_string());
        inferred_methodology_routes.insert("signal_detection".to_string());
        recommended_source_families.insert("openalex_psychology".to_string());
        recommended_source_families.insert("pubmed_psychology".to_string());
    }
    if methodology_hints.is_empty() && has_hypothesis_or_anova_scope {
        inferred_methodology_routes.insert("quantitative".to_string());
        inferred_methodology_routes.insert("statistics_methods".to_string());
        recommended_source_families.insert("openalex_psychology".to_string());
        recommended_source_families.insert("crossref_psychology".to_string());
    }
    for hint in &methodology_hints {
        match hint.as_str() {
            "experiment" | "quantitative" => {
                inferred_methodology_routes.insert("quantitative".to_string());
                recommended_source_families.insert("openalex_psychology".to_string());
                recommended_source_families.insert("crossref_psychology".to_string());
            }
            "survey" | "longitudinal" | "observational" | "qualitative" | "mixed_methods" => {
                inferred_methodology_routes.insert(hint.clone());
                recommended_source_families.insert("openalex_psychology".to_string());
                recommended_source_families.insert("crossref_psychology".to_string());
            }
            "meta_analysis" | "systematic_review" => {
                inferred_methodology_routes.insert(hint.clone());
                recommended_source_families.insert("openalex_psychology".to_string());
                recommended_source_families.insert("crossref_psychology".to_string());
                recommended_source_families.insert("pubmed_psychology".to_string());
            }
            "psychophysics" | "signal_detection" => {
                inferred_methodology_routes.insert(hint.clone());
                recommended_source_families.insert("openalex_psychology".to_string());
                recommended_source_families.insert("pubmed_psychology".to_string());
            }
            "clinical_trial" | "clinical" => {
                inferred_methodology_routes.insert("clinical_trial".to_string());
                recommended_source_families.insert("pubmed_psychology".to_string());
            }
            "educational" | "course" => {
                inferred_methodology_routes.insert("educational".to_string());
                recommended_source_families.insert("eric_psychology".to_string());
                recommended_source_families.insert("local_course_literature".to_string());
            }
            other => {
                psychology_scope_warnings.push(format!(
                    "Unknown psychology methodology hint preserved as preview-only hint: {other}."
                ));
            }
        }
    }
    if inferred_methodology_routes.is_empty() && has_signal_detection_scope {
        inferred_methodology_routes.insert("psychophysics".to_string());
        inferred_methodology_routes.insert("signal_detection".to_string());
    }
    if inferred_methodology_routes.is_empty() && has_hypothesis_or_anova_scope {
        inferred_methodology_routes.insert("quantitative".to_string());
        inferred_methodology_routes.insert("statistics_methods".to_string());
    }
    let inferred_methodology_routes = inferred_methodology_routes.into_iter().collect::<Vec<_>>();
    let recommended_source_families_from_methodology =
        recommended_source_families.into_iter().collect::<Vec<_>>();

    let mut population_filters_later = BTreeSet::new();
    let mut ethics_or_safety_notes = BTreeSet::new();
    for hint in &population_hints {
        population_filters_later.insert(format!("population_filter:{hint}"));
        if hint == "clinical_population" {
            ethics_or_safety_notes.insert(
                "clinical_population_requires_later_ethics_or_safety_review".to_string(),
            );
        }
    }
    let population_filters_later = population_filters_later.into_iter().collect::<Vec<_>>();
    let ethics_or_safety_notes = ethics_or_safety_notes.into_iter().collect::<Vec<_>>();

    let mut inferred_topic_areas = BTreeSet::new();
    if has_signal_detection_scope {
        inferred_topic_areas.insert("cognitive_psychology".to_string());
        inferred_topic_areas.insert("perception".to_string());
        inferred_topic_areas.insert("psychophysics".to_string());
    }
    if has_hypothesis_or_anova_scope {
        inferred_topic_areas.insert("research_methods".to_string());
        inferred_topic_areas.insert("statistics_methods".to_string());
    }
    for hint in &topic_area_hints {
        inferred_topic_areas.insert(hint.clone());
    }
    if normalized_mode == "course" {
        inferred_topic_areas.insert("course_context_alignment".to_string());
    }
    let inferred_topic_areas = inferred_topic_areas.into_iter().collect::<Vec<_>>();

    let mut topic_source_families = BTreeSet::new();
    for family in &recommended_source_families_from_methodology {
        topic_source_families.insert(family.clone());
    }
    if inferred_topic_areas
        .iter()
        .any(|topic| topic == "course_context_alignment")
    {
        topic_source_families.insert("local_course_literature".to_string());
    }
    let recommended_source_families_for_topic =
        topic_source_families.into_iter().collect::<Vec<_>>();

    let _methodology_routing_plan = ScholarChatPsychologyMethodologyRoutingPlan {
        methodology_hints: methodology_hints.clone(),
        unknown_methodology_hints: unknown_methodology_hints.clone(),
        inferred_methodology_routes: inferred_methodology_routes.clone(),
        recommended_source_families: recommended_source_families_from_methodology.clone(),
        summary: if methodology_hints.is_empty() {
            "Psychology methodology routing stays preview-only and infers later route hints from the topic context when possible."
                .to_string()
        } else {
            "Psychology methodology routing stays preview-only and only plans later route hints without calling connectors."
                .to_string()
        },
    };

    let _population_routing_plan = ScholarChatPsychologyPopulationRoutingPlan {
        population_hints: population_hints.clone(),
        recommended_filters_later: population_filters_later,
        ethics_or_safety_notes,
        summary: "Population routing is preview-only and only plans later study-population filters and ethics notes."
            .to_string(),
    };

    let _topic_area_routing_plan = ScholarChatPsychologyTopicAreaRoutingPlan {
        topic_area_hints: topic_area_hints.clone(),
        inferred_topic_areas: inferred_topic_areas.clone(),
        recommended_source_families: recommended_source_families_for_topic.clone(),
        summary: "Psychology topic-area routing stays preview-only and only plans later topic-area source alignment."
            .to_string(),
    };

    let evidence_requirements_plan = ScholarChatPsychologyEvidenceRequirementsPlan {
        evidence_requirements: vec![
            "peer_review_or_scholarly_source_status_later".to_string(),
            "DOI_or_stable_metadata_identifier_later".to_string(),
            "publication_year_later".to_string(),
            "authorship_later".to_string(),
            "journal_or_container_later".to_string(),
            "methodology_metadata_later".to_string(),
            "population_metadata_later_when_available".to_string(),
            "no_fabricated_citations".to_string(),
            "evidence_pack_required_before_answering".to_string(),
        ],
        preferred_metadata_fields: vec![
            "doi".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "source_family".to_string(),
            "journal_or_container".to_string(),
            "abstract_or_summary_when_legally_available".to_string(),
            "methodology_keywords".to_string(),
            "population_keywords".to_string(),
            "open_access_status".to_string(),
            "provider_record_id".to_string(),
        ],
        quality_signals: vec![
            "DOI/stable id".to_string(),
            "peer-reviewed venue later".to_string(),
            "citation count metadata later".to_string(),
            "methodology match".to_string(),
            "topic-area match".to_string(),
            "local-course relevance when course mode".to_string(),
        ],
        exclusion_notes: vec![
            "exclude fabricated sources".to_string(),
            "exclude unverified citations".to_string(),
            "exclude inaccessible full text from automated import".to_string(),
            "exclude paywalled full text scraping".to_string(),
            "exclude non-psychology sources unless explicitly requested".to_string(),
        ],
        downstream_evidence_pack_requirements: vec![
            "evidence_pack_required_before_answering".to_string(),
            "later_source_grounding_alignment".to_string(),
        ],
        summary: "Evidence requirements remain preview-only and only plan later metadata quality, citation safety, and Evidence Pack alignment."
            .to_string(),
    };

    let mut requires_institutional_access_review_later = false;
    let mut selected_psychology_source_families_sorted = selected_psychology_source_families.clone();
    selected_psychology_source_families_sorted.sort();
    selected_psychology_source_families_sorted.dedup();
    if selected_psychology_source_families_sorted
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        requires_institutional_access_review_later = true;
        psychology_scope_warnings.push(
            "APA PsycNet / PsycINFO planning remains a boundary only; no automated authenticated scraping or paywall bypass is attempted, and later manual or institutional access review is required."
                .to_string(),
        );
        psychology_scope_actions.push(
            "Review APA PsycNet / PsycINFO access policies later before any automated use."
                .to_string(),
        );
    }

    let compliance_plan = ScholarChatPsychologyConnectorCompliancePlan {
        external_metadata_only: true,
        no_fulltext_download: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        respects_rate_limits_later: true,
        requires_terms_review_later: true,
        requires_institutional_access_review_later,
        will_call_external_services: false,
        will_write_metadata: false,
        summary: if requires_institutional_access_review_later {
            "Psychology connector compliance is preview-only, external-metadata-only, and keeps APA PsycNet / PsycINFO as a later access-review boundary."
                .to_string()
        } else {
            "Psychology connector compliance is preview-only, external-metadata-only, and does not call external services."
                .to_string()
        },
    };

    let _source_family_selection_plan = ScholarChatPsychologySourceFamilySelectionPlan {
        requested_families: requested_psychology_source_families.clone(),
        supported_families: selected_psychology_source_families_sorted.clone(),
        unknown_families: unknown_psychology_source_families.clone(),
        selected_families: selected_psychology_source_families_sorted.clone(),
        needs_source_family_selection: !requested_psychology_source_families.is_empty()
            && selected_psychology_source_families_sorted.is_empty(),
        summary: if requested_psychology_source_families.is_empty() {
            format!(
                "Psychology source families default later to {} for mode {normalized_mode}.",
                selected_psychology_source_families_sorted.join(", ")
            )
        } else if selected_psychology_source_families_sorted.is_empty() {
            "No supported psychology source family remains after normalization; choose one later."
                .to_string()
        } else if unknown_psychology_source_families.is_empty() {
            "Psychology source family preferences were normalized and selected later without connector calls."
                .to_string()
        } else {
            "Supported psychology source families are selected later while unknown family hints remain preview-only."
                .to_string()
        },
    };

    let family_plan_values = psychology_supported_source_families()
        .into_iter()
        .map(|family_id| {
            let enabled = selected_psychology_source_families_sorted
                .iter()
                .any(|selected| selected == &family_id);
            let mut planned_filters = vec![
                format!("family_id={family_id}"),
                format!("mode={normalized_mode}"),
                format!("query={normalized_query}"),
            ];
            if request.require_open_access.unwrap_or(false) {
                planned_filters.push("require_open_access=true".to_string());
            }
            if request.require_doi.unwrap_or(false) {
                planned_filters.push("require_doi=true".to_string());
            }
            if let Some(year_from) = request.year_from {
                planned_filters.push(format!("year_from={year_from}"));
            }
            if let Some(year_to) = request.year_to {
                planned_filters.push(format!("year_to={year_to}"));
            }
            if !normalized_context_tags.is_empty() {
                planned_filters.push(format!("context_tags={}", normalized_context_tags.join(", ")));
            }
            if !methodology_hints.is_empty() {
                planned_filters.push(format!("methodology_hints={}", methodology_hints.join(", ")));
            }
            if !population_hints.is_empty() {
                planned_filters.push(format!("population_hints={}", population_hints.join(", ")));
            }
            if !topic_area_hints.is_empty() {
                planned_filters.push(format!("topic_area_hints={}", topic_area_hints.join(", ")));
            }
            planned_filters.sort();
            planned_filters.dedup();
            ScholarChatPsychologySourceFamilyPlan {
                family_id: family_id.clone(),
                enabled,
                source_family_label: psychology_source_family_label(&family_id).to_string(),
                connector_kind: psychology_source_family_connector_kind(&family_id).to_string(),
                planned_query_terms: if normalized_query.is_empty() {
                    vec!["psychology_preview_query".to_string()]
                } else {
                    vec![normalized_query.clone()]
                },
                planned_filters,
                planned_metadata_dependencies: psychology_source_family_metadata_dependencies(&family_id),
                expected_result_types: psychology_source_family_expected_result_types(&family_id),
                evidence_relevance: psychology_source_family_evidence_relevance(&family_id),
                access_boundary_notes: psychology_source_family_access_boundary_notes(&family_id),
                will_call_connector: false,
                will_fetch_url: false,
                will_scrape: false,
                will_authenticate: false,
                will_bypass_paywall: false,
                will_write_metadata: false,
                summary: if family_id == "apa_psycnet_discovery" {
                    "APA PsycNet / PsycINFO remains a planning boundary only; later manual or institutional access review is required."
                        .to_string()
                } else if family_id == "local_course_literature" {
                    "Local course literature remains a planning boundary only; no file read or local index access occurs now."
                        .to_string()
                } else {
                    format!(
                        "{} remains preview-only and does not call a connector or write metadata now.",
                        psychology_source_family_label(&family_id)
                    )
                },
            }
        })
        .collect::<Vec<_>>();

    let mut planned_source_connector_steps = Vec::new();
    let step_active = !matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::Blocked
    );
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::MetadataConnectorAlignment,
        "metadata_connector_alignment",
        "Metadata connector alignment",
        "Align the psychology connector preview with the existing OpenAlex/Crossref metadata connector preview only.",
        vec![
            format!("{:?}", metadata_connector_plan_status),
            format!("{:?}", metadata_connector_strategy),
            preferred_metadata_sources.join(", "),
        ],
        vec!["metadata_connector_alignment_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::PsychologyScopeClassification,
        "psychology_scope_classification",
        "Psychology scope classification",
        "Classify whether the query is psychology-related using deterministic preview-only heuristics.",
        vec![
            normalized_query.clone(),
            normalized_mode.clone(),
            request.course_context.clone().unwrap_or_default(),
            normalized_context_tags.join(", "),
            methodology_hints.join(", "),
            population_hints.join(", "),
            topic_area_hints.join(", "),
            recognized_concept.clone().unwrap_or_default(),
            label.clone().unwrap_or_default(),
        ]
        .into_iter()
        .filter(|value| !value.is_empty())
        .collect(),
        vec!["psychology_scope_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::SourceFamilySelection,
        "source_family_selection",
        "Source family selection",
        "Select psychology source families later without any connector calls or metadata writes.",
        vec![
            requested_psychology_source_families.join(", "),
            selected_psychology_source_families_sorted.join(", "),
            unknown_psychology_source_families.join(", "),
        ]
        .into_iter()
        .filter(|value| !value.is_empty())
        .collect(),
        vec!["source_family_selection_preview".to_string()],
        step_active,
    ));

    for family_id in [
        "openalex_psychology",
        "crossref_psychology",
        "pubmed_psychology",
        "eric_psychology",
        "apa_psycnet_discovery",
        "local_course_literature",
    ] {
        let family_name = psychology_source_family_label(family_id).to_string();
        planned_source_connector_steps.push(psychology_source_connector_step(
            match family_id {
                "openalex_psychology" => ScholarChatPsychologySourceConnectorStepKind::OpenalexPsychologyPlan,
                "crossref_psychology" => ScholarChatPsychologySourceConnectorStepKind::CrossrefPsychologyPlan,
                "pubmed_psychology" => ScholarChatPsychologySourceConnectorStepKind::PubmedPsychologyPlan,
                "eric_psychology" => ScholarChatPsychologySourceConnectorStepKind::EricPsychologyPlan,
                "apa_psycnet_discovery" => ScholarChatPsychologySourceConnectorStepKind::ApaPsycnetDiscoveryPlan,
                _ => ScholarChatPsychologySourceConnectorStepKind::LocalCourseLiteraturePlan,
            },
            &format!("{}_plan", family_id),
            &family_name,
            &format!("{family_name} remains preview-only and does not call a connector now."),
            vec![
                normalized_query.clone(),
                normalized_mode.clone(),
                requested_psychology_source_families.join(", "),
                methodology_hints.join(", "),
                population_hints.join(", "),
                topic_area_hints.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![format!("{}_preview", family_id)],
            step_active,
        ));
    }
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::MethodologyRouting,
        "methodology_routing",
        "Methodology routing",
        "Route psychology methodology hints later without connector calls or metadata writes.",
        vec![
            methodology_hints.join(", "),
            inferred_methodology_routes.join(", "),
        ]
        .into_iter()
        .filter(|value| !value.is_empty())
        .collect(),
        vec!["methodology_routing_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::PopulationRouting,
        "population_routing",
        "Population routing",
        "Route study population hints later without any user-identity inference.",
        vec![population_hints.join(", ")],
        vec!["population_routing_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::TopicAreaRouting,
        "topic_area_routing",
        "Topic area routing",
        "Route psychology topic areas later without running retrieval or connector calls.",
        vec![
            topic_area_hints.join(", "),
            inferred_topic_areas.join(", "),
        ]
        .into_iter()
        .filter(|value| !value.is_empty())
        .collect(),
        vec!["topic_area_routing_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::EvidenceRequirementMapping,
        "evidence_requirement_mapping",
        "Evidence requirement mapping",
        "Map psychology evidence requirements later without building an Evidence Pack now.",
        vec![
            evidence_requirements_plan.evidence_requirements.join(", "),
            evidence_requirements_plan.preferred_metadata_fields.join(", "),
            evidence_requirements_plan.quality_signals.join(", "),
        ],
        vec!["evidence_requirement_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::ComplianceBoundaryCheck,
        "compliance_boundary_check",
        "Compliance boundary check",
        "Confirm psychology metadata planning boundaries without any external call.",
        vec![
            format!("external_metadata_only={}", compliance_plan.external_metadata_only),
            format!("no_fulltext_download={}", compliance_plan.no_fulltext_download),
            format!("no_scraping={}", compliance_plan.no_scraping),
            format!("no_authenticated_library_access={}", compliance_plan.no_authenticated_library_access),
            format!("no_paywall_bypass={}", compliance_plan.no_paywall_bypass),
        ],
        vec!["compliance_boundary_preview".to_string()],
        step_active,
    ));
    planned_source_connector_steps.push(psychology_source_connector_step(
        ScholarChatPsychologySourceConnectorStepKind::DownstreamEvidencePackAlignment,
        "downstream_evidence_pack_alignment",
        "Downstream Evidence Pack alignment",
        "Plan downstream Evidence Pack alignment only; no Evidence Pack is created now.",
        vec![
            format!("{:?}", metadata_connector_plan_status),
            format!("{:?}", query_intent),
            recognized_concept.clone().unwrap_or_default(),
            topic_area_hints.join(", "),
        ]
        .into_iter()
        .filter(|value| !value.is_empty())
        .collect(),
        vec!["downstream_evidence_pack_alignment_preview".to_string()],
        step_active,
    ));

    let status = if normalized_query.is_empty()
        || matches!(
            metadata_connector_plan_status,
            ScholarChatMetadataConnectorPlanStatus::Blocked
        )
    {
        ScholarChatPsychologySourceConnectorPlanStatus::Blocked
    } else if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation
    ) {
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsDisambiguation
    } else if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::UnknownConcept
    ) {
        ScholarChatPsychologySourceConnectorPlanStatus::UnknownConcept
    } else if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    ) {
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsMetadataConnectorPlan
    } else if matches!(
        psychology_scope_status,
        ScholarChatPsychologyScopeStatus::NeedsPsychologyScope
    ) {
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsPsychologyScope
    } else if selected_psychology_source_families_sorted.is_empty() {
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
    } else {
        ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady
    };

    let source_family_strategy = if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::Blocked
    ) {
        ScholarChatPsychologySourceFamilyStrategy::Blocked
    } else if matches!(
        status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
    ) || matches!(
        psychology_scope_status,
        ScholarChatPsychologyScopeStatus::NeedsPsychologyScope
    ) {
        ScholarChatPsychologySourceFamilyStrategy::SourceFamilySelectionNeeded
    } else if normalized_mode == "course"
        && selected_psychology_source_families_sorted
            .iter()
            .any(|family| family == "local_course_literature")
    {
        ScholarChatPsychologySourceFamilyStrategy::CourseLiteratureFirst
    } else if selected_psychology_source_families_sorted
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        ScholarChatPsychologySourceFamilyStrategy::ScholarlyIndexFirst
    } else if !selected_psychology_source_families_sorted.is_empty()
        && selected_psychology_source_families_sorted
            .iter()
            .all(|family| family == "openalex_psychology" || family == "crossref_psychology")
    {
        ScholarChatPsychologySourceFamilyStrategy::OpenMetadataFirst
    } else {
        ScholarChatPsychologySourceFamilyStrategy::PsychologyMetadataBalanced
    };

    let mut blocker_notes = blockers.clone();
    let mut warning_notes = warnings.clone();
    let mut next_actions = next_required_actions.clone();
    if normalized_query.is_empty() {
        push_unique_text(&mut blocker_notes, "query_missing");
        push_unique_text(
            &mut next_actions,
            "Provide a psychology or scientific query before connector planning can continue.",
        );
    }
    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::Blocked
    ) {
        push_unique_text(
            &mut blocker_notes,
            "metadata_connector_plan_blocked",
        );
    }
    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation
    ) {
        push_unique_text(
            &mut warning_notes,
            "The scientific search plan still needs disambiguation before psychology source connector planning can continue.",
        );
        push_unique_text(
            &mut next_actions,
            "Narrow the scientific concept before psychology source connector planning can continue.",
        );
    }
    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::UnknownConcept
    ) {
        push_unique_text(
            &mut warning_notes,
            "The query still does not map to a known scientific concept for psychology source connector planning.",
        );
        push_unique_text(
            &mut next_actions,
            "Add discipline and source registry mappings before psychology source connector planning can continue.",
        );
    }
    if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    ) {
        push_unique_text(
            &mut warning_notes,
            "Choose OpenAlex or Crossref before psychology source connector planning can continue.",
        );
        push_unique_text(
            &mut next_actions,
            "Select a supported metadata connector source before psychology source connector planning can continue.",
        );
    }
    if matches!(
        psychology_scope_status,
        ScholarChatPsychologyScopeStatus::NeedsPsychologyScope
    ) {
        push_unique_text(
            &mut warning_notes,
            "Add psychology topic, method, population, or course context before psychology source connector planning can continue.",
        );
    }
    if requested_psychology_source_families.is_empty() {
        push_unique_text(
            &mut warning_notes,
            "Psychology source families default later based on the selected mode.",
        );
    } else if selected_psychology_source_families_sorted.is_empty() {
        push_unique_text(
            &mut warning_notes,
            "No supported psychology source family remains after normalization.",
        );
        push_unique_text(
            &mut next_actions,
            "Choose one of the supported psychology source families before connector planning can continue.",
        );
    } else if !unknown_psychology_source_families.is_empty() {
        push_unique_text(
            &mut warning_notes,
            "Unknown psychology source families are preserved as preview-only hints while supported families remain selected.",
        );
    }
    if !unknown_methodology_hints.is_empty() {
        push_unique_text(
            &mut warning_notes,
            "Unknown psychology methodology hints are preserved as preview-only hints.",
        );
    }
    if selected_psychology_source_families_sorted
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        push_unique_text(
            &mut warning_notes,
            "APA PsycNet / PsycINFO remains a planning boundary only; no automated authenticated scraping or paywall bypass is attempted, and later manual or institutional access review is required.",
        );
    }
    if selected_psychology_source_families_sorted
        .iter()
        .any(|family| family == "local_course_literature")
    {
        push_unique_text(
            &mut warning_notes,
            "Local course literature remains a preview-only boundary and no file read or local index access occurs now.",
        );
    }
    if matches!(status, ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily) {
        push_unique_text(
            &mut next_actions,
            "Choose a supported psychology source family before connector planning can continue.",
        );
    }
    if matches!(status, ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady) {
        push_unique_text(
            &mut next_actions,
            "Psychology source connector planning remains preview-only until a later implementation phase.",
        );
    }

    let source_family_selection_plan = ScholarChatPsychologySourceFamilySelectionPlan {
        requested_families: requested_psychology_source_families.clone(),
        supported_families: supported_psychology_source_families.clone(),
        unknown_families: unknown_psychology_source_families.clone(),
        selected_families: selected_psychology_source_families_sorted.clone(),
        needs_source_family_selection: matches!(
            status,
            ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
        ),
        summary: if requested_psychology_source_families.is_empty() {
            format!(
                "Psychology source families default later to {} for mode {normalized_mode}.",
                selected_psychology_source_families_sorted.join(", ")
            )
        } else if selected_psychology_source_families_sorted.is_empty() {
            "No supported psychology source family remains after normalization; choose one later."
                .to_string()
        } else if unknown_psychology_source_families.is_empty() {
            "Psychology source family preferences were normalized and selected later without connector calls."
                .to_string()
        } else {
            "Supported psychology source families are selected later while unknown family hints remain preview-only."
                .to_string()
        },
    };

    let _source_family_plans = psychology_supported_source_families()
        .into_iter()
        .map(|family_id| {
            let enabled = selected_psychology_source_families_sorted
                .iter()
                .any(|value| value == &family_id);
            let mut planned_filters = vec![
                format!("family_id={family_id}"),
                format!("mode={normalized_mode}"),
                format!("query={normalized_query}"),
            ];
            if request.require_open_access.unwrap_or(false) {
                planned_filters.push("require_open_access=true".to_string());
            }
            if request.require_doi.unwrap_or(false) {
                planned_filters.push("require_doi=true".to_string());
            }
            if let Some(year_from) = request.year_from {
                planned_filters.push(format!("year_from={year_from}"));
            }
            if let Some(year_to) = request.year_to {
                planned_filters.push(format!("year_to={year_to}"));
            }
            if !normalized_context_tags.is_empty() {
                planned_filters.push(format!("context_tags={}", normalized_context_tags.join(", ")));
            }
            if !methodology_hints.is_empty() {
                planned_filters.push(format!("methodology_hints={}", methodology_hints.join(", ")));
            }
            if !population_hints.is_empty() {
                planned_filters.push(format!("population_hints={}", population_hints.join(", ")));
            }
            if !topic_area_hints.is_empty() {
                planned_filters.push(format!("topic_area_hints={}", topic_area_hints.join(", ")));
            }
            planned_filters.sort();
            planned_filters.dedup();
            ScholarChatPsychologySourceFamilyPlan {
                family_id: family_id.clone(),
                enabled,
                source_family_label: psychology_source_family_label(&family_id).to_string(),
                connector_kind: psychology_source_family_connector_kind(&family_id).to_string(),
                planned_query_terms: if normalized_query.is_empty() {
                    vec!["psychology_preview_query".to_string()]
                } else {
                    vec![normalized_query.clone()]
                },
                planned_filters,
                planned_metadata_dependencies: psychology_source_family_metadata_dependencies(&family_id),
                expected_result_types: psychology_source_family_expected_result_types(&family_id),
                evidence_relevance: psychology_source_family_evidence_relevance(&family_id),
                access_boundary_notes: psychology_source_family_access_boundary_notes(&family_id),
                will_call_connector: false,
                will_fetch_url: false,
                will_scrape: false,
                will_authenticate: false,
                will_bypass_paywall: false,
                will_write_metadata: false,
                summary: if family_id == "apa_psycnet_discovery" {
                    "APA PsycNet / PsycINFO remains a planning boundary only; later manual or institutional access review is required."
                        .to_string()
                } else if family_id == "local_course_literature" {
                    "Local course literature remains a planning boundary only; no file read or local index access occurs now."
                        .to_string()
                } else {
                    format!(
                        "{} remains preview-only and does not call a connector or write metadata now.",
                        psychology_source_family_label(&family_id)
                    )
                },
            }
        })
        .collect::<Vec<_>>();

    let methodology_routing_plan = ScholarChatPsychologyMethodologyRoutingPlan {
        methodology_hints: methodology_hints.clone(),
        unknown_methodology_hints: unknown_methodology_hints.clone(),
        inferred_methodology_routes: {
            let mut routes = Vec::new();
            if methodology_hints.is_empty()
                && combined_scope_text.contains("signalentdeckung")
                || combined_scope_text.contains("psychophysics")
                || combined_scope_text.contains("signal_detection")
            {
                routes.push("psychophysics".to_string());
                routes.push("signal_detection".to_string());
            }
            if methodology_hints.is_empty()
                && (combined_scope_text.contains("hypothesentests")
                    || combined_scope_text.contains("anova")
                    || combined_scope_text.contains("hypothesis"))
            {
                routes.push("quantitative".to_string());
                routes.push("statistics_methods".to_string());
            }
            for hint in &methodology_hints {
                match hint.as_str() {
                    "experiment" | "quantitative" => {
                        routes.push("quantitative".to_string());
                    }
                    "survey" | "longitudinal" | "observational" | "qualitative" | "mixed_methods" => {
                        routes.push(hint.clone());
                    }
                    "meta_analysis" | "systematic_review" => {
                        routes.push(hint.clone());
                    }
                    "psychophysics" | "signal_detection" => {
                        routes.push(hint.clone());
                    }
                    "clinical_trial" | "clinical" => {
                        routes.push("clinical_trial".to_string());
                    }
                    "educational" | "course" => {
                        routes.push("educational".to_string());
                        routes.push("course".to_string());
                    }
                    _ => {}
                }
            }
            routes.sort();
            routes.dedup();
            routes
        },
        recommended_source_families: {
            let mut families = BTreeSet::new();
            if methodology_hints.is_empty()
                && (combined_scope_text.contains("signalentdeckung")
                    || combined_scope_text.contains("psychophysics")
                    || combined_scope_text.contains("signal_detection")
                    || combined_scope_text.contains("perception"))
            {
                families.insert("openalex_psychology".to_string());
                families.insert("pubmed_psychology".to_string());
            }
            if methodology_hints.is_empty()
                && (combined_scope_text.contains("hypothesentests")
                    || combined_scope_text.contains("anova")
                    || combined_scope_text.contains("hypothesis"))
            {
                families.insert("openalex_psychology".to_string());
                families.insert("crossref_psychology".to_string());
            }
            for hint in &methodology_hints {
                match hint.as_str() {
                    "meta_analysis" | "systematic_review" => {
                        families.insert("openalex_psychology".to_string());
                        families.insert("crossref_psychology".to_string());
                        families.insert("pubmed_psychology".to_string());
                    }
                    "clinical_trial" | "clinical" => {
                        families.insert("pubmed_psychology".to_string());
                    }
                    "educational" | "course" => {
                        families.insert("eric_psychology".to_string());
                        families.insert("local_course_literature".to_string());
                    }
                    "psychophysics" | "signal_detection" | "perception" => {
                        families.insert("openalex_psychology".to_string());
                        families.insert("pubmed_psychology".to_string());
                    }
                    "quantitative" | "experiment" => {
                        families.insert("openalex_psychology".to_string());
                        families.insert("crossref_psychology".to_string());
                    }
                    _ => {}
                }
            }
            families.into_iter().collect::<Vec<_>>()
        },
        summary: if methodology_hints.is_empty() {
            "Psychology methodology routing stays preview-only and infers later route hints from the topic context when possible."
                .to_string()
        } else {
            "Psychology methodology routing stays preview-only and only plans later route hints without calling connectors."
                .to_string()
        },
    };

    let population_routing_plan = ScholarChatPsychologyPopulationRoutingPlan {
        population_hints: population_hints.clone(),
        recommended_filters_later: population_hints
            .iter()
            .map(|hint| format!("population_filter:{hint}"))
            .collect::<Vec<_>>(),
        ethics_or_safety_notes: {
            let mut notes = Vec::new();
            if population_hints.iter().any(|hint| hint == "clinical_population") {
                notes.push(
                    "clinical_population_requires_later_ethics_or_safety_review".to_string(),
                );
            }
            notes
        },
        summary: "Population routing is preview-only and only plans later study-population filters and ethics notes."
            .to_string(),
    };

    let topic_area_routing_plan = ScholarChatPsychologyTopicAreaRoutingPlan {
        topic_area_hints: topic_area_hints.clone(),
        inferred_topic_areas: {
            let mut areas = BTreeSet::new();
            if combined_scope_text.contains("signalentdeckung")
                || combined_scope_text.contains("psychophysics")
                || combined_scope_text.contains("perception")
            {
                areas.insert("cognitive_psychology".to_string());
                areas.insert("perception".to_string());
                areas.insert("psychophysics".to_string());
            }
            if combined_scope_text.contains("hypothesentests")
                || combined_scope_text.contains("anova")
                || combined_scope_text.contains("hypothesis")
            {
                areas.insert("research_methods".to_string());
                areas.insert("statistics_methods".to_string());
            }
            for hint in &topic_area_hints {
                areas.insert(hint.clone());
            }
            if normalized_mode == "course" {
                areas.insert("course_context_alignment".to_string());
            }
            areas.into_iter().collect::<Vec<_>>()
        },
        recommended_source_families: {
            let mut families = BTreeSet::new();
            for family in &methodology_routing_plan.recommended_source_families {
                families.insert(family.clone());
            }
            if normalized_mode == "course" {
                families.insert("local_course_literature".to_string());
            }
            families.into_iter().collect::<Vec<_>>()
        },
        summary: "Psychology topic-area routing stays preview-only and only plans later topic-area source alignment."
            .to_string(),
    };

    let _psychology_scope_ready = !matches!(
        psychology_scope_status,
        ScholarChatPsychologyScopeStatus::NeedsPsychologyScope
    );
    let _source_connector_strategy = if matches!(
        status,
        ScholarChatPsychologySourceConnectorPlanStatus::Blocked
    ) {
        ScholarChatPsychologySourceFamilyStrategy::Blocked
    } else if matches!(
        status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
    ) {
        ScholarChatPsychologySourceFamilyStrategy::SourceFamilySelectionNeeded
    } else if normalized_mode == "course"
        && selected_psychology_source_families_sorted
            .iter()
            .any(|family| family == "local_course_literature")
    {
        ScholarChatPsychologySourceFamilyStrategy::CourseLiteratureFirst
    } else if selected_psychology_source_families_sorted
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        ScholarChatPsychologySourceFamilyStrategy::ScholarlyIndexFirst
    } else if !selected_psychology_source_families_sorted.is_empty()
        && selected_psychology_source_families_sorted
            .iter()
            .all(|family| family == "openalex_psychology" || family == "crossref_psychology")
    {
        ScholarChatPsychologySourceFamilyStrategy::OpenMetadataFirst
    } else {
        ScholarChatPsychologySourceFamilyStrategy::PsychologyMetadataBalanced
    };

    let _source_connector_steps = {
        let mut steps = vec![
            psychology_source_connector_step(
                ScholarChatPsychologySourceConnectorStepKind::MetadataConnectorAlignment,
                "metadata_connector_alignment",
                "Metadata connector alignment",
                "Align the psychology connector preview with the existing OpenAlex/Crossref metadata connector preview only.",
                vec![
                    format!("{:?}", metadata_connector_plan_status),
                    format!("{:?}", metadata_connector_strategy),
                    preferred_metadata_sources.join(", "),
                ],
                vec!["metadata_connector_alignment_preview".to_string()],
                !matches!(
                    metadata_connector_plan_status,
                    ScholarChatMetadataConnectorPlanStatus::Blocked
                ),
            ),
            psychology_source_connector_step(
                ScholarChatPsychologySourceConnectorStepKind::PsychologyScopeClassification,
                "psychology_scope_classification",
                "Psychology scope classification",
                "Classify whether the query is psychology-related using deterministic preview-only heuristics.",
                vec![
                    normalized_query.clone(),
                    normalized_mode.clone(),
                    request.course_context.clone().unwrap_or_default(),
                    normalized_context_tags.join(", "),
                    methodology_hints.join(", "),
                    population_hints.join(", "),
                    topic_area_hints.join(", "),
                    recognized_concept.clone().unwrap_or_default(),
                    label.clone().unwrap_or_default(),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["psychology_scope_preview".to_string()],
                !matches!(
                    metadata_connector_plan_status,
                    ScholarChatMetadataConnectorPlanStatus::Blocked
                ),
            ),
            psychology_source_connector_step(
                ScholarChatPsychologySourceConnectorStepKind::SourceFamilySelection,
                "source_family_selection",
                "Source family selection",
                "Select psychology source families later without any connector calls or metadata writes.",
                vec![
                    requested_psychology_source_families.join(", "),
                    selected_psychology_source_families_sorted.join(", "),
                    unknown_psychology_source_families.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["source_family_selection_preview".to_string()],
                !matches!(
                    metadata_connector_plan_status,
                    ScholarChatMetadataConnectorPlanStatus::Blocked
                ),
            ),
        ];
        for family_id in [
            "openalex_psychology",
            "crossref_psychology",
            "pubmed_psychology",
            "eric_psychology",
            "apa_psycnet_discovery",
            "local_course_literature",
        ] {
            let family_name = psychology_source_family_label(family_id).to_string();
            steps.push(psychology_source_connector_step(
                match family_id {
                    "openalex_psychology" => ScholarChatPsychologySourceConnectorStepKind::OpenalexPsychologyPlan,
                    "crossref_psychology" => ScholarChatPsychologySourceConnectorStepKind::CrossrefPsychologyPlan,
                    "pubmed_psychology" => ScholarChatPsychologySourceConnectorStepKind::PubmedPsychologyPlan,
                    "eric_psychology" => ScholarChatPsychologySourceConnectorStepKind::EricPsychologyPlan,
                    "apa_psycnet_discovery" => ScholarChatPsychologySourceConnectorStepKind::ApaPsycnetDiscoveryPlan,
                    _ => ScholarChatPsychologySourceConnectorStepKind::LocalCourseLiteraturePlan,
                },
                &format!("{}_plan", family_id),
                &family_name,
                &format!("{family_name} remains preview-only and does not call a connector now."),
                vec![
                    normalized_query.clone(),
                    normalized_mode.clone(),
                    requested_psychology_source_families.join(", "),
                    methodology_hints.join(", "),
                    population_hints.join(", "),
                    topic_area_hints.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec![format!("{}_preview", family_id)],
                !matches!(
                    metadata_connector_plan_status,
                    ScholarChatMetadataConnectorPlanStatus::Blocked
                ),
            ));
        }
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::MethodologyRouting,
            "methodology_routing",
            "Methodology routing",
            "Route psychology methodology hints later without connector calls or metadata writes.",
            vec![
                methodology_hints.join(", "),
                methodology_routing_plan.inferred_methodology_routes.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["methodology_routing_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::PopulationRouting,
            "population_routing",
            "Population routing",
            "Route study population hints later without any user-identity inference.",
            vec![population_hints.join(", ")],
            vec!["population_routing_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::TopicAreaRouting,
            "topic_area_routing",
            "Topic area routing",
            "Route psychology topic areas later without running retrieval or connector calls.",
            vec![
                topic_area_hints.join(", "),
                topic_area_routing_plan.inferred_topic_areas.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["topic_area_routing_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::EvidenceRequirementMapping,
            "evidence_requirement_mapping",
            "Evidence requirement mapping",
            "Map psychology evidence requirements later without building an Evidence Pack now.",
            vec![
                evidence_requirements_plan.evidence_requirements.join(", "),
                evidence_requirements_plan.preferred_metadata_fields.join(", "),
                evidence_requirements_plan.quality_signals.join(", "),
            ],
            vec!["evidence_requirement_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::ComplianceBoundaryCheck,
            "compliance_boundary_check",
            "Compliance boundary check",
            "Confirm psychology metadata planning boundaries without any external call.",
            vec![
                format!("external_metadata_only={}", compliance_plan.external_metadata_only),
                format!("no_fulltext_download={}", compliance_plan.no_fulltext_download),
                format!("no_scraping={}", compliance_plan.no_scraping),
                format!(
                    "no_authenticated_library_access={}",
                    compliance_plan.no_authenticated_library_access
                ),
                format!("no_paywall_bypass={}", compliance_plan.no_paywall_bypass),
            ],
            vec!["compliance_boundary_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps.push(psychology_source_connector_step(
            ScholarChatPsychologySourceConnectorStepKind::DownstreamEvidencePackAlignment,
            "downstream_evidence_pack_alignment",
            "Downstream Evidence Pack alignment",
            "Plan downstream Evidence Pack alignment only; no Evidence Pack is created now.",
            vec![
                format!("{:?}", metadata_connector_plan_status),
                format!("{:?}", query_intent),
                recognized_concept.clone().unwrap_or_default(),
                topic_area_hints.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_evidence_pack_alignment_preview".to_string()],
            !matches!(
                metadata_connector_plan_status,
                ScholarChatMetadataConnectorPlanStatus::Blocked
            ),
        ));
        steps
    };

    let summary = match status {
        ScholarChatPsychologySourceConnectorPlanStatus::Blocked => {
            "Psychology source connector preview is blocked because the query is blank or the upstream metadata connector preview is blocked."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsDisambiguation => {
            "Psychology source connector preview needs disambiguation before later connector planning can continue."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::UnknownConcept => {
            "Psychology source connector preview found an unknown concept and can only outline later routing steps."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsMetadataConnectorPlan => {
            "Psychology source connector preview is waiting for an OpenAlex or Crossref metadata connector plan."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsPsychologyScope => {
            "Psychology source connector preview needs a psychology topic, method, population, or course context before it can become ready later."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily => {
            "Psychology source connector preview needs a supported psychology source family selection before it can become ready later."
                .to_string()
        }
        ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady => {
            "Psychology source connector preview is ready later and only describes future psychology source-family routing, methodology routing, population routing, evidence requirements, and compliance."
                .to_string()
        }
    };

    Ok(ScholarChatPsychologySourceConnectorPlanPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids,
        expected_source_kinds,
        preferred_metadata_sources,
        preferred_psychology_source_families: selected_psychology_source_families_sorted.clone(),
        unknown_psychology_source_families,
        methodology_hints,
        unknown_methodology_hints,
        population_hints,
        topic_area_hints,
        normalized_max_results_per_source: metadata_connector_plan_preview.normalized_max_results_per_source,
        require_open_access: metadata_connector_plan_preview.require_open_access,
        require_doi: metadata_connector_plan_preview.require_doi,
        year_from: metadata_connector_plan_preview.year_from,
        year_to: metadata_connector_plan_preview.year_to,
        metadata_connector_plan_status,
        metadata_connector_strategy,
        scientific_search_plan_status,
        query_understanding_status,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        psychology_scope_status,
        source_family_strategy,
        source_family_selection_plan,
        psychology_source_family_plans: family_plan_values,
        methodology_routing_plan,
        population_routing_plan,
        topic_area_routing_plan,
        evidence_requirements_plan,
        compliance_plan,
        planned_source_connector_steps,
        blockers: blocker_notes,
        warnings: warning_notes,
        next_required_actions: next_actions,
        summary,
        preview_only: true,
        psychology_source_connector_preview_only: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

fn normalize_scientific_paper_review_section_preferences(
    values: Option<Vec<String>>,
) -> (Vec<String>, Vec<String>) {
    let mut normalized = values
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    let supported_sections = scientific_paper_literature_review_supported_sections();
    let requested_sections = normalized.clone();
    let unknown_sections = normalized
        .into_iter()
        .filter(|section| !supported_sections.contains(&section.as_str()))
        .collect::<Vec<_>>();
    (requested_sections, unknown_sections)
}

fn scientific_paper_literature_review_supported_sections() -> Vec<&'static str> {
    vec![
        "background",
        "search_strategy",
        "evidence_map",
        "methods",
        "findings",
        "limitations",
        "research_gap",
        "implications",
        "citation_plan",
        "compliance_notes",
    ]
}

fn scientific_paper_literature_review_step_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "scientific paper mode only".to_string(),
        "no literature review created".to_string(),
        "no answer generated".to_string(),
        "no Evidence Pack created".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no file read".to_string(),
        "no retrieval execution".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ]
}

fn scientific_paper_literature_review_step(
    kind: ScholarChatScientificPaperLiteratureReviewStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatScientificPaperLiteratureReviewStep {
    ScholarChatScientificPaperLiteratureReviewStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: scientific_paper_literature_review_step_notes(),
    }
}

fn scientific_paper_literature_review_plan_status(
    evidence_pack_plan_status: &ScholarChatScientificEvidencePackPlanStatus,
    normalized_research_question: &Option<String>,
) -> ScholarChatScientificPaperLiteratureReviewPlanStatus {
    match evidence_pack_plan_status {
        ScholarChatScientificEvidencePackPlanStatus::Blocked => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsDisambiguation
        }
        ScholarChatScientificEvidencePackPlanStatus::UnknownConcept => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::UnknownConcept
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsPsychologyScope
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsSourceFamily
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidenceSources
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsCitationMetadata
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan => {
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidencePackPlan
        }
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady => {
            if normalized_research_question.is_some() {
                ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady
            } else {
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion
            }
        }
    }
}

fn scientific_paper_literature_review_strategy(
    status: &ScholarChatScientificPaperLiteratureReviewPlanStatus,
    normalized_citation_style: &str,
    require_doi: bool,
) -> ScholarChatScientificPaperLiteratureReviewStrategy {
    if matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked
    ) {
        ScholarChatScientificPaperLiteratureReviewStrategy::Blocked
    } else if matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsPsychologyScope
    ) {
        ScholarChatScientificPaperLiteratureReviewStrategy::PsychologyScopeFirst
    } else if matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsSourceFamily
    ) {
        ScholarChatScientificPaperLiteratureReviewStrategy::SourceFamilyFirst
    } else if matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsCitationMetadata
    ) || normalized_citation_style == "doi_first"
        || require_doi
    {
        ScholarChatScientificPaperLiteratureReviewStrategy::CitationMetadataFirst
    } else if matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion
    ) {
        ScholarChatScientificPaperLiteratureReviewStrategy::ResearchQuestionFirst
    } else {
        ScholarChatScientificPaperLiteratureReviewStrategy::EvidencePackFirst
    }
}

fn scientific_evidence_pack_plan_boundary_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no file read".to_string(),
        "no retrieval execution".to_string(),
        "no Evidence Pack created".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ]
}

fn scientific_evidence_pack_plan_step(
    kind: ScholarChatScientificEvidencePackStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatScientificEvidencePackStep {
    ScholarChatScientificEvidencePackStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: scientific_evidence_pack_plan_boundary_notes(),
    }
}

fn scientific_evidence_pack_plan_item(
    item_id: &str,
    kind: &str,
    source_channel: &str,
    enabled: bool,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    source_ids: Vec<String>,
    required_fields: Vec<String>,
    quality_requirements: Vec<String>,
    citation_requirements: Vec<String>,
    summary: &str,
) -> ScholarChatScientificEvidenceItemPlan {
    ScholarChatScientificEvidenceItemPlan {
        item_id: item_id.to_string(),
        kind: kind.to_string(),
        source_channel: source_channel.to_string(),
        planned_inputs,
        planned_outputs,
        enabled,
        preview_only: true,
        will_read_file: false,
        will_call_connector: false,
        will_write_artifact: false,
        source_ids,
        required_fields,
        quality_requirements,
        citation_requirements,
        summary: summary.to_string(),
    }
}

fn scientific_evidence_pack_plan_has_citation_metadata_path(
    preferred_metadata_sources: &[String],
    preferred_psychology_source_families: &[String],
) -> bool {
    preferred_metadata_sources.iter().any(|source| {
        matches!(source.as_str(), "openalex" | "crossref" | "pubmed" | "eric")
    }) || preferred_psychology_source_families.iter().any(|family| {
        matches!(
            family.as_str(),
            "openalex_psychology"
                | "crossref_psychology"
                | "pubmed_psychology"
                | "eric_psychology"
        )
    })
}

fn scientific_evidence_pack_plan_status(
    psychology_source_connector_status: &ScholarChatPsychologySourceConnectorPlanStatus,
    normalized_query: &str,
    selected_local_source_ids: &[String],
    normalized_preferred_metadata_sources: &[String],
    normalized_preferred_psychology_source_families: &[String],
    require_doi: bool,
    normalized_citation_style: &str,
) -> ScholarChatScientificEvidencePackPlanStatus {
    if normalized_query.is_empty()
        || matches!(
            psychology_source_connector_status,
            ScholarChatPsychologySourceConnectorPlanStatus::Blocked
        )
    {
        ScholarChatScientificEvidencePackPlanStatus::Blocked
    } else if matches!(
        psychology_source_connector_status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsDisambiguation
    ) {
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
    } else if matches!(
        psychology_source_connector_status,
        ScholarChatPsychologySourceConnectorPlanStatus::UnknownConcept
    ) {
        ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
    } else if matches!(
        psychology_source_connector_status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsPsychologyScope
    ) {
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope
    } else if matches!(
        psychology_source_connector_status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
    ) {
        ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily
    } else if matches!(
        psychology_source_connector_status,
        ScholarChatPsychologySourceConnectorPlanStatus::NeedsMetadataConnectorPlan
    ) {
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan
    } else if selected_local_source_ids.is_empty()
        && normalized_preferred_psychology_source_families.is_empty()
        && normalized_preferred_metadata_sources.is_empty()
    {
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
    } else if (require_doi || normalized_citation_style == "doi_first")
        && !scientific_evidence_pack_plan_has_citation_metadata_path(
            normalized_preferred_metadata_sources,
            normalized_preferred_psychology_source_families,
        )
    {
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata
    } else {
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
    }
}

fn scientific_evidence_pack_plan_strategy(
    status: &ScholarChatScientificEvidencePackPlanStatus,
    normalized_mode: &str,
    selected_local_source_ids: &[String],
    normalized_preferred_psychology_source_families: &[String],
    require_doi: bool,
    normalized_citation_style: &str,
) -> ScholarChatScientificEvidencePackStrategy {
    if matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked) {
        ScholarChatScientificEvidencePackStrategy::Blocked
    } else if matches!(
        status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
            | ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily
            | ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope
            | ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan
            | ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
            | ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
            | ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata
    ) {
        ScholarChatScientificEvidencePackStrategy::EvidenceSourceSelectionNeeded
    } else if normalized_mode == "course" && !selected_local_source_ids.is_empty() {
        ScholarChatScientificEvidencePackStrategy::LocalContextFirst
    } else if require_doi || normalized_citation_style == "doi_first" {
        ScholarChatScientificEvidencePackStrategy::MetadataCitationFirst
    } else if !normalized_preferred_psychology_source_families.is_empty()
        && selected_local_source_ids.is_empty()
    {
        ScholarChatScientificEvidencePackStrategy::PsychologySourceFamilyFirst
    } else {
        ScholarChatScientificEvidencePackStrategy::BalancedLocalAndMetadata
    }
}

fn scientific_evidence_pack_plan_summary(
    status: &ScholarChatScientificEvidencePackPlanStatus,
    evidence_pack_strategy: &ScholarChatScientificEvidencePackStrategy,
) -> String {
    match status {
        ScholarChatScientificEvidencePackPlanStatus::Blocked => {
            "Scientific evidence pack preview is blocked until the query and upstream psychology planning become available."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation => {
            "Scientific evidence pack preview needs query disambiguation before later evidence planning can continue."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::UnknownConcept => {
            "Scientific evidence pack preview found an unknown concept and can only outline later planning steps."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope => {
            "Scientific evidence pack preview needs psychology scope before later evidence planning can continue."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily => {
            "Scientific evidence pack preview needs a supported psychology source family before later evidence planning can continue."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan => {
            "Scientific evidence pack preview is waiting for a psychology source connector plan before later evidence planning can continue."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources => {
            "Scientific evidence pack preview needs local sources, psychology source families, or metadata sources before evidence planning can continue."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata => {
            "Scientific evidence pack preview needs citation metadata planning before it can become ready later."
                .to_string()
        }
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady => format!(
            "Scientific evidence pack preview is ready later and only describes {:?} planning.",
            evidence_pack_strategy
        ),
    }
}

fn scientific_evidence_pack_plan_next_required_actions(
    status: &ScholarChatScientificEvidencePackPlanStatus,
    normalized_mode: &str,
    normalized_preferred_metadata_sources: &[String],
    normalized_preferred_psychology_source_families: &[String],
    selected_local_source_ids: &[String],
    normalized_citation_style: &str,
) -> Vec<String> {
    let mut actions = Vec::new();
    match status {
        ScholarChatScientificEvidencePackPlanStatus::Blocked => {
            push_unique_text(
                &mut actions,
                "Provide a non-empty scientific query so the evidence pack preview can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation => {
            push_unique_text(
                &mut actions,
                "Narrow the scientific concept before evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::UnknownConcept => {
            push_unique_text(
                &mut actions,
                "Map the query to a known scientific concept before evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope => {
            push_unique_text(
                &mut actions,
                "Add psychology topic, method, population, or course context before evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily => {
            push_unique_text(
                &mut actions,
                "Choose a supported psychology source family before evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan => {
            push_unique_text(
                &mut actions,
                "Provide an OpenAlex or Crossref metadata connector path before evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources => {
            push_unique_text(
                &mut actions,
                "Select local sources, psychology source families, or metadata sources so evidence pack planning can continue.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata => {
            push_unique_text(
                &mut actions,
                "Choose a DOI-capable or stable metadata path so citation planning can continue later.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady => {
            if normalized_mode == "course" && !selected_local_source_ids.is_empty() {
                push_unique_text(
                    &mut actions,
                    "Later evidence pack construction should keep course-local sources aligned with the course context.",
                );
            } else if !normalized_preferred_psychology_source_families.is_empty() {
                push_unique_text(
                    &mut actions,
                    "Later evidence pack construction should keep the selected psychology source families aligned with the scientific query.",
                );
            } else if !normalized_preferred_metadata_sources.is_empty() {
                push_unique_text(
                    &mut actions,
                    "Later evidence pack construction should preserve the selected metadata sources and citation metadata path.",
                );
            } else if normalized_citation_style == "doi_first" {
                push_unique_text(
                    &mut actions,
                    "Later evidence pack construction should prefer DOI-backed metadata for citation planning.",
                );
            }
        }
    }
    if actions.is_empty() {
        push_unique_text(
            &mut actions,
            "Keep this preview-only; do not create an Evidence Pack yet.",
        );
    }
    actions
}

pub fn preview_scholar_chat_scientific_evidence_pack_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificEvidencePackPlanRequest,
) -> AegisResult<ScholarChatScientificEvidencePackPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let normalized_expected_source_kinds = normalize_scientific_expected_source_kinds(
        request.expected_source_kinds.clone(),
    );
    let normalized_preferred_metadata_sources =
        normalize_metadata_connector_sources(request.preferred_metadata_sources.clone());
    let normalized_requested_psychology_source_families = normalize_psychology_connector_values(
        request.preferred_psychology_source_families.clone(),
    );
    let normalized_methodology_hints =
        normalize_psychology_connector_values(request.methodology_hints.clone());
    let normalized_population_hints =
        normalize_psychology_connector_values(request.population_hints.clone());
    let normalized_topic_area_hints =
        normalize_psychology_connector_values(request.topic_area_hints.clone());
    let normalized_evidence_goal = request
        .evidence_goal
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());
    let normalized_claim_scope = normalize_scientific_claim_scope(request.claim_scope.clone());
    let (normalized_citation_style, mut style_warnings) =
        normalize_scientific_citation_style(request.citation_style.clone());

    let psychology_source_connector_preview = preview_scholar_chat_psychology_source_connector_plan(
        &root,
        ScholarChatPsychologySourceConnectorPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
            expected_source_kinds: Some(normalized_expected_source_kinds.clone()),
            preferred_metadata_sources: Some(normalized_preferred_metadata_sources.clone()),
            preferred_psychology_source_families: Some(
                normalized_requested_psychology_source_families.clone(),
            ),
            methodology_hints: Some(normalized_methodology_hints.clone()),
            population_hints: Some(normalized_population_hints.clone()),
            topic_area_hints: Some(normalized_topic_area_hints.clone()),
            max_results_per_source: request.max_results_per_source,
            require_open_access: request.require_open_access,
            require_doi: request.require_doi,
            year_from: request.year_from,
            year_to: request.year_to,
        },
    )?;

    let psychology_source_connector_status =
        psychology_source_connector_preview.status.clone();
    let metadata_connector_plan_status =
        psychology_source_connector_preview.metadata_connector_plan_status.clone();
    let scientific_search_plan_status =
        psychology_source_connector_preview.scientific_search_plan_status.clone();
    let query_understanding_status =
        psychology_source_connector_preview.query_understanding_status.clone();
    let psychology_scope_status =
        psychology_source_connector_preview.psychology_scope_status.clone();
    let source_family_strategy = psychology_source_connector_preview.source_family_strategy.clone();
    let inferred_topic = psychology_source_connector_preview.inferred_topic.clone();
    let query_intent = psychology_source_connector_preview.query_intent.clone();
    let recognized_concept = psychology_source_connector_preview.recognized_concept.clone();
    let label = psychology_source_connector_preview.label.clone();
    let selected_psychology_source_families =
        psychology_source_connector_preview.preferred_psychology_source_families.clone();
    let effective_metadata_sources =
        psychology_source_connector_preview.preferred_metadata_sources.clone();
    let supported_psychology_source_families = psychology_supported_source_families();

    let status = scientific_evidence_pack_plan_status(
        &psychology_source_connector_status,
        &normalized_query,
        &selected_local_source_ids,
        &effective_metadata_sources,
        &selected_psychology_source_families,
        request.require_doi.unwrap_or(false),
        &normalized_citation_style,
    );

    let mut warnings = psychology_source_connector_preview.warnings.clone();
    warnings.append(&mut style_warnings);

    push_unique_text(
        &mut warnings,
        "This is a scientific evidence pack preview only; it does not create an Evidence Pack, perform retrieval, or write artifacts.",
    );

    let claim_scope_warnings: Vec<String> = normalized_claim_scope
        .iter()
        .filter(|claim| {
            !matches!(
                claim.as_str(),
                "definition"
                    | "explanation"
                    | "citation_support"
                    | "method"
                    | "limitation"
                    | "course_context"
            )
        })
        .map(|claim| {
            format!(
                "Claim scope item '{claim}' is unknown and will only be reviewed later."
            )
        })
        .collect();
    for warning in claim_scope_warnings {
        push_unique_text(&mut warnings, &warning);
    }
    let unknown_claim_scope = normalized_claim_scope
        .iter()
        .filter(|claim| {
            !matches!(
                claim.as_str(),
                "definition"
                    | "explanation"
                    | "citation_support"
                    | "method"
                    | "limitation"
                    | "course_context"
            )
        })
        .cloned()
        .collect::<Vec<_>>();

    let has_citation_metadata_path = scientific_evidence_pack_plan_has_citation_metadata_path(
        &effective_metadata_sources,
        &selected_psychology_source_families,
    );
    let status = if matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked) {
        ScholarChatScientificEvidencePackPlanStatus::Blocked
    } else if matches!(
        status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
            | ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
            | ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope
            | ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily
            | ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan
    ) {
        status
    } else if selected_local_source_ids.is_empty()
        && selected_psychology_source_families.is_empty()
        && effective_metadata_sources.is_empty()
    {
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
    } else if (request.require_doi.unwrap_or(false) || normalized_citation_style == "doi_first")
        && !has_citation_metadata_path
    {
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata
    } else {
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
    };

    let evidence_pack_strategy = scientific_evidence_pack_plan_strategy(
        &status,
        &normalized_mode,
        &selected_local_source_ids,
        &selected_psychology_source_families,
        request.require_doi.unwrap_or(false),
        &normalized_citation_style,
    );

    let local_evidence_context_plan = ScholarChatScientificEvidencePackLocalContextPlan {
        selected_source_ids: selected_local_source_ids.clone(),
        selected_source_count: selected_local_source_ids.len(),
        requires_local_sources: normalized_mode == "course"
            || normalized_claim_scope
                .iter()
                .any(|claim| claim == "course_context"),
        planned_local_source_roles: if normalized_mode == "course"
            || normalized_claim_scope
                .iter()
                .any(|claim| claim == "course_context")
        {
            vec![
                "course_context_alignment".to_string(),
                "local_source_context".to_string(),
            ]
        } else if !selected_local_source_ids.is_empty() {
            vec!["local_source_context".to_string()]
        } else {
            vec!["scientific_context_later".to_string()]
        },
        planned_local_evidence_requirements: if normalized_mode == "course"
            || normalized_claim_scope
                .iter()
                .any(|claim| claim == "course_context")
        {
            vec![
                "course_context_required_later".to_string(),
                "local_source_identity_required".to_string(),
            ]
        } else {
            vec!["local_source_identity_optional_later".to_string()]
        },
        will_read_files: false,
        will_index_files: false,
        will_run_retrieval: false,
        summary: "Local evidence context stays preview-only and only plans later local source alignment."
            .to_string(),
    };

    let metadata_evidence_plan = {
        let required_identifier_fields = if request.require_doi.unwrap_or(false)
            || normalized_citation_style == "doi_first"
        {
            vec!["doi".to_string(), "stable_identifier".to_string()]
        } else {
            vec!["doi_or_stable_identifier".to_string()]
        };
        let mut citation_metadata_notes = vec![
            "doi_or_stable_identifier_required_later".to_string(),
            "title_required_later".to_string(),
            "authors_required_later".to_string(),
            "publication_year_required_later".to_string(),
            "source_family_required_later".to_string(),
            "provider_record_id_required_later".to_string(),
            "journal_or_container_required_later".to_string(),
            "open_access_status_required_later".to_string(),
            "methodology_keywords_required_later".to_string(),
            "population_keywords_required_later".to_string(),
        ];
        if request.require_doi.unwrap_or(false) || normalized_citation_style == "doi_first" {
            push_unique_text(&mut citation_metadata_notes, "doi_required_later");
        }
        if request.require_open_access.unwrap_or(false) {
            push_unique_text(
                &mut citation_metadata_notes,
                "open_access_status_required_for_planning",
            );
        }
        ScholarChatScientificEvidencePackMetadataPlan {
            required_metadata_fields: vec![
                "doi_or_stable_identifier".to_string(),
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "source_family".to_string(),
                "provider_record_id".to_string(),
                "journal_or_container".to_string(),
                "open_access_status".to_string(),
                "methodology_keywords_later".to_string(),
                "population_keywords_later".to_string(),
            ],
            required_identifier_fields,
            preferred_metadata_sources: normalized_preferred_metadata_sources.clone(),
            citation_metadata_notes,
            require_open_access: request.require_open_access.unwrap_or(false),
            require_doi: request.require_doi.unwrap_or(false),
            will_call_metadata_connectors: false,
            will_write_metadata: false,
            summary: "Metadata evidence planning stays preview-only and only prepares later citation-safe metadata alignment."
                .to_string(),
        }
    };

    let source_family_requirements = selected_psychology_source_families
        .iter()
        .map(|family| match family.as_str() {
            "openalex_psychology" => {
                "open metadata record alignment later".to_string()
            }
            "crossref_psychology" => {
                "DOI/bibliographic metadata alignment later".to_string()
            }
            "pubmed_psychology" => {
                "biomedical citation metadata alignment later".to_string()
            }
            "eric_psychology" => {
                "education psychology metadata alignment later".to_string()
            }
            "apa_psycnet_discovery" => {
                "manual or institutional access review later".to_string()
            }
            "local_course_literature" => {
                "local course source context later".to_string()
            }
            other => format!("{other} requires later review"),
        })
        .collect::<Vec<_>>();
    let mut access_boundary_notes = vec![
        "no connector calls".to_string(),
        "no authenticated access".to_string(),
        "no paywall bypass".to_string(),
    ];
    if selected_psychology_source_families
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        push_unique_text(
            &mut access_boundary_notes,
            "manual or institutional access review required later",
        );
        push_unique_text(
            &mut access_boundary_notes,
            "no automated authenticated scraping",
        );
    }
    let source_family_evidence_plan = ScholarChatScientificEvidencePackSourceFamilyPlan {
        selected_families: selected_psychology_source_families.clone(),
        supported_families: supported_psychology_source_families.clone(),
        family_requirements: source_family_requirements,
        access_boundary_notes,
        will_call_source_connectors: false,
        will_authenticate: false,
        will_bypass_paywall: false,
        summary: "Source-family evidence planning stays preview-only and only outlines later psychology family alignment."
            .to_string(),
    };

    let claim_coverage_plan = {
        let mut planned_claim_kinds = if normalized_claim_scope.is_empty() {
            vec![
                "definition".to_string(),
                "explanation".to_string(),
                "citation_support".to_string(),
            ]
        } else {
            normalized_claim_scope.clone()
        };
        if matches!(query_intent, ScholarChatScientificQueryIntent::LiteratureSearch)
            || normalized_mode == "scientific_paper"
        {
            push_unique_text(&mut planned_claim_kinds, "method");
            push_unique_text(&mut planned_claim_kinds, "limitation");
        }
        if normalized_mode == "course" {
            push_unique_text(&mut planned_claim_kinds, "course_context");
        }
        ScholarChatScientificEvidencePackClaimCoveragePlan {
            claim_scope: normalized_claim_scope.clone(),
            planned_claim_kinds,
            unknown_claim_scope_items: unknown_claim_scope.clone(),
            minimum_evidence_requirements: vec![
                "at_least_one_verified_metadata_record_later".to_string(),
                "at_least_one_local_or_external_evidence_candidate_later".to_string(),
                "citation_safe_identifier_required_later".to_string(),
                "no_answer_without_evidence_pack_later".to_string(),
            ],
            unsupported_claim_handling: vec![
                "mark_unsupported_claims_for_review_later".to_string(),
                "do_not_fabricate_supporting_sources".to_string(),
                "block_answer_generation_until_grounded_later".to_string(),
            ],
            summary: "Claim coverage planning stays preview-only and only maps later claim support needs."
                .to_string(),
        }
    };

    let citation_attribution_plan = ScholarChatScientificEvidencePackCitationAttributionPlan {
        supported_citation_styles: scientific_evidence_pack_supported_citation_styles(),
        normalized_citation_style: normalized_citation_style.clone(),
        required_citation_fields: vec![
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "doi_or_stable_identifier".to_string(),
            "access_or_metadata_provider_note_later".to_string(),
        ],
        required_provider_attribution_notes: vec![
            "cite_metadata_provider_later".to_string(),
            "preserve_provider_record_ids_later".to_string(),
            "preserve_doi_and_source_url_later".to_string(),
            "include_access_date_later".to_string(),
            "no_fabricated_citations".to_string(),
        ],
        citation_safety_notes: vec![
            "metadata_preview_not_citation_evidence".to_string(),
            "full_citation_requires_verified_metadata_later".to_string(),
            "no_generated_sources".to_string(),
            "no_answer_without_evidence_pack_later".to_string(),
        ],
        will_emit_citations_now: false,
        summary: "Citation attribution planning stays preview-only and only prepares later citation-safe attribution."
            .to_string(),
    };

    let evidence_quality_plan = ScholarChatScientificEvidencePackQualityPlan {
        quality_signals: vec![
            "stable_identifier_present_later".to_string(),
            "peer_review_or_scholarly_status_later".to_string(),
            "methodology_match_later".to_string(),
            "topic_area_match_later".to_string(),
            "population_match_later_when_available".to_string(),
            "source_family_relevance_later".to_string(),
            "local_course_relevance_later_when_course_mode".to_string(),
        ],
        ranking_inputs_later: vec![
            "metadata_relevance".to_string(),
            "source_family_match".to_string(),
            "method_match".to_string(),
            "year_filter_match".to_string(),
            "open_access_status".to_string(),
            "local_context_match".to_string(),
            "citation_count_metadata_later".to_string(),
        ],
        exclusion_rules: vec![
            "exclude_fabricated_sources".to_string(),
            "exclude_unverified_citations".to_string(),
            "exclude_paywalled_full_text_scraping".to_string(),
            "exclude_automated_authenticated_library_records".to_string(),
            "exclude_non_scientific_sources_unless_explicitly_requested".to_string(),
        ],
        review_requirements: vec![
            "verify_metadata_before_citation".to_string(),
            "verify_evidence_item_before_answer".to_string(),
            "require_evidence_pack_before_final_answer".to_string(),
        ],
        summary: "Evidence quality planning stays preview-only and only describes later quality and ranking inputs."
            .to_string(),
    };

    let evidence_compliance_plan = ScholarChatScientificEvidencePackCompliancePlan {
        external_metadata_only: true,
        no_fulltext_download: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        respects_rate_limits_later: true,
        requires_terms_review_later: true,
        requires_institutional_access_review_later: selected_psychology_source_families
            .iter()
            .any(|family| family == "apa_psycnet_discovery"),
        will_call_external_services: false,
        will_write_metadata: false,
        will_create_evidence_pack: false,
        summary: "Evidence compliance planning stays preview-only and only documents later external metadata boundaries."
            .to_string(),
    };

    let planned_evidence_items = vec![
        scientific_evidence_pack_plan_item(
            "local_source_context",
            "local_source_context",
            "local_source_context",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "selected_local_source_ids".to_string(),
                "normalized_mode".to_string(),
            ],
            vec!["local_source_context_plan".to_string()],
            selected_local_source_ids.clone(),
            vec![
                "local_source_identity".to_string(),
                "source_locator_preview".to_string(),
            ],
            vec!["local_context_alignment_later".to_string()],
            vec!["no_file_reads".to_string(), "no_retrieval_execution".to_string()],
            "Plan later local source context alignment without reading any files.",
        ),
        scientific_evidence_pack_plan_item(
            "metadata_record",
            "metadata_record",
            "metadata_record",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "preferred_metadata_sources".to_string(),
                "citation_style".to_string(),
            ],
            vec!["metadata_evidence_plan".to_string()],
            normalized_preferred_metadata_sources.clone(),
            vec![
                "doi_or_stable_identifier".to_string(),
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
            ],
            vec!["metadata_alignment_later".to_string()],
            vec!["no_metadata_record_write".to_string()],
            "Plan later citation-safe metadata alignment without connector calls.",
        ),
        scientific_evidence_pack_plan_item(
            "source_family_record",
            "source_family_record",
            "source_family_record",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "preferred_psychology_source_families".to_string(),
                "psychology_source_connector_preview".to_string(),
            ],
            vec!["source_family_evidence_plan".to_string()],
            selected_psychology_source_families.clone(),
            vec![
                "family_requirement_alignment".to_string(),
                "access_boundary_notes".to_string(),
            ],
            vec!["psychology_family_alignment_later".to_string()],
            vec!["no_connector_call".to_string()],
            "Plan later psychology source-family alignment without connector calls.",
        ),
        scientific_evidence_pack_plan_item(
            "citation_attribution_record",
            "citation_attribution_record",
            "citation_attribution_record",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "preferred_metadata_sources".to_string(),
                "normalized_citation_style".to_string(),
            ],
            vec!["citation_attribution_plan".to_string()],
            normalized_preferred_metadata_sources.clone(),
            vec![
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "provider_record_id".to_string(),
            ],
            vec!["citation_attribution_later".to_string()],
            vec!["no_generated_citations".to_string()],
            "Plan later citation attribution without emitting citations now.",
        ),
        scientific_evidence_pack_plan_item(
            "quality_review_record",
            "quality_review_record",
            "quality_review_record",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "methodology_hints".to_string(),
                "population_hints".to_string(),
                "topic_area_hints".to_string(),
            ],
            vec!["evidence_quality_plan".to_string()],
            selected_local_source_ids.clone(),
            vec![
                "stable_identifier_present_later".to_string(),
                "peer_review_or_scholarly_status_later".to_string(),
            ],
            vec!["quality_review_later".to_string()],
            vec!["no_evidence_pack_created".to_string()],
            "Plan later evidence quality review without creating artifacts.",
        ),
        scientific_evidence_pack_plan_item(
            "claim_coverage_record",
            "claim_coverage_record",
            "claim_coverage_record",
            !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked),
            vec![
                "normalized_claim_scope".to_string(),
                "query_intent".to_string(),
            ],
            vec!["claim_coverage_plan".to_string()],
            Vec::new(),
            vec![
                "at_least_one_verified_metadata_record_later".to_string(),
                "no_answer_without_evidence_pack_later".to_string(),
            ],
            vec!["claim_coverage_review_later".to_string()],
            vec!["no_answer_generation".to_string()],
            "Plan later claim coverage review without fabricating support.",
        ),
    ];

    let planned_evidence_pack_steps = {
        let active = !matches!(status, ScholarChatScientificEvidencePackPlanStatus::Blocked);
        vec![
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::PsychologyConnectorAlignment,
                "psychology_connector_alignment",
                "Psychology connector alignment",
                "Align the evidence pack preview with the existing psychology source connector preview only.",
                vec![
                    format!("{:?}", psychology_source_connector_status),
                    format!("{:?}", source_family_strategy),
                    normalized_preferred_metadata_sources.join(", "),
                ],
                vec!["psychology_connector_alignment_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::EvidenceScopeReview,
                "evidence_scope_review",
                "Evidence scope review",
                "Review query, mode, and evidence goals without creating evidence artifacts.",
                vec![
                    normalized_query.clone(),
                    normalized_mode.clone(),
                    normalized_evidence_goal.clone().unwrap_or_default(),
                    normalized_claim_scope.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["evidence_scope_review_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::LocalEvidenceContextPlan,
                "local_evidence_context_plan",
                "Local evidence context plan",
                "Plan later local source context alignment without reading files.",
                vec![
                    selected_local_source_ids.join(", "),
                    normalized_mode.clone(),
                    normalized_claim_scope.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["local_evidence_context_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::MetadataEvidencePlan,
                "metadata_evidence_plan",
                "Metadata evidence plan",
                "Plan later citation-safe metadata alignment without metadata writes.",
                vec![
                    normalized_preferred_metadata_sources.join(", "),
                    normalized_citation_style.clone(),
                    format!("require_doi={}", request.require_doi.unwrap_or(false)),
                    format!("require_open_access={}", request.require_open_access.unwrap_or(false)),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["metadata_evidence_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::SourceFamilyEvidencePlan,
                "source_family_evidence_plan",
                "Source family evidence plan",
                "Plan later psychology source-family alignment without connector calls.",
                vec![
                    selected_psychology_source_families.join(", "),
                    format!("{:?}", source_family_strategy),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["source_family_evidence_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::ClaimCoveragePlan,
                "claim_coverage_plan",
                "Claim coverage plan",
                "Plan later claim coverage without fabricating support.",
                vec![
                    normalized_claim_scope.join(", "),
                    format!("{:?}", query_intent),
                    normalized_mode.clone(),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["claim_coverage_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::CitationAttributionPlan,
                "citation_attribution_plan",
                "Citation attribution plan",
                "Plan later citation attribution without emitting citations now.",
                vec![
                    normalized_citation_style.clone(),
                    normalized_preferred_metadata_sources.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["citation_attribution_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::EvidenceQualityPlan,
                "evidence_quality_plan",
                "Evidence quality plan",
                "Plan later evidence quality checks without ranking or retrieval execution.",
                vec![
                    normalized_methodology_hints.join(", "),
                    normalized_population_hints.join(", "),
                    normalized_topic_area_hints.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["evidence_quality_plan_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::ComplianceBoundaryCheck,
                "compliance_boundary_check",
                "Compliance boundary check",
                "Confirm evidence planning boundaries without any external calls.",
                vec![
                    format!("require_open_access={}", request.require_open_access.unwrap_or(false)),
                    format!("require_doi={}", request.require_doi.unwrap_or(false)),
                    normalized_preferred_metadata_sources.join(", "),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["compliance_boundary_check_preview".to_string()],
                active,
            ),
            scientific_evidence_pack_plan_step(
                ScholarChatScientificEvidencePackStepKind::DownstreamAnswerReadinessAlignment,
                "downstream_answer_readiness_alignment",
                "Downstream answer readiness alignment",
                "Plan downstream answer readiness only; no AnswerDraft or answer artifact is created now.",
                vec![
                    format!("{:?}", status),
                    normalized_claim_scope.join(", "),
                    normalized_citation_style.clone(),
                ]
                .into_iter()
                .filter(|value| !value.is_empty())
                .collect(),
                vec!["downstream_answer_readiness_alignment_preview".to_string()],
                active,
            ),
        ]
    };

    let summary = scientific_evidence_pack_plan_summary(&status, &evidence_pack_strategy);
    let next_required_actions = scientific_evidence_pack_plan_next_required_actions(
        &status,
        &normalized_mode,
        &normalized_preferred_metadata_sources,
        &normalized_requested_psychology_source_families,
        &selected_local_source_ids,
        &normalized_citation_style,
    );

    let mut blockers = psychology_source_connector_preview.blockers.clone();
    let mut warnings = psychology_source_connector_preview.warnings.clone();
    warnings.append(&mut style_warnings);
    push_unique_text(
        &mut warnings,
        "This is a scientific evidence pack preview only; it does not create an Evidence Pack, perform retrieval, or write artifacts.",
    );

    match status {
        ScholarChatScientificEvidencePackPlanStatus::Blocked => {
            if blockers.is_empty() {
                push_unique_text(
                    &mut blockers,
                    "scientific_query_missing",
                );
            }
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources => {
            push_unique_text(
                &mut blockers,
                "evidence_sources_missing",
            );
            push_unique_text(
                &mut warnings,
                "Select local sources, psychology source families, or metadata sources before evidence pack planning can become ready later.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata => {
            push_unique_text(
                &mut blockers,
                "citation_metadata_missing",
            );
            push_unique_text(
                &mut warnings,
                "Choose a DOI-capable or stable metadata path before citation planning can become ready later.",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady => {
            push_unique_text(
                &mut warnings,
                "evidence_pack_plan_ready",
            );
        }
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan
        | ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
        | ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
        | ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope
        | ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily => {
            push_unique_text(
                &mut warnings,
                "evidence_source_selection_needed",
            );
        }
    }

    Ok(ScholarChatScientificEvidencePackPlanPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids: selected_local_source_ids.clone(),
        selected_source_count: selected_local_source_ids.len(),
        normalized_expected_source_kinds,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families: normalized_requested_psychology_source_families,
        normalized_methodology_hints,
        normalized_population_hints,
        normalized_topic_area_hints,
        normalized_evidence_goal,
        normalized_claim_scope,
        normalized_citation_style,
        normalized_max_results_per_source: psychology_source_connector_preview.normalized_max_results_per_source,
        require_open_access: psychology_source_connector_preview.require_open_access,
        require_doi: psychology_source_connector_preview.require_doi,
        year_from: psychology_source_connector_preview.year_from,
        year_to: psychology_source_connector_preview.year_to,
        psychology_source_connector_status,
        metadata_connector_plan_status,
        scientific_search_plan_status,
        query_understanding_status,
        psychology_scope_status,
        source_family_strategy,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        evidence_pack_strategy,
        local_evidence_context_plan,
        metadata_evidence_plan,
        source_family_evidence_plan,
        claim_coverage_plan,
        unknown_claim_scope,
        citation_attribution_plan,
        evidence_quality_plan,
        evidence_compliance_plan,
        planned_evidence_items,
        planned_evidence_pack_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        scientific_evidence_pack_preview_only: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_scientific_paper_literature_review_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificPaperLiteratureReviewPlanRequest,
) -> AegisResult<ScholarChatScientificPaperLiteratureReviewPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let requested_mode = request
        .mode
        .as_deref()
        .map(|value| normalize_scientific_tag_text(value))
        .filter(|value| !value.is_empty());
    let normalized_mode = "scientific_paper".to_string();
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let normalized_expected_source_kinds =
        normalize_scientific_expected_source_kinds(request.expected_source_kinds.clone());
    let normalized_preferred_metadata_sources =
        normalize_metadata_connector_sources(request.preferred_metadata_sources.clone());
    let normalized_preferred_psychology_source_families = normalize_psychology_connector_values(
        request.preferred_psychology_source_families.clone(),
    );
    let normalized_methodology_hints =
        normalize_psychology_connector_values(request.methodology_hints.clone());
    let normalized_population_hints =
        normalize_psychology_connector_values(request.population_hints.clone());
    let normalized_topic_area_hints =
        normalize_psychology_connector_values(request.topic_area_hints.clone());
    let normalized_evidence_goal = request
        .evidence_goal
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());
    let normalized_claim_scope = normalize_scientific_claim_scope(request.claim_scope.clone());
    let (normalized_citation_style, _style_warnings) =
        normalize_scientific_citation_style(request.citation_style.clone());
    let normalized_review_goal = request
        .review_goal
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());
    let normalized_research_question = request
        .research_question
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());
    let (normalized_review_section_preferences, unknown_review_section_preferences) =
        normalize_scientific_paper_review_section_preferences(
            request.review_section_preferences.clone(),
        );
    let normalized_course_context = request
        .course_context
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());

    let evidence_pack_preview = preview_scholar_chat_scientific_evidence_pack_plan(
        &root,
        ScholarChatScientificEvidencePackPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: normalized_course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
            expected_source_kinds: Some(normalized_expected_source_kinds.clone()),
            preferred_metadata_sources: Some(normalized_preferred_metadata_sources.clone()),
            preferred_psychology_source_families: Some(
                normalized_preferred_psychology_source_families.clone(),
            ),
            methodology_hints: Some(normalized_methodology_hints.clone()),
            population_hints: Some(normalized_population_hints.clone()),
            topic_area_hints: Some(normalized_topic_area_hints.clone()),
            evidence_goal: normalized_evidence_goal.clone(),
            claim_scope: Some(normalized_claim_scope.clone()),
            citation_style: Some(normalized_citation_style.clone()),
            max_results_per_source: request.max_results_per_source,
            require_open_access: request.require_open_access,
            require_doi: request.require_doi,
            year_from: request.year_from,
            year_to: request.year_to,
        },
    )?;

    let evidence_pack_plan_status = evidence_pack_preview.status.clone();
    let psychology_source_connector_status =
        evidence_pack_preview.psychology_source_connector_status.clone();
    let metadata_connector_plan_status = evidence_pack_preview.metadata_connector_plan_status.clone();
    let scientific_search_plan_status = evidence_pack_preview.scientific_search_plan_status.clone();
    let query_understanding_status = evidence_pack_preview.query_understanding_status.clone();
    let psychology_scope_status = evidence_pack_preview.psychology_scope_status.clone();
    let source_family_strategy = evidence_pack_preview.source_family_strategy.clone();
    let evidence_pack_strategy = evidence_pack_preview.evidence_pack_strategy.clone();
    let inferred_topic = evidence_pack_preview.inferred_topic.clone();
    let query_intent = evidence_pack_preview.query_intent.clone();
    let recognized_concept = evidence_pack_preview.recognized_concept.clone();
    let label = evidence_pack_preview.label.clone();
    let selected_source_count = evidence_pack_preview.selected_source_count;
    let normalized_max_results_per_source = evidence_pack_preview.normalized_max_results_per_source;
    let require_open_access = evidence_pack_preview.require_open_access;
    let require_doi = evidence_pack_preview.require_doi;
    let year_from = evidence_pack_preview.year_from;
    let year_to = evidence_pack_preview.year_to;
    let status = scientific_paper_literature_review_plan_status(
        &evidence_pack_plan_status,
        &normalized_research_question,
    );
    let literature_review_strategy = scientific_paper_literature_review_strategy(
        &status,
        &normalized_citation_style,
        require_doi,
    );

    let mut warnings = evidence_pack_preview.warnings.clone();
    for unknown_section in &unknown_review_section_preferences {
        push_unique_text(
            &mut warnings,
            &format!(
                "Unknown review section preference '{unknown_section}' is retained only as a hint."
            ),
        );
    }
    if normalized_preferred_psychology_source_families
        .iter()
        .any(|family| family == "apa_psycnet_discovery")
    {
        push_unique_text(
            &mut warnings,
            "APA/PsycINFO planning boundary only; no automated authenticated scraping, no paywall bypass, and manual or institutional access review is required later.",
        );
    }

    let mut blockers = evidence_pack_preview.blockers.clone();
    let mut next_required_actions = evidence_pack_preview.next_required_actions.clone();
    if normalized_query.is_empty() {
        push_unique_text(&mut blockers, "query_missing");
        push_unique_text(
            &mut next_required_actions,
            "provide scientific query before literature-review planning",
        );
    }

    match status {
        ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked => {
            push_unique_text(
                &mut next_required_actions,
                "provide scientific query before literature-review planning",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsDisambiguation => {
            push_unique_text(
                &mut next_required_actions,
                "narrow scientific concept before Literature Review planning",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::UnknownConcept => {
            push_unique_text(
                &mut next_required_actions,
                "add discipline/source mappings before Literature Review planning",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsPsychologyScope => {
            push_unique_text(
                &mut next_required_actions,
                "add psychology topic, method, population, or course context",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsSourceFamily => {
            push_unique_text(
                &mut next_required_actions,
                "choose a supported psychology source family",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidenceSources => {
            push_unique_text(
                &mut next_required_actions,
                "select local sources or supported metadata/source families later",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsCitationMetadata => {
            push_unique_text(
                &mut next_required_actions,
                "provide DOI/stable identifier path or a supported metadata source later",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidencePackPlan => {
            push_unique_text(
                &mut next_required_actions,
                "resolve Scientific Evidence Pack Preview blockers first",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion => {
            push_unique_text(&mut blockers, "research_question_missing");
            push_unique_text(
                &mut next_required_actions,
                "provide or approve a research question before literature-review generation can be planned as ready",
            );
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady => {
            push_unique_text(
                &mut next_required_actions,
                "keep this preview-only and plan the literature review later",
            );
        }
    }

    let summary = match status {
        ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked => {
            "Scientific paper literature review preview is blocked until the query and upstream scientific evidence planning become available."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsDisambiguation => {
            "Scientific paper literature review preview needs query disambiguation before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::UnknownConcept => {
            "Scientific paper literature review preview found an unknown concept and can only outline later planning steps."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsPsychologyScope => {
            "Scientific paper literature review preview needs psychology scope before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsSourceFamily => {
            "Scientific paper literature review preview needs a supported psychology source family before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidenceSources => {
            "Scientific paper literature review preview needs local sources, psychology source families, or metadata sources before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsCitationMetadata => {
            "Scientific paper literature review preview needs citation metadata planning before it can become ready later."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidencePackPlan => {
            "Scientific paper literature review preview is waiting for the scientific evidence pack preview before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion => {
            "Scientific paper literature review preview needs a research question before later planning can continue."
                .to_string()
        }
        ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady => format!(
            "Scientific paper literature review preview is ready later and only describes {:?} planning.",
            literature_review_strategy
        ),
    };

    let planned_sections = if normalized_review_section_preferences.is_empty() {
        vec![
            "background".to_string(),
            "search_strategy".to_string(),
            "evidence_map".to_string(),
            "findings".to_string(),
            "limitations".to_string(),
            "research_gap".to_string(),
            "citation_plan".to_string(),
            "compliance_notes".to_string(),
        ]
    } else {
        let requested_section_set = normalized_review_section_preferences
            .iter()
            .cloned()
            .collect::<BTreeSet<_>>();
        scientific_paper_literature_review_supported_sections()
            .into_iter()
            .filter(|section| {
                requested_section_set.contains(*section)
                    || matches!(*section, "citation_plan" | "compliance_notes")
            })
            .map(|section| section.to_string())
            .collect::<Vec<_>>()
    };

    let mut section_requirements = vec!["evidence_pack_plan_ready_later".to_string()];
    if planned_sections.iter().any(|section| section == "findings") {
        push_unique_text(&mut section_requirements, "claim_coverage_plan_later");
    }
    if planned_sections.iter().any(|section| section == "citation_plan") {
        push_unique_text(&mut section_requirements, "verified_citation_metadata_later");
    }
    if planned_sections
        .iter()
        .any(|section| section == "compliance_notes")
    {
        push_unique_text(&mut section_requirements, "compliance_boundary_review_later");
    }

    let research_question_plan = ScholarChatScientificPaperResearchQuestionPlan {
        research_question_present: normalized_research_question.is_some(),
        normalized_research_question: normalized_research_question.clone(),
        will_derive_research_question_later: normalized_research_question.is_none(),
        required_inputs: {
            let mut inputs = vec![
                "normalized_query".to_string(),
                "evidence_pack_plan".to_string(),
            ];
            if normalized_review_goal.is_some() {
                inputs.push("review_goal".to_string());
            }
            if !normalized_methodology_hints.is_empty() {
                inputs.push("methodology_hints".to_string());
            }
            if !normalized_population_hints.is_empty() {
                inputs.push("population_hints".to_string());
            }
            if !normalized_topic_area_hints.is_empty() {
                inputs.push("topic_area_hints".to_string());
            }
            inputs
        },
        planned_outputs: vec![
            "research_question_later".to_string(),
            "inclusion_scope_later".to_string(),
            "review_focus_later".to_string(),
        ],
        summary: "No research question is generated now; the preview only reserves later derivation from the scientific query and evidence planning."
            .to_string(),
    };

    let search_strategy_plan = ScholarChatScientificPaperSearchStrategyPlan {
        query_understanding_status: evidence_pack_preview.query_understanding_status.clone(),
        scientific_search_plan_status: evidence_pack_preview.scientific_search_plan_status.clone(),
        planned_search_inputs: {
            let mut inputs = vec!["normalized_query".to_string()];
            if let Some(concept) = recognized_concept.as_ref() {
                inputs.push(format!("recognized_concept={concept}"));
            }
            if let Some(topic) = inferred_topic.as_ref() {
                inputs.push(format!("inferred_topic={topic}"));
            }
            if !normalized_methodology_hints.is_empty() {
                inputs.push(format!(
                    "methodology_hints={}",
                    normalized_methodology_hints.join(", ")
                ));
            }
            if !normalized_population_hints.is_empty() {
                inputs.push(format!(
                    "population_hints={}",
                    normalized_population_hints.join(", ")
                ));
            }
            if !normalized_topic_area_hints.is_empty() {
                inputs.push(format!(
                    "topic_area_hints={}",
                    normalized_topic_area_hints.join(", ")
                ));
            }
            if let Some(year_from) = year_from {
                inputs.push(format!("year_from={year_from}"));
            }
            if let Some(year_to) = year_to {
                inputs.push(format!("year_to={year_to}"));
            }
            if require_open_access {
                inputs.push("require_open_access=true".to_string());
            }
            if require_doi {
                inputs.push("require_doi=true".to_string());
            }
            if !normalized_expected_source_kinds.is_empty() {
                inputs.push(format!(
                    "expected_source_kinds={}",
                    normalized_expected_source_kinds.join(", ")
                ));
            }
            inputs
        },
        planned_search_filters: {
            let mut filters = Vec::new();
            if let Some(year_from) = year_from {
                filters.push(format!("year_from={year_from}"));
            }
            if let Some(year_to) = year_to {
                filters.push(format!("year_to={year_to}"));
            }
            if require_open_access {
                filters.push("open_access_only_later".to_string());
            }
            if require_doi {
                filters.push("doi_required_later".to_string());
            }
            if !selected_local_source_ids.is_empty()
                || !normalized_preferred_metadata_sources.is_empty()
                || !normalized_preferred_psychology_source_families.is_empty()
            {
                filters.push("source_family_filter_later".to_string());
            }
            filters
        },
        planned_source_channels: {
            let mut channels = Vec::new();
            if !selected_local_source_ids.is_empty() {
                channels.push("local_source_context".to_string());
            }
            if !normalized_preferred_metadata_sources.is_empty() {
                channels.push(format!(
                    "metadata_sources={}",
                    normalized_preferred_metadata_sources.join(", ")
                ));
            }
            if !normalized_preferred_psychology_source_families.is_empty() {
                channels.push(format!(
                    "psychology_source_families={}",
                    normalized_preferred_psychology_source_families.join(", ")
                ));
            }
            channels
        },
        will_run_search: false,
        will_call_connectors: false,
        summary: "Search strategy planning stays preview-only and only outlines later search inputs, filters, and source channels."
            .to_string(),
    };

    let evidence_pack_dependency_plan = ScholarChatScientificPaperEvidencePackDependencyPlan {
        evidence_pack_plan_status: evidence_pack_plan_status.clone(),
        evidence_pack_strategy: evidence_pack_strategy.clone(),
        required_evidence_pack_fields: vec![
            "evidence_items".to_string(),
            "citation_attribution_plan".to_string(),
            "evidence_quality_plan".to_string(),
            "compliance_plan".to_string(),
            "claim_coverage_plan".to_string(),
            "source_family_evidence_plan".to_string(),
            "metadata_evidence_plan".to_string(),
        ],
        required_evidence_item_kinds: vec![
            "local_source_context".to_string(),
            "metadata_record".to_string(),
            "source_family_record".to_string(),
            "citation_attribution_record".to_string(),
            "quality_review_record".to_string(),
            "claim_coverage_record".to_string(),
        ],
        will_create_evidence_pack: false,
        summary: "Scientific paper literature review planning depends on the scientific evidence pack preview only and does not create an Evidence Pack."
            .to_string(),
    };

    let review_section_plan = ScholarChatScientificPaperReviewSectionPlan {
        requested_sections: normalized_review_section_preferences.clone(),
        planned_sections,
        unknown_sections: unknown_review_section_preferences.clone(),
        section_requirements: section_requirements.clone(),
        will_generate_section_text: false,
        summary: "Review section planning stays preview-only and only prepares later section structure."
            .to_string(),
    };

    let claim_synthesis_plan = {
        let mut synthesis_units = vec![
            "claim_group_later".to_string(),
            "evidence_cluster_later".to_string(),
            "citation_support_later".to_string(),
            "limitation_note_later".to_string(),
            "research_gap_note_later".to_string(),
        ];
        if !normalized_claim_scope.is_empty() {
            push_unique_text(&mut synthesis_units, "claim_scope_alignment_later");
        }
        ScholarChatScientificPaperClaimSynthesisPlan {
            claim_scope: normalized_claim_scope.clone(),
            unknown_claim_scope: evidence_pack_preview.unknown_claim_scope.clone(),
            synthesis_units,
            evidence_requirements: vec![
                "no_claim_without_evidence_item_later".to_string(),
                "no_generated_sources".to_string(),
                "unsupported_claims_marked_for_review_later".to_string(),
                "evidence_pack_required_before_synthesis_later".to_string(),
            ],
            unsupported_claim_handling: vec![
                "mark_unsupported_claims_for_review_later".to_string(),
                "do_not_fabricate_supporting_sources".to_string(),
                "block_literature_review_generation_until_grounded_later".to_string(),
            ],
            will_generate_claims: false,
            summary: "Claim synthesis planning stays preview-only and only describes later claim grouping and support constraints."
                .to_string(),
        }
    };

    let citation_plan = ScholarChatScientificPaperCitationPlan {
        citation_style: normalized_citation_style.clone(),
        required_citation_fields: vec![
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "doi_or_stable_identifier".to_string(),
            "access_or_metadata_provider_note_later".to_string(),
        ],
        attribution_requirements: vec![
            "cite_metadata_provider_later".to_string(),
            "preserve_provider_record_ids_later".to_string(),
            "preserve_doi_and_source_url_later".to_string(),
            "include_access_date_later".to_string(),
            "no_fabricated_citations".to_string(),
        ],
        citation_safety_notes: vec![
            "metadata_preview_not_citation_evidence".to_string(),
            "full_citation_requires_verified_metadata_later".to_string(),
            "no_generated_sources".to_string(),
            "no_literature_review_without_evidence_pack_later".to_string(),
        ],
        will_emit_citations_now: false,
        summary: "Citation planning stays preview-only and only prepares later citation-safe attribution."
            .to_string(),
    };

    let quality_review_plan = ScholarChatScientificPaperQualityReviewPlan {
        quality_signals: vec![
            "stable_identifier_present_later".to_string(),
            "peer_review_or_scholarly_status_later".to_string(),
            "methodology_match_later".to_string(),
            "topic_area_match_later".to_string(),
            "population_match_later_when_available".to_string(),
            "source_family_relevance_later".to_string(),
            "evidence_pack_completeness_later".to_string(),
        ],
        review_checks: vec![
            "verify_metadata_before_citation".to_string(),
            "verify_evidence_item_before_review".to_string(),
            "verify_claim_coverage_before_generation".to_string(),
            "verify_limitations_are_marked_later".to_string(),
        ],
        exclusion_rules: vec![
            "exclude_fabricated_sources".to_string(),
            "exclude_unverified_citations".to_string(),
            "exclude_paywalled_full_text_scraping".to_string(),
            "exclude_automated_authenticated_library_records".to_string(),
            "exclude_non_scientific_sources_unless_explicitly_requested".to_string(),
        ],
        required_manual_checks_later: vec![
            "citation_metadata_review_later".to_string(),
            "methodology_scope_review_later".to_string(),
            "population_scope_review_later_when_applicable".to_string(),
            "institutional_access_review_later_when_required".to_string(),
        ],
        summary: "Quality review planning stays preview-only and only prepares later quality and review checks."
            .to_string(),
    };

    let compliance_review_plan = ScholarChatScientificPaperComplianceReviewPlan {
        no_fulltext_download: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        external_metadata_only: true,
        will_call_external_services: false,
        will_write_metadata: false,
        will_create_literature_review: false,
        summary: "Compliance planning stays preview-only and only documents later external metadata boundaries."
            .to_string(),
    };

    let step_active = !matches!(
        status,
        ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked
    );
    let planned_literature_review_steps = vec![
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::ScientificPaperModeAlignment,
            "scientific_paper_mode_alignment",
            "Scientific paper mode alignment",
            "Align the literature-review preview with Scientific Paper Mode only.",
            vec![
                normalized_mode.clone(),
                normalized_query.clone(),
                requested_mode
                    .clone()
                    .unwrap_or_else(|| "scientific_paper".to_string()),
            ],
            vec!["scientific_paper_literature_review_preview".to_string()],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::EvidencePackPlanAlignment,
            "evidence_pack_plan_alignment",
            "Evidence pack plan alignment",
            "Align the literature-review preview with the scientific evidence pack preview only.",
            vec![
                format!("{:?}", evidence_pack_plan_status),
                format!("{:?}", evidence_pack_strategy),
            ],
            vec!["evidence_pack_dependency_plan".to_string()],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::ResearchQuestionPlanning,
            "research_question_planning",
            "Research question planning",
            "Reserve later research-question derivation without generating one now.",
            vec![
                normalized_query.clone(),
                normalized_review_goal.clone().unwrap_or_default(),
                normalized_methodology_hints.join(", "),
                normalized_population_hints.join(", "),
                normalized_topic_area_hints.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "research_question_later".to_string(),
                "inclusion_scope_later".to_string(),
                "review_focus_later".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::SearchStrategyPlanning,
            "search_strategy_planning",
            "Search strategy planning",
            "Plan later search inputs, filters, and source channels without running search.",
            vec![
                normalized_query.clone(),
                recognized_concept.clone().unwrap_or_default(),
                inferred_topic.clone().unwrap_or_default(),
                normalized_methodology_hints.join(", "),
                normalized_population_hints.join(", "),
                normalized_topic_area_hints.join(", "),
                year_from
                    .map(|value| format!("year_from={value}"))
                    .unwrap_or_default(),
                year_to
                    .map(|value| format!("year_to={value}"))
                    .unwrap_or_default(),
                if require_open_access {
                    "require_open_access=true".to_string()
                } else {
                    String::new()
                },
                if require_doi {
                    "require_doi=true".to_string()
                } else {
                    String::new()
                },
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "planned_search_inputs".to_string(),
                "planned_search_filters".to_string(),
                "planned_source_channels".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::EvidenceMapPlanning,
            "evidence_map_planning",
            "Evidence map planning",
            "Plan later evidence-map structure from the scientific evidence pack preview only.",
            vec![
                selected_local_source_ids.join(", "),
                normalized_preferred_metadata_sources.join(", "),
                normalized_preferred_psychology_source_families.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["evidence_map_later".to_string()],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::ReviewSectionPlanning,
            "review_section_planning",
            "Review section planning",
            "Plan later literature-review sections without generating prose.",
            vec![
                normalized_review_section_preferences.join(", "),
                unknown_review_section_preferences.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "planned_sections".to_string(),
                "unknown_sections".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::ClaimSynthesisPlanning,
            "claim_synthesis_planning",
            "Claim synthesis planning",
            "Plan later claim grouping and synthesis without generating claims.",
            vec![
                normalized_claim_scope.join(", "),
                evidence_pack_preview.unknown_claim_scope.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "synthesis_units".to_string(),
                "evidence_requirements".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::CitationPlanning,
            "citation_planning",
            "Citation planning",
            "Plan later citation-safe attribution without emitting citations now.",
            vec![
                normalized_citation_style.clone(),
                if require_doi {
                    "require_doi=true".to_string()
                } else {
                    String::new()
                },
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "required_citation_fields".to_string(),
                "citation_safety_notes".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::QualityReviewPlanning,
            "quality_review_planning",
            "Quality review planning",
            "Plan later metadata, methodology, and citation quality checks without executing them.",
            vec![
                selected_local_source_ids.join(", "),
                normalized_preferred_metadata_sources.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "review_checks".to_string(),
                "required_manual_checks_later".to_string(),
            ],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::ComplianceBoundaryCheck,
            "compliance_boundary_check",
            "Compliance boundary check",
            "Plan later compliance boundaries without making web or connector calls.",
            vec![
                format!("{:?}", evidence_pack_plan_status),
                format!("{:?}", evidence_pack_strategy),
            ],
            vec!["compliance_review_plan".to_string()],
            step_active,
        ),
        scientific_paper_literature_review_step(
            ScholarChatScientificPaperLiteratureReviewStepKind::DownstreamPaperGenerationAlignment,
            "downstream_paper_generation_alignment",
            "Downstream paper-generation alignment",
            "Reserve later alignment for a future literature-review generation phase.",
            vec![
                "review_section_plan".to_string(),
                "claim_synthesis_plan".to_string(),
                "citation_plan".to_string(),
            ],
            vec!["future_literature_review_alignment".to_string()],
            step_active,
        ),
    ];

    Ok(ScholarChatScientificPaperLiteratureReviewPlanPreview {
        status,
        normalized_query,
        requested_mode,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids,
        selected_source_count,
        normalized_expected_source_kinds,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families,
        normalized_methodology_hints,
        normalized_population_hints,
        normalized_topic_area_hints,
        normalized_evidence_goal,
        normalized_claim_scope,
        unknown_claim_scope: evidence_pack_preview.unknown_claim_scope.clone(),
        normalized_citation_style,
        normalized_review_goal,
        normalized_research_question,
        normalized_review_section_preferences: normalized_review_section_preferences.clone(),
        unknown_review_section_preferences: unknown_review_section_preferences.clone(),
        normalized_max_results_per_source,
        require_open_access,
        require_doi,
        year_from,
        year_to,
        evidence_pack_plan_status,
        psychology_source_connector_status,
        metadata_connector_plan_status,
        scientific_search_plan_status,
        query_understanding_status,
        psychology_scope_status,
        source_family_strategy,
        evidence_pack_strategy,
        inferred_topic,
        query_intent,
        recognized_concept,
        label,
        literature_review_strategy,
        research_question_plan,
        search_strategy_plan,
        evidence_pack_dependency_plan,
        review_section_plan,
        claim_synthesis_plan,
        citation_plan,
        quality_review_plan,
        compliance_review_plan,
        planned_literature_review_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        scientific_paper_literature_review_preview_only: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_literature_review_created: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_answer_readiness(
    root: impl Into<PathBuf>,
    request: ScholarChatAnswerReadinessRequest,
) -> AegisResult<ScholarChatAnswerReadinessPreview> {
    let root = root.into();
    let scholar_chat_request = request.scholar_chat_request;
    let request_preview = preview_scholar_chat_request(&root, scholar_chat_request.clone())?;
    let retrieval_preview = preview_scholar_chat_retrieval(&root, scholar_chat_request.clone())?;
    let evidence_plan_preview = preview_scholar_chat_evidence_plan(&root, scholar_chat_request.clone())?;
    let prompt_pack_preview = preview_scholar_chat_prompt_pack(&root, scholar_chat_request.clone())?;
    let runtime_health_preview = preview_local_model_runtime_health(&root, request.runtime_config.clone())?;
    let invocation_plan_preview = preview_local_runtime_invocation_plan(
        &root,
        LocalRuntimeInvocationPlanRequest {
            runtime_config: request.runtime_config,
            prompt_text: Some(request_preview.normalized_prompt.clone()),
            estimated_input_char_count: Some(request_preview.normalized_prompt.chars().count() as u32),
            max_output_tokens: Some(1),
            stop_sequences: None,
        },
    )?;

    let selected_source_count = request_preview.selected_source_count;
    let retrieval_candidate_count = retrieval_preview.candidate_count;
    let evidence_candidate_count = evidence_plan_preview.evidence_candidate_count;
    let prompt_pack_ready = prompt_pack_preview.prompt_pack.section_count > 0;
    let runtime_ready = matches!(runtime_health_preview.status, LocalModelRuntimeHealthStatus::ReadyToTestLater);
    let invocation_ready = matches!(invocation_plan_preview.status, LocalRuntimeInvocationPlanStatus::ReadyToInvokeLater);

    let mut blockers = Vec::new();
    let mut warnings = Vec::new();
    let mut next_required_actions = Vec::new();

    for warning in request_preview.warnings {
        push_readiness_warning(&mut warnings, "request_preview", &warning);
    }
    for warning in retrieval_preview.warnings {
        push_readiness_warning(&mut warnings, "retrieval_preview", &warning);
    }
    for warning in evidence_plan_preview.warnings {
        push_readiness_warning(&mut warnings, "evidence_plan_preview", &warning);
    }
    for warning in prompt_pack_preview.warnings {
        push_readiness_warning(&mut warnings, "prompt_pack_preview", &warning);
    }
    for warning in runtime_health_preview.warnings {
        push_readiness_warning(&mut warnings, &warning.kind, &warning.message);
    }
    for warning in invocation_plan_preview.plan.warnings {
        push_readiness_warning(&mut warnings, &warning.kind, &warning.message);
    }
    for blocker in invocation_plan_preview.plan.blockers {
        push_readiness_blocker(&mut blockers, &blocker.kind, &blocker.message);
    }

    if selected_source_count == 0 {
        push_readiness_action(
            &mut next_required_actions,
            "Select one or more Scholar Chat sources.",
        );
        if matches!(request_preview.grounding_policy, GroundingPolicy::LocalOnly) {
            push_readiness_blocker(
                &mut blockers,
                "needs_sources",
                "local_only requires selected sources before a grounded draft can be prepared.",
            );
        }
    }

    if selected_source_count > 0 && retrieval_candidate_count == 0 {
        push_readiness_action(
            &mut next_required_actions,
            "Build or refresh the retrieval index for the selected sources.",
        );
        if matches!(request_preview.grounding_policy, GroundingPolicy::LocalOnly) {
            push_readiness_blocker(
                &mut blockers,
                "blocked",
                "local_only requires local evidence before a grounded draft can be prepared.",
            );
        } else {
            push_readiness_blocker(
                &mut blockers,
                "needs_retrieval_index",
                "Retrieval data is not ready for the selected sources yet.",
            );
        }
    }

    if selected_source_count > 0 && evidence_candidate_count == 0 {
        push_readiness_action(
            &mut next_required_actions,
            "Assemble local evidence candidates for the selected sources.",
        );
        if matches!(request_preview.grounding_policy, GroundingPolicy::LocalOnly) {
            push_readiness_blocker(
                &mut blockers,
                "blocked",
                "local_only requires local evidence before a grounded draft can be prepared.",
            );
        } else {
            push_readiness_blocker(
                &mut blockers,
                "needs_evidence_candidates",
                "No evidence candidates are available yet for the selected sources.",
            );
        }
    }

    if !runtime_ready {
        push_readiness_action(
            &mut next_required_actions,
            "Configure a local runtime model and executable.",
        );
        push_readiness_blocker(
            &mut blockers,
            "needs_runtime_config",
            "The local runtime configuration is not ready yet.",
        );
    }

    if !request.allow_model_execution {
        push_readiness_action(
            &mut next_required_actions,
            "Allow future model execution when you are ready to proceed.",
        );
        push_readiness_blocker(
            &mut blockers,
            "needs_execution_consent",
            "Future model execution is not allowed yet.",
        );
    }

    if prompt_pack_ready {
        push_readiness_action(
            &mut next_required_actions,
            "The prompt pack can be assembled later from the current request preview.",
        );
    }

    let status = readiness_status(
        request_preview.grounding_policy.clone(),
        selected_source_count,
        retrieval_candidate_count,
        evidence_candidate_count,
        runtime_ready,
        invocation_ready,
        request.allow_model_execution,
    );

    if matches!(request_preview.grounding_policy, GroundingPolicy::AllowMarkedModelKnowledge)
        && runtime_ready
        && invocation_ready
        && request.allow_model_execution
    {
        push_readiness_warning(
            &mut warnings,
            "future_draft_marking_required",
            "A future ungrounded draft would need explicit model-knowledge marking later.",
        );
    }

    Ok(ScholarChatAnswerReadinessPreview {
        status: status.clone(),
        normalized_prompt: request_preview.normalized_prompt,
        mode: request_preview.mode,
        grounding_policy: request_preview.grounding_policy,
        selected_source_count,
        retrieval_candidate_count,
        evidence_candidate_count,
        prompt_pack_ready,
        runtime_health_status: runtime_health_preview.status,
        invocation_plan_status: invocation_plan_preview.status,
        allow_model_execution: request.allow_model_execution,
        would_generate_answer_now: false,
        would_build_evidence_pack_now: false,
        would_create_final_answer_now: false,
        future_output_classification: readiness_output_classification(status),
        blockers,
        warnings,
        next_required_actions,
    })
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataExecutionBoundaryStatus {
    Blocked,
    ExecutionBoundaryReady,
    ExecutionRequestedButDisabled,
    NeedsMetadataConnectorPlan,
    NeedsProviderSelection,
    NeedsProviderTermsReview,
    NeedsNetworkPermission,
    NeedsWritePermission,
    NeedsCitationMetadata,
    NeedsEvidenceSources,
    NeedsDisambiguation,
    UnknownConcept,
    NeedsPsychologyScope,
    NeedsSourceFamily,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataExecutionBoundaryStrategy {
    DryRunOnly,
    ProviderPlanOnly,
    Blocked,
    ExecutionGatePending,
    MetadataWriteGatePending,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataExecutionBoundaryStepKind {
    EvidencePackAlignment,
    MetadataConnectorAlignment,
    ProviderSelection,
    ProviderTermsBoundary,
    NetworkPermissionGate,
    ProviderRequestPlanning,
    MetadataWriteGate,
    SafetyBoundaryCheck,
    DownstreamEvidencePackAlignment,
    DownstreamLiteratureReviewAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryPreviewRequest {
    pub query: String,
    pub mode: Option<String>,
    pub course_context: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub selected_local_source_ids: Option<Vec<String>>,
    pub expected_source_kinds: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub methodology_hints: Option<Vec<String>>,
    pub population_hints: Option<Vec<String>>,
    pub topic_area_hints: Option<Vec<String>>,
    pub evidence_goal: Option<String>,
    pub claim_scope: Option<Vec<String>>,
    pub citation_style: Option<String>,
    pub max_results_per_source: Option<u32>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    #[serde(default)]
    pub execution_requested: bool,
    #[serde(default)]
    pub allow_network: bool,
    #[serde(default)]
    pub allow_provider_terms_unreviewed: bool,
    #[serde(default)]
    pub allow_metadata_record_write: bool,
    pub provider_override: Option<Vec<String>>,
    pub dry_run_label: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryProviderSelectionPlan {
    pub requested_provider_ids: Vec<String>,
    pub supported_provider_ids: Vec<String>,
    pub selected_provider_ids: Vec<String>,
    pub unknown_provider_ids: Vec<String>,
    pub needs_provider_selection: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryProviderGate {
    pub provider_id: String,
    pub provider_label: String,
    pub connector_kind: String,
    pub selected: bool,
    pub requires_network_permission: bool,
    pub requires_terms_review: bool,
    pub requires_institutional_access_review_later: bool,
    pub requires_write_permission: bool,
    pub can_run_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryProviderGatePlan {
    pub supported_provider_ids: Vec<String>,
    pub selected_provider_ids: Vec<String>,
    pub supported_provider_count: usize,
    pub selected_provider_count: usize,
    pub provider_gates: Vec<ScholarChatScientificMetadataExecutionBoundaryProviderGate>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryProviderRequestPlan {
    pub selected_provider_ids: Vec<String>,
    pub selected_provider_count: usize,
    pub planned_query_inputs: Vec<String>,
    pub planned_filters: Vec<String>,
    pub planned_result_fields: Vec<String>,
    pub will_call_provider: bool,
    pub will_authenticate: bool,
    pub will_bypass_paywall: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryProviderTermsPlan {
    pub selected_provider_ids: Vec<String>,
    pub selected_provider_count: usize,
    pub terms_review_required: bool,
    pub requires_institutional_access_review_later: bool,
    pub allows_unreviewed_terms: bool,
    pub can_run_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryMetadataWritePlan {
    pub metadata_record_write_requested: bool,
    pub will_write_metadata_records: bool,
    pub persistence_target: String,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundarySafetyPlan {
    pub blocked_flags: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryStep {
    pub kind: ScholarChatScientificMetadataExecutionBoundaryStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub active: bool,
    pub preview_only: bool,
    pub boundary_notes: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataExecutionBoundaryPreview {
    pub status: ScholarChatScientificMetadataExecutionBoundaryStatus,
    pub normalized_query: String,
    pub normalized_mode: String,
    pub normalized_context_tags: Vec<String>,
    pub selected_local_source_ids: Vec<String>,
    pub selected_local_source_count: usize,
    pub normalized_expected_source_kinds: Vec<String>,
    pub normalized_preferred_metadata_sources: Vec<String>,
    pub normalized_preferred_psychology_source_families: Vec<String>,
    pub normalized_methodology_hints: Vec<String>,
    pub normalized_population_hints: Vec<String>,
    pub normalized_topic_area_hints: Vec<String>,
    pub normalized_evidence_goal: Option<String>,
    pub normalized_claim_scope: Vec<String>,
    pub normalized_citation_style: String,
    pub execution_requested: bool,
    pub allow_network: bool,
    pub allow_provider_terms_unreviewed: bool,
    pub allow_metadata_record_write: bool,
    pub normalized_provider_override: Option<Vec<String>>,
    pub normalized_dry_run_label: Option<String>,
    pub evidence_pack_plan_status: ScholarChatScientificEvidencePackPlanStatus,
    pub metadata_connector_plan_status: ScholarChatMetadataConnectorPlanStatus,
    pub psychology_source_connector_status: ScholarChatPsychologySourceConnectorPlanStatus,
    pub scientific_search_plan_status: ScholarChatScientificSearchPlanStatus,
    pub query_understanding_status: ScholarChatScientificQueryUnderstandingStatus,
    pub psychology_scope_status: ScholarChatPsychologyScopeStatus,
    pub source_family_strategy: ScholarChatPsychologySourceFamilyStrategy,
    pub evidence_pack_strategy: ScholarChatScientificEvidencePackStrategy,
    pub provider_selection_plan: ScholarChatScientificMetadataExecutionBoundaryProviderSelectionPlan,
    pub provider_gate_plan: ScholarChatScientificMetadataExecutionBoundaryProviderGatePlan,
    pub provider_request_plan: ScholarChatScientificMetadataExecutionBoundaryProviderRequestPlan,
    pub provider_terms_plan: ScholarChatScientificMetadataExecutionBoundaryProviderTermsPlan,
    pub metadata_write_plan: ScholarChatScientificMetadataExecutionBoundaryMetadataWritePlan,
    pub safety_boundary_plan: ScholarChatScientificMetadataExecutionBoundarySafetyPlan,
    pub planned_execution_steps: Vec<ScholarChatScientificMetadataExecutionBoundaryStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub metadata_execution_boundary_only: bool,
    pub dry_run_only: bool,
    pub execution_disabled: bool,
    pub no_network_call: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_metadata_persistence: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_literature_review_created: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataProviderConfigStatus {
    Blocked,
    ProviderConfigReady,
    NeedsProviderSelection,
    NeedsMetadataExecutionBoundary,
    NeedsProviderTermsReview,
    NeedsNetworkPolicyReview,
    NeedsInstitutionalAccessReview,
    NeedsAttributionPolicyReview,
    UnsupportedProvider,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataProviderConfigStrategy {
    ConfigPreviewOnly,
    ProviderPolicyReviewFirst,
    InstitutionalAccessReviewFirst,
    AttributionReviewFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataProviderAccessKind {
    OpenMetadata,
    PublicMetadataApi,
    InstitutionalDiscovery,
    AuthenticatedLibraryBoundary,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataProviderConfigStepKind {
    MetadataExecutionBoundaryAlignment,
    ProviderCapabilityCatalog,
    ProviderTermsPolicy,
    ProviderAccessPolicy,
    ProviderRateLimitPolicy,
    ProviderAttributionPolicy,
    ProviderSafetyBoundary,
    DownstreamMetadataExecutionAlignment,
    DownstreamEvidencePackAlignment,
    DownstreamLiteratureReviewAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderConfigPreviewRequest {
    pub query: String,
    pub mode: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub provider_override: Option<Vec<String>>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub config_goal: Option<String>,
    pub include_disabled_providers: Option<bool>,
    pub include_institutional_providers: Option<bool>,
    pub include_rate_limit_notes: Option<bool>,
    pub include_attribution_requirements: Option<bool>,
    #[serde(default)]
    pub execution_requested: bool,
    #[serde(default)]
    pub allow_network: bool,
    #[serde(default)]
    pub allow_provider_terms_unreviewed: bool,
    #[serde(default)]
    pub allow_metadata_record_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderConfigProviderRecord {
    pub provider_id: String,
    pub provider_label: String,
    pub supported: bool,
    pub selected: bool,
    pub access_kind: ScholarChatScientificMetadataProviderAccessKind,
    pub metadata_scope: String,
    pub planned_query_capabilities: Vec<String>,
    pub planned_filter_capabilities: Vec<String>,
    pub planned_result_fields: Vec<String>,
    pub requires_network_permission: bool,
    pub requires_terms_review: bool,
    pub requires_attribution: bool,
    pub requires_api_key_later: bool,
    pub requires_environment_later: bool,
    pub requires_institutional_access_review_later: bool,
    pub authenticated_access_blocked_now: bool,
    pub paywall_bypass_blocked_now: bool,
    pub can_execute_now: bool,
    pub will_call_provider: bool,
    pub will_use_http_client: bool,
    pub will_read_api_key: bool,
    pub will_read_environment: bool,
    pub will_write_metadata: bool,
    pub boundary_notes: Vec<String>,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderTermsPolicy {
    pub providers_requiring_terms_review: Vec<String>,
    pub providers_requiring_institutional_access_review_later: Vec<String>,
    pub terms_review_required: bool,
    pub unreviewed_terms_block_execution: bool,
    pub authenticated_access_blocked: bool,
    pub paywall_bypass_blocked: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderAccessPolicy {
    pub open_metadata_providers: Vec<String>,
    pub public_metadata_api_providers: Vec<String>,
    pub institutional_boundary_providers: Vec<String>,
    pub authenticated_access_blocked_now: bool,
    pub manual_access_review_required_later: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderRateLimitPolicy {
    pub providers_requiring_rate_limit_review_later: Vec<String>,
    pub provider_request_budget_planned_later: bool,
    pub will_enforce_rate_limit_now: bool,
    pub will_call_provider_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderAttributionPolicy {
    pub providers_requiring_attribution: Vec<String>,
    pub required_attribution_fields: Vec<String>,
    pub will_emit_citations_now: bool,
    pub will_write_attribution_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderSafetyBoundary {
    pub external_services_blocked_now: bool,
    pub connector_calls_blocked_now: bool,
    pub network_blocked_now: bool,
    pub http_client_blocked_now: bool,
    pub api_key_reads_blocked_now: bool,
    pub environment_reads_blocked_now: bool,
    pub scraping_blocked_now: bool,
    pub authenticated_library_access_blocked_now: bool,
    pub paywall_bypass_blocked_now: bool,
    pub metadata_writes_blocked_now: bool,
    pub source_import_blocked_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderConfigStep {
    pub kind: ScholarChatScientificMetadataProviderConfigStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub boundary_notes: Vec<String>,
    pub preview_only: bool,
    pub active: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataProviderConfigPreview {
    pub status: ScholarChatScientificMetadataProviderConfigStatus,
    pub normalized_query: String,
    pub normalized_mode: Option<String>,
    pub normalized_context_tags: Vec<String>,
    pub normalized_preferred_metadata_sources: Vec<String>,
    pub normalized_preferred_psychology_source_families: Vec<String>,
    pub normalized_provider_override: Option<Vec<String>>,
    pub unknown_provider_ids: Vec<String>,
    pub normalized_config_goal: Option<String>,
    pub include_disabled_providers: bool,
    pub include_institutional_providers: bool,
    pub include_rate_limit_notes: bool,
    pub include_attribution_requirements: bool,
    pub execution_requested: bool,
    pub allow_network: bool,
    pub allow_provider_terms_unreviewed: bool,
    pub allow_metadata_record_write: bool,
    pub metadata_execution_boundary_status: ScholarChatScientificMetadataExecutionBoundaryStatus,
    pub metadata_execution_boundary_strategy: ScholarChatScientificMetadataExecutionBoundaryStrategy,
    pub selected_provider_ids: Vec<String>,
    pub selected_provider_count: usize,
    pub provider_config_strategy: ScholarChatScientificMetadataProviderConfigStrategy,
    pub provider_configs: Vec<ScholarChatScientificMetadataProviderConfigProviderRecord>,
    pub provider_terms_policy: ScholarChatScientificMetadataProviderTermsPolicy,
    pub provider_access_policy: ScholarChatScientificMetadataProviderAccessPolicy,
    pub provider_rate_limit_policy: ScholarChatScientificMetadataProviderRateLimitPolicy,
    pub provider_attribution_policy: ScholarChatScientificMetadataProviderAttributionPolicy,
    pub provider_safety_boundary: ScholarChatScientificMetadataProviderSafetyBoundary,
    pub planned_config_steps: Vec<ScholarChatScientificMetadataProviderConfigStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub metadata_provider_config_preview_only: bool,
    pub dry_run_only: bool,
    pub execution_disabled: bool,
    pub no_network_call: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_metadata_persistence: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_literature_review_created: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataQueryPlanStatus {
    Blocked,
    QueryPlanReady,
    NeedsProviderConfig,
    NeedsProviderSelection,
    NeedsQueryGoal,
    NeedsNetworkPolicyReview,
    NeedsProviderTermsReview,
    NeedsInstitutionalAccessReview,
    UnsupportedProvider,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataQueryPlanStrategy {
    QueryPlanPreviewOnly,
    PublicMetadataQueryPlan,
    InstitutionalBoundaryQueryPlan,
    ProviderPolicyReviewFirst,
    Blocked,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataQueryProviderFamily {
    PublicMetadata,
    InstitutionalBoundary,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum ScholarChatScientificMetadataQueryPlanStepKind {
    ProviderConfigAlignment,
    ProviderFamilyPartition,
    QueryTemplatePlanning,
    FilterPlanning,
    ResultFieldPlanning,
    RateLimitAlignment,
    AttributionAlignment,
    SafetyBoundaryCheck,
    DownstreamMetadataConnectorAlignment,
    DownstreamEvidencePackAlignment,
    DownstreamLiteratureReviewAlignment,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatScientificMetadataQueryPlanPreviewRequest {
    pub query: String,
    pub mode: Option<String>,
    pub context_tags: Option<Vec<String>>,
    pub preferred_metadata_sources: Option<Vec<String>>,
    pub preferred_psychology_source_families: Option<Vec<String>>,
    pub provider_override: Option<Vec<String>>,
    pub query_goal: Option<String>,
    pub require_open_access: Option<bool>,
    pub require_doi: Option<bool>,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    #[serde(default)]
    pub include_disabled_providers: bool,
    #[serde(default)]
    pub include_institutional_providers: bool,
    #[serde(default)]
    pub include_rate_limit_notes: bool,
    #[serde(default)]
    pub include_attribution_requirements: bool,
    #[serde(default)]
    pub include_query_templates: bool,
    #[serde(default)]
    pub include_filter_plan: bool,
    #[serde(default)]
    pub include_result_field_plan: bool,
    #[serde(default)]
    pub execution_requested: bool,
    #[serde(default)]
    pub allow_network: bool,
    #[serde(default)]
    pub allow_provider_terms_unreviewed: bool,
    #[serde(default)]
    pub allow_metadata_record_write: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryPlanProviderRecord {
    pub provider_id: String,
    pub provider_label: String,
    pub provider_family: ScholarChatScientificMetadataQueryProviderFamily,
    pub access_kind_label: String,
    pub selected: bool,
    pub supported: bool,
    pub normalized_query: String,
    pub query_goal: Option<String>,
    pub planned_query_terms: Vec<String>,
    pub planned_query_templates: Vec<String>,
    pub planned_filters: Vec<String>,
    pub planned_result_fields: Vec<String>,
    pub planned_sorting: Vec<String>,
    pub planned_dedup_keys: Vec<String>,
    pub planned_rate_limit_notes: Vec<String>,
    pub planned_attribution_notes: Vec<String>,
    pub institutional_boundary_notes: Vec<String>,
    pub safety_boundary_notes: Vec<String>,
    pub can_execute_now: bool,
    pub will_call_provider: bool,
    pub will_build_url: bool,
    pub will_use_http_client: bool,
    pub will_read_api_key: bool,
    pub will_read_environment: bool,
    pub will_write_metadata: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryTemplatePlan {
    pub provider_ids: Vec<String>,
    pub public_metadata_provider_ids: Vec<String>,
    pub institutional_boundary_provider_ids: Vec<String>,
    pub normalized_query: String,
    pub normalized_query_goal: Option<String>,
    pub planned_query_templates: Vec<String>,
    pub query_template_preview_only: bool,
    pub will_build_urls: bool,
    pub will_call_providers: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryFilterPlan {
    pub require_open_access: bool,
    pub require_doi: bool,
    pub year_from: Option<u16>,
    pub year_to: Option<u16>,
    pub planned_filter_keys: Vec<String>,
    pub provider_filter_coverage: Vec<String>,
    pub filter_plan_preview_only: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryResultFieldPlan {
    pub planned_common_fields: Vec<String>,
    pub provider_specific_fields: Vec<String>,
    pub attribution_fields: Vec<String>,
    pub result_field_plan_preview_only: bool,
    pub will_emit_citations_now: bool,
    pub will_write_metadata_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryProviderFamilyBoundary {
    pub public_metadata_provider_ids: Vec<String>,
    pub institutional_boundary_provider_ids: Vec<String>,
    pub institutional_access_requires_manual_review_later: bool,
    pub authenticated_access_blocked_now: bool,
    pub automated_scraping_blocked_now: bool,
    pub paywall_bypass_blocked_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQuerySafetyBoundary {
    pub external_services_blocked_now: bool,
    pub url_building_blocked_now: bool,
    pub connector_calls_blocked_now: bool,
    pub network_blocked_now: bool,
    pub http_client_blocked_now: bool,
    pub api_key_reads_blocked_now: bool,
    pub environment_reads_blocked_now: bool,
    pub scraping_blocked_now: bool,
    pub authenticated_library_access_blocked_now: bool,
    pub paywall_bypass_blocked_now: bool,
    pub metadata_writes_blocked_now: bool,
    pub source_import_blocked_now: bool,
    pub summary: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct ScholarChatScientificMetadataQueryPlanStep {
    pub kind: ScholarChatScientificMetadataQueryPlanStepKind,
    pub id: String,
    pub label: String,
    pub description: String,
    pub planned_inputs: Vec<String>,
    pub planned_outputs: Vec<String>,
    pub boundary_notes: Vec<String>,
    pub preview_only: bool,
    pub active: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct ScholarChatScientificMetadataQueryPlanPreview {
    pub status: ScholarChatScientificMetadataQueryPlanStatus,
    pub normalized_query: String,
    pub normalized_mode: Option<String>,
    pub normalized_context_tags: Vec<String>,
    pub normalized_preferred_metadata_sources: Vec<String>,
    pub normalized_preferred_psychology_source_families: Vec<String>,
    pub normalized_provider_override: Option<Vec<String>>,
    pub unknown_provider_ids: Vec<String>,
    pub normalized_query_goal: Option<String>,
    pub include_disabled_providers: bool,
    pub include_institutional_providers: bool,
    pub include_rate_limit_notes: bool,
    pub include_attribution_requirements: bool,
    pub include_query_templates: bool,
    pub include_filter_plan: bool,
    pub include_result_field_plan: bool,
    pub execution_requested: bool,
    pub allow_network: bool,
    pub allow_provider_terms_unreviewed: bool,
    pub allow_metadata_record_write: bool,
    pub provider_config_status: ScholarChatScientificMetadataProviderConfigStatus,
    pub provider_config_strategy: ScholarChatScientificMetadataProviderConfigStrategy,
    pub selected_provider_ids: Vec<String>,
    pub selected_provider_count: usize,
    pub public_metadata_provider_ids: Vec<String>,
    pub institutional_boundary_provider_ids: Vec<String>,
    pub query_plan_strategy: ScholarChatScientificMetadataQueryPlanStrategy,
    pub provider_query_plans: Vec<ScholarChatScientificMetadataQueryPlanProviderRecord>,
    pub query_template_plan: ScholarChatScientificMetadataQueryTemplatePlan,
    pub filter_plan: ScholarChatScientificMetadataQueryFilterPlan,
    pub result_field_plan: ScholarChatScientificMetadataQueryResultFieldPlan,
    pub provider_family_boundary: ScholarChatScientificMetadataQueryProviderFamilyBoundary,
    pub safety_boundary: ScholarChatScientificMetadataQuerySafetyBoundary,
    pub planned_query_steps: Vec<ScholarChatScientificMetadataQueryPlanStep>,
    pub blockers: Vec<String>,
    pub warnings: Vec<String>,
    pub next_required_actions: Vec<String>,
    pub summary: String,
    pub preview_only: bool,
    pub metadata_query_plan_preview_only: bool,
    pub dry_run_only: bool,
    pub execution_disabled: bool,
    pub no_network_call: bool,
    pub no_web_request: bool,
    pub no_http_client: bool,
    pub no_api_key_read: bool,
    pub no_environment_read: bool,
    pub no_scraping: bool,
    pub no_authenticated_library_access: bool,
    pub no_paywall_bypass: bool,
    pub no_connector_call: bool,
    pub no_source_import: bool,
    pub no_metadata_record_write: bool,
    pub no_metadata_persistence: bool,
    pub no_local_file_indexing: bool,
    pub no_file_read: bool,
    pub no_pdf_extraction: bool,
    pub no_ocr: bool,
    pub no_chunking_run: bool,
    pub no_embedding_generation: bool,
    pub no_index_created: bool,
    pub no_retrieval_execution: bool,
    pub no_model_loading: bool,
    pub no_runtime_inference: bool,
    pub no_llm_call: bool,
    pub no_answer_generated: bool,
    pub no_literature_review_created: bool,
    pub no_evidence_pack_created: bool,
    pub no_artifact_write: bool,
    pub no_persistence: bool,
    pub no_registry_status_change: bool,
    pub no_audit_write: bool,
}

fn scientific_metadata_supported_providers() -> Vec<String> {
    vec![
        "openalex".to_string(),
        "crossref".to_string(),
        "pubmed".to_string(),
        "eric".to_string(),
        "apa_psycnet".to_string(),
        "psycinfo".to_string(),
    ]
}

fn scientific_metadata_provider_label(provider_id: &str) -> &'static str {
    match provider_id {
        "openalex" => "OpenAlex",
        "crossref" => "Crossref",
        "pubmed" => "PubMed",
        "eric" => "ERIC",
        "apa_psycnet" => "APA PsycNet",
        "psycinfo" => "PsycINFO",
        _ => "Unknown Scientific Metadata Provider",
    }
}

fn scientific_metadata_provider_connector_kind(provider_id: &str) -> &'static str {
    match provider_id {
        "openalex" => "open_metadata",
        "crossref" => "doi_metadata",
        "pubmed" => "biomedical_metadata",
        "eric" => "education_metadata",
        "apa_psycnet" | "psycinfo" => "subscription_discovery_boundary",
        _ => "unknown_connector_kind",
    }
}

fn scientific_metadata_provider_path_role(provider_id: &str) -> &'static str {
    match provider_id {
        "openalex" | "crossref" => "open_metadata",
        "pubmed" => "biomedical_metadata",
        "eric" => "education_metadata",
        "apa_psycnet" | "psycinfo" => "subscription_discovery_boundary",
        _ => "unknown_provider_path_role",
    }
}

fn scientific_metadata_provider_access_notes(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" | "crossref" | "pubmed" | "eric" => vec![
            "preview-only".to_string(),
            "no network call now".to_string(),
            "no web request now".to_string(),
            "no connector call now".to_string(),
            "no metadata write now".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "preview-only".to_string(),
            "no automated authenticated scraping".to_string(),
            "no paywall bypass".to_string(),
            "manual or institutional access review required later".to_string(),
        ],
        _ => vec!["preview-only".to_string()],
    }
}

fn scientific_metadata_provider_supported_result_fields(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" | "crossref" => vec![
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "doi_or_stable_identifier".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "abstract_or_metadata_summary_later".to_string(),
            "provider_access_note_later".to_string(),
        ],
        "pubmed" | "eric" | "apa_psycnet" | "psycinfo" => vec![
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "doi_or_stable_identifier".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "abstract_or_metadata_summary_later".to_string(),
            "provider_access_note_later".to_string(),
        ],
        _ => vec!["unknown_result_field".to_string()],
    }
}

fn scientific_metadata_provider_supports_citation_metadata(provider_id: &str) -> bool {
    matches!(
        provider_id,
        "openalex" | "crossref" | "apa_psycnet" | "psycinfo"
    )
}

fn scientific_metadata_provider_ids_for_psychology_source_family(family_id: &str) -> Vec<String> {
    match family_id {
        "openalex_psychology" => vec!["openalex".to_string()],
        "crossref_psychology" => vec!["crossref".to_string()],
        "pubmed_psychology" => vec!["pubmed".to_string()],
        "eric_psychology" => vec!["eric".to_string()],
        "apa_psycnet_discovery" => vec![
            "apa_psycnet".to_string(),
            "psycinfo".to_string(),
        ],
        "local_course_literature" => Vec::new(),
        _ => Vec::new(),
    }
}

fn normalize_scientific_metadata_provider_ids(providers: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = providers
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn normalize_scientific_metadata_provider_config_goal(goal: Option<String>) -> Option<String> {
    goal.as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty())
}

fn scientific_metadata_provider_access_kind(
    provider_id: &str,
) -> ScholarChatScientificMetadataProviderAccessKind {
    match provider_id {
        "openalex" => ScholarChatScientificMetadataProviderAccessKind::OpenMetadata,
        "crossref" | "pubmed" | "eric" => {
            ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
        }
        "apa_psycnet" => ScholarChatScientificMetadataProviderAccessKind::InstitutionalDiscovery,
        "psycinfo" => ScholarChatScientificMetadataProviderAccessKind::AuthenticatedLibraryBoundary,
        _ => ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi,
    }
}

fn scientific_metadata_provider_metadata_scope(provider_id: &str) -> String {
    match provider_id {
        "openalex" => "Open metadata planning for DOI, title, author, year, and topic hints."
            .to_string(),
        "crossref" => "Public metadata API planning for DOI, title, author, and year."
            .to_string(),
        "pubmed" => "Public biomedical metadata planning for title, author, year, and topic hints."
            .to_string(),
        "eric" => "Public education metadata planning for title, author, year, and topic hints."
            .to_string(),
        "apa_psycnet" => "Institutional psychology discovery planning with manual access review later."
            .to_string(),
        "psycinfo" => "Institutional psychology discovery planning with authenticated library boundary planning later."
            .to_string(),
        _ => "Unknown scientific metadata provider scope.".to_string(),
    }
}

fn scientific_metadata_provider_planned_query_capabilities(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" => vec![
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
            "topic".to_string(),
            "doi".to_string(),
        ],
        "crossref" => vec![
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
            "doi".to_string(),
        ],
        "pubmed" => vec![
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
            "topic".to_string(),
        ],
        "eric" => vec![
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
            "topic".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
            "psychology_topic".to_string(),
        ],
        _ => vec!["unknown_query_capability".to_string()],
    }
}

fn scientific_metadata_provider_planned_filter_capabilities(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" => vec![
            "year_range".to_string(),
            "doi".to_string(),
            "open_access".to_string(),
            "topic".to_string(),
        ],
        "crossref" => vec![
            "year_range".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "author".to_string(),
        ],
        "pubmed" => vec![
            "year_range".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "author".to_string(),
            "mesh_topic".to_string(),
        ],
        "eric" => vec![
            "year_range".to_string(),
            "title".to_string(),
            "author".to_string(),
            "education_topic".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "year_range".to_string(),
            "title".to_string(),
            "author".to_string(),
            "psychology_topic".to_string(),
        ],
        _ => vec!["unknown_filter_capability".to_string()],
    }
}

fn scientific_metadata_provider_requires_attribution(provider_id: &str) -> bool {
    matches!(provider_id, "crossref" | "pubmed" | "eric")
}

fn scientific_metadata_provider_requires_api_key_later(provider_id: &str) -> bool {
    matches!(provider_id, "apa_psycnet" | "psycinfo")
}

fn scientific_metadata_provider_requires_environment_later(_provider_id: &str) -> bool {
    false
}

fn scientific_metadata_provider_requires_institutional_access_review_later(provider_id: &str) -> bool {
    matches!(provider_id, "apa_psycnet" | "psycinfo")
}

fn scientific_metadata_provider_authenticated_access_blocked_now(provider_id: &str) -> bool {
    matches!(provider_id, "apa_psycnet" | "psycinfo")
}

fn scientific_metadata_provider_paywall_bypass_blocked_now(provider_id: &str) -> bool {
    matches!(provider_id, "apa_psycnet" | "psycinfo")
}

fn scientific_metadata_provider_config_boundary_notes(provider_id: &str) -> Vec<String> {
    let mut notes = scientific_metadata_provider_access_notes(provider_id);
    notes.extend([
        "provider config preview only".to_string(),
        "dry-run only".to_string(),
        "no metadata execution now".to_string(),
        "no rate-limit enforcement now".to_string(),
        "no citation emission now".to_string(),
    ]);
    notes.sort();
    notes.dedup();
    notes
}

fn scientific_metadata_provider_config_summary(provider_id: &str, selected: bool) -> String {
    let label = scientific_metadata_provider_label(provider_id);
    if selected {
        format!(
            "{label} remains preview-only with later access, terms, rate-limit, and attribution planning."
        )
    } else {
        format!(
            "{label} remains preview-only and can be selected later for provider config planning."
        )
    }
}

fn scientific_metadata_provider_config_record(
    provider_id: &str,
    selected: bool,
) -> ScholarChatScientificMetadataProviderConfigProviderRecord {
    ScholarChatScientificMetadataProviderConfigProviderRecord {
        provider_id: provider_id.to_string(),
        provider_label: scientific_metadata_provider_label(provider_id).to_string(),
        supported: true,
        selected,
        access_kind: scientific_metadata_provider_access_kind(provider_id),
        metadata_scope: scientific_metadata_provider_metadata_scope(provider_id),
        planned_query_capabilities: scientific_metadata_provider_planned_query_capabilities(provider_id),
        planned_filter_capabilities: scientific_metadata_provider_planned_filter_capabilities(provider_id),
        planned_result_fields: scientific_metadata_provider_supported_result_fields(provider_id),
        requires_network_permission: true,
        requires_terms_review: true,
        requires_attribution: scientific_metadata_provider_requires_attribution(provider_id),
        requires_api_key_later: scientific_metadata_provider_requires_api_key_later(provider_id),
        requires_environment_later: scientific_metadata_provider_requires_environment_later(provider_id),
        requires_institutional_access_review_later:
            scientific_metadata_provider_requires_institutional_access_review_later(provider_id),
        authenticated_access_blocked_now:
            scientific_metadata_provider_authenticated_access_blocked_now(provider_id),
        paywall_bypass_blocked_now: scientific_metadata_provider_paywall_bypass_blocked_now(provider_id),
        can_execute_now: false,
        will_call_provider: false,
        will_use_http_client: false,
        will_read_api_key: false,
        will_read_environment: false,
        will_write_metadata: false,
        boundary_notes: scientific_metadata_provider_config_boundary_notes(provider_id),
        summary: scientific_metadata_provider_config_summary(provider_id, selected),
    }
}

fn scientific_metadata_provider_config_step(
    kind: ScholarChatScientificMetadataProviderConfigStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatScientificMetadataProviderConfigStep {
    let mut boundary_notes = vec![
        "preview-only".to_string(),
        "provider config preview only".to_string(),
        "dry-run only".to_string(),
        "no network call".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no file read".to_string(),
        "no retrieval execution".to_string(),
        "no Evidence Pack created".to_string(),
        "no Literature Review created".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ];
    boundary_notes.sort();
    boundary_notes.dedup();
    ScholarChatScientificMetadataProviderConfigStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        boundary_notes,
        preview_only: true,
        active,
    }
}

fn scientific_metadata_execution_boundary_step_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no network call".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no metadata persistence".to_string(),
        "no local file indexing".to_string(),
        "no file read".to_string(),
        "no PDF extraction".to_string(),
        "no OCR".to_string(),
        "no chunking".to_string(),
        "no embedding generation".to_string(),
        "no index created".to_string(),
        "no retrieval execution".to_string(),
        "no model loading".to_string(),
        "no runtime inference".to_string(),
        "no LLM call".to_string(),
        "no answer generated".to_string(),
        "no literature review created".to_string(),
        "no Evidence Pack created".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
        "no registry status change".to_string(),
        "no audit write".to_string(),
    ]
}

fn scientific_metadata_execution_boundary_step(
    kind: ScholarChatScientificMetadataExecutionBoundaryStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatScientificMetadataExecutionBoundaryStep {
    ScholarChatScientificMetadataExecutionBoundaryStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: scientific_metadata_execution_boundary_step_notes(),
    }
}

pub fn preview_scholar_chat_scientific_metadata_execution_boundary(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificMetadataExecutionBoundaryPreviewRequest,
) -> AegisResult<ScholarChatScientificMetadataExecutionBoundaryPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_topic_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let selected_local_source_ids = normalize_scientific_selected_local_source_ids(
        request.selected_local_source_ids.clone(),
    );
    let normalized_expected_source_kinds =
        normalize_scientific_expected_source_kinds(request.expected_source_kinds.clone());
    let normalized_preferred_metadata_sources =
        normalize_scientific_metadata_provider_ids(request.preferred_metadata_sources.clone());
    let normalized_preferred_psychology_source_families =
        normalize_psychology_connector_values(request.preferred_psychology_source_families.clone());
    let normalized_methodology_hints =
        normalize_psychology_connector_values(request.methodology_hints.clone());
    let normalized_population_hints =
        normalize_psychology_connector_values(request.population_hints.clone());
    let normalized_topic_area_hints =
        normalize_psychology_connector_values(request.topic_area_hints.clone());
    let normalized_evidence_goal = request
        .evidence_goal
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());
    let normalized_claim_scope = normalize_scientific_claim_scope(request.claim_scope.clone());
    let (normalized_citation_style, citation_style_warnings) =
        normalize_scientific_citation_style(request.citation_style.clone());
    let normalized_provider_override =
        normalize_scientific_metadata_provider_ids(request.provider_override.clone());
    let normalized_dry_run_label = request
        .dry_run_label
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());

    let evidence_pack_preview = preview_scholar_chat_scientific_evidence_pack_plan(
        &root,
        ScholarChatScientificEvidencePackPlanRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            course_context: request.course_context.clone(),
            context_tags: Some(normalized_context_tags.clone()),
            selected_local_source_ids: Some(selected_local_source_ids.clone()),
            expected_source_kinds: Some(normalized_expected_source_kinds.clone()),
            preferred_metadata_sources: Some(normalized_preferred_metadata_sources.clone()),
            preferred_psychology_source_families: Some(
                normalized_preferred_psychology_source_families.clone(),
            ),
            methodology_hints: Some(normalized_methodology_hints.clone()),
            population_hints: Some(normalized_population_hints.clone()),
            topic_area_hints: Some(normalized_topic_area_hints.clone()),
            evidence_goal: normalized_evidence_goal.clone(),
            claim_scope: Some(normalized_claim_scope.clone()),
            citation_style: Some(normalized_citation_style.clone()),
            max_results_per_source: request.max_results_per_source,
            require_open_access: request.require_open_access,
            require_doi: request.require_doi,
            year_from: request.year_from,
            year_to: request.year_to,
        },
    )?;

    let evidence_pack_plan_status = evidence_pack_preview.status.clone();
    let metadata_connector_plan_status = evidence_pack_preview.metadata_connector_plan_status.clone();
    let psychology_source_connector_status =
        evidence_pack_preview.psychology_source_connector_status.clone();
    let scientific_search_plan_status = evidence_pack_preview.scientific_search_plan_status.clone();
    let query_understanding_status = evidence_pack_preview.query_understanding_status.clone();
    let psychology_scope_status = evidence_pack_preview.psychology_scope_status.clone();
    let source_family_strategy = evidence_pack_preview.source_family_strategy.clone();
    let evidence_pack_strategy = evidence_pack_preview.evidence_pack_strategy.clone();
    let selected_provider_ids_from_metadata = normalized_preferred_metadata_sources.clone();
    let selected_provider_ids_from_families = normalized_preferred_psychology_source_families
        .iter()
        .flat_map(|family_id| scientific_metadata_provider_ids_for_psychology_source_family(family_id))
        .collect::<Vec<_>>();
    let mut requested_provider_ids = Vec::new();
    requested_provider_ids.extend(selected_provider_ids_from_metadata.clone());
    requested_provider_ids.extend(selected_provider_ids_from_families.clone());
    requested_provider_ids.extend(normalized_provider_override.clone());
    requested_provider_ids.sort();
    requested_provider_ids.dedup();

    let supported_provider_ids = scientific_metadata_supported_providers();
    let mut selected_provider_ids = requested_provider_ids
        .iter()
        .filter(|provider_id| supported_provider_ids.contains(provider_id))
        .cloned()
        .collect::<Vec<_>>();
    selected_provider_ids.sort_by_key(|provider_id| {
        supported_provider_ids
            .iter()
            .position(|candidate| candidate == provider_id)
            .unwrap_or(usize::MAX)
    });
    selected_provider_ids.dedup();

    let unknown_provider_ids = requested_provider_ids
        .iter()
        .filter(|provider_id| !supported_provider_ids.contains(provider_id))
        .cloned()
        .collect::<Vec<_>>();
    let selected_provider_count = selected_provider_ids.len();
    let supported_provider_count = supported_provider_ids.len();
    let selected_local_source_count = selected_local_source_ids.len();
    let provider_count = selected_provider_count;
    let provider_terms_review_required = provider_count > 0;
    let requires_institutional_access_review_later = selected_provider_ids
        .iter()
        .any(|provider| matches!(provider.as_str(), "apa_psycnet" | "psycinfo"));
    let provider_selection_plan = ScholarChatScientificMetadataExecutionBoundaryProviderSelectionPlan {
        requested_provider_ids: requested_provider_ids.clone(),
        supported_provider_ids: supported_provider_ids.clone(),
        selected_provider_ids: selected_provider_ids.clone(),
        unknown_provider_ids: unknown_provider_ids.clone(),
        needs_provider_selection: provider_count == 0
            && (selected_local_source_count > 0 || !requested_provider_ids.is_empty()),
        summary: if requested_provider_ids.is_empty() {
            "Provider selection stays preview-only and defaults later from the scientific evidence pack preview."
                .to_string()
        } else if provider_count == 0 {
            "No supported metadata provider remains after normalization; choose a supported provider later."
                .to_string()
        } else if unknown_provider_ids.is_empty() {
            "Metadata provider preferences were normalized and selected later without connector calls."
                .to_string()
        } else {
            "Supported metadata providers are selected later while unknown provider hints remain preview-only."
                .to_string()
        },
    };

    let provider_gates = supported_provider_ids
        .iter()
        .map(|provider_id| {
            let path_role = scientific_metadata_provider_path_role(provider_id);
            let access_notes = scientific_metadata_provider_access_notes(provider_id);
            ScholarChatScientificMetadataExecutionBoundaryProviderGate {
                provider_id: provider_id.clone(),
                provider_label: scientific_metadata_provider_label(provider_id).to_string(),
                connector_kind: scientific_metadata_provider_connector_kind(provider_id)
                    .to_string(),
                selected: selected_provider_ids
                    .iter()
                    .any(|selected_provider| selected_provider == provider_id),
                requires_network_permission: true,
                requires_terms_review: true,
                requires_institutional_access_review_later: matches!(
                    provider_id.as_str(),
                    "apa_psycnet" | "psycinfo"
                ),
                requires_write_permission: true,
                can_run_now: false,
                summary: if matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo") {
                    format!(
                        "{} remains preview-only with path role {path_role}; {}; later manual or institutional access review is required.",
                        scientific_metadata_provider_label(provider_id),
                        access_notes.join(", ")
                    )
                } else {
                    format!(
                        "{} remains preview-only with path role {path_role} and does not call a connector or write metadata now; {}.",
                        scientific_metadata_provider_label(provider_id),
                        access_notes.join(", ")
                    )
                },
            }
        })
        .collect::<Vec<_>>();
    let provider_gate_plan = ScholarChatScientificMetadataExecutionBoundaryProviderGatePlan {
        supported_provider_ids: supported_provider_ids.clone(),
        selected_provider_ids: selected_provider_ids.clone(),
        supported_provider_count,
        selected_provider_count,
        provider_gates,
        summary: "Every supported metadata provider remains preview-only; no connector calls, authentication, or metadata writes occur now."
            .to_string(),
    };

    let mut planned_query_inputs = vec![
        normalized_query.clone(),
        request.course_context.clone().unwrap_or_default(),
    ];
    if let Some(concept) = evidence_pack_preview.recognized_concept.as_ref() {
        planned_query_inputs.push(format!("recognized_concept={concept}"));
    }
    if let Some(topic) = evidence_pack_preview.inferred_topic.as_ref() {
        planned_query_inputs.push(format!("inferred_topic={topic}"));
    }
    if !normalized_context_tags.is_empty() {
        planned_query_inputs.push(format!("context_tags={}", normalized_context_tags.join(", ")));
    }
    if !normalized_methodology_hints.is_empty() {
        planned_query_inputs.push(format!("methodology_hints={}", normalized_methodology_hints.join(", ")));
    }
    if !normalized_population_hints.is_empty() {
        planned_query_inputs.push(format!("population_hints={}", normalized_population_hints.join(", ")));
    }
    if !normalized_topic_area_hints.is_empty() {
        planned_query_inputs.push(format!("topic_area_hints={}", normalized_topic_area_hints.join(", ")));
    }
    if !selected_local_source_ids.is_empty() {
        planned_query_inputs.push(format!("local_source_ids={}", selected_local_source_ids.join(", ")));
    }
    planned_query_inputs.sort();
    planned_query_inputs.dedup();

    let mut planned_filters = Vec::new();
    if let Some(year_from) = request.year_from {
        planned_filters.push(format!("year_from={year_from}"));
    }
    if let Some(year_to) = request.year_to {
        planned_filters.push(format!("year_to={year_to}"));
    }
    if request.require_open_access.unwrap_or(false) {
        planned_filters.push("require_open_access=true".to_string());
    }
    if request.require_doi.unwrap_or(false) {
        planned_filters.push("require_doi=true".to_string());
    }
    planned_filters.sort();
    planned_filters.dedup();

    let planned_result_fields = supported_provider_ids
        .iter()
        .flat_map(|provider_id| scientific_metadata_provider_supported_result_fields(provider_id))
        .collect::<BTreeSet<_>>()
        .into_iter()
        .collect::<Vec<_>>();

    let provider_request_plan = ScholarChatScientificMetadataExecutionBoundaryProviderRequestPlan {
        selected_provider_ids: selected_provider_ids.clone(),
        selected_provider_count,
        planned_query_inputs: planned_query_inputs.clone(),
        planned_filters: planned_filters.clone(),
        planned_result_fields: if planned_result_fields.is_empty() {
            vec![
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "doi_or_stable_identifier".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "abstract_or_metadata_summary_later".to_string(),
                "provider_access_note_later".to_string(),
            ]
        } else {
            planned_result_fields.clone()
        },
        will_call_provider: false,
        will_authenticate: false,
        will_bypass_paywall: false,
        summary: if selected_provider_count == 0 {
            "Provider request planning stays preview-only until supported providers are selected."
                .to_string()
        } else {
            "Provider request planning stays preview-only and only outlines later query shaping and result-field expectations."
                .to_string()
        },
    };

    let provider_terms_plan = ScholarChatScientificMetadataExecutionBoundaryProviderTermsPlan {
        selected_provider_ids: selected_provider_ids.clone(),
        selected_provider_count,
        terms_review_required: provider_terms_review_required,
        requires_institutional_access_review_later,
        allows_unreviewed_terms: request.allow_provider_terms_unreviewed,
        can_run_now: false,
        summary: if requires_institutional_access_review_later {
            "Provider terms remain preview-only, no automated authenticated scraping or paywall bypass is attempted, and later manual or institutional access review is required."
                .to_string()
        } else if provider_terms_review_required && !request.allow_provider_terms_unreviewed {
            "Provider terms remain preview-only and require later review before any future execution."
                .to_string()
        } else {
            "Provider terms remain preview-only and only record later review boundaries."
                .to_string()
        },
    };

    let metadata_write_plan = ScholarChatScientificMetadataExecutionBoundaryMetadataWritePlan {
        metadata_record_write_requested: request.execution_requested,
        will_write_metadata_records: false,
        persistence_target: "phase_105_preview_boundary".to_string(),
        summary: if request.execution_requested {
            "Metadata record write is requested for the future plan, but this preview never writes metadata records."
                .to_string()
        } else {
            "Metadata record write stays preview-only and is not requested in this dry run."
                .to_string()
        },
    };

    let safety_blocked_flags = vec![
        "no_network_call".to_string(),
        "no_web_request".to_string(),
        "no_http_client".to_string(),
        "no_api_key_read".to_string(),
        "no_environment_read".to_string(),
        "no_scraping".to_string(),
        "no_authenticated_library_access".to_string(),
        "no_paywall_bypass".to_string(),
        "no_connector_call".to_string(),
        "no_source_import".to_string(),
        "no_metadata_record_write".to_string(),
        "no_metadata_persistence".to_string(),
        "no_local_file_indexing".to_string(),
        "no_file_read".to_string(),
        "no_pdf_extraction".to_string(),
        "no_ocr".to_string(),
        "no_chunking_run".to_string(),
        "no_embedding_generation".to_string(),
        "no_index_created".to_string(),
        "no_retrieval_execution".to_string(),
        "no_model_loading".to_string(),
        "no_runtime_inference".to_string(),
        "no_llm_call".to_string(),
        "no_answer_generated".to_string(),
        "no_literature_review_created".to_string(),
        "no_evidence_pack_created".to_string(),
        "no_artifact_write".to_string(),
        "no_persistence".to_string(),
        "no_registry_status_change".to_string(),
        "no_audit_write".to_string(),
    ];
    let safety_boundary_plan = ScholarChatScientificMetadataExecutionBoundarySafetyPlan {
        blocked_flags: safety_blocked_flags.clone(),
        summary: "All execution boundaries remain blocked in this preview-only scientific metadata planner."
            .to_string(),
    };

    let mut warnings = evidence_pack_preview.warnings.clone();
    warnings.extend(citation_style_warnings.clone());
    warnings.sort();
    warnings.dedup();

    if !normalized_dry_run_label.is_none() {
        push_unique_text(
            &mut warnings,
            "dry_run_label is preserved only as a preview-only hint.",
        );
    }
    if !normalized_provider_override.is_empty() && !unknown_provider_ids.is_empty() {
        push_unique_text(
            &mut warnings,
            "Unknown provider override is preserved only as a preview-only hint.",
        );
    }
    if requires_institutional_access_review_later {
        push_unique_text(
            &mut warnings,
            "APA PsycNet / PsycINFO remains a planning boundary only; no automated authenticated scraping or paywall bypass is attempted, and later manual or institutional access review is required.",
        );
    }
    if provider_count > 0 {
        push_unique_text(
            &mut warnings,
            "Provider planning is preview-only and never calls a connector, loads models, or performs retrieval execution.",
        );
    }

    let status = if normalized_query.is_empty() {
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::Blocked
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsDisambiguation
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::UnknownConcept
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsPsychologyScope
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources
    } else if matches!(
        evidence_pack_plan_status,
        ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata
    } else if matches!(
        metadata_connector_plan_status,
        ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
    ) {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan
    } else if selected_provider_count == 0 {
        if selected_local_source_count == 0 && requested_provider_ids.is_empty() {
            ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources
        } else {
            ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection
        }
    } else if !request.allow_provider_terms_unreviewed {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview
    } else if !request.allow_network {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission
    } else if (request.require_doi.unwrap_or(false)
        || normalized_citation_style == "doi_first")
        && !selected_provider_ids
            .iter()
            .any(|provider_id| scientific_metadata_provider_supports_citation_metadata(provider_id))
    {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata
    } else if !request.allow_metadata_record_write {
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission
    } else if request.execution_requested {
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled
    } else {
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady
    };

    let execution_strategy = match status {
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::Blocked
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsDisambiguation
        | ScholarChatScientificMetadataExecutionBoundaryStatus::UnknownConcept
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsPsychologyScope
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ProviderPlanOnly
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::MetadataWriteGatePending
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::DryRunOnly
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
        }
    };

    let mut next_required_actions = evidence_pack_preview.next_required_actions.clone();
    if normalized_query.is_empty() {
        push_unique_text(
            &mut next_required_actions,
            "Provide a scientific query before metadata connector planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Select a supported metadata connector source before future execution can be planned.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Choose one or more supported metadata providers before future execution can be planned.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Review provider terms before any future execution can be allowed.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Allow network access before future execution can be planned.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Allow metadata record writes before future execution can be planned.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Select citation metadata capable providers or loosen citation requirements before future execution can be planned.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Keep this preview-only; the requested execution remains disabled in this phase.",
        );
    }
    if selected_provider_count > 0 {
        push_unique_text(
            &mut next_required_actions,
            "Keep provider planning preview-only until a later implementation phase.",
        );
    }

    let planned_execution_steps = vec![
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::EvidencePackAlignment,
            "evidence_pack_alignment",
            "Evidence pack alignment",
            "Align metadata connector planning with the existing Scientific Evidence Pack Preview only.",
            vec![
                format!("{:?}", evidence_pack_plan_status),
                format!("{:?}", evidence_pack_strategy),
            ],
            vec!["evidence_pack_alignment_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::MetadataConnectorAlignment,
            "metadata_connector_alignment",
            "Metadata connector alignment",
            "Align metadata connector execution planning with the existing metadata connector preview only.",
            vec![
                format!("{:?}", metadata_connector_plan_status),
                format!("{:?}", psychology_source_connector_status),
                format!("{:?}", scientific_search_plan_status),
                format!("{:?}", query_understanding_status),
            ],
            vec!["metadata_connector_alignment_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderSelection,
            "provider_selection",
            "Provider selection",
            "Select supported metadata providers later without any connector call.",
            vec![
                requested_provider_ids.join(", "),
                supported_provider_ids.join(", "),
                selected_provider_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["provider_selection_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderTermsBoundary,
            "provider_terms_boundary",
            "Provider terms boundary",
            "Review provider terms and institutional access boundaries later without automated access.",
            vec![
                format!("allow_provider_terms_unreviewed={}", request.allow_provider_terms_unreviewed),
                selected_provider_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["provider_terms_boundary_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::NetworkPermissionGate,
            "network_permission_gate",
            "Network permission gate",
            "Require network permission later before any future connector execution could be attempted.",
            vec![format!("allow_network={}", request.allow_network)],
            vec!["network_permission_gate_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderRequestPlanning,
            "provider_request_planning",
            "Provider request planning",
            "Plan later provider request inputs, filters, and result fields without calling any provider.",
            vec![
                planned_query_inputs.join(", "),
                planned_filters.join(", "),
                normalized_citation_style.clone(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec![
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "doi_or_stable_identifier".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "abstract_or_metadata_summary_later".to_string(),
                "provider_access_note_later".to_string(),
            ],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::MetadataWriteGate,
            "metadata_write_gate",
            "Metadata write gate",
            "Gate future metadata record writes without writing anything now.",
            vec![
                format!("metadata_record_write_requested={}", request.execution_requested),
                format!("allow_metadata_record_write={}", request.allow_metadata_record_write),
                "phase_105_preview_boundary".to_string(),
            ],
            vec!["metadata_write_gate_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::SafetyBoundaryCheck,
            "safety_boundary_check",
            "Safety boundary check",
            "Confirm that all requested execution boundaries remain blocked now.",
            safety_blocked_flags.clone(),
            vec!["safety_boundary_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::DownstreamEvidencePackAlignment,
            "downstream_evidence_pack_alignment",
            "Downstream Evidence Pack alignment",
            "Plan later Evidence Pack alignment only; no Evidence Pack is created now.",
            vec![
                format!("{:?}", evidence_pack_plan_status),
                normalized_citation_style.clone(),
                selected_provider_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_evidence_pack_alignment_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
        scientific_metadata_execution_boundary_step(
            ScholarChatScientificMetadataExecutionBoundaryStepKind::DownstreamLiteratureReviewAlignment,
            "downstream_literature_review_alignment",
            "Downstream literature review alignment",
            "Plan later scientific paper literature review alignment only; no literature review is created now.",
            vec![
                format!("{:?}", evidence_pack_plan_status),
                format!("{:?}", source_family_strategy),
                normalized_dry_run_label.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_literature_review_alignment_preview".to_string()],
            !matches!(status, ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked),
        ),
    ];

    let mut blocker_messages = evidence_pack_preview.blockers.clone();
    let mut warning_messages = warnings.clone();
    if selected_provider_count == 0 && requested_provider_ids.is_empty() && selected_local_source_count == 0 {
        push_unique_text(
            &mut blocker_messages,
            "evidence_sources_missing",
        );
    }
    if !normalized_query.is_empty()
        && matches!(
            evidence_pack_plan_status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        )
        && provider_count == 0
    {
        push_unique_text(
            &mut warning_messages,
            "No supported metadata provider remains after normalization.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled
    ) {
        push_unique_text(
            &mut warning_messages,
            "Execution was requested but this phase stays preview-only and disabled.",
        );
    }

    let summary = match status {
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked => {
            "Scientific metadata execution boundary preview is blocked until the query and upstream scientific planning become available."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady => {
            format!(
                "Scientific metadata execution boundary preview is ready later and only describes {:?} planning.",
                execution_strategy
            )
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled => {
            "Scientific metadata execution boundary preview is ready later, but execution remains disabled in preview-only mode."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan => {
            "Scientific metadata execution boundary preview needs a metadata connector plan before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection => {
            "Scientific metadata execution boundary preview needs supported provider selection before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview => {
            "Scientific metadata execution boundary preview needs provider terms review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission => {
            "Scientific metadata execution boundary preview needs network permission before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission => {
            "Scientific metadata execution boundary preview needs metadata write permission before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata => {
            "Scientific metadata execution boundary preview needs citation metadata before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources => {
            "Scientific metadata execution boundary preview needs local sources or supported providers before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsDisambiguation => {
            "Scientific metadata execution boundary preview needs query disambiguation before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::UnknownConcept => {
            "Scientific metadata execution boundary preview found an unknown concept and can only outline later planning steps."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsPsychologyScope => {
            "Scientific metadata execution boundary preview needs psychology scope before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily => {
            "Scientific metadata execution boundary preview needs a supported psychology source family before later planning can continue."
                .to_string()
        }
    };

    Ok(ScholarChatScientificMetadataExecutionBoundaryPreview {
        status,
        normalized_query,
        normalized_mode,
        normalized_context_tags,
        selected_local_source_ids,
        selected_local_source_count,
        normalized_expected_source_kinds,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families,
        normalized_methodology_hints,
        normalized_population_hints,
        normalized_topic_area_hints,
        normalized_evidence_goal,
        normalized_claim_scope,
        normalized_citation_style,
        execution_requested: request.execution_requested,
        allow_network: request.allow_network,
        allow_provider_terms_unreviewed: request.allow_provider_terms_unreviewed,
        allow_metadata_record_write: request.allow_metadata_record_write,
        normalized_provider_override: if normalized_provider_override.is_empty() {
            None
        } else {
            Some(normalized_provider_override)
        },
        normalized_dry_run_label,
        evidence_pack_plan_status,
        metadata_connector_plan_status,
        psychology_source_connector_status,
        scientific_search_plan_status,
        query_understanding_status,
        psychology_scope_status,
        source_family_strategy,
        evidence_pack_strategy,
        provider_selection_plan,
        provider_gate_plan,
        provider_request_plan,
        provider_terms_plan,
        metadata_write_plan,
        safety_boundary_plan,
        planned_execution_steps,
        blockers: blocker_messages,
        warnings: warning_messages,
        next_required_actions,
        summary,
        preview_only: true,
        metadata_execution_boundary_only: true,
        dry_run_only: true,
        execution_disabled: true,
        no_network_call: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_metadata_persistence: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_literature_review_created: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_scientific_metadata_provider_config(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificMetadataProviderConfigPreviewRequest,
) -> AegisResult<ScholarChatScientificMetadataProviderConfigPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_topic_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let normalized_preferred_metadata_sources =
        normalize_scientific_metadata_provider_ids(request.preferred_metadata_sources.clone());
    let normalized_preferred_psychology_source_families =
        normalize_psychology_connector_values(request.preferred_psychology_source_families.clone());
    let normalized_provider_override =
        normalize_scientific_metadata_provider_ids(request.provider_override.clone());
    let normalized_config_goal =
        normalize_scientific_metadata_provider_config_goal(request.config_goal.clone());

    let metadata_execution_boundary_preview =
        preview_scholar_chat_scientific_metadata_execution_boundary(
            &root,
            ScholarChatScientificMetadataExecutionBoundaryPreviewRequest {
                query: normalized_query.clone(),
                mode: Some(normalized_mode.clone()),
                course_context: normalized_config_goal.clone(),
                context_tags: Some(normalized_context_tags.clone()),
                selected_local_source_ids: Some(Vec::new()),
                expected_source_kinds: None,
                preferred_metadata_sources: Some(normalized_preferred_metadata_sources.clone()),
                preferred_psychology_source_families: Some(
                    normalized_preferred_psychology_source_families.clone(),
                ),
                methodology_hints: None,
                population_hints: None,
                topic_area_hints: None,
                evidence_goal: normalized_config_goal.clone(),
                claim_scope: None,
                citation_style: None,
                max_results_per_source: None,
                require_open_access: request.require_open_access,
                require_doi: request.require_doi,
                year_from: request.year_from,
                year_to: request.year_to,
                execution_requested: request.execution_requested,
                allow_network: request.allow_network,
                allow_provider_terms_unreviewed: request.allow_provider_terms_unreviewed,
                allow_metadata_record_write: request.allow_metadata_record_write,
                provider_override: if normalized_provider_override.is_empty() {
                    None
                } else {
                    Some(normalized_provider_override.clone())
                },
                dry_run_label: normalized_config_goal.clone(),
            },
        )?;

    let metadata_execution_boundary_status =
        metadata_execution_boundary_preview.status.clone();
    let metadata_execution_boundary_strategy = match metadata_execution_boundary_status.clone() {
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::Blocked
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsDisambiguation
        | ScholarChatScientificMetadataExecutionBoundaryStatus::UnknownConcept
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsPsychologyScope
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ProviderPlanOnly
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata
        | ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::MetadataWriteGatePending
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::DryRunOnly
        }
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled => {
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
        }
    };
    let selected_provider_ids =
        metadata_execution_boundary_preview
            .provider_selection_plan
            .selected_provider_ids
            .clone();
    let selected_provider_count = selected_provider_ids.len();
    let selected_provider_set = selected_provider_ids
        .iter()
        .cloned()
        .collect::<BTreeSet<_>>();
    let unknown_provider_ids =
        metadata_execution_boundary_preview
            .provider_selection_plan
            .unknown_provider_ids
            .clone();

    let supported_provider_ids = scientific_metadata_supported_providers();
    let provider_configs = supported_provider_ids
        .iter()
        .map(|provider_id| {
            scientific_metadata_provider_config_record(
                provider_id,
                selected_provider_set.contains(provider_id),
            )
        })
        .collect::<Vec<_>>();

    let providers_requiring_terms_review = selected_provider_ids.clone();
    let providers_requiring_institutional_access_review_later = selected_provider_ids
        .iter()
        .filter(|provider_id| {
            scientific_metadata_provider_requires_institutional_access_review_later(provider_id)
        })
        .cloned()
        .collect::<Vec<_>>();
    let providers_requiring_rate_limit_review_later = selected_provider_ids
        .iter()
        .filter(|provider_id| {
            matches!(provider_id.as_str(), "openalex" | "crossref" | "pubmed" | "eric")
        })
        .cloned()
        .collect::<Vec<_>>();
    let providers_requiring_attribution = selected_provider_ids
        .iter()
        .filter(|provider_id| scientific_metadata_provider_requires_attribution(provider_id))
        .cloned()
        .collect::<Vec<_>>();

    let provider_terms_policy = ScholarChatScientificMetadataProviderTermsPolicy {
        providers_requiring_terms_review: providers_requiring_terms_review.clone(),
        providers_requiring_institutional_access_review_later:
            providers_requiring_institutional_access_review_later.clone(),
        terms_review_required: !selected_provider_ids.is_empty(),
        unreviewed_terms_block_execution:
            !request.allow_provider_terms_unreviewed && !selected_provider_ids.is_empty(),
        authenticated_access_blocked: true,
        paywall_bypass_blocked: true,
        summary: if providers_requiring_institutional_access_review_later.is_empty() {
            "Provider terms remain preview-only and no authenticated access or paywall bypass is attempted now."
                .to_string()
        } else {
            "Provider terms remain preview-only; selected institutional providers still require later manual or institutional access review and no authenticated access or paywall bypass is attempted now."
                .to_string()
        },
    };

    let provider_access_policy = ScholarChatScientificMetadataProviderAccessPolicy {
        open_metadata_providers: supported_provider_ids
            .iter()
            .filter(|provider_id| provider_id.as_str() == "openalex")
            .cloned()
            .collect::<Vec<_>>(),
        public_metadata_api_providers: supported_provider_ids
            .iter()
            .filter(|provider_id| matches!(provider_id.as_str(), "crossref" | "pubmed" | "eric"))
            .cloned()
            .collect::<Vec<_>>(),
        institutional_boundary_providers: supported_provider_ids
            .iter()
            .filter(|provider_id| matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo"))
            .cloned()
            .collect::<Vec<_>>(),
        authenticated_access_blocked_now: true,
        manual_access_review_required_later: !providers_requiring_institutional_access_review_later
            .is_empty(),
        summary: "Open metadata, public metadata API, and institutional discovery boundaries are described only; later manual access review remains necessary for institutional providers."
            .to_string(),
    };

    let provider_rate_limit_policy = ScholarChatScientificMetadataProviderRateLimitPolicy {
        providers_requiring_rate_limit_review_later: providers_requiring_rate_limit_review_later
            .clone(),
        provider_request_budget_planned_later: !selected_provider_ids.is_empty()
            || request.include_rate_limit_notes.unwrap_or(false),
        will_enforce_rate_limit_now: false,
        will_call_provider_now: false,
        summary: if request.include_rate_limit_notes.unwrap_or(false) {
            "Rate-limit policy remains preview-only; no provider request is attempted and no limit is enforced now."
                .to_string()
        } else {
            "Rate-limit policy remains preview-only and no provider request is attempted now."
                .to_string()
        },
    };

    let provider_attribution_policy = ScholarChatScientificMetadataProviderAttributionPolicy {
        providers_requiring_attribution: providers_requiring_attribution.clone(),
        required_attribution_fields: if providers_requiring_attribution.is_empty() {
            vec![
                "provider_name".to_string(),
                "provider_record_id".to_string(),
            ]
        } else {
            vec![
                "provider_name".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "provider_terms_note".to_string(),
            ]
        },
        will_emit_citations_now: false,
        will_write_attribution_now: false,
        summary: if providers_requiring_attribution.is_empty() {
            "Attribution planning remains preview-only and no citations are emitted now."
                .to_string()
        } else {
            "Attribution planning remains preview-only; citations are not emitted now and later attribution review is still required."
                .to_string()
        },
    };

    let provider_safety_boundary = ScholarChatScientificMetadataProviderSafetyBoundary {
        external_services_blocked_now: true,
        connector_calls_blocked_now: true,
        network_blocked_now: true,
        http_client_blocked_now: true,
        api_key_reads_blocked_now: true,
        environment_reads_blocked_now: true,
        scraping_blocked_now: true,
        authenticated_library_access_blocked_now: true,
        paywall_bypass_blocked_now: true,
        metadata_writes_blocked_now: true,
        source_import_blocked_now: true,
        summary: "Provider safety boundaries remain blocked now; no external service, connector, network, authenticated access, or metadata write is performed."
            .to_string(),
    };

    let status = if normalized_query.is_empty() {
        ScholarChatScientificMetadataProviderConfigStatus::Blocked
    } else if matches!(
        metadata_execution_boundary_status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked
    ) {
        ScholarChatScientificMetadataProviderConfigStatus::Blocked
    } else if !matches!(
        metadata_execution_boundary_status,
        ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady
            | ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled
    ) {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
    } else if selected_provider_ids.is_empty() {
        if !unknown_provider_ids.is_empty() {
            ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection
        } else {
            ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection
        }
    } else if request.execution_requested && !request.allow_network {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview
    } else if !providers_requiring_institutional_access_review_later.is_empty() {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview
    } else if provider_terms_policy.unreviewed_terms_block_execution {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview
    } else if !request.include_attribution_requirements.unwrap_or(false)
        && !providers_requiring_attribution.is_empty()
    {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview
    } else {
        ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady
    };

    let provider_config_strategy = match status {
        ScholarChatScientificMetadataProviderConfigStatus::Blocked => {
            ScholarChatScientificMetadataProviderConfigStrategy::Blocked
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview => {
            ScholarChatScientificMetadataProviderConfigStrategy::InstitutionalAccessReviewFirst
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview => {
            ScholarChatScientificMetadataProviderConfigStrategy::AttributionReviewFirst
        }
        ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady => {
            ScholarChatScientificMetadataProviderConfigStrategy::ConfigPreviewOnly
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection
        | ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
        | ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview
        | ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview
        | ScholarChatScientificMetadataProviderConfigStatus::UnsupportedProvider => {
            ScholarChatScientificMetadataProviderConfigStrategy::ProviderPolicyReviewFirst
        }
    };

    let mut warnings = metadata_execution_boundary_preview.warnings.clone();
    if !unknown_provider_ids.is_empty() {
        push_unique_text(
            &mut warnings,
            "Unknown provider override is preserved only as a preview-only hint.",
        );
    }
    if request.include_disabled_providers.unwrap_or(false) {
        push_unique_text(
            &mut warnings,
            "Disabled providers remain preview-only and may be surfaced later only as configuration hints.",
        );
    }
    if request.include_institutional_providers.unwrap_or(false) {
        push_unique_text(
            &mut warnings,
            "Institutional providers remain preview-only and are never accessed automatically.",
        );
    }
    if request.include_rate_limit_notes.unwrap_or(false) {
        push_unique_text(
            &mut warnings,
            "Rate-limit notes remain preview-only and are not enforced now.",
        );
    }
    if request.include_attribution_requirements.unwrap_or(false) {
        push_unique_text(
            &mut warnings,
            "Attribution requirements remain preview-only and no citations are emitted now.",
        );
    }
    if request.execution_requested && !request.allow_network {
        push_unique_text(
            &mut warnings,
            "Execution was requested but network access is not allowed in this preview-only phase.",
        );
    }

    let mut blockers = metadata_execution_boundary_preview.blockers.clone();
    if normalized_query.is_empty() {
        push_unique_text(&mut blockers, "query_missing");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection
    ) {
        push_unique_text(&mut blockers, "provider_selection_missing");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
    ) {
        push_unique_text(&mut blockers, "metadata_execution_boundary_pending");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview
    ) {
        push_unique_text(&mut blockers, "network_policy_review_required");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview
    ) {
        push_unique_text(&mut blockers, "provider_terms_review_required");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview
    ) {
        push_unique_text(&mut blockers, "institutional_access_review_required");
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview
    ) {
        push_unique_text(&mut blockers, "attribution_policy_review_required");
    }

    let mut next_required_actions = metadata_execution_boundary_preview
        .next_required_actions
        .clone();
    if normalized_query.is_empty() {
        push_unique_text(
            &mut next_required_actions,
            "Provide a scientific query before provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Select one or more supported providers before future provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Keep the upstream scientific metadata execution boundary ready before later provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Allow network access before later provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Review provider terms before later provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Review institutional access for APA PsycNet or PsycINFO before later provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Review attribution requirements before later provider config planning can continue.",
        );
    }
    if matches!(
        status,
        ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady
    ) {
        push_unique_text(
            &mut next_required_actions,
            "Keep provider config planning preview-only; no provider call is performed now.",
        );
    }

    let planned_config_steps = {
        let active = !matches!(
            status,
            ScholarChatScientificMetadataProviderConfigStatus::Blocked
        );
        vec![
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::MetadataExecutionBoundaryAlignment,
                "metadata_execution_boundary_alignment",
                "Metadata execution boundary alignment",
                "Align provider config planning with the existing scientific metadata execution boundary preview only.",
                vec![
                    format!("{:?}", metadata_execution_boundary_status),
                    format!("{:?}", metadata_execution_boundary_strategy),
                    normalized_config_goal.clone().unwrap_or_default(),
                ],
                vec!["metadata_execution_boundary_alignment_preview".to_string()],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderCapabilityCatalog,
                "provider_capability_catalog",
                "Provider capability catalog",
                "Catalog later provider capabilities without any provider call or execution.",
                vec![
                    selected_provider_ids.join(", "),
                    supported_provider_ids.join(", "),
                ],
                vec![
                    "provider_id".to_string(),
                    "provider_label".to_string(),
                    "access_kind".to_string(),
                    "metadata_scope".to_string(),
                ],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderTermsPolicy,
                "provider_terms_policy",
                "Provider terms policy",
                "Review provider terms later and keep authenticated access blocked now.",
                vec![
                    providers_requiring_terms_review.join(", "),
                    providers_requiring_institutional_access_review_later.join(", "),
                    format!(
                        "allow_provider_terms_unreviewed={}",
                        request.allow_provider_terms_unreviewed
                    ),
                ],
                vec![
                    "terms_review_required".to_string(),
                    "authenticated_access_blocked".to_string(),
                    "paywall_bypass_blocked".to_string(),
                ],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAccessPolicy,
                "provider_access_policy",
                "Provider access policy",
                "Plan later access boundaries for open metadata, public metadata APIs, and institutional discovery.",
                vec![
                    provider_access_policy.open_metadata_providers.join(", "),
                    provider_access_policy.public_metadata_api_providers.join(", "),
                    provider_access_policy
                        .institutional_boundary_providers
                        .join(", "),
                ],
                vec![
                    "open_metadata".to_string(),
                    "public_metadata_api".to_string(),
                    "institutional_boundary".to_string(),
                ],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderRateLimitPolicy,
                "provider_rate_limit_policy",
                "Provider rate-limit policy",
                "Plan later rate-limit review without enforcement now.",
                vec![
                    providers_requiring_rate_limit_review_later.join(", "),
                    format!(
                        "include_rate_limit_notes={}",
                        request.include_rate_limit_notes.unwrap_or(false)
                    ),
                ],
                vec![
                    "provider_request_budget".to_string(),
                    "rate_limit_review".to_string(),
                ],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAttributionPolicy,
                "provider_attribution_policy",
                "Provider attribution policy",
                "Plan later attribution requirements while citations stay disabled now.",
                vec![
                    providers_requiring_attribution.join(", "),
                    format!(
                        "include_attribution_requirements={}",
                        request.include_attribution_requirements.unwrap_or(false)
                    ),
                ],
                provider_attribution_policy.required_attribution_fields.clone(),
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderSafetyBoundary,
                "provider_safety_boundary",
                "Provider safety boundary",
                "Confirm that all provider safety boundaries remain blocked now.",
                provider_safety_boundary
                    .summary
                    .split_whitespace()
                    .map(|value| value.to_string())
                    .collect(),
                vec![
                    "external_services_blocked_now".to_string(),
                    "connector_calls_blocked_now".to_string(),
                    "network_blocked_now".to_string(),
                    "http_client_blocked_now".to_string(),
                    "api_key_reads_blocked_now".to_string(),
                    "environment_reads_blocked_now".to_string(),
                    "scraping_blocked_now".to_string(),
                    "authenticated_library_access_blocked_now".to_string(),
                    "paywall_bypass_blocked_now".to_string(),
                    "metadata_writes_blocked_now".to_string(),
                    "source_import_blocked_now".to_string(),
                ],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamMetadataExecutionAlignment,
                "downstream_metadata_execution_alignment",
                "Downstream metadata execution alignment",
                "Align later metadata execution with provider config planning only.",
                vec![
                    format!("{:?}", metadata_execution_boundary_status),
                    format!("{:?}", provider_config_strategy),
                    normalized_config_goal.clone().unwrap_or_default(),
                ],
                vec!["downstream_metadata_execution_alignment_preview".to_string()],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamEvidencePackAlignment,
                "downstream_evidence_pack_alignment",
                "Downstream Evidence Pack alignment",
                "Plan later Evidence Pack alignment only; no Evidence Pack is created now.",
                vec![
                    provider_configs
                        .iter()
                        .map(|config| config.provider_id.clone())
                        .collect::<Vec<_>>()
                        .join(", "),
                    format!("{:?}", metadata_execution_boundary_strategy),
                ],
                vec!["downstream_evidence_pack_alignment_preview".to_string()],
                active,
            ),
            scientific_metadata_provider_config_step(
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamLiteratureReviewAlignment,
                "downstream_literature_review_alignment",
                "Downstream literature review alignment",
                "Plan later Literature Review alignment only; no literature review is created now.",
                vec![
                    normalized_config_goal.clone().unwrap_or_default(),
                    providers_requiring_institutional_access_review_later.join(", "),
                ],
                vec!["downstream_literature_review_alignment_preview".to_string()],
                active,
            ),
        ]
    };

    let summary = match status {
        ScholarChatScientificMetadataProviderConfigStatus::Blocked => {
            "Scientific metadata provider config preview is blocked until the query and upstream metadata execution boundary become available."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection => {
            "Scientific metadata provider config preview needs supported provider selection before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary => {
            "Scientific metadata provider config preview needs the upstream metadata execution boundary before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview => {
            "Scientific metadata provider config preview needs network policy review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview => {
            "Scientific metadata provider config preview needs provider terms review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview => {
            "Scientific metadata provider config preview needs institutional access review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview => {
            "Scientific metadata provider config preview needs attribution policy review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::UnsupportedProvider => {
            "Scientific metadata provider config preview contains unsupported provider hints and needs a supported provider later."
                .to_string()
        }
        ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady => {
            format!(
                "Scientific metadata provider config preview is ready later and only describes {:?} planning.",
                provider_config_strategy
            )
        }
    };

    Ok(ScholarChatScientificMetadataProviderConfigPreview {
        status,
        normalized_query,
        normalized_mode: Some(normalized_mode),
        normalized_context_tags,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families,
        normalized_provider_override: if normalized_provider_override.is_empty() {
            None
        } else {
            Some(normalized_provider_override)
        },
        unknown_provider_ids,
        normalized_config_goal,
        include_disabled_providers: request.include_disabled_providers.unwrap_or(false),
        include_institutional_providers: request.include_institutional_providers.unwrap_or(false),
        include_rate_limit_notes: request.include_rate_limit_notes.unwrap_or(false),
        include_attribution_requirements: request.include_attribution_requirements.unwrap_or(false),
        execution_requested: request.execution_requested,
        allow_network: request.allow_network,
        allow_provider_terms_unreviewed: request.allow_provider_terms_unreviewed,
        allow_metadata_record_write: request.allow_metadata_record_write,
        metadata_execution_boundary_status,
        metadata_execution_boundary_strategy,
        selected_provider_ids,
        selected_provider_count,
        provider_config_strategy,
        provider_configs,
        provider_terms_policy,
        provider_access_policy,
        provider_rate_limit_policy,
        provider_attribution_policy,
        provider_safety_boundary,
        planned_config_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        metadata_provider_config_preview_only: true,
        dry_run_only: true,
        execution_disabled: true,
        no_network_call: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_metadata_persistence: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_literature_review_created: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_draft_inference(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftInferenceRequest,
) -> AegisResult<ScholarChatDraftInferencePreview> {
    let root = root.into();
    let scholar_chat_request = request.scholar_chat_request;
    let request_preview = preview_scholar_chat_request(&root, scholar_chat_request.clone())?;
    let mut blockers = Vec::new();
    let mut warnings = Vec::new();

    for warning in &request_preview.warnings {
        push_draft_warning(&mut warnings, "request_preview", warning);
    }

    if matches!(request_preview.grounding_policy, GroundingPolicy::ExternalAdaptersLater) {
        push_draft_warning(
            &mut warnings,
            "external_adapters_unavailable",
            "External adapters are not implemented yet and are unused in this preview.",
        );
    }

    if !request.allow_model_execution {
        push_draft_blocker(
            &mut blockers,
            "needs_execution_consent",
            "Future model execution is not allowed yet.",
        );
        push_draft_warning(
            &mut warnings,
            "execution_consent_required",
            "Draft inference preview will not consult the local runtime until execution consent is granted.",
        );
        return Ok(build_draft_inference_preview(
            request_preview.normalized_prompt,
            request_preview.mode,
            request_preview.grounding_policy,
            request_preview.selected_source_count,
            0,
            0,
            0,
            String::new(),
            LocalModelRuntimeHealthStatus::NotConfigured,
            LocalRuntimeInvocationPlanStatus::PreviewOnly,
            ScholarChatDraftInferenceStatus::NeedsExecutionConsent,
            ScholarChatDraftOutputClassification::Blocked,
            request.allow_model_execution,
            false,
            None,
            None,
            String::new(),
            String::new(),
            0,
            None,
            blockers,
            warnings,
        ));
    }

    let prompt_ready = request_preview.selected_source_count > 0
        || matches!(request_preview.grounding_policy, GroundingPolicy::AllowMarkedModelKnowledge);
    if !prompt_ready {
        push_draft_blocker(
            &mut blockers,
            "needs_sources",
            "Selected sources are required before a local draft can be prepared.",
        );
        return Ok(build_draft_inference_preview(
            request_preview.normalized_prompt,
            request_preview.mode,
            request_preview.grounding_policy,
            request_preview.selected_source_count,
            0,
            0,
            0,
            String::new(),
            LocalModelRuntimeHealthStatus::NotConfigured,
            LocalRuntimeInvocationPlanStatus::PreviewOnly,
            ScholarChatDraftInferenceStatus::NeedsSources,
            ScholarChatDraftOutputClassification::Blocked,
            request.allow_model_execution,
            false,
            None,
            None,
            String::new(),
            String::new(),
            0,
            None,
            blockers,
            warnings,
        ));
    }

    let runtime_health_preview = preview_local_model_runtime_health(&root, request.runtime_config.clone())?;
    let runtime_health_status = runtime_health_preview.status.clone();
    for warning in &runtime_health_preview.warnings {
        push_draft_warning(&mut warnings, &warning.kind, &warning.message);
    }
    if !matches!(runtime_health_status, LocalModelRuntimeHealthStatus::ReadyToTestLater) {
        push_draft_blocker(
            &mut blockers,
            "needs_runtime_config",
            "The local runtime configuration is not ready yet.",
        );
        return Ok(build_draft_inference_preview(
            request_preview.normalized_prompt,
            request_preview.mode,
            request_preview.grounding_policy,
            request_preview.selected_source_count,
            0,
            0,
            0,
            String::new(),
            runtime_health_status,
            LocalRuntimeInvocationPlanStatus::NotConfigured,
            ScholarChatDraftInferenceStatus::NeedsRuntimeConfig,
            ScholarChatDraftOutputClassification::Blocked,
            request.allow_model_execution,
            false,
            runtime_health_preview.model_file_name,
            None,
            String::new(),
            String::new(),
            0,
            None,
            blockers,
            warnings,
        ));
    }

    let evidence_plan_preview = if matches!(request_preview.grounding_policy, GroundingPolicy::LocalOnly) {
        Some(preview_scholar_chat_evidence_plan(&root, scholar_chat_request.clone())?)
    } else {
        None
    };
    let retrieval_candidate_count = evidence_plan_preview
        .as_ref()
        .map_or(0, |preview| preview.retrieval_candidate_count);
    let evidence_candidate_count = evidence_plan_preview
        .as_ref()
        .map_or(0, |preview| preview.evidence_candidate_count);

    if let Some(evidence_plan_preview) = &evidence_plan_preview {
        for warning in &evidence_plan_preview.warnings {
            push_draft_warning(&mut warnings, "evidence_plan_preview", warning);
        }
    }

    if matches!(request_preview.grounding_policy, GroundingPolicy::LocalOnly) && evidence_candidate_count == 0 {
        push_draft_blocker(
            &mut blockers,
            "needs_evidence",
            "local_only requires local evidence candidates before draft inference can proceed.",
        );
        push_draft_warning(
            &mut warnings,
            "evidence_required",
            "No local evidence candidates are available for a local_only draft preview.",
        );
        return Ok(build_draft_inference_preview(
            request_preview.normalized_prompt,
            request_preview.mode,
            request_preview.grounding_policy,
            request_preview.selected_source_count,
            retrieval_candidate_count,
            evidence_candidate_count,
            0,
            String::new(),
            runtime_health_status,
            LocalRuntimeInvocationPlanStatus::PreviewOnly,
            ScholarChatDraftInferenceStatus::NeedsEvidence,
            ScholarChatDraftOutputClassification::Blocked,
            request.allow_model_execution,
            false,
            runtime_health_preview.model_file_name,
            None,
            String::new(),
            String::new(),
            0,
            None,
            blockers,
            warnings,
        ));
    }

    let prompt_pack_preview = preview_scholar_chat_prompt_pack(&root, scholar_chat_request.clone())?;
    let prompt_pack_text = render_prompt_pack_for_runtime(&prompt_pack_preview.prompt_pack);
    let invocation_plan_preview = preview_local_runtime_invocation_plan(
        &root,
        LocalRuntimeInvocationPlanRequest {
            runtime_config: request.runtime_config.clone(),
            prompt_text: Some(prompt_pack_text.clone()),
            estimated_input_char_count: Some(prompt_pack_text.chars().count() as u32),
            max_output_tokens: request.max_output_tokens,
            stop_sequences: None,
        },
    )?;
    let invocation_plan_status = invocation_plan_preview.status.clone();
    for warning in &prompt_pack_preview.warnings {
        push_draft_warning(&mut warnings, "prompt_pack_preview", warning);
    }
    for warning in &invocation_plan_preview.plan.warnings {
        push_draft_warning(&mut warnings, &warning.kind, &warning.message);
    }
    for blocker in &invocation_plan_preview.plan.blockers {
        push_draft_blocker(&mut blockers, &blocker.kind, &blocker.message);
    }

    let output_classification = draft_output_classification(&request_preview.grounding_policy, false);

    let smoke_result = smoke_test_local_runtime_inference(
        &root,
        LocalRuntimeSmokeInferenceRequest {
            runtime_config: request.runtime_config,
            allow_execution: true,
            prompt: Some(prompt_pack_text.clone()),
            timeout_ms: request.timeout_ms,
            max_output_tokens: request.max_output_tokens,
        },
    )?;
    for warning in smoke_result.warnings.iter() {
        push_draft_warning(&mut warnings, &warning.kind, &warning.message);
    }
    for blocker in smoke_result.blockers.iter() {
        push_draft_blocker(&mut blockers, &blocker.kind, &blocker.message);
    }

    let status = match smoke_result.status {
        LocalRuntimeSmokeInferenceStatus::InferenceSucceeded => ScholarChatDraftInferenceStatus::InferenceSucceeded,
        LocalRuntimeSmokeInferenceStatus::InferenceFailed => ScholarChatDraftInferenceStatus::InferenceFailed,
        LocalRuntimeSmokeInferenceStatus::TimedOut => ScholarChatDraftInferenceStatus::TimedOut,
        LocalRuntimeSmokeInferenceStatus::Blocked => ScholarChatDraftInferenceStatus::Blocked,
        LocalRuntimeSmokeInferenceStatus::NotConfigured
        | LocalRuntimeSmokeInferenceStatus::ModelMissing
        | LocalRuntimeSmokeInferenceStatus::ExecutableMissing => ScholarChatDraftInferenceStatus::NeedsRuntimeConfig,
    };

    Ok(build_draft_inference_preview(
        request_preview.normalized_prompt,
        request_preview.mode,
        request_preview.grounding_policy,
        request_preview.selected_source_count,
        retrieval_candidate_count,
        evidence_candidate_count,
        prompt_pack_preview.prompt_pack.section_count,
        prompt_pack_text,
        runtime_health_status,
        invocation_plan_status,
        status,
        output_classification,
        request.allow_model_execution,
        smoke_result.execution_attempted,
        smoke_result.safe_model_file_name,
        smoke_result.safe_executable_file_name,
        smoke_result.stdout_preview,
        smoke_result.stderr_preview,
        smoke_result.duration_ms,
        smoke_result.exit_code,
        blockers,
        warnings,
    ))
}

pub fn preview_scholar_chat_draft_grounding_inspection(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftGroundingInspectionRequest,
) -> AegisResult<ScholarChatDraftGroundingInspectionPreview> {
    let root = root.into();
    let scholar_chat_request = request.scholar_chat_request;
    let request_preview = preview_scholar_chat_request(&root, scholar_chat_request.clone())?;
    let mut blockers = Vec::new();
    let mut warnings = Vec::new();
    let normalized_draft_text = normalize_optional_draft_text(request.draft_text);

    for warning in &request_preview.warnings {
        push_grounding_inspection_warning(&mut warnings, "request_preview", warning);
    }
    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a draft grounding inspection preview only; no grounded answer, final answer, Evidence Pack, or persistence was created.",
    );

    let Some(normalized_draft_text) = normalized_draft_text else {
        push_grounding_inspection_blocker(
            &mut blockers,
            "draft_text_missing",
            "No draft text was provided to inspect.",
        );
        return Ok(build_draft_grounding_inspection_preview(
            request_preview.normalized_prompt,
            0,
            request_preview.selected_source_count,
            0,
            Vec::new(),
            0,
            0,
            0,
            blockers,
            warnings,
        ));
    };

    if request_preview.selected_source_count == 0 {
        push_grounding_inspection_blocker(
            &mut blockers,
            "needs_sources",
            "No Scholar Chat source context was selected for this inspection.",
        );
        push_grounding_inspection_warning(
            &mut warnings,
            "unscoped_inspection",
            "No Scholar Chat source context selected; inspection will be unscoped.",
        );
        return Ok(build_draft_grounding_inspection_preview(
            request_preview.normalized_prompt,
            normalized_draft_text.chars().count(),
            request_preview.selected_source_count,
            0,
            Vec::new(),
            0,
            0,
            0,
            blockers,
            warnings,
        ));
    }

    let evidence_plan_preview = preview_scholar_chat_evidence_plan(&root, scholar_chat_request.clone())?;
    for warning in &evidence_plan_preview.warnings {
        push_grounding_inspection_warning(&mut warnings, "evidence_plan_preview", warning);
    }

    let evidence_candidate_count = evidence_plan_preview.evidence_candidate_count;
    if evidence_candidate_count == 0 {
        push_grounding_inspection_blocker(
            &mut blockers,
            "needs_evidence_candidates",
            "No local evidence candidates were available for the selected sources.",
        );
        push_grounding_inspection_warning(
            &mut warnings,
            "evidence_required",
            "No local evidence candidates are available yet for draft grounding inspection.",
        );
        return Ok(build_draft_grounding_inspection_preview(
            request_preview.normalized_prompt,
            normalized_draft_text.chars().count(),
            request_preview.selected_source_count,
            evidence_candidate_count,
            Vec::new(),
            0,
            0,
            0,
            blockers,
            warnings,
        ));
    }

    let max_items = request
        .max_items
        .unwrap_or(SCHOLAR_CHAT_DRAFT_GROUNDING_INSPECTION_LIMIT)
        .clamp(1, SCHOLAR_CHAT_DRAFT_GROUNDING_INSPECTION_LIMIT);
    let inspected_items = inspect_draft_grounding_items(&normalized_draft_text, &evidence_plan_preview.candidates, max_items);
    if inspected_items.items.is_empty() {
        push_grounding_inspection_blocker(
            &mut blockers,
            "draft_text_missing",
            "Draft text did not contain inspectable content.",
        );
        push_grounding_inspection_warning(
            &mut warnings,
            "draft_text_empty",
            "No inspectable draft sentences were found in the provided draft text.",
        );
        return Ok(build_draft_grounding_inspection_preview(
            request_preview.normalized_prompt,
            normalized_draft_text.chars().count(),
            request_preview.selected_source_count,
            evidence_candidate_count,
            Vec::new(),
            0,
            0,
            0,
            blockers,
            warnings,
        ));
    }
    if inspected_items.was_clamped {
        push_grounding_inspection_warning(
            &mut warnings,
            "inspection_clamped",
            &format!("Only the first {max_items} draft items were inspected."),
        );
    }
    warnings.extend(inspected_items.warnings);

    Ok(build_draft_grounding_inspection_preview(
        request_preview.normalized_prompt,
        normalized_draft_text.chars().count(),
        request_preview.selected_source_count,
        evidence_candidate_count,
        inspected_items.items,
        inspected_items.supported_item_count,
        inspected_items.weakly_supported_item_count,
        inspected_items.unsupported_item_count,
        blockers,
        warnings,
    ))
}

pub fn preview_scholar_chat_grounded_draft_readiness(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftGroundingInspectionRequest,
) -> AegisResult<ScholarChatGroundedDraftReadinessPreview> {
    let root = root.into();
    let inspection_preview = preview_scholar_chat_draft_grounding_inspection(&root, request)?;
    let status = grounded_draft_readiness_status(&inspection_preview);
    let mut blockers = inspection_preview.blockers.clone();
    let mut warnings = inspection_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-draft readiness preview only; it is not a grounded answer, final answer, Evidence Pack, or persisted artifact.",
    );

    match status {
        ScholarChatGroundedDraftReadinessStatus::Blocked => {
            if blockers.is_empty() {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "readiness_blocked",
                    "Grounded-draft readiness is blocked until draft text, source context, and local evidence are available.",
                );
            }
        }
        ScholarChatGroundedDraftReadinessStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "needs_review",
                "Weakly supported or unsupported draft items remain and need review before a grounded-answer path is added.",
            );
        }
        ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "ready_for_grounded_draft_later",
                "All inspected items were supported by local evidence. This is still only a readiness preview.",
            );
        }
    }

    let summary = grounded_draft_readiness_summary(&status, &inspection_preview);
    let next_required_actions = grounded_draft_readiness_next_required_actions(&status, &inspection_preview);

    Ok(ScholarChatGroundedDraftReadinessPreview {
        status,
        inspection_status: inspection_preview.status,
        normalized_prompt: inspection_preview.normalized_prompt,
        selected_source_count: inspection_preview.selected_source_count,
        evidence_candidate_count: inspection_preview.evidence_candidate_count,
        inspected_item_count: inspection_preview.inspected_item_count,
        supported_item_count: inspection_preview.supported_item_count,
        weakly_supported_item_count: inspection_preview.weakly_supported_item_count,
        unsupported_item_count: inspection_preview.unsupported_item_count,
        summary,
        preview_only: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        blockers,
        warnings,
        next_required_actions,
    })
}

pub fn preview_scholar_chat_grounded_answer_build_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftGroundingInspectionRequest,
) -> AegisResult<ScholarChatGroundedAnswerBuildPlanPreview> {
    let root = root.into();
    let readiness_preview = preview_scholar_chat_grounded_draft_readiness(&root, request)?;
    let status = grounded_answer_build_plan_status(&readiness_preview);
    let mut blockers = readiness_preview.blockers.clone();
    let mut warnings = readiness_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer build plan preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    match status {
        ScholarChatGroundedAnswerBuildPlanStatus::Blocked => {
            if blockers.is_empty() {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "build_plan_blocked",
                    "Grounded-answer build planning is blocked until draft grounding readiness is available.",
                );
            }
        }
        ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "needs_review",
                "Weakly supported or unsupported draft items remain and should be reviewed before planning a GroundedAnswer.",
            );
        }
        ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "plan_ready_later",
                "All inspected items were supported by local evidence. This is still only a plan preview.",
            );
        }
    }

    let summary = grounded_answer_build_plan_summary(&status, &readiness_preview);
    let planned_steps = grounded_answer_build_plan_planned_steps(&status);
    let next_required_actions =
        grounded_answer_build_plan_next_required_actions(&status, &readiness_preview);

    Ok(ScholarChatGroundedAnswerBuildPlanPreview {
        status,
        readiness_status: readiness_preview.status,
        normalized_prompt: readiness_preview.normalized_prompt,
        selected_source_count: readiness_preview.selected_source_count,
        evidence_candidate_count: readiness_preview.evidence_candidate_count,
        inspected_item_count: readiness_preview.inspected_item_count,
        supported_item_count: readiness_preview.supported_item_count,
        weakly_supported_item_count: readiness_preview.weakly_supported_item_count,
        unsupported_item_count: readiness_preview.unsupported_item_count,
        summary,
        planned_steps,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        blockers,
        warnings,
        next_required_actions,
    })
}

pub fn preview_scholar_chat_grounded_answer_candidate(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftGroundingInspectionRequest,
) -> AegisResult<ScholarChatGroundedAnswerCandidatePreview> {
    let root = root.into();
    let build_plan_preview = preview_scholar_chat_grounded_answer_build_plan(&root, request.clone())?;
    let status = grounded_answer_candidate_status(&build_plan_preview);
    let mut blockers = build_plan_preview.blockers.clone();
    let mut warnings = build_plan_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer candidate preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    let candidate_items = if matches!(status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater) {
        let inspection_preview = preview_scholar_chat_draft_grounding_inspection(&root, request)?;
        grounded_answer_candidate_items_from_inspection(&inspection_preview)
    } else {
        Vec::new()
    };

    match status {
        ScholarChatGroundedAnswerCandidateStatus::Blocked => {
            if blockers.is_empty() {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "candidate_blocked",
                    "Grounded-answer candidate preview is blocked until grounded-draft readiness is available.",
                );
            }
        }
        ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "needs_review",
                "Weakly supported or unsupported draft items remain and should be reviewed before a grounded-answer candidate is considered.",
            );
        }
        ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "candidate_ready_later",
                "All inspected items were supported by local evidence. This is still only a grounded-answer candidate preview.",
            );
        }
    }

    let summary = grounded_answer_candidate_summary(&status, &build_plan_preview);
    let next_required_actions = grounded_answer_candidate_next_required_actions(&status, &build_plan_preview);

    Ok(ScholarChatGroundedAnswerCandidatePreview {
        status,
        normalized_prompt: build_plan_preview.normalized_prompt,
        selected_source_count: build_plan_preview.selected_source_count,
        evidence_candidate_count: build_plan_preview.evidence_candidate_count,
        inspected_item_count: build_plan_preview.inspected_item_count,
        supported_item_count: build_plan_preview.supported_item_count,
        weakly_supported_item_count: build_plan_preview.weakly_supported_item_count,
        unsupported_item_count: build_plan_preview.unsupported_item_count,
        candidate_statement_count: candidate_items.len(),
        summary,
        candidate_items,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        blockers,
        warnings,
        next_required_actions,
    })
}

pub fn preview_scholar_chat_grounded_answer_write_eligibility(
    root: impl Into<PathBuf>,
    request: ScholarChatDraftGroundingInspectionRequest,
) -> AegisResult<ScholarChatGroundedAnswerWriteEligibilityPreview> {
    let root = root.into();
    let candidate_preview = preview_scholar_chat_grounded_answer_candidate(&root, request)?;
    Ok(grounded_answer_write_eligibility_preview_from_candidate_preview(candidate_preview))
}

const SCHOLAR_CHAT_DRAFT_GROUNDING_INSPECTION_LIMIT: usize = 8;

struct DraftGroundingInspectionItems {
    items: Vec<ScholarChatDraftGroundingInspectionItem>,
    supported_item_count: usize,
    weakly_supported_item_count: usize,
    unsupported_item_count: usize,
    warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
    was_clamped: bool,
}

fn build_draft_grounding_inspection_preview(
    normalized_prompt: String,
    draft_char_count: usize,
    selected_source_count: usize,
    evidence_candidate_count: usize,
    items: Vec<ScholarChatDraftGroundingInspectionItem>,
    supported_item_count: usize,
    weakly_supported_item_count: usize,
    unsupported_item_count: usize,
    blockers: Vec<ScholarChatDraftGroundingInspectionBlocker>,
    warnings: Vec<ScholarChatDraftGroundingInspectionWarning>,
) -> ScholarChatDraftGroundingInspectionPreview {
    let inspected_item_count = items.len();
    ScholarChatDraftGroundingInspectionPreview {
        status: if draft_char_count == 0 {
            ScholarChatDraftGroundingInspectionStatus::NoDraftText
        } else if selected_source_count == 0 {
            ScholarChatDraftGroundingInspectionStatus::Blocked
        } else if evidence_candidate_count == 0 {
            ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates
        } else {
            ScholarChatDraftGroundingInspectionStatus::Inspected
        },
        normalized_prompt,
        draft_char_count,
        selected_source_count,
        evidence_candidate_count,
        inspected_item_count,
        unsupported_item_count,
        weakly_supported_item_count,
        supported_item_count,
        items,
        inspection_only: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_evidence_pack_built: true,
        no_answer_artifact_created: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        blockers,
        warnings,
    }
}

fn grounded_draft_readiness_status(
    inspection_preview: &ScholarChatDraftGroundingInspectionPreview,
) -> ScholarChatGroundedDraftReadinessStatus {
    match inspection_preview.status {
        ScholarChatDraftGroundingInspectionStatus::Blocked
        | ScholarChatDraftGroundingInspectionStatus::NoDraftText
        | ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates => {
            ScholarChatGroundedDraftReadinessStatus::Blocked
        }
        ScholarChatDraftGroundingInspectionStatus::Inspected => {
            if inspection_preview.inspected_item_count == 0 {
                ScholarChatGroundedDraftReadinessStatus::Blocked
            } else if inspection_preview.unsupported_item_count > 0
                || inspection_preview.weakly_supported_item_count > 0
            {
                ScholarChatGroundedDraftReadinessStatus::NeedsReview
            } else {
                ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater
            }
        }
    }
}

fn grounded_draft_readiness_summary(
    status: &ScholarChatGroundedDraftReadinessStatus,
    inspection_preview: &ScholarChatDraftGroundingInspectionPreview,
) -> String {
    match status {
        ScholarChatGroundedDraftReadinessStatus::Blocked => {
            match inspection_preview.status {
                ScholarChatDraftGroundingInspectionStatus::NoDraftText => {
                    "Grounded-draft readiness is blocked because no draft text was provided.".to_string()
                }
                ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates => {
                    "Grounded-draft readiness is blocked because no local evidence candidates were available.".to_string()
                }
                ScholarChatDraftGroundingInspectionStatus::Blocked => {
                    "Grounded-draft readiness is blocked because no Scholar Chat source context was selected.".to_string()
                }
                ScholarChatDraftGroundingInspectionStatus::Inspected => {
                    "Grounded-draft readiness is blocked because no inspectable draft items were found.".to_string()
                }
            }
        }
        ScholarChatGroundedDraftReadinessStatus::NeedsReview => {
            "The draft is not ready yet: weakly supported or unsupported items remain.".to_string()
        }
        ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater => {
            "All inspected items were supported by local evidence. This is still only a readiness preview.".to_string()
        }
    }
}

fn grounded_draft_readiness_next_required_actions(
    status: &ScholarChatGroundedDraftReadinessStatus,
    inspection_preview: &ScholarChatDraftGroundingInspectionPreview,
) -> Vec<String> {
    match status {
        ScholarChatGroundedDraftReadinessStatus::Blocked => {
            match inspection_preview.status {
                ScholarChatDraftGroundingInspectionStatus::NoDraftText => {
                    vec!["Provide draft text before previewing grounded-draft readiness.".to_string()]
                }
                ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates => {
                    vec![
                        "Add local evidence candidates for the selected sources before retrying grounded-draft readiness."
                            .to_string(),
                    ]
                }
                ScholarChatDraftGroundingInspectionStatus::Blocked => {
                    vec!["Select Scholar Chat source context before previewing grounded-draft readiness.".to_string()]
                }
                ScholarChatDraftGroundingInspectionStatus::Inspected => {
                    vec!["Review the draft grounding inspection and try again.".to_string()]
                }
            }
        }
        ScholarChatGroundedDraftReadinessStatus::NeedsReview => vec![
            "Review weakly supported and unsupported draft items before treating this draft as ready for a future grounded-answer path."
                .to_string(),
        ],
        ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater => vec![
            "A grounded-answer implementation can be added later without changing this readiness preview.".to_string(),
        ],
    }
}

fn grounded_answer_build_plan_status(
    readiness_preview: &ScholarChatGroundedDraftReadinessPreview,
) -> ScholarChatGroundedAnswerBuildPlanStatus {
    match readiness_preview.status {
        ScholarChatGroundedDraftReadinessStatus::Blocked => ScholarChatGroundedAnswerBuildPlanStatus::Blocked,
        ScholarChatGroundedDraftReadinessStatus::NeedsReview => ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview,
        ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater => {
            ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater
        }
    }
}

fn grounded_answer_build_plan_summary(
    status: &ScholarChatGroundedAnswerBuildPlanStatus,
    readiness_preview: &ScholarChatGroundedDraftReadinessPreview,
) -> String {
    match status {
        ScholarChatGroundedAnswerBuildPlanStatus::Blocked => match readiness_preview.status {
            ScholarChatGroundedDraftReadinessStatus::Blocked => {
                "Grounded-answer build planning is blocked because grounded-draft readiness is blocked.".to_string()
            }
            ScholarChatGroundedDraftReadinessStatus::NeedsReview => {
                "Grounded-answer build planning is blocked because grounded-draft readiness still needs review.".to_string()
            }
            ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater => {
                "Grounded-answer build planning is blocked until the readiness preview is available.".to_string()
            }
        },
        ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => {
            "The draft is not yet ready for a grounded-answer build plan because weakly supported or unsupported items remain.".to_string()
        }
        ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => {
            "All inspected items were supported by local evidence. This is still only a grounded-answer build plan preview.".to_string()
        }
    }
}

fn grounded_answer_build_plan_planned_steps(
    status: &ScholarChatGroundedAnswerBuildPlanStatus,
) -> Vec<String> {
    match status {
        ScholarChatGroundedAnswerBuildPlanStatus::Blocked => vec![
            "Resolve grounded-draft readiness blockers.".to_string(),
            "Re-run draft grounding inspection and readiness preview.".to_string(),
            "Only then add a future GroundedAnswer implementation.".to_string(),
        ],
        ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => vec![
            "Review supported draft items.".to_string(),
            "Resolve weakly supported and unsupported items.".to_string(),
            "Require an explicit implementation phase before writing GroundedAnswer.".to_string(),
        ],
        ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => vec![
            "Review supported draft items.".to_string(),
            "Map supported draft items to future grounded claims.".to_string(),
            "Require an explicit implementation phase before writing GroundedAnswer.".to_string(),
        ],
    }
}

fn grounded_answer_build_plan_next_required_actions(
    status: &ScholarChatGroundedAnswerBuildPlanStatus,
    readiness_preview: &ScholarChatGroundedDraftReadinessPreview,
) -> Vec<String> {
    let mut next_required_actions = readiness_preview.next_required_actions.clone();
    match status {
        ScholarChatGroundedAnswerBuildPlanStatus::Blocked => {
            push_unique_text(
                &mut next_required_actions,
                "Resolve grounded-draft readiness blockers before any GroundedAnswer implementation.",
            );
        }
        ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review weakly supported and unsupported draft items before any GroundedAnswer implementation.",
            );
        }
        ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A GroundedAnswer implementation can be added later without changing this plan preview.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_candidate_status(
    build_plan_preview: &ScholarChatGroundedAnswerBuildPlanPreview,
) -> ScholarChatGroundedAnswerCandidateStatus {
    match build_plan_preview.status {
        ScholarChatGroundedAnswerBuildPlanStatus::Blocked => ScholarChatGroundedAnswerCandidateStatus::Blocked,
        ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => ScholarChatGroundedAnswerCandidateStatus::NeedsReview,
        ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => {
            ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater
        }
    }
}

fn grounded_answer_candidate_summary(
    status: &ScholarChatGroundedAnswerCandidateStatus,
    build_plan_preview: &ScholarChatGroundedAnswerBuildPlanPreview,
) -> String {
    match status {
        ScholarChatGroundedAnswerCandidateStatus::Blocked => match build_plan_preview.status {
            ScholarChatGroundedAnswerBuildPlanStatus::Blocked => {
                "Grounded-answer candidate preview is blocked because grounded-answer build planning is blocked."
                    .to_string()
            }
            ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview => {
                "Grounded-answer candidate preview is blocked because grounded-answer build planning still needs review."
                    .to_string()
            }
            ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater => {
                "Grounded-answer candidate preview is blocked until the build-plan preview is available.".to_string()
            }
        },
        ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
            "The draft is not yet ready for a grounded-answer candidate because weakly supported or unsupported items remain."
                .to_string()
        }
        ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
            "All inspected items were supported by local evidence. This is still only a grounded-answer candidate preview."
                .to_string()
        }
    }
}

fn grounded_answer_candidate_next_required_actions(
    status: &ScholarChatGroundedAnswerCandidateStatus,
    build_plan_preview: &ScholarChatGroundedAnswerBuildPlanPreview,
) -> Vec<String> {
    let mut next_required_actions = build_plan_preview.next_required_actions.clone();
    match status {
        ScholarChatGroundedAnswerCandidateStatus::Blocked => {
            push_unique_text(
                &mut next_required_actions,
                "Resolve grounded-answer build-plan blockers before considering a grounded-answer candidate.",
            );
        }
        ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review weakly supported and unsupported draft items before considering a grounded-answer candidate.",
            );
        }
        ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A GroundedAnswer implementation can be added later without changing this candidate preview.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_candidate_items_from_inspection(
    inspection_preview: &ScholarChatDraftGroundingInspectionPreview,
) -> Vec<ScholarChatGroundedAnswerCandidateItem> {
    inspection_preview
        .items
        .iter()
        .map(|item| ScholarChatGroundedAnswerCandidateItem {
            item_index: item.item_index,
            statement_preview: item.text_preview.clone(),
            support_status: item.support_status.clone(),
            source_ids: item.source_ids.clone(),
            locator_previews: item.locator_previews.clone(),
            matched_evidence_count: item.matched_evidence_count,
        })
        .collect()
}

fn grounded_answer_write_eligibility_status(
    candidate_preview: &ScholarChatGroundedAnswerCandidatePreview,
) -> ScholarChatGroundedAnswerWriteEligibilityStatus {
    match candidate_preview.status {
        ScholarChatGroundedAnswerCandidateStatus::Blocked => {
            ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked
        }
        ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
            ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview
        }
        ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
            if candidate_preview.candidate_statement_count > 0 {
                ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater
            } else {
                ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked
            }
        }
    }
}

fn grounded_answer_write_eligibility_summary(
    status: &ScholarChatGroundedAnswerWriteEligibilityStatus,
    candidate_preview: &ScholarChatGroundedAnswerCandidatePreview,
) -> String {
    match status {
        ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => {
            match candidate_preview.status {
                ScholarChatGroundedAnswerCandidateStatus::Blocked => {
                    "Grounded-answer write eligibility is blocked because grounded-answer candidate preview is blocked.".to_string()
                }
                ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
                    "Grounded-answer write eligibility is blocked because grounded-answer candidate preview still needs review.".to_string()
                }
                ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
                    if candidate_preview.candidate_statement_count == 0 {
                        "Grounded-answer write eligibility is blocked because no candidate statements were available.".to_string()
                    } else {
                        "Grounded-answer write eligibility is blocked until the candidate preview is available.".to_string()
                    }
                }
            }
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
            "The draft is not yet ready for a grounded-answer write because weakly supported or unsupported items remain.".to_string()
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
            "All inspected items were supported by local evidence. A future GroundedAnswer write can be added later.".to_string()
        }
    }
}

fn grounded_answer_write_eligibility_reasons(
    status: &ScholarChatGroundedAnswerWriteEligibilityStatus,
    candidate_preview: &ScholarChatGroundedAnswerCandidatePreview,
) -> Vec<String> {
    let mut reasons = vec![
        format!("Candidate status: {:?}", candidate_preview.status),
        format!("Candidate statements: {}", candidate_preview.candidate_statement_count),
    ];
    match status {
        ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => match candidate_preview.status {
            ScholarChatGroundedAnswerCandidateStatus::Blocked => {
                reasons.push("Grounded-answer candidate preview is blocked.".to_string());
            }
            ScholarChatGroundedAnswerCandidateStatus::NeedsReview => {
                reasons.push("Grounded-answer candidate preview still needs review.".to_string());
            }
            ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater => {
                if candidate_preview.candidate_statement_count == 0 {
                    reasons.push("No candidate statements were available.".to_string());
                } else {
                    reasons.push("Grounded-answer write eligibility is still blocked.".to_string());
                }
            }
        },
        ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
            reasons.push("Weakly supported or unsupported draft items remain.".to_string());
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
            reasons.push("All inspected items were supported by local evidence.".to_string());
            reasons.push(
                "A future GroundedAnswer write can be added later after an explicit implementation phase."
                    .to_string(),
            );
        }
    }
    reasons
}

fn grounded_answer_write_eligibility_next_required_actions(
    status: &ScholarChatGroundedAnswerWriteEligibilityStatus,
    candidate_preview: &ScholarChatGroundedAnswerCandidatePreview,
) -> Vec<String> {
    let mut next_required_actions = candidate_preview.next_required_actions.clone();
    match status {
        ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => {
            push_unique_text(
                &mut next_required_actions,
                "Resolve grounded-answer candidate blockers before any GroundedAnswer write implementation.",
            );
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review weakly supported and unsupported draft items before any GroundedAnswer write implementation.",
            );
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
            push_unique_text(
                &mut next_required_actions,
                "A GroundedAnswer write implementation can be added later without changing this preview.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_write_eligibility_preview_from_candidate_preview(
    candidate_preview: ScholarChatGroundedAnswerCandidatePreview,
) -> ScholarChatGroundedAnswerWriteEligibilityPreview {
    let status = grounded_answer_write_eligibility_status(&candidate_preview);
    let mut blockers = candidate_preview.blockers.clone();
    let mut warnings = candidate_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer write-eligibility preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    match status {
        ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => {
            if blockers.is_empty() {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "write_eligibility_blocked",
                    "Grounded-answer write eligibility is blocked until grounded-answer candidate preview is available.",
                );
            }
            if candidate_preview.candidate_statement_count == 0 {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "candidate_statements_missing",
                    "No candidate statements were available for write eligibility.",
                );
            }
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "needs_review",
                "Weakly supported or unsupported draft items remain and need review before a future GroundedAnswer write is added.",
            );
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "write_eligible_later",
                "All inspected items were supported by local evidence. This is still only a write-eligibility preview.",
            );
        }
    }

    let summary = grounded_answer_write_eligibility_summary(&status, &candidate_preview);
    let eligibility_reasons = grounded_answer_write_eligibility_reasons(&status, &candidate_preview);
    let next_required_actions =
        grounded_answer_write_eligibility_next_required_actions(&status, &candidate_preview);

    ScholarChatGroundedAnswerWriteEligibilityPreview {
        status,
        candidate_status: candidate_preview.status,
        normalized_prompt: candidate_preview.normalized_prompt,
        selected_source_count: candidate_preview.selected_source_count,
        evidence_candidate_count: candidate_preview.evidence_candidate_count,
        inspected_item_count: candidate_preview.inspected_item_count,
        supported_item_count: candidate_preview.supported_item_count,
        weakly_supported_item_count: candidate_preview.weakly_supported_item_count,
        unsupported_item_count: candidate_preview.unsupported_item_count,
        candidate_statement_count: candidate_preview.candidate_statement_count,
        eligibility_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
    }
}

pub fn preview_scholar_chat_grounded_answer_build_intent(
    root: impl Into<PathBuf>,
    request: ScholarChatGroundedAnswerBuildIntentRequest,
) -> AegisResult<ScholarChatGroundedAnswerBuildIntentPreview> {
    let root = root.into();
    let normalized_prompt = normalized_prompt_or_err(request.grounding_request.scholar_chat_request.prompt.clone())?;
    let normalized_answer_draft_id = normalize_optional_answer_draft_id(request.answer_draft_id)?;
    let write_eligibility_preview =
        preview_scholar_chat_grounded_answer_write_eligibility(&root, request.grounding_request.clone())?;
    Ok(grounded_answer_build_intent_preview_from_write_eligibility_preview(
        write_eligibility_preview,
        normalized_prompt,
        normalized_answer_draft_id,
        request.explicit_user_intent,
    ))
}

pub fn preview_scholar_chat_grounded_answer_build_request(
    root: impl Into<PathBuf>,
    request: ScholarChatGroundedAnswerBuildRequestPreviewRequest,
) -> AegisResult<ScholarChatGroundedAnswerBuildRequestPreview> {
    let root = root.into();
    let normalized_prompt = normalized_prompt_or_err(
        request
            .build_intent_request
            .grounding_request
            .scholar_chat_request
            .prompt
            .clone(),
    )?;
    let normalized_answer_draft_id =
        normalize_optional_answer_draft_id(request.build_intent_request.answer_draft_id.clone())?;
    let (normalized_selected_source_ids, _selected_source_count) = normalize_selected_source_ids(
        request
            .build_intent_request
            .grounding_request
            .scholar_chat_request
            .selected_source_ids
            .clone(),
    )?;
    let normalized_build_intent_request = ScholarChatGroundedAnswerBuildIntentRequest {
        grounding_request: ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: normalized_prompt.clone(),
                mode: request
                    .build_intent_request
                    .grounding_request
                    .scholar_chat_request
                    .mode,
                grounding_policy: request
                    .build_intent_request
                    .grounding_request
                    .scholar_chat_request
                    .grounding_policy,
                selected_source_ids: normalized_selected_source_ids.clone(),
            },
            draft_text: request.build_intent_request.grounding_request.draft_text.clone(),
            max_items: request.build_intent_request.grounding_request.max_items,
        },
        answer_draft_id: normalized_answer_draft_id.clone(),
        explicit_user_intent: request.build_intent_request.explicit_user_intent,
    };
    let build_intent_preview =
        preview_scholar_chat_grounded_answer_build_intent(&root, normalized_build_intent_request)?;
    Ok(grounded_answer_build_request_preview_from_build_intent_preview(
        build_intent_preview,
        normalized_prompt,
        normalized_answer_draft_id,
        normalized_selected_source_ids,
    ))
}

pub fn preview_scholar_chat_grounded_answer_build_preflight(
    root: impl Into<PathBuf>,
    request: ScholarChatGroundedAnswerBuildPreflightPreviewRequest,
) -> AegisResult<ScholarChatGroundedAnswerBuildPreflightPreview> {
    let root = root.into();
    let build_request_preview =
        preview_scholar_chat_grounded_answer_build_request(&root, request.build_request_preview_request)?;
    let normalized_prompt = build_request_preview.normalized_prompt.clone();
    let selected_source_ids = build_request_preview.selected_source_ids.clone();
    let answer_draft_id = build_request_preview.answer_draft_id.clone();
    let answer_draft_id_present = answer_draft_id.is_some();
    let mut answer_draft_present = false;
    let mut answer_draft_readable = false;
    let mut answer_draft_claim_count = 0usize;
    let mut blockers = build_request_preview.blockers.clone();
    let mut warnings = build_request_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer build preflight preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    let status = match build_request_preview.status {
        ScholarChatGroundedAnswerBuildRequestStatus::Blocked => ScholarChatGroundedAnswerBuildPreflightStatus::Blocked,
        ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview => ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview,
        ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater => {
            if let Some(answer_draft_id) = answer_draft_id.as_ref() {
                let corpus_paths = CorpusPaths::new(root.clone());
                let registry = SourceRegistry::load(&corpus_paths.registry_path());
                if let Ok(registry) = registry {
                    let mut answer_draft_missing = true;
                    for lookup_source_id in &selected_source_ids {
                        let record = match registry.get_source(lookup_source_id) {
                            Ok(record) => record,
                            Err(_) => continue,
                        };
                        let draft_path = corpus_paths
                            .source_version_dir(&record.source_id, &record.version_id)
                            .join("answer_drafts")
                            .join(format!("{answer_draft_id}.json"));
                        if !draft_path.exists() {
                            continue;
                        }
                        answer_draft_missing = false;
                        answer_draft_present = true;
                        match fs::read_to_string(&draft_path)
                            .ok()
                            .and_then(|content| serde_json::from_str::<crate::answer_draft::AnswerDraft>(&content).ok())
                        {
                            Some(answer_draft) => {
                                answer_draft_readable = true;
                                answer_draft_claim_count = answer_draft.claim_count;
                                break;
                            }
                            None => {
                                push_grounding_inspection_blocker(
                                    &mut blockers,
                                    "answer_draft_unreadable",
                                    "The referenced AnswerDraft is unreadable.",
                                );
                                break;
                            }
                        }
                    }
                    if answer_draft_missing {
                        push_grounding_inspection_blocker(
                            &mut blockers,
                            "answer_draft_missing",
                            "The referenced AnswerDraft is missing.",
                        );
                    }
                } else {
                    push_grounding_inspection_blocker(
                        &mut blockers,
                        "answer_draft_missing",
                        "The referenced AnswerDraft is missing.",
                    );
                }
                if answer_draft_readable {
                    ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
                } else {
                    if answer_draft_present && !blockers.iter().any(|blocker| blocker.kind == "answer_draft_unreadable") {
                        push_grounding_inspection_blocker(
                            &mut blockers,
                            "answer_draft_unreadable",
                            "The referenced AnswerDraft is unreadable.",
                        );
                    }
                    ScholarChatGroundedAnswerBuildPreflightStatus::Blocked
                }
            } else {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "answer_draft_id_missing",
                    "No answer draft ID was provided.",
                );
                ScholarChatGroundedAnswerBuildPreflightStatus::Blocked
            }
        }
    };

    if matches!(status, ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview) {
        push_grounding_inspection_warning(
            &mut warnings,
            "needs_review",
            "The build request still needs review before AnswerDraft preflight can be accepted.",
        );
    }
    if matches!(status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater) {
        push_grounding_inspection_warning(
            &mut warnings,
            "preflight_ready_later",
            "The referenced AnswerDraft is readable. This is still only a preflight preview.",
        );
    }

    let required_inputs = grounded_answer_build_preflight_required_inputs();
    let missing_inputs = grounded_answer_build_preflight_missing_inputs(
        &build_request_preview,
        answer_draft_id_present,
        answer_draft_id.as_ref(),
        answer_draft_present,
        answer_draft_readable,
        &status,
    );
    let preflight_reasons = grounded_answer_build_preflight_reasons(
        &build_request_preview,
        answer_draft_id_present,
        answer_draft_present,
        answer_draft_readable,
        answer_draft_claim_count,
        &status,
    );
    let next_required_actions = grounded_answer_build_preflight_next_required_actions(
        &status,
        &build_request_preview,
        answer_draft_id_present,
        answer_draft_present,
        answer_draft_readable,
    );
    let summary = grounded_answer_build_preflight_summary(
        &status,
        &build_request_preview,
        answer_draft_id_present,
        answer_draft_present,
        answer_draft_readable,
        answer_draft_claim_count,
    );

    Ok(ScholarChatGroundedAnswerBuildPreflightPreview {
        status,
        build_request_status: build_request_preview.status,
        build_intent_status: build_request_preview.build_intent_status,
        write_eligibility_status: build_request_preview.write_eligibility_status,
        candidate_status: build_request_preview.candidate_status,
        normalized_prompt,
        selected_source_count: build_request_preview.selected_source_count,
        evidence_candidate_count: build_request_preview.evidence_candidate_count,
        inspected_item_count: build_request_preview.inspected_item_count,
        supported_item_count: build_request_preview.supported_item_count,
        weakly_supported_item_count: build_request_preview.weakly_supported_item_count,
        unsupported_item_count: build_request_preview.unsupported_item_count,
        candidate_statement_count: build_request_preview.candidate_statement_count,
        answer_draft_id,
        selected_source_ids,
        answer_draft_present,
        answer_draft_readable,
        answer_draft_claim_count,
        required_inputs,
        missing_inputs,
        preflight_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
        no_grounded_answer_service_call: true,
        no_grounded_answer_write: true,
    })
}

pub fn preview_scholar_chat_grounded_answer_execution_readiness(
    root: impl Into<PathBuf>,
    request: ScholarChatGroundedAnswerExecutionReadinessPreviewRequest,
) -> AegisResult<ScholarChatGroundedAnswerExecutionReadinessPreview> {
    let root = root.into();
    let build_preflight_preview =
        preview_scholar_chat_grounded_answer_build_preflight(&root, request.build_preflight_preview_request)?;
    let normalized_prompt = build_preflight_preview.normalized_prompt.clone();
    let answer_draft_id = build_preflight_preview.answer_draft_id.clone();
    let selected_source_ids = build_preflight_preview.selected_source_ids.clone();
    let answer_draft_present = build_preflight_preview.answer_draft_present;
    let answer_draft_readable = build_preflight_preview.answer_draft_readable;
    let answer_draft_claim_count = build_preflight_preview.answer_draft_claim_count;
    let build_preflight_status = build_preflight_preview.status.clone();
    let mut blockers = build_preflight_preview.blockers.clone();
    let mut warnings = build_preflight_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is an execution-readiness preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );
    if !request.execution_consent {
        push_grounding_inspection_blocker(
            &mut blockers,
            "execution_consent_missing",
            "Execution consent was not given.",
        );
    }

    let status = match build_preflight_status {
        ScholarChatGroundedAnswerBuildPreflightStatus::Blocked => ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked,
        ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview => ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview,
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater => {
            if request.execution_consent {
                ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater
            } else {
                ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked
            }
        }
    };

    if matches!(status, ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview) {
        push_grounding_inspection_warning(
            &mut warnings,
            "needs_review",
            "The grounded-answer build preflight still needs review before execution readiness can be accepted.",
        );
    }
    if matches!(status, ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater) {
        push_grounding_inspection_warning(
            &mut warnings,
            "execution_ready_later",
            "Execution consent is given and the build preflight is ready later. This is still only a preview.",
        );
    }

    let required_inputs = grounded_answer_execution_readiness_required_inputs();
    let missing_inputs = grounded_answer_execution_readiness_missing_inputs(
        &build_preflight_status,
        answer_draft_readable,
        request.execution_consent,
    );
    let readiness_reasons = grounded_answer_execution_readiness_reasons(
        &build_preflight_preview,
        request.execution_consent,
        &status,
    );
    let next_required_actions = grounded_answer_execution_readiness_next_required_actions(
        &status,
        &build_preflight_preview,
        request.execution_consent,
    );
    let summary = grounded_answer_execution_readiness_summary(
        &status,
        &build_preflight_preview,
        request.execution_consent,
    );

    Ok(ScholarChatGroundedAnswerExecutionReadinessPreview {
        status,
        build_preflight_status,
        build_request_status: build_preflight_preview.build_request_status,
        build_intent_status: build_preflight_preview.build_intent_status,
        write_eligibility_status: build_preflight_preview.write_eligibility_status,
        candidate_status: build_preflight_preview.candidate_status,
        normalized_prompt,
        answer_draft_id,
        selected_source_ids,
        selected_source_count: build_preflight_preview.selected_source_count,
        evidence_candidate_count: build_preflight_preview.evidence_candidate_count,
        inspected_item_count: build_preflight_preview.inspected_item_count,
        supported_item_count: build_preflight_preview.supported_item_count,
        weakly_supported_item_count: build_preflight_preview.weakly_supported_item_count,
        unsupported_item_count: build_preflight_preview.unsupported_item_count,
        candidate_statement_count: build_preflight_preview.candidate_statement_count,
        answer_draft_present,
        answer_draft_readable,
        answer_draft_claim_count,
        execution_consent: request.execution_consent,
        required_inputs,
        missing_inputs,
        readiness_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
        no_grounded_answer_service_call: true,
        no_grounded_answer_write: true,
    })
}

pub fn preview_scholar_chat_grounded_answer_execution_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatGroundedAnswerExecutionPlanPreviewRequest,
) -> AegisResult<ScholarChatGroundedAnswerExecutionPlanPreview> {
    let root = root.into();
    let readiness_preview = preview_scholar_chat_grounded_answer_execution_readiness(
        &root,
        request.execution_readiness_preview_request,
    )?;
    let readiness_status = readiness_preview.status.clone();
    let planned_operation = "future_grounded_answer_build".to_string();
    let status = match readiness_status {
        ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked => {
            ScholarChatGroundedAnswerExecutionPlanStatus::Blocked
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview => {
            ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater => {
            ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater
        }
    };
    let planned_inputs = grounded_answer_execution_plan_planned_inputs();
    let planned_outputs = grounded_answer_execution_plan_planned_outputs();
    let planned_write_targets = grounded_answer_execution_plan_planned_write_targets();
    let required_inputs = grounded_answer_execution_plan_required_inputs();
    let missing_inputs = grounded_answer_execution_plan_missing_inputs(&readiness_preview);
    let mut plan_reasons = grounded_answer_execution_plan_reasons(
        &readiness_preview,
        &status,
        &planned_operation,
        &planned_inputs,
        &planned_outputs,
        &planned_write_targets,
    );
    let mut blockers = readiness_preview.blockers.clone();
    let mut warnings = readiness_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is an execution-plan preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );
    if !readiness_preview.execution_consent {
        push_grounding_inspection_blocker(
            &mut blockers,
            "execution_consent_missing",
            "Execution consent was not given.",
        );
    }

    let next_required_actions = grounded_answer_execution_plan_next_required_actions(
        &status,
        &readiness_preview,
    );
    let summary = grounded_answer_execution_plan_summary(&status, &readiness_preview);

    if matches!(status, ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview) {
        push_unique_text(
            &mut plan_reasons,
            "The grounded-answer execution readiness still needs review before planning a future build.",
        );
    }
    if matches!(status, ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater) {
        push_unique_text(
            &mut plan_reasons,
            "The grounded-answer execution readiness is ready later and execution consent is true.",
        );
    }

    Ok(ScholarChatGroundedAnswerExecutionPlanPreview {
        status,
        readiness_status,
        build_preflight_status: readiness_preview.build_preflight_status,
        build_request_status: readiness_preview.build_request_status,
        build_intent_status: readiness_preview.build_intent_status,
        write_eligibility_status: readiness_preview.write_eligibility_status,
        candidate_status: readiness_preview.candidate_status,
        normalized_prompt: readiness_preview.normalized_prompt,
        answer_draft_id: readiness_preview.answer_draft_id,
        selected_source_ids: readiness_preview.selected_source_ids,
        selected_source_count: readiness_preview.selected_source_count,
        evidence_candidate_count: readiness_preview.evidence_candidate_count,
        inspected_item_count: readiness_preview.inspected_item_count,
        supported_item_count: readiness_preview.supported_item_count,
        weakly_supported_item_count: readiness_preview.weakly_supported_item_count,
        unsupported_item_count: readiness_preview.unsupported_item_count,
        candidate_statement_count: readiness_preview.candidate_statement_count,
        answer_draft_present: readiness_preview.answer_draft_present,
        answer_draft_readable: readiness_preview.answer_draft_readable,
        answer_draft_claim_count: readiness_preview.answer_draft_claim_count,
        execution_consent: readiness_preview.execution_consent,
        planned_operation,
        planned_inputs,
        planned_outputs,
        planned_write_targets,
        required_inputs,
        missing_inputs,
        plan_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
        no_grounded_answer_service_call: true,
        no_grounded_answer_write: true,
    })
}

pub fn preview_scholar_chat_runtime_diagnostic_bridge(
    root: impl Into<PathBuf>,
    request: ScholarChatRuntimeDiagnosticBridgePreviewRequest,
) -> AegisResult<ScholarChatRuntimeDiagnosticBridgePreview> {
    let root = root.into();
    let normalized_prompt = request.scholar_chat_request.prompt.trim().to_string();
    let (_selected_source_ids, selected_source_count) =
        normalize_selected_source_ids(request.scholar_chat_request.selected_source_ids)?;
    let smoke_execution_plan_preview =
        preview_llama_runtime_smoke_execution_plan(&root, request.smoke_execution_plan_preview_request)?;
    let smoke_execution_plan_status = smoke_execution_plan_preview.status.clone();
    let smoke_readiness_status = smoke_execution_plan_preview.smoke_readiness_status.clone();
    let capability_status = smoke_execution_plan_preview.capability_status.clone();
    let version_probe_status = smoke_execution_plan_preview.version_probe_status.clone();
    let probe_readiness_status = smoke_execution_plan_preview.probe_readiness_status.clone();
    let validation_status = smoke_execution_plan_preview.validation_status.clone();
    let adapter_contract_status = smoke_execution_plan_preview.adapter_contract_status.clone();
    let adapter_kind = smoke_execution_plan_preview.adapter_kind.clone();
    let normalized_model_family = smoke_execution_plan_preview.normalized_model_family.clone();
    let normalized_model_format = smoke_execution_plan_preview.normalized_model_format.clone();
    let safe_executable_file_name = smoke_execution_plan_preview.safe_executable_file_name.clone();
    let safe_model_file_name = smoke_execution_plan_preview.safe_model_file_name.clone();
    let diagnostic_prompt_char_count = smoke_execution_plan_preview.diagnostic_prompt_char_count;
    let max_output_tokens = smoke_execution_plan_preview.max_output_tokens;
    let timeout_ms = smoke_execution_plan_preview.timeout_ms;
    let prompt_is_blank = normalized_prompt.is_empty();
    let selected_sources_missing = selected_source_count == 0;

    let status = if prompt_is_blank || selected_sources_missing {
        ScholarChatRuntimeDiagnosticBridgeStatus::Blocked
    } else {
        match smoke_execution_plan_status {
            LocalRuntimeSmokeExecutionPlanStatus::Blocked => ScholarChatRuntimeDiagnosticBridgeStatus::Blocked,
            LocalRuntimeSmokeExecutionPlanStatus::NeedsReview => ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview,
            LocalRuntimeSmokeExecutionPlanStatus::PlanReadyLater => {
                ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater
            }
        }
    };

    let mut blockers = smoke_execution_plan_preview.blockers.clone();
    let mut warnings = smoke_execution_plan_preview.warnings.clone();
    let mut runtime_diagnostic_reasons = smoke_execution_plan_preview.plan_reasons.clone();
    let mut next_required_actions = smoke_execution_plan_preview.next_required_actions.clone();

    push_runtime_diagnostic_warning(
        &mut warnings,
        "boundary",
        "This is a bridge preview only; it does not run smoke diagnostics, inference, or Scholar Chat answers.",
    );

    if prompt_is_blank {
        push_runtime_diagnostic_blocker(
            &mut blockers,
            "scholar_chat_prompt_missing",
            "The Scholar Chat prompt is blank.",
        );
        push_unique_text(
            &mut runtime_diagnostic_reasons,
            "The Scholar Chat prompt is blank.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Provide a nonblank Scholar Chat prompt.",
        );
    }

    if selected_sources_missing {
        push_runtime_diagnostic_blocker(
            &mut blockers,
            "scholar_chat_sources_missing",
            "No Scholar Chat sources are selected.",
        );
        push_unique_text(
            &mut runtime_diagnostic_reasons,
            "No Scholar Chat sources are selected.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Select at least one Scholar Chat source.",
        );
    }

    match status {
        ScholarChatRuntimeDiagnosticBridgeStatus::Blocked => {
            push_unique_text(
                &mut runtime_diagnostic_reasons,
                "The llama.cpp smoke execution plan is not ready later for a Scholar Chat runtime diagnostic bridge.",
            );
            if matches!(smoke_execution_plan_status, LocalRuntimeSmokeExecutionPlanStatus::Blocked) {
                push_unique_text(
                    &mut next_required_actions,
                    "Make the llama.cpp smoke execution plan ready later first.",
                );
            }
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview => {
            push_unique_text(
                &mut runtime_diagnostic_reasons,
                "The llama.cpp smoke execution plan still needs review before the bridge can be ready later.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Resolve the llama.cpp smoke execution plan review items before previewing a future runtime diagnostic bridge.",
            );
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater => {
            push_unique_text(
                &mut runtime_diagnostic_reasons,
                "The llama.cpp smoke execution plan is ready later and the Scholar Chat runtime diagnostic bridge is ready later.",
            );
            push_unique_text(
                &mut next_required_actions,
                "A future runtime diagnostic execution step can be added later.",
            );
        }
    }

    let summary = match status {
        ScholarChatRuntimeDiagnosticBridgeStatus::Blocked => {
            if prompt_is_blank {
                "The Scholar Chat runtime diagnostic bridge is blocked until the prompt is nonblank.".to_string()
            } else if selected_sources_missing {
                "The Scholar Chat runtime diagnostic bridge is blocked until at least one Scholar Chat source is selected.".to_string()
            } else {
                "The Scholar Chat runtime diagnostic bridge is blocked until the llama.cpp smoke execution plan is ready later.".to_string()
            }
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview => {
            "The Scholar Chat runtime diagnostic bridge still needs review because the llama.cpp smoke execution plan still needs review.".to_string()
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater => {
            "The Scholar Chat runtime diagnostic bridge is ready later for a future runtime diagnostic step.".to_string()
        }
    };

    Ok(ScholarChatRuntimeDiagnosticBridgePreview {
        status,
        normalized_prompt,
        selected_source_count,
        smoke_execution_plan_status,
        smoke_readiness_status,
        capability_status,
        version_probe_status,
        probe_readiness_status,
        validation_status,
        adapter_contract_status,
        adapter_kind,
        normalized_model_family,
        normalized_model_format,
        safe_executable_file_name,
        safe_model_file_name,
        diagnostic_prompt_char_count,
        max_output_tokens,
        timeout_ms,
        runtime_diagnostic_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        no_smoke_execution: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_answer_draft_created: true,
        no_grounded_answer_created: true,
        no_final_answer_created: true,
        no_grounding_applied: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_artifact_write: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

pub fn preview_scholar_chat_runtime_answer_pipeline_gate(
    root: impl Into<PathBuf>,
    request: ScholarChatRuntimeAnswerPipelineGatePreviewRequest,
) -> AegisResult<ScholarChatRuntimeAnswerPipelineGatePreview> {
    let root = root.into();
    let grounded_answer_execution_plan_preview_request = request.grounded_answer_execution_plan_preview_request;
    let runtime_diagnostic_result_preview_request = request.runtime_diagnostic_result_preview_request;

    let grounded_answer_scholar_chat_request = grounded_answer_execution_plan_preview_request
        .execution_readiness_preview_request
        .build_preflight_preview_request
        .build_request_preview_request
        .build_intent_request
        .grounding_request
        .scholar_chat_request
        .clone();
    let runtime_diagnostic_scholar_chat_request = runtime_diagnostic_result_preview_request
        .bridge_preview_request
        .scholar_chat_request
        .clone();

    let (grounded_selected_source_ids, selected_source_count) =
        normalize_selected_source_ids(grounded_answer_scholar_chat_request.selected_source_ids.clone())?;
    let (runtime_selected_source_ids, _runtime_selected_source_count) =
        normalize_selected_source_ids(runtime_diagnostic_scholar_chat_request.selected_source_ids.clone())?;
    let normalized_prompt = grounded_answer_scholar_chat_request.prompt.trim().to_string();
    let runtime_normalized_prompt = runtime_diagnostic_scholar_chat_request.prompt.trim().to_string();
    let prompt_is_blank = normalized_prompt.is_empty();
    let sources_are_missing = selected_source_count == 0;
    let normalized_requests_match = ScholarChatRequest {
        prompt: normalized_prompt.clone(),
        mode: grounded_answer_scholar_chat_request.mode.clone(),
        grounding_policy: grounded_answer_scholar_chat_request.grounding_policy.clone(),
        selected_source_ids: grounded_selected_source_ids.clone(),
    } == ScholarChatRequest {
        prompt: runtime_normalized_prompt.clone(),
        mode: runtime_diagnostic_scholar_chat_request.mode.clone(),
        grounding_policy: runtime_diagnostic_scholar_chat_request.grounding_policy.clone(),
        selected_source_ids: runtime_selected_source_ids.clone(),
    };

    if prompt_is_blank || sources_are_missing {
        let mut blockers = Vec::new();
        let mut warnings = Vec::new();
        let mut pipeline_gate_reasons = Vec::new();
        let mut next_required_actions = Vec::new();

        push_unique_text(
            &mut warnings,
            "boundary: This is a pipeline-gate preview only; it does not run smoke diagnostics, inference, or Scholar Chat answers.",
        );

        if prompt_is_blank {
            push_unique_text(
                &mut blockers,
                "scholar_chat_prompt_missing: The Scholar Chat prompt is blank.",
            );
            push_unique_text(
                &mut pipeline_gate_reasons,
                "The future runtime answer pipeline is blocked until the Scholar Chat prompt is nonblank.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Provide a nonblank Scholar Chat prompt before previewing the runtime answer pipeline gate.",
            );
        }

        if sources_are_missing {
            push_unique_text(
                &mut blockers,
                "scholar_chat_sources_missing: No Scholar Chat sources are selected.",
            );
            push_unique_text(
                &mut pipeline_gate_reasons,
                "The future runtime answer pipeline is blocked until at least one Scholar Chat source is selected.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Select at least one Scholar Chat source before previewing the runtime answer pipeline gate.",
            );
        }

        push_unique_text(
            &mut next_required_actions,
            "A future grounded-answer execution plan and runtime diagnostic result can be added later once the preview inputs are valid.",
        );

        return Ok(ScholarChatRuntimeAnswerPipelineGatePreview {
            status: ScholarChatRuntimeAnswerPipelineGateStatus::Blocked,
            grounded_answer_execution_plan_status: ScholarChatGroundedAnswerExecutionPlanStatus::Blocked,
            grounded_answer_execution_readiness_status: ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked,
            runtime_diagnostic_result_status: ScholarChatRuntimeDiagnosticResultStatus::Blocked,
            runtime_diagnostic_bridge_status: ScholarChatRuntimeDiagnosticBridgeStatus::Blocked,
            smoke_diagnostic_status: LocalRuntimeSmokeDiagnosticStatus::Blocked,
            smoke_execution_plan_status: LocalRuntimeSmokeExecutionPlanStatus::Blocked,
            smoke_readiness_status: LocalRuntimeSmokeReadinessStatus::Blocked,
            capability_status: LocalRuntimeCapabilityStatus::Blocked,
            version_probe_status: LocalRuntimeVersionProbeStatus::Blocked,
            probe_readiness_status: LocalRuntimeProbeReadinessStatus::Blocked,
            validation_status: LocalRuntimeValidationStatus::Blocked,
            adapter_contract_status: LocalRuntimeAdapterContractStatus::Blocked,
            adapter_kind: LocalRuntimeAdapterKind::LlamaCpp,
            selected_source_count,
            normalized_model_family: None,
            normalized_model_format: "unknown".to_string(),
            safe_executable_file_name: None,
            safe_model_file_name: None,
            diagnostic_prompt_char_count: normalized_prompt.chars().count(),
            max_output_tokens: 0,
            timeout_ms: 0,
            pipeline_gate_reasons,
            blockers,
            warnings,
            next_required_actions,
            summary: if prompt_is_blank {
                "The future runtime answer pipeline is blocked until the Scholar Chat prompt is nonblank.".to_string()
            } else {
                "The future runtime answer pipeline is blocked until at least one Scholar Chat source is selected.".to_string()
            },
            preview_only: true,
            gate_only: true,
            no_smoke_execution: true,
            no_runtime_inference: true,
            no_new_process_spawn: true,
            no_llm_call: true,
            no_answer_generated: true,
            no_answer_draft_created: true,
            no_grounded_answer_created: true,
            no_final_answer_created: true,
            no_grounding_applied: true,
            no_evidence_pack_built: true,
            no_persistence: true,
            no_artifact_write: true,
            no_registry_status_change: true,
            no_audit_write: true,
        });
    }

    let grounded_answer_execution_plan_preview = preview_scholar_chat_grounded_answer_execution_plan(
        &root,
        grounded_answer_execution_plan_preview_request,
    )?;
    let runtime_diagnostic_result_preview = preview_scholar_chat_runtime_diagnostic_result(
        &root,
        runtime_diagnostic_result_preview_request,
    )?;

    let mut status = if matches!(
        grounded_answer_execution_plan_preview.status,
        ScholarChatGroundedAnswerExecutionPlanStatus::Blocked
    ) || matches!(
        runtime_diagnostic_result_preview.status,
        ScholarChatRuntimeDiagnosticResultStatus::Blocked
    ) || matches!(
        runtime_diagnostic_result_preview.status,
        ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed
    ) {
        ScholarChatRuntimeAnswerPipelineGateStatus::Blocked
    } else if matches!(
        grounded_answer_execution_plan_preview.status,
        ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview
    ) || matches!(
        runtime_diagnostic_result_preview.status,
        ScholarChatRuntimeDiagnosticResultStatus::NeedsReview
    ) {
        ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview
    } else if matches!(
        grounded_answer_execution_plan_preview.status,
        ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater
    ) && matches!(
        runtime_diagnostic_result_preview.status,
        ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticSucceededLater
    ) && normalized_requests_match {
        ScholarChatRuntimeAnswerPipelineGateStatus::ReadyLater
    } else {
        ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview
    };

    let grounded_answer_execution_plan_status = grounded_answer_execution_plan_preview.status.clone();
    let grounded_answer_execution_readiness_status = grounded_answer_execution_plan_preview.readiness_status.clone();
    let runtime_diagnostic_result_status = runtime_diagnostic_result_preview.status.clone();
    let runtime_diagnostic_bridge_status = runtime_diagnostic_result_preview.bridge_status.clone();
    let smoke_diagnostic_status = runtime_diagnostic_result_preview.smoke_diagnostic_status.clone();
    let smoke_execution_plan_status = runtime_diagnostic_result_preview.smoke_execution_plan_status.clone();
    let smoke_readiness_status = runtime_diagnostic_result_preview.smoke_readiness_status.clone();
    let capability_status = runtime_diagnostic_result_preview.capability_status.clone();
    let version_probe_status = runtime_diagnostic_result_preview.version_probe_status.clone();
    let probe_readiness_status = runtime_diagnostic_result_preview.probe_readiness_status.clone();
    let validation_status = runtime_diagnostic_result_preview.validation_status.clone();
    let adapter_contract_status = runtime_diagnostic_result_preview.adapter_contract_status.clone();
    let adapter_kind = runtime_diagnostic_result_preview.adapter_kind.clone();
    let normalized_model_family = runtime_diagnostic_result_preview.normalized_model_family.clone();
    let normalized_model_format = runtime_diagnostic_result_preview.normalized_model_format.clone();
    let safe_executable_file_name = runtime_diagnostic_result_preview.safe_executable_file_name.clone();
    let safe_model_file_name = runtime_diagnostic_result_preview.safe_model_file_name.clone();
    let diagnostic_prompt_char_count = runtime_diagnostic_result_preview.diagnostic_prompt_char_count;
    let max_output_tokens = runtime_diagnostic_result_preview.max_output_tokens;
    let timeout_ms = runtime_diagnostic_result_preview.timeout_ms;

    let mut pipeline_gate_reasons = grounded_answer_execution_plan_preview.plan_reasons.clone();
    pipeline_gate_reasons.extend(runtime_diagnostic_result_preview.runtime_result_reasons.clone());
    let mut blockers = grounded_answer_execution_plan_preview
        .blockers
        .iter()
        .map(|blocker| format!("{}: {}", blocker.kind, blocker.message))
        .collect::<Vec<String>>();
    blockers.extend(
        runtime_diagnostic_result_preview
            .blockers
            .iter()
            .map(|blocker| format!("{}: {}", blocker.kind, blocker.message)),
    );
    let mut warnings = grounded_answer_execution_plan_preview
        .warnings
        .iter()
        .map(|warning| format!("{}: {}", warning.kind, warning.message))
        .collect::<Vec<String>>();
    warnings.extend(
        runtime_diagnostic_result_preview
            .warnings
            .iter()
            .map(|warning| format!("{}: {}", warning.kind, warning.message)),
    );
    let mut next_required_actions = grounded_answer_execution_plan_preview.next_required_actions.clone();
    next_required_actions.extend(runtime_diagnostic_result_preview.next_required_actions.clone());

    if !normalized_requests_match {
        push_unique_text(
            &mut blockers,
            "runtime_answer_pipeline_gate_metadata_mismatch: The grounded-answer execution plan preview and runtime diagnostic result preview do not use the same normalized Scholar Chat inputs.",
        );
        push_unique_text(
            &mut warnings,
            "runtime_answer_pipeline_gate_metadata_mismatch: The grounded-answer execution plan preview and runtime diagnostic result preview do not use the same normalized Scholar Chat inputs.",
        );
        push_unique_text(
            &mut pipeline_gate_reasons,
            "The grounded-answer execution plan preview and runtime diagnostic result preview do not use the same normalized Scholar Chat inputs.",
        );
        push_unique_text(
            &mut next_required_actions,
            "Make the grounded-answer execution plan preview and runtime diagnostic result preview inputs match before retrying the pipeline gate.",
        );
        if !matches!(status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked) {
            status = ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview;
        }
    }

    match status {
        ScholarChatRuntimeAnswerPipelineGateStatus::Blocked => {
            push_unique_text(
                &mut pipeline_gate_reasons,
                "The future runtime answer pipeline cannot proceed later because one or more dependencies are blocked or failed.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Resolve the blocked dependency preview before previewing the runtime answer pipeline gate again.",
            );
        }
        ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview => {
            push_unique_text(
                &mut pipeline_gate_reasons,
                "The future runtime answer pipeline still needs review before it can be allowed later.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Review the dependency preview warnings before trying the runtime answer pipeline gate again.",
            );
        }
        ScholarChatRuntimeAnswerPipelineGateStatus::ReadyLater => {
            push_unique_text(
                &mut pipeline_gate_reasons,
                "The future runtime answer pipeline gate is satisfied and no answer was generated.",
            );
            push_unique_text(
                &mut next_required_actions,
                "A future Scholar Chat runtime answer pipeline step can be added later without changing this preview.",
            );
        }
    }

    let summary = match status {
        ScholarChatRuntimeAnswerPipelineGateStatus::Blocked => {
            if grounded_answer_execution_plan_preview.status == ScholarChatGroundedAnswerExecutionPlanStatus::Blocked {
                "The future runtime answer pipeline is blocked because the grounded-answer execution plan is blocked.".to_string()
            } else if runtime_diagnostic_result_preview.status == ScholarChatRuntimeDiagnosticResultStatus::Blocked {
                "The future runtime answer pipeline is blocked because the runtime diagnostic result is blocked.".to_string()
            } else if runtime_diagnostic_result_preview.status == ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed {
                "The future runtime answer pipeline is blocked because the runtime diagnostic result failed.".to_string()
            } else {
                "The future runtime answer pipeline is blocked until the dependency previews are ready later.".to_string()
            }
        }
        ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview => {
            if !normalized_requests_match {
                "The future runtime answer pipeline needs review because the dependency previews do not use the same normalized Scholar Chat inputs.".to_string()
            } else if grounded_answer_execution_plan_preview.status == ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview {
                "The future runtime answer pipeline needs review because the grounded-answer execution plan still needs review.".to_string()
            } else if runtime_diagnostic_result_preview.status == ScholarChatRuntimeDiagnosticResultStatus::NeedsReview {
                "The future runtime answer pipeline needs review because the runtime diagnostic result still needs review.".to_string()
            } else {
                "The future runtime answer pipeline needs review before it can be allowed later.".to_string()
            }
        }
        ScholarChatRuntimeAnswerPipelineGateStatus::ReadyLater => {
            "The future runtime answer pipeline gate is ready later, but no answer was generated.".to_string()
        }
    };

    Ok(ScholarChatRuntimeAnswerPipelineGatePreview {
        status,
        grounded_answer_execution_plan_status,
        grounded_answer_execution_readiness_status,
        runtime_diagnostic_result_status,
        runtime_diagnostic_bridge_status,
        smoke_diagnostic_status,
        smoke_execution_plan_status,
        smoke_readiness_status,
        capability_status,
        version_probe_status,
        probe_readiness_status,
        validation_status,
        adapter_contract_status,
        adapter_kind,
        selected_source_count,
        normalized_model_family,
        normalized_model_format,
        safe_executable_file_name,
        safe_model_file_name,
        diagnostic_prompt_char_count,
        max_output_tokens,
        timeout_ms,
        pipeline_gate_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        gate_only: true,
        no_smoke_execution: true,
        no_runtime_inference: true,
        no_new_process_spawn: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_answer_draft_created: true,
        no_grounded_answer_created: true,
        no_final_answer_created: true,
        no_grounding_applied: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_artifact_write: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

fn runtime_diagnostic_result_metadata_mismatch_fields(
    bridge_preview: &ScholarChatRuntimeDiagnosticBridgePreview,
    diagnostic_preview: &LocalRuntimeSmokeDiagnosticPreview,
) -> Vec<&'static str> {
    let mut mismatches = Vec::new();
    if bridge_preview.smoke_execution_plan_status != diagnostic_preview.smoke_execution_plan_status {
        mismatches.push("smoke_execution_plan_status");
    }
    if bridge_preview.smoke_readiness_status != diagnostic_preview.smoke_readiness_status {
        mismatches.push("smoke_readiness_status");
    }
    if bridge_preview.capability_status != diagnostic_preview.capability_status {
        mismatches.push("capability_status");
    }
    if bridge_preview.version_probe_status != diagnostic_preview.version_probe_status {
        mismatches.push("version_probe_status");
    }
    if bridge_preview.probe_readiness_status != diagnostic_preview.probe_readiness_status {
        mismatches.push("probe_readiness_status");
    }
    if bridge_preview.validation_status != diagnostic_preview.validation_status {
        mismatches.push("validation_status");
    }
    if bridge_preview.adapter_contract_status != diagnostic_preview.adapter_contract_status {
        mismatches.push("adapter_contract_status");
    }
    if bridge_preview.adapter_kind != diagnostic_preview.adapter_kind {
        mismatches.push("adapter_kind");
    }
    if bridge_preview.normalized_model_family != diagnostic_preview.normalized_model_family {
        mismatches.push("normalized_model_family");
    }
    if bridge_preview.normalized_model_format != diagnostic_preview.normalized_model_format {
        mismatches.push("normalized_model_format");
    }
    if bridge_preview.safe_executable_file_name != diagnostic_preview.safe_executable_file_name {
        mismatches.push("safe_executable_file_name");
    }
    if bridge_preview.safe_model_file_name != diagnostic_preview.safe_model_file_name {
        mismatches.push("safe_model_file_name");
    }
    mismatches
}

pub fn preview_scholar_chat_runtime_diagnostic_result(
    root: impl Into<PathBuf>,
    request: ScholarChatRuntimeDiagnosticResultPreviewRequest,
) -> AegisResult<ScholarChatRuntimeDiagnosticResultPreview> {
    let bridge_preview = preview_scholar_chat_runtime_diagnostic_bridge(root, request.bridge_preview_request)?;
    let diagnostic_preview = request.diagnostic_preview;
    let mismatched_fields =
        runtime_diagnostic_result_metadata_mismatch_fields(&bridge_preview, &diagnostic_preview);

    let mut blockers = bridge_preview.blockers.clone();
    let mut warnings = bridge_preview.warnings.clone();
    let mut runtime_result_reasons = bridge_preview.runtime_diagnostic_reasons.clone();
    let mut next_required_actions = bridge_preview.next_required_actions.clone();

    for blocker in &diagnostic_preview.blockers {
        push_runtime_diagnostic_blocker(&mut blockers, &blocker.kind, &blocker.message);
    }
    for warning in &diagnostic_preview.warnings {
        push_runtime_diagnostic_warning(&mut warnings, &warning.kind, &warning.message);
    }

    let metadata_mismatch_message = if mismatched_fields.is_empty() {
        None
    } else {
        Some(format!(
            "Runtime diagnostic metadata mismatch between the bridge preview and the provided smoke diagnostic preview: {}.",
            mismatched_fields.join(", ")
        ))
    };
    let has_metadata_mismatch = metadata_mismatch_message.is_some();

    let status = if matches!(bridge_preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked) {
        ScholarChatRuntimeDiagnosticResultStatus::Blocked
    } else if matches!(bridge_preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview) {
        ScholarChatRuntimeDiagnosticResultStatus::NeedsReview
    } else if matches!(diagnostic_preview.status, LocalRuntimeSmokeDiagnosticStatus::Blocked) {
        ScholarChatRuntimeDiagnosticResultStatus::Blocked
    } else if metadata_mismatch_message.is_some() {
        ScholarChatRuntimeDiagnosticResultStatus::NeedsReview
    } else {
        match diagnostic_preview.status {
            LocalRuntimeSmokeDiagnosticStatus::SmokeFailed | LocalRuntimeSmokeDiagnosticStatus::TimedOut => {
                ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed
            }
            LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded => {
                ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticSucceededLater
            }
            LocalRuntimeSmokeDiagnosticStatus::Blocked => ScholarChatRuntimeDiagnosticResultStatus::Blocked,
        }
    };

    match bridge_preview.status {
        ScholarChatRuntimeDiagnosticBridgeStatus::Blocked => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The runtime diagnostic bridge preview is blocked.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Resolve the runtime diagnostic bridge preview blockers first.",
            );
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The runtime diagnostic bridge preview still needs review.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Resolve the runtime diagnostic bridge preview review items first.",
            );
        }
        ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The runtime diagnostic bridge preview is ready later for a smoke diagnostic result preview.",
            );
        }
    }

    match diagnostic_preview.status {
        LocalRuntimeSmokeDiagnosticStatus::Blocked => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The provided smoke diagnostic preview is blocked.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Provide a smoke diagnostic preview that is not blocked.",
            );
        }
        LocalRuntimeSmokeDiagnosticStatus::SmokeFailed => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The provided smoke diagnostic preview failed.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Review the failed smoke diagnostic preview before using it for Scholar Chat.",
            );
        }
        LocalRuntimeSmokeDiagnosticStatus::TimedOut => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The provided smoke diagnostic preview timed out.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Review the timed-out smoke diagnostic preview before using it for Scholar Chat.",
            );
        }
        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded => {
            push_unique_text(
                &mut runtime_result_reasons,
                "The provided smoke diagnostic preview succeeded.",
            );
            push_unique_text(
                &mut next_required_actions,
                "A future Scholar Chat runtime diagnostic step can reuse this result later.",
            );
        }
    }

    if let Some(message) = &metadata_mismatch_message {
        push_runtime_diagnostic_warning(
            &mut warnings,
            "runtime_diagnostic_metadata_mismatch",
            &message,
        );
        push_runtime_diagnostic_blocker(
            &mut blockers,
            "runtime_diagnostic_metadata_mismatch",
            &message,
        );
        push_unique_text(&mut runtime_result_reasons, &message);
        push_unique_text(
            &mut next_required_actions,
            "Make the bridge preview and smoke diagnostic preview metadata match before reusing this result.",
        );
    }

    let summary = match status {
        ScholarChatRuntimeDiagnosticResultStatus::Blocked => {
            if matches!(bridge_preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked) {
                "The runtime diagnostic result preview is blocked because the bridge preview is blocked.".to_string()
            } else if matches!(diagnostic_preview.status, LocalRuntimeSmokeDiagnosticStatus::Blocked) {
                "The runtime diagnostic result preview is blocked because the provided smoke diagnostic preview is blocked.".to_string()
            } else {
                "The runtime diagnostic result preview is blocked until the bridge preview and provided smoke diagnostic preview are ready later.".to_string()
            }
        }
        ScholarChatRuntimeDiagnosticResultStatus::NeedsReview => {
            if has_metadata_mismatch {
                "The runtime diagnostic result preview needs review because the bridge preview and provided smoke diagnostic preview do not match.".to_string()
            } else {
                "The runtime diagnostic result preview still needs review because the bridge preview still needs review.".to_string()
            }
        }
        ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed => {
            "The provided smoke diagnostic preview failed, so the runtime diagnostic result cannot be used yet.".to_string()
        }
        ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticSucceededLater => {
            "The provided smoke diagnostic preview succeeded, so the runtime diagnostic result is ready later for Scholar Chat.".to_string()
        }
    };

    Ok(ScholarChatRuntimeDiagnosticResultPreview {
        status,
        bridge_status: bridge_preview.status.clone(),
        smoke_diagnostic_status: diagnostic_preview.status,
        smoke_execution_plan_status: diagnostic_preview.smoke_execution_plan_status,
        smoke_readiness_status: diagnostic_preview.smoke_readiness_status,
        capability_status: diagnostic_preview.capability_status,
        version_probe_status: diagnostic_preview.version_probe_status,
        probe_readiness_status: diagnostic_preview.probe_readiness_status,
        validation_status: diagnostic_preview.validation_status,
        adapter_contract_status: diagnostic_preview.adapter_contract_status,
        adapter_kind: diagnostic_preview.adapter_kind,
        normalized_model_family: diagnostic_preview.normalized_model_family,
        normalized_model_format: diagnostic_preview.normalized_model_format,
        safe_executable_file_name: diagnostic_preview.safe_executable_file_name,
        safe_model_file_name: diagnostic_preview.safe_model_file_name,
        diagnostic_prompt_char_count: diagnostic_preview.diagnostic_prompt_char_count,
        max_output_tokens: diagnostic_preview.max_output_tokens,
        timeout_ms: diagnostic_preview.timeout_ms,
        exit_code: diagnostic_preview.exit_code,
        stdout_preview: diagnostic_preview.stdout_preview,
        stderr_preview: diagnostic_preview.stderr_preview,
        stdout_truncated: diagnostic_preview.stdout_truncated,
        stderr_truncated: diagnostic_preview.stderr_truncated,
        runtime_result_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        diagnostic_result_only: true,
        no_smoke_execution: true,
        no_runtime_inference: true,
        no_new_process_spawn: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_answer_draft_created: true,
        no_grounded_answer_created: true,
        no_final_answer_created: true,
        no_grounding_applied: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_artifact_write: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

fn grounded_answer_build_preflight_required_inputs() -> Vec<String> {
    vec![
        "build_request_ready_later".to_string(),
        "answer_draft_id".to_string(),
        "answer_draft_present".to_string(),
        "answer_draft_readable".to_string(),
    ]
}

fn grounded_answer_build_preflight_missing_inputs(
    build_request_preview: &ScholarChatGroundedAnswerBuildRequestPreview,
    answer_draft_id_present: bool,
    answer_draft_id: Option<&String>,
    answer_draft_present: bool,
    answer_draft_readable: bool,
    status: &ScholarChatGroundedAnswerBuildPreflightStatus,
) -> Vec<String> {
    let mut missing_inputs = Vec::new();
    if !matches!(
        build_request_preview.status,
        ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater
    ) {
        missing_inputs.push("build_request_ready_later".to_string());
    } else if !matches!(status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater) {
        if !answer_draft_id_present || answer_draft_id.is_none() {
            missing_inputs.push("answer_draft_id".to_string());
        } else if !answer_draft_present {
            missing_inputs.push("answer_draft_present".to_string());
            missing_inputs.push("answer_draft_readable".to_string());
        } else if !answer_draft_readable {
            missing_inputs.push("answer_draft_readable".to_string());
        }
    }
    missing_inputs
}

fn grounded_answer_build_preflight_summary(
    status: &ScholarChatGroundedAnswerBuildPreflightStatus,
    build_request_preview: &ScholarChatGroundedAnswerBuildRequestPreview,
    answer_draft_id_present: bool,
    answer_draft_present: bool,
    answer_draft_readable: bool,
    answer_draft_claim_count: usize,
) -> String {
    match status {
        ScholarChatGroundedAnswerBuildPreflightStatus::Blocked => {
            if !matches!(
                build_request_preview.status,
                ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater
            ) {
                format!(
                    "Grounded-answer build preflight is blocked because the build request is {:?}.",
                    build_request_preview.status
                )
            } else if !answer_draft_id_present {
                "Grounded-answer build preflight is blocked until an answer draft ID is provided.".to_string()
            } else if !answer_draft_present {
                "Grounded-answer build preflight is blocked until a matching AnswerDraft artifact is found."
                    .to_string()
            } else if !answer_draft_readable {
                "Grounded-answer build preflight is blocked because the referenced AnswerDraft artifact is unreadable."
                    .to_string()
            } else {
                "Grounded-answer build preflight is blocked until the request is ready later.".to_string()
            }
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview => {
            "Grounded-answer build preflight needs review because the build request still needs review."
                .to_string()
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater => format!(
            "The grounded-answer build preflight is ready later: the build request is ready later, the referenced AnswerDraft is readable, and it contains {} claim(s).",
            answer_draft_claim_count
        ),
    }
}

fn grounded_answer_build_preflight_reasons(
    build_request_preview: &ScholarChatGroundedAnswerBuildRequestPreview,
    answer_draft_id_present: bool,
    answer_draft_present: bool,
    answer_draft_readable: bool,
    answer_draft_claim_count: usize,
    status: &ScholarChatGroundedAnswerBuildPreflightStatus,
) -> Vec<String> {
    let mut reasons = vec![
        format!("Build request status: {:?}", build_request_preview.status),
        format!("Build intent status: {:?}", build_request_preview.build_intent_status),
        format!("Write eligibility status: {:?}", build_request_preview.write_eligibility_status),
        format!("Candidate status: {:?}", build_request_preview.candidate_status),
        format!("Answer draft ID present: {}", answer_draft_id_present),
        format!("Answer draft present: {}", answer_draft_present),
        format!("Answer draft readable: {}", answer_draft_readable),
        format!("Answer draft claim count: {}", answer_draft_claim_count),
    ];
    match status {
        ScholarChatGroundedAnswerBuildPreflightStatus::Blocked => {
            push_unique_text(
                &mut reasons,
                "The preflight is blocked until the build request is ready later and the referenced AnswerDraft artifact is readable.",
            );
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview => {
            push_unique_text(
                &mut reasons,
                "The build request still needs review before the AnswerDraft preflight can be accepted.",
            );
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater => {
            push_unique_text(
                &mut reasons,
                "The referenced AnswerDraft artifact is readable and the preflight is ready later.",
            );
        }
    }
    reasons
}

fn grounded_answer_build_preflight_next_required_actions(
    status: &ScholarChatGroundedAnswerBuildPreflightStatus,
    build_request_preview: &ScholarChatGroundedAnswerBuildRequestPreview,
    answer_draft_id_present: bool,
    answer_draft_present: bool,
    answer_draft_readable: bool,
) -> Vec<String> {
    let mut next_required_actions = Vec::new();
    match status {
        ScholarChatGroundedAnswerBuildPreflightStatus::Blocked => {
            if !matches!(
                build_request_preview.status,
                ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater
            ) {
                push_unique_text(
                    &mut next_required_actions,
                    "Bring the grounded-answer build request to request_ready_later first.",
                );
            } else if !answer_draft_id_present {
                push_unique_text(
                    &mut next_required_actions,
                    "Provide an answer draft ID before a future grounded-answer build request can proceed.",
                );
            } else if !answer_draft_present {
                push_unique_text(
                    &mut next_required_actions,
                    "Locate or create the matching AnswerDraft artifact before a future grounded-answer build request can proceed.",
                );
            } else if !answer_draft_readable {
                push_unique_text(
                    &mut next_required_actions,
                    "Create or repair the referenced AnswerDraft artifact before a future grounded-answer build request can proceed.",
                );
            }
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review weakly supported or unsupported items before checking preflight again.",
            );
        }
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A future GroundedAnswer service call can be added later without changing this preview.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_execution_readiness_required_inputs() -> Vec<String> {
    vec![
        "build_preflight_ready_later".to_string(),
        "answer_draft_readable".to_string(),
        "execution_consent".to_string(),
    ]
}

fn grounded_answer_execution_readiness_missing_inputs(
    build_preflight_status: &ScholarChatGroundedAnswerBuildPreflightStatus,
    answer_draft_readable: bool,
    execution_consent: bool,
) -> Vec<String> {
    let mut missing_inputs = Vec::new();
    if !matches!(
        build_preflight_status,
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
    ) {
        missing_inputs.push("build_preflight_ready_later".to_string());
    }
    if !answer_draft_readable {
        missing_inputs.push("answer_draft_readable".to_string());
    }
    if !execution_consent {
        missing_inputs.push("execution_consent".to_string());
    }
    missing_inputs
}

fn grounded_answer_execution_readiness_summary(
    status: &ScholarChatGroundedAnswerExecutionReadinessStatus,
    build_preflight_preview: &ScholarChatGroundedAnswerBuildPreflightPreview,
    execution_consent: bool,
) -> String {
    match status {
        ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked => {
            if !matches!(
                build_preflight_preview.status,
                ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
            ) {
                format!(
                    "Execution-readiness preview is blocked because the grounded-answer build preflight is {:?}.",
                    build_preflight_preview.status
                )
            } else if !build_preflight_preview.answer_draft_readable {
                "Execution-readiness preview is blocked because the referenced AnswerDraft artifact is unreadable."
                    .to_string()
            } else if !execution_consent {
                "Execution-readiness preview is blocked until execution consent is given.".to_string()
            } else {
                "Execution-readiness preview is blocked until the request is ready later.".to_string()
            }
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview => {
            "Execution-readiness preview needs review because the grounded-answer build preflight still needs review."
                .to_string()
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater => {
            "The execution-readiness preview is ready later: the build preflight is ready later, the referenced AnswerDraft is readable, and execution consent is true."
                .to_string()
        }
    }
}

fn grounded_answer_execution_readiness_reasons(
    build_preflight_preview: &ScholarChatGroundedAnswerBuildPreflightPreview,
    execution_consent: bool,
    status: &ScholarChatGroundedAnswerExecutionReadinessStatus,
) -> Vec<String> {
    let mut reasons = vec![
        format!("Build preflight status: {:?}", build_preflight_preview.status),
        format!("Build request status: {:?}", build_preflight_preview.build_request_status),
        format!("Build intent status: {:?}", build_preflight_preview.build_intent_status),
        format!("Write eligibility status: {:?}", build_preflight_preview.write_eligibility_status),
        format!("Candidate status: {:?}", build_preflight_preview.candidate_status),
        format!("Answer draft present: {}", build_preflight_preview.answer_draft_present),
        format!("Answer draft readable: {}", build_preflight_preview.answer_draft_readable),
        format!("Answer draft claim count: {}", build_preflight_preview.answer_draft_claim_count),
        format!("Execution consent: {}", execution_consent),
    ];
    match status {
        ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked => {
            push_unique_text(
                &mut reasons,
                "The execution-readiness preview is blocked until the build preflight is ready later, the referenced AnswerDraft is readable, and execution consent is given.",
            );
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview => {
            push_unique_text(
                &mut reasons,
                "The build preflight still needs review before execution readiness can be accepted.",
            );
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater => {
            push_unique_text(
                &mut reasons,
                "The referenced AnswerDraft is readable, execution consent is true, and the execution-readiness preview is ready later.",
            );
        }
    }
    reasons
}

fn grounded_answer_execution_readiness_next_required_actions(
    status: &ScholarChatGroundedAnswerExecutionReadinessStatus,
    build_preflight_preview: &ScholarChatGroundedAnswerBuildPreflightPreview,
    execution_consent: bool,
) -> Vec<String> {
    let mut next_required_actions = Vec::new();
    match status {
        ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked => {
            if !matches!(
                build_preflight_preview.status,
                ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
            ) {
                push_unique_text(
                    &mut next_required_actions,
                    "Bring the grounded-answer build preflight to preflight_ready_later first.",
                );
            }
            if !build_preflight_preview.answer_draft_readable {
                push_unique_text(
                    &mut next_required_actions,
                    "Create or repair the referenced AnswerDraft artifact before execution readiness can proceed.",
                );
            }
            if !execution_consent {
                push_unique_text(
                    &mut next_required_actions,
                    "Confirm execution consent before a future grounded-answer build can proceed.",
                );
            }
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review the grounded-answer build preflight before checking execution readiness again.",
            );
        }
        ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A future GroundedAnswer service call can be added later when execution is enabled.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_execution_plan_planned_inputs() -> Vec<String> {
    vec![
        "normalized_prompt".to_string(),
        "selected_source_ids".to_string(),
        "answer_draft_id".to_string(),
        "answer_draft_readable".to_string(),
        "execution_consent".to_string(),
    ]
}

fn grounded_answer_execution_plan_planned_outputs() -> Vec<String> {
    vec![
        "grounded_answer_execution_plan".to_string(),
        "future_grounded_answer_plan_metadata".to_string(),
        "future_grounded_answer_status".to_string(),
    ]
}

fn grounded_answer_execution_plan_planned_write_targets() -> Vec<String> {
    vec![
        "grounded_answer_artifact".to_string(),
        "registry_status_change".to_string(),
        "audit_log_entry".to_string(),
    ]
}

fn grounded_answer_execution_plan_required_inputs() -> Vec<String> {
    vec![
        "build_preflight_ready_later".to_string(),
        "answer_draft_readable".to_string(),
        "execution_consent".to_string(),
    ]
}

fn grounded_answer_execution_plan_missing_inputs(
    readiness_preview: &ScholarChatGroundedAnswerExecutionReadinessPreview,
) -> Vec<String> {
    let mut missing_inputs = Vec::new();
    if !matches!(
        readiness_preview.build_preflight_status,
        ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
    ) {
        missing_inputs.push("build_preflight_ready_later".to_string());
    }
    if !readiness_preview.answer_draft_readable {
        missing_inputs.push("answer_draft_readable".to_string());
    }
    if !readiness_preview.execution_consent {
        missing_inputs.push("execution_consent".to_string());
    }
    missing_inputs
}

fn grounded_answer_execution_plan_summary(
    status: &ScholarChatGroundedAnswerExecutionPlanStatus,
    readiness_preview: &ScholarChatGroundedAnswerExecutionReadinessPreview,
) -> String {
    match status {
        ScholarChatGroundedAnswerExecutionPlanStatus::Blocked => {
            if !matches!(
                readiness_preview.build_preflight_status,
                ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
            ) {
                "Execution plan preview is blocked until the grounded-answer execution readiness is ready later.".to_string()
            } else if !readiness_preview.execution_consent {
                "Execution plan preview is blocked until execution consent is given.".to_string()
            } else if !readiness_preview.answer_draft_readable {
                "Execution plan preview is blocked until the referenced AnswerDraft is readable.".to_string()
            } else {
                "Execution plan preview is blocked.".to_string()
            }
        }
        ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview => {
            "Execution plan preview needs review because the grounded-answer execution readiness still needs review."
                .to_string()
        }
        ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater => {
            "Execution plan preview is ready later: the grounded-answer execution readiness is ready later and execution consent is true."
                .to_string()
        }
    }
}

fn grounded_answer_execution_plan_reasons(
    readiness_preview: &ScholarChatGroundedAnswerExecutionReadinessPreview,
    status: &ScholarChatGroundedAnswerExecutionPlanStatus,
    planned_operation: &str,
    planned_inputs: &[String],
    planned_outputs: &[String],
    planned_write_targets: &[String],
) -> Vec<String> {
    let mut reasons = vec![
        format!("Plan status: {:?}", status),
        format!("Readiness status: {:?}", readiness_preview.status),
        format!("Build preflight status: {:?}", readiness_preview.build_preflight_status),
        format!("Build request status: {:?}", readiness_preview.build_request_status),
        format!("Build intent status: {:?}", readiness_preview.build_intent_status),
        format!("Write eligibility status: {:?}", readiness_preview.write_eligibility_status),
        format!("Candidate status: {:?}", readiness_preview.candidate_status),
        format!("Execution consent: {}", readiness_preview.execution_consent),
        format!("Planned operation: {planned_operation}"),
        format!("Planned inputs: {}", planned_inputs.join(", ")),
        format!("Planned outputs: {}", planned_outputs.join(", ")),
        format!("Planned write targets: {}", planned_write_targets.join(", ")),
        format!("Answer draft present: {}", readiness_preview.answer_draft_present),
        format!("Answer draft readable: {}", readiness_preview.answer_draft_readable),
        format!("Answer draft claim count: {}", readiness_preview.answer_draft_claim_count),
    ];
    reasons.extend(readiness_preview.readiness_reasons.iter().cloned());
    reasons
}

fn grounded_answer_execution_plan_next_required_actions(
    status: &ScholarChatGroundedAnswerExecutionPlanStatus,
    readiness_preview: &ScholarChatGroundedAnswerExecutionReadinessPreview,
) -> Vec<String> {
    let mut next_required_actions = Vec::new();
    match status {
        ScholarChatGroundedAnswerExecutionPlanStatus::Blocked => {
            if !matches!(
                readiness_preview.build_preflight_status,
                ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater
            ) {
                push_unique_text(
                    &mut next_required_actions,
                    "Bring the grounded-answer execution readiness to execution_ready_later first.",
                );
            }
            if !readiness_preview.answer_draft_readable {
                push_unique_text(
                    &mut next_required_actions,
                    "Create or repair the referenced AnswerDraft artifact before planning execution.",
                );
            }
            if !readiness_preview.execution_consent {
                push_unique_text(
                    &mut next_required_actions,
                    "Confirm execution consent before a future GroundedAnswer build can be planned.",
                );
            }
        }
        ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review the grounded-answer execution readiness before planning execution again.",
            );
        }
        ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A future GroundedAnswer build can be planned later when execution is enabled.",
            );
        }
    }
    next_required_actions
}

fn normalize_optional_draft_text(draft_text: Option<String>) -> Option<String> {
    draft_text
        .map(|text| text.trim().to_string())
        .and_then(|text| if text.is_empty() { None } else { Some(text) })
}

fn normalize_optional_answer_draft_id(answer_draft_id: Option<String>) -> AegisResult<Option<String>> {
    match answer_draft_id {
        None => Ok(None),
        Some(answer_draft_id) => {
            let normalized_answer_draft_id = answer_draft_id.trim().to_string();
            if normalized_answer_draft_id.is_empty() {
                Ok(None)
            } else {
                validate_answer_draft_id(&normalized_answer_draft_id)?;
                Ok(Some(normalized_answer_draft_id))
            }
        }
    }
}

fn grounded_answer_build_intent_status(
    write_eligibility_preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    explicit_user_intent: bool,
    answer_draft_id_present: bool,
) -> ScholarChatGroundedAnswerBuildIntentStatus {
    match write_eligibility_preview.status {
        ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => ScholarChatGroundedAnswerBuildIntentStatus::Blocked,
        ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
            ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview
        }
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
            if explicit_user_intent && answer_draft_id_present {
                ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater
            } else {
                ScholarChatGroundedAnswerBuildIntentStatus::Blocked
            }
        }
    }
}

fn grounded_answer_build_intent_required_inputs() -> Vec<String> {
    vec![
        "write_eligible_later".to_string(),
        "explicit_user_intent".to_string(),
        "answer_draft_id".to_string(),
    ]
}

fn grounded_answer_build_intent_missing_inputs(
    write_eligibility_preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    explicit_user_intent: bool,
    answer_draft_id_present: bool,
) -> Vec<String> {
    let mut missing_inputs = Vec::new();
    if !matches!(
        write_eligibility_preview.status,
        ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater
    ) {
        missing_inputs.push("write_eligible_later".to_string());
    }
    if !explicit_user_intent {
        missing_inputs.push("explicit_user_intent".to_string());
    }
    if !answer_draft_id_present {
        missing_inputs.push("answer_draft_id".to_string());
    }
    missing_inputs
}

fn grounded_answer_build_intent_summary(
    status: &ScholarChatGroundedAnswerBuildIntentStatus,
    write_eligibility_preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    explicit_user_intent: bool,
    answer_draft_id_present: bool,
) -> String {
    match status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => match write_eligibility_preview.status {
            ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => {
                "Grounded-answer build intent is blocked because grounded-answer write eligibility is blocked."
                    .to_string()
            }
            ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
                "Grounded-answer build intent is blocked because grounded-answer write eligibility still needs review."
                    .to_string()
            }
            ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
                if !explicit_user_intent && !answer_draft_id_present {
                    "Grounded-answer build intent is blocked until explicit user intent and an answer draft ID are provided."
                        .to_string()
                } else if !explicit_user_intent {
                    "Grounded-answer build intent is blocked until explicit user intent is provided."
                        .to_string()
                } else {
                    "Grounded-answer build intent is blocked until an answer draft ID is provided."
                        .to_string()
                }
            }
        },
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            "The draft is not yet ready for a grounded-answer build intent because weakly supported or unsupported items remain."
                .to_string()
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            "All inputs are present. A future user-confirmed GroundedAnswer build intent can be accepted later."
                .to_string()
        }
    }
}

fn grounded_answer_build_intent_reasons(
    status: &ScholarChatGroundedAnswerBuildIntentStatus,
    write_eligibility_preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    explicit_user_intent: bool,
    answer_draft_id_present: bool,
) -> Vec<String> {
    let mut reasons = vec![
        format!("Write eligibility status: {:?}", write_eligibility_preview.status),
        format!("Candidate status: {:?}", write_eligibility_preview.candidate_status),
        format!("Explicit user intent: {}", explicit_user_intent),
        format!("Answer draft ID provided: {}", answer_draft_id_present),
    ];
    match status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => match write_eligibility_preview.status {
            ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked => {
                reasons.push("Grounded-answer write eligibility is blocked.".to_string());
            }
            ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview => {
                reasons.push("Grounded-answer write eligibility still needs review.".to_string());
            }
            ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater => {
                if !explicit_user_intent && !answer_draft_id_present {
                    reasons.push("Explicit user intent and answer draft ID are both missing.".to_string());
                } else if !explicit_user_intent {
                    reasons.push("Explicit user intent is missing.".to_string());
                } else {
                    reasons.push("Answer draft ID is missing.".to_string());
                }
            }
        },
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            reasons.push("Weakly supported or unsupported draft items remain.".to_string());
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            reasons.push("All inspected items were supported by local evidence.".to_string());
            reasons.push(
                "A future user-confirmed GroundedAnswer build intent can be accepted later after an explicit implementation phase."
                    .to_string(),
            );
        }
    }
    reasons
}

fn grounded_answer_build_intent_next_required_actions(
    status: &ScholarChatGroundedAnswerBuildIntentStatus,
    write_eligibility_preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    explicit_user_intent: bool,
    answer_draft_id_present: bool,
) -> Vec<String> {
    let mut next_required_actions = write_eligibility_preview.next_required_actions.clone();
    match status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => {
            if !matches!(
                write_eligibility_preview.status,
                ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater
            ) {
                push_unique_text(
                    &mut next_required_actions,
                    "Resolve grounded-answer write-eligibility blockers before any GroundedAnswer service call.",
                );
            }
            if !explicit_user_intent {
                push_unique_text(
                    &mut next_required_actions,
                    "Provide explicit user intent before any GroundedAnswer service call.",
                );
            }
            if !answer_draft_id_present {
                push_unique_text(
                    &mut next_required_actions,
                    "Provide an answer draft ID before any GroundedAnswer service call.",
                );
            }
        }
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review weakly supported and unsupported draft items before any GroundedAnswer service call.",
            );
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A future user-confirmed GroundedAnswer service call can be added later without changing this preview.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_build_intent_preview_from_write_eligibility_preview(
    write_eligibility_preview: ScholarChatGroundedAnswerWriteEligibilityPreview,
    normalized_prompt: String,
    normalized_answer_draft_id: Option<String>,
    explicit_user_intent: bool,
) -> ScholarChatGroundedAnswerBuildIntentPreview {
    let answer_draft_id_present = normalized_answer_draft_id.is_some();
    let status = grounded_answer_build_intent_status(
        &write_eligibility_preview,
        explicit_user_intent,
        answer_draft_id_present,
    );
    let mut blockers = write_eligibility_preview.blockers.clone();
    let mut warnings = write_eligibility_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer build-intent preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    match status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => {
            if !matches!(
                write_eligibility_preview.status,
                ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater
            ) {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "write_eligibility_blocked",
                    "Grounded-answer build intent is blocked until grounded-answer write eligibility is available.",
                );
            }
            if !explicit_user_intent {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "explicit_user_intent_missing",
                    "Explicit user intent was not provided.",
                );
            }
            if !answer_draft_id_present {
                push_grounding_inspection_blocker(
                    &mut blockers,
                    "answer_draft_id_missing",
                    "No answer draft ID was provided.",
                );
            }
        }
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "needs_review",
                "Weakly supported or unsupported draft items remain and need review before a future GroundedAnswer build intent is added.",
            );
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "intent_ready_later",
                "All inputs are present. This is still only a grounded-answer build-intent preview.",
            );
        }
    }

    let summary = grounded_answer_build_intent_summary(
        &status,
        &write_eligibility_preview,
        explicit_user_intent,
        answer_draft_id_present,
    );
    let required_inputs = grounded_answer_build_intent_required_inputs();
    let missing_inputs = grounded_answer_build_intent_missing_inputs(
        &write_eligibility_preview,
        explicit_user_intent,
        answer_draft_id_present,
    );
    let intent_reasons = grounded_answer_build_intent_reasons(
        &status,
        &write_eligibility_preview,
        explicit_user_intent,
        answer_draft_id_present,
    );
    let next_required_actions = grounded_answer_build_intent_next_required_actions(
        &status,
        &write_eligibility_preview,
        explicit_user_intent,
        answer_draft_id_present,
    );

    ScholarChatGroundedAnswerBuildIntentPreview {
        status,
        write_eligibility_status: write_eligibility_preview.status,
        candidate_status: write_eligibility_preview.candidate_status,
        normalized_prompt,
        selected_source_count: write_eligibility_preview.selected_source_count,
        evidence_candidate_count: write_eligibility_preview.evidence_candidate_count,
        inspected_item_count: write_eligibility_preview.inspected_item_count,
        supported_item_count: write_eligibility_preview.supported_item_count,
        weakly_supported_item_count: write_eligibility_preview.weakly_supported_item_count,
        unsupported_item_count: write_eligibility_preview.unsupported_item_count,
        candidate_statement_count: write_eligibility_preview.candidate_statement_count,
        required_inputs,
        missing_inputs,
        intent_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
        no_grounded_answer_service_call: true,
    }
}

fn grounded_answer_build_request_status(
    build_intent_preview: &ScholarChatGroundedAnswerBuildIntentPreview,
) -> ScholarChatGroundedAnswerBuildRequestStatus {
    match build_intent_preview.status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => ScholarChatGroundedAnswerBuildRequestStatus::Blocked,
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater
        }
    }
}

fn grounded_answer_build_request_required_inputs() -> Vec<String> {
    vec![
        "build_intent_ready_later".to_string(),
        "answer_draft_id".to_string(),
        "selected_source_ids".to_string(),
    ]
}

fn grounded_answer_build_request_missing_inputs(
    build_intent_preview: &ScholarChatGroundedAnswerBuildIntentPreview,
    answer_draft_id: &Option<String>,
    selected_source_ids: &[String],
) -> Vec<String> {
    let mut missing_inputs = Vec::new();
    if !matches!(
        build_intent_preview.status,
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater
    ) {
        missing_inputs.push("build_intent_ready_later".to_string());
    }
    if answer_draft_id.is_none() {
        missing_inputs.push("answer_draft_id".to_string());
    }
    if selected_source_ids.is_empty() {
        missing_inputs.push("selected_source_ids".to_string());
    }
    missing_inputs
}

fn grounded_answer_build_request_summary(
    status: &ScholarChatGroundedAnswerBuildRequestStatus,
    build_intent_preview: &ScholarChatGroundedAnswerBuildIntentPreview,
    answer_draft_id: &Option<String>,
    selected_source_ids: &[String],
) -> String {
    let answer_draft_id_summary = if answer_draft_id.is_some() { "present" } else { "missing" };
    let selected_source_summary = if selected_source_ids.is_empty() {
        "no selected source IDs"
    } else {
        "selected source IDs are normalized and ready"
    };
    match status {
        ScholarChatGroundedAnswerBuildRequestStatus::Blocked => format!(
            "Grounded-answer build request is blocked because the build intent is {:?}; answer draft ID is {}; {}.",
            build_intent_preview.status,
            answer_draft_id_summary,
            selected_source_summary,
        ),
        ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview => format!(
            "Grounded-answer build request still needs review because the build intent is {:?}; answer draft ID is {}; {}.",
            build_intent_preview.status,
            answer_draft_id_summary,
            selected_source_summary,
        ),
        ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater => {
            "The grounded-answer build request is normalized and ready later for a future GroundedAnswer service call."
                .to_string()
        }
    }
}

fn grounded_answer_build_request_reasons(
    build_intent_preview: &ScholarChatGroundedAnswerBuildIntentPreview,
    answer_draft_id: &Option<String>,
    selected_source_ids: &[String],
) -> Vec<String> {
    let mut request_reasons = vec![
        format!("Build intent status: {:?}", build_intent_preview.status),
        format!("Write eligibility status: {:?}", build_intent_preview.write_eligibility_status),
        format!("Candidate status: {:?}", build_intent_preview.candidate_status),
        format!("Answer draft ID provided: {}", answer_draft_id.is_some()),
        format!("Selected source count: {}", selected_source_ids.len()),
    ];
    match build_intent_preview.status {
        ScholarChatGroundedAnswerBuildIntentStatus::Blocked => {
            request_reasons.push("Grounded-answer build intent must be ready later before a request can be accepted.".to_string());
        }
        ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview => {
            request_reasons.push(
                "Weakly supported or unsupported draft items remain and need review before a future GroundedAnswer service call."
                    .to_string(),
            );
        }
        ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater => {
            request_reasons.push(
                "The request fields are normalized and ready later for a future GroundedAnswer service call.".to_string(),
            );
        }
    }
    request_reasons
}

fn grounded_answer_build_request_next_required_actions(
    status: &ScholarChatGroundedAnswerBuildRequestStatus,
    build_intent_preview: &ScholarChatGroundedAnswerBuildIntentPreview,
    answer_draft_id: &Option<String>,
    selected_source_ids: &[String],
) -> Vec<String> {
    let mut next_required_actions = build_intent_preview.next_required_actions.clone();
    match status {
        ScholarChatGroundedAnswerBuildRequestStatus::Blocked => {
            if !matches!(
                build_intent_preview.status,
                ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater
            ) {
                push_unique_text(
                    &mut next_required_actions,
                    "Resolve grounded-answer build-intent blockers before any GroundedAnswer service call.",
                );
            }
            if answer_draft_id.is_none() {
                push_unique_text(
                    &mut next_required_actions,
                    "Provide an answer draft ID before any GroundedAnswer service call.",
                );
            }
            if selected_source_ids.is_empty() {
                push_unique_text(
                    &mut next_required_actions,
                    "Select at least one source ID before any GroundedAnswer service call.",
                );
            }
        }
        ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview => {
            push_unique_text(
                &mut next_required_actions,
                "Review the normalized request fields before any GroundedAnswer service call.",
            );
        }
        ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater => {
            push_unique_text(
                &mut next_required_actions,
                "A future user-confirmed GroundedAnswer service call can be added later using the normalized request fields.",
            );
        }
    }
    next_required_actions
}

fn grounded_answer_build_request_preview_from_build_intent_preview(
    build_intent_preview: ScholarChatGroundedAnswerBuildIntentPreview,
    normalized_prompt: String,
    normalized_answer_draft_id: Option<String>,
    normalized_selected_source_ids: Vec<String>,
) -> ScholarChatGroundedAnswerBuildRequestPreview {
    let status = grounded_answer_build_request_status(&build_intent_preview);
    let mut blockers = build_intent_preview.blockers.clone();
    let mut warnings = build_intent_preview.warnings.clone();

    push_grounding_inspection_warning(
        &mut warnings,
        "boundary",
        "This is a grounded-answer build-request preview only; it is not an AnswerDraft, GroundedAnswer, FinalAnswer, Evidence Pack, or persisted artifact.",
    );

    match status {
        ScholarChatGroundedAnswerBuildRequestStatus::Blocked => {
            push_grounding_inspection_blocker(
                &mut blockers,
                "build_request_blocked",
                "Grounded-answer build request is blocked until the build intent is ready later.",
            );
        }
        ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview => {
            push_grounding_inspection_warning(
                &mut warnings,
                "request_needs_review",
                "The normalized grounded-answer build request still needs review before any GroundedAnswer service call.",
            );
        }
        ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater => {
            push_grounding_inspection_warning(
                &mut warnings,
                "request_ready_later",
                "The normalized grounded-answer build request is ready later for a future GroundedAnswer service call.",
            );
        }
    }

    let answer_draft_id = normalized_answer_draft_id;
    let selected_source_count = normalized_selected_source_ids.len();
    let required_inputs = grounded_answer_build_request_required_inputs();
    let missing_inputs = grounded_answer_build_request_missing_inputs(
        &build_intent_preview,
        &answer_draft_id,
        &normalized_selected_source_ids,
    );
    let request_reasons = grounded_answer_build_request_reasons(
        &build_intent_preview,
        &answer_draft_id,
        &normalized_selected_source_ids,
    );
    let next_required_actions = grounded_answer_build_request_next_required_actions(
        &status,
        &build_intent_preview,
        &answer_draft_id,
        &normalized_selected_source_ids,
    );
    let summary = grounded_answer_build_request_summary(
        &status,
        &build_intent_preview,
        &answer_draft_id,
        &normalized_selected_source_ids,
    );

    ScholarChatGroundedAnswerBuildRequestPreview {
        status,
        build_intent_status: build_intent_preview.status,
        write_eligibility_status: build_intent_preview.write_eligibility_status,
        candidate_status: build_intent_preview.candidate_status,
        normalized_prompt,
        selected_source_count,
        evidence_candidate_count: build_intent_preview.evidence_candidate_count,
        inspected_item_count: build_intent_preview.inspected_item_count,
        supported_item_count: build_intent_preview.supported_item_count,
        weakly_supported_item_count: build_intent_preview.weakly_supported_item_count,
        unsupported_item_count: build_intent_preview.unsupported_item_count,
        candidate_statement_count: build_intent_preview.candidate_statement_count,
        answer_draft_id,
        selected_source_ids: normalized_selected_source_ids,
        required_inputs,
        missing_inputs,
        request_reasons,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        not_answer_draft: true,
        not_grounded_answer: true,
        not_final_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        no_llm_call: true,
        no_runtime_execution: true,
        no_registry_status_change: true,
        no_audit_write: true,
        no_grounded_answer_service_call: true,
    }
}

fn push_grounding_inspection_warning(
    warnings: &mut Vec<ScholarChatDraftGroundingInspectionWarning>,
    kind: &str,
    message: &str,
) {
    if !warnings.iter().any(|warning| warning.kind == kind && warning.message == message) {
        warnings.push(ScholarChatDraftGroundingInspectionWarning {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_unique_text(items: &mut Vec<String>, value: &str) {
    if !items.iter().any(|item| item == value) {
        items.push(value.to_string());
    }
}

fn push_runtime_diagnostic_warning(
    warnings: &mut Vec<LocalRuntimeProbeWarning>,
    kind: &str,
    message: &str,
) {
    if !warnings.iter().any(|warning| warning.kind == kind && warning.message == message) {
        warnings.push(LocalRuntimeProbeWarning {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_runtime_diagnostic_blocker(
    blockers: &mut Vec<LocalRuntimeProbeWarning>,
    kind: &str,
    message: &str,
) {
    if !blockers.iter().any(|blocker| blocker.kind == kind && blocker.message == message) {
        blockers.push(LocalRuntimeProbeWarning {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_grounding_inspection_blocker(
    blockers: &mut Vec<ScholarChatDraftGroundingInspectionBlocker>,
    kind: &str,
    message: &str,
) {
    if !blockers.iter().any(|blocker| blocker.kind == kind && blocker.message == message) {
        blockers.push(ScholarChatDraftGroundingInspectionBlocker {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn inspect_draft_grounding_items(
    draft_text: &str,
    evidence_candidates: &[ScholarChatEvidenceCandidate],
    max_items: usize,
) -> DraftGroundingInspectionItems {
    let mut items = Vec::new();
    let mut supported_item_count = 0;
    let mut weakly_supported_item_count = 0;
    let mut unsupported_item_count = 0;
    let warnings = Vec::new();

    let segments = split_draft_text_into_segments(draft_text);
    let was_clamped = segments.len() > max_items;
    for (item_index, segment) in segments.into_iter().take(max_items).enumerate() {
        let normalized_segment = compact_text_preview(&segment, 180);
        let item_terms = inspection_terms(&normalized_segment);
        if item_terms.is_empty() {
            continue;
        }

        let mut matched_evidence = Vec::new();
        for candidate in evidence_candidates {
            let candidate_terms = inspection_terms(&candidate.preview)
                .into_iter()
                .chain(candidate.matched_terms.iter().flat_map(|term| inspection_terms(term).into_iter()))
                .collect::<BTreeSet<_>>();
            let overlap = item_terms.intersection(&candidate_terms).count();
            if overlap > 0 {
                matched_evidence.push((overlap, candidate));
            }
        }

        let support_status = if matched_evidence.is_empty() {
            unsupported_item_count += 1;
            ScholarChatDraftGroundingSupportStatus::Unsupported
        } else {
            let best_overlap = matched_evidence.iter().map(|(overlap, _)| *overlap).max().unwrap_or(0);
            if best_overlap >= 2 {
                supported_item_count += 1;
                ScholarChatDraftGroundingSupportStatus::SupportedByLocalEvidence
            } else {
                weakly_supported_item_count += 1;
                ScholarChatDraftGroundingSupportStatus::WeaklySupported
            }
        };

        let mut source_ids = BTreeSet::new();
        let mut locator_previews = BTreeSet::new();
        let matched_evidence_count = matched_evidence.len();
        for (_, candidate) in matched_evidence {
            source_ids.insert(candidate.source_id.clone());
            locator_previews.insert(locator_preview(&candidate.locator));
        }

        items.push(ScholarChatDraftGroundingInspectionItem {
            item_index,
            text_preview: normalized_segment,
            support_status,
            matched_evidence_count,
            source_ids: source_ids.into_iter().collect(),
            locator_previews: locator_previews.into_iter().collect(),
        });
    }

    DraftGroundingInspectionItems {
        items,
        supported_item_count,
        weakly_supported_item_count,
        unsupported_item_count,
        warnings,
        was_clamped,
    }
}

fn split_draft_text_into_segments(draft_text: &str) -> Vec<String> {
    let mut segments = Vec::new();
    let mut current = String::new();

    for ch in draft_text.chars() {
        if matches!(ch, '.' | '!' | '?' | '\n' | '\r') {
            let trimmed = current.trim();
            if !trimmed.is_empty() {
                segments.push(trimmed.to_string());
            }
            current.clear();
        } else {
            current.push(ch);
        }
    }

    let trimmed = current.trim();
    if !trimmed.is_empty() {
        segments.push(trimmed.to_string());
    }

    if segments.is_empty() && !draft_text.trim().is_empty() {
        segments.push(draft_text.trim().to_string());
    }

    segments
}

fn compact_text_preview(text: &str, max_chars: usize) -> String {
    let compacted = text.split_whitespace().collect::<Vec<_>>().join(" ");
    let mut preview = compacted.trim().to_string();
    if preview.chars().count() > max_chars {
        let mut truncated = String::new();
        for ch in preview.chars().take(max_chars.saturating_sub(1)) {
            truncated.push(ch);
        }
        truncated.push('…');
        preview = truncated;
    }
    preview
}

fn inspection_terms(text: &str) -> BTreeSet<String> {
    const MIN_MEANINGFUL_TERM_LEN: usize = 3;

    text.split(|ch: char| !ch.is_alphanumeric())
        .filter_map(|term| {
            let normalized = term.trim().to_lowercase();
            if normalized.is_empty() {
                None
            } else if is_stopword_for_draft_grounding_inspection(&normalized) {
                None
            } else if normalized.chars().all(|ch| ch.is_numeric()) {
                Some(normalized)
            } else if normalized.chars().count() < MIN_MEANINGFUL_TERM_LEN {
                None
            } else {
                Some(normalized)
            }
        })
        .collect()
}

fn is_stopword_for_draft_grounding_inspection(term: &str) -> bool {
    matches!(
        term,
        "the" | "and" | "or" | "of" | "to" | "in" | "a" | "an" | "is" | "are" | "was" | "were" | "with" | "for" | "on" | "by" | "as" | "at" | "from" | "this" | "that"
    )
}

fn locator_preview(locator: &CitationLocator) -> String {
    let mut parts = vec![format!("{:?}", locator.locator_type).to_lowercase()];
    if !locator.label.trim().is_empty() {
        parts.push(format!("label={}", locator.label.trim()));
    }
    if let Some(page) = locator.page {
        parts.push(format!("page={page}"));
    }
    if let Some(slide) = locator.slide {
        parts.push(format!("slide={slide}"));
    }
    if let Some(start) = locator.character_start {
        parts.push(format!("chars={start}-{}", locator.character_end.unwrap_or(start)));
    }
    parts.join(" | ")
}

fn render_prompt_pack_for_runtime(prompt_pack: &ScholarChatPromptPack) -> String {
    let mut lines = Vec::new();
    for section in &prompt_pack.sections {
        lines.push(format!("## {}", section.title));
        lines.extend(section.lines.iter().cloned());
        lines.push(String::new());
    }
    lines.join("\n").trim().to_string()
}

fn draft_output_classification(
    grounding_policy: &GroundingPolicy,
    blocked: bool,
) -> ScholarChatDraftOutputClassification {
    if blocked {
        return ScholarChatDraftOutputClassification::Blocked;
    }
    match grounding_policy {
        GroundingPolicy::AllowMarkedModelKnowledge => ScholarChatDraftOutputClassification::UngroundedModelDraft,
        GroundingPolicy::LocalOnly => ScholarChatDraftOutputClassification::GroundedDraftCandidate,
        GroundingPolicy::LocalFirst | GroundingPolicy::ExternalAdaptersLater => {
            ScholarChatDraftOutputClassification::SourceContextDraft
        }
    }
}

fn push_draft_warning(
    warnings: &mut Vec<ScholarChatDraftInferenceWarning>,
    kind: &str,
    message: &str,
) {
    if !warnings.iter().any(|warning| warning.kind == kind && warning.message == message) {
        warnings.push(ScholarChatDraftInferenceWarning {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_draft_blocker(
    blockers: &mut Vec<ScholarChatDraftInferenceBlocker>,
    kind: &str,
    message: &str,
) {
    if !blockers.iter().any(|blocker| blocker.kind == kind && blocker.message == message) {
        blockers.push(ScholarChatDraftInferenceBlocker {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

#[allow(clippy::too_many_arguments)]
fn build_draft_inference_preview(
    normalized_prompt: String,
    mode: ScholarChatMode,
    grounding_policy: GroundingPolicy,
    selected_source_count: usize,
    retrieval_candidate_count: usize,
    evidence_candidate_count: usize,
    prompt_pack_section_count: usize,
    prompt_pack_text: String,
    runtime_health_status: LocalModelRuntimeHealthStatus,
    invocation_plan_status: LocalRuntimeInvocationPlanStatus,
    status: ScholarChatDraftInferenceStatus,
    output_classification: ScholarChatDraftOutputClassification,
    allow_model_execution: bool,
    execution_attempted: bool,
    safe_model_file_name: Option<String>,
    safe_executable_file_name: Option<String>,
    stdout_preview: String,
    stderr_preview: String,
    duration_ms: u64,
    exit_code: Option<i32>,
    blockers: Vec<ScholarChatDraftInferenceBlocker>,
    warnings: Vec<ScholarChatDraftInferenceWarning>,
) -> ScholarChatDraftInferencePreview {
    ScholarChatDraftInferencePreview {
        status,
        output_classification,
        normalized_prompt,
        mode,
        grounding_policy,
        selected_source_count,
        retrieval_candidate_count,
        evidence_candidate_count,
        prompt_pack_section_count,
        prompt_char_count: prompt_pack_text.chars().count(),
        runtime_health_status,
        invocation_plan_status,
        allow_model_execution,
        execution_attempted,
        safe_model_file_name,
        safe_executable_file_name,
        stdout_preview,
        stderr_preview,
        duration_ms,
        exit_code,
        draft_only: true,
        preview_only: true,
        not_final_answer: true,
        not_grounded_answer: true,
        no_answer_artifact_created: true,
        no_evidence_pack_built: true,
        no_persistence: true,
        blockers,
        warnings,
    }
}

fn normalized_prompt_or_err(prompt: String) -> AegisResult<String> {
    let normalized_prompt = prompt.trim().to_string();
    if normalized_prompt.is_empty() {
        return Err(AegisError::ScholarChatPromptEmpty);
    }
    Ok(normalized_prompt)
}

fn normalize_selected_source_ids(source_ids: Vec<String>) -> AegisResult<(Vec<String>, usize)> {
    let mut selected_source_ids = Vec::new();
    for source_id in source_ids {
        let normalized_source_id = source_id.trim().to_string();
        validate_source_id(&normalized_source_id)?;
        selected_source_ids.push(normalized_source_id);
    }
    let selected_source_count = selected_source_ids.len();
    Ok((selected_source_ids, selected_source_count))
}

fn normalize_scientific_context_tags(tags: Option<Vec<String>>) -> Vec<String> {
    let mut normalized_tags = BTreeSet::new();
    if let Some(tags) = tags {
        for tag in tags {
            let normalized = normalize_scientific_tag_text(&tag);
            if !normalized.is_empty() {
                normalized_tags.insert(normalized);
            }
        }
    }
    normalized_tags.into_iter().collect()
}

fn normalize_scientific_tag_text(value: &str) -> String {
    let mut normalized = String::new();
    let mut last_was_separator = false;
    for ch in value.trim().to_lowercase().chars() {
        if ch.is_alphanumeric() {
            normalized.push(ch);
            last_was_separator = false;
        } else if matches!(ch, ' ' | '-') && !normalized.is_empty() && !last_was_separator {
            normalized.push('_');
            last_was_separator = true;
        } else if matches!(ch, ' ' | '-') && normalized.is_empty() {
            continue;
        } else if !normalized.is_empty() && !last_was_separator {
            normalized.push('_');
            last_was_separator = true;
        }
    }
    normalized.trim_matches('_').to_string()
}

fn scientific_context_mentions(context: &Option<String>, needles: &[&str]) -> bool {
    let Some(context) = context.as_deref() else {
        return false;
    };
    let context = context.to_lowercase();
    needles.iter().any(|needle| context.contains(needle))
}

fn scientific_context_tags_contain(tags: &[String], needles: &[&str]) -> bool {
    tags.iter().any(|tag| needles.iter().any(|needle| tag == needle || tag.contains(needle)))
}

fn scientific_source_registry_family(
    id: &str,
    label: &str,
    domain: &str,
    access_class: ScholarChatScientificSourceAccessClass,
    priority: ScholarChatScientificSourcePriority,
    applies_when: &str,
    active_for_current_context: bool,
    planned_use: &str,
    query_roles: &[&str],
    boundary_notes: &[&str],
) -> ScholarChatScientificSourceRegistrySourceFamily {
    ScholarChatScientificSourceRegistrySourceFamily {
        id: id.to_string(),
        label: label.to_string(),
        domain: domain.to_string(),
        access_class,
        priority,
        applies_when: applies_when.to_string(),
        active_for_current_context,
        planned_use: planned_use.to_string(),
        query_roles: query_roles.iter().map(|value| (*value).to_string()).collect(),
        boundary_notes: boundary_notes.iter().map(|value| (*value).to_string()).collect(),
    }
}

fn scientific_source_registry_access_classes(
    source_families: &[ScholarChatScientificSourceRegistrySourceFamily],
) -> Vec<ScholarChatScientificSourceAccessClass> {
    let mut access_classes = Vec::new();
    for family in source_families {
        if !access_classes.contains(&family.access_class) {
            access_classes.push(family.access_class.clone());
        }
    }
    access_classes
}

fn scientific_source_registry_plan_summary(status: &ScholarChatScientificSourceRegistryStatus, label: Option<&str>, normalized_mode: &str) -> String {
    match status {
        ScholarChatScientificSourceRegistryStatus::Blocked => {
            "Scientific source registry preview blocked because the topic is blank.".to_string()
        }
        ScholarChatScientificSourceRegistryStatus::UnknownConcept => {
            format!(
                "Scientific source registry preview could not yet map the topic '{}' in {} mode.",
                label.unwrap_or("unknown topic"),
                normalized_mode
            )
        }
        ScholarChatScientificSourceRegistryStatus::SourcePlanReady => {
            format!(
                "Scientific source registry preview is ready later for {} in {} mode.",
                label.unwrap_or("the mapped concept"),
                normalized_mode
            )
        }
    }
}

fn scientific_source_registry_plan_steps(normalized_mode: &str) -> Vec<String> {
    let mut steps = vec![
        "Normalize topic, mode, and context tags.".to_string(),
        "Map the topic through the scientific discipline registry preview.".to_string(),
        "Select active source families for the current context.".to_string(),
        "Plan metadata-only queries before later retrieval or indexing.".to_string(),
    ];
    if normalized_mode == "scientific_paper" {
        steps.push("Prefer literature-search planning, deduplication, and citation-safe follow-up phases.".to_string());
    } else if normalized_mode == "course" {
        steps.push("Prefer curriculum metadata, module context, prerequisites, and learning-path support in later phases.".to_string());
    } else {
        steps.push("Plan local evidence first before later Scholar Chat answering.".to_string());
    }
    steps
}

fn scientific_source_registry_ranking_hints(
    recognized_concept: Option<&str>,
    normalized_mode: &str,
    normalized_context_tags: &[String],
    course_context: &Option<String>,
) -> Vec<String> {
    let mut hints = Vec::new();
    match recognized_concept {
        Some("signal_detection_theory") => {
            hints.push("Prefer psychology and psychophysics sources first.".to_string());
            hints.push("Use biomedical sources only when biomedical context is explicit.".to_string());
            hints.push("Prefer method and review sources later where available.".to_string());
        }
        Some("analysis_of_variance") => {
            hints.push("Prefer statistics and methods sources first.".to_string());
            hints.push("Activate psychology-specific sources only when psychology context is explicit.".to_string());
            hints.push("Activate theory sources only when theory or statistics context is explicit.".to_string());
        }
        Some("hypothesis_testing") => {
            hints.push("Prefer methods and statistics sources first.".to_string());
            hints.push("Use psychology sources when applied psychology context is explicit.".to_string());
            hints.push("Later phases should deduplicate by DOI, title, and source identifiers.".to_string());
        }
        _ => {}
    }
    if normalized_mode == "course" {
        hints.push("Course Mode should favor curriculum metadata and local course materials later.".to_string());
    }
    if scientific_context_tags_contain(normalized_context_tags, &["biomedical", "medical", "neuroscience", "clinical", "diagnostics", "medicine"]) {
        hints.push("Biomedical context can activate biomedical source families later.".to_string());
    }
    if scientific_context_mentions(course_context, &["psychology", "psychologie"]) {
        hints.push("Course context can activate psychology-specific source families later.".to_string());
    }
    hints
}

fn scientific_source_registry_deduplication_hints(
    recognized_concept: Option<&str>,
    normalized_mode: &str,
) -> Vec<String> {
    let mut hints = vec![
        "Deduplicate later by DOI, title, and source identifiers.".to_string(),
        "Keep source-family provenance when merging overlapping metadata records.".to_string(),
    ];
    if recognized_concept == Some("signal_detection_theory") {
        hints.push("Deduplicate psychophysics records against broader psychology metadata later.".to_string());
    }
    if normalized_mode == "scientific_paper" {
        hints.push("Deduplicate review and preprint records before evidence-pack planning later.".to_string());
    }
    hints
}

#[derive(Clone, Copy)]
struct ScientificQueryAliasSpec {
    alias: &'static str,
    concept: &'static str,
    topic_label: &'static str,
    language: &'static str,
}

#[derive(Clone, Copy)]
struct ScientificQueryConceptCandidate {
    concept: &'static str,
    topic_label: &'static str,
    start_index: usize,
}

const SCIENTIFIC_QUERY_ALIAS_SPECS: &[ScientificQueryAliasSpec] = &[
    ScientificQueryAliasSpec {
        alias: "Signalentdeckung",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "Signalentdeckungstheorie",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "signal detection",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "signal detection theory",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "d prime",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "d-prime",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "dprime",
        concept: "signal_detection_theory",
        topic_label: "Signalentdeckung",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "ANOVA",
        concept: "analysis_of_variance",
        topic_label: "ANOVA",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "Varianzanalyse",
        concept: "analysis_of_variance",
        topic_label: "ANOVA",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "analysis of variance",
        concept: "analysis_of_variance",
        topic_label: "ANOVA",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "factorial ANOVA",
        concept: "analysis_of_variance",
        topic_label: "ANOVA",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "repeated measures ANOVA",
        concept: "analysis_of_variance",
        topic_label: "ANOVA",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "Hypothesentests",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "Hypothesentest",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "hypothesis testing",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "null hypothesis",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "nullhypothese",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "german",
    },
    ScientificQueryAliasSpec {
        alias: "p-value",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "p value",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "english",
    },
    ScientificQueryAliasSpec {
        alias: "p-wert",
        concept: "hypothesis_testing",
        topic_label: "Hypothesentests",
        language: "german",
    },
];

fn normalize_scientific_query_text(query: &str) -> String {
    query.split_whitespace().collect::<Vec<_>>().join(" ")
}

fn scientific_query_contains_any(query_lower: &str, needles: &[&str]) -> bool {
    needles.iter().any(|needle| query_lower.contains(needle))
}

fn scientific_query_detected_aliases(query: &str) -> Vec<ScholarChatScientificQueryDetectedAlias> {
    let query_lower = query.to_lowercase();
    let mut aliases = Vec::new();
    for spec in SCIENTIFIC_QUERY_ALIAS_SPECS {
        let alias_search = spec.alias.to_lowercase();
        if let Some(byte_index) = query_lower.find(&alias_search) {
            aliases.push(ScholarChatScientificQueryDetectedAlias {
                alias: spec.alias.to_string(),
                concept: spec.concept.to_string(),
                language: spec.language.to_string(),
                start_index: query[..byte_index].chars().count(),
            });
        }
    }
    aliases.sort_by(|left, right| {
        left.start_index
            .cmp(&right.start_index)
            .then_with(|| left.alias.cmp(&right.alias))
            .then_with(|| left.concept.cmp(&right.concept))
    });
    aliases
}

fn scientific_query_detected_concepts(query: &str) -> Vec<ScientificQueryConceptCandidate> {
    let query_lower = query.to_lowercase();
    let mut concepts = BTreeMap::<&'static str, ScientificQueryConceptCandidate>::new();
    for spec in SCIENTIFIC_QUERY_ALIAS_SPECS {
        let alias_search = spec.alias.to_lowercase();
        if let Some(byte_index) = query_lower.find(&alias_search) {
            let start_index = query[..byte_index].chars().count();
            let candidate = ScientificQueryConceptCandidate {
                concept: spec.concept,
                topic_label: spec.topic_label,
                start_index,
            };
            concepts
                .entry(spec.concept)
                .and_modify(|existing| {
                    if candidate.start_index < existing.start_index
                        || (candidate.start_index == existing.start_index
                            && candidate.topic_label < existing.topic_label)
                    {
                        *existing = candidate;
                    }
                })
                .or_insert(candidate);
        }
    }
    let mut detected: Vec<_> = concepts.into_values().collect();
    detected.sort_by(|left, right| {
        left.start_index
            .cmp(&right.start_index)
            .then_with(|| left.topic_label.cmp(right.topic_label))
            .then_with(|| left.concept.cmp(right.concept))
    });
    detected
}

fn scientific_query_ambiguity_warnings(
    detected_concepts: &[ScientificQueryConceptCandidate],
) -> Vec<String> {
    if detected_concepts.len() <= 1 {
        return Vec::new();
    }
    let concept_list = detected_concepts
        .iter()
        .map(|candidate| candidate.topic_label.to_string())
        .collect::<Vec<_>>()
        .join(", ");
    vec![
        format!("Detected multiple scientific concepts: {}.", concept_list),
        format!(
            "Preferred first occurrence: {}.",
            detected_concepts
                .first()
                .map(|candidate| candidate.topic_label)
                .unwrap_or("the first inferred concept")
        ),
    ]
}

fn scientific_query_language_hints(
    query_lower: &str,
    detected_aliases: &[ScholarChatScientificQueryDetectedAlias],
) -> Vec<String> {
    let mut hints = Vec::new();
    if detected_aliases.iter().any(|alias| alias.language == "german")
        || scientific_query_contains_any(
            query_lower,
            &[
                "was ist",
                "erklär",
                "erklaer",
                "lernen",
                "klausur",
                "prüfung",
                "pruefung",
                "vorlesung",
                "seminar",
                "modul",
                "auswertung",
                "berechnen",
                "beispiel",
                "unterschied",
                "vergleich",
                "metaanalyse",
                "quelle",
                "quellen",
                "studie",
                "studien",
            ],
        )
    {
        push_unique_text(&mut hints, "german");
    }
    if detected_aliases.iter().any(|alias| alias.language == "english")
        || scientific_query_contains_any(
            query_lower,
            &[
                "what is",
                "explain",
                "define",
                "definition",
                "literature",
                "literatur",
                "paper",
                "papers",
                "review",
                "meta-analysis",
                "citation",
                "cite",
                "compare",
                "comparison",
                "example",
                "calculate",
                "dataset",
                "interpret",
                "regression",
                "experiment",
            ],
        )
    {
        push_unique_text(&mut hints, "english");
    }
    hints
}

fn scientific_query_intent(normalized_mode: &str, query_lower: &str) -> ScholarChatScientificQueryIntent {
    if normalized_mode == "scientific_paper" {
        ScholarChatScientificQueryIntent::LiteratureSearch
    } else if normalized_mode == "course" {
        ScholarChatScientificQueryIntent::CourseLearning
    } else if scientific_query_contains_any(
        query_lower,
        &[
            "literature",
            "literatur",
            "paper",
            "papers",
            "studie",
            "studien",
            "review",
            "meta-analysis",
            "metaanalyse",
            "systematic review",
            "citation",
            "cite",
            "quelle",
            "quellen",
        ],
    ) {
        ScholarChatScientificQueryIntent::LiteratureSearch
    } else if scientific_query_contains_any(
        query_lower,
        &[
            "kurs",
            "course",
            "lernen",
            "explain for exam",
            "klausur",
            "prüfung",
            "pruefung",
            "modul",
            "vorlesung",
            "seminar",
        ],
    ) {
        ScholarChatScientificQueryIntent::CourseLearning
    } else if scientific_query_contains_any(
        query_lower,
        &[
            "anwenden",
            "berechnen",
            "calculate",
            "example",
            "beispiel",
            "dataset",
            "daten",
            "auswertung",
            "interpretieren",
            "regression",
            "experiment",
        ],
    ) {
        ScholarChatScientificQueryIntent::MethodApplication
    } else if scientific_query_contains_any(
        query_lower,
        &[
            "unterschied",
            "compare",
            "comparison",
            "vs",
            "versus",
            "unterschied zwischen",
        ],
    ) {
        ScholarChatScientificQueryIntent::Comparison
    } else if scientific_query_contains_any(
        query_lower,
        &[
            "was ist",
            "erklären",
            "erklaeren",
            "explain",
            "define",
            "definition",
            "meaning",
            "bedeutung",
        ],
    ) {
        ScholarChatScientificQueryIntent::ConceptExplanation
    } else {
        ScholarChatScientificQueryIntent::Unknown
    }
}

fn scientific_query_planned_local_search_queries(recognized_concept: Option<&str>, normalized_query: &str) -> Vec<String> {
    match recognized_concept {
        Some("signal_detection_theory") => vec![
            "Signalentdeckung".to_string(),
            "Signalentdeckungstheorie".to_string(),
            "d prime".to_string(),
            "ROC".to_string(),
        ],
        Some("analysis_of_variance") => vec![
            "ANOVA".to_string(),
            "Varianzanalyse".to_string(),
            "F test".to_string(),
            "repeated measures".to_string(),
        ],
        Some("hypothesis_testing") => vec![
            "Hypothesentests".to_string(),
            "Nullhypothese".to_string(),
            "p-Wert".to_string(),
            "Fehler 1. Art".to_string(),
            "Fehler 2. Art".to_string(),
        ],
        _ if normalized_query.is_empty() => Vec::new(),
        _ => vec![normalized_query.to_string()],
    }
}

fn scientific_query_planned_expanded_queries(recognized_concept: Option<&str>) -> Vec<String> {
    match recognized_concept {
        Some("signal_detection_theory") => vec![
            "Signal Detection Theory".to_string(),
            "Signalentdeckungstheorie".to_string(),
            "d-prime".to_string(),
            "ROC analysis".to_string(),
        ],
        Some("analysis_of_variance") => vec![
            "analysis of variance".to_string(),
            "Varianzanalyse".to_string(),
            "F-test".to_string(),
            "repeated measures ANOVA".to_string(),
        ],
        Some("hypothesis_testing") => vec![
            "hypothesis testing".to_string(),
            "Hypothesentests".to_string(),
            "null hypothesis".to_string(),
            "p-value".to_string(),
        ],
        _ => Vec::new(),
    }
}

fn scientific_query_evidence_requirements(
    normalized_mode: &str,
    query_intent: &ScholarChatScientificQueryIntent,
    source_registry_status: &ScholarChatScientificSourceRegistryStatus,
) -> Vec<String> {
    let mut requirements = Vec::new();
    if !matches!(source_registry_status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady) {
        push_unique_text(
            &mut requirements,
            "source_family_plan_required",
        );
    }
    if normalized_mode == "scholar_chat" || normalized_mode == "course" {
        push_unique_text(
            &mut requirements,
            "local_evidence_required_before_answer",
        );
    }
    if normalized_mode == "scientific_paper"
        || matches!(query_intent, ScholarChatScientificQueryIntent::LiteratureSearch)
    {
        push_unique_text(
            &mut requirements,
            "citation_safe_metadata_required",
        );
        push_unique_text(
            &mut requirements,
            "deduplication_required_before_literature_review",
        );
    }
    if normalized_mode == "course"
        || matches!(query_intent, ScholarChatScientificQueryIntent::CourseLearning)
    {
        push_unique_text(
            &mut requirements,
            "course_material_alignment_required",
        );
    }
    requirements
}

fn normalize_scientific_selected_local_source_ids(ids: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = ids
        .unwrap_or_default()
        .into_iter()
        .map(|value| value.trim().to_string())
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn scientific_search_plan_strategy(
    status: &ScholarChatScientificSearchPlanStatus,
    normalized_mode: &str,
    query_intent: &ScholarChatScientificQueryIntent,
) -> ScholarChatScientificSearchStrategy {
    if matches!(status, ScholarChatScientificSearchPlanStatus::Blocked) {
        ScholarChatScientificSearchStrategy::Blocked
    } else if normalized_mode == "course" {
        ScholarChatScientificSearchStrategy::CourseLocalFirst
    } else if normalized_mode == "scientific_paper"
        || matches!(query_intent, ScholarChatScientificQueryIntent::LiteratureSearch)
    {
        ScholarChatScientificSearchStrategy::MetadataFirst
    } else {
        ScholarChatScientificSearchStrategy::LocalFirst
    }
}

fn scientific_search_plan_step_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no retrieval executed".to_string(),
        "no files read".to_string(),
        "no web request".to_string(),
        "no connector call".to_string(),
        "no index built".to_string(),
    ]
}

fn scientific_search_plan_step(
    kind: ScholarChatScientificSearchPlanStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_queries: Vec<String>,
    source_ids: Vec<String>,
    depends_on: Vec<String>,
    active: bool,
) -> ScholarChatScientificSearchPlanStep {
    ScholarChatScientificSearchPlanStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_queries,
        source_ids,
        depends_on,
        active,
        preview_only: true,
        boundary_notes: scientific_search_plan_step_notes(),
    }
}

fn scientific_search_plan_source_routing(
    preferred_source_ids: &[String],
    conditional_source_ids: &[String],
    excluded_source_ids: &[String],
) -> ScholarChatScientificSourceRoutingPlan {
    let mut active_routes = preferred_source_ids.to_vec();
    active_routes.sort();
    active_routes.dedup();

    let mut conditional_routes = conditional_source_ids.to_vec();
    conditional_routes.sort();
    conditional_routes.dedup();

    let mut excluded_routes = excluded_source_ids.to_vec();
    excluded_routes.sort();
    excluded_routes.dedup();

    let route_count = active_routes.len() + conditional_routes.len() + excluded_routes.len();
    ScholarChatScientificSourceRoutingPlan {
        route_count,
        active_routes,
        conditional_routes,
        excluded_routes,
        summary: if route_count == 0 {
            "No source routes are planned yet.".to_string()
        } else {
            "Source routing is planned only; no routing, connector calls, or registry writes were performed."
                .to_string()
        },
    }
}

fn scientific_search_plan_local_search(
    normalized_mode: &str,
    selected_local_source_ids: Vec<String>,
    planned_queries: Vec<String>,
) -> ScholarChatScientificLocalSearchPlan {
    let local_first = normalized_mode == "scholar_chat" || normalized_mode == "course";
    ScholarChatScientificLocalSearchPlan {
        local_source_count: selected_local_source_ids.len(),
        selected_local_source_ids,
        planned_queries,
        local_first,
        requires_local_evidence_before_answer: local_first,
        will_read_files: false,
        will_build_index: false,
        summary: if local_first {
            "Local search is planned only; no files are read and no indexes are built.".to_string()
        } else {
            "Local search is deferred; this preview does not read files or build indexes.".to_string()
        },
    }
}

fn scientific_search_plan_metadata(
    source_registry_status: &ScholarChatScientificSourceRegistryStatus,
    preferred_source_ids: Vec<String>,
    conditional_source_ids: Vec<String>,
    excluded_source_ids: Vec<String>,
    planned_queries: Vec<String>,
) -> ScholarChatScientificMetadataSearchPlan {
    let mut source_family_count = preferred_source_ids.len();
    source_family_count += conditional_source_ids.len();
    source_family_count += excluded_source_ids.len();
    ScholarChatScientificMetadataSearchPlan {
        source_family_count,
        preferred_source_ids,
        conditional_source_ids,
        excluded_source_ids,
        planned_queries,
        will_call_connectors: false,
        will_make_web_requests: false,
        summary: if matches!(source_registry_status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady) {
            "Metadata search is planned only; no connectors or web requests were performed.".to_string()
        } else {
            "Metadata search is planned only; routing still depends on the discipline and source registry previews.".to_string()
        },
    }
}

fn scientific_search_plan_evidence_requirements(
    normalized_mode: &str,
    query_intent: &ScholarChatScientificQueryIntent,
    source_registry_status: &ScholarChatScientificSourceRegistryStatus,
) -> Vec<String> {
    let mut requirements = scientific_query_evidence_requirements(
        normalized_mode,
        query_intent,
        source_registry_status,
    );
    push_unique_text(&mut requirements, "local_search_plan_required");
    push_unique_text(&mut requirements, "metadata_search_plan_required");
    push_unique_text(&mut requirements, "source_routing_required");
    push_unique_text(&mut requirements, "ranking_required_before_answer");
    if normalized_mode == "scientific_paper"
        || matches!(query_intent, ScholarChatScientificQueryIntent::LiteratureSearch)
    {
        push_unique_text(
            &mut requirements,
            "deduplication_required_before_literature_review",
        );
    }
    if normalized_mode == "course" {
        push_unique_text(&mut requirements, "course_material_alignment_required");
    }
    if normalized_mode == "scholar_chat" || normalized_mode == "course" {
        push_unique_text(&mut requirements, "local_evidence_required_before_answer");
    }
    requirements
}

fn scientific_search_plan_combined_queries(
    planned_local_queries: &[String],
    planned_metadata_queries: &[String],
    planned_expanded_queries: &[String],
) -> Vec<String> {
    let mut queries = Vec::new();
    for query in planned_expanded_queries
        .iter()
        .chain(planned_local_queries.iter())
        .chain(planned_metadata_queries.iter())
    {
        push_unique_text(&mut queries, query);
    }
    queries
}

fn scientific_search_plan_steps(
    normalized_mode: &str,
    status: &ScholarChatScientificSearchPlanStatus,
    selected_local_source_ids: &[String],
    preferred_source_ids: &[String],
    conditional_source_ids: &[String],
    excluded_source_ids: &[String],
    planned_local_queries: &[String],
    planned_metadata_queries: &[String],
    planned_expanded_queries: &[String],
    evidence_requirements: &[String],
) -> Vec<ScholarChatScientificSearchPlanStep> {
    let active = !matches!(status, ScholarChatScientificSearchPlanStatus::Blocked);
    let local_step_kind = if normalized_mode == "course" {
        ScholarChatScientificSearchPlanStepKind::LocalCourseMaterialSearch
    } else {
        ScholarChatScientificSearchPlanStepKind::LocalSourceSearch
    };
    let local_step_id = if normalized_mode == "course" {
        "local_course_material_search"
    } else {
        "local_source_search"
    };
    let local_step_label = if normalized_mode == "course" {
        "Local course material search"
    } else {
        "Local source search"
    };
    let local_step_description = if normalized_mode == "course" {
        "Plan future local course-material search only; no files are read and no indexes are built."
    } else {
        "Plan future local source search only; no files are read and no indexes are built."
    };

    let routing_source_ids = preferred_source_ids
        .iter()
        .chain(conditional_source_ids.iter())
        .chain(excluded_source_ids.iter())
        .cloned()
        .collect::<Vec<_>>();

    let mut ranking_queries = scientific_search_plan_combined_queries(
        planned_local_queries,
        planned_metadata_queries,
        planned_expanded_queries,
    );
    if ranking_queries.is_empty() {
        ranking_queries = planned_metadata_queries.to_vec();
    }

    vec![
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::QueryExpansion,
            "query_expansion",
            "Query expansion",
            "Preview query expansion only; no retrieval or indexing is executed.",
            planned_expanded_queries.to_vec(),
            Vec::new(),
            Vec::new(),
            active,
        ),
        scientific_search_plan_step(
            local_step_kind,
            local_step_id,
            local_step_label,
            local_step_description,
            planned_local_queries.to_vec(),
            selected_local_source_ids.to_vec(),
            vec!["query_expansion".to_string()],
            active,
        ),
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::SourceFamilyRouting,
            "source_family_routing",
            "Source-family routing",
            "Preview source-family routing only; no registry writes or connector calls are performed.",
            planned_metadata_queries.to_vec(),
            routing_source_ids.clone(),
            vec![local_step_id.to_string()],
            active,
        ),
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::MetadataSourceSearch,
            "metadata_source_search",
            "Metadata source search",
            "Preview metadata search only; no web requests or connectors are executed.",
            planned_metadata_queries.to_vec(),
            preferred_source_ids
                .iter()
                .chain(conditional_source_ids.iter())
                .cloned()
                .collect::<Vec<_>>(),
            vec!["source_family_routing".to_string()],
            active,
        ),
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::RankingPlan,
            "ranking_plan",
            "Ranking plan",
            "Preview ranking only; no ranking is executed and no answers are generated.",
            ranking_queries.clone(),
            Vec::new(),
            vec!["metadata_source_search".to_string()],
            active,
        ),
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::DeduplicationPlan,
            "deduplication_plan",
            "Deduplication plan",
            "Preview deduplication only; no evidence or answer artifacts are created.",
            ranking_queries,
            Vec::new(),
            vec!["ranking_plan".to_string()],
            active,
        ),
        scientific_search_plan_step(
            ScholarChatScientificSearchPlanStepKind::EvidenceRequirementCheck,
            "evidence_requirement_check",
            "Evidence requirement check",
            "Preview evidence requirements only; no Evidence Packs or answers are created.",
            evidence_requirements.to_vec(),
            Vec::new(),
            vec!["deduplication_plan".to_string()],
            active,
        ),
    ]
}

fn normalize_scientific_expected_source_kinds(kinds: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = kinds
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_scientific_tag_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn scientific_local_literature_index_status(
    search_plan_status: &ScholarChatScientificSearchPlanStatus,
    selected_local_source_ids: &[String],
) -> ScholarChatLocalLiteratureIndexStatus {
    match search_plan_status {
        ScholarChatScientificSearchPlanStatus::Blocked => ScholarChatLocalLiteratureIndexStatus::Blocked,
        ScholarChatScientificSearchPlanStatus::NeedsDisambiguation => {
            ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation
        }
        ScholarChatScientificSearchPlanStatus::UnknownConcept => {
            ScholarChatLocalLiteratureIndexStatus::UnknownConcept
        }
        ScholarChatScientificSearchPlanStatus::SearchPlanReady => {
            if selected_local_source_ids.is_empty() {
                ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources
            } else {
                ScholarChatLocalLiteratureIndexStatus::IndexPlanReady
            }
        }
    }
}

fn scientific_local_literature_index_strategy(
    status: &ScholarChatLocalLiteratureIndexStatus,
    normalized_mode: &str,
) -> ScholarChatLocalLiteratureIndexStrategy {
    if matches!(status, ScholarChatLocalLiteratureIndexStatus::Blocked) {
        ScholarChatLocalLiteratureIndexStrategy::Blocked
    } else if normalized_mode == "course" {
        ScholarChatLocalLiteratureIndexStrategy::CourseMaterialLocalFirst
    } else if normalized_mode == "scientific_paper" {
        ScholarChatLocalLiteratureIndexStrategy::ScientificPaperCitationLocalFirst
    } else {
        ScholarChatLocalLiteratureIndexStrategy::ScholarChatLocalFirst
    }
}

fn scientific_local_literature_index_boundary_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no file read".to_string(),
        "no pdf extraction".to_string(),
        "no ocr".to_string(),
        "no chunking run".to_string(),
        "no embeddings generated".to_string(),
        "no index created".to_string(),
        "no artifact write".to_string(),
    ]
}

fn scientific_local_literature_index_step(
    kind: ScholarChatLocalLiteratureIndexStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatLocalLiteratureIndexStep {
    ScholarChatLocalLiteratureIndexStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: scientific_local_literature_index_boundary_notes(),
    }
}

fn scientific_local_literature_index_planned_artifact_ids() -> Vec<String> {
    vec![
        "local_corpus_manifest_preview".to_string(),
        "local_literature_metadata_map_preview".to_string(),
        "lexical_index_plan_preview".to_string(),
        "vector_index_plan_preview".to_string(),
        "retrieval_readiness_plan_preview".to_string(),
    ]
}

fn scientific_local_literature_index_planned_artifact_descriptions() -> Vec<String> {
    vec![
        "Future local corpus manifest preview describing selected sources without reading files.".to_string(),
        "Future local literature metadata map preview describing source metadata only.".to_string(),
        "Future lexical index plan preview describing BM25-ready planning without index creation.".to_string(),
        "Future vector index plan preview describing embedding-ready planning without generation.".to_string(),
        "Future retrieval readiness plan preview describing later retrieval compatibility only.".to_string(),
    ]
}

fn scientific_local_literature_index_planned_index_fields() -> Vec<String> {
    vec![
        "source_id".to_string(),
        "source_kind".to_string(),
        "title".to_string(),
        "authors".to_string(),
        "year".to_string(),
        "doi".to_string(),
        "url_or_locator".to_string(),
        "course_context".to_string(),
        "discipline_concept".to_string(),
        "chunk_id_later".to_string(),
        "page_or_section_later".to_string(),
        "language_hint".to_string(),
        "local_query_terms".to_string(),
    ]
}

fn scientific_local_literature_index_chunking_policy() -> Vec<String> {
    vec![
        "chunking_not_run_in_preview".to_string(),
        "later_pdf_text_extraction_required_before_chunking".to_string(),
        "later_markdown_section_chunking_candidate".to_string(),
        "later_course_slide_section_chunking_candidate".to_string(),
        "preserve_page_or_section_locator_later".to_string(),
        "keep_citation_metadata_attached_to_chunks_later".to_string(),
    ]
}

fn scientific_local_literature_index_metadata_requirements(
    normalized_mode: &str,
) -> Vec<String> {
    let mut requirements = vec![
        "source_id_required".to_string(),
        "source_kind_required".to_string(),
        "title_required_when_available".to_string(),
        "authors_required_when_available".to_string(),
        "year_required_when_available".to_string(),
        "doi_or_stable_locator_preferred".to_string(),
        "local_file_locator_required_later_but_not_read_now".to_string(),
    ];
    if normalized_mode == "course" {
        push_unique_text(&mut requirements, "course_context_required_for_course_mode");
    }
    if normalized_mode == "scientific_paper" {
        push_unique_text(
            &mut requirements,
            "citation_metadata_required_for_scientific_paper",
        );
    }
    if normalized_mode == "scholar_chat" {
        push_unique_text(
            &mut requirements,
            "local_evidence_metadata_required_for_scholar_chat",
        );
    }
    requirements
}

fn scientific_local_literature_index_local_queries(
    planned_local_queries: &[String],
    normalized_query: &str,
) -> Vec<String> {
    if planned_local_queries.is_empty() && !normalized_query.is_empty() {
        vec![normalized_query.to_string()]
    } else {
        planned_local_queries.to_vec()
    }
}

fn normalize_course_optional_text(value: Option<String>) -> Option<String> {
    value.map(|value| value.split_whitespace().collect::<Vec<_>>().join(" "))
        .and_then(|value| if value.is_empty() { None } else { Some(value) })
}

fn normalize_course_identity_segment(value: &str) -> String {
    normalize_scientific_tag_text(value)
}

fn normalize_course_material_kind_text(value: &str) -> String {
    normalize_scientific_tag_text(value)
}

fn normalize_course_material_kinds(kinds: Option<Vec<String>>) -> Vec<String> {
    let mut normalized = kinds
        .unwrap_or_default()
        .into_iter()
        .map(|value| normalize_course_material_kind_text(&value))
        .filter(|value| !value.is_empty())
        .collect::<Vec<_>>();
    normalized.sort();
    normalized.dedup();
    normalized
}

fn course_literature_registry_boundary_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "no file read".to_string(),
        "no pdf extraction".to_string(),
        "no ocr".to_string(),
        "no chunking run".to_string(),
        "no embeddings generated".to_string(),
        "no index created".to_string(),
        "no web request".to_string(),
        "no scraping".to_string(),
        "no connector call".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
        "no source import".to_string(),
        "no model loading".to_string(),
        "no runtime inference".to_string(),
        "no llm call".to_string(),
        "no answer generation".to_string(),
        "no evidence pack creation".to_string(),
    ]
}

fn course_literature_registry_step(
    kind: ScholarChatCourseLiteratureRegistryStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatCourseLiteratureRegistryStep {
    ScholarChatCourseLiteratureRegistryStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        active,
        preview_only: true,
        boundary_notes: course_literature_registry_boundary_notes(),
    }
}

fn course_literature_registry_status(
    local_status: &ScholarChatLocalLiteratureIndexStatus,
    normalized_course_context: &Option<String>,
    normalized_module_code: &Option<String>,
    normalized_course_title: &Option<String>,
    selected_local_source_ids: &[String],
) -> ScholarChatCourseLiteratureRegistryStatus {
    match local_status {
        ScholarChatLocalLiteratureIndexStatus::Blocked => ScholarChatCourseLiteratureRegistryStatus::Blocked,
        ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation => {
            ScholarChatCourseLiteratureRegistryStatus::NeedsDisambiguation
        }
        ScholarChatLocalLiteratureIndexStatus::UnknownConcept => {
            ScholarChatCourseLiteratureRegistryStatus::UnknownConcept
        }
        ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources => {
            if normalized_course_context.is_none()
                && normalized_module_code.is_none()
                && normalized_course_title.is_none()
            {
                ScholarChatCourseLiteratureRegistryStatus::NeedsCourseContext
            } else {
                ScholarChatCourseLiteratureRegistryStatus::NeedsLocalSources
            }
        }
        ScholarChatLocalLiteratureIndexStatus::IndexPlanReady => {
            if normalized_course_context.is_none()
                && normalized_module_code.is_none()
                && normalized_course_title.is_none()
            {
                ScholarChatCourseLiteratureRegistryStatus::NeedsCourseContext
            } else if selected_local_source_ids.is_empty() {
                ScholarChatCourseLiteratureRegistryStatus::NeedsLocalSources
            } else {
                ScholarChatCourseLiteratureRegistryStatus::CourseRegistryPlanReady
            }
        }
    }
}

fn course_literature_registry_strategy(
    status: &ScholarChatCourseLiteratureRegistryStatus,
    normalized_module_code: &Option<String>,
    selected_local_source_ids: &[String],
) -> ScholarChatCourseLiteratureRegistryStrategy {
    if matches!(status, ScholarChatCourseLiteratureRegistryStatus::Blocked) {
        ScholarChatCourseLiteratureRegistryStrategy::Blocked
    } else if normalized_module_code.is_some() {
        ScholarChatCourseLiteratureRegistryStrategy::ModuleContextFirst
    } else if !selected_local_source_ids.is_empty() {
        ScholarChatCourseLiteratureRegistryStrategy::LocalSourceAlignmentFirst
    } else {
        ScholarChatCourseLiteratureRegistryStrategy::CourseMaterialAlignmentFirst
    }
}

fn course_literature_registry_course_identity(
    normalized_course_context: Option<String>,
    normalized_module_code: Option<String>,
    normalized_course_title: Option<String>,
    normalized_instructor: Option<String>,
    normalized_semester: Option<String>,
) -> ScholarChatCourseIdentityPreview {
    let identity_key_parts = [
        normalized_module_code
            .as_deref()
            .map(normalize_course_identity_segment),
        normalized_course_title
            .as_deref()
            .map(normalize_course_identity_segment),
        normalized_semester
            .as_deref()
            .map(normalize_course_identity_segment),
    ]
    .into_iter()
    .flatten()
    .filter(|value| !value.is_empty())
    .collect::<Vec<_>>();
    let identity_key = if identity_key_parts.is_empty() {
        None
    } else {
        Some(identity_key_parts.join("::"))
    };
    let has_course_context = normalized_course_context.is_some();
    let has_module_code = normalized_module_code.is_some();
    let has_course_title = normalized_course_title.is_some();
    let summary = if identity_key.is_some() {
        "Course identity preview combines the module code, course title, and semester when available; course context and instructor remain descriptive hints only.".to_string()
    } else {
        "Course identity preview has no module code, course title, or semester yet; course context and instructor remain descriptive hints only.".to_string()
    };

    ScholarChatCourseIdentityPreview {
        course_context: normalized_course_context,
        module_code: normalized_module_code,
        course_title: normalized_course_title,
        instructor: normalized_instructor,
        semester: normalized_semester,
        identity_key,
        has_course_context,
        has_module_code,
        has_course_title,
        summary,
    }
}

fn course_literature_registry_known_material_kinds() -> BTreeSet<&'static str> {
    [
        "syllabus",
        "module_handbook",
        "lecture_slide",
        "seminar_reading",
        "exercise_sheet",
        "assignment",
        "textbook_chapter",
        "article",
        "notes",
        "exam_prep",
        "unknown",
    ]
    .into_iter()
    .collect()
}

fn course_literature_registry_course_material_plan(
    selected_local_source_ids: Vec<String>,
    expected_course_material_kinds: Vec<String>,
) -> ScholarChatCourseMaterialPlan {
    let known_material_kind_catalog = course_literature_registry_known_material_kinds();
    let known_material_kinds = expected_course_material_kinds
        .iter()
        .filter(|kind| known_material_kind_catalog.contains(kind.as_str()))
        .cloned()
        .collect::<Vec<_>>();
    let unknown_material_kinds = expected_course_material_kinds
        .iter()
        .filter(|kind| !known_material_kind_catalog.contains(kind.as_str()))
        .cloned()
        .collect::<Vec<_>>();

    ScholarChatCourseMaterialPlan {
        selected_source_count: selected_local_source_ids.len(),
        selected_local_source_ids,
        expected_course_material_kinds,
        known_material_kinds,
        unknown_material_kinds,
        will_read_files: false,
        will_import_sources: false,
        will_create_registry: false,
        summary: "Course material planning is preview-only; no files are read, no sources are imported, and no registry is created.".to_string(),
    }
}

fn course_literature_registry_curriculum_alignment_plan(
    normalized_course_context: &Option<String>,
    normalized_module_code: &Option<String>,
) -> ScholarChatCurriculumAlignmentPlan {
    ScholarChatCurriculumAlignmentPlan {
        requires_course_context: normalized_course_context.is_none()
            && normalized_module_code.is_none(),
        requires_module_metadata: normalized_module_code.is_none(),
        requires_learning_objectives_later: true,
        requires_prerequisites_later: true,
        requires_session_or_week_mapping_later: true,
        will_scrape_curriculum_sources: false,
        will_call_connectors: false,
        summary: "Curriculum alignment planning is preview-only; no TU/ULB scraping or connector call occurs, and later phases would still need learning objectives, prerequisites, and session or week mapping.".to_string(),
    }
}

fn course_literature_registry_planned_course_metadata_requirements(
    course_identity: &ScholarChatCourseIdentityPreview,
    course_material_plan: &ScholarChatCourseMaterialPlan,
) -> Vec<String> {
    let mut requirements = vec![
        "course_context_or_module_identity_required".to_string(),
        "module_code_recommended".to_string(),
        "course_title_recommended".to_string(),
        "semester_recommended".to_string(),
        "instructor_optional".to_string(),
        "local_source_ids_required_for_course_material_alignment".to_string(),
        "material_kind_required_when_available".to_string(),
        "learning_objectives_required_later".to_string(),
        "prerequisites_required_later".to_string(),
        "session_or_week_mapping_required_later".to_string(),
        "citation_metadata_required_for_scientific_course_materials".to_string(),
        "no_curriculum_scraping_in_preview".to_string(),
    ];
    if !course_identity.has_course_context {
        requirements.push("course_context_missing".to_string());
    }
    if !course_identity.has_module_code {
        requirements.push("module_code_missing".to_string());
    }
    if !course_identity.has_course_title {
        requirements.push("course_title_missing".to_string());
    }
    if course_material_plan.selected_local_source_ids.is_empty() {
        requirements.push("local_sources_missing".to_string());
    }
    requirements
}

fn push_unique_string(items: &mut Vec<String>, value: String) {
    if !items.contains(&value) {
        items.push(value);
    }
}

fn course_literature_registry_planned_course_material_queries(
    normalized_query: &str,
    normalized_module_code: &Option<String>,
    normalized_course_title: &Option<String>,
    base_queries: &[String],
) -> Vec<String> {
    let mut planned_queries = base_queries.to_vec();
    if !normalized_query.is_empty() {
        push_unique_string(
            &mut planned_queries,
            format!("{normalized_query} course materials"),
        );
        push_unique_string(
            &mut planned_queries,
            format!("{normalized_query} lecture notes"),
        );
        push_unique_string(
            &mut planned_queries,
            format!("{normalized_query} module context"),
        );
        push_unique_string(
            &mut planned_queries,
            format!("{normalized_query} exam preparation"),
        );
        if let Some(module_code) = normalized_module_code {
            push_unique_string(
                &mut planned_queries,
                format!("{module_code} {normalized_query}"),
            );
        }
        if let Some(course_title) = normalized_course_title {
            push_unique_string(
                &mut planned_queries,
                format!("{course_title} {normalized_query}"),
            );
        }
    }
    planned_queries
}

#[derive(Clone)]
struct ScientificDisciplineRegistryEntry {
    recognized_concept: &'static str,
    label: &'static str,
    science_class: ScholarChatScientificDisciplineScienceClass,
    discipline_path: &'static [&'static str],
    parent_path: &'static [&'static str],
    related_methods: &'static [&'static str],
    appears_in: &'static [&'static str],
    preferred_sources: &'static [&'static str],
    curriculum_sources: &'static [&'static str],
    canonical_mappings: &'static [&'static str],
    planned_queries: &'static [&'static str],
}

fn normalize_scientific_topic_text(topic: &str) -> String {
    topic.split_whitespace().collect::<Vec<_>>().join(" ")
}

fn normalize_scientific_mode(mode: Option<String>) -> String {
    let normalized_mode = mode
        .as_deref()
        .map(|value| {
            let mut result = String::new();
            let mut last_was_separator = false;
            for ch in value.trim().chars() {
                if ch.is_alphanumeric() {
                    for lower in ch.to_lowercase() {
                        result.push(lower);
                    }
                    last_was_separator = false;
                } else if !result.is_empty() && !last_was_separator {
                    result.push('_');
                    last_was_separator = true;
                }
            }
            result.trim_matches('_').to_string()
        })
        .unwrap_or_default();
    match normalized_mode.as_str() {
        "scientific_paper" | "course" | "scholar_chat" => normalized_mode,
        _ => "scholar_chat".to_string(),
    }
}

fn normalize_scientific_topic_key(topic: &str) -> String {
    let mut normalized = String::new();
    let mut last_was_separator = false;
    let normalized_topic = topic.trim().to_lowercase().replace("d'", "d prime");
    let mut chars = normalized_topic.chars().peekable();
    while let Some(ch) = chars.next() {
        if ch.is_alphanumeric() {
            for lower in ch.to_lowercase() {
                normalized.push(lower);
            }
            last_was_separator = false;
        } else if !normalized.is_empty() && !last_was_separator {
            normalized.push(' ');
            last_was_separator = true;
        }
    }
    normalized.trim().to_string()
}

fn scientific_discipline_registry_entry(topic_key: &str) -> Option<ScientificDisciplineRegistryEntry> {
    match topic_key {
        "signalentdeckung"
        | "signalentdeckungstheorie"
        | "signal detection"
        | "signal detection theory"
        | "d prime" => Some(ScientificDisciplineRegistryEntry {
            recognized_concept: "signal_detection_theory",
            label: "Signalentdeckungstheorie",
            science_class: ScholarChatScientificDisciplineScienceClass::CoreScience,
            discipline_path: &[
                "psychology",
                "general_psychology",
                "perception",
                "psychophysics",
                "signal_detection_theory",
            ],
            parent_path: &["psychology", "general_psychology", "perception", "psychophysics"],
            related_methods: &["statistics", "probability_theory", "decision_theory", "roc_analysis"],
            appears_in: &[
                "perception_psychology",
                "psychophysics",
                "diagnostics",
                "neuroscience",
                "medical_testing",
                "machine_learning_evaluation",
            ],
            preferred_sources: &[
                "pubpsych",
                "psycharchives",
                "openalex",
                "crossref",
                "pubmed_if_biomedical_context",
            ],
            curriculum_sources: &["tu_darmstadt_module_handbook_candidate", "local_course_materials_later"],
            canonical_mappings: &[
                "psychology",
                "general_psychology",
                "psychophysics",
                "signal_detection_theory",
            ],
            planned_queries: &[
                "Signalentdeckungstheorie",
                "signal detection theory",
                "psychophysics signal detection",
                "d prime criterion ROC",
            ],
        }),
        "anova" | "varianzanalyse" | "analysis of variance" | "factorial anova" | "repeated measures anova" => Some(ScientificDisciplineRegistryEntry {
            recognized_concept: "analysis_of_variance",
            label: "ANOVA / Varianzanalyse",
            science_class: ScholarChatScientificDisciplineScienceClass::CoreScience,
            discipline_path: &["statistics", "inferential_statistics", "hypothesis_testing", "analysis_of_variance"],
            parent_path: &["statistics", "inferential_statistics", "hypothesis_testing"],
            related_methods: &["linear_models", "f_test", "effect_size", "post_hoc_tests", "repeated_measures"],
            appears_in: &[
                "psychology_methods",
                "experimental_design",
                "biomedical_statistics",
                "education_research",
            ],
            preferred_sources: &[
                "openalex",
                "crossref",
                "psycharchives_if_psychology_context",
                "pubpsych_if_psychology_context",
                "zbmath_if_theory_context",
                "arxiv_if_theory_context",
            ],
            curriculum_sources: &["tu_darmstadt_module_handbook_candidate", "local_course_materials_later"],
            canonical_mappings: &[
                "statistics",
                "inferential_statistics",
                "hypothesis_testing",
                "analysis_of_variance",
            ],
            planned_queries: &[
                "ANOVA",
                "Varianzanalyse",
                "analysis of variance",
                "factorial ANOVA",
                "repeated measures ANOVA",
            ],
        }),
        "hypothesentests"
        | "hypothesentest"
        | "hypothesis testing"
        | "null hypothesis"
        | "p value"
        | "null hypothesis p value"
        | "statistical power type i error type ii error"
        | "confidence intervals hypothesis testing" => Some(ScientificDisciplineRegistryEntry {
            recognized_concept: "hypothesis_testing",
            label: "Hypothesentests",
            science_class: ScholarChatScientificDisciplineScienceClass::CoreScience,
            discipline_path: &["statistics", "inferential_statistics", "hypothesis_testing"],
            parent_path: &["statistics", "inferential_statistics"],
            related_methods: &[
                "null_hypothesis",
                "alternative_hypothesis",
                "p_value",
                "type_i_error",
                "type_ii_error",
                "power",
                "confidence_intervals",
            ],
            appears_in: &["psychology_methods", "experimental_design", "biomedical_statistics", "data_analysis"],
            preferred_sources: &[
                "openalex",
                "crossref",
                "zbmath",
                "arxiv",
                "pubpsych_if_psychology_context",
                "psycharchives_if_psychology_context",
            ],
            curriculum_sources: &["tu_darmstadt_module_handbook_candidate", "local_course_materials_later"],
            canonical_mappings: &["statistics", "inferential_statistics", "hypothesis_testing"],
            planned_queries: &[
                "Hypothesentests",
                "hypothesis testing",
                "null hypothesis p value",
                "statistical power type I error type II error",
                "confidence intervals hypothesis testing",
            ],
        }),
        _ => None,
    }
}

fn preview_warnings(policy: &GroundingPolicy, selected_source_count: usize, kind: ScholarChatPreviewKind) -> Vec<String> {
    let mut warnings = Vec::new();
    if selected_source_count == 0 {
        match kind {
            ScholarChatPreviewKind::Request => {
                warnings.push("No selected sources; preview cannot plan source-scoped grounding yet.".to_string());
            }
            ScholarChatPreviewKind::Retrieval => {
                warnings.push("No selected sources; retrieval preview is unscoped.".to_string());
            }
        }
    }
    match policy {
        GroundingPolicy::LocalOnly => warnings.push("local_only requires local evidence before an answer can be presented as grounded.".to_string()),
        GroundingPolicy::ExternalAdaptersLater => warnings.push("External scholarly adapters are not implemented in this preview.".to_string()),
        GroundingPolicy::AllowMarkedModelKnowledge => warnings.push("Model knowledge is not used in this preview and would need to be clearly marked later.".to_string()),
        GroundingPolicy::LocalFirst => {}
    }
    match kind {
        ScholarChatPreviewKind::Request => warnings.push("This is a contract preview only; no retrieval, evidence-pack build, or model inference was run.".to_string()),
        ScholarChatPreviewKind::Retrieval => warnings.push("This is a retrieval preview only; no answer was generated.".to_string()),
    }
    warnings
}

fn grounding_plan(mode: &ScholarChatMode, policy: &GroundingPolicy, selected_source_count: usize) -> ScholarChatGroundingPlan {
    ScholarChatGroundingPlan {
        selected_source_count,
        local_corpus_required: matches!(policy, GroundingPolicy::LocalOnly | GroundingPolicy::LocalFirst),
        retrieval_would_run: selected_source_count > 0,
        evidence_pack_would_be_required: true,
        model_knowledge_allowed: matches!(policy, GroundingPolicy::AllowMarkedModelKnowledge),
        external_adapters_available: false,
        summary: grounding_summary(mode, policy, selected_source_count),
        steps: vec![
            "Normalize prompt and validate selected source IDs.".to_string(),
            "Resolve selected course or project context before retrieval.".to_string(),
            "Search registered local sources before any answer synthesis.".to_string(),
            "Assemble an Evidence Pack before grounded answer generation.".to_string(),
            "Return source/evidence status with any future answer.".to_string(),
        ],
    }
}

fn convert_retrieval_response(response: RetrievalResponse) -> Vec<ScholarChatRetrievalCandidate> {
    response
        .results
        .into_iter()
        .map(|result| ScholarChatRetrievalCandidate {
            source_id: result.source_id,
            version_id: result.version_id,
            chunk_id: result.chunk_id,
            score: result.score,
            matched_terms: result.matched_terms,
            preview: result.preview,
            locator: result.locator,
        })
        .collect()
}

fn convert_retrieval_candidates_from_preview(
    candidates: &[ScholarChatRetrievalCandidate],
) -> Vec<ScholarChatEvidenceCandidate> {
    candidates
        .iter()
        .map(|result| ScholarChatEvidenceCandidate {
            source_id: result.source_id.clone(),
            version_id: result.version_id.clone(),
            chunk_id: result.chunk_id.clone(),
            score: result.score,
            matched_terms: result.matched_terms.clone(),
            preview: result.preview.clone(),
            locator: result.locator.clone(),
        })
        .collect()
}

fn convert_evidence_candidates_to_prompt_context_items(
    candidates: &[ScholarChatEvidenceCandidate],
) -> Vec<ScholarChatPromptContextItem> {
    candidates
        .iter()
        .map(|result| ScholarChatPromptContextItem {
            source_id: result.source_id.clone(),
            version_id: result.version_id.clone(),
            chunk_id: result.chunk_id.clone(),
            score: result.score,
            matched_terms: result.matched_terms.clone(),
            preview: result.preview.clone(),
            locator: result.locator.clone(),
        })
        .collect()
}

fn build_prompt_pack(
    mode: &ScholarChatMode,
    policy: &GroundingPolicy,
    normalized_prompt: &str,
    selected_source_ids: &[String],
    context_items: &[ScholarChatPromptContextItem],
    evidence_candidate_count: usize,
) -> ScholarChatPromptPack {
    let sections = vec![
        ScholarChatPromptPackSection {
            kind: ScholarChatPromptPackSectionKind::SystemOrPolicyInstructions,
            title: "System or policy instructions".to_string(),
            lines: system_or_policy_instructions(policy),
        },
        ScholarChatPromptPackSection {
            kind: ScholarChatPromptPackSectionKind::ModeInstructions,
            title: "Mode instructions".to_string(),
            lines: mode_instructions(mode),
        },
        ScholarChatPromptPackSection {
            kind: ScholarChatPromptPackSectionKind::GroundingInstructions,
            title: "Grounding instructions".to_string(),
            lines: grounding_pack_instructions(policy, selected_source_ids.len(), evidence_candidate_count),
        },
        ScholarChatPromptPackSection {
            kind: ScholarChatPromptPackSectionKind::SourceContext,
            title: "Source context".to_string(),
            lines: source_context_lines(selected_source_ids, context_items),
        },
        ScholarChatPromptPackSection {
            kind: ScholarChatPromptPackSectionKind::UserPrompt,
            title: "User prompt".to_string(),
            lines: vec![normalized_prompt.to_string()],
        },
    ];

    ScholarChatPromptPack {
        section_count: sections.len(),
        context_item_count: context_items.len(),
        estimated_input_char_count: estimate_prompt_pack_char_count(&sections, context_items),
        sections,
    }
}

fn system_or_policy_instructions(policy: &GroundingPolicy) -> Vec<String> {
    let mut lines = vec![
        "AEGIS Scholar local-first academic Scholar Chat workspace.".to_string(),
        "Preview only; no model inference or answer generation is run here.".to_string(),
    ];
    match policy {
        GroundingPolicy::LocalOnly => lines.push("Use only selected local evidence.".to_string()),
        GroundingPolicy::LocalFirst => lines.push("Prefer selected local evidence before any later fallback.".to_string()),
        GroundingPolicy::AllowMarkedModelKnowledge => lines.push("Model knowledge may be used later only if clearly marked.".to_string()),
        GroundingPolicy::ExternalAdaptersLater => lines.push("External scholarly adapters are not implemented yet.".to_string()),
    }
    lines
}

fn mode_instructions(mode: &ScholarChatMode) -> Vec<String> {
    match mode {
        ScholarChatMode::LectureLearning => vec![
            "Answer from course or lecture material first.".to_string(),
            "Prioritize what was taught and keep explanations grounded.".to_string(),
        ],
        ScholarChatMode::ThesisWriting => vec![
            "Support scientific writing, outlining, and literature synthesis.".to_string(),
            "Keep claims grounded and ready for citation.".to_string(),
        ],
        ScholarChatMode::LiteratureReview => vec![
            "Compare and synthesize papers with provenance.".to_string(),
            "Prefer source-linked evidence over general summaries.".to_string(),
        ],
        ScholarChatMode::Flashcards => vec![
            "Generate source-linked study-card candidates later.".to_string(),
            "Keep prompts compact and recall-oriented.".to_string(),
        ],
        ScholarChatMode::StatisticsMethods => vec![
            "Explain methods and support reproducible academic work.".to_string(),
            "Keep terminology precise and source-linked.".to_string(),
        ],
        ScholarChatMode::GeneralScholar => vec![
            "General academic assistant, local-first.".to_string(),
            "Use selected sources before any later fallback.".to_string(),
        ],
    }
}

fn grounding_pack_instructions(
    policy: &GroundingPolicy,
    selected_source_count: usize,
    evidence_candidate_count: usize,
) -> Vec<String> {
    let mut lines = vec![
        format!("Selected source count: {selected_source_count}."),
        format!("Evidence candidate count: {evidence_candidate_count}."),
    ];
    match policy {
        GroundingPolicy::LocalOnly => lines.push("local_only cannot answer without local evidence.".to_string()),
        GroundingPolicy::LocalFirst => lines.push("Prefer local evidence before any later fallback.".to_string()),
        GroundingPolicy::AllowMarkedModelKnowledge => lines.push("Model knowledge is only allowed when clearly marked later.".to_string()),
        GroundingPolicy::ExternalAdaptersLater => lines.push("External adapters are not implemented in this preview.".to_string()),
    }
    lines
}

fn source_context_lines(selected_source_ids: &[String], context_items: &[ScholarChatPromptContextItem]) -> Vec<String> {
    if selected_source_ids.is_empty() {
        return vec!["No selected sources; prompt pack preview is unscoped.".to_string()];
    }

    let mut counts_by_source = BTreeMap::new();
    for item in context_items {
        *counts_by_source.entry(item.source_id.clone()).or_insert(0usize) += 1;
    }

    let mut lines = vec![format!("Selected source IDs: {}.", selected_source_ids.join(", "))];
    for source_id in selected_source_ids {
        let count = counts_by_source.get(source_id).copied().unwrap_or(0);
        lines.push(format!("{source_id}: {count} evidence candidate(s)."));
    }
    lines
}

fn estimate_prompt_pack_char_count(
    sections: &[ScholarChatPromptPackSection],
    context_items: &[ScholarChatPromptContextItem],
) -> usize {
    let section_chars = sections.iter().fold(0usize, |acc, section| {
        let title_chars = section.title.chars().count();
        let line_chars = section.lines.iter().map(|line| line.chars().count()).sum::<usize>();
        let separator_chars = section.lines.len().saturating_sub(1);
        acc + title_chars + line_chars + separator_chars
    });
    let context_chars = context_items.iter().fold(0usize, |acc, item| {
        acc + item.source_id.chars().count()
            + item.version_id.chars().count()
            + item.chunk_id.chars().count()
            + item.preview.chars().count()
            + item.matched_terms.iter().map(|term| term.chars().count()).sum::<usize>()
            + locator_summary_chars(&item.locator)
    });
    section_chars + context_chars
}

fn locator_summary_chars(locator: &CitationLocator) -> usize {
    let section = locator
        .section_path
        .as_ref()
        .map(|value| value.iter().map(|part| part.chars().count()).sum::<usize>())
        .unwrap_or(0);
    let start = locator.character_start.map(|value| value.to_string().chars().count()).unwrap_or(0);
    let end = locator.character_end.map(|value| value.to_string().chars().count()).unwrap_or(0);
    locator.label.chars().count() + section + start + end
}

fn push_warning(warnings: &mut Vec<String>, message: &str) {
    if !warnings.iter().any(|warning| warning == message) {
        warnings.push(message.to_string());
    }
}

fn push_readiness_warning(
    warnings: &mut Vec<ScholarChatAnswerReadinessWarning>,
    kind: &str,
    message: &str,
) {
    if !warnings.iter().any(|warning| warning.kind == kind && warning.message == message) {
        warnings.push(ScholarChatAnswerReadinessWarning {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_readiness_blocker(
    blockers: &mut Vec<ScholarChatAnswerReadinessBlocker>,
    kind: &str,
    message: &str,
) {
    if !blockers.iter().any(|blocker| blocker.kind == kind && blocker.message == message) {
        blockers.push(ScholarChatAnswerReadinessBlocker {
            kind: kind.to_string(),
            message: message.to_string(),
        });
    }
}

fn push_readiness_action(actions: &mut Vec<String>, action: &str) {
    if !actions.iter().any(|existing| existing == action) {
        actions.push(action.to_string());
    }
}

fn readiness_status(
    policy: GroundingPolicy,
    selected_source_count: usize,
    retrieval_candidate_count: usize,
    evidence_candidate_count: usize,
    runtime_ready: bool,
    invocation_ready: bool,
    allow_model_execution: bool,
) -> ScholarChatAnswerReadinessStatus {
    if selected_source_count == 0 {
        if matches!(policy, GroundingPolicy::AllowMarkedModelKnowledge) && runtime_ready && invocation_ready && allow_model_execution {
            return ScholarChatAnswerReadinessStatus::ReadyForDraftInferenceLater;
        }
        return ScholarChatAnswerReadinessStatus::NeedsSources;
    }
    if retrieval_candidate_count == 0 {
        return if matches!(policy, GroundingPolicy::LocalOnly) {
            ScholarChatAnswerReadinessStatus::Blocked
        } else {
            ScholarChatAnswerReadinessStatus::NeedsRetrievalIndex
        };
    }
    if evidence_candidate_count == 0 {
        return if matches!(policy, GroundingPolicy::LocalOnly) {
            ScholarChatAnswerReadinessStatus::Blocked
        } else {
            ScholarChatAnswerReadinessStatus::NeedsEvidenceCandidates
        };
    }
    if !runtime_ready {
        return ScholarChatAnswerReadinessStatus::NeedsRuntimeConfig;
    }
    if !allow_model_execution {
        return ScholarChatAnswerReadinessStatus::NeedsExecutionConsent;
    }
    if matches!(policy, GroundingPolicy::AllowMarkedModelKnowledge) {
        ScholarChatAnswerReadinessStatus::ReadyForDraftInferenceLater
    } else {
        ScholarChatAnswerReadinessStatus::ReadyForGroundedDraftLater
    }
}

fn readiness_output_classification(
    status: ScholarChatAnswerReadinessStatus,
) -> ScholarChatAnswerReadinessOutputClassification {
    match status {
        ScholarChatAnswerReadinessStatus::Blocked
        | ScholarChatAnswerReadinessStatus::NeedsSources
        | ScholarChatAnswerReadinessStatus::NeedsRuntimeConfig
        | ScholarChatAnswerReadinessStatus::NeedsExecutionConsent => {
            ScholarChatAnswerReadinessOutputClassification::Blocked
        }
        ScholarChatAnswerReadinessStatus::NeedsRetrievalIndex
        | ScholarChatAnswerReadinessStatus::NeedsEvidenceCandidates => {
            ScholarChatAnswerReadinessOutputClassification::SourceContextDraft
        }
        ScholarChatAnswerReadinessStatus::ReadyForDraftInferenceLater => {
            ScholarChatAnswerReadinessOutputClassification::UngroundedDraft
        }
        ScholarChatAnswerReadinessStatus::ReadyForGroundedDraftLater => {
            ScholarChatAnswerReadinessOutputClassification::GroundedDraftCandidate
        }
    }
}

fn evidence_plan(
    mode: &ScholarChatMode,
    policy: &GroundingPolicy,
    selected_source_count: usize,
    retrieval_candidate_count: usize,
    evidence_candidate_count: usize,
) -> ScholarChatEvidencePlan {
    ScholarChatEvidencePlan {
        retrieval_candidate_count,
        evidence_candidate_count,
        evidence_required: true,
        evidence_pack_would_be_built_later: true,
        summary: evidence_plan_summary(mode, policy, selected_source_count, retrieval_candidate_count, evidence_candidate_count),
        steps: vec![
            "Normalize prompt and validate selected source IDs.".to_string(),
            "Reuse retrieval-preview candidates over the selected local sources.".to_string(),
            "Mark retrieval candidates that would be eligible for Evidence Pack assembly later.".to_string(),
            "Return preview-only evidence readiness warnings without building an Evidence Pack.".to_string(),
        ],
    }
}

fn evidence_plan_summary(
    mode: &ScholarChatMode,
    policy: &GroundingPolicy,
    selected_source_count: usize,
    retrieval_candidate_count: usize,
    evidence_candidate_count: usize,
) -> String {
    format!(
        "Preview plans a {:?} request with {:?} grounding over {} selected source(s), yielding {} retrieval candidate(s) and {} evidence candidate(s); no Evidence Pack is built yet.",
        mode,
        policy,
        selected_source_count,
        retrieval_candidate_count,
        evidence_candidate_count,
    )
}

fn grounding_summary(mode: &ScholarChatMode, policy: &GroundingPolicy, selected_source_count: usize) -> String {
    format!(
        "Preview plans a {:?} request with {:?} grounding over {} selected source(s); no answer is generated.",
        mode, policy, selected_source_count
    )
}

fn validate_source_id(source_id: &str) -> AegisResult<()> {
    if source_id.trim().is_empty() {
        return Err(AegisError::ScholarChatInvalidSourceId);
    }
    if source_id.contains('/') || source_id.contains('\\') || source_id.contains("..") {
        return Err(AegisError::ScholarChatInvalidSourceId);
    }
    Ok(())
}

fn validate_answer_draft_id(answer_draft_id: &str) -> AegisResult<()> {
    if answer_draft_id.trim().is_empty() {
        return Err(AegisError::AnswerDraftInvalidId);
    }
    if answer_draft_id.contains('/') || answer_draft_id.contains('\\') || answer_draft_id.contains("..") {
        return Err(AegisError::AnswerDraftInvalidId);
    }
    Ok(())
}

fn scientific_metadata_query_supported_public_providers() -> Vec<String> {
    scientific_metadata_supported_providers()
        .into_iter()
        .filter(|provider_id| {
            matches!(provider_id.as_str(), "openalex" | "crossref" | "pubmed" | "eric")
        })
        .collect()
}

fn scientific_metadata_query_supported_institutional_providers() -> Vec<String> {
    scientific_metadata_supported_providers()
        .into_iter()
        .filter(|provider_id| matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo"))
        .collect()
}

fn scientific_metadata_query_plan_provider_family(
    provider_id: &str,
) -> ScholarChatScientificMetadataQueryProviderFamily {
    if matches!(provider_id, "apa_psycnet" | "psycinfo") {
        ScholarChatScientificMetadataQueryProviderFamily::InstitutionalBoundary
    } else {
        ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata
    }
}

fn scientific_metadata_query_plan_access_kind_label(provider_id: &str) -> String {
    match scientific_metadata_provider_access_kind(provider_id) {
        ScholarChatScientificMetadataProviderAccessKind::OpenMetadata => "open_metadata",
        ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi => "public_metadata_api",
        ScholarChatScientificMetadataProviderAccessKind::InstitutionalDiscovery => {
            "institutional_discovery"
        }
        ScholarChatScientificMetadataProviderAccessKind::AuthenticatedLibraryBoundary => {
            "authenticated_library_boundary"
        }
    }
    .to_string()
}

fn scientific_metadata_query_plan_provider_query_terms(
    provider_id: &str,
    normalized_query: &str,
    normalized_query_goal: &Option<String>,
    normalized_context_tags: &[String],
    normalized_preferred_metadata_sources: &[String],
    normalized_preferred_psychology_source_families: &[String],
) -> Vec<String> {
    let mut terms = BTreeSet::new();
    if !normalized_query.is_empty() {
        terms.insert(normalized_query.to_string());
    }
    if let Some(goal) = normalized_query_goal.as_ref() {
        terms.insert(goal.clone());
    }
    for tag in normalized_context_tags {
        terms.insert(format!("context_tag:{tag}"));
    }
    for source in normalized_preferred_metadata_sources {
        terms.insert(format!("preferred_metadata_source:{source}"));
    }
    for family in normalized_preferred_psychology_source_families {
        terms.insert(format!("preferred_psychology_source_family:{family}"));
    }
    for capability in scientific_metadata_provider_planned_query_capabilities(provider_id) {
        terms.insert(capability);
    }
    terms.into_iter().collect()
}

fn scientific_metadata_query_plan_provider_query_templates(
    provider_id: &str,
    normalized_query: &str,
    normalized_query_goal: &Option<String>,
) -> Vec<String> {
    let query_preview = if normalized_query.is_empty() {
        "blank_query".to_string()
    } else {
        compact_text_preview(normalized_query, 72)
    };
    let goal_preview = normalized_query_goal
        .as_deref()
        .map(|goal| compact_text_preview(goal, 72))
        .unwrap_or_else(|| "query_goal_missing".to_string());
    match provider_id {
        "openalex" => vec![
            format!("openalex title/topic/author/year planning for {query_preview}"),
            format!("openalex doi/open-access/year planning for {goal_preview}"),
        ],
        "crossref" => vec![
            format!("crossref DOI/title/author/year planning for {query_preview}"),
            format!("crossref bibliographic metadata planning for {goal_preview}"),
        ],
        "pubmed" => vec![
            format!("pubmed title/MeSH/author/year planning for {query_preview}"),
            format!("pubmed biomedical metadata planning for {goal_preview}"),
        ],
        "eric" => vec![
            format!("ERIC title/descriptor/author/year planning for {query_preview}"),
            format!("ERIC education metadata planning for {goal_preview}"),
        ],
        "apa_psycnet" => vec![
            "APA PsycNet manual institutional discovery planning only".to_string(),
            format!("APA PsycNet psychology discovery boundary for {goal_preview}"),
        ],
        "psycinfo" => vec![
            "PsycINFO manual authenticated-library boundary planning only".to_string(),
            format!("PsycINFO psychology discovery boundary for {goal_preview}"),
        ],
        _ => vec!["unknown_query_template".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_filters(
    provider_id: &str,
    require_open_access: bool,
    require_doi: bool,
    year_from: Option<u16>,
    year_to: Option<u16>,
) -> Vec<String> {
    let mut filters = vec![format!("provider_id={provider_id}")];
    if require_open_access {
        filters.push("require_open_access=true".to_string());
    }
    if require_doi {
        filters.push("require_doi=true".to_string());
    }
    if let Some(year_from) = year_from {
        filters.push(format!("year_from={year_from}"));
    }
    if let Some(year_to) = year_to {
        filters.push(format!("year_to={year_to}"));
    }
    match provider_id {
        "openalex" => {
            filters.push("supports_open_access_filter".to_string());
            filters.push("supports_doi_filter".to_string());
        }
        "crossref" => {
            filters.push("supports_doi_filter".to_string());
            filters.push("supports_bibliographic_year_filter".to_string());
        }
        "pubmed" => {
            filters.push("supports_year_filter".to_string());
            filters.push("supports_mesh_or_keyword_planning".to_string());
        }
        "eric" => {
            filters.push("supports_year_filter".to_string());
            filters.push("supports_open_access_where_available".to_string());
        }
        "apa_psycnet" | "psycinfo" => {
            filters.push("manual_access_review_later".to_string());
            filters.push("no_authenticated_query_execution".to_string());
        }
        _ => {
            filters.push("unknown_filter_support".to_string());
        }
    }
    filters.sort();
    filters.dedup();
    filters
}

fn scientific_metadata_query_plan_provider_result_fields(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" => vec![
            "openalex_id".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "venue_or_source".to_string(),
            "provider_attribution".to_string(),
        ],
        "crossref" => vec![
            "doi".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "publisher_or_container".to_string(),
            "provider_attribution".to_string(),
        ],
        "pubmed" => vec![
            "pmid".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "journal".to_string(),
            "publication_year".to_string(),
            "provider_attribution".to_string(),
        ],
        "eric" => vec![
            "eric_id".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "source".to_string(),
            "provider_attribution".to_string(),
        ],
        "apa_psycnet" => vec![
            "psycnet_record_id".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "source".to_string(),
            "provider_attribution".to_string(),
            "manual_access_review_later".to_string(),
        ],
        "psycinfo" => vec![
            "psycinfo_record_id".to_string(),
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "source".to_string(),
            "provider_attribution".to_string(),
            "manual_access_review_later".to_string(),
        ],
        _ => vec!["unknown_result_field".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_sorting(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" => vec![
            "relevance desc".to_string(),
            "publication_year desc".to_string(),
            "citation_count desc".to_string(),
            "title asc".to_string(),
        ],
        "crossref" => vec![
            "relevance desc".to_string(),
            "publication_year desc".to_string(),
            "title asc".to_string(),
            "doi asc".to_string(),
        ],
        "pubmed" => vec![
            "relevance desc".to_string(),
            "publication_year desc".to_string(),
            "pmid asc".to_string(),
            "title asc".to_string(),
        ],
        "eric" => vec![
            "relevance desc".to_string(),
            "publication_year desc".to_string(),
            "eric_id asc".to_string(),
            "title asc".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "relevance desc".to_string(),
            "publication_year desc".to_string(),
            "title asc".to_string(),
            "manual_access_review_later".to_string(),
        ],
        _ => vec!["unknown_sorting".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_dedup_keys(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" | "crossref" => vec![
            "doi".to_string(),
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
        ],
        "pubmed" => vec![
            "pmid".to_string(),
            "doi".to_string(),
            "title".to_string(),
            "year".to_string(),
        ],
        "eric" => vec![
            "eric_id".to_string(),
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
        ],
        "apa_psycnet" => vec![
            "psycnet_record_id".to_string(),
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
        ],
        "psycinfo" => vec![
            "psycinfo_record_id".to_string(),
            "title".to_string(),
            "author".to_string(),
            "year".to_string(),
        ],
        _ => vec!["unknown_dedup_key".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_rate_limit_notes(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" | "crossref" | "pubmed" | "eric" => vec![
            "preview-only".to_string(),
            "no rate-limit enforcement now".to_string(),
            "no provider call now".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "preview-only".to_string(),
            "manual institutional review later".to_string(),
            "no automated provider call now".to_string(),
        ],
        _ => vec!["preview-only".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_attribution_notes(provider_id: &str) -> Vec<String> {
    match provider_id {
        "openalex" => vec![
            "provider_name".to_string(),
            "provider_record_id".to_string(),
            "provider_attribution".to_string(),
        ],
        "crossref" | "pubmed" | "eric" => vec![
            "provider_name".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "provider_terms_note".to_string(),
        ],
        "apa_psycnet" | "psycinfo" => vec![
            "provider_name".to_string(),
            "provider_record_id".to_string(),
            "manual_access_review_later".to_string(),
        ],
        _ => vec!["provider_attribution_preview".to_string()],
    }
}

fn scientific_metadata_query_plan_provider_institutional_boundary_notes(
    provider_id: &str,
) -> Vec<String> {
    match provider_id {
        "apa_psycnet" | "psycinfo" => vec![
            "preview-only".to_string(),
            "no automated authenticated access".to_string(),
            "no paywall bypass".to_string(),
            "manual or institutional access review required later".to_string(),
        ],
        _ => vec![
            "preview-only".to_string(),
            "no provider call".to_string(),
            "no URL building".to_string(),
            "no network call".to_string(),
        ],
    }
}

fn scientific_metadata_query_plan_provider_safety_notes() -> Vec<String> {
    vec![
        "preview-only".to_string(),
        "metadata query plan preview only".to_string(),
        "dry-run only".to_string(),
        "no provider call".to_string(),
        "no URL building".to_string(),
        "no network call".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no file read".to_string(),
        "no retrieval execution".to_string(),
        "no Evidence Pack created".to_string(),
        "no Literature Review created".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ]
}

fn scientific_metadata_query_plan_step_notes() -> Vec<String> {
    let mut notes = vec![
        "preview-only".to_string(),
        "metadata query plan preview only".to_string(),
        "dry-run only".to_string(),
        "no provider call".to_string(),
        "no URL building".to_string(),
        "no network call".to_string(),
        "no web request".to_string(),
        "no HTTP client".to_string(),
        "no API key read".to_string(),
        "no environment read".to_string(),
        "no scraping".to_string(),
        "no authenticated library access".to_string(),
        "no paywall bypass".to_string(),
        "no connector call".to_string(),
        "no source import".to_string(),
        "no metadata record write".to_string(),
        "no file read".to_string(),
        "no retrieval execution".to_string(),
        "no Evidence Pack created".to_string(),
        "no Literature Review created".to_string(),
        "no artifact write".to_string(),
        "no persistence".to_string(),
    ];
    notes.sort();
    notes.dedup();
    notes
}

fn scientific_metadata_query_plan_step(
    kind: ScholarChatScientificMetadataQueryPlanStepKind,
    id: &str,
    label: &str,
    description: &str,
    planned_inputs: Vec<String>,
    planned_outputs: Vec<String>,
    active: bool,
) -> ScholarChatScientificMetadataQueryPlanStep {
    ScholarChatScientificMetadataQueryPlanStep {
        kind,
        id: id.to_string(),
        label: label.to_string(),
        description: description.to_string(),
        planned_inputs,
        planned_outputs,
        boundary_notes: scientific_metadata_query_plan_step_notes(),
        preview_only: true,
        active,
    }
}

fn scientific_metadata_query_plan_provider_record(
    provider_id: &str,
    selected: bool,
    normalized_query: &str,
    normalized_query_goal: &Option<String>,
    normalized_context_tags: &[String],
    normalized_preferred_metadata_sources: &[String],
    normalized_preferred_psychology_source_families: &[String],
    require_open_access: bool,
    require_doi: bool,
    year_from: Option<u16>,
    year_to: Option<u16>,
) -> ScholarChatScientificMetadataQueryPlanProviderRecord {
    let provider_family = scientific_metadata_query_plan_provider_family(provider_id);
    let access_kind_label = scientific_metadata_query_plan_access_kind_label(provider_id);
    let planned_query_terms = scientific_metadata_query_plan_provider_query_terms(
        provider_id,
        normalized_query,
        normalized_query_goal,
        normalized_context_tags,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families,
    );
    let planned_query_templates =
        scientific_metadata_query_plan_provider_query_templates(provider_id, normalized_query, normalized_query_goal);
    let planned_filters = scientific_metadata_query_plan_provider_filters(
        provider_id,
        require_open_access,
        require_doi,
        year_from,
        year_to,
    );
    let planned_result_fields = scientific_metadata_query_plan_provider_result_fields(provider_id);
    let planned_sorting = scientific_metadata_query_plan_provider_sorting(provider_id);
    let planned_dedup_keys = scientific_metadata_query_plan_provider_dedup_keys(provider_id);
    let planned_rate_limit_notes = scientific_metadata_query_plan_provider_rate_limit_notes(provider_id);
    let planned_attribution_notes = scientific_metadata_query_plan_provider_attribution_notes(provider_id);
    let institutional_boundary_notes =
        scientific_metadata_query_plan_provider_institutional_boundary_notes(provider_id);
    let safety_boundary_notes = scientific_metadata_query_plan_provider_safety_notes();

    ScholarChatScientificMetadataQueryPlanProviderRecord {
        provider_id: provider_id.to_string(),
        provider_label: scientific_metadata_provider_label(provider_id).to_string(),
        provider_family,
        access_kind_label,
        selected,
        supported: true,
        normalized_query: normalized_query.to_string(),
        query_goal: normalized_query_goal.clone(),
        planned_query_terms,
        planned_query_templates,
        planned_filters,
        planned_result_fields,
        planned_sorting,
        planned_dedup_keys,
        planned_rate_limit_notes,
        planned_attribution_notes,
        institutional_boundary_notes,
        safety_boundary_notes,
        can_execute_now: false,
        will_call_provider: false,
        will_build_url: false,
        will_use_http_client: false,
        will_read_api_key: false,
        will_read_environment: false,
        will_write_metadata: false,
        summary: if selected {
            format!(
                "{} remains preview-only and only outlines later query templates, filters, and result fields.",
                scientific_metadata_provider_label(provider_id)
            )
        } else {
            format!(
                "{} remains preview-only and can be selected later for query planning.",
                scientific_metadata_provider_label(provider_id)
            )
        },
    }
}

fn scientific_metadata_query_plan_status(
    normalized_query: &str,
    normalized_query_goal: &Option<String>,
    provider_config_status: &ScholarChatScientificMetadataProviderConfigStatus,
    selected_provider_ids: &[String],
    normalized_provider_override: &Option<Vec<String>>,
    unknown_provider_ids: &[String],
    execution_requested: bool,
    allow_network: bool,
    allow_provider_terms_unreviewed: bool,
) -> ScholarChatScientificMetadataQueryPlanStatus {
    if normalized_query.trim().is_empty() {
        return ScholarChatScientificMetadataQueryPlanStatus::Blocked;
    }
    if matches!(
        provider_config_status,
        ScholarChatScientificMetadataProviderConfigStatus::Blocked
    ) {
        return ScholarChatScientificMetadataQueryPlanStatus::Blocked;
    }
    if selected_provider_ids.is_empty()
        && !unknown_provider_ids.is_empty()
        && normalized_provider_override
            .as_ref()
            .map(|values| !values.is_empty())
            .unwrap_or(false)
    {
        return ScholarChatScientificMetadataQueryPlanStatus::UnsupportedProvider;
    }
    if selected_provider_ids.is_empty() {
        return ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection;
    }
    match provider_config_status {
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
        | ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview => {
            return ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview => {
            return ScholarChatScientificMetadataQueryPlanStatus::NeedsNetworkPolicyReview
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview => {
            return ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderTermsReview
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview => {
            return ScholarChatScientificMetadataQueryPlanStatus::NeedsInstitutionalAccessReview
        }
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection => {
            return ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection
        }
        ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady
        | ScholarChatScientificMetadataProviderConfigStatus::UnsupportedProvider => {}
        ScholarChatScientificMetadataProviderConfigStatus::Blocked => {
            return ScholarChatScientificMetadataQueryPlanStatus::Blocked
        }
    }
    if normalized_query_goal.is_none() {
        return ScholarChatScientificMetadataQueryPlanStatus::NeedsQueryGoal;
    }
    if execution_requested && !allow_network {
        return ScholarChatScientificMetadataQueryPlanStatus::NeedsNetworkPolicyReview;
    }
    if !allow_provider_terms_unreviewed
        && selected_provider_ids
            .iter()
            .any(|provider_id| scientific_metadata_provider_requires_attribution(provider_id))
    {
        return ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderTermsReview;
    }
    if selected_provider_ids
        .iter()
        .any(|provider_id| scientific_metadata_provider_requires_institutional_access_review_later(provider_id))
    {
        return ScholarChatScientificMetadataQueryPlanStatus::NeedsInstitutionalAccessReview;
    }
    ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady
}

fn scientific_metadata_query_plan_strategy(
    status: &ScholarChatScientificMetadataQueryPlanStatus,
    selected_provider_ids: &[String],
) -> ScholarChatScientificMetadataQueryPlanStrategy {
    match status {
        ScholarChatScientificMetadataQueryPlanStatus::Blocked => {
            ScholarChatScientificMetadataQueryPlanStrategy::Blocked
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig
        | ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection
        | ScholarChatScientificMetadataQueryPlanStatus::NeedsQueryGoal
        | ScholarChatScientificMetadataQueryPlanStatus::NeedsNetworkPolicyReview
        | ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderTermsReview
        | ScholarChatScientificMetadataQueryPlanStatus::NeedsInstitutionalAccessReview
        | ScholarChatScientificMetadataQueryPlanStatus::UnsupportedProvider => {
            ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst
        }
        ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady => {
            if selected_provider_ids
                .iter()
                .any(|provider_id| matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo"))
            {
                ScholarChatScientificMetadataQueryPlanStrategy::InstitutionalBoundaryQueryPlan
            } else if selected_provider_ids.is_empty() {
                ScholarChatScientificMetadataQueryPlanStrategy::QueryPlanPreviewOnly
            } else {
                ScholarChatScientificMetadataQueryPlanStrategy::PublicMetadataQueryPlan
            }
        }
    }
}

pub fn preview_scholar_chat_scientific_metadata_query_plan(
    root: impl Into<PathBuf>,
    request: ScholarChatScientificMetadataQueryPlanPreviewRequest,
) -> AegisResult<ScholarChatScientificMetadataQueryPlanPreview> {
    let root = root.into();
    let normalized_query = normalize_scientific_query_text(&request.query);
    let normalized_mode = normalize_scientific_mode(request.mode.clone());
    let normalized_context_tags = normalize_scientific_context_tags(request.context_tags.clone());
    let normalized_preferred_metadata_sources =
        normalize_scientific_metadata_provider_ids(request.preferred_metadata_sources.clone());
    let normalized_preferred_psychology_source_families =
        normalize_psychology_connector_values(request.preferred_psychology_source_families.clone());
    let normalized_provider_override =
        normalize_scientific_metadata_provider_ids(request.provider_override.clone());
    let normalized_query_goal = request
        .query_goal
        .as_deref()
        .map(normalize_scientific_topic_text)
        .filter(|value| !value.is_empty());

    let provider_config_preview = preview_scholar_chat_scientific_metadata_provider_config(
        &root,
        ScholarChatScientificMetadataProviderConfigPreviewRequest {
            query: normalized_query.clone(),
            mode: Some(normalized_mode.clone()),
            context_tags: Some(normalized_context_tags.clone()),
            preferred_metadata_sources: Some(normalized_preferred_metadata_sources.clone()),
            preferred_psychology_source_families: Some(
                normalized_preferred_psychology_source_families.clone(),
            ),
            provider_override: if normalized_provider_override.is_empty() {
                None
            } else {
                Some(normalized_provider_override.clone())
            },
            require_open_access: request.require_open_access,
            require_doi: request.require_doi,
            year_from: request.year_from,
            year_to: request.year_to,
            config_goal: normalized_query_goal.clone(),
            include_disabled_providers: Some(request.include_disabled_providers),
            include_institutional_providers: Some(request.include_institutional_providers),
            include_rate_limit_notes: Some(request.include_rate_limit_notes),
            include_attribution_requirements: Some(request.include_attribution_requirements),
            execution_requested: request.execution_requested,
            allow_network: request.allow_network,
            allow_provider_terms_unreviewed: request.allow_provider_terms_unreviewed,
            allow_metadata_record_write: request.allow_metadata_record_write,
        },
    )?;

    let provider_config_status = provider_config_preview.status.clone();
    let provider_config_strategy = provider_config_preview.provider_config_strategy.clone();
    let selected_provider_ids = provider_config_preview.selected_provider_ids.clone();
    let selected_provider_count = selected_provider_ids.len();
    let supported_provider_ids = scientific_metadata_supported_providers();
    let public_metadata_provider_ids = scientific_metadata_query_supported_public_providers();
    let institutional_boundary_provider_ids =
        scientific_metadata_query_supported_institutional_providers();
    let provider_query_plan_ids = if request.include_disabled_providers {
        supported_provider_ids.clone()
    } else {
        selected_provider_ids.clone()
    };
    let provider_query_plan_ids = provider_query_plan_ids
        .into_iter()
        .filter(|provider_id| {
            request.include_institutional_providers
                || !matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo")
                || selected_provider_ids.contains(provider_id)
        })
        .collect::<Vec<_>>();

    let query_plan_status = scientific_metadata_query_plan_status(
        &normalized_query,
        &normalized_query_goal,
        &provider_config_status,
        &selected_provider_ids,
        &Some(normalized_provider_override.clone()),
        &provider_config_preview.unknown_provider_ids,
        request.execution_requested,
        request.allow_network,
        request.allow_provider_terms_unreviewed,
    );
    let query_plan_strategy =
        scientific_metadata_query_plan_strategy(&query_plan_status, &selected_provider_ids);

    let mut provider_query_plans = provider_query_plan_ids
        .iter()
        .map(|provider_id| {
            scientific_metadata_query_plan_provider_record(
                provider_id,
                selected_provider_ids.contains(provider_id),
                &normalized_query,
                &normalized_query_goal,
                &normalized_context_tags,
                &normalized_preferred_metadata_sources,
                &normalized_preferred_psychology_source_families,
                request.require_open_access.unwrap_or(false),
                request.require_doi.unwrap_or(false),
                request.year_from,
                request.year_to,
            )
        })
        .collect::<Vec<_>>();
    provider_query_plans.sort_by_key(|record| {
        supported_provider_ids
            .iter()
            .position(|provider_id| provider_id == &record.provider_id)
            .unwrap_or(usize::MAX)
    });
    provider_query_plans.dedup_by(|left, right| left.provider_id == right.provider_id);

    let query_template_plan = ScholarChatScientificMetadataQueryTemplatePlan {
        provider_ids: provider_query_plans
            .iter()
            .map(|record| record.provider_id.clone())
            .collect(),
        public_metadata_provider_ids: public_metadata_provider_ids.clone(),
        institutional_boundary_provider_ids: institutional_boundary_provider_ids.clone(),
        normalized_query: normalized_query.clone(),
        normalized_query_goal: normalized_query_goal.clone(),
        planned_query_templates: provider_query_plans
            .iter()
            .flat_map(|record| record.planned_query_templates.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect(),
        query_template_preview_only: true,
        will_build_urls: false,
        will_call_providers: false,
        summary: if normalized_query_goal.is_some() {
            "Query template planning remains preview-only and only outlines later per-provider metadata queries without URL building or provider calls."
                .to_string()
        } else {
            "Query template planning remains preview-only and is waiting for a query goal before later provider-specific planning can be accepted."
                .to_string()
        },
    };

    let filter_plan = ScholarChatScientificMetadataQueryFilterPlan {
        require_open_access: request.require_open_access.unwrap_or(false),
        require_doi: request.require_doi.unwrap_or(false),
        year_from: request.year_from,
        year_to: request.year_to,
        planned_filter_keys: provider_query_plans
            .iter()
            .flat_map(|record| record.planned_filters.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect(),
        provider_filter_coverage: provider_query_plans
            .iter()
            .map(|record| {
                format!(
                    "{}: {}",
                    record.provider_id,
                    record.planned_filters.join(", ")
                )
            })
            .collect(),
        filter_plan_preview_only: true,
        summary: "Filter planning remains preview-only and only outlines later DOI, open-access, and publication-year filters."
            .to_string(),
    };

    let result_field_plan = ScholarChatScientificMetadataQueryResultFieldPlan {
        planned_common_fields: vec![
            "title".to_string(),
            "authors".to_string(),
            "publication_year".to_string(),
            "provider_attribution".to_string(),
        ],
        provider_specific_fields: provider_query_plans
            .iter()
            .flat_map(|record| record.planned_result_fields.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect(),
        attribution_fields: vec![
            "provider_name".to_string(),
            "provider_record_id".to_string(),
            "source_family".to_string(),
            "provider_terms_note".to_string(),
        ],
        result_field_plan_preview_only: true,
        will_emit_citations_now: false,
        will_write_metadata_now: false,
        summary: "Result-field planning remains preview-only and only outlines later stable identifiers, titles, authors, years, and provider attribution fields."
            .to_string(),
    };

    let provider_family_boundary = ScholarChatScientificMetadataQueryProviderFamilyBoundary {
        public_metadata_provider_ids: public_metadata_provider_ids.clone(),
        institutional_boundary_provider_ids: institutional_boundary_provider_ids.clone(),
        institutional_access_requires_manual_review_later: true,
        authenticated_access_blocked_now: true,
        automated_scraping_blocked_now: true,
        paywall_bypass_blocked_now: true,
        summary: "Public metadata providers stay separate from APA PsycNet and PsycINFO institutional boundaries, and no automated authenticated access or paywall bypass is attempted now."
            .to_string(),
    };

    let safety_boundary = ScholarChatScientificMetadataQuerySafetyBoundary {
        external_services_blocked_now: true,
        url_building_blocked_now: true,
        connector_calls_blocked_now: true,
        network_blocked_now: true,
        http_client_blocked_now: true,
        api_key_reads_blocked_now: true,
        environment_reads_blocked_now: true,
        scraping_blocked_now: true,
        authenticated_library_access_blocked_now: true,
        paywall_bypass_blocked_now: true,
        metadata_writes_blocked_now: true,
        source_import_blocked_now: true,
        summary: "Scientific metadata query planning remains dry-run only; no external services, URL building, connector calls, network requests, authenticated access, scraping, metadata writes, or source import are attempted now."
            .to_string(),
    };

    let mut blockers = provider_config_preview.blockers.clone();
    let mut warnings = provider_config_preview.warnings.clone();
    let mut next_required_actions = provider_config_preview.next_required_actions.clone();

    if normalized_query.is_empty() {
        push_unique_text(&mut blockers, "query_missing");
        push_unique_text(
            &mut next_required_actions,
            "Provide a scientific query before metadata query planning can continue.",
        );
    }
    if selected_provider_ids.is_empty() {
        if !provider_config_preview.unknown_provider_ids.is_empty()
            && !normalized_provider_override.is_empty()
        {
            push_unique_text(&mut blockers, "unsupported_provider");
            push_unique_text(
                &mut warnings,
                "Unknown provider overrides are preserved only as preview-only hints.",
            );
            push_unique_text(
                &mut next_required_actions,
                "Choose a supported metadata provider before metadata query planning can continue.",
            );
        } else {
            push_unique_text(&mut blockers, "provider_selection_missing");
            push_unique_text(
                &mut next_required_actions,
                "Select one or more supported providers before metadata query planning can continue.",
            );
        }
    }
    if normalized_query_goal.is_none() && !selected_provider_ids.is_empty() {
        push_unique_text(&mut blockers, "query_goal_missing");
        push_unique_text(
            &mut next_required_actions,
            "Provide a query goal before metadata query planning can continue.",
        );
    }
    if matches!(
        provider_config_status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
            | ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview
    ) {
        push_unique_text(&mut blockers, "provider_config_ready_missing");
        push_unique_text(
            &mut next_required_actions,
            "Keep the upstream scientific metadata provider config preview ready before query planning can continue.",
        );
    }
    if matches!(
        provider_config_status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview
    ) && !request.allow_network
    {
        push_unique_text(&mut blockers, "network_policy_review_required");
        push_unique_text(
            &mut next_required_actions,
            "Allow network access before later provider query planning can continue.",
        );
    }
    if matches!(
        provider_config_status,
        ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview
    ) || (!request.allow_provider_terms_unreviewed
        && selected_provider_ids
            .iter()
            .any(|provider_id| scientific_metadata_provider_requires_attribution(provider_id)))
    {
        push_unique_text(&mut blockers, "provider_terms_review_required");
        push_unique_text(
            &mut next_required_actions,
            "Review provider terms before later provider query planning can continue.",
        );
    }
    if selected_provider_ids
        .iter()
        .any(|provider_id| scientific_metadata_provider_requires_institutional_access_review_later(provider_id))
    {
        push_unique_text(&mut blockers, "institutional_access_review_required");
        push_unique_text(
            &mut next_required_actions,
            "Review APA PsycNet or PsycINFO institutional access before later provider query planning can continue.",
        );
        push_unique_text(
            &mut warnings,
            "Institutional providers remain a later manual-access boundary and are never executed automatically.",
        );
    }
    if request.execution_requested && !request.allow_network {
        push_unique_text(
            &mut warnings,
            "Execution was requested but network access is disabled in this preview-only phase.",
        );
    }
    if request.include_disabled_providers {
        push_unique_text(
            &mut warnings,
            "Disabled providers remain preview-only and are included in the query-plan records for inspection.",
        );
    }
    if request.include_institutional_providers {
        push_unique_text(
            &mut warnings,
            "Institutional boundary providers remain preview-only and are included in the query-plan records for inspection.",
        );
    }
    if request.include_rate_limit_notes {
        push_unique_text(
            &mut warnings,
            "Rate-limit notes remain preview-only and are not enforced now.",
        );
    }
    if request.include_attribution_requirements {
        push_unique_text(
            &mut warnings,
            "Attribution requirements remain preview-only and no citations are emitted now.",
        );
    }

    let planned_query_steps = vec![
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::ProviderConfigAlignment,
            "provider_config_alignment",
            "Provider config alignment",
            "Align later query planning with the existing scientific metadata provider config preview only.",
            vec![
                format!("{:?}", provider_config_status),
                format!("{:?}", provider_config_strategy),
                normalized_query.clone(),
            ],
            vec!["provider_config_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::ProviderFamilyPartition,
            "provider_family_partition",
            "Provider family partition",
            "Separate public metadata providers from APA PsycNet and PsycINFO institutional boundaries.",
            vec![
                public_metadata_provider_ids.join(", "),
                institutional_boundary_provider_ids.join(", "),
            ],
            vec!["provider_family_partition_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::QueryTemplatePlanning,
            "query_template_planning",
            "Query template planning",
            "Plan later provider-specific query templates without building URLs or calling providers.",
            vec![
                normalized_query.clone(),
                normalized_query_goal.clone().unwrap_or_default(),
                normalized_context_tags.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["query_template_plan_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::FilterPlanning,
            "filter_planning",
            "Filter planning",
            "Plan later DOI, open-access, and publication-year filters without executing retrieval.",
            vec![
                format!("require_open_access={}", request.require_open_access.unwrap_or(false)),
                format!("require_doi={}", request.require_doi.unwrap_or(false)),
                request.year_from.map(|value| value.to_string()).unwrap_or_default(),
                request.year_to.map(|value| value.to_string()).unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["filter_plan_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::ResultFieldPlanning,
            "result_field_planning",
            "Result-field planning",
            "Plan later stable identifiers and provider attribution fields without emitting citations or writing metadata.",
            vec![
                normalized_preferred_metadata_sources.join(", "),
                normalized_preferred_psychology_source_families.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["result_field_plan_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::RateLimitAlignment,
            "rate_limit_alignment",
            "Rate-limit alignment",
            "Plan later rate-limit review without enforcement now.",
            vec![
                format!("include_rate_limit_notes={}", request.include_rate_limit_notes),
                selected_provider_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["rate_limit_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::AttributionAlignment,
            "attribution_alignment",
            "Attribution alignment",
            "Plan later attribution requirements while citations stay disabled now.",
            vec![
                format!("include_attribution_requirements={}", request.include_attribution_requirements),
                selected_provider_ids.join(", "),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["attribution_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::SafetyBoundaryCheck,
            "safety_boundary_check",
            "Safety boundary check",
            "Confirm that the scientific metadata query plan remains dry-run only and never executes provider calls.",
            vec![
                format!("{:?}", provider_config_status),
                format!("{:?}", query_plan_status),
                format!("execution_requested={}", request.execution_requested),
                format!("allow_network={}", request.allow_network),
            ],
            vec!["safety_boundary_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::DownstreamMetadataConnectorAlignment,
            "downstream_metadata_connector_alignment",
            "Downstream metadata connector alignment",
            "Align future metadata connector phases with this preview only; no connector is called now.",
            vec![
                format!("{:?}", provider_config_strategy),
                selected_provider_ids.join(", "),
            ],
            vec!["downstream_metadata_connector_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::DownstreamEvidencePackAlignment,
            "downstream_evidence_pack_alignment",
            "Downstream Evidence Pack alignment",
            "Plan later Evidence Pack alignment only; no Evidence Pack is created now.",
            vec![
                normalized_query.clone(),
                normalized_query_goal.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_evidence_pack_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
        scientific_metadata_query_plan_step(
            ScholarChatScientificMetadataQueryPlanStepKind::DownstreamLiteratureReviewAlignment,
            "downstream_literature_review_alignment",
            "Downstream literature review alignment",
            "Plan later Literature Review alignment only; no Literature Review is created now.",
            vec![
                normalized_mode.clone(),
                normalized_query_goal.clone().unwrap_or_default(),
            ]
            .into_iter()
            .filter(|value| !value.is_empty())
            .collect(),
            vec!["downstream_literature_review_alignment_preview".to_string()],
            !matches!(query_plan_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked),
        ),
    ];

    let summary = match query_plan_status {
        ScholarChatScientificMetadataQueryPlanStatus::Blocked => {
            if normalized_query.trim().is_empty() {
                "Scientific metadata query plan preview is blocked because the query is blank."
                    .to_string()
            } else {
                "Scientific metadata query plan preview is blocked until the upstream provider config preview is ready later."
                    .to_string()
            }
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig => {
            "Scientific metadata query plan preview needs provider config readiness before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection => {
            "Scientific metadata query plan preview needs supported provider selection before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsQueryGoal => {
            "Scientific metadata query plan preview needs a query goal before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsNetworkPolicyReview => {
            "Scientific metadata query plan preview needs network policy review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderTermsReview => {
            "Scientific metadata query plan preview needs provider terms review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::NeedsInstitutionalAccessReview => {
            "Scientific metadata query plan preview needs institutional access review before later planning can continue."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::UnsupportedProvider => {
            "Scientific metadata query plan preview contains only unsupported provider hints and needs a supported provider later."
                .to_string()
        }
        ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady => {
            let family_summary = if selected_provider_ids
                .iter()
                .any(|provider_id| matches!(provider_id.as_str(), "apa_psycnet" | "psycinfo"))
            {
                "institutional boundary"
            } else {
                "public metadata"
            };
            format!(
                "Scientific metadata query plan preview is ready later and only describes {:?} planning over {} {} provider(s).",
                query_plan_strategy,
                selected_provider_count,
                family_summary
            )
        }
    };

    Ok(ScholarChatScientificMetadataQueryPlanPreview {
        status: query_plan_status,
        normalized_query,
        normalized_mode: Some(normalized_mode),
        normalized_context_tags,
        normalized_preferred_metadata_sources,
        normalized_preferred_psychology_source_families,
        normalized_provider_override: if normalized_provider_override.is_empty() {
            None
        } else {
            Some(normalized_provider_override)
        },
        unknown_provider_ids: provider_config_preview.unknown_provider_ids.clone(),
        normalized_query_goal,
        include_disabled_providers: request.include_disabled_providers,
        include_institutional_providers: request.include_institutional_providers,
        include_rate_limit_notes: request.include_rate_limit_notes,
        include_attribution_requirements: request.include_attribution_requirements,
        include_query_templates: request.include_query_templates,
        include_filter_plan: request.include_filter_plan,
        include_result_field_plan: request.include_result_field_plan,
        execution_requested: request.execution_requested,
        allow_network: request.allow_network,
        allow_provider_terms_unreviewed: request.allow_provider_terms_unreviewed,
        allow_metadata_record_write: request.allow_metadata_record_write,
        provider_config_status,
        provider_config_strategy,
        selected_provider_ids,
        selected_provider_count,
        public_metadata_provider_ids,
        institutional_boundary_provider_ids,
        query_plan_strategy,
        provider_query_plans,
        query_template_plan,
        filter_plan,
        result_field_plan,
        provider_family_boundary,
        safety_boundary,
        planned_query_steps,
        blockers,
        warnings,
        next_required_actions,
        summary,
        preview_only: true,
        metadata_query_plan_preview_only: true,
        dry_run_only: true,
        execution_disabled: true,
        no_network_call: true,
        no_web_request: true,
        no_http_client: true,
        no_api_key_read: true,
        no_environment_read: true,
        no_scraping: true,
        no_authenticated_library_access: true,
        no_paywall_bypass: true,
        no_connector_call: true,
        no_source_import: true,
        no_metadata_record_write: true,
        no_metadata_persistence: true,
        no_local_file_indexing: true,
        no_file_read: true,
        no_pdf_extraction: true,
        no_ocr: true,
        no_chunking_run: true,
        no_embedding_generation: true,
        no_index_created: true,
        no_retrieval_execution: true,
        no_model_loading: true,
        no_runtime_inference: true,
        no_llm_call: true,
        no_answer_generated: true,
        no_literature_review_created: true,
        no_evidence_pack_created: true,
        no_artifact_write: true,
        no_persistence: true,
        no_registry_status_change: true,
        no_audit_write: true,
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::local_runtime::{
        LocalModelRuntimeKind,
        LocalRuntimeAdapterContractPreviewRequest,
        LocalRuntimeAdapterKind,
        LocalRuntimeCapabilityPreviewRequest,
        LocalRuntimeCapabilityStatus,
        LocalRuntimeProbeReadinessPreviewRequest,
        LocalRuntimeProbeReadinessStatus,
        LocalRuntimeSmokeExecutionPlanPreviewRequest,
        LocalRuntimeSmokeExecutionPlanStatus,
        LocalRuntimeSmokeReadinessPreviewRequest,
        LocalRuntimeSmokeReadinessStatus,
        LocalRuntimeValidationPreviewRequest,
        LocalRuntimeValidationStatus,
        LocalRuntimeVersionProbePreviewRequest,
        LocalRuntimeVersionProbeStatus,
    };
    use std::{env, fs, path::PathBuf, process::Command};

    fn request(prompt: &str) -> ScholarChatRequest {
        ScholarChatRequest {
            prompt: prompt.to_string(),
            mode: ScholarChatMode::LectureLearning,
            grounding_policy: GroundingPolicy::LocalFirst,
            selected_source_ids: vec![" src_demo ".to_string()],
        }
    }

    fn grounding_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
    ) -> ScholarChatDraftGroundingInspectionRequest {
        ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: prompt.to_string(),
                mode: ScholarChatMode::LectureLearning,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids,
            },
            draft_text: draft_text.map(|value| value.to_string()),
            max_items: Some(4),
        }
    }

    fn build_intent_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
        answer_draft_id: Option<&str>,
        explicit_user_intent: bool,
    ) -> ScholarChatGroundedAnswerBuildIntentRequest {
        ScholarChatGroundedAnswerBuildIntentRequest {
            grounding_request: grounding_request(prompt, draft_text, selected_source_ids),
            answer_draft_id: answer_draft_id.map(|value| value.to_string()),
            explicit_user_intent,
        }
    }

    fn build_request_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
        answer_draft_id: Option<&str>,
        explicit_user_intent: bool,
    ) -> ScholarChatGroundedAnswerBuildRequestPreviewRequest {
        ScholarChatGroundedAnswerBuildRequestPreviewRequest {
            build_intent_request: build_intent_request(
                prompt,
                draft_text,
                selected_source_ids,
                answer_draft_id,
                explicit_user_intent,
            ),
        }
    }

    fn build_preflight_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
        answer_draft_id: Option<&str>,
        explicit_user_intent: bool,
    ) -> ScholarChatGroundedAnswerBuildPreflightPreviewRequest {
        ScholarChatGroundedAnswerBuildPreflightPreviewRequest {
            build_request_preview_request: build_request_request(
                prompt,
                draft_text,
                selected_source_ids,
                answer_draft_id,
                explicit_user_intent,
            ),
        }
    }

    fn execution_readiness_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
        answer_draft_id: Option<&str>,
        explicit_user_intent: bool,
        execution_consent: bool,
    ) -> ScholarChatGroundedAnswerExecutionReadinessPreviewRequest {
        ScholarChatGroundedAnswerExecutionReadinessPreviewRequest {
            build_preflight_preview_request: build_preflight_request(
                prompt,
                draft_text,
                selected_source_ids,
                answer_draft_id,
                explicit_user_intent,
            ),
            execution_consent,
        }
    }

    fn execution_plan_request(
        prompt: &str,
        draft_text: Option<&str>,
        selected_source_ids: Vec<String>,
        answer_draft_id: Option<&str>,
        explicit_user_intent: bool,
        execution_consent: bool,
    ) -> ScholarChatGroundedAnswerExecutionPlanPreviewRequest {
        ScholarChatGroundedAnswerExecutionPlanPreviewRequest {
            execution_readiness_preview_request: execution_readiness_request(
                prompt,
                draft_text,
                selected_source_ids,
                answer_draft_id,
                explicit_user_intent,
                execution_consent,
            ),
        }
    }

    #[test]
    fn scholar_chat_preview_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_request(temp.path(), request("   "));
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_preview_trims_prompt_and_sources_deterministically() {
        let temp = tempfile::tempdir().unwrap();
        let first = preview_scholar_chat_request(temp.path(), request("  Explain retrieval  ")).unwrap();
        let second = preview_scholar_chat_request(temp.path(), request("  Explain retrieval  ")).unwrap();
        assert_eq!(first, second);
        assert_eq!(first.normalized_prompt, "Explain retrieval");
        assert_eq!(first.selected_source_ids, vec!["src_demo"]);
        assert_eq!(first.status, ScholarChatStatus::PreviewOnly);
    }

    #[test]
    fn scholar_chat_preview_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let mut request = request("Explain alpha");
            request.selected_source_ids = vec![invalid.to_string()];
            let result = preview_scholar_chat_request(temp.path(), request);
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_preview_allows_no_selected_sources_with_warning() {
        let temp = tempfile::tempdir().unwrap();
        let mut request = request("Explain alpha");
        request.selected_source_ids = Vec::new();
        let response = preview_scholar_chat_request(temp.path(), request).unwrap();
        assert_eq!(response.selected_source_count, 0);
        assert!(response.warnings.iter().any(|warning| warning.contains("No selected sources")));
        assert!(!response.grounding_plan.retrieval_would_run);
    }

    #[test]
    fn scholar_chat_preview_local_only_policy_is_visible_in_warnings_and_plan() {
        let temp = tempfile::tempdir().unwrap();
        let mut request = request("Explain alpha");
        request.grounding_policy = GroundingPolicy::LocalOnly;
        let response = preview_scholar_chat_request(temp.path(), request).unwrap();
        assert!(response.grounding_plan.local_corpus_required);
        assert!(response.warnings.iter().any(|warning| warning.contains("local_only")));
    }

    #[test]
    fn scholar_chat_preview_output_is_path_free_and_non_mutating() {
        let temp = tempfile::tempdir().unwrap();
        let response = preview_scholar_chat_request(temp.path(), request("Explain alpha")).unwrap();
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert!(!temp.path().join(".aegis").exists());
        assert_eq!(fs::read_dir(temp.path()).unwrap().count(), 0);
    }

    fn retrieval_request(prompt: &str, selected_source_ids: Vec<String>) -> ScholarChatRequest {
        ScholarChatRequest {
            prompt: prompt.to_string(),
            mode: ScholarChatMode::LectureLearning,
            grounding_policy: GroundingPolicy::LocalFirst,
            selected_source_ids,
        }
    }

    fn evidence_plan_request(prompt: &str, selected_source_ids: Vec<String>) -> ScholarChatRequest {
        retrieval_request(prompt, selected_source_ids)
    }

    fn prompt_pack_request(prompt: &str, selected_source_ids: Vec<String>) -> ScholarChatRequest {
        ScholarChatRequest {
            prompt: prompt.to_string(),
            mode: ScholarChatMode::ThesisWriting,
            grounding_policy: GroundingPolicy::LocalOnly,
            selected_source_ids,
        }
    }

    fn runtime_config(model_path: Option<&str>, executable_path: Option<&str>) -> LocalModelRuntimeConfig {
        LocalModelRuntimeConfig {
            runtime_kind: LocalModelRuntimeKind::LlamaCpp,
            model_path: model_path.map(|value| value.to_string()),
            executable_path: executable_path.map(|value| value.to_string()),
            context_window: Some(512),
            gpu_layers: Some(0),
            temperature: Some(0.0),
        }
    }

    fn answer_readiness_request(
        prompt: &str,
        grounding_policy: GroundingPolicy,
        selected_source_ids: Vec<String>,
        runtime_config: LocalModelRuntimeConfig,
        allow_model_execution: bool,
    ) -> ScholarChatAnswerReadinessRequest {
        ScholarChatAnswerReadinessRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: prompt.to_string(),
                mode: ScholarChatMode::ThesisWriting,
                grounding_policy,
                selected_source_ids,
            },
            runtime_config,
            allow_model_execution,
        }
    }

    fn build_source_with_index(temp: &tempfile::TempDir, text: &str) -> String {
        let source_path = temp.path().join("note.md");
        fs::write(&source_path, text).unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();
        RetrievalService::new(temp.path())
            .build_index(&source.source_id)
            .unwrap();
        source.source_id
    }

    fn build_readable_answer_draft_fixture(temp: &tempfile::TempDir) -> (String, String, String, usize) {
        let source_id = build_source_with_index(temp, "alpha beta gamma\nalpha beta delta\n");
        let evidence = crate::evidence::EvidenceService::new(temp.path())
            .build_evidence_pack(&source_id, "alpha grounded evidence", 4)
            .unwrap();
        let draft = crate::answer_draft::AnswerDraftService::new(temp.path())
            .build_answer_draft(&source_id, &evidence.evidence_pack_id)
            .unwrap();
        (source_id, draft.answer_draft_id, draft.version_id, draft.claim_count)
    }

    fn build_runtime_fixture(temp: &tempfile::TempDir) -> LocalModelRuntimeConfig {
        let model_path = temp.path().join("ready-model.gguf");
        let executable_path = temp.path().join("ready-smoke-helper.exe");
        fs::write(&model_path, "gguf placeholder").unwrap();
        fs::write(&executable_path, "runtime placeholder").unwrap();
        runtime_config(
            Some(model_path.to_string_lossy().as_ref()),
            Some(executable_path.to_string_lossy().as_ref()),
        )
    }

    fn smoke_helper_executable(temp: &tempfile::TempDir) -> PathBuf {
        let source_path = temp.path().join("smoke_helper.rs");
        let executable_path = temp.path().join(if cfg!(windows) { "smoke_helper.exe" } else { "smoke_helper" });
        let source = r#"
use std::{env, thread, time::Duration};

fn prompt_argument(args: &[String]) -> String {
    args.windows(2)
        .find(|pair| pair[0] == "-p" || pair[0] == "--prompt")
        .map(|pair| pair[1].clone())
        .unwrap_or_default()
}

fn main() {
    let args: Vec<String> = env::args().collect();
    let prompt = prompt_argument(&args);
    println!("stdout marker");
    println!("args={}", args.join(" | "));
    println!("{}", "S".repeat(5000));
    eprintln!("stderr marker");
    eprintln!("args={}", args.join(" | "));
    eprintln!("{}", "E".repeat(5000));
    if prompt.contains("SLEEP") {
        thread::sleep(Duration::from_millis(700));
    }
    if prompt.contains("FAIL") {
        std::process::exit(7);
    }
}
"#;
        fs::write(&source_path, source).unwrap();
        let rustc = env::var("RUSTC").unwrap_or_else(|_| "rustc".to_string());
        let status = Command::new(rustc)
            .arg("--crate-type")
            .arg("bin")
            .arg("--edition")
            .arg("2021")
            .arg(&source_path)
            .arg("-o")
            .arg(&executable_path)
            .status()
            .unwrap();
        assert!(status.success());
        executable_path
    }

    fn build_draft_runtime_fixture(temp: &tempfile::TempDir) -> LocalModelRuntimeConfig {
        let model_path = temp.path().join("draft-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        let executable_path = smoke_helper_executable(temp);
        runtime_config(
            Some(model_path.to_string_lossy().as_ref()),
            Some(executable_path.to_string_lossy().as_ref()),
        )
    }

    fn version_probe_helper_executable_with_source(
        temp: &tempfile::TempDir,
        executable_name: &str,
        source: &str,
    ) -> PathBuf {
        let source_path = temp.path().join(format!("{executable_name}.rs"));
        let executable_path = temp.path().join(executable_name);
        let crate_name = source_path
            .file_stem()
            .and_then(|value| value.to_str())
            .unwrap_or("runtime_diagnostic_bridge_helper")
            .chars()
            .map(|value| if value.is_ascii_alphanumeric() || value == '_' { value } else { '_' })
            .collect::<String>();
        fs::write(&source_path, source).unwrap();
        let rustc = env::var("RUSTC").unwrap_or_else(|_| "rustc".to_string());
        let status = Command::new(rustc)
            .arg("--crate-name")
            .arg(&crate_name)
            .arg("--edition=2021")
            .arg(&source_path)
            .arg("-o")
            .arg(&executable_path)
            .status()
            .unwrap();
        assert!(status.success());
        executable_path
    }

    fn runtime_diagnostic_bridge_helper_executable(temp: &tempfile::TempDir, executable_name: &str) -> PathBuf {
        let counter_path = temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt");
        let marker_path = temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt");
        let counter_literal = format!("{:?}", counter_path.to_string_lossy().to_string());
        let marker_literal = format!("{:?}", marker_path.to_string_lossy().to_string());
        let source = r#"
use std::{env, fs, path::PathBuf};

fn main() {
    let args: Vec<String> = env::args().collect();
    let exe_name = env::current_exe()
        .ok()
        .and_then(|path| path.file_name().and_then(|value| value.to_str()).map(|value| value.to_string()))
        .unwrap_or_default();
    if args.iter().any(|arg| matches!(arg.as_str(), "-p" | "--prompt" | "-n" | "--max_output_tokens" | "-m" | "--ctx-size" | "-ngl")) {
        let marker = PathBuf::from(MARKER_PATH);
        let _ = fs::write(marker, args.join(" | "));
        std::process::exit(91);
    }
    if args.iter().any(|arg| arg == "--version") {
        let counter = PathBuf::from(COUNTER_PATH);
        let current = fs::read_to_string(&counter)
            .ok()
            .and_then(|value| value.trim().parse::<usize>().ok())
            .unwrap_or(0);
        let _ = fs::write(counter, (current + 1).to_string());
        if exe_name.contains("fail") {
            std::process::exit(7);
        }
        println!("llama.cpp version 1.2.3");
        return;
    }
    println!("stdout marker");
    println!("args={}", args.join(" | "));
    eprintln!("stderr marker");
    eprintln!("args={}", args.join(" | "));
}
"#
        .replace("COUNTER_PATH", &counter_literal)
        .replace("MARKER_PATH", &marker_literal);
        version_probe_helper_executable_with_source(temp, executable_name, &source)
    }

    fn prepare_runtime_diagnostic_bridge_spies(temp: &tempfile::TempDir) {
        fs::write(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt"), "0").unwrap();
        fs::write(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"), "").unwrap();
    }

    fn count_entries_recursively(path: &std::path::Path) -> usize {
        fn inner(path: &std::path::Path, count: &mut usize) {
            if let Ok(entries) = fs::read_dir(path) {
                for entry in entries.flatten() {
                    *count += 1;
                    let entry_path = entry.path();
                    if entry_path.is_dir() {
                        inner(&entry_path, count);
                    }
                }
            }
        }

        let mut count = 0;
        inner(path, &mut count);
        count
    }

    fn runtime_diagnostic_bridge_request(
        prompt: &str,
        selected_source_ids: Vec<String>,
        executable_path: Option<&str>,
        model_path: Option<&str>,
        probe_consent: bool,
        allow_probe_execution: bool,
        smoke_consent: bool,
        diagnostic_prompt: Option<&str>,
        max_output_tokens: Option<u32>,
        timeout_ms: Option<u64>,
    ) -> ScholarChatRuntimeDiagnosticBridgePreviewRequest {
        ScholarChatRuntimeDiagnosticBridgePreviewRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: prompt.to_string(),
                mode: ScholarChatMode::LectureLearning,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids,
            },
            smoke_execution_plan_preview_request: LocalRuntimeSmokeExecutionPlanPreviewRequest {
                smoke_readiness_preview_request: LocalRuntimeSmokeReadinessPreviewRequest {
                    capability_preview_request: LocalRuntimeCapabilityPreviewRequest {
                        version_probe_preview_request: LocalRuntimeVersionProbePreviewRequest {
                            probe_readiness_preview_request: LocalRuntimeProbeReadinessPreviewRequest {
                                validation_preview_request: LocalRuntimeValidationPreviewRequest {
                                    adapter_contract_request: LocalRuntimeAdapterContractPreviewRequest {
                                        adapter_kind: LocalRuntimeAdapterKind::LlamaCpp,
                                        executable_path: executable_path.map(|value| value.to_string()),
                                        model_path: model_path.map(|value| value.to_string()),
                                        model_family: Some("llama".to_string()),
                                        model_format: Some("gguf".to_string()),
                                        context_window_tokens: Some(8192),
                                        gpu_layers: Some(0),
                                        threads: Some(8),
                                        batch_size: Some(256),
                                        chat_template: Some("template".to_string()),
                                    },
                                },
                                probe_consent,
                            },
                            allow_probe_execution,
                            timeout_ms,
                        },
                    },
                    smoke_consent,
                    diagnostic_prompt: diagnostic_prompt.map(|value| value.to_string()),
                    max_output_tokens,
                    timeout_ms,
                },
            },
        }
    }

    fn scientific_discipline_registry_request(
        topic: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
    ) -> ScholarChatScientificDisciplineRegistryPreviewRequest {
        ScholarChatScientificDisciplineRegistryPreviewRequest {
            topic: topic.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
        }
    }

    fn scientific_source_registry_request(
        topic: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
    ) -> ScholarChatScientificSourceRegistryPreviewRequest {
        ScholarChatScientificSourceRegistryPreviewRequest {
            topic: topic.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags.map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
        }
    }

    fn scientific_query_understanding_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
    ) -> ScholarChatScientificQueryUnderstandingPreviewRequest {
        ScholarChatScientificQueryUnderstandingPreviewRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags.map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
        }
    }

    fn scientific_search_plan_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
        selected_local_source_ids: Option<Vec<&str>>,
    ) -> ScholarChatScientificSearchPlanRequest {
        ScholarChatScientificSearchPlanRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            selected_local_source_ids: selected_local_source_ids
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
        }
    }

    fn scientific_local_literature_index_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
        selected_local_source_ids: Option<Vec<&str>>,
        expected_source_kinds: Option<Vec<&str>>,
    ) -> ScholarChatLocalLiteratureIndexRequest {
        ScholarChatLocalLiteratureIndexRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            selected_local_source_ids: selected_local_source_ids
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            expected_source_kinds: expected_source_kinds
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
        }
    }

    fn course_literature_registry_request(
        query: &str,
        course_context: Option<&str>,
        module_code: Option<&str>,
        course_title: Option<&str>,
        instructor: Option<&str>,
        semester: Option<&str>,
        context_tags: Option<Vec<&str>>,
        selected_local_source_ids: Option<Vec<&str>>,
        expected_course_material_kinds: Option<Vec<&str>>,
    ) -> ScholarChatCourseLiteratureRegistryPreviewRequest {
        ScholarChatCourseLiteratureRegistryPreviewRequest {
            query: query.to_string(),
            course_context: course_context.map(|value| value.to_string()),
            module_code: module_code.map(|value| value.to_string()),
            course_title: course_title.map(|value| value.to_string()),
            instructor: instructor.map(|value| value.to_string()),
            semester: semester.map(|value| value.to_string()),
            context_tags: context_tags
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            selected_local_source_ids: selected_local_source_ids
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            expected_course_material_kinds: expected_course_material_kinds
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
        }
    }

    fn metadata_connector_plan_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
        selected_local_source_ids: Option<Vec<&str>>,
        expected_source_kinds: Option<Vec<&str>>,
        preferred_metadata_sources: Option<Vec<&str>>,
        max_results_per_source: Option<u32>,
        require_open_access: Option<bool>,
        require_doi: Option<bool>,
        year_from: Option<u16>,
        year_to: Option<u16>,
    ) -> ScholarChatMetadataConnectorPlanRequest {
        ScholarChatMetadataConnectorPlanRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            selected_local_source_ids: selected_local_source_ids
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            expected_source_kinds: expected_source_kinds
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            preferred_metadata_sources: preferred_metadata_sources
                .map(|tags| tags.into_iter().map(|value| value.to_string()).collect()),
            max_results_per_source,
            require_open_access,
            require_doi,
            year_from,
            year_to,
        }
    }

    fn runtime_diagnostic_result_request(
        bridge_preview_request: ScholarChatRuntimeDiagnosticBridgePreviewRequest,
        diagnostic_preview: LocalRuntimeSmokeDiagnosticPreview,
    ) -> ScholarChatRuntimeDiagnosticResultPreviewRequest {
        ScholarChatRuntimeDiagnosticResultPreviewRequest {
            bridge_preview_request,
            diagnostic_preview,
        }
    }

    fn runtime_answer_pipeline_gate_request(
        grounded_answer_execution_plan_preview_request: ScholarChatGroundedAnswerExecutionPlanPreviewRequest,
        runtime_diagnostic_result_preview_request: ScholarChatRuntimeDiagnosticResultPreviewRequest,
    ) -> ScholarChatRuntimeAnswerPipelineGatePreviewRequest {
        ScholarChatRuntimeAnswerPipelineGatePreviewRequest {
            grounded_answer_execution_plan_preview_request,
            runtime_diagnostic_result_preview_request,
        }
    }

    fn runtime_diagnostic_preview_like_bridge(
        status: LocalRuntimeSmokeDiagnosticStatus,
        stdout_preview: &str,
        stderr_preview: &str,
    ) -> LocalRuntimeSmokeDiagnosticPreview {
        let _execution_attempted = matches!(
            status,
            LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded
                | LocalRuntimeSmokeDiagnosticStatus::SmokeFailed
                | LocalRuntimeSmokeDiagnosticStatus::TimedOut
        );
        let exit_code = match status {
            LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded => Some(0),
            LocalRuntimeSmokeDiagnosticStatus::SmokeFailed => Some(1),
            LocalRuntimeSmokeDiagnosticStatus::TimedOut | LocalRuntimeSmokeDiagnosticStatus::Blocked => None,
        };
        LocalRuntimeSmokeDiagnosticPreview {
            status: status.clone(),
            smoke_execution_plan_status: LocalRuntimeSmokeExecutionPlanStatus::PlanReadyLater,
            smoke_readiness_status: LocalRuntimeSmokeReadinessStatus::SmokeReadyLater,
            capability_status: LocalRuntimeCapabilityStatus::CapabilityReadyLater,
            version_probe_status: LocalRuntimeVersionProbeStatus::ProbeSucceeded,
            probe_readiness_status: LocalRuntimeProbeReadinessStatus::ProbeReadyLater,
            validation_status: LocalRuntimeValidationStatus::ValidationReadyLater,
            adapter_contract_status: LocalRuntimeAdapterContractStatus::ContractReadyLater,
            adapter_kind: LocalRuntimeAdapterKind::LlamaCpp,
            normalized_model_family: Some("llama".to_string()),
            normalized_model_format: "gguf".to_string(),
            safe_executable_file_name: Some("runtime_diagnostic_bridge_ready.exe".to_string()),
            safe_model_file_name: Some("runtime-diagnostic-bridge-model.gguf".to_string()),
            probe_consent: true,
            allow_probe_execution: true,
            smoke_consent: true,
            allow_smoke_execution: true,
            execution_attempted: matches!(
                status,
                LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded
                    | LocalRuntimeSmokeDiagnosticStatus::SmokeFailed
                    | LocalRuntimeSmokeDiagnosticStatus::TimedOut
            ),
            normalized_diagnostic_prompt: "Diagnostic smoke prompt.".to_string(),
            diagnostic_prompt_char_count: "Diagnostic smoke prompt.".chars().count(),
            max_output_tokens: 32,
            timeout_ms: 1_500,
            duration_ms: 12,
            exit_code,
            stdout_preview: stdout_preview.to_string(),
            stderr_preview: stderr_preview.to_string(),
            stdout_truncated: false,
            stderr_truncated: false,
            blockers: vec![],
            warnings: vec![],
            next_required_actions: vec![],
            summary: "Diagnostic preview only.".to_string(),
            diagnostic_only: true,
            not_scholar_chat_answer: true,
            no_answer_generated: true,
            no_grounding_applied: true,
            no_evidence_pack_used: true,
            no_persistence: true,
            no_artifact_write: true,
            no_registry_status_change: true,
            no_audit_write: true,
        }
    }

    fn draft_inference_request(
        prompt: &str,
        grounding_policy: GroundingPolicy,
        selected_source_ids: Vec<String>,
        runtime_config: LocalModelRuntimeConfig,
        allow_model_execution: bool,
        timeout_ms: Option<u64>,
        max_output_tokens: Option<u32>,
    ) -> ScholarChatDraftInferenceRequest {
        ScholarChatDraftInferenceRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: prompt.to_string(),
                mode: ScholarChatMode::ThesisWriting,
                grounding_policy,
                selected_source_ids,
            },
            runtime_config,
            allow_model_execution,
            timeout_ms,
            max_output_tokens,
        }
    }

    fn assert_readiness_boundary_fields(preview: &ScholarChatAnswerReadinessPreview) {
        assert!(!preview.would_generate_answer_now);
        assert!(!preview.would_build_evidence_pack_now);
        assert!(!preview.would_create_final_answer_now);
        assert!(preview.prompt_pack_ready);
    }

    fn assert_draft_boundary_fields(preview: &ScholarChatDraftInferencePreview) {
        assert!(preview.draft_only);
        assert!(preview.preview_only);
        assert!(preview.not_final_answer);
        assert!(preview.not_grounded_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
    }

    fn assert_draft_grounding_inspection_boundary_fields(
        preview: &ScholarChatDraftGroundingInspectionPreview,
    ) {
        assert!(preview.inspection_only);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
    }

    fn assert_grounded_draft_readiness_boundary_fields(
        preview: &ScholarChatGroundedDraftReadinessPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
    }

    fn assert_grounded_draft_readiness_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatDraftGroundingInspectionRequest,
    ) -> ScholarChatGroundedDraftReadinessPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_draft_readiness(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_draft_readiness(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_draft_readiness_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_build_plan_boundary_fields(
        preview: &ScholarChatGroundedAnswerBuildPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
    }

    fn assert_grounded_answer_candidate_boundary_fields(
        preview: &ScholarChatGroundedAnswerCandidatePreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
    }

    fn assert_grounded_answer_write_eligibility_boundary_fields(
        preview: &ScholarChatGroundedAnswerWriteEligibilityPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_grounded_answer_build_intent_boundary_fields(
        preview: &ScholarChatGroundedAnswerBuildIntentPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_grounded_answer_service_call);
    }

    fn assert_grounded_answer_build_request_boundary_fields(
        preview: &ScholarChatGroundedAnswerBuildRequestPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_grounded_answer_service_call);
    }

    fn assert_grounded_answer_build_preflight_boundary_fields(
        preview: &ScholarChatGroundedAnswerBuildPreflightPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_grounded_answer_service_call);
        assert!(preview.no_grounded_answer_write);
    }

    fn assert_grounded_answer_execution_readiness_boundary_fields(
        preview: &ScholarChatGroundedAnswerExecutionReadinessPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_grounded_answer_service_call);
        assert!(preview.no_grounded_answer_write);
    }

    fn assert_grounded_answer_build_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatDraftGroundingInspectionRequest,
    ) -> ScholarChatGroundedAnswerBuildPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_build_plan(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_build_plan(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_build_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_candidate_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatDraftGroundingInspectionRequest,
    ) -> ScholarChatGroundedAnswerCandidatePreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_candidate(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_candidate(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_candidate_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_write_eligibility_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatDraftGroundingInspectionRequest,
    ) -> ScholarChatGroundedAnswerWriteEligibilityPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_write_eligibility(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_write_eligibility(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_write_eligibility_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_build_intent_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatGroundedAnswerBuildIntentRequest,
    ) -> ScholarChatGroundedAnswerBuildIntentPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_build_intent(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_build_intent(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_build_intent_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_execution_readiness_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatGroundedAnswerExecutionReadinessPreviewRequest,
    ) -> ScholarChatGroundedAnswerExecutionReadinessPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_execution_readiness(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_execution_readiness(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_execution_readiness_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_execution_plan_boundary_fields(
        preview: &ScholarChatGroundedAnswerExecutionPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.not_answer_draft);
        assert!(preview.not_grounded_answer);
        assert!(preview.not_final_answer);
        assert!(preview.no_answer_artifact_created);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_llm_call);
        assert!(preview.no_runtime_execution);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_grounded_answer_service_call);
        assert!(preview.no_grounded_answer_write);
    }

    fn assert_grounded_answer_execution_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatGroundedAnswerExecutionPlanPreviewRequest,
    ) -> ScholarChatGroundedAnswerExecutionPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_execution_plan(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_execution_plan(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_execution_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_metadata_execution_boundary_boundary_fields(
        preview: &ScholarChatScientificMetadataExecutionBoundaryPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.metadata_execution_boundary_only);
        assert!(preview.dry_run_only);
        assert!(preview.execution_disabled);
        assert!(preview.no_network_call);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_metadata_persistence);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_metadata_execution_boundary_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatScientificMetadataExecutionBoundaryPreviewRequest,
    ) -> ScholarChatScientificMetadataExecutionBoundaryPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first =
            preview_scholar_chat_scientific_metadata_execution_boundary(temp.path(), request.clone())
                .unwrap();
        let second =
            preview_scholar_chat_scientific_metadata_execution_boundary(temp.path(), request)
                .unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_metadata_execution_boundary_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_discipline_registry_boundary_fields(
        preview: &ScholarChatScientificDisciplineRegistryPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.registry_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_bm25_index);
        assert!(preview.no_vector_index);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_source_registry_boundary_fields(
        preview: &ScholarChatScientificSourceRegistryPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.source_registry_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_bm25_index);
        assert!(preview.no_vector_index);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_query_understanding_boundary_fields(
        preview: &ScholarChatScientificQueryUnderstandingPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.query_understanding_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_bm25_index);
        assert!(preview.no_vector_index);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_query_understanding_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatScientificQueryUnderstandingPreviewRequest,
    ) -> ScholarChatScientificQueryUnderstandingPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_scientific_query_understanding(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_scientific_query_understanding(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_query_understanding_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_search_plan_boundary_fields(preview: &ScholarChatScientificSearchPlanPreview) {
        assert!(preview.preview_only);
        assert!(preview.scientific_search_plan_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_bm25_index);
        assert!(preview.no_vector_index);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_search_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatScientificSearchPlanRequest,
    ) -> ScholarChatScientificSearchPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_scientific_search_plan(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_scientific_search_plan(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_search_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_search_plan_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_search_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(body.matches("preview_scholar_chat_scientific_query_understanding").count(), 1);
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("preview_scholar_chat_answer_readiness"));
        assert!(!body.contains("preview_scholar_chat_draft_inference"));
        assert!(!body.contains("preview_scholar_chat_grounded_answer"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    fn assert_runtime_diagnostic_bridge_boundary_fields(
        preview: &ScholarChatRuntimeDiagnosticBridgePreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.no_smoke_execution);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_answer_draft_created);
        assert!(preview.no_grounded_answer_created);
        assert!(preview.no_final_answer_created);
        assert!(preview.no_grounding_applied);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_artifact_write);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_runtime_diagnostic_bridge_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatRuntimeDiagnosticBridgePreviewRequest,
    ) -> ScholarChatRuntimeDiagnosticBridgePreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_runtime_diagnostic_bridge(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_runtime_diagnostic_bridge(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_runtime_diagnostic_bridge_boundary_fields(preview);
        }
        first
    }

    fn assert_runtime_diagnostic_result_boundary_fields(
        preview: &ScholarChatRuntimeDiagnosticResultPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.diagnostic_result_only);
        assert!(preview.no_smoke_execution);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_new_process_spawn);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_answer_draft_created);
        assert!(preview.no_grounded_answer_created);
        assert!(preview.no_final_answer_created);
        assert!(preview.no_grounding_applied);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_artifact_write);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_runtime_answer_pipeline_gate_boundary_fields(
        preview: &ScholarChatRuntimeAnswerPipelineGatePreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.gate_only);
        assert!(preview.no_smoke_execution);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_new_process_spawn);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_answer_draft_created);
        assert!(preview.no_grounded_answer_created);
        assert!(preview.no_final_answer_created);
        assert!(preview.no_grounding_applied);
        assert!(preview.no_evidence_pack_built);
        assert!(preview.no_persistence);
        assert!(preview.no_artifact_write);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_runtime_diagnostic_result_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatRuntimeDiagnosticResultPreviewRequest,
    ) -> ScholarChatRuntimeDiagnosticResultPreview {
        let before_entries = count_entries_recursively(temp.path());
        let before_aegis_exists = temp.path().join(".aegis").exists();
        let first = preview_scholar_chat_runtime_diagnostic_result(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_runtime_diagnostic_result(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        let after_aegis_exists = temp.path().join(".aegis").exists();
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert_eq!(before_aegis_exists, after_aegis_exists);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_runtime_diagnostic_result_boundary_fields(preview);
        }
        first
    }

    fn assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatRuntimeAnswerPipelineGatePreviewRequest,
    ) -> ScholarChatRuntimeAnswerPipelineGatePreview {
        let before_entries = count_entries_recursively(temp.path());
        let before_aegis_exists = temp.path().join(".aegis").exists();
        let first = preview_scholar_chat_runtime_answer_pipeline_gate(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_runtime_answer_pipeline_gate(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        let after_aegis_exists = temp.path().join(".aegis").exists();
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert_eq!(before_aegis_exists, after_aegis_exists);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_runtime_answer_pipeline_gate_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_build_request_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatGroundedAnswerBuildRequestPreviewRequest,
    ) -> ScholarChatGroundedAnswerBuildRequestPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_build_request(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_build_request(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_build_request_boundary_fields(preview);
        }
        first
    }

    fn assert_grounded_answer_build_preflight_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatGroundedAnswerBuildPreflightPreviewRequest,
    ) -> ScholarChatGroundedAnswerBuildPreflightPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_grounded_answer_build_preflight(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_grounded_answer_build_preflight(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_grounded_answer_build_preflight_boundary_fields(preview);
        }
        first
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_drops_stopwords_and_short_noise_terms() {
        let terms = inspection_terms("the and of to in a an is are was were with for on by as at from this that 12 alpha 2024 x y");
        assert!(!terms.contains("the"));
        assert!(!terms.contains("and"));
        assert!(!terms.contains("x"));
        assert!(terms.contains("12"));
        assert!(terms.contains("2024"));
        assert!(terms.contains("alpha"));
    }

    #[test]
    fn scholar_chat_retrieval_preview_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_retrieval(temp.path(), retrieval_request("   ", vec![]));
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_retrieval_preview_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_retrieval(temp.path(), retrieval_request("Explain alpha", vec![invalid.to_string()]));
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_retrieval_preview_allows_empty_sources_with_warning() {
        let temp = tempfile::tempdir().unwrap();
        let response = preview_scholar_chat_retrieval(temp.path(), retrieval_request("Explain alpha", vec![])).unwrap();
        assert_eq!(response.selected_source_count, 0);
        assert_eq!(response.candidate_count, 0);
        assert!(response.warnings.iter().any(|warning| warning.contains("unscoped")));
        assert_eq!(response.status, ScholarChatStatus::PreviewOnly);
    }

    #[test]
    fn scholar_chat_retrieval_preview_does_not_build_missing_indexes() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority.register_source(&source_path, crate::source_metadata::SourceMetadataInput {
            title: "Notes".to_string(),
            source_type: crate::source_metadata::SourceType::MarkdownNote,
            discipline: "psychology".to_string(),
            subdiscipline: Some("statistics".to_string()),
            language: "en".to_string(),
            tags: vec!["study".to_string()],
            reliability_notes: None,
        }).unwrap();
        crate::extraction::ExtractionService::new(temp.path()).extract_source(&source.source_id).unwrap();
        crate::chunking::ChunkingService::new(temp.path()).chunk_source(&source.source_id).unwrap();
        let source_id = source.source_id.clone();
        let version_id = source.version_id.clone();

        let response = preview_scholar_chat_retrieval(
            temp.path(),
            retrieval_request("alpha", vec![source_id.clone()]),
        )
        .unwrap();

        assert_eq!(response.selected_source_ids, vec![source_id.clone()]);
        assert_eq!(response.candidate_count, 0);
        assert!(response.warnings.iter().any(|warning| warning.contains("not ready")));
        let index_path = temp
            .path()
            .join(".aegis")
            .join("corpus")
            .join("sources")
            .join(&source_id)
            .join("versions")
            .join(&version_id)
            .join("retrieval")
            .join("index.json");
        assert!(!index_path.exists());
    }

    #[test]
    fn scholar_chat_retrieval_preview_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let source_a = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let source_b = build_source_with_index(&temp, "alpha delta\n\nalpha epsilon\n");
        let request = retrieval_request("  alpha  ", vec![source_b.clone(), source_a.clone()]);
        let first = preview_scholar_chat_retrieval(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_retrieval(temp.path(), request).unwrap();
        assert_eq!(first, second);
        assert_eq!(first.normalized_prompt, "alpha");
        assert_eq!(first.selected_source_ids, vec![source_b, source_a]);
        assert!(first.candidates.windows(2).all(|pair| pair[0].score >= pair[1].score));
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert!(!temp.path().join(".aegis").join("corpus").join("sources").join("missing").exists());
    }

    #[test]
    fn scholar_chat_evidence_plan_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_evidence_plan(temp.path(), evidence_plan_request("   ", vec![]));
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_evidence_plan_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_evidence_plan(temp.path(), evidence_plan_request("Explain alpha", vec![invalid.to_string()]));
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_evidence_plan_allows_no_selected_sources_with_warning() {
        let temp = tempfile::tempdir().unwrap();
        let response = preview_scholar_chat_evidence_plan(temp.path(), evidence_plan_request("Explain alpha", vec![])).unwrap();
        assert_eq!(response.selected_source_count, 0);
        assert_eq!(response.retrieval_candidate_count, 0);
        assert_eq!(response.evidence_candidate_count, 0);
        assert!(response.warnings.iter().any(|warning| warning.contains("No selected sources")));
        assert!(response.warnings.iter().any(|warning| warning.contains("Evidence Pack was built")));
        assert_eq!(response.status, ScholarChatEvidencePlanStatus::EvidencePlanPreview);
        assert!(response.evidence_plan.evidence_required);
        assert!(response.evidence_plan.evidence_pack_would_be_built_later);
    }

    #[test]
    fn scholar_chat_evidence_plan_does_not_build_missing_indexes() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority.register_source(&source_path, crate::source_metadata::SourceMetadataInput {
            title: "Notes".to_string(),
            source_type: crate::source_metadata::SourceType::MarkdownNote,
            discipline: "psychology".to_string(),
            subdiscipline: Some("statistics".to_string()),
            language: "en".to_string(),
            tags: vec!["study".to_string()],
            reliability_notes: None,
        }).unwrap();
        crate::extraction::ExtractionService::new(temp.path()).extract_source(&source.source_id).unwrap();
        crate::chunking::ChunkingService::new(temp.path()).chunk_source(&source.source_id).unwrap();
        let source_id = source.source_id.clone();
        let version_id = source.version_id.clone();

        let response = preview_scholar_chat_evidence_plan(
            temp.path(),
            evidence_plan_request("alpha", vec![source_id.clone()]),
        )
        .unwrap();

        assert_eq!(response.selected_source_ids, vec![source_id.clone()]);
        assert_eq!(response.retrieval_candidate_count, 0);
        assert_eq!(response.evidence_candidate_count, 0);
        assert!(response.warnings.iter().any(|warning| warning.contains("not ready")));
        let index_path = temp
            .path()
            .join(".aegis")
            .join("corpus")
            .join("sources")
            .join(&source_id)
            .join("versions")
            .join(&version_id)
            .join("retrieval")
            .join("index.json");
        assert!(!index_path.exists());
    }

    #[test]
    fn scholar_chat_evidence_plan_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let source_a = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let source_b = build_source_with_index(&temp, "alpha delta\n\nalpha epsilon\n");
        let request = evidence_plan_request("  alpha  ", vec![source_b.clone(), source_a.clone()]);
        let first = preview_scholar_chat_evidence_plan(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_evidence_plan(temp.path(), request).unwrap();
        assert_eq!(first, second);
        assert_eq!(first.normalized_prompt, "alpha");
        assert_eq!(first.selected_source_ids, vec![source_b, source_a]);
        assert_eq!(first.retrieval_candidate_count, first.candidates.len());
        assert_eq!(first.evidence_candidate_count, first.candidates.len());
        assert!(first.candidates.windows(2).all(|pair| pair[0].score >= pair[1].score));
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert!(!temp.path().join(".aegis").join("corpus").join("sources").join("missing").exists());
    }

    #[test]
    fn scholar_chat_prompt_pack_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_prompt_pack(temp.path(), prompt_pack_request("   ", vec![]));
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_prompt_pack_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_prompt_pack(temp.path(), prompt_pack_request("Explain alpha", vec![invalid.to_string()]));
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_prompt_pack_allows_no_selected_sources_with_warning() {
        let temp = tempfile::tempdir().unwrap();
        let response = preview_scholar_chat_prompt_pack(temp.path(), prompt_pack_request("Explain alpha", vec![])).unwrap();
        assert_eq!(response.selected_source_count, 0);
        assert_eq!(response.evidence_candidate_count, 0);
        assert_eq!(response.context_items.len(), 0);
        assert_eq!(response.status, ScholarChatPromptPackStatus::PromptPackPreview);
        assert!(response.warnings.iter().any(|warning| warning.contains("unscoped")));
        assert!(response.warnings.iter().any(|warning| warning.contains("no model inference")));
    }

    #[test]
    fn scholar_chat_prompt_pack_includes_mode_and_grounding_sections() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let response = preview_scholar_chat_prompt_pack(
            temp.path(),
            prompt_pack_request("  alpha  ", vec![source_id.clone()]),
        )
        .unwrap();
        assert_eq!(response.normalized_prompt, "alpha");
        assert_eq!(response.selected_source_ids, vec![source_id.clone()]);
        assert_eq!(response.prompt_pack.section_count, 5);
        assert_eq!(response.prompt_pack.context_item_count, response.context_items.len());
        assert!(response.prompt_pack.sections.iter().any(|section| section.kind == ScholarChatPromptPackSectionKind::SystemOrPolicyInstructions));
        assert!(response.prompt_pack.sections.iter().any(|section| section.kind == ScholarChatPromptPackSectionKind::ModeInstructions));
        assert!(response.prompt_pack.sections.iter().any(|section| section.kind == ScholarChatPromptPackSectionKind::GroundingInstructions));
        assert!(response.prompt_pack.sections.iter().any(|section| section.kind == ScholarChatPromptPackSectionKind::SourceContext));
        assert!(response.prompt_pack.sections.iter().any(|section| section.kind == ScholarChatPromptPackSectionKind::UserPrompt));
        assert!(response.prompt_pack.sections.iter().any(|section| section.lines.iter().any(|line| line.contains("local evidence"))));
        assert!(response.warnings.iter().any(|warning| warning.contains("local evidence")));
    }

    #[test]
    fn scholar_chat_prompt_pack_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let source_a = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let source_b = build_source_with_index(&temp, "alpha delta\n\nalpha epsilon\n");
        let request = prompt_pack_request("  alpha  ", vec![source_b.clone(), source_a.clone()]);
        let first = preview_scholar_chat_prompt_pack(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_prompt_pack(temp.path(), request).unwrap();
        assert_eq!(first, second);
        assert_eq!(first.selected_source_ids, vec![source_b, source_a]);
        assert_eq!(first.prompt_pack.section_count, first.prompt_pack.sections.len());
        assert_eq!(first.prompt_pack.context_item_count, first.context_items.len());
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert!(!temp.path().join(".aegis").join("corpus").join("sources").join("missing").exists());
    }

    #[test]
    fn scholar_chat_answer_readiness_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_answer_readiness(
            temp.path(),
            answer_readiness_request(
                "   ",
                GroundingPolicy::LocalOnly,
                vec![],
                runtime_config(None, None),
                false,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_answer_readiness_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_answer_readiness(
                temp.path(),
                answer_readiness_request(
                    "Explain alpha",
                    GroundingPolicy::LocalOnly,
                    vec![invalid.to_string()],
                    runtime_config(None, None),
                    false,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_answer_readiness_requires_sources_for_local_only() {
        let temp = tempfile::tempdir().unwrap();
        let runtime_config = build_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_answer_readiness(
            temp.path(),
            answer_readiness_request(
                "Explain alpha",
                GroundingPolicy::LocalOnly,
                vec![],
                runtime_config,
                true,
            ),
        )
        .unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(response.status, ScholarChatAnswerReadinessStatus::NeedsSources);
        assert_eq!(response.future_output_classification, ScholarChatAnswerReadinessOutputClassification::Blocked);
        assert_eq!(response.selected_source_count, 0);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(response.next_required_actions.iter().any(|action| action.contains("Select one or more Scholar Chat sources")));
        assert_eq!(before_entries, after_entries);
        assert_readiness_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_answer_readiness_blocks_local_only_without_retrieval_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority.register_source(&source_path, crate::source_metadata::SourceMetadataInput {
            title: "Notes".to_string(),
            source_type: crate::source_metadata::SourceType::MarkdownNote,
            discipline: "psychology".to_string(),
            subdiscipline: Some("statistics".to_string()),
            language: "en".to_string(),
            tags: vec!["study".to_string()],
            reliability_notes: None,
        }).unwrap();
        crate::extraction::ExtractionService::new(temp.path()).extract_source(&source.source_id).unwrap();
        crate::chunking::ChunkingService::new(temp.path()).chunk_source(&source.source_id).unwrap();
        let runtime_config = build_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_answer_readiness(
            temp.path(),
            answer_readiness_request(
                "Explain alpha",
                GroundingPolicy::LocalOnly,
                vec![source.source_id.clone()],
                runtime_config,
                true,
            ),
        )
        .unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(response.status, ScholarChatAnswerReadinessStatus::Blocked);
        assert_eq!(response.future_output_classification, ScholarChatAnswerReadinessOutputClassification::Blocked);
        assert_eq!(response.selected_source_count, 1);
        assert_eq!(response.retrieval_candidate_count, 0);
        assert_eq!(response.evidence_candidate_count, 0);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "blocked"));
        assert!(response.next_required_actions.iter().any(|action| action.contains("retrieval index")));
        assert_eq!(before_entries, after_entries);
        assert_readiness_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_answer_readiness_needs_runtime_config_when_local_runtime_is_missing() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_answer_readiness(
            temp.path(),
            answer_readiness_request(
                "Explain alpha",
                GroundingPolicy::LocalFirst,
                vec![source_id.clone()],
                runtime_config(None, None),
                true,
            ),
        )
        .unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(response.status, ScholarChatAnswerReadinessStatus::NeedsRuntimeConfig);
        assert_eq!(response.runtime_health_status, LocalModelRuntimeHealthStatus::ConfigPresent);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_runtime_config"));
        assert_eq!(before_entries, after_entries);
        assert_readiness_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_answer_readiness_requires_execution_consent_when_runtime_is_ready() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let runtime_config = build_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_answer_readiness(
            temp.path(),
            answer_readiness_request(
                "Explain alpha",
                GroundingPolicy::LocalFirst,
                vec![source_id.clone()],
                runtime_config,
                false,
            ),
        )
        .unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(response.status, ScholarChatAnswerReadinessStatus::NeedsExecutionConsent);
        assert_eq!(response.invocation_plan_status, LocalRuntimeInvocationPlanStatus::ReadyToInvokeLater);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_execution_consent"));
        assert_eq!(before_entries, after_entries);
        assert_readiness_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_answer_readiness_can_be_ready_for_draft_inference_later() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let runtime_config = build_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let request = answer_readiness_request(
            "  Explain alpha  ",
            GroundingPolicy::AllowMarkedModelKnowledge,
            vec![source_id.clone()],
            runtime_config,
            true,
        );
        let first = preview_scholar_chat_answer_readiness(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_answer_readiness(temp.path(), request).unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(first, second);
        assert_eq!(first.status, ScholarChatAnswerReadinessStatus::ReadyForDraftInferenceLater);
        assert_eq!(first.future_output_classification, ScholarChatAnswerReadinessOutputClassification::UngroundedDraft);
        assert_eq!(first.normalized_prompt, "Explain alpha");
        assert_eq!(first.mode, ScholarChatMode::ThesisWriting);
        assert_eq!(first.grounding_policy, GroundingPolicy::AllowMarkedModelKnowledge);
        assert_eq!(first.selected_source_count, 1);
        assert!(first.prompt_pack_ready);
        assert!(first.would_generate_answer_now == false);
        assert!(first.would_build_evidence_pack_now == false);
        assert!(first.would_create_final_answer_now == false);
        assert!(first.warnings.iter().any(|warning| warning.kind == "future_draft_marking_required"));
        assert!(first.next_required_actions.iter().any(|action| action.contains("prompt pack")));
        assert_eq!(before_entries, after_entries);
        assert_readiness_boundary_fields(&first);
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_inference_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let runtime_config = build_draft_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let result = preview_scholar_chat_draft_inference(
            temp.path(),
            draft_inference_request(
                "   ",
                GroundingPolicy::AllowMarkedModelKnowledge,
                vec![],
                runtime_config,
                true,
                None,
                None,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_draft_inference_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        let runtime_config = build_draft_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_draft_inference(
                temp.path(),
                draft_inference_request(
                    "Explain alpha",
                    GroundingPolicy::AllowMarkedModelKnowledge,
                    vec![invalid.to_string()],
                    runtime_config.clone(),
                    true,
                    None,
                    None,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(before_entries, after_entries);
    }

    #[test]
    fn scholar_chat_draft_inference_blocks_when_execution_is_disabled() {
        let temp = tempfile::tempdir().unwrap();
        let runtime_config = build_draft_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let request = draft_inference_request(
            "Explain alpha",
            GroundingPolicy::AllowMarkedModelKnowledge,
            vec![],
            runtime_config,
            false,
            None,
            None,
        );
        let first = preview_scholar_chat_draft_inference(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_draft_inference(temp.path(), request).unwrap();
        let response = first.clone();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(first, second);
        assert_eq!(response.status, ScholarChatDraftInferenceStatus::NeedsExecutionConsent);
        assert_eq!(response.output_classification, ScholarChatDraftOutputClassification::Blocked);
        assert!(!response.execution_attempted);
        assert_eq!(response.prompt_pack_section_count, 0);
        assert_eq!(response.prompt_char_count, 0);
        assert_eq!(response.runtime_health_status, LocalModelRuntimeHealthStatus::NotConfigured);
        assert_eq!(response.invocation_plan_status, LocalRuntimeInvocationPlanStatus::PreviewOnly);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_execution_consent"));
        assert_eq!(before_entries, after_entries);
        assert_draft_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_inference_blocks_local_only_without_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();
        let runtime_config = build_draft_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_draft_inference(
            temp.path(),
            draft_inference_request(
                "Explain alpha",
                GroundingPolicy::LocalOnly,
                vec![source.source_id.clone()],
                runtime_config,
                true,
                None,
                None,
            ),
        )
        .unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(response.status, ScholarChatDraftInferenceStatus::NeedsEvidence);
        assert_eq!(response.output_classification, ScholarChatDraftOutputClassification::Blocked);
        assert!(!response.execution_attempted);
        assert_eq!(response.prompt_pack_section_count, 0);
        assert_eq!(response.prompt_char_count, 0);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_evidence"));
        assert!(response.warnings.iter().any(|warning| warning.kind == "evidence_required"));
        assert_eq!(before_entries, after_entries);
        assert_draft_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_inference_needs_runtime_config_when_runtime_is_missing() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_draft_inference(
            temp.path(),
            draft_inference_request(
                "Explain alpha",
                GroundingPolicy::LocalFirst,
                vec![source_id.clone()],
                LocalModelRuntimeConfig {
                    runtime_kind: LocalModelRuntimeKind::None,
                    model_path: None,
                    executable_path: None,
                    context_window: Some(512),
                    gpu_layers: Some(0),
                    temperature: Some(0.0),
                },
                true,
                None,
                None,
            ),
        )
        .unwrap();
        assert_eq!(response.status, ScholarChatDraftInferenceStatus::NeedsRuntimeConfig);
        assert_eq!(response.output_classification, ScholarChatDraftOutputClassification::Blocked);
        assert!(!response.execution_attempted);
        assert_eq!(response.prompt_pack_section_count, 0);
        assert_eq!(response.prompt_char_count, 0);
        assert!(response.blockers.iter().any(|blocker| blocker.kind == "needs_runtime_config"));
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(before_entries, after_entries);
        assert_draft_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_inference_reports_missing_model_and_executable_without_paths() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta\n\nalpha gamma\n");
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let response = preview_scholar_chat_draft_inference(
            temp.path(),
            draft_inference_request(
                "Explain alpha",
                GroundingPolicy::LocalFirst,
                vec![source_id.clone()],
                runtime_config(
                    Some(temp.path().join("missing-model.gguf").to_string_lossy().as_ref()),
                    Some(temp.path().join("missing-draft-helper.exe").to_string_lossy().as_ref()),
                ),
                true,
                None,
                None,
            ),
        )
        .unwrap();
        assert_eq!(response.status, ScholarChatDraftInferenceStatus::NeedsRuntimeConfig);
        assert_eq!(response.output_classification, ScholarChatDraftOutputClassification::Blocked);
        assert!(!response.execution_attempted);
        assert_eq!(response.prompt_pack_section_count, 0);
        assert_eq!(response.prompt_char_count, 0);
        assert_eq!(response.safe_model_file_name.as_deref(), Some("missing-model.gguf"));
        assert!(response.safe_executable_file_name.is_none());
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        assert_eq!(before_entries, after_entries);
        assert_draft_boundary_fields(&response);
        let debug = format!("{response:?}");
        let json = serde_json::to_string(&response).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_inference_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let runtime_config = build_draft_runtime_fixture(&temp);
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let request = draft_inference_request(
            "  Explain alpha  ",
            GroundingPolicy::AllowMarkedModelKnowledge,
            vec![],
            runtime_config,
            true,
            None,
            None,
        );
        let first = preview_scholar_chat_draft_inference(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_draft_inference(temp.path(), request).unwrap();
        let after_entries = fs::read_dir(temp.path()).unwrap().count();
        let mut first_sanitized = first.clone();
        let mut second_sanitized = second.clone();
        first_sanitized.duration_ms = 0;
        second_sanitized.duration_ms = 0;
        assert_eq!(first_sanitized, second_sanitized);
        assert_eq!(first.status, ScholarChatDraftInferenceStatus::InferenceSucceeded);
        assert_eq!(first.output_classification, ScholarChatDraftOutputClassification::UngroundedModelDraft);
        assert_eq!(first.normalized_prompt, "Explain alpha");
        assert_eq!(first.mode, ScholarChatMode::ThesisWriting);
        assert_eq!(first.grounding_policy, GroundingPolicy::AllowMarkedModelKnowledge);
        assert_eq!(first.selected_source_count, 0);
        assert_eq!(first.retrieval_candidate_count, 0);
        assert_eq!(first.evidence_candidate_count, 0);
        assert!(first.prompt_pack_section_count > 0);
        assert!(first.execution_attempted);
        assert_eq!(first.allow_model_execution, true);
        assert!(first.stdout_preview.contains("stdout marker"));
        assert!(first.stderr_preview.contains("stderr marker"));
        assert_draft_boundary_fields(&first);
        assert_eq!(before_entries, after_entries);
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: request("   "),
                draft_text: Some("Draft text".to_string()),
                max_items: Some(4),
            },
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_draft_grounding_inspection(
                temp.path(),
                ScholarChatDraftGroundingInspectionRequest {
                    scholar_chat_request: ScholarChatRequest {
                        prompt: "Explain grounded text".to_string(),
                        mode: ScholarChatMode::LectureLearning,
                        grounding_policy: GroundingPolicy::LocalFirst,
                        selected_source_ids: vec![invalid.to_string()],
                    },
                    draft_text: Some("alpha beta.".to_string()),
                    max_items: Some(4),
                },
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_reports_no_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: request("Explain grounded text"),
                draft_text: Some("   ".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::NoDraftText);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "draft_text_missing"));
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "Explain grounded text".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("alpha beta.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.message.contains("No Scholar Chat source context selected")));
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_reports_no_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();

        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "Explain grounded text".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalOnly,
                    selected_source_ids: vec![source.source_id.clone()],
                },
                draft_text: Some("alpha beta.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates);
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "needs_evidence_candidates"));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.message.contains("No local evidence candidates")));
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let before_entries = count_entries_recursively(temp.path());
        let request = ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: "  alpha beta grounded evidence  ".to_string(),
                mode: ScholarChatMode::ThesisWriting,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids: vec![source_id.clone()],
            },
            draft_text: Some("Alpha beta. Gamma delta? Alpha beta gamma.".to_string()),
            max_items: Some(8),
        };
        let first = preview_scholar_chat_draft_grounding_inspection(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_draft_grounding_inspection(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(first.normalized_prompt, "alpha beta grounded evidence");
        assert_eq!(first.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(first.selected_source_count, 1);
        assert!(!first.items.is_empty());
        assert!(first.items.iter().any(|item| item.support_status == ScholarChatDraftGroundingSupportStatus::SupportedByLocalEvidence));
        let debug = format!("{first:?}");
        let json = serde_json::to_string(&first).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(before_entries, after_entries);
        assert_draft_grounding_inspection_boundary_fields(&first);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_supports_local_evidence_and_clamps_items() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some(
                    "Alpha beta support. Gamma. Theta. Alpha beta gamma evidence. Delta alpha beta. More alpha beta. Another alpha beta.".to_string(),
                ),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(result.items.len(), 4);
        assert_eq!(
            result.supported_item_count + result.weakly_supported_item_count + result.unsupported_item_count,
            result.items.len()
        );
        assert!(result.items.iter().any(|item| item.support_status == ScholarChatDraftGroundingSupportStatus::SupportedByLocalEvidence));
        assert!(result.items.iter().any(|item| item.support_status == ScholarChatDraftGroundingSupportStatus::WeaklySupported));
        assert!(result.items.iter().any(|item| item.support_status == ScholarChatDraftGroundingSupportStatus::Unsupported));
        assert!(result.items.iter().all(|item| !item.text_preview.contains("  ")));
        assert!(result.items.iter().all(|item| !item.locator_previews.iter().any(|preview| preview.contains("section_path"))));
        assert!(result.warnings.iter().any(|warning| warning.kind == "inspection_clamped"));
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_local_only_support_needs_clear_overlap() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::AllowMarkedModelKnowledge,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("Gamma.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(result.items.len(), 1);
        assert_eq!(result.supported_item_count, 0);
        assert_eq!(result.weakly_supported_item_count, 1);
        assert_eq!(result.unsupported_item_count, 0);
        assert_eq!(result.items[0].support_status, ScholarChatDraftGroundingSupportStatus::WeaklySupported);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.message.contains("Model knowledge is not used in this preview")));
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_marks_single_meaningful_overlap_as_weakly_supported() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha beta grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("The alpha.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(result.items.len(), 1);
        assert_eq!(result.supported_item_count, 0);
        assert_eq!(result.weakly_supported_item_count, 1);
        assert_eq!(result.unsupported_item_count, 0);
        assert_eq!(result.items[0].support_status, ScholarChatDraftGroundingSupportStatus::WeaklySupported);
        assert_eq!(result.items[0].matched_evidence_count, 1);
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_requires_two_meaningful_terms_for_supported_overlap() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha beta grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("The alpha beta.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(result.items.len(), 1);
        assert_eq!(result.supported_item_count, 1);
        assert_eq!(result.weakly_supported_item_count, 0);
        assert_eq!(result.unsupported_item_count, 0);
        assert_eq!(result.items[0].support_status, ScholarChatDraftGroundingSupportStatus::SupportedByLocalEvidence);
        assert!(result.items[0].matched_evidence_count >= 1);
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_draft_grounding_inspection_leaves_unrelated_items_unsupported() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = preview_scholar_chat_draft_grounding_inspection(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha beta grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("Zeta kappa.".to_string()),
                max_items: Some(4),
            },
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(result.items.len(), 1);
        assert_eq!(result.supported_item_count, 0);
        assert_eq!(result.weakly_supported_item_count, 0);
        assert_eq!(result.unsupported_item_count, 1);
        assert_eq!(result.items[0].support_status, ScholarChatDraftGroundingSupportStatus::Unsupported);
        assert_eq!(result.items[0].matched_evidence_count, 0);
        assert_draft_grounding_inspection_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_draft_readiness(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "   ".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_blocks_without_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_draft_readiness_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("   ".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert_eq!(result.inspection_status, ScholarChatDraftGroundingInspectionStatus::NoDraftText);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "draft_text_missing"));
        assert!(result.summary.contains("blocked"));
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_draft_readiness_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert_eq!(result.inspection_status, ScholarChatDraftGroundingInspectionStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select Scholar Chat source context")));
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_blocks_without_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();

        let result = assert_grounded_draft_readiness_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalOnly,
                    selected_source_ids: vec![source.source_id.clone()],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert_eq!(result.inspection_status, ScholarChatDraftGroundingInspectionStatus::NoEvidenceCandidates);
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "needs_evidence_candidates"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add local evidence candidates")));
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_marks_weak_or_unsupported_items_for_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let weak_result = assert_grounded_draft_readiness_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id.clone()],
                },
                draft_text: Some("The alpha.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(weak_result.status, ScholarChatGroundedDraftReadinessStatus::NeedsReview);
        assert_eq!(weak_result.inspection_status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(weak_result.weakly_supported_item_count, 1);
        assert_eq!(weak_result.unsupported_item_count, 0);
        assert!(weak_result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));

        let unsupported_result = assert_grounded_draft_readiness_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("Zeta kappa.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(unsupported_result.status, ScholarChatGroundedDraftReadinessStatus::NeedsReview);
        assert_eq!(unsupported_result.inspection_status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(unsupported_result.supported_item_count, 0);
        assert_eq!(unsupported_result.weakly_supported_item_count, 0);
        assert_eq!(unsupported_result.unsupported_item_count, 1);
    }

    #[test]
    fn scholar_chat_grounded_draft_readiness_is_ready_only_when_every_item_has_local_support() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let request = ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: "alpha grounded evidence".to_string(),
                mode: ScholarChatMode::LectureLearning,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids: vec![source_id],
            },
            draft_text: Some("Alpha beta.".to_string()),
            max_items: Some(4),
        };
        let first = assert_grounded_draft_readiness_deterministic_and_path_free(&temp, request);
        assert_eq!(first.status, ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater);
        assert_eq!(first.inspection_status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(first.inspected_item_count, 1);
        assert_eq!(first.supported_item_count, 1);
        assert_eq!(first.weakly_supported_item_count, 0);
        assert_eq!(first.unsupported_item_count, 0);
        assert!(first.summary.contains("All inspected items were supported by local evidence"));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_build_plan(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "   ".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_blocks_without_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_plan_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("   ".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPlanStatus::Blocked);
        assert_eq!(result.readiness_status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "draft_text_missing"));
        assert!(result.summary.contains("blocked"));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_build_plan_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPlanStatus::Blocked);
        assert_eq!(result.readiness_status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select Scholar Chat source context")));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_blocks_without_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();

        let result = assert_grounded_answer_build_plan_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalOnly,
                    selected_source_ids: vec![source.source_id.clone()],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPlanStatus::Blocked);
        assert_eq!(result.readiness_status, ScholarChatGroundedDraftReadinessStatus::Blocked);
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "needs_evidence_candidates"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add local evidence candidates")));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_marks_weak_or_unsupported_items_for_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let weak_result = assert_grounded_answer_build_plan_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id.clone()],
                },
                draft_text: Some("The alpha.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(weak_result.status, ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview);
        assert_eq!(weak_result.readiness_status, ScholarChatGroundedDraftReadinessStatus::NeedsReview);
        assert_eq!(weak_result.weakly_supported_item_count, 1);
        assert_eq!(weak_result.unsupported_item_count, 0);
        assert!(weak_result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));

        let unsupported_result = assert_grounded_answer_build_plan_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("Zeta kappa.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(unsupported_result.status, ScholarChatGroundedAnswerBuildPlanStatus::NeedsReview);
        assert_eq!(unsupported_result.readiness_status, ScholarChatGroundedDraftReadinessStatus::NeedsReview);
        assert_eq!(unsupported_result.supported_item_count, 0);
        assert_eq!(unsupported_result.weakly_supported_item_count, 0);
        assert_eq!(unsupported_result.unsupported_item_count, 1);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_plan_is_ready_only_when_every_item_has_local_support() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let request = ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: "alpha grounded evidence".to_string(),
                mode: ScholarChatMode::LectureLearning,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids: vec![source_id],
            },
            draft_text: Some("Alpha beta.".to_string()),
            max_items: Some(4),
        };
        let first = assert_grounded_answer_build_plan_deterministic_and_path_free(&temp, request);
        assert_eq!(first.status, ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater);
        assert_eq!(
            first.readiness_status,
            ScholarChatGroundedDraftReadinessStatus::ReadyForGroundedDraftLater
        );
        assert_eq!(first.inspected_item_count, 1);
        assert_eq!(first.supported_item_count, 1);
        assert_eq!(first.weakly_supported_item_count, 0);
        assert_eq!(first.unsupported_item_count, 0);
        assert!(first.summary.contains("All inspected items were supported by local evidence"));
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_candidate(
            temp.path(),
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "   ".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_candidate_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result.candidate_items.is_empty());
        assert_eq!(result.inspected_item_count, 0);
        assert_eq!(result.supported_item_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select Scholar Chat source context")));
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_blocks_without_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_candidate_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("   ".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result.candidate_items.is_empty());
        assert_eq!(result.inspected_item_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "draft_text_missing"));
        assert!(result.summary.contains("blocked"));
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_blocks_without_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();

        let result = assert_grounded_answer_candidate_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalOnly,
                    selected_source_ids: vec![source.source_id.clone()],
                },
                draft_text: Some("Alpha beta.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result.candidate_items.is_empty());
        assert_eq!(result.evidence_candidate_count, 0);
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "needs_evidence_candidates"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add local evidence candidates")));
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_marks_weak_or_unsupported_items_for_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let weak_result = assert_grounded_answer_candidate_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id.clone()],
                },
                draft_text: Some("The alpha.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(weak_result.status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert_eq!(weak_result.candidate_statement_count, 0);
        assert!(weak_result.candidate_items.is_empty());
        assert_eq!(weak_result.weakly_supported_item_count, 1);
        assert_eq!(weak_result.unsupported_item_count, 0);
        assert!(weak_result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));

        let unsupported_result = assert_grounded_answer_candidate_deterministic_and_path_free(
            &temp,
            ScholarChatDraftGroundingInspectionRequest {
                scholar_chat_request: ScholarChatRequest {
                    prompt: "alpha grounded evidence".to_string(),
                    mode: ScholarChatMode::LectureLearning,
                    grounding_policy: GroundingPolicy::LocalFirst,
                    selected_source_ids: vec![source_id],
                },
                draft_text: Some("Zeta kappa.".to_string()),
                max_items: Some(4),
            },
        );
        assert_eq!(unsupported_result.status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert_eq!(unsupported_result.candidate_statement_count, 0);
        assert!(unsupported_result.candidate_items.is_empty());
        assert_eq!(unsupported_result.supported_item_count, 0);
        assert_eq!(unsupported_result.weakly_supported_item_count, 0);
        assert_eq!(unsupported_result.unsupported_item_count, 1);
    }

    #[test]
    fn scholar_chat_grounded_answer_candidate_is_ready_only_when_every_item_has_local_support() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let request = ScholarChatDraftGroundingInspectionRequest {
            scholar_chat_request: ScholarChatRequest {
                prompt: "alpha grounded evidence".to_string(),
                mode: ScholarChatMode::LectureLearning,
                grounding_policy: GroundingPolicy::LocalFirst,
                selected_source_ids: vec![source_id.clone()],
            },
            draft_text: Some("Alpha beta. Alpha beta gamma.".to_string()),
            max_items: Some(4),
        };
        let candidate_preview = assert_grounded_answer_candidate_deterministic_and_path_free(&temp, request.clone());
        let build_plan_preview = preview_scholar_chat_grounded_answer_build_plan(temp.path(), request.clone()).unwrap();
        assert_eq!(build_plan_preview.status, ScholarChatGroundedAnswerBuildPlanStatus::PlanReadyLater);
        assert_eq!(candidate_preview.selected_source_count, build_plan_preview.selected_source_count);
        assert_eq!(candidate_preview.evidence_candidate_count, build_plan_preview.evidence_candidate_count);
        assert_eq!(candidate_preview.inspected_item_count, build_plan_preview.inspected_item_count);
        assert_eq!(candidate_preview.supported_item_count, build_plan_preview.supported_item_count);
        assert_eq!(candidate_preview.weakly_supported_item_count, build_plan_preview.weakly_supported_item_count);
        assert_eq!(candidate_preview.unsupported_item_count, build_plan_preview.unsupported_item_count);
        assert_eq!(candidate_preview.status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(candidate_preview.candidate_statement_count, 2);
        assert_eq!(candidate_preview.candidate_statement_count, candidate_preview.candidate_items.len());
        assert_eq!(candidate_preview.inspected_item_count, 2);
        assert_eq!(candidate_preview.supported_item_count, 2);
        assert_eq!(candidate_preview.weakly_supported_item_count, 0);
        assert_eq!(candidate_preview.unsupported_item_count, 0);
        assert!(candidate_preview.summary.contains("All inspected items were supported by local evidence"));
        let inspection_preview = preview_scholar_chat_draft_grounding_inspection(temp.path(), request).unwrap();
        assert_eq!(inspection_preview.status, ScholarChatDraftGroundingInspectionStatus::Inspected);
        assert_eq!(candidate_preview.candidate_items.len(), inspection_preview.items.len());
        for (candidate_item, inspection_item) in candidate_preview.candidate_items.iter().zip(inspection_preview.items.iter()) {
            assert_eq!(candidate_item.item_index, inspection_item.item_index);
            assert_eq!(candidate_item.statement_preview, inspection_item.text_preview);
            assert_eq!(candidate_item.support_status, inspection_item.support_status);
            assert_eq!(candidate_item.matched_evidence_count, inspection_item.matched_evidence_count);
            assert_eq!(candidate_item.source_ids, inspection_item.source_ids);
            assert_eq!(candidate_item.locator_previews, inspection_item.locator_previews);
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_build_intent(
            temp.path(),
            build_intent_request(
                "   ",
                Some("Alpha beta."),
                vec!["src_demo".to_string()],
                Some("draft-1"),
                true,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_rejects_invalid_answer_draft_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/draft", "evil\\draft"] {
            let result = preview_scholar_chat_grounded_answer_build_intent(
                temp.path(),
                build_intent_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some(invalid),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::AnswerDraftInvalidId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_treats_whitespace_answer_draft_id_as_missing_input() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("   "),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert!(result.required_inputs.contains(&"answer_draft_id".to_string()));
        assert!(result.missing_inputs.contains(&"answer_draft_id".to_string()));
        assert!(!result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "answer_draft_id_invalid"));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_blocks_without_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("   "),
                vec![source_id],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.missing_inputs.contains(&"write_eligible_later".to_string()));
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "write_eligibility_blocked"));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("Alpha beta."),
                vec![],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.missing_inputs.contains(&"write_eligible_later".to_string()));
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "write_eligibility_blocked"));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_needs_review_when_candidate_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert_eq!(result.selected_source_count, 1);
        assert_eq!(result.evidence_candidate_count, 1);
        assert_eq!(result.inspected_item_count, 1);
        assert_eq!(result.supported_item_count, 0);
        assert_eq!(result.weakly_supported_item_count, 1);
        assert_eq!(result.unsupported_item_count, 0);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result
            .intent_reasons
            .iter()
            .any(|reason| reason.contains("Weakly supported or unsupported draft items remain.")));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_blocks_without_explicit_user_intent() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("draft-1"),
                false,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert!(result.required_inputs.contains(&"explicit_user_intent".to_string()));
        assert!(result.missing_inputs.contains(&"explicit_user_intent".to_string()));
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "explicit_user_intent_missing"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Provide explicit user intent before any GroundedAnswer service call.")));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_blocks_without_answer_draft_id() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_intent_deterministic_and_path_free(
            &temp,
            build_intent_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                None,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert!(result.required_inputs.contains(&"answer_draft_id".to_string()));
        assert!(result.missing_inputs.contains(&"answer_draft_id".to_string()));
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "answer_draft_id_missing"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Provide an answer draft ID before any GroundedAnswer service call.")));
        assert_grounded_answer_build_intent_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_intent_is_ready_only_when_all_inputs_present() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let request = build_intent_request(
            "alpha grounded evidence",
            Some("Alpha beta. Alpha beta gamma."),
            vec![source_id.clone()],
            Some("draft-1"),
            true,
        );
        let build_intent_preview = assert_grounded_answer_build_intent_deterministic_and_path_free(&temp, request.clone());
        let write_eligibility_preview = assert_grounded_answer_write_eligibility_deterministic_and_path_free(&temp, request.grounding_request);
        assert_eq!(build_intent_preview.status, ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater);
        assert_eq!(build_intent_preview.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(build_intent_preview.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(build_intent_preview.selected_source_count, write_eligibility_preview.selected_source_count);
        assert_eq!(build_intent_preview.evidence_candidate_count, write_eligibility_preview.evidence_candidate_count);
        assert_eq!(build_intent_preview.inspected_item_count, write_eligibility_preview.inspected_item_count);
        assert_eq!(build_intent_preview.supported_item_count, write_eligibility_preview.supported_item_count);
        assert_eq!(build_intent_preview.weakly_supported_item_count, write_eligibility_preview.weakly_supported_item_count);
        assert_eq!(build_intent_preview.unsupported_item_count, write_eligibility_preview.unsupported_item_count);
        assert_eq!(build_intent_preview.candidate_statement_count, write_eligibility_preview.candidate_statement_count);
        assert_eq!(
            build_intent_preview.required_inputs,
            vec![
                "write_eligible_later".to_string(),
                "explicit_user_intent".to_string(),
                "answer_draft_id".to_string(),
            ]
        );
        assert!(build_intent_preview.missing_inputs.is_empty());
        assert!(build_intent_preview
            .intent_reasons
            .iter()
            .any(|reason| reason.contains("All inspected items were supported by local evidence")));
        assert!(build_intent_preview
            .warnings
            .iter()
            .any(|warning| warning.kind == "intent_ready_later"));
        assert!(build_intent_preview
            .next_required_actions
            .iter()
            .any(|action| action.contains("A future user-confirmed GroundedAnswer service call can be added later without changing this preview.")));
        assert_grounded_answer_build_intent_boundary_fields(&build_intent_preview);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_build_request(
            temp.path(),
            build_request_request(
                "   ",
                Some("Alpha beta."),
                vec!["src_demo".to_string()],
                Some("draft-1"),
                true,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_rejects_invalid_answer_draft_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/draft", "evil\\draft"] {
            let result = preview_scholar_chat_grounded_answer_build_request(
                temp.path(),
                build_request_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some(invalid),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::AnswerDraftInvalidId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_build_request(
                temp.path(),
                build_request_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec![invalid.to_string()],
                    Some("draft-1"),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_build_request_deterministic_and_path_free(
            &temp,
            build_request_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.required_inputs.contains(&"selected_source_ids".to_string()));
        assert!(result.missing_inputs.contains(&"selected_source_ids".to_string()));
        assert_grounded_answer_build_request_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_treats_whitespace_answer_draft_id_as_missing_input() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_request_deterministic_and_path_free(
            &temp,
            build_request_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("   "),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert!(result.required_inputs.contains(&"answer_draft_id".to_string()));
        assert!(result.missing_inputs.contains(&"answer_draft_id".to_string()));
        assert!(result.answer_draft_id.is_none());
        assert_grounded_answer_build_request_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_needs_review_when_build_intent_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_request_deterministic_and_path_free(
            &temp,
            build_request_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result
            .request_reasons
            .iter()
            .any(|reason| reason.contains("Weakly supported or unsupported draft items remain")));
        assert_grounded_answer_build_request_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_is_ready_only_when_build_intent_is_ready_later() {
        let temp = tempfile::tempdir().unwrap();
        let source_a = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let source_b = build_source_with_index(&temp, "alpha beta gamma\nalpha beta epsilon\n");
        let request = build_request_request(
            "alpha grounded evidence",
            Some("Alpha beta. Alpha beta gamma."),
            vec![format!("  {source_b}  "), format!("  {source_a}  ")],
            Some("  draft-1  "),
            true,
        );
        let first = assert_grounded_answer_build_request_deterministic_and_path_free(&temp, request.clone());
        let build_intent_preview = preview_scholar_chat_grounded_answer_build_intent(
            temp.path(),
            request.build_intent_request.clone(),
        )
        .unwrap();
        assert_eq!(first.status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert_eq!(first.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater);
        assert_eq!(first.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(first.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(first.selected_source_count, 2);
        assert_eq!(first.selected_source_ids, vec![source_b.clone(), source_a.clone()]);
        assert_eq!(first.answer_draft_id.as_deref(), Some("draft-1"));
        assert_eq!(first.selected_source_count, build_intent_preview.selected_source_count);
        assert_eq!(first.evidence_candidate_count, build_intent_preview.evidence_candidate_count);
        assert_eq!(first.inspected_item_count, build_intent_preview.inspected_item_count);
        assert_eq!(first.supported_item_count, build_intent_preview.supported_item_count);
        assert_eq!(first.weakly_supported_item_count, build_intent_preview.weakly_supported_item_count);
        assert_eq!(first.unsupported_item_count, build_intent_preview.unsupported_item_count);
        assert_eq!(first.candidate_statement_count, build_intent_preview.candidate_statement_count);
        assert!(first
            .request_reasons
            .iter()
            .any(|reason| reason.contains("ready later for a future GroundedAnswer service call")));
        assert_grounded_answer_build_request_boundary_fields(&first);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_request_allows_missing_answer_draft_file_when_ready_later() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_request_deterministic_and_path_free(
            &temp,
            build_request_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("missing-draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(result.answer_draft_id.as_deref(), Some("missing-draft-1"));
        assert!(result.missing_inputs.contains(&"answer_draft_id".to_string()) == false);
        assert_grounded_answer_build_request_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_build_preflight(
            temp.path(),
            build_preflight_request(
                "   ",
                Some("Alpha beta."),
                vec!["src_demo".to_string()],
                Some("draft-1"),
                true,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_rejects_invalid_answer_draft_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/draft", "evil\\draft"] {
            let result = preview_scholar_chat_grounded_answer_build_preflight(
                temp.path(),
                build_preflight_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some(invalid),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::AnswerDraftInvalidId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_build_preflight(
                temp.path(),
                build_preflight_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec![invalid.to_string()],
                    Some("draft-1"),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_rejects_invalid_selected_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_build_preflight(
                temp.path(),
                build_preflight_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec![invalid.to_string()],
                    Some("draft-1"),
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_blocks_when_build_request_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.missing_inputs.contains(&"build_request_ready_later".to_string()));
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_needs_review_when_build_request_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
                Some("draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert!(result
            .preflight_reasons
            .iter()
            .any(|reason| reason.contains("needs review")));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_blocks_without_answer_draft_id() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                None,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_id_missing"));
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_treats_whitespace_answer_draft_id_as_missing_input() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("   "),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_id_missing"));
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_blocks_when_answer_draft_missing() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some("missing-draft-1"),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
        assert_eq!(result.answer_draft_claim_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_missing"));
        assert!(result.missing_inputs.contains(&"answer_draft_present".to_string()));
        assert!(result.missing_inputs.contains(&"answer_draft_readable".to_string()));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_blocks_when_answer_draft_malformed() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, version_id, _) = build_readable_answer_draft_fixture(&temp);
        let draft_path = crate::corpus_paths::CorpusPaths::new(temp.path())
            .source_version_dir(&source_id, &version_id)
            .join("answer_drafts")
            .join(format!("{answer_draft_id}.json"));
        fs::write(&draft_path, "{not valid json").unwrap();
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(
            &temp,
            build_preflight_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some(&answer_draft_id),
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert!(result.answer_draft_present);
        assert!(!result.answer_draft_readable);
        assert_eq!(result.answer_draft_claim_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_unreadable"));
        assert!(result.missing_inputs.contains(&"answer_draft_readable".to_string()));
    }

    #[test]
    fn scholar_chat_grounded_answer_build_preflight_is_ready_only_when_build_request_is_ready_and_answer_draft_readable() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, claim_count) = build_readable_answer_draft_fixture(&temp);
        let request = build_preflight_request(
            "alpha grounded evidence",
            Some("Alpha beta. Alpha beta gamma."),
            vec![format!("  {source_id}  ")],
            Some(&answer_draft_id),
            true,
        );
        let result = assert_grounded_answer_build_preflight_deterministic_and_path_free(&temp, request);
        assert_eq!(result.status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert_eq!(result.answer_draft_id.as_deref(), Some(answer_draft_id.as_str()));
        assert!(result.answer_draft_present);
        assert!(result.answer_draft_readable);
        assert_eq!(result.answer_draft_claim_count, claim_count);
        assert_eq!(result.selected_source_ids, vec![source_id]);
        assert!(result
            .preflight_reasons
            .iter()
            .any(|reason| reason.contains("Answer draft readable: true")));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.kind == "preflight_ready_later"));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_execution_readiness(
            temp.path(),
            execution_readiness_request(
                "   ",
                Some("Alpha beta."),
                vec!["src_demo".to_string()],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_rejects_invalid_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/draft", "evil\\draft"] {
            let result = preview_scholar_chat_grounded_answer_execution_readiness(
                temp.path(),
                execution_readiness_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some(invalid),
                    true,
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::AnswerDraftInvalidId)));
            assert!(!temp.path().join(".aegis").exists());
        }
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_execution_readiness(
                temp.path(),
                execution_readiness_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec![invalid.to_string()],
                    Some("draft-1"),
                    true,
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_blocks_when_build_preflight_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.missing_inputs.contains(&"build_preflight_ready_later".to_string()));
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_needs_review_when_build_preflight_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert!(result
            .readiness_reasons
            .iter()
            .any(|reason| reason.contains("needs review")));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_blocks_when_answer_draft_missing() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let draft_path = crate::corpus_paths::CorpusPaths::new(temp.path())
            .source_version_dir(&source_id, &version_id)
            .join("answer_drafts")
            .join(format!("{answer_draft_id}.json"));
        fs::remove_file(&draft_path).unwrap();
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![format!("  {source_id}  ")],
                Some(&format!("  {answer_draft_id}  ")),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_missing"));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_blocks_when_answer_draft_malformed() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let draft_path = crate::corpus_paths::CorpusPaths::new(temp.path())
            .source_version_dir(&source_id, &version_id)
            .join("answer_drafts")
            .join(format!("{answer_draft_id}.json"));
        fs::write(&draft_path, "{not valid json").unwrap();
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![format!("  {source_id}  ")],
                Some(&format!("  {answer_draft_id}  ")),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert!(result.answer_draft_present);
        assert!(!result.answer_draft_readable);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "answer_draft_unreadable"));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_blocks_without_execution_consent() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, claim_count) = build_readable_answer_draft_fixture(&temp);
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "  alpha grounded evidence  ",
                Some("  Alpha beta. Alpha beta gamma.  "),
                vec![format!("  {source_id}  ")],
                Some(&format!("  {answer_draft_id}  ")),
                true,
                false,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater);
        assert_eq!(result.answer_draft_id.as_deref(), Some(answer_draft_id.as_str()));
        assert_eq!(result.selected_source_ids, vec![source_id]);
        assert_eq!(result.answer_draft_claim_count, claim_count);
        assert_eq!(result.missing_inputs, vec!["execution_consent".to_string()]);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "execution_consent_missing"));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_readiness_is_ready_later_only_when_execution_consent_is_given() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, claim_count) = build_readable_answer_draft_fixture(&temp);
        let result = assert_grounded_answer_execution_readiness_deterministic_and_path_free(
            &temp,
            execution_readiness_request(
                "  alpha grounded evidence  ",
                Some("  Alpha beta. Alpha beta gamma.  "),
                vec![format!("  {source_id}  ")],
                Some(&format!("  {answer_draft_id}  ")),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater);
        assert_eq!(result.answer_draft_id.as_deref(), Some(answer_draft_id.as_str()));
        assert_eq!(result.selected_source_ids, vec![source_id]);
        assert_eq!(result.answer_draft_claim_count, claim_count);
        assert!(result.missing_inputs.is_empty());
        assert!(result
            .readiness_reasons
            .iter()
            .any(|reason| reason.contains("Execution consent: true")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("future GroundedAnswer service call")));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_execution_plan(
            temp.path(),
            execution_plan_request(
                "   ",
                Some("Alpha beta."),
                vec!["src_demo".to_string()],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_rejects_invalid_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["..", "../evil", "evil/draft", "evil\\draft"] {
            let result = preview_scholar_chat_grounded_answer_execution_plan(
                temp.path(),
                execution_plan_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some(invalid),
                    true,
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::AnswerDraftInvalidId)));
            assert!(!temp.path().join(".aegis").exists());
        }
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_execution_plan(
                temp.path(),
                execution_plan_request(
                    "alpha grounded evidence",
                    Some("Alpha beta."),
                    vec![invalid.to_string()],
                    Some("draft-1"),
                    true,
                    true,
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_blocks_when_readiness_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_execution_plan_deterministic_and_path_free(
            &temp,
            execution_plan_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionPlanStatus::Blocked);
        assert_eq!(result.readiness_status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::Blocked);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::Blocked);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::Blocked);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert!(result.missing_inputs.contains(&"build_preflight_ready_later".to_string()));
        assert!(!result.answer_draft_present);
        assert!(!result.answer_draft_readable);
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_needs_review_when_readiness_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_execution_plan_deterministic_and_path_free(
            &temp,
            execution_plan_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
                Some("draft-1"),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview);
        assert_eq!(result.readiness_status, ScholarChatGroundedAnswerExecutionReadinessStatus::NeedsReview);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::NeedsReview);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::NeedsReview);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::NeedsReview);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert!(result
            .plan_reasons
            .iter()
            .any(|reason| reason.contains("still needs review")));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_is_ready_later_only_when_execution_consent_is_given() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, claim_count) = build_readable_answer_draft_fixture(&temp);
        let result = assert_grounded_answer_execution_plan_deterministic_and_path_free(
            &temp,
            execution_plan_request(
                "  alpha grounded evidence  ",
                Some("  Alpha beta. Alpha beta gamma.  "),
                vec![format!("  {source_id}  ")],
                Some(&format!("  {answer_draft_id}  ")),
                true,
                true,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater);
        assert_eq!(result.readiness_status, ScholarChatGroundedAnswerExecutionReadinessStatus::ExecutionReadyLater);
        assert_eq!(result.build_preflight_status, ScholarChatGroundedAnswerBuildPreflightStatus::PreflightReadyLater);
        assert_eq!(result.build_request_status, ScholarChatGroundedAnswerBuildRequestStatus::RequestReadyLater);
        assert_eq!(result.build_intent_status, ScholarChatGroundedAnswerBuildIntentStatus::IntentReadyLater);
        assert_eq!(result.write_eligibility_status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(result.answer_draft_id.as_deref(), Some(answer_draft_id.as_str()));
        assert_eq!(result.selected_source_ids, vec![source_id]);
        assert_eq!(result.answer_draft_claim_count, claim_count);
        assert!(result.missing_inputs.is_empty());
        assert_eq!(result.planned_operation, "future_grounded_answer_build");
        assert!(result.planned_inputs.contains(&"execution_consent".to_string()));
        assert!(result.planned_outputs.contains(&"future_grounded_answer_status".to_string()));
        assert!(result.planned_write_targets.contains(&"audit_log_entry".to_string()));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("planned later")));
    }

    #[test]
    fn scholar_chat_grounded_answer_execution_plan_blocks_when_execution_consent_is_missing() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let result = assert_grounded_answer_execution_plan_deterministic_and_path_free(
            &temp,
            execution_plan_request(
                "alpha grounded evidence",
                Some("Alpha beta. Alpha beta gamma."),
                vec![source_id],
                Some(&answer_draft_id),
                true,
                false,
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerExecutionPlanStatus::Blocked);
        assert_eq!(result.readiness_status, ScholarChatGroundedAnswerExecutionReadinessStatus::Blocked);
        assert_eq!(result.missing_inputs, vec!["execution_consent".to_string()]);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "execution_consent_missing"));
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_blocks_when_topic_is_blank() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("   ", Some("course"), Some("Module handbook")),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificDisciplineRegistryStatus::Blocked);
        assert!(result.recognized_concept.is_none());
        assert!(result.discipline_path.is_empty());
        assert!(result.planned_queries.is_empty());
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.contains("topic_missing")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Provide a scientific topic")));
        assert_scientific_discipline_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_maps_signalentdeckung() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request(" Signalentdeckung ", None, None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(result.normalized_topic, "Signalentdeckung");
        assert_eq!(result.normalized_mode, "scholar_chat");
        assert_eq!(result.recognized_concept.as_deref(), Some("signal_detection_theory"));
        assert_eq!(result.label.as_deref(), Some("Signalentdeckungstheorie"));
        assert_eq!(
            result.discipline_path,
            vec![
                "psychology".to_string(),
                "general_psychology".to_string(),
                "perception".to_string(),
                "psychophysics".to_string(),
                "signal_detection_theory".to_string(),
            ]
        );
        assert_eq!(
            result.parent_path,
            vec![
                "psychology".to_string(),
                "general_psychology".to_string(),
                "perception".to_string(),
                "psychophysics".to_string(),
            ]
        );
        assert_eq!(
            result.related_methods,
            vec![
                "statistics".to_string(),
                "probability_theory".to_string(),
                "decision_theory".to_string(),
                "roc_analysis".to_string(),
            ]
        );
        assert_eq!(
            result.preferred_sources,
            vec![
                "pubpsych".to_string(),
                "psycharchives".to_string(),
                "openalex".to_string(),
                "crossref".to_string(),
                "pubmed_if_biomedical_context".to_string(),
            ]
        );
        assert_eq!(
            result.curriculum_sources,
            vec![
                "tu_darmstadt_module_handbook_candidate".to_string(),
                "local_course_materials_later".to_string(),
            ]
        );
        assert_eq!(
            result.canonical_mappings,
            vec![
                "psychology".to_string(),
                "general_psychology".to_string(),
                "psychophysics".to_string(),
                "signal_detection_theory".to_string(),
            ]
        );
        assert_eq!(
            result.planned_queries,
            vec![
                "Signalentdeckungstheorie".to_string(),
                "signal detection theory".to_string(),
                "psychophysics signal detection".to_string(),
                "d prime criterion ROC".to_string(),
            ]
        );
        assert_eq!(result.science_class, Some(ScholarChatScientificDisciplineScienceClass::CoreScience));
        assert_scientific_discipline_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_maps_anova() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("ANOVA", Some("scientific_paper"), None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(result.recognized_concept.as_deref(), Some("analysis_of_variance"));
        assert_eq!(result.label.as_deref(), Some("ANOVA / Varianzanalyse"));
        assert_eq!(
            result.discipline_path,
            vec![
                "statistics".to_string(),
                "inferential_statistics".to_string(),
                "hypothesis_testing".to_string(),
                "analysis_of_variance".to_string(),
            ]
        );
        assert_eq!(result.science_class, Some(ScholarChatScientificDisciplineScienceClass::CoreScience));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Scientific Paper Mode")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("literature search")));
        assert_scientific_discipline_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_maps_hypothesentests() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("Hypothesentests", Some("course"), Some("Module 123")),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(result.recognized_concept.as_deref(), Some("hypothesis_testing"));
        assert_eq!(result.label.as_deref(), Some("Hypothesentests"));
        assert_eq!(
            result.discipline_path,
            vec![
                "statistics".to_string(),
                "inferential_statistics".to_string(),
                "hypothesis_testing".to_string(),
            ]
        );
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Course Mode")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("course materials")));
        assert_scientific_discipline_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_returns_unknown_concept_for_unmapped_topic() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("Signal graph theory", None, None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificDisciplineRegistryStatus::UnknownConcept);
        assert_eq!(result.normalized_topic, "Signal graph theory");
        assert!(result.recognized_concept.is_none());
        assert!(result.discipline_path.is_empty());
        assert_eq!(result.planned_queries, vec!["Signal graph theory".to_string()]);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("not yet in the local preview registry")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add a discipline registry mapping")));
        assert_eq!(result.planned_queries, vec!["Signal graph theory".to_string()]);
        assert_scientific_discipline_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("  ANOVA  ", Some("scientific paper"), Some("Linear models")),
        )
        .unwrap();
        let second = preview_scholar_chat_scientific_discipline_registry(
            temp.path(),
            scientific_discipline_registry_request("  ANOVA  ", Some("scientific paper"), Some("Linear models")),
        )
        .unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_discipline_registry_boundary_fields(preview);
        }
    }

    #[test]
    fn scholar_chat_scientific_discipline_registry_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_discipline_registry")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest"));
        assert!(!body.contains("ureq"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_scientific_source_registry_blocks_when_topic_is_blank() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request("   ", Some("course"), Some("Module handbook"), Some(vec!["psychology"])),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificSourceRegistryStatus::Blocked);
        assert_eq!(result.discipline_status, ScholarChatScientificDisciplineRegistryStatus::Blocked);
        assert!(result.recognized_concept.is_none());
        assert!(result.source_families.is_empty());
        assert!(result.preferred_source_ids.is_empty());
        assert!(result.conditional_source_ids.is_empty());
        assert!(result.planned_metadata_queries.is_empty());
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.contains("topic_missing")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Provide a scientific topic")));
        assert_scientific_source_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_maps_signalentdeckung() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request("Signalentdeckung", None, None, None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady);
        assert_eq!(result.discipline_status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(result.normalized_mode, "scholar_chat");
        assert_eq!(result.recognized_concept.as_deref(), Some("signal_detection_theory"));
        assert_eq!(
            result.preferred_source_ids,
            vec![
                "pubpsych".to_string(),
                "psycharchives".to_string(),
                "openalex".to_string(),
                "crossref".to_string(),
            ]
        );
        assert_eq!(result.conditional_source_ids, vec!["pubmed_if_biomedical_context".to_string()]);
        assert_eq!(result.excluded_source_ids, vec!["pubmed_if_biomedical_context".to_string()]);
        assert_eq!(result.planned_metadata_queries[0], "Signalentdeckungstheorie");
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "pubpsych" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "psycharchives" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "openalex" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "crossref" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "pubmed_if_biomedical_context" && !family.active_for_current_context));
        assert_eq!(result.access_classes, vec![ScholarChatScientificSourceAccessClass::OpenMetadata]);
        assert_eq!(result.source_plan.source_family_count, 5);
        assert_eq!(result.source_plan.active_source_family_count, 4);
        assert_eq!(result.source_plan.conditional_source_family_count, 1);
        assert!(result
            .ranking_hints
            .iter()
            .any(|hint| hint.contains("psychology and psychophysics")));
        assert!(result
            .deduplication_hints
            .iter()
            .any(|hint| hint.contains("DOI, title, and source identifiers")));
        assert_scientific_source_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_activates_pubmed_for_biomedical_context() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "Signalentdeckung",
                None,
                None,
                Some(vec!["BiomEdical", " neuroscience ", "medical"]),
            ),
        )
        .unwrap();
        let pubmed = result
            .source_families
            .iter()
            .find(|family| family.id == "pubmed_if_biomedical_context")
            .unwrap();
        assert!(pubmed.active_for_current_context);
        assert_eq!(result.excluded_source_ids, Vec::<String>::new());
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("biomedical")));
        assert_scientific_source_registry_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_maps_anova_and_activates_theory_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "ANOVA",
                Some("scientific_paper"),
                Some("Psychology statistics seminar"),
                None,
            ),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady);
        assert_eq!(result.discipline_status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(
            result.preferred_source_ids,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "pubpsych_if_psychology_context" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "psycharchives_if_psychology_context" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "zbmath_if_theory_context" && family.active_for_current_context));
        assert!(result
            .source_families
            .iter()
            .any(|family| family.id == "arxiv_if_theory_context" && family.active_for_current_context));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("literature search planning")));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Science Paper Mode"))
            || result
                .warnings
                .iter()
                .any(|warning| warning.contains("Scientific Paper Mode")));
        assert_eq!(result.source_plan.conditional_source_family_count, 4);
        assert_eq!(result.source_plan.active_source_family_count, 6);
        assert_scientific_source_registry_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_activates_zbmath_and_arxiv_for_theory_context() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "ANOVA",
                Some("course"),
                Some("Linear models"),
                Some(vec![" theory ", "statistics", "statistics", "math"]),
            ),
        )
        .unwrap();
        let zbmath = result
            .source_families
            .iter()
            .find(|family| family.id == "zbmath_if_theory_context")
            .unwrap();
        let arxiv = result
            .source_families
            .iter()
            .find(|family| family.id == "arxiv_if_theory_context")
            .unwrap();
        assert!(zbmath.active_for_current_context);
        assert!(arxiv.active_for_current_context);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Course Mode")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("local course materials")));
        assert_scientific_source_registry_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_maps_hypothesentests() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request("Hypothesentests", None, None, None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady);
        assert_eq!(result.discipline_status, ScholarChatScientificDisciplineRegistryStatus::ConceptMapped);
        assert_eq!(
            result.preferred_source_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "zbmath".to_string(),
                "arxiv".to_string(),
            ]
        );
        assert_eq!(
            result.conditional_source_ids,
            vec![
                "pubpsych_if_psychology_context".to_string(),
                "psycharchives_if_psychology_context".to_string(),
            ]
        );
        assert_eq!(
            result.excluded_source_ids,
            vec![
                "pubpsych_if_psychology_context".to_string(),
                "psycharchives_if_psychology_context".to_string(),
            ]
        );
        assert_eq!(result.planned_metadata_queries[0], "Hypothesentests");
        assert_eq!(result.access_classes, vec![ScholarChatScientificSourceAccessClass::OpenMetadata]);
        assert_scientific_source_registry_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_returns_unknown_concept_for_unmapped_topic() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request("Signal graph theory", Some("scholar_chat"), None, Some(vec!["unknown"])),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificSourceRegistryStatus::UnknownConcept);
        assert_eq!(result.discipline_status, ScholarChatScientificDisciplineRegistryStatus::UnknownConcept);
        assert!(result.recognized_concept.is_none());
        assert!(result.source_families.is_empty());
        assert_eq!(result.planned_metadata_queries, vec!["Signal graph theory".to_string()]);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("not yet mapped through the discipline registry")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("adding discipline mapping")));
        assert_scientific_source_registry_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
    }

    #[test]
    fn scholar_chat_scientific_source_registry_normalizes_context_tags() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "ANOVA",
                None,
                None,
                Some(vec!["  Psychology  ", "theory", "psychology", "statistics", "theory-methods", ""]),
            ),
        )
        .unwrap();
        assert_eq!(
            result.normalized_context_tags,
            vec![
                "psychology".to_string(),
                "statistics".to_string(),
                "theory".to_string(),
                "theory_methods".to_string(),
            ]
        );
        assert_scientific_source_registry_boundary_fields(&result);
    }

    #[test]
    fn scholar_chat_scientific_source_registry_is_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "  ANOVA  ",
                Some("scientific paper"),
                Some("Psychology statistics seminar"),
                Some(vec!["statistics", "psychology", "theory"]),
            ),
        )
        .unwrap();
        let second = preview_scholar_chat_scientific_source_registry(
            temp.path(),
            scientific_source_registry_request(
                "  ANOVA  ",
                Some("scientific paper"),
                Some("Psychology statistics seminar"),
                Some(vec!["statistics", "psychology", "theory"]),
            ),
        )
        .unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_source_registry_boundary_fields(preview);
        }
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_scientific_query_understanding(
            temp.path(),
            scientific_query_understanding_request("   ", None, None, None),
        )
        .unwrap();
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Blocked);
        assert!(result.normalized_query.is_empty());
        assert!(result.inferred_topic.is_none());
        assert!(result.recognized_concept.is_none());
        assert!(result.label.is_none());
        assert!(result.detected_aliases.is_empty());
        assert!(result.planned_metadata_queries.is_empty());
        assert!(result.planned_local_search_queries.is_empty());
        assert!(result.planned_expanded_queries.is_empty());
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.contains("query_missing")));
        assert_scientific_query_understanding_boundary_fields(&result);
        let debug = format!("{result:?}");
        let json = serde_json::to_string(&result).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert_eq!(count_entries_recursively(temp.path()), 0);
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_maps_signalentdeckung_and_includes_registry_ids() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("Signalentdeckung", None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Understood);
        assert_eq!(result.inferred_topic.as_deref(), Some("Signalentdeckung"));
        assert_eq!(result.recognized_concept.as_deref(), Some("signal_detection_theory"));
        assert_eq!(result.label.as_deref(), Some("Signalentdeckungstheorie"));
        assert_eq!(result.source_registry_status, ScholarChatScientificSourceRegistryStatus::SourcePlanReady);
        assert!(result.preferred_source_ids.iter().any(|value| value == "pubpsych"));
        assert!(result.preferred_source_ids.iter().any(|value| value == "psycharchives"));
        assert!(result.preferred_source_ids.iter().any(|value| value == "openalex"));
        assert!(result.preferred_source_ids.iter().any(|value| value == "crossref"));
        assert!(result
            .planned_metadata_queries
            .iter()
            .any(|value| value == "Signalentdeckungstheorie"));
        assert!(result
            .planned_local_search_queries
            .iter()
            .any(|value| value == "Signalentdeckung"));
        assert!(result
            .detected_aliases
            .iter()
            .any(|alias| alias.alias == "Signalentdeckung" && alias.language == "german"));
        assert!(result.language_hints.iter().any(|hint| hint == "german"));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_detects_english_signal_detection_alias_and_bridge_queries() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("Explain signal detection theory and d-prime", None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Understood);
        assert_eq!(result.query_intent, ScholarChatScientificQueryIntent::ConceptExplanation);
        assert!(result
            .detected_aliases
            .iter()
            .any(|alias| alias.alias == "signal detection theory" && alias.language == "english"));
        assert!(result.language_hints.iter().any(|hint| hint == "english"));
        assert!(result
            .planned_expanded_queries
            .iter()
            .any(|value| value == "Signal Detection Theory"));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_anova_literature_query_prefers_literature_search_and_evidence_requirements() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("ANOVA literature review", Some("scholar_chat"), None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Understood);
        assert_eq!(result.query_intent, ScholarChatScientificQueryIntent::LiteratureSearch);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "citation_safe_metadata_required"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "deduplication_required_before_literature_review"));
        assert!(result
            .planned_metadata_queries
            .iter()
            .any(|value| value == "ANOVA"));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_hypothesentests_course_query_prefers_course_learning_and_course_requirements() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("Hypothesentests für die Klausur", Some("course"), None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Understood);
        assert_eq!(result.query_intent, ScholarChatScientificQueryIntent::CourseLearning);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "course_material_alignment_required"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "local_evidence_required_before_answer"));
        assert!(result.no_local_file_indexing);
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_marks_mixed_concepts_ambiguous_and_prefers_first_occurrence() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("Hypothesentests und ANOVA Vergleich", None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::Ambiguous);
        assert_eq!(result.ambiguity_level, ScholarChatScientificAmbiguityLevel::Medium);
        assert_eq!(result.inferred_topic.as_deref(), Some("Hypothesentests"));
        assert!(result
            .ambiguity_warnings
            .iter()
            .any(|warning| warning.contains("Hypothesentests")));
        assert!(result
            .ambiguity_warnings
            .iter()
            .any(|warning| warning.contains("ANOVA")));
        assert_eq!(result.detected_aliases.first().map(|alias| alias.alias.as_str()), Some("Hypothesentest"));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_returns_unknown_concept_with_normalized_query_and_local_search() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("  Signal graph theory  ", None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificQueryUnderstandingStatus::UnknownConcept);
        assert_eq!(result.inferred_topic.as_deref(), Some("Signal graph theory"));
        assert!(result.recognized_concept.is_none());
        assert!(result.label.is_none());
        assert_eq!(result.planned_metadata_queries, vec!["Signal graph theory".to_string()]);
        assert_eq!(result.planned_local_search_queries, vec!["Signal graph theory".to_string()]);
        assert!(result.planned_expanded_queries.is_empty());
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("does not yet map to a known scientific concept")));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_normalizes_context_tags_and_passes_them_through() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request(
                "ANOVA",
                None,
                Some("Psychology statistics seminar"),
                Some(vec!["  neuroscience  ", "Psychology", "neuroscience", "clinical-science"]),
            ),
        );
        assert_eq!(
            result.normalized_context_tags,
            vec![
                "clinical_science".to_string(),
                "neuroscience".to_string(),
                "psychology".to_string(),
            ]
        );
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_scientific_paper_mode_forces_literature_search() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("x", Some("scientific_paper"), None, None),
        );
        assert_eq!(result.query_intent, ScholarChatScientificQueryIntent::LiteratureSearch);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "citation_safe_metadata_required"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "deduplication_required_before_literature_review"));
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_course_mode_forces_course_learning_and_no_local_file_indexing() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_query_understanding_deterministic_and_path_free(
            &temp,
            scientific_query_understanding_request("x", Some("course"), None, None),
        );
        assert_eq!(result.query_intent, ScholarChatScientificQueryIntent::CourseLearning);
        assert!(result.no_local_file_indexing);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "course_material_alignment_required"));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("   ", None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificSearchPlanStatus::Blocked);
        assert_eq!(result.search_strategy, ScholarChatScientificSearchStrategy::Blocked);
        assert_eq!(result.query_understanding_status, ScholarChatScientificQueryUnderstandingStatus::Blocked);
        assert!(result.normalized_query.is_empty());
        assert!(result.inferred_topic.is_none());
        assert!(result.recognized_concept.is_none());
        assert!(result.label.is_none());
        assert!(result.selected_local_source_ids.is_empty());
        assert!(result.planned_local_queries.is_empty());
        assert!(result.planned_metadata_queries.is_empty());
        assert!(result.planned_expanded_queries.is_empty());
        assert!(result.blockers.iter().any(|blocker| blocker.contains("query_missing")));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_maps_signalentdeckung_to_local_first_and_includes_source_routing() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("Signalentdeckung", None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificSearchPlanStatus::SearchPlanReady);
        assert_eq!(result.search_strategy, ScholarChatScientificSearchStrategy::LocalFirst);
        assert_eq!(result.query_understanding_status, ScholarChatScientificQueryUnderstandingStatus::Understood);
        assert_eq!(result.inferred_topic.as_deref(), Some("Signalentdeckung"));
        assert_eq!(result.local_search_plan.local_source_count, 0);
        assert!(result.local_search_plan.local_first);
        assert!(result
            .source_routing_plan
            .active_routes
            .iter()
            .any(|value| value == "pubpsych"));
        assert!(result
            .source_routing_plan
            .active_routes
            .iter()
            .any(|value| value == "psycharchives"));
        assert!(result
            .source_routing_plan
            .active_routes
            .iter()
            .any(|value| value == "openalex"));
        assert!(result
            .source_routing_plan
            .active_routes
            .iter()
            .any(|value| value == "crossref"));
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("No local sources selected")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select or import local sources")));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_normalizes_selected_local_source_ids_without_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["  local-b  ", "local-a", "local-b", ""]),
            ),
        );
        assert_eq!(
            result.selected_local_source_ids,
            vec!["local-a".to_string(), "local-b".to_string()]
        );
        assert_eq!(result.local_search_plan.local_source_count, 2);
        assert!(result
            .local_search_plan
            .selected_local_source_ids
            .iter()
            .all(|value| value == "local-a" || value == "local-b"));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_uses_metadata_first_for_scientific_paper_mode() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("ANOVA", Some("scientific_paper"), None, None, None),
        );
        assert_eq!(result.search_strategy, ScholarChatScientificSearchStrategy::MetadataFirst);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "citation_safe_metadata_required"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "deduplication_required_before_literature_review"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "ranking_required_before_answer"));
        assert!(!result.metadata_search_plan.will_call_connectors);
        assert!(!result.metadata_search_plan.will_make_web_requests);
    }

    #[test]
    fn scholar_chat_scientific_search_plan_uses_course_local_first_and_course_alignment() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("Hypothesentests", Some("course"), Some("Module 123"), None, None),
        );
        assert_eq!(result.search_strategy, ScholarChatScientificSearchStrategy::CourseLocalFirst);
        assert!(result.local_search_plan.local_first);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "course_material_alignment_required"));
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "local_evidence_required_before_answer"));
        assert!(!result.local_search_plan.will_read_files);
        assert!(!result.local_search_plan.will_build_index);
    }

    #[test]
    fn scholar_chat_scientific_search_plan_marks_mixed_concepts_needs_disambiguation_and_keeps_first_concept() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("ANOVA und Hypothesentests Vergleich", None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificSearchPlanStatus::NeedsDisambiguation);
        assert_eq!(result.query_understanding_status, ScholarChatScientificQueryUnderstandingStatus::Ambiguous);
        assert!(result.inferred_topic.is_some());
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("multiple scientific concepts")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Narrow the scientific concept")));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_returns_unknown_concept_with_search_requirements() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request("Signal graph theory", None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatScientificSearchPlanStatus::UnknownConcept);
        assert_eq!(result.query_understanding_status, ScholarChatScientificQueryUnderstandingStatus::UnknownConcept);
        assert_eq!(result.normalized_query, "Signal graph theory");
        assert_eq!(result.planned_local_queries, vec!["Signal graph theory".to_string()]);
        assert_eq!(result.planned_metadata_queries, vec!["Signal graph theory".to_string()]);
        assert!(result
            .evidence_requirements
            .iter()
            .any(|value| value == "source_family_plan_required"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add discipline and source registry mappings")));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_emits_stable_step_order_and_preview_only_constraints() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_scientific_search_plan_deterministic_and_path_free(
            &temp,
            scientific_search_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["local-b", "local-a"]),
            ),
        );
        let kinds = result
            .planned_search_steps
            .iter()
            .map(|step| step.kind.clone())
            .collect::<Vec<_>>();
        assert_eq!(
            kinds,
            vec![
                ScholarChatScientificSearchPlanStepKind::QueryExpansion,
                ScholarChatScientificSearchPlanStepKind::LocalSourceSearch,
                ScholarChatScientificSearchPlanStepKind::SourceFamilyRouting,
                ScholarChatScientificSearchPlanStepKind::MetadataSourceSearch,
                ScholarChatScientificSearchPlanStepKind::RankingPlan,
                ScholarChatScientificSearchPlanStepKind::DeduplicationPlan,
                ScholarChatScientificSearchPlanStepKind::EvidenceRequirementCheck,
            ]
        );
        assert!(result
            .planned_search_steps
            .iter()
            .all(|step| step.preview_only && step.boundary_notes.iter().any(|note| note == "preview-only")));
        assert!(!result.metadata_search_plan.will_call_connectors);
        assert!(!result.metadata_search_plan.will_make_web_requests);
        assert!(!result.local_search_plan.will_read_files);
        assert!(!result.local_search_plan.will_build_index);
    }

    #[test]
    fn scholar_chat_local_literature_index_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request("   ", None, None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatLocalLiteratureIndexStatus::Blocked);
        assert_eq!(result.local_index_strategy, ScholarChatLocalLiteratureIndexStrategy::Blocked);
        assert_eq!(result.search_plan_status, ScholarChatScientificSearchPlanStatus::Blocked);
        assert_eq!(result.query_understanding_status, ScholarChatScientificQueryUnderstandingStatus::Blocked);
        assert!(result.normalized_query.is_empty());
        assert!(result.selected_local_source_ids.is_empty());
        assert!(result.expected_source_kinds.is_empty());
        assert!(result.blockers.iter().any(|blocker| blocker.contains("query_missing")));
    }

    #[test]
    fn scholar_chat_local_literature_index_maps_signalentdeckung_to_scholar_chat_local_first() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["  source-b  ", "source-a", "source-b"]),
                Some(vec![" pdf ", "markdown"]),
            ),
        );
        assert_eq!(result.status, ScholarChatLocalLiteratureIndexStatus::IndexPlanReady);
        assert_eq!(result.local_index_strategy, ScholarChatLocalLiteratureIndexStrategy::ScholarChatLocalFirst);
        assert_eq!(result.search_plan_status, ScholarChatScientificSearchPlanStatus::SearchPlanReady);
        assert_eq!(
            result.selected_local_source_ids,
            vec!["source-a".to_string(), "source-b".to_string()]
        );
        assert!(result
            .local_corpus_plan
            .selected_local_source_ids
            .iter()
            .any(|value| value == "source-a"));
        assert_eq!(
            result.expected_source_kinds,
            vec!["markdown".to_string(), "pdf".to_string()]
        );
        assert!(result
            .planned_index_steps
            .iter()
            .all(|step| step.active));
    }

    #[test]
    fn scholar_chat_local_literature_index_reports_needs_local_sources_without_reading_files() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request("Signalentdeckung", None, None, None, None, None),
        );
        assert_eq!(result.status, ScholarChatLocalLiteratureIndexStatus::NeedsLocalSources);
        assert_eq!(result.ingestion_readiness, ScholarChatLocalLiteratureIngestionReadiness::NeedsSources);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("No local sources selected")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select or import local sources")));
        assert!(!result.local_corpus_plan.corpus_manifest_would_be_required);
        assert!(!result.local_corpus_plan.will_create_corpus);
        assert!(!result.local_corpus_plan.will_read_files);
    }

    #[test]
    fn scholar_chat_local_literature_index_normalizes_expected_source_kinds_and_warns_on_unknown_kind() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["source-a"]),
                Some(vec!["  book-chapter  ", "notes", "alien-kind", "book_chapter", ""]),
            ),
        );
        assert_eq!(
            result.expected_source_kinds,
            vec![
                "alien_kind".to_string(),
                "book_chapter".to_string(),
                "notes".to_string(),
            ]
        );
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Unknown expected source kinds")));
    }

    #[test]
    fn scholar_chat_local_literature_index_uses_scientific_paper_mode_for_citation_metadata() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "ANOVA",
                Some("scientific_paper"),
                None,
                None,
                Some(vec!["source-a"]),
                Some(vec!["article"]),
            ),
        );
        assert_eq!(
            result.local_index_strategy,
            ScholarChatLocalLiteratureIndexStrategy::ScientificPaperCitationLocalFirst
        );
        assert!(result
            .planned_metadata_requirements
            .iter()
            .any(|value| value == "citation_metadata_required_for_scientific_paper"));
        assert!(result
            .planned_metadata_requirements
            .iter()
            .any(|value| value == "doi_or_stable_locator_preferred"));
    }

    #[test]
    fn scholar_chat_local_literature_index_uses_course_mode_for_course_context_requirements() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "Hypothesentests",
                Some("course"),
                Some("Module 123"),
                Some(vec![" lecture notes ", "course-work"]),
                Some(vec!["source-a"]),
                Some(vec!["course_material", "lecture-slide"]),
            ),
        );
        assert_eq!(
            result.local_index_strategy,
            ScholarChatLocalLiteratureIndexStrategy::CourseMaterialLocalFirst
        );
        assert!(result
            .planned_metadata_requirements
            .iter()
            .any(|value| value == "course_context_required_for_course_mode"));
        assert_eq!(
            result.planned_chunking_policy,
            vec![
                "chunking_not_run_in_preview".to_string(),
                "later_pdf_text_extraction_required_before_chunking".to_string(),
                "later_markdown_section_chunking_candidate".to_string(),
                "later_course_slide_section_chunking_candidate".to_string(),
                "preserve_page_or_section_locator_later".to_string(),
                "keep_citation_metadata_attached_to_chunks_later".to_string(),
            ]
        );
    }

    #[test]
    fn scholar_chat_local_literature_index_marks_ambiguous_and_unknown_concept_statuses() {
        let temp = tempfile::tempdir().unwrap();
        let ambiguous = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "ANOVA und Hypothesentests Vergleich",
                None,
                None,
                None,
                Some(vec!["source-a"]),
                None,
            ),
        );
        assert_eq!(
            ambiguous.status,
            ScholarChatLocalLiteratureIndexStatus::NeedsDisambiguation
        );
        assert_eq!(
            ambiguous.ingestion_readiness,
            ScholarChatLocalLiteratureIngestionReadiness::NeedsDisambiguation
        );

        let unknown = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "Signal graph theory",
                None,
                None,
                None,
                Some(vec!["source-a"]),
                None,
            ),
        );
        assert_eq!(unknown.status, ScholarChatLocalLiteratureIndexStatus::UnknownConcept);
        assert_eq!(
            unknown.ingestion_readiness,
            ScholarChatLocalLiteratureIngestionReadiness::UnknownConceptMappingNeeded
        );
    }

    #[test]
    fn scholar_chat_local_literature_index_emits_stable_step_order_and_preview_only_constraints() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_local_literature_index_deterministic_and_path_free(
            &temp,
            scientific_local_literature_index_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["source-b", "source-a"]),
                Some(vec!["pdf"]),
            ),
        );
        let kinds = result
            .planned_index_steps
            .iter()
            .map(|step| step.kind.clone())
            .collect::<Vec<_>>();
        assert_eq!(
            kinds,
            vec![
                ScholarChatLocalLiteratureIndexStepKind::SourceSelectionReview,
                ScholarChatLocalLiteratureIndexStepKind::MetadataRequirementCheck,
                ScholarChatLocalLiteratureIndexStepKind::CorpusManifestPlan,
                ScholarChatLocalLiteratureIndexStepKind::ExtractionPlan,
                ScholarChatLocalLiteratureIndexStepKind::ChunkingPolicyPlan,
                ScholarChatLocalLiteratureIndexStepKind::LexicalIndexPlan,
                ScholarChatLocalLiteratureIndexStepKind::VectorIndexPlan,
                ScholarChatLocalLiteratureIndexStepKind::DeduplicationPlan,
                ScholarChatLocalLiteratureIndexStepKind::RetrievalReadinessCheck,
            ]
        );
        assert!(result
            .planned_index_steps
            .iter()
            .all(|step| step.preview_only && step.boundary_notes.iter().any(|note| note == "preview-only")));
    }

    #[test]
    fn scholar_chat_local_literature_index_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_local_literature_index")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_metadata_connector_plan")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(body.matches("preview_scholar_chat_scientific_search_plan").count(), 1);
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("CorpusAuthority::"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("extract_source"));
        assert!(!body.contains("chunk_source"));
        assert!(!body.contains("build_retrieval_index"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("preview_scholar_chat_answer_readiness"));
        assert!(!body.contains("preview_scholar_chat_draft_inference"));
        assert!(!body.contains("preview_scholar_chat_grounded_answer"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_course_literature_registry_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "   ",
                Some("Psychology course"),
                Some("PSY-201"),
                Some("Signalentdeckung"),
                Some("Prof. Example"),
                Some("WS 2025"),
                None,
                None,
                None,
            ),
        );
        assert_eq!(
            result.status,
            ScholarChatCourseLiteratureRegistryStatus::Blocked
        );
        assert_eq!(
            result.course_registry_strategy,
            ScholarChatCourseLiteratureRegistryStrategy::Blocked
        );
        assert!(result.normalized_query.is_empty());
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.contains("query_missing")));
    }

    #[test]
    fn scholar_chat_course_literature_registry_maps_signalentdeckung_to_course_registry_plan_ready() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "Signalentdeckung",
                Some("  Psychology   course  "),
                Some(" PSY-201 "),
                Some(" Signalentdeckung "),
                Some("  Prof. Example  "),
                Some(" WS 2025 "),
                Some(vec![" lecture-material ", "psychology", "lecture-material"]),
                Some(vec!["  source-b  ", "source-a", "source-b"]),
                Some(vec![" lecture-slide ", "seminar-reading", "unknown-kind", "module handbook", "lecture_slide"]),
            ),
        );
        assert_eq!(
            result.status,
            ScholarChatCourseLiteratureRegistryStatus::CourseRegistryPlanReady
        );
        assert_eq!(
            result.course_registry_strategy,
            ScholarChatCourseLiteratureRegistryStrategy::ModuleContextFirst
        );
        assert_eq!(
            result.local_literature_index_status,
            ScholarChatLocalLiteratureIndexStatus::IndexPlanReady
        );
        assert_eq!(
            result.search_plan_status,
            ScholarChatScientificSearchPlanStatus::SearchPlanReady
        );
        assert_eq!(
            result.query_understanding_status,
            ScholarChatScientificQueryUnderstandingStatus::Understood
        );
        assert_eq!(result.normalized_query, "Signalentdeckung");
        assert_eq!(result.normalized_course_context.as_deref(), Some("Psychology course"));
        assert_eq!(result.normalized_module_code.as_deref(), Some("PSY-201"));
        assert_eq!(result.normalized_course_title.as_deref(), Some("Signalentdeckung"));
        assert_eq!(result.normalized_instructor.as_deref(), Some("Prof. Example"));
        assert_eq!(result.normalized_semester.as_deref(), Some("WS 2025"));
        assert_eq!(
            result.selected_local_source_ids,
            vec!["source-a".to_string(), "source-b".to_string()]
        );
        assert_eq!(
            result.expected_course_material_kinds,
            vec![
                "lecture_slide".to_string(),
                "module_handbook".to_string(),
                "seminar_reading".to_string(),
                "unknown_kind".to_string(),
            ]
        );
        assert_eq!(
            result.course_identity.identity_key.as_deref(),
            Some("psy_201::signalentdeckung::ws_2025")
        );
        assert!(result.course_identity.has_course_context);
        assert!(result.course_identity.has_module_code);
        assert!(result.course_identity.has_course_title);
        assert_eq!(
            result.course_material_plan.known_material_kinds,
            vec![
                "lecture_slide".to_string(),
                "module_handbook".to_string(),
                "seminar_reading".to_string(),
            ]
        );
        assert_eq!(
            result.course_material_plan.unknown_material_kinds,
            vec!["unknown_kind".to_string()]
        );
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("Unknown course material kinds")));
        assert!(!result.course_material_plan.will_read_files);
        assert!(!result.course_material_plan.will_import_sources);
        assert!(!result.course_material_plan.will_create_registry);
        assert!(!result.curriculum_alignment_plan.requires_course_context);
        assert!(!result.curriculum_alignment_plan.requires_module_metadata);
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "module_code_recommended"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "local_source_ids_required_for_course_material_alignment"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "no_curriculum_scraping_in_preview"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "Signalentdeckung course materials"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "Signalentdeckung lecture notes"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "PSY-201 Signalentdeckung"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "Signalentdeckung exam preparation"));
        assert_eq!(
            result
                .planned_registry_steps
                .iter()
                .map(|step| step.kind.clone())
                .collect::<Vec<_>>(),
            vec![
                ScholarChatCourseLiteratureRegistryStepKind::CourseIdentityReview,
                ScholarChatCourseLiteratureRegistryStepKind::ModuleContextReview,
                ScholarChatCourseLiteratureRegistryStepKind::CourseMaterialKindPlan,
                ScholarChatCourseLiteratureRegistryStepKind::LocalSourceAlignmentPlan,
                ScholarChatCourseLiteratureRegistryStepKind::CurriculumMetadataRequirementCheck,
                ScholarChatCourseLiteratureRegistryStepKind::LocalLiteratureIndexAlignment,
                ScholarChatCourseLiteratureRegistryStepKind::RetrievalReadinessCheck,
                ScholarChatCourseLiteratureRegistryStepKind::LearningPathAlignmentPlan,
            ]
        );
        assert!(result.summary.contains("ready later"));
    }

    #[test]
    fn scholar_chat_course_literature_registry_reports_needs_course_context_when_identity_is_missing() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "Signalentdeckung",
                None,
                None,
                None,
                None,
                None,
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                Some(vec!["lecture-slide"]),
            ),
        );
        assert_eq!(
            result.status,
            ScholarChatCourseLiteratureRegistryStatus::NeedsCourseContext
        );
        assert_eq!(
            result.course_registry_strategy,
            ScholarChatCourseLiteratureRegistryStrategy::LocalSourceAlignmentFirst
        );
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("No course context, module code, or course title")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Provide course context, module code, or course title")));
    }

    #[test]
    fn scholar_chat_course_literature_registry_reports_needs_local_sources_when_identity_exists() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "Hypothesentests",
                Some("Statistics course"),
                Some("STAT-101"),
                Some("Hypothesentests"),
                Some("Prof. Example"),
                Some("WS 2025"),
                Some(vec!["statistics"]),
                None,
                Some(vec!["exam-prep"]),
            ),
        );
        assert_eq!(
            result.status,
            ScholarChatCourseLiteratureRegistryStatus::NeedsLocalSources
        );
        assert_eq!(
            result.course_registry_strategy,
            ScholarChatCourseLiteratureRegistryStrategy::ModuleContextFirst
        );
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.contains("No local course sources selected")));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select or import local course material sources later")));
        assert_eq!(result.course_material_plan.selected_source_count, 0);
        assert_eq!(result.course_material_plan.selected_local_source_ids, Vec::<String>::new());
    }

    #[test]
    fn scholar_chat_course_literature_registry_marks_ambiguous_and_unknown_concepts() {
        let temp = tempfile::tempdir().unwrap();
        let ambiguous = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "ANOVA und Hypothesentests Vergleich",
                Some("Statistics course"),
                Some("STAT-101"),
                Some("ANOVA"),
                Some("Prof. Example"),
                Some("WS 2025"),
                Some(vec!["statistics"]),
                Some(vec!["source-a"]),
                Some(vec!["module-handbook"]),
            ),
        );
        assert_eq!(
            ambiguous.status,
            ScholarChatCourseLiteratureRegistryStatus::NeedsDisambiguation
        );

        let unknown = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "Signal graph theory",
                Some("Theory course"),
                Some("THE-201"),
                Some("Graph theory"),
                Some("Prof. Example"),
                Some("WS 2025"),
                Some(vec!["theory"]),
                Some(vec!["source-a"]),
                Some(vec!["notes"]),
            ),
        );
        assert_eq!(
            unknown.status,
            ScholarChatCourseLiteratureRegistryStatus::UnknownConcept
        );
    }

    #[test]
    fn scholar_chat_course_literature_registry_includes_course_metadata_requirements_and_planned_queries() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_course_literature_registry_deterministic_and_path_free(
            &temp,
            course_literature_registry_request(
                "ANOVA",
                Some("Statistics course"),
                Some("STAT-201"),
                Some("ANOVA seminar"),
                Some("Prof. Example"),
                Some("SS 2025"),
                Some(vec!["statistics"]),
                Some(vec!["source-a"]),
                Some(vec!["module-handbook", "article", "exam_prep"]),
            ),
        );
        assert_eq!(
            result.status,
            ScholarChatCourseLiteratureRegistryStatus::CourseRegistryPlanReady
        );
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "course_context_or_module_identity_required"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "module_code_recommended"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "course_title_recommended"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "semester_recommended"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "instructor_optional"));
        assert!(result
            .planned_course_metadata_requirements
            .iter()
            .any(|requirement| requirement == "learning_objectives_required_later"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "ANOVA course materials"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "ANOVA lecture notes"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "STAT-201 ANOVA"));
        assert!(result
            .planned_course_material_queries
            .iter()
            .any(|query| query == "ANOVA seminar ANOVA"));
    }

    #[test]
    fn scholar_chat_course_literature_registry_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_course_literature_registry")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_metadata_connector_plan")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(body.matches("preview_scholar_chat_local_literature_index").count(), 1);
        assert!(body.contains("mode: Some(\"course\".to_string())"));
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("CorpusAuthority::"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("extract_source"));
        assert!(!body.contains("chunk_source"));
        assert!(!body.contains("build_retrieval_index"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("preview_scholar_chat_answer_readiness"));
        assert!(!body.contains("preview_scholar_chat_draft_inference"));
        assert!(!body.contains("preview_scholar_chat_grounded_answer"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_search_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(body.matches("preview_scholar_chat_scientific_query_understanding").count(), 1);
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("preview_scholar_chat_answer_readiness"));
        assert!(!body.contains("preview_scholar_chat_draft_inference"));
        assert!(!body.contains("preview_scholar_chat_grounded_answer"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_scientific_search_plan_body_guard_helper_is_still_valid() {
        assert_scientific_search_plan_body_does_not_call_execution_functions();
    }

    fn assert_local_literature_index_boundary_fields(
        preview: &ScholarChatLocalLiteratureIndexPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.local_literature_index_preview_only);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_bm25_index);
        assert!(preview.no_vector_index);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_local_literature_index_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatLocalLiteratureIndexRequest,
    ) -> ScholarChatLocalLiteratureIndexPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_local_literature_index(temp.path(), request.clone()).unwrap();
        let second = preview_scholar_chat_local_literature_index(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_local_literature_index_boundary_fields(preview);
        }
        first
    }

    fn assert_course_literature_registry_boundary_fields(
        preview: &ScholarChatCourseLiteratureRegistryPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.course_literature_registry_preview_only);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_web_request);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_course_literature_registry_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatCourseLiteratureRegistryPreviewRequest,
    ) -> ScholarChatCourseLiteratureRegistryPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_course_literature_registry(temp.path(), request.clone())
            .unwrap();
        let second = preview_scholar_chat_course_literature_registry(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_course_literature_registry_boundary_fields(preview);
        }
        first
    }

    fn assert_metadata_connector_plan_boundary_fields(
        preview: &ScholarChatMetadataConnectorPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.metadata_connector_plan_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_psychology_source_connector_plan_boundary_fields(
        preview: &ScholarChatPsychologySourceConnectorPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.psychology_source_connector_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_metadata_connector_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatMetadataConnectorPlanRequest,
    ) -> ScholarChatMetadataConnectorPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_metadata_connector_plan(temp.path(), request.clone())
            .unwrap();
        let second = preview_scholar_chat_metadata_connector_plan(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_metadata_connector_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_psychology_source_connector_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatPsychologySourceConnectorPlanRequest,
    ) -> ScholarChatPsychologySourceConnectorPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_psychology_source_connector_plan(temp.path(), request.clone())
            .unwrap();
        let second = preview_scholar_chat_psychology_source_connector_plan(temp.path(), request)
            .unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_psychology_source_connector_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_evidence_pack_plan_boundary_fields(
        preview: &ScholarChatScientificEvidencePackPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.scientific_evidence_pack_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_evidence_pack_item_boundary_fields(
        item: &ScholarChatScientificEvidenceItemPlan,
    ) {
        assert!(item.preview_only);
        assert!(!item.will_read_file);
        assert!(!item.will_call_connector);
        assert!(!item.will_write_artifact);
    }

    fn assert_scientific_evidence_pack_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatScientificEvidencePackPlanRequest,
    ) -> ScholarChatScientificEvidencePackPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first = preview_scholar_chat_scientific_evidence_pack_plan(temp.path(), request.clone())
            .unwrap();
        let second =
            preview_scholar_chat_scientific_evidence_pack_plan(temp.path(), request).unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert_scientific_evidence_pack_plan_boundary_fields(preview);
        }
        first
    }

    fn assert_scientific_paper_literature_review_plan_boundary_fields(
        preview: &ScholarChatScientificPaperLiteratureReviewPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.scientific_paper_literature_review_preview_only);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
        temp: &tempfile::TempDir,
        request: ScholarChatScientificPaperLiteratureReviewPlanRequest,
    ) -> ScholarChatScientificPaperLiteratureReviewPlanPreview {
        let before_entries = count_entries_recursively(temp.path());
        let first =
            preview_scholar_chat_scientific_paper_literature_review_plan(temp.path(), request.clone())
                .unwrap();
        let second = preview_scholar_chat_scientific_paper_literature_review_plan(temp.path(), request)
            .unwrap();
        let after_entries = count_entries_recursively(temp.path());
        assert_eq!(first, second);
        assert_eq!(before_entries, after_entries);
        assert!(!temp.path().join(".aegis").exists());
        let temp_path = temp.path().to_string_lossy();
        for preview in [&first, &second] {
            let debug = format!("{preview:?}");
            let json = serde_json::to_string(preview).unwrap();
            assert!(!debug.contains(temp_path.as_ref()));
            assert!(!json.contains(temp_path.as_ref()));
            assert!(!debug.contains("http://"));
            assert!(!debug.contains("https://"));
            assert!(!json.contains("http://"));
            assert!(!json.contains("https://"));
            assert_scientific_paper_literature_review_plan_boundary_fields(preview);
        }
        first
    }

    fn scientific_paper_literature_review_plan_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        context_tags: Option<Vec<&str>>,
        selected_local_source_ids: Option<Vec<&str>>,
        expected_source_kinds: Option<Vec<&str>>,
        preferred_metadata_sources: Option<Vec<&str>>,
        preferred_psychology_source_families: Option<Vec<&str>>,
        methodology_hints: Option<Vec<&str>>,
        population_hints: Option<Vec<&str>>,
        topic_area_hints: Option<Vec<&str>>,
        evidence_goal: Option<&str>,
        claim_scope: Option<Vec<&str>>,
        citation_style: Option<&str>,
        review_goal: Option<&str>,
        research_question: Option<&str>,
        review_section_preferences: Option<Vec<&str>>,
        max_results_per_source: Option<u32>,
        require_open_access: Option<bool>,
        require_doi: Option<bool>,
        year_from: Option<u16>,
        year_to: Option<u16>,
    ) -> ScholarChatScientificPaperLiteratureReviewPlanRequest {
        ScholarChatScientificPaperLiteratureReviewPlanRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: context_tags.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            selected_local_source_ids: selected_local_source_ids.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            expected_source_kinds: expected_source_kinds.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            preferred_metadata_sources: preferred_metadata_sources.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            preferred_psychology_source_families: preferred_psychology_source_families.map(
                |values| values.into_iter().map(|value| value.to_string()).collect(),
            ),
            methodology_hints: methodology_hints.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            population_hints: population_hints.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            topic_area_hints: topic_area_hints.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            evidence_goal: evidence_goal.map(|value| value.to_string()),
            claim_scope: claim_scope.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            citation_style: citation_style.map(|value| value.to_string()),
            review_goal: review_goal.map(|value| value.to_string()),
            research_question: research_question.map(|value| value.to_string()),
            review_section_preferences: review_section_preferences.map(|values| {
                values.into_iter().map(|value| value.to_string()).collect()
            }),
            max_results_per_source,
            require_open_access,
            require_doi,
            year_from,
            year_to,
        }
    }

    fn scientific_evidence_pack_plan_request(
        query: &str,
        mode: Option<&str>,
        course_context: Option<&str>,
        selected_local_source_ids: Vec<&str>,
        preferred_metadata_sources: Vec<&str>,
        preferred_psychology_source_families: Option<Vec<&str>>,
        claim_scope: Option<Vec<&str>>,
        citation_style: Option<&str>,
        require_doi: bool,
        require_open_access: bool,
    ) -> ScholarChatScientificEvidencePackPlanRequest {
        ScholarChatScientificEvidencePackPlanRequest {
            query: query.to_string(),
            mode: mode.map(|value| value.to_string()),
            course_context: course_context.map(|value| value.to_string()),
            context_tags: None,
            selected_local_source_ids: Some(
                selected_local_source_ids
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            expected_source_kinds: None,
            preferred_metadata_sources: Some(
                preferred_metadata_sources
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            preferred_psychology_source_families: preferred_psychology_source_families.map(
                |values| values.into_iter().map(|value| value.to_string()).collect(),
            ),
            methodology_hints: None,
            population_hints: None,
            topic_area_hints: None,
            evidence_goal: None,
            claim_scope: claim_scope.map(|values| {
                values
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect()
            }),
            citation_style: citation_style.map(|value| value.to_string()),
            max_results_per_source: None,
            require_open_access: Some(require_open_access),
            require_doi: Some(require_doi),
            year_from: None,
            year_to: None,
        }
    }

    fn assert_public_declaration_marker_count(source: &str, marker: &str) {
        let count = source
            .lines()
            .filter(|line| {
                let trimmed = line.trim_start();
                trimmed.starts_with(marker)
                    && trimmed[marker.len()..]
                        .chars()
                        .next()
                        .map(|next| !next.is_ascii_alphanumeric() && next != '_')
                        .unwrap_or(true)
            })
            .count();
        assert_eq!(count, 1, "{marker} should be declared exactly once");
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatScientificMetadataExecutionBoundaryStatus",
            "pub enum ScholarChatScientificMetadataExecutionBoundaryStrategy",
            "pub enum ScholarChatScientificMetadataExecutionBoundaryStepKind",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryPreviewRequest",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryPreview",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryProviderSelectionPlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryProviderGate",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryProviderGatePlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryProviderRequestPlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryProviderTermsPlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryMetadataWritePlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundarySafetyPlan",
            "pub struct ScholarChatScientificMetadataExecutionBoundaryStep",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady,
                "\"execution_boundary_ready\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionRequestedButDisabled,
                "\"execution_requested_but_disabled\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsMetadataConnectorPlan,
                "\"needs_metadata_connector_plan\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderSelection,
                "\"needs_provider_selection\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsProviderTermsReview,
                "\"needs_provider_terms_review\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission,
                "\"needs_network_permission\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsWritePermission,
                "\"needs_write_permission\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsCitationMetadata,
                "\"needs_citation_metadata\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsEvidenceSources,
                "\"needs_evidence_sources\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsDisambiguation,
                "\"needs_disambiguation\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::UnknownConcept,
                "\"unknown_concept\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsPsychologyScope,
                "\"needs_psychology_scope\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily,
                "\"needs_source_family\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatScientificMetadataExecutionBoundaryStrategy::DryRunOnly,
                "\"dry_run_only\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStrategy::ProviderPlanOnly,
                "\"provider_plan_only\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStrategy::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending,
                "\"execution_gate_pending\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStrategy::MetadataWriteGatePending,
                "\"metadata_write_gate_pending\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::EvidencePackAlignment,
                "\"evidence_pack_alignment\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::MetadataConnectorAlignment,
                "\"metadata_connector_alignment\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderSelection,
                "\"provider_selection\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderTermsBoundary,
                "\"provider_terms_boundary\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::NetworkPermissionGate,
                "\"network_permission_gate\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::ProviderRequestPlanning,
                "\"provider_request_planning\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::MetadataWriteGate,
                "\"metadata_write_gate\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::SafetyBoundaryCheck,
                "\"safety_boundary_check\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::DownstreamEvidencePackAlignment,
                "\"downstream_evidence_pack_alignment\"",
            ),
            (
                ScholarChatScientificMetadataExecutionBoundaryStepKind::DownstreamLiteratureReviewAlignment,
                "\"downstream_literature_review_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatScientificMetadataProviderConfigStatus",
            "pub enum ScholarChatScientificMetadataProviderConfigStrategy",
            "pub enum ScholarChatScientificMetadataProviderAccessKind",
            "pub enum ScholarChatScientificMetadataProviderConfigStepKind",
            "pub struct ScholarChatScientificMetadataProviderConfigPreviewRequest",
            "pub struct ScholarChatScientificMetadataProviderConfigProviderRecord",
            "pub struct ScholarChatScientificMetadataProviderTermsPolicy",
            "pub struct ScholarChatScientificMetadataProviderAccessPolicy",
            "pub struct ScholarChatScientificMetadataProviderRateLimitPolicy",
            "pub struct ScholarChatScientificMetadataProviderAttributionPolicy",
            "pub struct ScholarChatScientificMetadataProviderSafetyBoundary",
            "pub struct ScholarChatScientificMetadataProviderConfigStep",
            "pub struct ScholarChatScientificMetadataProviderConfigPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatScientificMetadataProviderConfigStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady,
                "\"provider_config_ready\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderSelection,
                "\"needs_provider_selection\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary,
                "\"needs_metadata_execution_boundary\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsProviderTermsReview,
                "\"needs_provider_terms_review\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsNetworkPolicyReview,
                "\"needs_network_policy_review\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsInstitutionalAccessReview,
                "\"needs_institutional_access_review\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::NeedsAttributionPolicyReview,
                "\"needs_attribution_policy_review\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStatus::UnsupportedProvider,
                "\"unsupported_provider\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatScientificMetadataProviderConfigStrategy::ConfigPreviewOnly,
                "\"config_preview_only\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStrategy::ProviderPolicyReviewFirst,
                "\"provider_policy_review_first\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStrategy::InstitutionalAccessReviewFirst,
                "\"institutional_access_review_first\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStrategy::AttributionReviewFirst,
                "\"attribution_review_first\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let access_kind_cases = [
            (
                ScholarChatScientificMetadataProviderAccessKind::OpenMetadata,
                "\"open_metadata\"",
            ),
            (
                ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi,
                "\"public_metadata_api\"",
            ),
            (
                ScholarChatScientificMetadataProviderAccessKind::InstitutionalDiscovery,
                "\"institutional_discovery\"",
            ),
            (
                ScholarChatScientificMetadataProviderAccessKind::AuthenticatedLibraryBoundary,
                "\"authenticated_library_boundary\"",
            ),
        ];
        for (value, expected) in access_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatScientificMetadataProviderConfigStepKind::MetadataExecutionBoundaryAlignment,
                "\"metadata_execution_boundary_alignment\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderCapabilityCatalog,
                "\"provider_capability_catalog\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderTermsPolicy,
                "\"provider_terms_policy\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAccessPolicy,
                "\"provider_access_policy\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderRateLimitPolicy,
                "\"provider_rate_limit_policy\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAttributionPolicy,
                "\"provider_attribution_policy\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderSafetyBoundary,
                "\"provider_safety_boundary\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamMetadataExecutionAlignment,
                "\"downstream_metadata_execution_alignment\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamEvidencePackAlignment,
                "\"downstream_evidence_pack_alignment\"",
            ),
            (
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamLiteratureReviewAlignment,
                "\"downstream_literature_review_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatScientificMetadataQueryPlanStatus",
            "pub enum ScholarChatScientificMetadataQueryPlanStrategy",
            "pub enum ScholarChatScientificMetadataQueryProviderFamily",
            "pub enum ScholarChatScientificMetadataQueryPlanStepKind",
            "pub struct ScholarChatScientificMetadataQueryPlanPreviewRequest",
            "pub struct ScholarChatScientificMetadataQueryPlanProviderRecord",
            "pub struct ScholarChatScientificMetadataQueryTemplatePlan",
            "pub struct ScholarChatScientificMetadataQueryFilterPlan",
            "pub struct ScholarChatScientificMetadataQueryResultFieldPlan",
            "pub struct ScholarChatScientificMetadataQueryProviderFamilyBoundary",
            "pub struct ScholarChatScientificMetadataQuerySafetyBoundary",
            "pub struct ScholarChatScientificMetadataQueryPlanStep",
            "pub struct ScholarChatScientificMetadataQueryPlanPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatScientificMetadataQueryPlanStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady,
                "\"query_plan_ready\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                "\"needs_provider_config\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection,
                "\"needs_provider_selection\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsQueryGoal,
                "\"needs_query_goal\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsNetworkPolicyReview,
                "\"needs_network_policy_review\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                "\"needs_provider_config\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                "\"needs_provider_config\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStatus::UnsupportedProvider,
                "\"unsupported_provider\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatScientificMetadataQueryPlanStrategy::QueryPlanPreviewOnly,
                "\"query_plan_preview_only\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStrategy::PublicMetadataQueryPlan,
                "\"public_metadata_query_plan\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStrategy::InstitutionalBoundaryQueryPlan,
                "\"institutional_boundary_query_plan\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
                "\"provider_policy_review_first\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let provider_family_cases = [
            (
                ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata,
                "\"public_metadata\"",
            ),
            (
                ScholarChatScientificMetadataQueryProviderFamily::InstitutionalBoundary,
                "\"institutional_boundary\"",
            ),
        ];
        for (value, expected) in provider_family_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatScientificMetadataQueryPlanStepKind::ProviderConfigAlignment,
                "\"provider_config_alignment\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::ProviderFamilyPartition,
                "\"provider_family_partition\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::QueryTemplatePlanning,
                "\"query_template_planning\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::FilterPlanning,
                "\"filter_planning\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::ResultFieldPlanning,
                "\"result_field_planning\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::RateLimitAlignment,
                "\"rate_limit_alignment\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::AttributionAlignment,
                "\"attribution_alignment\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::SafetyBoundaryCheck,
                "\"safety_boundary_check\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamMetadataConnectorAlignment,
                "\"downstream_metadata_connector_alignment\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamEvidencePackAlignment,
                "\"downstream_evidence_pack_alignment\"",
            ),
            (
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamLiteratureReviewAlignment,
                "\"downstream_literature_review_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_metadata_connector_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatMetadataConnectorPlanStatus",
            "pub enum ScholarChatMetadataConnectorStrategy",
            "pub enum ScholarChatMetadataConnectorStepKind",
            "pub struct ScholarChatMetadataConnectorSourceSelectionPlan",
            "pub struct ScholarChatMetadataConnectorQueryPlan",
            "pub struct ScholarChatMetadataDeduplicationPlan",
            "pub struct ScholarChatMetadataAttributionPlan",
            "pub struct ScholarChatMetadataCompliancePlan",
            "pub struct ScholarChatMetadataConnectorStep",
            "pub struct ScholarChatMetadataConnectorPlanRequest",
            "pub struct ScholarChatMetadataConnectorPlanPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_psychology_source_connector_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatPsychologySourceConnectorPlanStatus",
            "pub enum ScholarChatPsychologySourceFamilyStrategy",
            "pub enum ScholarChatPsychologyScopeStatus",
            "pub enum ScholarChatPsychologySourceConnectorStepKind",
            "pub struct ScholarChatPsychologySourceFamilySelectionPlan",
            "pub struct ScholarChatPsychologySourceFamilyPlan",
            "pub struct ScholarChatPsychologyMethodologyRoutingPlan",
            "pub struct ScholarChatPsychologyPopulationRoutingPlan",
            "pub struct ScholarChatPsychologyTopicAreaRoutingPlan",
            "pub struct ScholarChatPsychologyEvidenceRequirementsPlan",
            "pub struct ScholarChatPsychologyConnectorCompliancePlan",
            "pub struct ScholarChatPsychologySourceConnectorStep",
            "pub struct ScholarChatPsychologySourceConnectorPlanRequest",
            "pub struct ScholarChatPsychologySourceConnectorPlanPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_public_dto_declarations_are_declared_once() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatScientificEvidencePackPlanStatus",
            "pub enum ScholarChatScientificEvidencePackStrategy",
            "pub enum ScholarChatScientificEvidencePackStepKind",
            "pub struct ScholarChatScientificEvidencePackLocalContextPlan",
            "pub struct ScholarChatScientificEvidencePackMetadataPlan",
            "pub struct ScholarChatScientificEvidencePackSourceFamilyPlan",
            "pub struct ScholarChatScientificEvidencePackClaimCoveragePlan",
            "pub struct ScholarChatScientificEvidencePackCitationAttributionPlan",
            "pub struct ScholarChatScientificEvidencePackQualityPlan",
            "pub struct ScholarChatScientificEvidencePackCompliancePlan",
            "pub struct ScholarChatScientificEvidenceItemPlan",
            "pub struct ScholarChatScientificEvidencePackStep",
            "pub struct ScholarChatScientificEvidencePackPlanRequest",
            "pub struct ScholarChatScientificEvidencePackPlanPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_metadata_connector_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatMetadataConnectorPlanStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady,
                "\"connector_plan_ready\"",
            ),
            (
                ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation,
                "\"needs_disambiguation\"",
            ),
            (
                ScholarChatMetadataConnectorPlanStatus::UnknownConcept,
                "\"unknown_concept\"",
            ),
            (
                ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource,
                "\"needs_metadata_source\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatMetadataConnectorStrategy::OpenalexAndCrossref,
                "\"openalex_and_crossref\"",
            ),
            (
                ScholarChatMetadataConnectorStrategy::OpenalexFirst,
                "\"openalex_first\"",
            ),
            (
                ScholarChatMetadataConnectorStrategy::CrossrefFirst,
                "\"crossref_first\"",
            ),
            (
                ScholarChatMetadataConnectorStrategy::MetadataSourceSelectionNeeded,
                "\"metadata_source_selection_needed\"",
            ),
            (
                ScholarChatMetadataConnectorStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatMetadataConnectorStepKind::QueryUnderstandingAlignment,
                "\"query_understanding_alignment\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::MetadataSourceSelection,
                "\"metadata_source_selection\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::OpenalexQueryPlan,
                "\"openalex_query_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::CrossrefQueryPlan,
                "\"crossref_query_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::ResultShapeMappingPlan,
                "\"result_shape_mapping_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::DoiFilterPlan,
                "\"doi_filter_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::OpenAccessFilterPlan,
                "\"open_access_filter_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::YearFilterPlan,
                "\"year_filter_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::DeduplicationPlan,
                "\"deduplication_plan\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::AttributionRequirementCheck,
                "\"attribution_requirement_check\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::ComplianceBoundaryCheck,
                "\"compliance_boundary_check\"",
            ),
            (
                ScholarChatMetadataConnectorStepKind::DownstreamEvidencePackAlignment,
                "\"downstream_evidence_pack_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_psychology_source_connector_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatPsychologySourceConnectorPlanStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady,
                "\"source_connector_plan_ready\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::NeedsPsychologyScope,
                "\"needs_psychology_scope\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::NeedsMetadataConnectorPlan,
                "\"needs_metadata_connector_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::NeedsDisambiguation,
                "\"needs_disambiguation\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::UnknownConcept,
                "\"unknown_concept\"",
            ),
            (
                ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily,
                "\"needs_source_family\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatPsychologySourceFamilyStrategy::PsychologyMetadataBalanced,
                "\"psychology_metadata_balanced\"",
            ),
            (
                ScholarChatPsychologySourceFamilyStrategy::OpenMetadataFirst,
                "\"open_metadata_first\"",
            ),
            (
                ScholarChatPsychologySourceFamilyStrategy::ScholarlyIndexFirst,
                "\"scholarly_index_first\"",
            ),
            (
                ScholarChatPsychologySourceFamilyStrategy::CourseLiteratureFirst,
                "\"course_literature_first\"",
            ),
            (
                ScholarChatPsychologySourceFamilyStrategy::SourceFamilySelectionNeeded,
                "\"source_family_selection_needed\"",
            ),
            (
                ScholarChatPsychologySourceFamilyStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let scope_cases = [
            (
                ScholarChatPsychologyScopeStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatPsychologyScopeStatus::NeedsPsychologyScope,
                "\"needs_psychology_scope\"",
            ),
            (
                ScholarChatPsychologyScopeStatus::PsychologyScopeReady,
                "\"psychology_scope_ready\"",
            ),
        ];
        for (value, expected) in scope_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatPsychologySourceConnectorStepKind::MetadataConnectorAlignment,
                "\"metadata_connector_alignment\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::PsychologyScopeClassification,
                "\"psychology_scope_classification\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::SourceFamilySelection,
                "\"source_family_selection\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::OpenalexPsychologyPlan,
                "\"openalex_psychology_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::CrossrefPsychologyPlan,
                "\"crossref_psychology_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::PubmedPsychologyPlan,
                "\"pubmed_psychology_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::EricPsychologyPlan,
                "\"eric_psychology_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::ApaPsycnetDiscoveryPlan,
                "\"apa_psycnet_discovery_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::LocalCourseLiteraturePlan,
                "\"local_course_literature_plan\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::MethodologyRouting,
                "\"methodology_routing\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::PopulationRouting,
                "\"population_routing\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::TopicAreaRouting,
                "\"topic_area_routing\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::EvidenceRequirementMapping,
                "\"evidence_requirement_mapping\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::ComplianceBoundaryCheck,
                "\"compliance_boundary_check\"",
            ),
            (
                ScholarChatPsychologySourceConnectorStepKind::DownstreamEvidencePackAlignment,
                "\"downstream_evidence_pack_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatScientificEvidencePackPlanStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady,
                "\"evidence_pack_plan_ready\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologySourceConnectorPlan,
                "\"needs_psychology_source_connector_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources,
                "\"needs_evidence_sources\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsCitationMetadata,
                "\"needs_citation_metadata\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation,
                "\"needs_disambiguation\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::UnknownConcept,
                "\"unknown_concept\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsPsychologyScope,
                "\"needs_psychology_scope\"",
            ),
            (
                ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily,
                "\"needs_source_family\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatScientificEvidencePackStrategy::BalancedLocalAndMetadata,
                "\"balanced_local_and_metadata\"",
            ),
            (
                ScholarChatScientificEvidencePackStrategy::LocalContextFirst,
                "\"local_context_first\"",
            ),
            (
                ScholarChatScientificEvidencePackStrategy::MetadataCitationFirst,
                "\"metadata_citation_first\"",
            ),
            (
                ScholarChatScientificEvidencePackStrategy::PsychologySourceFamilyFirst,
                "\"psychology_source_family_first\"",
            ),
            (
                ScholarChatScientificEvidencePackStrategy::EvidenceSourceSelectionNeeded,
                "\"evidence_source_selection_needed\"",
            ),
            (
                ScholarChatScientificEvidencePackStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatScientificEvidencePackStepKind::PsychologyConnectorAlignment,
                "\"psychology_connector_alignment\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::EvidenceScopeReview,
                "\"evidence_scope_review\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::LocalEvidenceContextPlan,
                "\"local_evidence_context_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::MetadataEvidencePlan,
                "\"metadata_evidence_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::SourceFamilyEvidencePlan,
                "\"source_family_evidence_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::ClaimCoveragePlan,
                "\"claim_coverage_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::CitationAttributionPlan,
                "\"citation_attribution_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::EvidenceQualityPlan,
                "\"evidence_quality_plan\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::ComplianceBoundaryCheck,
                "\"compliance_boundary_check\"",
            ),
            (
                ScholarChatScientificEvidencePackStepKind::DownstreamAnswerReadinessAlignment,
                "\"downstream_answer_readiness_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_public_dto_declarations_are_declared_once()
    {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        for marker in [
            "pub enum ScholarChatScientificPaperLiteratureReviewPlanStatus",
            "pub enum ScholarChatScientificPaperLiteratureReviewStrategy",
            "pub enum ScholarChatScientificPaperLiteratureReviewStepKind",
            "pub struct ScholarChatScientificPaperResearchQuestionPlan",
            "pub struct ScholarChatScientificPaperSearchStrategyPlan",
            "pub struct ScholarChatScientificPaperEvidencePackDependencyPlan",
            "pub struct ScholarChatScientificPaperReviewSectionPlan",
            "pub struct ScholarChatScientificPaperClaimSynthesisPlan",
            "pub struct ScholarChatScientificPaperCitationPlan",
            "pub struct ScholarChatScientificPaperQualityReviewPlan",
            "pub struct ScholarChatScientificPaperComplianceReviewPlan",
            "pub struct ScholarChatScientificPaperLiteratureReviewStep",
            "pub struct ScholarChatScientificPaperLiteratureReviewPlanRequest",
            "pub struct ScholarChatScientificPaperLiteratureReviewPlanPreview",
        ] {
            assert_public_declaration_marker_count(source, marker);
        }
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_serde_values_are_snake_case() {
        let status_cases = [
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked,
                "\"blocked\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady,
                "\"literature_review_plan_ready\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidencePackPlan,
                "\"needs_evidence_pack_plan\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion,
                "\"needs_research_question\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsEvidenceSources,
                "\"needs_evidence_sources\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsCitationMetadata,
                "\"needs_citation_metadata\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsDisambiguation,
                "\"needs_disambiguation\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::UnknownConcept,
                "\"unknown_concept\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsPsychologyScope,
                "\"needs_psychology_scope\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsSourceFamily,
                "\"needs_source_family\"",
            ),
        ];
        for (value, expected) in status_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let strategy_cases = [
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::EvidencePackFirst,
                "\"evidence_pack_first\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::ResearchQuestionFirst,
                "\"research_question_first\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::CitationMetadataFirst,
                "\"citation_metadata_first\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::PsychologyScopeFirst,
                "\"psychology_scope_first\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::SourceFamilyFirst,
                "\"source_family_first\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStrategy::Blocked,
                "\"blocked\"",
            ),
        ];
        for (value, expected) in strategy_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }

        let step_kind_cases = [
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::ScientificPaperModeAlignment,
                "\"scientific_paper_mode_alignment\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::EvidencePackPlanAlignment,
                "\"evidence_pack_plan_alignment\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::ResearchQuestionPlanning,
                "\"research_question_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::SearchStrategyPlanning,
                "\"search_strategy_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::EvidenceMapPlanning,
                "\"evidence_map_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::ReviewSectionPlanning,
                "\"review_section_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::ClaimSynthesisPlanning,
                "\"claim_synthesis_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::CitationPlanning,
                "\"citation_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::QualityReviewPlanning,
                "\"quality_review_planning\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::ComplianceBoundaryCheck,
                "\"compliance_boundary_check\"",
            ),
            (
                ScholarChatScientificPaperLiteratureReviewStepKind::DownstreamPaperGenerationAlignment,
                "\"downstream_paper_generation_alignment\"",
            ),
        ];
        for (value, expected) in step_kind_cases {
            assert_eq!(serde_json::to_string(&value).unwrap(), expected);
        }
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_body_composes_metadata_connector_preview_and_forbids_execution_calls() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_psychology_source_connector_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_metadata_connector_plan").count(),
            1
        );
        for forbidden_call in [
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_retrieval",
            "preview_scholar_chat_evidence_plan",
            "preview_scholar_chat_prompt_pack",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "Command::new",
            "reqwest::",
            "ureq::",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "http://",
            "https://",
            "CorpusAuthority::",
            "SourceRegistry::",
            "RetrievalService::new",
            "extract_source",
            "chunk_source",
            "build_retrieval_index",
            "smoke_test_local_runtime_inference",
            "run_llama_runtime_smoke_diagnostic",
            "run_smoke_inference_probe",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
        ] {
            assert!(
                !body.contains(forbidden_call),
                "psychology source connector preview body should not contain {forbidden_call}"
            );
        }
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_body_composes_psychology_source_connector_preview_and_forbids_execution_calls() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_evidence_pack_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_scientific_paper_literature_review_plan")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_psychology_source_connector_plan").count(),
            1
        );
        for forbidden_call in [
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_retrieval",
            "preview_scholar_chat_evidence_plan",
            "preview_scholar_chat_prompt_pack",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "preview_scholar_chat_grounded_answer_write_eligibility",
            "preview_scholar_chat_grounded_answer_execution_plan",
            "Command::new",
            "reqwest::",
            "ureq::",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "http://",
            "https://",
            "CorpusAuthority::",
            "SourceRegistry::",
            "RetrievalService::new",
            "extract_source",
            "chunk_source",
            "build_retrieval_index",
            "smoke_test_local_runtime_inference",
            "run_llama_runtime_smoke_diagnostic",
            "run_smoke_inference_probe",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
        ] {
            assert!(
                !body.contains(forbidden_call),
                "scientific evidence pack preview body should not contain {forbidden_call}"
            );
        }
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_body_composes_scientific_evidence_pack_preview_and_forbids_execution_calls() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_paper_literature_review_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_scientific_evidence_pack_plan").count(),
            1
        );
        for forbidden_call in [
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_retrieval",
            "preview_scholar_chat_evidence_plan",
            "preview_scholar_chat_prompt_pack",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "preview_scholar_chat_grounded_answer_write_eligibility",
            "preview_scholar_chat_grounded_answer_execution_plan",
            "Command::new",
            "reqwest::",
            "ureq::",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "http://",
            "https://",
            "CorpusAuthority::",
            "SourceRegistry::",
            "RetrievalService::new",
            "extract_source",
            "chunk_source",
            "build_retrieval_index",
            "smoke_test_local_runtime_inference",
            "run_llama_runtime_smoke_diagnostic",
            "run_smoke_inference_probe",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
        ] {
            assert!(
                !body.contains(forbidden_call),
                "scientific paper literature review preview body should not contain {forbidden_call}"
            );
        }
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
            &temp,
            scientific_paper_literature_review_plan_request(
                "   ",
                Some("scientific_paper"),
                None,
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                None,
                None,
                None,
                Some("review local literature"),
                None,
                Some("doi_first"),
                Some("literature review"),
                None,
                Some(vec!["background", "findings"]),
                Some(3),
                Some(true),
                Some(true),
                Some(2018),
                Some(2024),
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificPaperLiteratureReviewPlanStatus::Blocked
        );
        assert_eq!(
            preview.literature_review_strategy,
            ScholarChatScientificPaperLiteratureReviewStrategy::Blocked
        );
        assert!(preview.normalized_query.is_empty());
        assert!(preview
            .blockers
            .iter()
            .any(|value| value.contains("query_missing")));
        assert!(preview
            .planned_literature_review_steps
            .iter()
            .all(|step| !step.active && step.preview_only));
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_answer_generated);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_llm_call);
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
            &temp,
            scientific_paper_literature_review_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology literature review"),
                Some(vec!["psychology"]),
                Some(vec![]),
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["methods"]),
                Some(vec!["students"]),
                Some(vec!["signal detection"]),
                Some("review local literature"),
                Some(vec!["signal detection"]),
                Some("doi_first"),
                Some("literature review"),
                Some("What predicts signal detection?"),
                Some(vec!["background", "findings"]),
                Some(3),
                Some(true),
                Some(true),
                Some(2018),
                Some(2024),
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady
        );
        assert_eq!(preview.selected_source_count, 0);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_answer_generated);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_llm_call);
        assert!(preview
            .planned_literature_review_steps
            .iter()
            .all(|step| step.active));
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_requires_research_question_when_upstream_is_ready() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
            &temp,
            scientific_paper_literature_review_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology literature review"),
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["methods"]),
                Some(vec!["students"]),
                Some(vec!["signal detection"]),
                Some("review local literature"),
                Some(vec!["signal detection"]),
                Some("doi_first"),
                Some("literature review"),
                None,
                Some(vec!["background", "findings", "citation_plan", "compliance_notes", "unknown_section"]),
                Some(3),
                Some(true),
                Some(true),
                Some(2018),
                Some(2024),
            ),
        );

        assert_eq!(
            preview.evidence_pack_plan_status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
        assert_eq!(
            preview.status,
            ScholarChatScientificPaperLiteratureReviewPlanStatus::NeedsResearchQuestion
        );
        assert_eq!(
            preview.literature_review_strategy,
            ScholarChatScientificPaperLiteratureReviewStrategy::CitationMetadataFirst
        );
        assert_eq!(
            preview.review_section_plan.requested_sections,
            vec![
                "background".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
                "findings".to_string(),
                "unknown_section".to_string(),
            ]
        );
        assert_eq!(
            preview.review_section_plan.unknown_sections,
            vec!["unknown_section".to_string()]
        );
        assert_eq!(
            preview.review_section_plan.planned_sections,
            vec![
                "background".to_string(),
                "findings".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
            ]
        );
        assert_eq!(
            preview.review_section_plan.section_requirements,
            vec![
                "evidence_pack_plan_ready_later".to_string(),
                "claim_coverage_plan_later".to_string(),
                "verified_citation_metadata_later".to_string(),
                "compliance_boundary_review_later".to_string(),
            ]
        );
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_is_ready_later_and_normalizes_step_order() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
            &temp,
            scientific_paper_literature_review_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology literature review"),
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["methods", "methods"]),
                Some(vec!["students"]),
                Some(vec!["signal detection"]),
                Some("review local literature"),
                Some(vec!["signal detection"]),
                Some("doi_first"),
                Some("literature review"),
                Some("What predicts signal detection?"),
                Some(vec![" background ", "findings", "citation_plan", "compliance_notes", "background"]),
                Some(3),
                Some(true),
                Some(true),
                Some(2018),
                Some(2024),
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady
        );
        assert_eq!(
            preview.literature_review_strategy,
            ScholarChatScientificPaperLiteratureReviewStrategy::CitationMetadataFirst
        );
        assert_eq!(preview.normalized_mode, "scientific_paper");
        assert_eq!(
            preview.requested_mode,
            Some("scientific_paper".to_string())
        );
        assert_eq!(
            preview.evidence_pack_plan_status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
        assert!(preview.research_question_plan.research_question_present);
        assert!(!preview
            .research_question_plan
            .will_derive_research_question_later);
        assert_eq!(
            preview.research_question_plan.required_inputs,
            vec![
                "normalized_query".to_string(),
                "evidence_pack_plan".to_string(),
                "review_goal".to_string(),
                "methodology_hints".to_string(),
                "population_hints".to_string(),
                "topic_area_hints".to_string(),
            ]
        );
        assert_eq!(
            preview.research_question_plan.planned_outputs,
            vec![
                "research_question_later".to_string(),
                "inclusion_scope_later".to_string(),
                "review_focus_later".to_string(),
            ]
        );
        assert!(!preview.search_strategy_plan.will_run_search);
        assert!(!preview.search_strategy_plan.will_call_connectors);
        assert!(preview
            .search_strategy_plan
            .planned_search_inputs
            .iter()
            .any(|value| value == "normalized_query"));
        assert!(preview
            .search_strategy_plan
            .planned_source_channels
            .contains(&"local_source_context".to_string()));
        assert!(!preview.review_section_plan.will_generate_section_text);
        assert_eq!(
            preview.review_section_plan.requested_sections,
            vec![
                "background".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
                "findings".to_string(),
            ]
        );
        assert_eq!(
            preview.review_section_plan.unknown_sections,
            Vec::<String>::new()
        );
        assert_eq!(
            preview.review_section_plan.planned_sections,
            vec![
                "background".to_string(),
                "findings".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
            ]
        );
        assert_eq!(
            preview.claim_synthesis_plan.claim_scope,
            vec!["signal_detection".to_string()]
        );
        assert_eq!(
            preview.claim_synthesis_plan.unknown_claim_scope,
            vec!["signal_detection".to_string()]
        );
        assert_eq!(
            preview.citation_plan.citation_style,
            "doi_first"
        );
        assert!(!preview.citation_plan.will_emit_citations_now);
        assert_eq!(
            preview.citation_plan.required_citation_fields,
            vec![
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "doi_or_stable_identifier".to_string(),
                "access_or_metadata_provider_note_later".to_string(),
            ]
        );
        assert!(preview
            .quality_review_plan
            .review_checks
            .iter()
            .any(|value| value == "verify_metadata_before_citation"));
        assert!(preview.compliance_review_plan.no_fulltext_download);
        assert!(preview.compliance_review_plan.no_scraping);
        assert!(preview.compliance_review_plan.no_authenticated_library_access);
        assert!(preview.compliance_review_plan.no_paywall_bypass);
        assert!(preview.compliance_review_plan.external_metadata_only);
        assert!(!preview
            .compliance_review_plan
            .will_call_external_services);
        assert!(!preview.compliance_review_plan.will_write_metadata);
        assert!(!preview
            .compliance_review_plan
            .will_create_literature_review);
        assert_eq!(
            preview.selected_source_count,
            preview.selected_local_source_ids.len()
        );
        assert_eq!(
            preview.review_section_plan.requested_sections,
            vec![
                "background".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
                "findings".to_string(),
            ]
        );
        assert_eq!(
            preview.review_section_plan.planned_sections,
            vec![
                "background".to_string(),
                "findings".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
            ]
        );
        assert_eq!(preview.planned_literature_review_steps.len(), 11);
        assert!(preview
            .planned_literature_review_steps
            .iter()
            .all(|step| step.active && step.preview_only && step.boundary_notes == scientific_paper_literature_review_step_notes()));
        assert_eq!(
            preview
                .planned_literature_review_steps
                .iter()
                .map(|step| step.kind.clone())
                .collect::<Vec<_>>(),
            vec![
                ScholarChatScientificPaperLiteratureReviewStepKind::ScientificPaperModeAlignment,
                ScholarChatScientificPaperLiteratureReviewStepKind::EvidencePackPlanAlignment,
                ScholarChatScientificPaperLiteratureReviewStepKind::ResearchQuestionPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::SearchStrategyPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::EvidenceMapPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::ReviewSectionPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::ClaimSynthesisPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::CitationPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::QualityReviewPlanning,
                ScholarChatScientificPaperLiteratureReviewStepKind::ComplianceBoundaryCheck,
                ScholarChatScientificPaperLiteratureReviewStepKind::DownstreamPaperGenerationAlignment,
            ]
        );
        assert!(preview.next_required_actions.iter().any(|action| {
            action.contains("keep this preview-only and plan the literature review later")
        }));
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_answer_generated);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
        assert!(preview.no_llm_call);
    }

    #[test]
    fn scholar_chat_scientific_paper_literature_review_plan_handles_mode_forcing_and_warning_boundaries() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_paper_literature_review_plan_deterministic_and_path_free(
            &temp,
            scientific_paper_literature_review_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("course"),
                Some("Psychology literature review"),
                Some(vec!["psychology", "  psychology  "]),
                Some(vec!["source-a"]),
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(vec![
                    "apa_psycnet_discovery",
                    "apa_psycnet_discovery",
                ]),
                Some(vec!["methods"]),
                Some(vec!["students"]),
                Some(vec!["signal detection"]),
                Some("review local literature"),
                Some(vec!["signal detection", "unknown_claim_scope"]),
                Some("unknown style"),
                Some("literature review"),
                Some("What predicts signal detection?"),
                Some(vec![" background ", "findings", "citation_plan", "compliance_notes", "unknown_section"]),
                Some(3),
                Some(true),
                Some(false),
                Some(2018),
                Some(2024),
            ),
        );

        assert_eq!(preview.status, ScholarChatScientificPaperLiteratureReviewPlanStatus::LiteratureReviewPlanReady);
        assert_eq!(preview.normalized_mode, "scientific_paper");
        assert_eq!(preview.requested_mode, Some("course".to_string()));
        assert_eq!(
            preview.literature_review_strategy,
            ScholarChatScientificPaperLiteratureReviewStrategy::EvidencePackFirst
        );
        assert_eq!(preview.normalized_citation_style, "neutral");
        assert!(preview
            .warnings
            .iter()
            .any(|warning| warning.contains("Unknown review section preference 'unknown_section'")));
        assert!(preview
            .warnings
            .iter()
            .any(|warning| warning.contains("APA/PsycINFO planning boundary only")));
        assert_eq!(
            preview.review_section_plan.unknown_sections,
            vec!["unknown_section".to_string()]
        );
        assert_eq!(
            preview.review_section_plan.planned_sections,
            vec![
                "background".to_string(),
                "findings".to_string(),
                "citation_plan".to_string(),
                "compliance_notes".to_string(),
            ]
        );
        assert!(preview
            .review_section_plan
            .planned_sections
            .iter()
            .all(|section| section != "unknown_section"));
        assert_eq!(
            preview.claim_synthesis_plan.unknown_claim_scope,
            vec![
                "signal_detection".to_string(),
                "unknown_claim_scope".to_string(),
            ]
        );
        assert_eq!(
            preview.claim_synthesis_plan.claim_scope,
            vec!["signal_detection".to_string(), "unknown_claim_scope".to_string()]
        );
        assert!(preview
            .quality_review_plan
            .required_manual_checks_later
            .contains(&"institutional_access_review_later_when_required".to_string()));
        assert!(preview
            .compliance_review_plan
            .no_authenticated_library_access);
        assert!(preview.compliance_review_plan.no_paywall_bypass);
        assert!(!preview
            .compliance_review_plan
            .will_call_external_services);
        assert!(!preview.compliance_review_plan.will_write_metadata);
        assert!(!preview
            .compliance_review_plan
            .will_create_literature_review);
    }

    #[test]
    fn scholar_chat_scientific_query_understanding_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_query_understanding")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_scientific_source_registry_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_source_registry")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_scientific_preview_command_surface_is_wired_once_in_lib_rs() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/lib.rs"));
        let handler_start = source
            .find(".invoke_handler(tauri::generate_handler![")
            .unwrap();
        let handler_end = source[handler_start..]
            .find("        ])")
            .unwrap();
        let handler = &source[handler_start..handler_start + handler_end];

        let imports_start = source.find("use scholar_chat::{").unwrap();
        let imports_end = source[imports_start..].find("};").unwrap() + imports_start + 2;
        let imports = &source[imports_start..imports_end];

        let expected: [(&str, &str, &str, &str); 13] = [
            (
                "preview_scholar_chat_scientific_discipline_registry",
                "ScholarChatScientificDisciplineRegistryPreviewRequest",
                "ScholarChatScientificDisciplineRegistryPreview",
                "preview_scholar_chat_scientific_discipline_registry_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_source_registry",
                "ScholarChatScientificSourceRegistryPreviewRequest",
                "ScholarChatScientificSourceRegistryPreview",
                "preview_scholar_chat_scientific_source_registry_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_query_understanding",
                "ScholarChatScientificQueryUnderstandingPreviewRequest",
                "ScholarChatScientificQueryUnderstandingPreview",
                "preview_scholar_chat_scientific_query_understanding_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_search_plan",
                "ScholarChatScientificSearchPlanRequest",
                "ScholarChatScientificSearchPlanPreview",
                "preview_scholar_chat_scientific_search_plan_impl(root, request)",
            ),
            (
                "preview_scholar_chat_local_literature_index",
                "ScholarChatLocalLiteratureIndexRequest",
                "ScholarChatLocalLiteratureIndexPreview",
                "preview_scholar_chat_local_literature_index_impl(root, request)",
            ),
            (
                "preview_scholar_chat_course_literature_registry",
                "ScholarChatCourseLiteratureRegistryPreviewRequest",
                "ScholarChatCourseLiteratureRegistryPreview",
                "preview_scholar_chat_course_literature_registry_impl(root, request)",
            ),
            (
                "preview_scholar_chat_metadata_connector_plan",
                "ScholarChatMetadataConnectorPlanRequest",
                "ScholarChatMetadataConnectorPlanPreview",
                "preview_scholar_chat_metadata_connector_plan_impl(root, request)",
            ),
            (
                "preview_scholar_chat_psychology_source_connector_plan",
                "ScholarChatPsychologySourceConnectorPlanRequest",
                "ScholarChatPsychologySourceConnectorPlanPreview",
                "preview_scholar_chat_psychology_source_connector_plan_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_evidence_pack_plan",
                "ScholarChatScientificEvidencePackPlanRequest",
                "ScholarChatScientificEvidencePackPlanPreview",
                "preview_scholar_chat_scientific_evidence_pack_plan_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_paper_literature_review_plan",
                "ScholarChatScientificPaperLiteratureReviewPlanRequest",
                "ScholarChatScientificPaperLiteratureReviewPlanPreview",
                "preview_scholar_chat_scientific_paper_literature_review_plan_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_metadata_execution_boundary",
                "ScholarChatScientificMetadataExecutionBoundaryPreviewRequest",
                "ScholarChatScientificMetadataExecutionBoundaryPreview",
                "preview_scholar_chat_scientific_metadata_execution_boundary_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_metadata_provider_config",
                "ScholarChatScientificMetadataProviderConfigPreviewRequest",
                "ScholarChatScientificMetadataProviderConfigPreview",
                "preview_scholar_chat_scientific_metadata_provider_config_impl(root, request)",
            ),
            (
                "preview_scholar_chat_scientific_metadata_query_plan",
                "ScholarChatScientificMetadataQueryPlanPreviewRequest",
                "ScholarChatScientificMetadataQueryPlanPreview",
                "preview_scholar_chat_scientific_metadata_query_plan_impl(root, request)",
            ),
        ];

        let mut previous_position = None;
        for (name, request_ty, response_ty, impl_call) in expected {
            let wrapper_marker = format!("#[tauri::command]\nfn {name}(");
            assert_eq!(source.matches(&wrapper_marker).count(), 1, "{name} wrapper should appear exactly once");
            assert_eq!(source.matches(&format!("request: {request_ty}")).count(), 1, "{name} request type should appear exactly once");
            assert_eq!(source.matches(&format!("Result<{response_ty}, String>")).count(), 1, "{name} response type should appear exactly once");
        assert_eq!(source.matches(impl_call).count(), 1, "{name} should call its impl exactly once");
        assert!(imports.contains(request_ty), "{name} request type should be imported");
        assert!(imports.contains(response_ty), "{name} response type should be imported");
            let wrapper_start = source.find(&wrapper_marker).unwrap();
            let wrapper_end = source[wrapper_start + wrapper_marker.len()..]
                .find("#[tauri::command]\nfn ")
                .or_else(|| source[wrapper_start..].find("pub fn run()"))
                .unwrap();
            let wrapper_body = &source[wrapper_start..wrapper_start + wrapper_marker.len() + wrapper_end];
            assert_eq!(wrapper_body.matches(impl_call).count(), 1, "{name} wrapper should delegate exactly once");
            assert_eq!(wrapper_body.matches(".map_err(to_user_error)").count(), 1, "{name} wrapper should map errors exactly once");
            let position = handler.find(name).unwrap();
            if let Some(previous) = previous_position {
                assert!(previous < position, "{name} should appear after the previous command in the handler");
            }
            previous_position = Some(position);
            assert_eq!(handler.matches(name).count(), 1, "{name} should appear exactly once in the handler");
        }

        let scientific_evidence_pack_position =
            handler.find("preview_scholar_chat_scientific_evidence_pack_plan").unwrap();
        let scientific_paper_literature_review_position = handler
            .find("preview_scholar_chat_scientific_paper_literature_review_plan")
            .unwrap();
        let psychology_source_connector_position =
            handler.find("preview_scholar_chat_psychology_source_connector_plan").unwrap();
        assert!(
            psychology_source_connector_position < scientific_evidence_pack_position,
            "scientific evidence pack preview should follow psychology source connector preview"
        );
        assert!(
            scientific_evidence_pack_position < scientific_paper_literature_review_position,
            "scientific paper literature review preview should follow scientific evidence pack preview"
        );
        let scientific_metadata_execution_boundary_position = handler
            .find("preview_scholar_chat_scientific_metadata_execution_boundary")
            .unwrap();
        let scientific_metadata_provider_config_position = handler
            .find("preview_scholar_chat_scientific_metadata_provider_config")
            .unwrap();
        assert!(
            scientific_paper_literature_review_position
                < scientific_metadata_execution_boundary_position,
            "scientific metadata execution boundary preview should follow scientific paper literature review preview"
        );
        assert!(
            scientific_metadata_execution_boundary_position
                < scientific_metadata_provider_config_position,
            "scientific metadata provider config preview should follow scientific metadata execution boundary preview"
        );
        let scientific_metadata_query_plan_position = handler
            .find("preview_scholar_chat_scientific_metadata_query_plan")
            .unwrap();
        assert!(
            scientific_metadata_provider_config_position < scientific_metadata_query_plan_position,
            "scientific metadata query plan preview should follow scientific metadata provider config preview"
        );
        for runtime_command in [
            "preview_scholar_chat_runtime_answer_pipeline_gate",
            "preview_scholar_chat_runtime_diagnostic_bridge",
            "preview_scholar_chat_runtime_diagnostic_result",
            "run_llama_runtime_version_probe",
            "run_llama_runtime_smoke_diagnostic",
            "smoke_test_local_runtime_inference",
        ] {
            if let Some(runtime_position) = handler.find(runtime_command) {
                assert!(
                    scientific_metadata_provider_config_position < runtime_position,
                    "{runtime_command} should appear after scientific metadata provider config preview"
                );
            }
        }
        if let Some(runtime_diagnostic_bridge_position) =
            handler.find("preview_scholar_chat_runtime_diagnostic_bridge")
        {
            assert!(
                scientific_metadata_query_plan_position < runtime_diagnostic_bridge_position,
                "scientific metadata query plan preview should appear before runtime diagnostic bridge"
            );
        }
        assert!(handler.contains("preview_scholar_chat_scientific_metadata_query_plan"));

        assert!(handler.contains("preview_scholar_chat_scientific_discipline_registry"));
        assert!(handler.contains("preview_scholar_chat_scientific_source_registry"));
        assert!(handler.contains("preview_scholar_chat_scientific_query_understanding"));
        assert!(handler.contains("preview_scholar_chat_scientific_search_plan"));
        assert!(handler.contains("preview_scholar_chat_local_literature_index"));
        assert!(handler.contains("preview_scholar_chat_course_literature_registry"));
        assert!(handler.contains("preview_scholar_chat_metadata_connector_plan"));
        assert!(handler.contains("preview_scholar_chat_psychology_source_connector_plan"));
        assert!(handler.contains("preview_scholar_chat_scientific_evidence_pack_plan"));
        assert!(handler.contains("preview_scholar_chat_scientific_paper_literature_review_plan"));
        assert!(handler.contains("preview_scholar_chat_scientific_metadata_execution_boundary"));
        assert!(handler.contains("preview_scholar_chat_scientific_metadata_provider_config"));
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_body_composes_execution_boundary_once_and_stays_preview_only() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_metadata_provider_config")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_draft_inference")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_scientific_metadata_execution_boundary(")
                .count(),
            1
        );
        for forbidden in [
            "Command::new",
            "reqwest::",
            "ureq::",
            "smoke_test_local_runtime_inference",
            "run_llama_runtime_smoke_diagnostic",
            "run_smoke_inference_probe",
            "preview_scholar_chat_scientific_evidence_pack_plan",
            "preview_scholar_chat_metadata_connector_plan",
            "preview_scholar_chat_psychology_source_connector_plan",
            "preview_scholar_chat_scientific_search_plan",
            "preview_scholar_chat_scientific_query_understanding",
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_retrieval",
            "preview_scholar_chat_evidence_plan",
            "preview_scholar_chat_prompt_pack",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "preview_scholar_chat_grounded_answer_write_eligibility",
            "preview_scholar_chat_grounded_answer_execution_plan",
            "preview_scholar_chat_scientific_paper_literature_review_plan",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "CorpusAuthority::",
            "SourceRegistry::",
            "RetrievalService::new",
            "extract_source",
            "chunk_source",
            "build_retrieval_index",
            "http://",
            "https://",
        ] {
            assert!(!body.contains(forbidden), "{forbidden} should not appear in the provider config preview body");
        }
    }

    fn scientific_metadata_provider_config_preview_request(
        query: &str,
        preferred_metadata_sources: Vec<&str>,
        provider_override: Option<Vec<&str>>,
        allow_provider_terms_unreviewed: bool,
        include_attribution_requirements: bool,
        execution_requested: bool,
        allow_network: bool,
        allow_metadata_record_write: bool,
    ) -> ScholarChatScientificMetadataProviderConfigPreviewRequest {
        ScholarChatScientificMetadataProviderConfigPreviewRequest {
            query: query.to_string(),
            mode: Some("scientific_paper".to_string()),
            context_tags: Some(vec!["science".to_string(), "metadata".to_string()]),
            preferred_metadata_sources: Some(
                preferred_metadata_sources
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            preferred_psychology_source_families: None,
            provider_override: provider_override.map(|values| {
                values
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect()
            }),
            require_open_access: None,
            require_doi: None,
            year_from: None,
            year_to: None,
            config_goal: Some("provider config planning".to_string()),
            include_disabled_providers: Some(false),
            include_institutional_providers: Some(false),
            include_rate_limit_notes: Some(false),
            include_attribution_requirements: Some(include_attribution_requirements),
            execution_requested,
            allow_network,
            allow_provider_terms_unreviewed,
            allow_metadata_record_write,
        }
    }

    fn assert_scientific_metadata_provider_config_preview_boundary_flags(
        preview: &ScholarChatScientificMetadataProviderConfigPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.metadata_provider_config_preview_only);
        assert!(preview.dry_run_only);
        assert!(preview.execution_disabled);
        assert!(preview.no_network_call);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_metadata_persistence);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_metadata_provider_config_preview_is_path_free(
        preview: &ScholarChatScientificMetadataProviderConfigPreview,
        temp_path: &str,
    ) {
        let debug = format!("{preview:?}");
        let json = serde_json::to_string(preview).unwrap();
        for fragment in [
            temp_path,
            ".aegis",
            "http://",
            "https://",
            "AppData",
            "target/",
            "\\target\\",
            "dist/",
            "\\dist\\",
            "models/",
            "\\models\\",
            ":\\",
        ] {
            assert!(!debug.contains(fragment), "debug output should not contain {fragment}");
            assert!(!json.contains(fragment), "json output should not contain {fragment}");
        }
    }

    fn assert_scientific_metadata_provider_config_step_boundary_notes(
        step: &ScholarChatScientificMetadataProviderConfigStep,
    ) {
        for note in [
            "preview-only",
            "provider config preview only",
            "dry-run only",
            "no network call",
            "no web request",
            "no HTTP client",
            "no API key read",
            "no environment read",
            "no scraping",
            "no authenticated library access",
            "no paywall bypass",
            "no connector call",
            "no source import",
            "no metadata record write",
            "no file read",
            "no retrieval execution",
            "no Evidence Pack created",
            "no Literature Review created",
            "no artifact write",
            "no persistence",
        ] {
            assert!(
                step.boundary_notes.iter().any(|value| value == note),
                "step {:?} should include boundary note {note}",
                step.kind
            );
        }
    }

    fn scientific_metadata_query_plan_preview_request(
        query: &str,
        query_goal: Option<&str>,
        preferred_metadata_sources: Vec<&str>,
        preferred_psychology_source_families: Option<Vec<&str>>,
        provider_override: Option<Vec<&str>>,
        execution_requested: bool,
        allow_network: bool,
        allow_provider_terms_unreviewed: bool,
        allow_metadata_record_write: bool,
    ) -> ScholarChatScientificMetadataQueryPlanPreviewRequest {
        ScholarChatScientificMetadataQueryPlanPreviewRequest {
            query: query.to_string(),
            mode: Some("scientific_paper".to_string()),
            context_tags: Some(vec!["science".to_string(), "metadata".to_string()]),
            preferred_metadata_sources: Some(
                preferred_metadata_sources
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            preferred_psychology_source_families: preferred_psychology_source_families.map(
                |values| values.into_iter().map(|value| value.to_string()).collect(),
            ),
            provider_override: provider_override.map(|values| {
                values
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect()
            }),
            query_goal: query_goal.map(|value| value.to_string()),
            require_open_access: Some(false),
            require_doi: Some(false),
            year_from: Some(2018),
            year_to: Some(2026),
            include_disabled_providers: false,
            include_institutional_providers: true,
            include_rate_limit_notes: true,
            include_attribution_requirements: true,
            include_query_templates: true,
            include_filter_plan: true,
            include_result_field_plan: true,
            execution_requested,
            allow_network,
            allow_provider_terms_unreviewed,
            allow_metadata_record_write,
        }
    }

    fn assert_scientific_metadata_query_plan_preview_boundary_flags(
        preview: &ScholarChatScientificMetadataQueryPlanPreview,
    ) {
        assert!(preview.preview_only);
        assert!(preview.metadata_query_plan_preview_only);
        assert!(preview.dry_run_only);
        assert!(preview.execution_disabled);
        assert!(preview.no_network_call);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_metadata_persistence);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    fn assert_scientific_metadata_query_plan_preview_is_path_free(
        preview: &ScholarChatScientificMetadataQueryPlanPreview,
        temp_path: &str,
    ) {
        let debug = format!("{preview:?}");
        let json = serde_json::to_string(preview).unwrap();
        for fragment in [
            temp_path,
            ".aegis",
            "http://",
            "https://",
            "AppData",
            "target/",
            "\\target\\",
            "dist/",
            "\\dist\\",
            "models/",
            "\\models\\",
            ":\\",
        ] {
            assert!(!debug.contains(fragment), "debug output should not contain {fragment}");
            assert!(!json.contains(fragment), "json output should not contain {fragment}");
        }
    }

    fn assert_scientific_metadata_query_plan_provider_record_invariants(
        record: &ScholarChatScientificMetadataQueryPlanProviderRecord,
    ) {
        assert!(!record.provider_id.is_empty());
        assert!(!record.provider_label.is_empty());
        assert!(!record.access_kind_label.is_empty());
        assert!(!record.normalized_query.is_empty());
        assert!(!record.planned_query_terms.is_empty());
        assert!(!record.planned_query_templates.is_empty());
        assert!(!record.planned_filters.is_empty());
        assert!(!record.planned_result_fields.is_empty());
        assert!(!record.planned_sorting.is_empty());
        assert!(!record.planned_dedup_keys.is_empty());
        assert!(!record.planned_rate_limit_notes.is_empty());
        assert!(!record.planned_attribution_notes.is_empty());
        assert!(!record.safety_boundary_notes.is_empty());
        assert!(!record.summary.is_empty());
        assert!(!record.can_execute_now);
        assert!(!record.will_call_provider);
        assert!(!record.will_build_url);
        assert!(!record.will_use_http_client);
        assert!(!record.will_read_api_key);
        assert!(!record.will_read_environment);
        assert!(!record.will_write_metadata);
    }

    fn assert_scientific_metadata_query_plan_step_boundary_notes(
        step: &ScholarChatScientificMetadataQueryPlanStep,
    ) {
        for note in [
            "preview-only",
            "metadata query plan preview only",
            "dry-run only",
            "no provider call",
            "no URL building",
            "no network call",
            "no web request",
            "no HTTP client",
            "no API key read",
            "no environment read",
            "no scraping",
            "no authenticated library access",
            "no paywall bypass",
            "no connector call",
            "no source import",
            "no metadata record write",
            "no file read",
            "no retrieval execution",
            "no Evidence Pack created",
            "no Literature Review created",
            "no artifact write",
            "no persistence",
        ] {
            assert!(
                step.boundary_notes.iter().any(|value| value == note),
                "step {:?} should include boundary note {note}",
                step.kind
            );
        }
    }

    fn assert_scientific_metadata_provider_config_record_invariants(
        record: &ScholarChatScientificMetadataProviderConfigProviderRecord,
    ) {
        assert!(!record.provider_id.is_empty());
        assert!(!record.provider_label.is_empty());
        assert!(!record.metadata_scope.is_empty());
        assert!(!record.planned_query_capabilities.is_empty());
        assert!(!record.planned_filter_capabilities.is_empty());
        assert!(!record.planned_result_fields.is_empty());
        assert!(!record.boundary_notes.is_empty());
        assert!(!record.summary.is_empty());
        assert!(!record.can_execute_now);
        assert!(!record.will_call_provider);
        assert!(!record.will_use_http_client);
        assert!(!record.will_read_api_key);
        assert!(!record.will_read_environment);
        assert!(!record.will_write_metadata);
    }

    fn scientific_metadata_execution_boundary_preview_request(
        query: &str,
        selected_local_source_ids: Vec<&str>,
        preferred_metadata_sources: Vec<&str>,
        preferred_psychology_source_families: Option<Vec<&str>>,
        provider_override: Option<Vec<&str>>,
        execution_requested: bool,
        allow_network: bool,
        allow_provider_terms_unreviewed: bool,
        allow_metadata_record_write: bool,
    ) -> ScholarChatScientificMetadataExecutionBoundaryPreviewRequest {
        ScholarChatScientificMetadataExecutionBoundaryPreviewRequest {
            query: query.to_string(),
            mode: Some("scientific_paper".to_string()),
            course_context: Some("Psychology source connector preview".to_string()),
            context_tags: Some(vec!["science".to_string(), "metadata".to_string()]),
            selected_local_source_ids: Some(
                selected_local_source_ids
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            expected_source_kinds: Some(vec!["paper".to_string()]),
            preferred_metadata_sources: Some(
                preferred_metadata_sources
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect(),
            ),
            preferred_psychology_source_families: preferred_psychology_source_families.map(
                |families| families.into_iter().map(|value| value.to_string()).collect(),
            ),
            methodology_hints: Some(vec!["planning".to_string()]),
            population_hints: None,
            topic_area_hints: Some(vec!["psychology".to_string()]),
            evidence_goal: Some("metadata planning".to_string()),
            claim_scope: Some(vec!["review".to_string()]),
            citation_style: None,
            max_results_per_source: Some(20),
            require_open_access: None,
            require_doi: None,
            year_from: None,
            year_to: None,
            execution_requested,
            allow_network,
            allow_provider_terms_unreviewed,
            allow_metadata_record_write,
            provider_override: provider_override.map(|values| {
                values
                    .into_iter()
                    .map(|value| value.to_string())
                    .collect()
            }),
            dry_run_label: Some("phase_105".to_string()),
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_body_composes_scientific_evidence_pack_preview_and_forbids_execution_calls() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_metadata_execution_boundary")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_draft_inference")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_scientific_evidence_pack_plan").count(),
            1
        );
        for forbidden_call in [
            "preview_scholar_chat_metadata_connector_plan",
            "preview_scholar_chat_psychology_source_connector_plan",
            "preview_scholar_chat_scientific_search_plan",
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "run_llama_runtime_smoke_diagnostic",
            "smoke_test_local_runtime_inference",
            "Command::new",
            "reqwest::",
            "ureq::",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "http://",
            "https://",
            "CorpusAuthority::",
            "SourceRegistry::",
            "RetrievalService::new",
            "extract_source",
            "chunk_source",
            "build_retrieval_index",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
        ] {
            assert!(
                !body.contains(forbidden_call),
                "scientific metadata execution boundary preview body should not contain {forbidden_call}"
            );
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_rejects_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_metadata_execution_boundary_deterministic_and_path_free(
            &temp,
            scientific_metadata_execution_boundary_preview_request(
                "   ",
                vec!["source-a"],
                vec!["openalex"],
                None,
                None,
                false,
                true,
                true,
                true,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataExecutionBoundaryStatus::Blocked
        );
        assert!(preview.blockers.iter().any(|value| value == "query_missing"));
        assert!(
            preview
                .next_required_actions
                .iter()
                .any(|value| value.contains("Provide a scientific query"))
        );
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_uses_default_false_for_missing_boolean_flags() {
        let request_json = serde_json::json!({
            "query": "Signalentdeckung und Wahrnehmung"
        });
        let request: ScholarChatScientificMetadataExecutionBoundaryPreviewRequest =
            serde_json::from_value(request_json).unwrap();
        assert!(!request.execution_requested);
        assert!(!request.allow_network);
        assert!(!request.allow_provider_terms_unreviewed);
        assert!(!request.allow_metadata_record_write);
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_needs_source_family_for_unknown_preference_only() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_metadata_execution_boundary_deterministic_and_path_free(
            &temp,
            scientific_metadata_execution_boundary_preview_request(
                "Signalentdeckung und Wahrnehmung",
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["unknown_family"]),
                None,
                false,
                true,
                true,
                true,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsSourceFamily
        );
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"openalex".to_string()));
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"crossref".to_string()));
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"pubmed".to_string()));
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"eric".to_string()));
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"apa_psycnet".to_string()));
        assert!(preview
            .provider_selection_plan
            .supported_provider_ids
            .contains(&"psycinfo".to_string()));
        assert_eq!(
            preview.provider_selection_plan.selected_provider_ids,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert!(!preview
            .provider_selection_plan
            .needs_provider_selection);
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_normalizes_multiple_provider_overrides_deterministically() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_metadata_execution_boundary_deterministic_and_path_free(
            &temp,
            scientific_metadata_execution_boundary_preview_request(
                "Signalentdeckung und Wahrnehmung",
                vec!["source-a"],
                vec!["openalex", "crossref"],
                None,
                Some(vec![
                    "APA_PsycNet",
                    "crossref",
                    "openalex",
                    "crossref",
                    "Unknown_Family",
                ]),
                false,
                true,
                true,
                true,
            ),
        );

        assert_eq!(
            preview.normalized_provider_override,
            Some(vec![
                "apa_psycnet".to_string(),
                "crossref".to_string(),
                "openalex".to_string(),
                "unknown_family".to_string(),
            ])
        );
        assert_eq!(
            preview.provider_selection_plan.requested_provider_ids,
            vec![
                "apa_psycnet".to_string(),
                "crossref".to_string(),
                "openalex".to_string(),
                "unknown_family".to_string(),
            ]
        );
        assert_eq!(
            preview.provider_selection_plan.selected_provider_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "apa_psycnet".to_string(),
            ]
        );
        assert_eq!(
            preview.provider_selection_plan.unknown_provider_ids,
            vec!["unknown_family".to_string()]
        );
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("Unknown provider override is preserved only as a preview-only hint.")));
    }

    #[test]
    fn scholar_chat_scientific_metadata_execution_boundary_is_ready_later_with_supported_providers_and_boundary_flags() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_metadata_execution_boundary_deterministic_and_path_free(
            &temp,
            scientific_metadata_execution_boundary_preview_request(
                "Signalentdeckung und Wahrnehmung",
                vec!["source-a"],
                vec!["openalex", "crossref"],
                None,
                Some(vec!["APA_PsycNet", "crossref", "openalex", "crossref"]),
                false,
                true,
                true,
                true,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataExecutionBoundaryStatus::ExecutionBoundaryReady
        );
        assert_eq!(
            preview.provider_selection_plan.supported_provider_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "pubmed".to_string(),
                "eric".to_string(),
                "apa_psycnet".to_string(),
                "psycinfo".to_string(),
            ]
        );
        assert_eq!(
            preview.provider_selection_plan.selected_provider_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "apa_psycnet".to_string(),
            ]
        );
        assert!(!preview.provider_request_plan.will_call_provider);
        assert!(!preview.provider_request_plan.will_authenticate);
        assert!(!preview.provider_request_plan.will_bypass_paywall);
        assert!(preview
            .provider_request_plan
            .planned_query_inputs
            .iter()
            .any(|value| value.contains("Signalentdeckung und Wahrnehmung")));
        assert!(preview
            .provider_request_plan
            .planned_result_fields
            .iter()
            .any(|value| value == "doi_or_stable_identifier"));
        assert!(!preview.metadata_write_plan.will_write_metadata_records);
        assert_eq!(
            preview.metadata_write_plan.persistence_target,
            "phase_105_preview_boundary"
        );
        assert!(preview
            .metadata_write_plan
            .summary
            .contains("not requested in this dry run"));
        assert!(preview
            .safety_boundary_plan
            .blocked_flags
            .iter()
            .any(|value| value == "no_network_call"));
        assert_eq!(
            preview
                .planned_execution_steps
                .iter()
                .map(|step| step.id.as_str())
                .collect::<Vec<_>>(),
            vec![
                "evidence_pack_alignment",
                "metadata_connector_alignment",
                "provider_selection",
                "provider_terms_boundary",
                "network_permission_gate",
                "provider_request_planning",
                "metadata_write_gate",
                "safety_boundary_check",
                "downstream_evidence_pack_alignment",
                "downstream_literature_review_alignment",
            ]
        );
        assert!(preview
            .planned_execution_steps
            .iter()
            .all(|step| step.preview_only));
        assert!(preview
            .summary
            .contains("ready later and only describes"));
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_blocks_on_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = preview_scholar_chat_scientific_metadata_provider_config(
            temp.path(),
            scientific_metadata_provider_config_preview_request(
                "   ",
                vec!["openalex", "crossref"],
                Some(vec!["openalex"]),
                false,
                false,
                false,
                false,
                false,
            ),
        )
        .unwrap();

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataProviderConfigStatus::Blocked
        );
        assert!(preview.blockers.iter().any(|value| value == "query_missing"));
        assert!(preview.preview_only);
        assert!(preview.metadata_provider_config_preview_only);
        assert!(preview.dry_run_only);
        assert!(preview.execution_disabled);
        assert!(preview.no_network_call);
        assert!(preview.no_web_request);
        assert!(preview.no_http_client);
        assert!(preview.no_api_key_read);
        assert!(preview.no_environment_read);
        assert!(preview.no_scraping);
        assert!(preview.no_authenticated_library_access);
        assert!(preview.no_paywall_bypass);
        assert!(preview.no_connector_call);
        assert!(preview.no_source_import);
        assert!(preview.no_metadata_record_write);
        assert!(preview.no_metadata_persistence);
        assert!(preview.no_local_file_indexing);
        assert!(preview.no_file_read);
        assert!(preview.no_pdf_extraction);
        assert!(preview.no_ocr);
        assert!(preview.no_chunking_run);
        assert!(preview.no_embedding_generation);
        assert!(preview.no_index_created);
        assert!(preview.no_retrieval_execution);
        assert!(preview.no_model_loading);
        assert!(preview.no_runtime_inference);
        assert!(preview.no_llm_call);
        assert!(preview.no_answer_generated);
        assert!(preview.no_literature_review_created);
        assert!(preview.no_evidence_pack_created);
        assert!(preview.no_artifact_write);
        assert!(preview.no_persistence);
        assert!(preview.no_registry_status_change);
        assert!(preview.no_audit_write);
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_needs_metadata_execution_boundary_with_supported_openalex_and_deterministic_provider_catalog() {
        let temp = tempfile::tempdir().unwrap();
        let mut request = scientific_metadata_provider_config_preview_request(
            "  Signalentdeckung   und   Wahrnehmung  ",
            vec!["openalex", "crossref", "unknown_provider"],
            Some(vec!["OpenAlex", "openalex", "crossref", "OPENALEX"]),
            true,
            false,
            false,
            false,
            false,
        );
        request.config_goal = Some("  provider   config   planning  ".to_string());
        let first =
            preview_scholar_chat_scientific_metadata_provider_config(temp.path(), request.clone())
                .unwrap();
        let second =
            preview_scholar_chat_scientific_metadata_provider_config(temp.path(), request)
                .unwrap();
        let temp_path = temp.path().to_string_lossy();

        assert_eq!(first, second);
        assert!(!temp.path().join(".aegis").exists());
        for preview in [&first, &second] {
            assert_scientific_metadata_provider_config_preview_boundary_flags(preview);
            assert_scientific_metadata_provider_config_preview_is_path_free(
                preview,
                temp_path.as_ref(),
            );
            assert_eq!(
                preview.status,
                ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary
            );
            assert_eq!(
                preview.provider_config_strategy,
                ScholarChatScientificMetadataProviderConfigStrategy::ProviderPolicyReviewFirst
            );
            assert_eq!(
                preview.normalized_provider_override,
                Some(vec!["crossref".to_string(), "openalex".to_string()])
            );
            assert_eq!(
                preview.normalized_query,
                "Signalentdeckung und Wahrnehmung"
            );
            assert_eq!(
                preview.normalized_mode,
                Some("scientific_paper".to_string())
            );
            assert_eq!(
                preview.normalized_config_goal,
                Some("provider config planning".to_string())
            );
            assert_eq!(preview.selected_provider_ids, vec!["openalex", "crossref"]);
            assert_eq!(preview.selected_provider_count, 2);
            assert_eq!(preview.unknown_provider_ids, vec!["unknown_provider"]);
            assert!(preview
                .warnings
                .iter()
                .any(|warning| warning.contains("Unknown provider override is preserved only as a preview-only hint.")));
            assert_eq!(
                preview
                    .metadata_execution_boundary_strategy
                    .clone(),
                ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
            );
            assert_eq!(
                preview.provider_configs.len(),
                scientific_metadata_supported_providers().len()
            );
            assert_eq!(
                preview
                    .provider_configs
                    .iter()
                    .map(|record| record.provider_id.clone())
                    .collect::<Vec<_>>(),
                scientific_metadata_supported_providers()
            );
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| record.supported));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.selected || preview.selected_provider_ids.contains(&record.provider_id)));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.can_execute_now));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.will_call_provider));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.will_use_http_client));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.will_read_api_key));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.will_read_environment));
            assert!(preview
                .provider_configs
                .iter()
                .all(|record| !record.will_write_metadata));
            for record in &preview.provider_configs {
                assert_scientific_metadata_provider_config_record_invariants(record);
                match record.provider_id.as_str() {
                    "openalex" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::OpenMetadata
                        );
                        assert!(record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(!record.requires_attribution);
                        assert!(!record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(!record.requires_institutional_access_review_later);
                        assert!(!record.authenticated_access_blocked_now);
                        assert!(!record.paywall_bypass_blocked_now);
                    }
                    "crossref" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                        );
                        assert!(record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(record.requires_attribution);
                        assert!(!record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(!record.requires_institutional_access_review_later);
                        assert!(!record.authenticated_access_blocked_now);
                        assert!(!record.paywall_bypass_blocked_now);
                    }
                    "pubmed" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                        );
                        assert!(!record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(record.requires_attribution);
                        assert!(!record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(!record.requires_institutional_access_review_later);
                        assert!(!record.authenticated_access_blocked_now);
                        assert!(!record.paywall_bypass_blocked_now);
                    }
                    "eric" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                        );
                        assert!(!record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(record.requires_attribution);
                        assert!(!record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(!record.requires_institutional_access_review_later);
                        assert!(!record.authenticated_access_blocked_now);
                        assert!(!record.paywall_bypass_blocked_now);
                    }
                    "apa_psycnet" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::InstitutionalDiscovery
                        );
                        assert!(!record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(!record.requires_attribution);
                        assert!(record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(record.requires_institutional_access_review_later);
                        assert!(record.authenticated_access_blocked_now);
                        assert!(record.paywall_bypass_blocked_now);
                    }
                    "psycinfo" => {
                        assert_eq!(
                            record.access_kind,
                            ScholarChatScientificMetadataProviderAccessKind::AuthenticatedLibraryBoundary
                        );
                        assert!(!record.selected);
                        assert!(record.requires_network_permission);
                        assert!(record.requires_terms_review);
                        assert!(!record.requires_attribution);
                        assert!(record.requires_api_key_later);
                        assert!(!record.requires_environment_later);
                        assert!(record.requires_institutional_access_review_later);
                        assert!(record.authenticated_access_blocked_now);
                        assert!(record.paywall_bypass_blocked_now);
                    }
                    other => panic!("unexpected provider id {other}"),
                }
            }
            assert_eq!(
                preview.provider_terms_policy.providers_requiring_terms_review,
                vec!["openalex".to_string(), "crossref".to_string()]
            );
            assert!(preview
                .provider_terms_policy
                .providers_requiring_institutional_access_review_later
                .is_empty());
            assert!(preview.provider_terms_policy.terms_review_required);
            assert!(!preview.provider_terms_policy.unreviewed_terms_block_execution);
            assert!(preview.provider_terms_policy.authenticated_access_blocked);
            assert!(preview.provider_terms_policy.paywall_bypass_blocked);
            assert!(!preview.provider_terms_policy.summary.is_empty());
            assert_eq!(
                preview.provider_access_policy.open_metadata_providers,
                vec!["openalex".to_string()]
            );
            assert_eq!(
                preview.provider_access_policy.public_metadata_api_providers,
                vec![
                    "crossref".to_string(),
                    "pubmed".to_string(),
                    "eric".to_string()
                ]
            );
            assert_eq!(
                preview.provider_access_policy.institutional_boundary_providers,
                vec!["apa_psycnet".to_string(), "psycinfo".to_string()]
            );
            assert!(preview.provider_access_policy.authenticated_access_blocked_now);
            assert!(!preview
                .provider_access_policy
                .manual_access_review_required_later);
            assert!(!preview.provider_access_policy.summary.is_empty());
            assert_eq!(
                preview.provider_rate_limit_policy.providers_requiring_rate_limit_review_later,
                vec!["openalex".to_string(), "crossref".to_string()]
            );
            assert!(preview
                .provider_rate_limit_policy
                .provider_request_budget_planned_later);
            assert!(!preview
                .provider_rate_limit_policy
                .will_enforce_rate_limit_now);
            assert!(!preview.provider_rate_limit_policy.will_call_provider_now);
            assert!(!preview.provider_rate_limit_policy.summary.is_empty());
            assert_eq!(
                preview.provider_attribution_policy.providers_requiring_attribution,
                vec!["crossref".to_string()]
            );
            assert_eq!(
                preview.provider_attribution_policy.required_attribution_fields,
                vec![
                    "provider_name".to_string(),
                    "provider_record_id".to_string(),
                    "source_family".to_string(),
                    "provider_terms_note".to_string()
                ]
            );
            assert!(!preview.provider_attribution_policy.will_emit_citations_now);
            assert!(!preview.provider_attribution_policy.will_write_attribution_now);
            assert!(!preview.provider_attribution_policy.summary.is_empty());
            assert!(preview.provider_safety_boundary.external_services_blocked_now);
            assert!(preview.provider_safety_boundary.connector_calls_blocked_now);
            assert!(preview.provider_safety_boundary.network_blocked_now);
            assert!(preview.provider_safety_boundary.http_client_blocked_now);
            assert!(preview.provider_safety_boundary.api_key_reads_blocked_now);
            assert!(preview.provider_safety_boundary.environment_reads_blocked_now);
            assert!(preview.provider_safety_boundary.scraping_blocked_now);
            assert!(preview
                .provider_safety_boundary
                .authenticated_library_access_blocked_now);
            assert!(preview.provider_safety_boundary.paywall_bypass_blocked_now);
            assert!(preview.provider_safety_boundary.metadata_writes_blocked_now);
            assert!(preview.provider_safety_boundary.source_import_blocked_now);
            assert!(!preview.provider_safety_boundary.summary.is_empty());
            let expected_step_kinds = vec![
                ScholarChatScientificMetadataProviderConfigStepKind::MetadataExecutionBoundaryAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderCapabilityCatalog,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderTermsPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAccessPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderRateLimitPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAttributionPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderSafetyBoundary,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamMetadataExecutionAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamEvidencePackAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamLiteratureReviewAlignment,
            ];
            assert_eq!(
                preview
                    .planned_config_steps
                    .iter()
                    .map(|step| step.kind.clone())
                    .collect::<Vec<_>>(),
                expected_step_kinds
            );
            assert!(preview.planned_config_steps.iter().all(|step| step.preview_only));
            assert!(preview.planned_config_steps.iter().all(|step| step.active));
            for step in &preview.planned_config_steps {
                assert!(!step.id.is_empty());
                assert!(!step.label.is_empty());
                assert!(!step.description.is_empty());
                assert!(!step.planned_inputs.is_empty());
                assert!(!step.planned_outputs.is_empty());
                assert_scientific_metadata_provider_config_step_boundary_notes(step);
            }
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_minimal_request_deserializes_with_safe_defaults() {
        let request: ScholarChatScientificMetadataProviderConfigPreviewRequest =
            serde_json::from_str(r#"{"query":"  provider config planning  "}"#).unwrap();

        assert_eq!(request.query, "  provider config planning  ");
        assert_eq!(request.mode, None);
        assert_eq!(request.context_tags, None);
        assert_eq!(request.preferred_metadata_sources, None);
        assert_eq!(request.preferred_psychology_source_families, None);
        assert_eq!(request.provider_override, None);
        assert_eq!(request.require_open_access, None);
        assert_eq!(request.require_doi, None);
        assert_eq!(request.year_from, None);
        assert_eq!(request.year_to, None);
        assert_eq!(request.config_goal, None);
        assert_eq!(request.include_disabled_providers, None);
        assert_eq!(request.include_institutional_providers, None);
        assert_eq!(request.include_rate_limit_notes, None);
        assert_eq!(request.include_attribution_requirements, None);
        assert!(!request.execution_requested);
        assert!(!request.allow_network);
        assert!(!request.allow_provider_terms_unreviewed);
        assert!(!request.allow_metadata_record_write);
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_status_matrix_matches_preview_contract() {
        let temp = tempfile::tempdir().unwrap();
        let mut cases = Vec::new();

        let mut blank_query = scientific_metadata_provider_config_preview_request(
            "   ",
            vec!["openalex"],
            None,
            false,
            false,
            false,
            false,
            false,
        );
        blank_query.config_goal = Some("   ".to_string());
        cases.push((
            "blank query",
            blank_query,
            ScholarChatScientificMetadataProviderConfigStatus::Blocked,
            ScholarChatScientificMetadataProviderConfigStrategy::Blocked,
        ));

        cases.push((
            "no selected provider",
            scientific_metadata_provider_config_preview_request(
                "Signalentdeckung und Wahrnehmung",
                vec![],
                Some(vec!["unknown_provider"]),
                true,
                true,
                false,
                true,
                true,
            ),
            ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary,
            ScholarChatScientificMetadataProviderConfigStrategy::ProviderPolicyReviewFirst,
        ));

        for (label, request, expected_status, expected_strategy) in cases {
            let preview =
                preview_scholar_chat_scientific_metadata_provider_config(temp.path(), request)
                    .unwrap();
            assert_eq!(preview.status, expected_status, "{label}");
            assert_eq!(preview.provider_config_strategy, expected_strategy, "{label}");
            assert_scientific_metadata_provider_config_preview_boundary_flags(&preview);
            assert!(!preview.summary.is_empty(), "{label}");
            if matches!(
                expected_status,
                ScholarChatScientificMetadataProviderConfigStatus::Blocked
            ) {
                assert!(preview
                    .planned_config_steps
                    .iter()
                    .all(|step| !step.active),
                    "{label}"
                );
            } else {
                assert!(preview
                    .planned_config_steps
                    .iter()
                    .all(|step| step.active),
                    "{label}"
                );
            }
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_provider_config_policy_and_step_invariants_remain_deterministic_and_path_free() {
        let temp = tempfile::tempdir().unwrap();
        let mut request = scientific_metadata_provider_config_preview_request(
            "  Signalentdeckung   und   Wahrnehmung  ",
            vec!["openalex", "crossref", "unknown_provider"],
            Some(vec!["OpenAlex", "openalex", "crossref", "OPENALEX"]),
            true,
            false,
            false,
            false,
            false,
        );
        request.config_goal = Some("  provider   config   planning  ".to_string());

        let first =
            preview_scholar_chat_scientific_metadata_provider_config(temp.path(), request.clone())
                .unwrap();
        let second =
            preview_scholar_chat_scientific_metadata_provider_config(temp.path(), request)
                .unwrap();
        let temp_path = temp.path().to_string_lossy();

        assert_eq!(first, second);
        assert!(!temp.path().join(".aegis").exists());
        assert_scientific_metadata_provider_config_preview_is_path_free(&first, temp_path.as_ref());
        assert_scientific_metadata_provider_config_preview_is_path_free(&second, temp_path.as_ref());
        assert_eq!(
            first.normalized_query,
            "Signalentdeckung und Wahrnehmung"
        );
        assert_eq!(
            first.normalized_mode,
            Some("scientific_paper".to_string())
        );
        assert_eq!(
            first.normalized_config_goal,
            Some("provider config planning".to_string())
        );
        assert_eq!(first.status, ScholarChatScientificMetadataProviderConfigStatus::NeedsMetadataExecutionBoundary);
        assert_eq!(
            first.provider_config_strategy,
            ScholarChatScientificMetadataProviderConfigStrategy::ProviderPolicyReviewFirst
        );
        assert_eq!(first.selected_provider_ids, vec!["openalex", "crossref"]);
        assert_eq!(first.selected_provider_count, 2);
        assert_eq!(first.unknown_provider_ids, vec!["unknown_provider"]);
        assert!(
            first
                .warnings
                .iter()
                .any(|warning| warning.contains("Unknown provider override is preserved only as a preview-only hint."))
        );
        assert_eq!(
            first.metadata_execution_boundary_status,
            ScholarChatScientificMetadataExecutionBoundaryStatus::NeedsNetworkPermission
        );
        assert_eq!(
            first.metadata_execution_boundary_strategy,
            ScholarChatScientificMetadataExecutionBoundaryStrategy::ExecutionGatePending
        );
        assert_eq!(
            first.provider_terms_policy.providers_requiring_terms_review,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert!(first
            .provider_terms_policy
            .providers_requiring_institutional_access_review_later
            .is_empty());
        assert!(first.provider_terms_policy.terms_review_required);
        assert!(!first.provider_terms_policy.unreviewed_terms_block_execution);
        assert!(first.provider_terms_policy.authenticated_access_blocked);
        assert!(first.provider_terms_policy.paywall_bypass_blocked);
        assert!(!first.provider_terms_policy.summary.is_empty());
        assert_eq!(
            first.provider_access_policy.open_metadata_providers,
            vec!["openalex".to_string()]
        );
        assert_eq!(
            first.provider_access_policy.public_metadata_api_providers,
            vec![
                "crossref".to_string(),
                "pubmed".to_string(),
                "eric".to_string()
            ]
        );
        assert_eq!(
            first.provider_access_policy.institutional_boundary_providers,
            vec!["apa_psycnet".to_string(), "psycinfo".to_string()]
        );
        assert!(first.provider_access_policy.authenticated_access_blocked_now);
        assert!(!first.provider_access_policy.manual_access_review_required_later);
        assert!(!first.provider_access_policy.summary.is_empty());
        assert_eq!(
            first.provider_rate_limit_policy.providers_requiring_rate_limit_review_later,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert!(first.provider_rate_limit_policy.provider_request_budget_planned_later);
        assert!(!first.provider_rate_limit_policy.will_enforce_rate_limit_now);
        assert!(!first.provider_rate_limit_policy.will_call_provider_now);
        assert!(!first.provider_rate_limit_policy.summary.is_empty());
        assert_eq!(
            first.provider_attribution_policy.providers_requiring_attribution,
            vec!["crossref".to_string()]
        );
        assert_eq!(
            first.provider_attribution_policy.required_attribution_fields,
            vec![
                "provider_name".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "provider_terms_note".to_string()
            ]
        );
        assert!(!first.provider_attribution_policy.will_emit_citations_now);
        assert!(!first.provider_attribution_policy.will_write_attribution_now);
        assert!(!first.provider_attribution_policy.summary.is_empty());
        assert!(first.provider_safety_boundary.external_services_blocked_now);
        assert!(first.provider_safety_boundary.connector_calls_blocked_now);
        assert!(first.provider_safety_boundary.network_blocked_now);
        assert!(first.provider_safety_boundary.http_client_blocked_now);
        assert!(first.provider_safety_boundary.api_key_reads_blocked_now);
        assert!(first.provider_safety_boundary.environment_reads_blocked_now);
        assert!(first.provider_safety_boundary.scraping_blocked_now);
        assert!(first
            .provider_safety_boundary
            .authenticated_library_access_blocked_now);
        assert!(first.provider_safety_boundary.paywall_bypass_blocked_now);
        assert!(first.provider_safety_boundary.metadata_writes_blocked_now);
        assert!(first.provider_safety_boundary.source_import_blocked_now);
        assert!(!first.provider_safety_boundary.summary.is_empty());
        assert_eq!(
            first.planned_config_steps.iter().map(|step| step.kind.clone()).collect::<Vec<_>>(),
            vec![
                ScholarChatScientificMetadataProviderConfigStepKind::MetadataExecutionBoundaryAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderCapabilityCatalog,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderTermsPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAccessPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderRateLimitPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderAttributionPolicy,
                ScholarChatScientificMetadataProviderConfigStepKind::ProviderSafetyBoundary,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamMetadataExecutionAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamEvidencePackAlignment,
                ScholarChatScientificMetadataProviderConfigStepKind::DownstreamLiteratureReviewAlignment,
            ]
        );
        assert!(first.planned_config_steps.iter().all(|step| step.preview_only));
        assert!(first.planned_config_steps.iter().all(|step| step.active));
        for step in &first.planned_config_steps {
            assert!(!step.id.is_empty());
            assert!(!step.label.is_empty());
            assert!(!step.description.is_empty());
            assert!(!step.planned_inputs.is_empty());
            assert!(!step.planned_outputs.is_empty());
            assert_scientific_metadata_provider_config_step_boundary_notes(step);
        }
        for record in &first.provider_configs {
            assert_scientific_metadata_provider_config_record_invariants(record);
            match record.provider_id.as_str() {
                "openalex" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::OpenMetadata
                    );
                    assert!(record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(!record.requires_attribution);
                    assert!(!record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(!record.requires_institutional_access_review_later);
                    assert!(!record.authenticated_access_blocked_now);
                    assert!(!record.paywall_bypass_blocked_now);
                }
                "crossref" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                    );
                    assert!(record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(record.requires_attribution);
                    assert!(!record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(!record.requires_institutional_access_review_later);
                    assert!(!record.authenticated_access_blocked_now);
                    assert!(!record.paywall_bypass_blocked_now);
                }
                "pubmed" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                    );
                    assert!(!record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(record.requires_attribution);
                    assert!(!record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(!record.requires_institutional_access_review_later);
                    assert!(!record.authenticated_access_blocked_now);
                    assert!(!record.paywall_bypass_blocked_now);
                }
                "eric" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::PublicMetadataApi
                    );
                    assert!(!record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(record.requires_attribution);
                    assert!(!record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(!record.requires_institutional_access_review_later);
                    assert!(!record.authenticated_access_blocked_now);
                    assert!(!record.paywall_bypass_blocked_now);
                }
                "apa_psycnet" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::InstitutionalDiscovery
                    );
                    assert!(!record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(!record.requires_attribution);
                    assert!(record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(record.requires_institutional_access_review_later);
                    assert!(record.authenticated_access_blocked_now);
                    assert!(record.paywall_bypass_blocked_now);
                }
                "psycinfo" => {
                    assert_eq!(
                        record.access_kind,
                        ScholarChatScientificMetadataProviderAccessKind::AuthenticatedLibraryBoundary
                    );
                    assert!(!record.selected);
                    assert!(record.requires_network_permission);
                    assert!(record.requires_terms_review);
                    assert!(!record.requires_attribution);
                    assert!(record.requires_api_key_later);
                    assert!(!record.requires_environment_later);
                    assert!(record.requires_institutional_access_review_later);
                    assert!(record.authenticated_access_blocked_now);
                    assert!(record.paywall_bypass_blocked_now);
                }
                other => panic!("unexpected provider id {other}"),
            }
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_body_composes_provider_config_once_and_stays_preview_only() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_scientific_metadata_query_plan")
            .unwrap();
        let end = source[start..]
            .find("#[cfg(test)]")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(
            body.matches("preview_scholar_chat_scientific_metadata_provider_config(")
                .count(),
            1
        );
        for forbidden in [
            "preview_scholar_chat_scientific_metadata_execution_boundary",
            "preview_scholar_chat_scientific_evidence_pack_plan",
            "preview_scholar_chat_metadata_connector_plan",
            "preview_scholar_chat_psychology_source_connector_plan",
            "preview_scholar_chat_scientific_search_plan",
            "preview_scholar_chat_scientific_query_understanding",
            "preview_scholar_chat_local_literature_index",
            "preview_scholar_chat_course_literature_registry",
            "preview_scholar_chat_retrieval",
            "preview_scholar_chat_prompt_pack",
            "preview_scholar_chat_answer_readiness",
            "preview_scholar_chat_draft_inference",
            "preview_scholar_chat_grounded_answer",
            "preview_scholar_chat_grounded_answer_write_eligibility",
            "preview_scholar_chat_grounded_answer_execution_plan",
            "run_llama_runtime_smoke_diagnostic",
            "smoke_test_local_runtime_inference",
            "run_smoke_inference_probe",
            "Command::new",
            "reqwest::",
            "ureq::",
            "build_answer_draft",
            "build_grounded_answer",
            "build_final_answer",
            "build_evidence_pack",
            "export_answer_artifacts",
            "std::fs",
            "fs::",
            "std::env",
            "env::",
            "http://",
            "https://",
        ] {
            assert!(
                !body.contains(forbidden),
                "scientific metadata query plan preview body should not contain {forbidden}"
            );
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = preview_scholar_chat_scientific_metadata_query_plan(
            temp.path(),
            scientific_metadata_query_plan_preview_request(
                "   ",
                Some("metadata planning"),
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology"]),
                None,
                false,
                true,
                true,
                true,
            ),
        )
        .unwrap();

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataQueryPlanStatus::Blocked
        );
        assert!(preview.blockers.iter().any(|value| value == "query_missing"));
        assert_scientific_metadata_query_plan_preview_boundary_flags(&preview);
        assert_scientific_metadata_query_plan_preview_is_path_free(
            &preview,
            temp.path().to_string_lossy().as_ref(),
        );
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_needs_provider_selection_when_no_supported_providers_are_selected() {
        let temp = tempfile::tempdir().unwrap();
        let preview = preview_scholar_chat_scientific_metadata_query_plan(
            temp.path(),
            scientific_metadata_query_plan_preview_request(
                "Signalentdeckung und Wahrnehmung",
                Some("metadata planning"),
                vec![],
                None,
                None,
                false,
                true,
                true,
                true,
            ),
        )
        .unwrap();

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection
        );
        assert_eq!(
            preview.query_plan_strategy,
            ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst
        );
        assert_scientific_metadata_query_plan_preview_boundary_flags(&preview);
        assert!(preview
            .next_required_actions
            .iter()
            .any(|value| value.contains("Select one or more supported providers")));
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_is_ready_later_with_supported_providers_and_boundary_flags() {
        let temp = tempfile::tempdir().unwrap();
        let request = scientific_metadata_query_plan_preview_request(
            "  Signalentdeckung und Wahrnehmung  ",
            Some("  scientific metadata planning  "),
            vec!["openalex", "crossref"],
            Some(vec!["openalex_psychology", "crossref_psychology"]),
            Some(vec!["openalex", "crossref"]),
            false,
            true,
            true,
            true,
        );
        let first = preview_scholar_chat_scientific_metadata_query_plan(temp.path(), request.clone())
            .unwrap();
        let second = preview_scholar_chat_scientific_metadata_query_plan(temp.path(), request)
            .unwrap();

        assert_eq!(first, second);
        assert!(!temp.path().join(".aegis").exists());
        for preview in [&first, &second] {
            assert_scientific_metadata_query_plan_preview_boundary_flags(preview);
            assert_scientific_metadata_query_plan_preview_is_path_free(
                preview,
                temp.path().to_string_lossy().as_ref(),
            );
            assert_eq!(
                preview.status,
                ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady
            );
            assert_eq!(
                preview.provider_config_status,
                ScholarChatScientificMetadataProviderConfigStatus::ProviderConfigReady
            );
            assert_eq!(
                preview.query_plan_strategy,
                ScholarChatScientificMetadataQueryPlanStrategy::PublicMetadataQueryPlan
            );
            assert_eq!(preview.selected_provider_ids, vec!["openalex", "crossref"]);
            assert_eq!(preview.selected_provider_count, 2);
            assert_eq!(
                preview.public_metadata_provider_ids,
                vec![
                    "openalex".to_string(),
                    "crossref".to_string(),
                    "pubmed".to_string(),
                    "eric".to_string(),
                ]
            );
            assert_eq!(
                preview.institutional_boundary_provider_ids,
                vec!["apa_psycnet".to_string(), "psycinfo".to_string()]
            );
            assert!(preview.blockers.is_empty());
            assert!(!preview.next_required_actions.is_empty());
            assert!(preview
                .summary
                .contains("ready later and only describes"));
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_minimal_request_deserializes_with_safe_defaults() {
        let request: ScholarChatScientificMetadataQueryPlanPreviewRequest =
            serde_json::from_str(r#"{"query":"  scientific metadata planning  "}"#).unwrap();

        assert_eq!(request.query, "  scientific metadata planning  ");
        assert_eq!(request.mode, None);
        assert_eq!(request.context_tags, None);
        assert_eq!(request.preferred_metadata_sources, None);
        assert_eq!(request.preferred_psychology_source_families, None);
        assert_eq!(request.provider_override, None);
        assert_eq!(request.query_goal, None);
        assert_eq!(request.require_open_access, None);
        assert_eq!(request.require_doi, None);
        assert_eq!(request.year_from, None);
        assert_eq!(request.year_to, None);
        assert!(!request.include_disabled_providers);
        assert!(!request.include_institutional_providers);
        assert!(!request.include_rate_limit_notes);
        assert!(!request.include_attribution_requirements);
        assert!(!request.include_query_templates);
        assert!(!request.include_filter_plan);
        assert!(!request.include_result_field_plan);
        assert!(!request.execution_requested);
        assert!(!request.allow_network);
        assert!(!request.allow_provider_terms_unreviewed);
        assert!(!request.allow_metadata_record_write);
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_normalizes_provider_overrides_and_query_goal_deterministically() {
        let temp = tempfile::tempdir().unwrap();
        let request = scientific_metadata_query_plan_preview_request(
            "  Signalentdeckung und Wahrnehmung  ",
            Some("  scientific metadata planning  "),
            vec!["openalex", "crossref"],
            Some(vec!["openalex_psychology", "crossref_psychology"]),
            Some(vec![
                "OpenAlex",
                "openalex",
                "CROSSREF",
                "crossref",
                "Unknown_Family",
                "unknown_family",
            ]),
            false,
            true,
            true,
            true,
        );
        let before_entries = fs::read_dir(temp.path()).unwrap().count();
        let first = preview_scholar_chat_scientific_metadata_query_plan(temp.path(), request.clone())
            .unwrap();
        let second = preview_scholar_chat_scientific_metadata_query_plan(temp.path(), request)
            .unwrap();
        let temp_path = temp.path().to_string_lossy();

        assert_eq!(first, second);
        assert_eq!(before_entries, fs::read_dir(temp.path()).unwrap().count());
        assert!(!temp.path().join(".aegis").exists());
        assert_scientific_metadata_query_plan_preview_boundary_flags(&first);
        assert_scientific_metadata_query_plan_preview_is_path_free(&first, temp_path.as_ref());
        assert_scientific_metadata_query_plan_preview_is_path_free(&second, temp_path.as_ref());
        assert_eq!(
            first.status,
            ScholarChatScientificMetadataQueryPlanStatus::QueryPlanReady
        );
        assert_eq!(
            first.query_plan_strategy,
            ScholarChatScientificMetadataQueryPlanStrategy::PublicMetadataQueryPlan
        );
        assert_eq!(
            first.normalized_query,
            "Signalentdeckung und Wahrnehmung"
        );
        assert_eq!(
            first.normalized_query_goal,
            Some("scientific metadata planning".to_string())
        );
        assert_eq!(
            first.normalized_provider_override,
            Some(vec![
                "crossref".to_string(),
                "openalex".to_string(),
                "unknown_family".to_string(),
            ])
        );
        assert_eq!(first.unknown_provider_ids, vec!["unknown_family".to_string()]);
        assert_eq!(first.selected_provider_ids, vec!["openalex".to_string(), "crossref".to_string()]);
        assert_eq!(first.selected_provider_count, 2);
        assert!(first
            .warnings
            .iter()
            .any(|value| value.contains("Unknown provider override is preserved only as a preview-only hint.")));
        assert!(!first.blockers.is_empty() || first.summary.contains("ready later"));
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_status_matrix_matches_preview_contract() {
        let temp = tempfile::tempdir().unwrap();
        let cases = vec![
            (
                "blank query",
                scientific_metadata_query_plan_preview_request(
                    "   ",
                    Some("metadata planning"),
                    vec!["openalex"],
                    None,
                    None,
                    false,
                    true,
                    true,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::Blocked,
                ScholarChatScientificMetadataQueryPlanStrategy::Blocked,
            ),
            (
                "no selected provider",
                scientific_metadata_query_plan_preview_request(
                    "Signalentdeckung und Wahrnehmung",
                    Some("metadata planning"),
                    vec![],
                    None,
                    None,
                    false,
                    true,
                    true,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderSelection,
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
            ),
            (
                "execution requested without network",
                scientific_metadata_query_plan_preview_request(
                    "Signalentdeckung und Wahrnehmung",
                    Some("metadata planning"),
                    vec!["openalex", "crossref"],
                    None,
                    None,
                    true,
                    false,
                    true,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
            ),
            (
                "provider terms review required",
                scientific_metadata_query_plan_preview_request(
                    "Signalentdeckung und Wahrnehmung",
                    Some("metadata planning"),
                    vec!["openalex", "crossref"],
                    None,
                    None,
                    false,
                    true,
                    false,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
            ),
            (
                "institutional providers selected",
                scientific_metadata_query_plan_preview_request(
                    "Signalentdeckung und Wahrnehmung",
                    Some("metadata planning"),
                    vec!["apa_psycnet"],
                    None,
                    None,
                    false,
                    true,
                    true,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::NeedsProviderConfig,
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
            ),
            (
                "unsupported provider override",
                scientific_metadata_query_plan_preview_request(
                    "Signalentdeckung und Wahrnehmung",
                    Some("metadata planning"),
                    vec![],
                    None,
                    Some(vec!["unknown_provider"]),
                    false,
                    true,
                    true,
                    true,
                ),
                ScholarChatScientificMetadataQueryPlanStatus::UnsupportedProvider,
                ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst,
            ),
        ];

        for (label, request, expected_status, expected_strategy) in cases {
            let preview = preview_scholar_chat_scientific_metadata_query_plan(temp.path(), request)
                .unwrap();
            assert_eq!(preview.status, expected_status, "{label}");
            assert_eq!(preview.query_plan_strategy, expected_strategy, "{label}");
            assert_scientific_metadata_query_plan_preview_boundary_flags(&preview);
            assert!(!preview.summary.is_empty(), "{label}");
            if matches!(expected_status, ScholarChatScientificMetadataQueryPlanStatus::Blocked) {
                assert!(preview.planned_query_steps.iter().all(|step| !step.active), "{label}");
            } else {
                assert!(preview.planned_query_steps.iter().all(|step| step.active), "{label}");
            }
        }
    }

    #[test]
    fn scholar_chat_scientific_metadata_query_plan_provider_family_partition_and_provider_query_records_remain_consistent() {
        let temp = tempfile::tempdir().unwrap();
        let preview = preview_scholar_chat_scientific_metadata_query_plan(
            temp.path(),
            scientific_metadata_query_plan_preview_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific metadata planning"),
                vec![
                    "openalex",
                    "crossref",
                    "pubmed",
                    "eric",
                    "apa_psycnet",
                    "psycinfo",
                ],
                Some(vec![
                    "openalex_psychology",
                    "crossref_psychology",
                    "pubmed_psychology",
                    "eric_psychology",
                    "apa_psycnet_discovery",
                    "local_course_literature",
                ]),
                Some(vec![
                    "openalex",
                    "crossref",
                    "pubmed",
                    "eric",
                    "apa_psycnet",
                    "psycinfo",
                ]),
                false,
                true,
                true,
                true,
            ),
        )
        .unwrap();

        assert_eq!(
            preview.status,
            ScholarChatScientificMetadataQueryPlanStatus::NeedsInstitutionalAccessReview
        );
        assert_eq!(
            preview.query_plan_strategy,
            ScholarChatScientificMetadataQueryPlanStrategy::ProviderPolicyReviewFirst
        );
        assert_scientific_metadata_query_plan_preview_boundary_flags(&preview);
        assert_eq!(
            preview.public_metadata_provider_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "pubmed".to_string(),
                "eric".to_string(),
            ]
        );
        assert_eq!(
            preview.institutional_boundary_provider_ids,
            vec!["apa_psycnet".to_string(), "psycinfo".to_string()]
        );
        assert_eq!(
            preview.provider_family_boundary.public_metadata_provider_ids,
            preview.public_metadata_provider_ids
        );
        assert_eq!(
            preview.provider_family_boundary.institutional_boundary_provider_ids,
            preview.institutional_boundary_provider_ids
        );
        assert!(preview
            .provider_family_boundary
            .institutional_access_requires_manual_review_later);
        assert!(preview.provider_family_boundary.authenticated_access_blocked_now);
        assert!(preview.provider_family_boundary.automated_scraping_blocked_now);
        assert!(preview.provider_family_boundary.paywall_bypass_blocked_now);
        assert!(preview
            .provider_family_boundary
            .summary
            .contains("APA PsycNet"));
        assert_eq!(
            preview
                .query_template_plan
                .provider_ids,
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "pubmed".to_string(),
                "eric".to_string(),
                "apa_psycnet".to_string(),
                "psycinfo".to_string(),
            ]
        );
        assert_eq!(
            preview
                .query_template_plan
                .public_metadata_provider_ids,
            preview.public_metadata_provider_ids
        );
        assert_eq!(
            preview
                .query_template_plan
                .institutional_boundary_provider_ids,
            preview.institutional_boundary_provider_ids
        );
        assert!(preview.query_template_plan.query_template_preview_only);
        assert!(!preview.query_template_plan.will_build_urls);
        assert!(!preview.query_template_plan.will_call_providers);
        assert!(!preview.query_template_plan.summary.is_empty());
        assert!(preview
            .filter_plan
            .provider_filter_coverage
            .iter()
            .all(|value| !value.is_empty()));
        assert!(preview.filter_plan.filter_plan_preview_only);
        assert!(!preview.filter_plan.summary.is_empty());
        assert!(preview
            .result_field_plan
            .planned_common_fields
            .iter()
            .any(|value| value == "title"));
        assert!(preview
            .result_field_plan
            .provider_specific_fields
            .iter()
            .any(|value| value == "doi"));
        assert!(preview.result_field_plan.result_field_plan_preview_only);
        assert!(!preview.result_field_plan.will_emit_citations_now);
        assert!(!preview.result_field_plan.will_write_metadata_now);
        assert!(!preview.result_field_plan.summary.is_empty());
        assert!(preview.safety_boundary.external_services_blocked_now);
        assert!(preview.safety_boundary.url_building_blocked_now);
        assert!(preview.safety_boundary.connector_calls_blocked_now);
        assert!(preview.safety_boundary.network_blocked_now);
        assert!(preview.safety_boundary.http_client_blocked_now);
        assert!(preview.safety_boundary.api_key_reads_blocked_now);
        assert!(preview.safety_boundary.environment_reads_blocked_now);
        assert!(preview.safety_boundary.scraping_blocked_now);
        assert!(preview
            .safety_boundary
            .authenticated_library_access_blocked_now);
        assert!(preview.safety_boundary.paywall_bypass_blocked_now);
        assert!(preview.safety_boundary.metadata_writes_blocked_now);
        assert!(preview.safety_boundary.source_import_blocked_now);
        assert!(!preview.safety_boundary.summary.is_empty());
        assert_eq!(
            preview
                .planned_query_steps
                .iter()
                .map(|step| step.kind.clone())
                .collect::<Vec<_>>(),
            vec![
                ScholarChatScientificMetadataQueryPlanStepKind::ProviderConfigAlignment,
                ScholarChatScientificMetadataQueryPlanStepKind::ProviderFamilyPartition,
                ScholarChatScientificMetadataQueryPlanStepKind::QueryTemplatePlanning,
                ScholarChatScientificMetadataQueryPlanStepKind::FilterPlanning,
                ScholarChatScientificMetadataQueryPlanStepKind::ResultFieldPlanning,
                ScholarChatScientificMetadataQueryPlanStepKind::RateLimitAlignment,
                ScholarChatScientificMetadataQueryPlanStepKind::AttributionAlignment,
                ScholarChatScientificMetadataQueryPlanStepKind::SafetyBoundaryCheck,
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamMetadataConnectorAlignment,
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamEvidencePackAlignment,
                ScholarChatScientificMetadataQueryPlanStepKind::DownstreamLiteratureReviewAlignment,
            ]
        );
        assert!(preview.planned_query_steps.iter().all(|step| step.preview_only));
        assert!(preview.planned_query_steps.iter().all(|step| step.active));
        for step in &preview.planned_query_steps {
            assert!(!step.id.is_empty());
            assert!(!step.label.is_empty());
            assert!(!step.description.is_empty());
            assert!(!step.planned_inputs.is_empty());
            assert!(!step.planned_outputs.is_empty());
            assert_scientific_metadata_query_plan_step_boundary_notes(step);
        }

        assert_eq!(preview.provider_query_plans.len(), 6);
        assert_eq!(
            preview
                .provider_query_plans
                .iter()
                .map(|record| record.provider_id.clone())
                .collect::<Vec<_>>(),
            vec![
                "openalex".to_string(),
                "crossref".to_string(),
                "pubmed".to_string(),
                "eric".to_string(),
                "apa_psycnet".to_string(),
                "psycinfo".to_string(),
            ]
        );
        for record in &preview.provider_query_plans {
            assert_scientific_metadata_query_plan_provider_record_invariants(record);
            match record.provider_id.as_str() {
                "openalex" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata);
                    assert_eq!(record.access_kind_label, "open_metadata");
                    assert!(record.selected);
                }
                "crossref" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata);
                    assert_eq!(record.access_kind_label, "public_metadata_api");
                    assert!(record.selected);
                }
                "pubmed" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata);
                    assert_eq!(record.access_kind_label, "public_metadata_api");
                    assert!(record.selected);
                }
                "eric" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::PublicMetadata);
                    assert_eq!(record.access_kind_label, "public_metadata_api");
                    assert!(record.selected);
                }
                "apa_psycnet" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::InstitutionalBoundary);
                    assert_eq!(record.access_kind_label, "institutional_discovery");
                    assert!(record.selected);
                    assert!(!record.institutional_boundary_notes.is_empty());
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("manual or institutional access review required later")));
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("no automated authenticated access")));
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("no paywall bypass")));
                }
                "psycinfo" => {
                    assert_eq!(record.provider_family, ScholarChatScientificMetadataQueryProviderFamily::InstitutionalBoundary);
                    assert_eq!(record.access_kind_label, "authenticated_library_boundary");
                    assert!(record.selected);
                    assert!(!record.institutional_boundary_notes.is_empty());
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("manual or institutional access review required later")));
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("no automated authenticated access")));
                    assert!(record
                        .institutional_boundary_notes
                        .iter()
                        .any(|value| value.contains("no paywall bypass")));
                }
                other => panic!("unexpected provider id {other}"),
            }
        }
    }

    #[test]
    fn scholar_chat_scientific_preview_implementations_compose_the_expected_chain_without_execution() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let bodies: [(&str, &str, &[&str], &[&str]); 8] = [
            (
                "pub fn preview_scholar_chat_scientific_discipline_registry",
                "pub fn preview_scholar_chat_scientific_source_registry",
                &[][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_scientific_source_registry",
                "pub fn preview_scholar_chat_scientific_query_understanding",
                &["preview_scholar_chat_scientific_discipline_registry"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_scientific_query_understanding",
                "pub fn preview_scholar_chat_scientific_search_plan",
                &[
                    "preview_scholar_chat_scientific_discipline_registry",
                    "preview_scholar_chat_scientific_source_registry",
                ][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_scientific_search_plan",
                "pub fn preview_scholar_chat_local_literature_index",
                &["preview_scholar_chat_scientific_query_understanding"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_local_literature_index",
                "pub fn preview_scholar_chat_course_literature_registry",
                &["preview_scholar_chat_scientific_search_plan"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "CorpusAuthority::",
                    "SourceRegistry::",
                    "RetrievalService::new",
                    "extract_source",
                    "chunk_source",
                    "build_retrieval_index",
                    "preview_scholar_chat_retrieval",
                    "preview_scholar_chat_evidence_plan",
                    "preview_scholar_chat_prompt_pack",
                    "preview_scholar_chat_answer_readiness",
                    "preview_scholar_chat_draft_inference",
                    "preview_scholar_chat_grounded_answer",
                    "smoke_test_local_runtime_inference",
                    "run_llama_runtime_smoke_diagnostic",
                    "run_smoke_inference_probe",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_course_literature_registry",
                "pub fn preview_scholar_chat_answer_readiness",
                &["preview_scholar_chat_local_literature_index", "mode: Some(\"course\".to_string())"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "CorpusAuthority::",
                    "SourceRegistry::",
                    "RetrievalService::new",
                    "extract_source",
                    "chunk_source",
                    "build_retrieval_index",
                    "preview_scholar_chat_retrieval",
                    "preview_scholar_chat_evidence_plan",
                    "preview_scholar_chat_prompt_pack",
                    "preview_scholar_chat_answer_readiness",
                    "preview_scholar_chat_draft_inference",
                    "preview_scholar_chat_grounded_answer",
                    "smoke_test_local_runtime_inference",
                    "run_llama_runtime_smoke_diagnostic",
                    "run_smoke_inference_probe",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_metadata_connector_plan",
                "pub fn preview_scholar_chat_psychology_source_connector_plan",
                &["preview_scholar_chat_scientific_search_plan"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "std::env",
                    "env::",
                    "CorpusAuthority::",
                    "SourceRegistry::",
                    "RetrievalService::new",
                    "extract_source",
                    "chunk_source",
                    "build_retrieval_index",
                    "preview_scholar_chat_local_literature_index",
                    "preview_scholar_chat_course_literature_registry",
                    "preview_scholar_chat_retrieval",
                    "preview_scholar_chat_evidence_plan",
                    "preview_scholar_chat_prompt_pack",
                    "preview_scholar_chat_answer_readiness",
                    "preview_scholar_chat_draft_inference",
                    "preview_scholar_chat_grounded_answer",
                    "smoke_test_local_runtime_inference",
                    "run_llama_runtime_smoke_diagnostic",
                    "run_smoke_inference_probe",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                    "http://",
                    "https://",
                ][..],
            ),
            (
                "pub fn preview_scholar_chat_psychology_source_connector_plan",
                "pub fn preview_scholar_chat_answer_readiness",
                &["preview_scholar_chat_metadata_connector_plan"][..],
                &[
                    "Command::new",
                    "reqwest::",
                    "ureq::",
                    "std::fs",
                    "fs::",
                    "std::env",
                    "env::",
                    "CorpusAuthority::",
                    "SourceRegistry::",
                    "RetrievalService::new",
                    "extract_source",
                    "chunk_source",
                    "build_retrieval_index",
                    "preview_scholar_chat_local_literature_index",
                    "preview_scholar_chat_course_literature_registry",
                    "preview_scholar_chat_retrieval",
                    "preview_scholar_chat_evidence_plan",
                    "preview_scholar_chat_prompt_pack",
                    "preview_scholar_chat_answer_readiness",
                    "preview_scholar_chat_draft_inference",
                    "preview_scholar_chat_grounded_answer",
                    "smoke_test_local_runtime_inference",
                    "run_llama_runtime_smoke_diagnostic",
                    "run_smoke_inference_probe",
                    "build_answer_draft",
                    "build_grounded_answer",
                    "build_final_answer",
                    "build_evidence_pack",
                    "export_answer_artifacts",
                    "http://",
                    "https://",
                ][..],
            ),
        ];

        for (start_marker, end_marker, required_once, forbidden) in bodies {
            let start = source.find(start_marker).unwrap();
            let end = source[start..].find(end_marker).unwrap();
            let body = &source[start..start + end];
            for required in required_once {
                assert_eq!(body.matches(required).count(), 1, "{start_marker} should mention {required} exactly once");
            }
            for forbidden_call in forbidden {
                assert!(!body.contains(forbidden_call), "{start_marker} should not call {forbidden_call}");
            }
        }
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatMetadataConnectorPlanRequest {
                query: "   ".to_string(),
                mode: None,
                course_context: None,
                context_tags: None,
                selected_local_source_ids: None,
                expected_source_kinds: None,
                preferred_metadata_sources: None,
                max_results_per_source: None,
                require_open_access: None,
                require_doi: None,
                year_from: None,
                year_to: None,
            },
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::Blocked
        );
        assert!(preview.normalized_query.is_empty());
        assert!(preview
            .blockers
            .iter()
            .any(|value| value.contains("query_missing")));
        assert_metadata_connector_plan_boundary_fields(&preview);
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_defaults_to_openalex_and_crossref() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatMetadataConnectorPlanRequest {
                query: "Signalentdeckung".to_string(),
                mode: None,
                course_context: None,
                context_tags: Some(vec!["psychology".to_string()]),
                selected_local_source_ids: Some(vec![
                    " source-b ".to_string(),
                    "source-a".to_string(),
                ]),
                expected_source_kinds: Some(vec![" pdf ".to_string(), "markdown".to_string()]),
                preferred_metadata_sources: None,
                max_results_per_source: None,
                require_open_access: None,
                require_doi: None,
                year_from: None,
                year_to: None,
            },
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady
        );
        assert_eq!(
            preview.connector_strategy,
            ScholarChatMetadataConnectorStrategy::OpenalexAndCrossref
        );
        assert_eq!(
            preview.preferred_metadata_sources,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert_eq!(
            preview.source_selection_plan.selected_sources,
            vec!["openalex".to_string(), "crossref".to_string()]
        );
        assert!(preview.openalex_plan.enabled);
        assert!(preview.crossref_plan.enabled);
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("balanced")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_prefers_openalex_only_when_requested() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("scientific_paper"),
                None,
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                None,
                Some(vec!["openalex"]),
                Some(40),
                Some(true),
                Some(true),
                Some(2020),
                Some(2024),
            ),
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady
        );
        assert_eq!(
            preview.connector_strategy,
            ScholarChatMetadataConnectorStrategy::OpenalexFirst
        );
        assert_eq!(preview.preferred_metadata_sources, vec!["openalex".to_string()]);
        assert!(preview.openalex_plan.enabled);
        assert!(!preview.crossref_plan.enabled);
        assert!(preview
            .openalex_plan
            .planned_filters
            .iter()
            .any(|value| value == "open_access=true"));
        assert!(preview
            .openalex_plan
            .planned_filters
            .iter()
            .any(|value| value == "has_doi=true"));
        assert!(preview
            .crossref_plan
            .planned_filters
            .iter()
            .any(|value| value == "has_doi=true"));
        assert!(!preview.deduplication_plan.title_year_fallback_allowed);
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("citation-safe metadata")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_prefers_crossref_only_when_requested() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("course"),
                Some("Psychology course"),
                Some(vec!["course"]),
                Some(vec!["source-b"]),
                Some(vec!["lecture_slide"]),
                Some(vec!["crossref"]),
                Some(15),
                Some(false),
                Some(false),
                Some(2021),
                Some(2023),
            ),
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::ConnectorPlanReady
        );
        assert_eq!(
            preview.connector_strategy,
            ScholarChatMetadataConnectorStrategy::CrossrefFirst
        );
        assert_eq!(preview.preferred_metadata_sources, vec!["crossref".to_string()]);
        assert!(!preview.openalex_plan.enabled);
        assert!(preview.crossref_plan.enabled);
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("course-context metadata")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_needs_metadata_source_for_unknown_preference_only() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                None,
                None,
                Some(vec!["  custom-source  "]),
                Some(25),
                None,
                None,
                None,
                None,
            ),
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::NeedsMetadataSource
        );
        assert!(preview.preferred_metadata_sources.is_empty());
        assert_eq!(preview.unknown_metadata_sources, vec!["custom_source".to_string()]);
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("No supported metadata source selected")));
        assert!(preview
            .next_required_actions
            .iter()
            .any(|value| value.contains("Choose OpenAlex or Crossref")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_clamps_zero_and_high_max_results() {
        let temp = tempfile::tempdir().unwrap();
        let zero_preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                None,
                None,
                None,
                Some(0),
                None,
                None,
                None,
                None,
            ),
        );
        assert_eq!(zero_preview.normalized_max_results_per_source, 1);
        assert!(zero_preview
            .warnings
            .iter()
            .any(|value| value.contains("clamped to 1")));

        let high_preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                None,
                None,
                None,
                Some(250),
                None,
                None,
                None,
                None,
            ),
        );
        assert_eq!(high_preview.normalized_max_results_per_source, 200);
        assert!(high_preview
            .warnings
            .iter()
            .any(|value| value.contains("clamped to 200")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_rejects_invalid_year_range() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("scientific_paper"),
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                Some(2024),
                Some(2020),
            ),
        );
        assert_eq!(
            preview.status,
            ScholarChatMetadataConnectorPlanStatus::Blocked
        );
        assert!(preview
            .blockers
            .iter()
            .any(|value| value.contains("invalid_year_range")));
        assert!(preview
            .warnings
            .iter()
            .any(|value| value.contains("year_from is after year_to")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_uses_expected_filters_for_doi_and_open_access() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &temp,
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("scientific_paper"),
                None,
                None,
                None,
                Some(vec!["pdf"]),
                Some(vec!["openalex", "crossref"]),
                Some(25),
                Some(true),
                Some(true),
                Some(2018),
                Some(2022),
            ),
        );
        assert!(preview.deduplication_plan.doi_required);
        assert!(!preview.deduplication_plan.title_year_fallback_allowed);
        assert!(preview
            .openalex_plan
            .planned_filters
            .iter()
            .any(|value| value == "open_access=true"));
        assert!(preview
            .openalex_plan
            .planned_filters
            .iter()
            .any(|value| value == "has_doi=true"));
        assert!(preview
            .crossref_plan
            .planned_filters
            .iter()
            .any(|value| value == "has_doi=true"));
        assert!(preview
            .crossref_plan
            .planned_filters
            .iter()
            .any(|value| value == "open_access_filter_not_native"));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_uses_mode_specific_notes() {
        let scientific_paper = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("scientific_paper"),
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
            ),
        );
        assert!(scientific_paper
            .warnings
            .iter()
            .any(|value| value.contains("citation-safe metadata")));

        let course = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("course"),
                Some("Psychology course"),
                Some(vec!["course"]),
                Some(vec!["source-a"]),
                Some(vec!["lecture_slide"]),
                None,
                None,
                None,
                None,
                None,
                None,
            ),
        );
        assert!(course
            .warnings
            .iter()
            .any(|value| value.contains("course-context metadata")));
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_reports_ambiguous_and_unknown_concepts() {
        let ambiguous = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Hypothesentests und ANOVA Vergleich",
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
            ),
        );
        assert_eq!(
            ambiguous.status,
            ScholarChatMetadataConnectorPlanStatus::NeedsDisambiguation
        );
        assert_eq!(
            ambiguous.search_plan_status,
            ScholarChatScientificSearchPlanStatus::NeedsDisambiguation
        );

        let unknown = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Signal graph theory",
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
            ),
        );
        assert_eq!(
            unknown.status,
            ScholarChatMetadataConnectorPlanStatus::UnknownConcept
        );
        assert_eq!(
            unknown.search_plan_status,
            ScholarChatScientificSearchPlanStatus::UnknownConcept
        );
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_emits_stable_step_order_and_boundary_only_plans() {
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Signalentdeckung",
                None,
                None,
                None,
                Some(vec!["source-b", "source-a"]),
                Some(vec!["pdf", "markdown"]),
                None,
                Some(25),
                Some(true),
                Some(true),
                Some(2020),
                Some(2024),
            ),
        );
        assert_eq!(
            preview
                .planned_connector_steps
                .iter()
                .map(|step| step.kind.clone())
                .collect::<Vec<_>>(),
            vec![
                ScholarChatMetadataConnectorStepKind::QueryUnderstandingAlignment,
                ScholarChatMetadataConnectorStepKind::MetadataSourceSelection,
                ScholarChatMetadataConnectorStepKind::OpenalexQueryPlan,
                ScholarChatMetadataConnectorStepKind::CrossrefQueryPlan,
                ScholarChatMetadataConnectorStepKind::ResultShapeMappingPlan,
                ScholarChatMetadataConnectorStepKind::DoiFilterPlan,
                ScholarChatMetadataConnectorStepKind::OpenAccessFilterPlan,
                ScholarChatMetadataConnectorStepKind::YearFilterPlan,
                ScholarChatMetadataConnectorStepKind::DeduplicationPlan,
                ScholarChatMetadataConnectorStepKind::AttributionRequirementCheck,
                ScholarChatMetadataConnectorStepKind::ComplianceBoundaryCheck,
                ScholarChatMetadataConnectorStepKind::DownstreamEvidencePackAlignment,
            ]
        );
        assert!(preview.planned_connector_steps.iter().all(|step| step.preview_only));
        assert!(preview.planned_connector_steps.iter().all(|step| step.active));
        assert!(preview
            .planned_connector_steps
            .iter()
            .all(|step| step.boundary_notes.iter().any(|note| note == "preview-only")));
        assert!(preview
            .planned_connector_steps
            .iter()
            .all(|step| step.boundary_notes.iter().any(|note| note == "no web request")));
        for note in [
            "preview-only",
            "no web request",
            "no HTTP client",
            "no API key read",
            "no environment read",
            "no scraping",
            "no connector call",
            "no metadata record write",
            "no artifact write",
            "no persistence",
        ] {
            assert!(preview
                .planned_connector_steps
                .iter()
                .all(|step| step.boundary_notes.iter().any(|value| value == note)));
        }
        assert!(preview.openalex_plan.summary.contains("preview-only"));
        assert!(preview.crossref_plan.summary.contains("preview-only"));
        assert!(preview.compliance_plan.external_metadata_only);
        assert!(preview.compliance_plan.no_fulltext_download);
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_preserves_query_plan_compliance_and_attribution_invariants() {
        let preview = assert_metadata_connector_plan_deterministic_and_path_free(
            &tempfile::tempdir().unwrap(),
            metadata_connector_plan_request(
                "Signalentdeckung",
                Some("scientific_paper"),
                None,
                Some(vec!["psychology"]),
                Some(vec!["source-a"]),
                Some(vec!["pdf"]),
                None,
                Some(25),
                Some(true),
                Some(true),
                Some(2018),
                Some(2022),
            ),
        );

        assert_eq!(preview.openalex_plan.source_id, "openalex");
        assert_eq!(preview.openalex_plan.endpoint_family, "openalex_works");
        assert!(!preview.openalex_plan.requires_api_key);
        assert!(!preview.openalex_plan.will_call_api);
        assert!(!preview.openalex_plan.will_fetch_url);
        assert!(!preview.openalex_plan.will_scrape);
        for field in [
            "id",
            "doi",
            "title",
            "display_name",
            "publication_year",
            "authorships",
            "primary_location",
            "open_access",
            "cited_by_count",
            "concepts",
        ] {
            assert!(preview
                .openalex_plan
                .expected_result_fields
                .iter()
                .any(|value| value == field));
        }

        assert_eq!(preview.crossref_plan.source_id, "crossref");
        assert_eq!(preview.crossref_plan.endpoint_family, "crossref_works");
        assert!(!preview.crossref_plan.requires_api_key);
        assert!(!preview.crossref_plan.will_call_api);
        assert!(!preview.crossref_plan.will_fetch_url);
        assert!(!preview.crossref_plan.will_scrape);
        for field in [
            "DOI",
            "title",
            "author",
            "issued",
            "container-title",
            "publisher",
            "type",
            "URL",
            "is-referenced-by-count",
        ] {
            assert!(preview
                .crossref_plan
                .expected_result_fields
                .iter()
                .any(|value| value == field));
        }

        assert!(preview.compliance_plan.external_metadata_only);
        assert!(preview.compliance_plan.no_fulltext_download);
        assert!(preview.compliance_plan.no_scraping);
        assert!(preview.compliance_plan.respects_rate_limits_later);
        assert!(preview.compliance_plan.requires_terms_review_later);
        assert!(!preview.compliance_plan.will_call_external_services);

        assert!(!preview.attribution_plan.will_emit_attribution_now);
        for note in [
            "cite_metadata_provider_later",
            "preserve_provider_record_ids_later",
            "preserve_doi_and_source_url_later",
            "include_access_date_later",
            "no_fabricated_citations",
        ] {
            assert!(preview
                .attribution_plan
                .required_attribution_notes
                .iter()
                .any(|value| value == note));
        }
        for note in [
            "metadata_preview_not_citation_evidence",
            "full_citation_requires_verified_metadata_later",
            "no_generated_sources",
        ] {
            assert!(preview
                .attribution_plan
                .citation_safety_notes
                .iter()
                .any(|value| value == note));
        }
    }

    #[test]
    fn scholar_chat_metadata_connector_plan_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_metadata_connector_plan")
            .unwrap();
        let end = source[start..]
            .find("pub fn preview_scholar_chat_answer_readiness")
            .unwrap();
        let body = &source[start..start + end];
        assert_eq!(body.matches("preview_scholar_chat_scientific_search_plan").count(), 1);
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("reqwest::"));
        assert!(!body.contains("ureq::"));
        assert!(!body.contains("std::fs"));
        assert!(!body.contains("fs::"));
        assert!(!body.contains("std::env"));
        assert!(!body.contains("env::"));
        assert!(!body.contains("CorpusAuthority::"));
        assert!(!body.contains("SourceRegistry::"));
        assert!(!body.contains("RetrievalService::new"));
        assert!(!body.contains("extract_source"));
        assert!(!body.contains("chunk_source"));
        assert!(!body.contains("build_retrieval_index"));
        assert!(!body.contains("preview_scholar_chat_local_literature_index"));
        assert!(!body.contains("preview_scholar_chat_course_literature_registry"));
        assert!(!body.contains("preview_scholar_chat_retrieval"));
        assert!(!body.contains("preview_scholar_chat_evidence_plan"));
        assert!(!body.contains("preview_scholar_chat_prompt_pack"));
        assert!(!body.contains("preview_scholar_chat_answer_readiness"));
        assert!(!body.contains("preview_scholar_chat_draft_inference"));
        assert!(!body.contains("preview_scholar_chat_grounded_answer"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
        assert!(!body.contains("http://"));
        assert!(!body.contains("https://"));
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_psychology_source_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatPsychologySourceConnectorPlanRequest {
                query: "   ".to_string(),
                mode: None,
                course_context: None,
                context_tags: None,
                selected_local_source_ids: None,
                expected_source_kinds: None,
                preferred_metadata_sources: None,
                preferred_psychology_source_families: None,
                methodology_hints: None,
                population_hints: None,
                topic_area_hints: None,
                max_results_per_source: None,
                require_open_access: None,
                require_doi: None,
                year_from: None,
                year_to: None,
            },
        );
        assert_eq!(
            preview.status,
            ScholarChatPsychologySourceConnectorPlanStatus::Blocked
        );
        assert_eq!(
            preview.psychology_scope_status,
            ScholarChatPsychologyScopeStatus::Blocked
        );
        assert!(preview.summary.contains("blocked"));
        assert!(preview
            .next_required_actions
            .iter()
            .any(|action| !action.is_empty()));
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_needs_source_family_for_unknown_preference_only() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_psychology_source_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatPsychologySourceConnectorPlanRequest {
                query: "Signalentdeckung und Wahrnehmung".to_string(),
                mode: Some("scientific_paper".to_string()),
                course_context: Some("Psychology source connector preview".to_string()),
                context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                selected_local_source_ids: Some(vec!["source-a".to_string()]),
                expected_source_kinds: Some(vec!["pdf".to_string()]),
                preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                preferred_psychology_source_families: Some(vec!["unknown_family".to_string()]),
                methodology_hints: None,
                population_hints: None,
                topic_area_hints: None,
                max_results_per_source: Some(25),
                require_open_access: Some(true),
                require_doi: Some(true),
                year_from: Some(2020),
                year_to: Some(2024),
            },
        );
        assert_eq!(
            preview.status,
            ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
        );
        assert_eq!(
            preview.source_family_strategy,
            ScholarChatPsychologySourceFamilyStrategy::SourceFamilySelectionNeeded
        );
        assert!(preview
            .source_family_selection_plan
            .needs_source_family_selection);
        assert_eq!(
            preview.source_family_selection_plan.supported_families,
            vec![
                "openalex_psychology".to_string(),
                "crossref_psychology".to_string(),
                "pubmed_psychology".to_string(),
                "eric_psychology".to_string(),
                "apa_psycnet_discovery".to_string(),
                "local_course_literature".to_string(),
            ]
        );
        assert_eq!(
            preview.source_family_selection_plan.unknown_families,
            vec!["unknown_family".to_string()]
        );
        assert!(preview
            .next_required_actions
            .iter()
            .any(|action| action.contains("Choose a supported psychology source family")));
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_is_ready_later_with_supported_source_families() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_psychology_source_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatPsychologySourceConnectorPlanRequest {
                query: "Signalentdeckung und Wahrnehmung".to_string(),
                mode: Some("scientific_paper".to_string()),
                course_context: Some("Psychology source connector preview".to_string()),
                context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                selected_local_source_ids: Some(vec!["source-a".to_string()]),
                expected_source_kinds: Some(vec!["pdf".to_string()]),
                preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                preferred_psychology_source_families: Some(vec![
                    "openalex_psychology".to_string(),
                    "crossref_psychology".to_string(),
                ]),
                methodology_hints: Some(vec!["psychophysics".to_string()]),
                population_hints: Some(vec!["adult_population".to_string()]),
                topic_area_hints: Some(vec!["perception".to_string()]),
                max_results_per_source: Some(25),
                require_open_access: Some(true),
                require_doi: Some(true),
                year_from: Some(2020),
                year_to: Some(2024),
            },
        );
        assert_eq!(
            preview.status,
            ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady
        );
        assert_eq!(
            preview.source_family_strategy,
            ScholarChatPsychologySourceFamilyStrategy::OpenMetadataFirst
        );
        assert_eq!(
            preview.source_family_selection_plan.selected_families,
            vec![
                "crossref_psychology".to_string(),
                "openalex_psychology".to_string()
            ]
        );
        let step_kinds = preview
            .planned_source_connector_steps
            .iter()
            .map(|step| step.kind.clone())
            .collect::<Vec<_>>();
        assert_eq!(
            step_kinds.first(),
            Some(&ScholarChatPsychologySourceConnectorStepKind::MetadataConnectorAlignment)
        );
        assert_eq!(
            step_kinds.get(1),
            Some(&ScholarChatPsychologySourceConnectorStepKind::PsychologyScopeClassification)
        );
        assert_eq!(
            step_kinds.get(2),
            Some(&ScholarChatPsychologySourceConnectorStepKind::SourceFamilySelection)
        );
        assert_eq!(
            step_kinds.last(),
            Some(&ScholarChatPsychologySourceConnectorStepKind::DownstreamEvidencePackAlignment)
        );
        assert!(preview
            .planned_source_connector_steps
            .iter()
            .all(|step| {
                step.preview_only
                    && !step
                        .boundary_notes
                        .iter()
                        .any(|note| note.contains("http://"))
            }));
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_uses_expected_family_strategies_for_supported_modes() {
        let temp = tempfile::tempdir().unwrap();
        let full_supported_families = vec![
            "openalex_psychology".to_string(),
            "crossref_psychology".to_string(),
            "pubmed_psychology".to_string(),
            "eric_psychology".to_string(),
            "apa_psycnet_discovery".to_string(),
            "local_course_literature".to_string(),
        ];
        let cases = [
            (
                "default scholar_chat",
                ScholarChatPsychologySourceConnectorPlanRequest {
                    query: "Signalentdeckung und Wahrnehmung".to_string(),
                    mode: None,
                    course_context: Some("Psychology source connector preview".to_string()),
                    context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                    selected_local_source_ids: Some(vec!["source-a".to_string()]),
                    expected_source_kinds: Some(vec!["pdf".to_string()]),
                    preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                    preferred_psychology_source_families: None,
                    methodology_hints: None,
                    population_hints: None,
                    topic_area_hints: None,
                    max_results_per_source: Some(25),
                    require_open_access: Some(true),
                    require_doi: Some(true),
                    year_from: Some(2020),
                    year_to: Some(2024),
                },
                ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady,
                ScholarChatPsychologySourceFamilyStrategy::PsychologyMetadataBalanced,
                vec![
                    "crossref_psychology".to_string(),
                    "openalex_psychology".to_string(),
                    "pubmed_psychology".to_string(),
                ],
            ),
            (
                "scientific_paper",
                ScholarChatPsychologySourceConnectorPlanRequest {
                    query: "Signalentdeckung und Wahrnehmung".to_string(),
                    mode: Some("scientific_paper".to_string()),
                    course_context: Some("Psychology source connector preview".to_string()),
                    context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                    selected_local_source_ids: Some(vec!["source-a".to_string()]),
                    expected_source_kinds: Some(vec!["pdf".to_string()]),
                    preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                    preferred_psychology_source_families: None,
                    methodology_hints: None,
                    population_hints: None,
                    topic_area_hints: None,
                    max_results_per_source: Some(25),
                    require_open_access: Some(true),
                    require_doi: Some(true),
                    year_from: Some(2020),
                    year_to: Some(2024),
                },
                ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady,
                ScholarChatPsychologySourceFamilyStrategy::ScholarlyIndexFirst,
                vec![
                    "apa_psycnet_discovery".to_string(),
                    "crossref_psychology".to_string(),
                    "openalex_psychology".to_string(),
                    "pubmed_psychology".to_string(),
                ],
            ),
            (
                "course",
                ScholarChatPsychologySourceConnectorPlanRequest {
                    query: "Signalentdeckung und Wahrnehmung".to_string(),
                    mode: Some("course".to_string()),
                    course_context: Some("Psychology source connector preview".to_string()),
                    context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                    selected_local_source_ids: Some(vec!["source-a".to_string()]),
                    expected_source_kinds: Some(vec!["pdf".to_string()]),
                    preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                    preferred_psychology_source_families: None,
                    methodology_hints: None,
                    population_hints: None,
                    topic_area_hints: None,
                    max_results_per_source: Some(25),
                    require_open_access: Some(true),
                    require_doi: Some(true),
                    year_from: Some(2020),
                    year_to: Some(2024),
                },
                ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady,
                ScholarChatPsychologySourceFamilyStrategy::CourseLiteratureFirst,
                vec![
                    "crossref_psychology".to_string(),
                    "eric_psychology".to_string(),
                    "local_course_literature".to_string(),
                    "openalex_psychology".to_string(),
                ],
            ),
            (
                "openalex/crossref only",
                ScholarChatPsychologySourceConnectorPlanRequest {
                    query: "Signalentdeckung und Wahrnehmung".to_string(),
                    mode: Some("scientific_paper".to_string()),
                    course_context: Some("Psychology source connector preview".to_string()),
                    context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                    selected_local_source_ids: Some(vec!["source-a".to_string()]),
                    expected_source_kinds: Some(vec!["pdf".to_string()]),
                    preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                    preferred_psychology_source_families: Some(vec![
                        "openalex_psychology".to_string(),
                        "crossref_psychology".to_string(),
                    ]),
                    methodology_hints: None,
                    population_hints: None,
                    topic_area_hints: None,
                    max_results_per_source: Some(25),
                    require_open_access: Some(true),
                    require_doi: Some(true),
                    year_from: Some(2020),
                    year_to: Some(2024),
                },
                ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady,
                ScholarChatPsychologySourceFamilyStrategy::OpenMetadataFirst,
                vec![
                    "crossref_psychology".to_string(),
                    "openalex_psychology".to_string(),
                ],
            ),
        ];

        for (label, request, expected_status, expected_strategy, expected_selected_families) in cases {
            let preview = assert_psychology_source_connector_plan_deterministic_and_path_free(
                &temp,
                request,
            );
            assert_eq!(preview.status, expected_status, "{label}");
            assert_eq!(preview.source_family_strategy, expected_strategy, "{label}");
            assert!(!preview
                .source_family_selection_plan
                .needs_source_family_selection, "{label}");
            assert_eq!(
                preview.source_family_selection_plan.supported_families,
                full_supported_families,
                "{label}"
            );
            assert_eq!(
                preview.source_family_selection_plan.selected_families,
                expected_selected_families,
                "{label}"
            );
        }
    }

    #[test]
    fn scholar_chat_psychology_source_connector_plan_emits_supported_family_plans_and_routing_invariants() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_psychology_source_connector_plan_deterministic_and_path_free(
            &temp,
            ScholarChatPsychologySourceConnectorPlanRequest {
                query: "Signalentdeckung und Wahrnehmung".to_string(),
                mode: Some("scientific_paper".to_string()),
                course_context: Some("Psychology source connector preview".to_string()),
                context_tags: Some(vec!["psychology".to_string(), "signal_detection".to_string()]),
                selected_local_source_ids: Some(vec!["source-a".to_string()]),
                expected_source_kinds: Some(vec!["pdf".to_string()]),
                preferred_metadata_sources: Some(vec!["openalex".to_string(), "crossref".to_string()]),
                preferred_psychology_source_families: None,
                methodology_hints: Some(vec!["psychophysics".to_string()]),
                population_hints: Some(vec!["clinical_population".to_string()]),
                topic_area_hints: Some(vec!["perception".to_string()]),
                max_results_per_source: Some(25),
                require_open_access: Some(true),
                require_doi: Some(true),
                year_from: Some(2020),
                year_to: Some(2024),
            },
        );

        assert_eq!(
            preview.status,
            ScholarChatPsychologySourceConnectorPlanStatus::SourceConnectorPlanReady
        );
        assert_eq!(
            preview.source_family_strategy,
            ScholarChatPsychologySourceFamilyStrategy::ScholarlyIndexFirst
        );
        assert_eq!(
            preview.source_family_selection_plan.supported_families,
            vec![
                "openalex_psychology".to_string(),
                "crossref_psychology".to_string(),
                "pubmed_psychology".to_string(),
                "eric_psychology".to_string(),
                "apa_psycnet_discovery".to_string(),
                "local_course_literature".to_string(),
            ]
        );
        assert_eq!(
            preview.source_family_selection_plan.selected_families,
            vec![
                "apa_psycnet_discovery".to_string(),
                "crossref_psychology".to_string(),
                "openalex_psychology".to_string(),
                "pubmed_psychology".to_string(),
            ]
        );
        assert_eq!(
            preview.psychology_source_family_plans.iter().map(|plan| plan.family_id.clone()).collect::<Vec<_>>(),
            vec![
                "openalex_psychology".to_string(),
                "crossref_psychology".to_string(),
                "pubmed_psychology".to_string(),
                "eric_psychology".to_string(),
                "apa_psycnet_discovery".to_string(),
                "local_course_literature".to_string(),
            ]
        );
        assert!(preview
            .psychology_source_family_plans
            .iter()
            .all(|plan| !plan.will_call_connector
                && !plan.will_fetch_url
                && !plan.will_scrape
                && !plan.will_authenticate
                && !plan.will_bypass_paywall
                && !plan.will_write_metadata));

        let openalex = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "openalex_psychology")
            .unwrap();
        assert_eq!(openalex.connector_kind, "open_metadata");
        assert!(openalex
            .planned_metadata_dependencies
            .iter()
            .any(|value| value == "openalex"));
        let crossref = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "crossref_psychology")
            .unwrap();
        assert_eq!(crossref.connector_kind, "doi_metadata");
        assert!(crossref
            .planned_metadata_dependencies
            .iter()
            .any(|value| value == "crossref"));
        let pubmed = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "pubmed_psychology")
            .unwrap();
        assert_eq!(pubmed.connector_kind, "biomedical_metadata");
        assert!(pubmed
            .expected_result_types
            .iter()
            .any(|value| value == "biomedical_citation_metadata"));
        assert!(pubmed
            .access_boundary_notes
            .iter()
            .any(|value| value.contains("no web request")));
        let eric = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "eric_psychology")
            .unwrap();
        assert_eq!(eric.connector_kind, "education_psychology_metadata");
        assert!(eric
            .expected_result_types
            .iter()
            .any(|value| value == "education_research_metadata"));
        let apa = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "apa_psycnet_discovery")
            .unwrap();
        assert_eq!(apa.connector_kind, "subscription_discovery_boundary");
        assert!(apa
            .access_boundary_notes
            .iter()
            .any(|value| value.contains("no automated authenticated scraping")));
        assert!(apa
            .access_boundary_notes
            .iter()
            .any(|value| value.contains("no paywall bypass")));
        assert!(apa.summary.contains("later manual or institutional access review"));
        let local_course = preview
            .psychology_source_family_plans
            .iter()
            .find(|plan| plan.family_id == "local_course_literature")
            .unwrap();
        assert_eq!(local_course.connector_kind, "local_course_context");
        assert!(local_course
            .access_boundary_notes
            .iter()
            .any(|value| value.contains("no file read")));
        assert!(local_course
            .access_boundary_notes
            .iter()
            .any(|value| value.contains("no local index access")));

        assert!(preview
            .methodology_routing_plan
            .inferred_methodology_routes
            .iter()
            .any(|value| value == "psychophysics"));
        assert!(preview
            .methodology_routing_plan
            .inferred_methodology_routes
            .iter()
            .any(|value| value == "signal_detection"));
        assert!(preview
            .topic_area_routing_plan
            .inferred_topic_areas
            .iter()
            .any(|value| value == "cognitive_psychology"));
        assert!(preview
            .topic_area_routing_plan
            .inferred_topic_areas
            .iter()
            .any(|value| value == "perception"));
        assert!(preview
            .topic_area_routing_plan
            .inferred_topic_areas
            .iter()
            .any(|value| value == "psychophysics"));
        assert!(preview
            .population_routing_plan
            .ethics_or_safety_notes
            .iter()
            .any(|value| value == "clinical_population_requires_later_ethics_or_safety_review"));
        for requirement in [
            "peer_review_or_scholarly_source_status_later",
            "DOI_or_stable_metadata_identifier_later",
            "publication_year_later",
            "authorship_later",
            "journal_or_container_later",
            "methodology_metadata_later",
            "population_metadata_later_when_available",
            "no_fabricated_citations",
            "evidence_pack_required_before_answering",
        ] {
            assert!(preview
                .evidence_requirements_plan
                .evidence_requirements
                .iter()
                .any(|value| value == requirement));
        }
        for field in [
            "doi",
            "title",
            "authors",
            "publication_year",
            "source_family",
            "journal_or_container",
            "abstract_or_summary_when_legally_available",
            "methodology_keywords",
            "population_keywords",
            "open_access_status",
            "provider_record_id",
        ] {
            assert!(preview
                .evidence_requirements_plan
                .preferred_metadata_fields
                .iter()
                .any(|value| value == field));
        }
        assert!(preview.compliance_plan.external_metadata_only);
        assert!(preview.compliance_plan.no_fulltext_download);
        assert!(preview.compliance_plan.no_scraping);
        assert!(preview.compliance_plan.no_authenticated_library_access);
        assert!(preview.compliance_plan.no_paywall_bypass);
        assert!(preview.compliance_plan.respects_rate_limits_later);
        assert!(preview.compliance_plan.requires_terms_review_later);
        assert!(preview
            .compliance_plan
            .requires_institutional_access_review_later);
        assert!(!preview.compliance_plan.will_call_external_services);
        assert!(!preview.compliance_plan.will_write_metadata);
        let step_kinds = preview
            .planned_source_connector_steps
            .iter()
            .map(|step| step.kind.clone())
            .collect::<Vec<_>>();
        assert_eq!(
            step_kinds,
            vec![
                ScholarChatPsychologySourceConnectorStepKind::MetadataConnectorAlignment,
                ScholarChatPsychologySourceConnectorStepKind::PsychologyScopeClassification,
                ScholarChatPsychologySourceConnectorStepKind::SourceFamilySelection,
                ScholarChatPsychologySourceConnectorStepKind::OpenalexPsychologyPlan,
                ScholarChatPsychologySourceConnectorStepKind::CrossrefPsychologyPlan,
                ScholarChatPsychologySourceConnectorStepKind::PubmedPsychologyPlan,
                ScholarChatPsychologySourceConnectorStepKind::EricPsychologyPlan,
                ScholarChatPsychologySourceConnectorStepKind::ApaPsycnetDiscoveryPlan,
                ScholarChatPsychologySourceConnectorStepKind::LocalCourseLiteraturePlan,
                ScholarChatPsychologySourceConnectorStepKind::MethodologyRouting,
                ScholarChatPsychologySourceConnectorStepKind::PopulationRouting,
                ScholarChatPsychologySourceConnectorStepKind::TopicAreaRouting,
                ScholarChatPsychologySourceConnectorStepKind::EvidenceRequirementMapping,
                ScholarChatPsychologySourceConnectorStepKind::ComplianceBoundaryCheck,
                ScholarChatPsychologySourceConnectorStepKind::DownstreamEvidencePackAlignment,
            ]
        );
        assert!(preview
            .planned_source_connector_steps
            .iter()
            .all(|step| step.preview_only && step.active));
        for note in [
            "preview-only",
            "no web request",
            "no HTTP client",
            "no API key read",
            "no environment read",
            "no scraping",
            "no authenticated library access",
            "no paywall bypass",
            "no connector call",
            "no source import",
            "no metadata record write",
            "no artifact write",
            "no persistence",
        ] {
            assert!(preview
                .planned_source_connector_steps
                .iter()
                .all(|step| step.boundary_notes.iter().any(|value| value == note)));
        }
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_blocks_blank_query_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "   ",
                Some("scientific_paper"),
                None,
                vec![],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::Blocked
        );
        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::Blocked
        );
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.contains("query_missing")));
        assert!(preview
            .planned_evidence_items
            .iter()
            .all(|item| !item.enabled && item.preview_only));
        assert!(preview
            .planned_evidence_pack_steps
            .iter()
            .all(|step| !step.active && step.preview_only));
        assert!(preview
            .next_required_actions
            .iter()
            .any(|action| !action.is_empty()));
        assert_scientific_evidence_pack_plan_boundary_fields(&preview);
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_returns_ready_for_signalentdeckung_with_supported_families() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                None,
                Some(vec!["definition", "citation_support"]),
                None,
                true,
                true,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
        assert!(!matches!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
        ));
        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::MetadataCitationFirst
        );
        assert_eq!(preview.selected_source_count, 1);
        assert!(
            !preview
                .source_family_evidence_plan
                .selected_families
                .is_empty()
                || !preview
                    .metadata_evidence_plan
                    .preferred_metadata_sources
                    .is_empty()
        );
        assert!(preview
            .summary
            .contains("ready later and only describes"));
        assert!(preview
            .next_required_actions
            .iter()
            .any(|action| action.contains("Later evidence pack construction")));
        assert!(preview
            .evidence_compliance_plan
            .requires_institutional_access_review_later);
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_uses_local_context_first_for_course_mode() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("course"),
                Some("Course literature registry preview"),
                vec!["course-source-a", "course-source-b"],
                vec!["openalex"],
                None,
                Some(vec!["course_context"]),
                Some("neutral"),
                false,
                false,
            ),
        );

        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::LocalContextFirst
        );
        assert_eq!(preview.local_evidence_context_plan.selected_source_count, 2);
        assert!(preview
            .local_evidence_context_plan
            .requires_local_sources);
        assert!(!preview.local_evidence_context_plan.will_read_files);
        assert!(!preview.local_evidence_context_plan.will_index_files);
        assert!(!preview.local_evidence_context_plan.will_run_retrieval);
        assert!(preview
            .local_evidence_context_plan
            .planned_local_source_roles
            .iter()
            .any(|role| role == "course_context_alignment"));
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_uses_metadata_citation_first_when_doi_is_required() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition"]),
                Some("doi_first"),
                true,
                true,
            ),
        );

        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::MetadataCitationFirst
        );
        assert!(preview
            .metadata_evidence_plan
            .required_identifier_fields
            .iter()
            .any(|field| field == "doi"));
        assert!(preview
            .metadata_evidence_plan
            .citation_metadata_notes
            .iter()
            .any(|note| note == "doi_required_later"));
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_uses_psychology_source_family_first_without_local_sources() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec![],
                vec!["openalex"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );

        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::PsychologySourceFamilyFirst
        );
        assert!(preview.selected_local_source_ids.is_empty());
        assert!(!preview
            .source_family_evidence_plan
            .will_call_source_connectors);
        assert!(!preview.source_family_evidence_plan.will_authenticate);
        assert!(!preview.source_family_evidence_plan.will_bypass_paywall);
        assert!(preview
            .source_family_evidence_plan
            .selected_families
            .iter()
            .any(|family| family == "openalex_psychology"));
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_needs_source_family_for_unknown_preference_only() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["unknown_family"]),
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );

        assert_eq!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::NeedsSourceFamily
        );
        assert_eq!(
            preview.psychology_source_connector_status,
            ScholarChatPsychologySourceConnectorPlanStatus::NeedsSourceFamily
        );
        assert_eq!(
            preview.source_family_strategy,
            ScholarChatPsychologySourceFamilyStrategy::SourceFamilySelectionNeeded
        );
        assert_eq!(
            preview
                .source_family_evidence_plan
                .supported_families,
            vec![
                "openalex_psychology".to_string(),
                "crossref_psychology".to_string(),
                "pubmed_psychology".to_string(),
                "eric_psychology".to_string(),
                "apa_psycnet_discovery".to_string(),
                "local_course_literature".to_string(),
            ]
        );
        assert!(preview
            .source_family_evidence_plan
            .selected_families
            .is_empty());
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_maps_ambiguous_and_unknown_concepts_through_upstream_preview() {
        let temp = tempfile::tempdir().unwrap();
        let ambiguous = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "ANOVA und Hypothesentests Vergleich",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );
        assert_eq!(
            ambiguous.status,
            ScholarChatScientificEvidencePackPlanStatus::NeedsDisambiguation
        );
        assert_eq!(
            ambiguous.psychology_source_connector_status,
            ScholarChatPsychologySourceConnectorPlanStatus::NeedsDisambiguation
        );

        let unknown = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signal graph theory",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );
        assert_eq!(
            unknown.status,
            ScholarChatScientificEvidencePackPlanStatus::UnknownConcept
        );
        assert_eq!(
            unknown.psychology_source_connector_status,
            ScholarChatPsychologySourceConnectorPlanStatus::UnknownConcept
        );

        let scope = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition", "unknown_claim_scope"]),
                None,
                false,
                false,
            ),
        );
        assert_eq!(
            scope.status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
        assert_eq!(
            scope.unknown_claim_scope,
            vec!["unknown_claim_scope".to_string()]
        );
        assert_eq!(
            scope
                .claim_coverage_plan
                .unknown_claim_scope_items,
            vec!["unknown_claim_scope".to_string()]
        );
        assert!(scope
            .next_required_actions
            .iter()
            .any(|action| !action.is_empty()));
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_unknown_citation_style_normalizes_to_neutral_and_warns_only() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition", "citation_support", "unknown_claim_scope"]),
                Some("some_weird_style"),
                false,
                false,
            ),
        );

        assert_eq!(preview.normalized_citation_style, "neutral");
        assert_eq!(
            preview.evidence_pack_strategy,
            ScholarChatScientificEvidencePackStrategy::BalancedLocalAndMetadata
        );
        assert_eq!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_uses_effective_upstream_channels_instead_of_needs_evidence_sources() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec![],
                vec![],
                None,
                Some(vec!["definition"]),
                None,
                false,
                false,
            ),
        );

        assert!(!matches!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::NeedsEvidenceSources
        ));
        assert_eq!(
            preview.status,
            ScholarChatScientificEvidencePackPlanStatus::EvidencePackPlanReady
        );
        assert!(
            !preview
                .source_family_evidence_plan
                .selected_families
                .is_empty()
                || !preview
                    .metadata_evidence_plan
                    .preferred_metadata_sources
                    .is_empty()
        );
        assert!(
            preview
                .source_family_evidence_plan
                .selected_families
                .iter()
                .all(|family| !family.is_empty())
        );
        assert!(
            preview
                .metadata_evidence_plan
                .preferred_metadata_sources
                .iter()
                .all(|source| !source.is_empty())
        );
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_preserves_dto_and_plan_invariants_for_valid_signalentdeckung_requests() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a", "source-b"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition", "method", "limitation", "unknown_claim_scope"]),
                Some("doi_first"),
                true,
                true,
            ),
        );

        assert_eq!(
            preview
                .planned_evidence_items
                .iter()
                .map(|item| item.item_id.clone())
                .collect::<Vec<_>>(),
            vec![
                "local_source_context".to_string(),
                "metadata_record".to_string(),
                "source_family_record".to_string(),
                "citation_attribution_record".to_string(),
                "quality_review_record".to_string(),
                "claim_coverage_record".to_string(),
            ]
        );
        assert!(preview.planned_evidence_items.iter().all(|item| {
            assert_scientific_evidence_pack_item_boundary_fields(item);
            !item.source_channel.is_empty()
                && !item.planned_inputs.is_empty()
                && !item.planned_outputs.is_empty()
                && !item.summary.contains(temp.path().to_string_lossy().as_ref())
                && !item.summary.contains("http://")
                && !item.summary.contains("https://")
        }));
        assert_eq!(
            preview
                .planned_evidence_pack_steps
                .iter()
                .map(|step| step.kind.clone())
                .collect::<Vec<_>>(),
            vec![
                ScholarChatScientificEvidencePackStepKind::PsychologyConnectorAlignment,
                ScholarChatScientificEvidencePackStepKind::EvidenceScopeReview,
                ScholarChatScientificEvidencePackStepKind::LocalEvidenceContextPlan,
                ScholarChatScientificEvidencePackStepKind::MetadataEvidencePlan,
                ScholarChatScientificEvidencePackStepKind::SourceFamilyEvidencePlan,
                ScholarChatScientificEvidencePackStepKind::ClaimCoveragePlan,
                ScholarChatScientificEvidencePackStepKind::CitationAttributionPlan,
                ScholarChatScientificEvidencePackStepKind::EvidenceQualityPlan,
                ScholarChatScientificEvidencePackStepKind::ComplianceBoundaryCheck,
                ScholarChatScientificEvidencePackStepKind::DownstreamAnswerReadinessAlignment,
            ]
        );
        assert!(preview
            .planned_evidence_pack_steps
            .iter()
            .all(|step| step.preview_only && step.active));
        for note in [
            "preview-only",
            "no web request",
            "no HTTP client",
            "no API key read",
            "no environment read",
            "no scraping",
            "no authenticated library access",
            "no paywall bypass",
            "no connector call",
            "no source import",
            "no metadata record write",
            "no file read",
            "no retrieval execution",
            "no Evidence Pack created",
            "no artifact write",
            "no persistence",
        ] {
            assert!(preview
                .planned_evidence_pack_steps
                .iter()
                .all(|step| step.boundary_notes.iter().any(|value| value == note)));
        }
        assert_eq!(
            preview.claim_coverage_plan.minimum_evidence_requirements,
            vec![
                "at_least_one_verified_metadata_record_later".to_string(),
                "at_least_one_local_or_external_evidence_candidate_later".to_string(),
                "citation_safe_identifier_required_later".to_string(),
                "no_answer_without_evidence_pack_later".to_string(),
            ]
        );
        assert_eq!(
            preview.claim_coverage_plan.unsupported_claim_handling,
            vec![
                "mark_unsupported_claims_for_review_later".to_string(),
                "do_not_fabricate_supporting_sources".to_string(),
                "block_answer_generation_until_grounded_later".to_string(),
            ]
        );
        assert_eq!(
            preview.citation_attribution_plan.required_citation_fields,
            vec![
                "title".to_string(),
                "authors".to_string(),
                "publication_year".to_string(),
                "provider_record_id".to_string(),
                "source_family".to_string(),
                "doi_or_stable_identifier".to_string(),
                "access_or_metadata_provider_note_later".to_string(),
            ]
        );
        assert_eq!(
            preview.citation_attribution_plan.required_provider_attribution_notes,
            vec![
                "cite_metadata_provider_later".to_string(),
                "preserve_provider_record_ids_later".to_string(),
                "preserve_doi_and_source_url_later".to_string(),
                "include_access_date_later".to_string(),
                "no_fabricated_citations".to_string(),
            ]
        );
        assert_eq!(
            preview.citation_attribution_plan.citation_safety_notes,
            vec![
                "metadata_preview_not_citation_evidence".to_string(),
                "full_citation_requires_verified_metadata_later".to_string(),
                "no_generated_sources".to_string(),
                "no_answer_without_evidence_pack_later".to_string(),
            ]
        );
        assert_eq!(
            preview.evidence_quality_plan.quality_signals,
            vec![
                "stable_identifier_present_later".to_string(),
                "peer_review_or_scholarly_status_later".to_string(),
                "methodology_match_later".to_string(),
                "topic_area_match_later".to_string(),
                "population_match_later_when_available".to_string(),
                "source_family_relevance_later".to_string(),
                "local_course_relevance_later_when_course_mode".to_string(),
            ]
        );
        assert!(preview.evidence_compliance_plan.external_metadata_only);
        assert!(preview.evidence_compliance_plan.no_fulltext_download);
        assert!(preview.evidence_compliance_plan.no_scraping);
        assert!(preview
            .evidence_compliance_plan
            .no_authenticated_library_access);
        assert!(preview.evidence_compliance_plan.no_paywall_bypass);
        assert!(preview.evidence_compliance_plan.respects_rate_limits_later);
        assert!(preview.evidence_compliance_plan.requires_terms_review_later);
        assert!(!preview
            .evidence_compliance_plan
            .will_call_external_services);
        assert!(!preview.evidence_compliance_plan.will_write_metadata);
        assert!(!preview.evidence_compliance_plan.will_create_evidence_pack);
        assert_eq!(
            preview
                .source_family_evidence_plan
                .supported_families,
            vec![
                "openalex_psychology".to_string(),
                "crossref_psychology".to_string(),
                "pubmed_psychology".to_string(),
                "eric_psychology".to_string(),
                "apa_psycnet_discovery".to_string(),
                "local_course_literature".to_string(),
            ]
        );
        assert!(!preview
            .source_family_evidence_plan
            .will_call_source_connectors);
        assert!(!preview.source_family_evidence_plan.will_authenticate);
        assert!(!preview.source_family_evidence_plan.will_bypass_paywall);
        assert!(!preview.citation_attribution_plan.will_emit_citations_now);
        assert!(
            preview
                .metadata_evidence_plan
                .required_identifier_fields
                .iter()
                .any(|field| field == "doi")
        );
        assert!(
            preview
                .metadata_evidence_plan
                .preferred_metadata_sources
                .iter()
                .any(|source| source == "openalex" || source == "crossref")
        );
    }

    #[test]
    fn scholar_chat_scientific_evidence_pack_plan_is_deterministic_preview_only_and_keeps_boundary_fields() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_scientific_evidence_pack_plan_deterministic_and_path_free(
            &temp,
            scientific_evidence_pack_plan_request(
                "Signalentdeckung und Wahrnehmung",
                Some("scientific_paper"),
                Some("Psychology source connector preview"),
                vec!["source-a", "source-b"],
                vec!["openalex", "crossref"],
                Some(vec!["openalex_psychology", "crossref_psychology"]),
                Some(vec!["definition", "method", "limitation"]),
                Some("doi_first"),
                true,
                true,
            ),
        );

        assert_eq!(preview.planned_evidence_items.len(), 6);
        assert_eq!(preview.planned_evidence_pack_steps.len(), 10);
        let expected_items = [
            (
                "local_source_context",
                "local_source_context",
                vec!["selected_local_source_ids", "normalized_mode"],
                vec!["local_source_context_plan"],
            ),
            (
                "metadata_record",
                "metadata_record",
                vec!["preferred_metadata_sources", "citation_style"],
                vec!["metadata_evidence_plan"],
            ),
            (
                "source_family_record",
                "source_family_record",
                vec![
                    "preferred_psychology_source_families",
                    "psychology_source_connector_preview",
                ],
                vec!["source_family_evidence_plan"],
            ),
            (
                "citation_attribution_record",
                "citation_attribution_record",
                vec!["preferred_metadata_sources", "normalized_citation_style"],
                vec!["citation_attribution_plan"],
            ),
            (
                "quality_review_record",
                "quality_review_record",
                vec![
                    "methodology_hints",
                    "population_hints",
                    "topic_area_hints",
                ],
                vec!["evidence_quality_plan"],
            ),
            (
                "claim_coverage_record",
                "claim_coverage_record",
                vec!["normalized_claim_scope", "query_intent"],
                vec!["claim_coverage_plan"],
            ),
        ];
        for (item, (kind, source_channel, planned_inputs, planned_outputs)) in preview
            .planned_evidence_items
            .iter()
            .zip(expected_items.iter())
        {
            assert_eq!(item.kind, *kind);
            assert_eq!(item.source_channel, *source_channel);
            assert_eq!(
                item.planned_inputs,
                planned_inputs
                    .iter()
                    .map(|value| value.to_string())
                    .collect::<Vec<_>>()
            );
            assert_eq!(
                item.planned_outputs,
                planned_outputs
                    .iter()
                    .map(|value| value.to_string())
                    .collect::<Vec<_>>()
            );
            assert_scientific_evidence_pack_item_boundary_fields(item);
            assert_eq!(item.enabled, !matches!(
                preview.status,
                ScholarChatScientificEvidencePackPlanStatus::Blocked
            ));
        }
        assert!(preview
            .planned_evidence_pack_steps
            .iter()
            .all(|step| step.preview_only));
        assert!(preview
            .planned_evidence_pack_steps
            .iter()
            .all(|step| step.boundary_notes.iter().any(|note| note.contains("preview-only"))));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_rejects_empty_prompt() {
        let temp = tempfile::tempdir().unwrap();
        let result = preview_scholar_chat_grounded_answer_write_eligibility(
            temp.path(),
            grounding_request("   ", Some("Alpha beta."), vec![]),
        );
        assert!(matches!(result, Err(AegisError::ScholarChatPromptEmpty)));
        assert!(!temp.path().join(".aegis").exists());
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_blocks_without_selected_sources() {
        let temp = tempfile::tempdir().unwrap();
        let result = assert_grounded_answer_write_eligibility_deterministic_and_path_free(
            &temp,
            grounding_request("alpha grounded evidence", Some("Alpha beta."), vec![]),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "needs_sources"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Select Scholar Chat source context")));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        for invalid in ["", " ", "..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_grounded_answer_write_eligibility(
                temp.path(),
                grounding_request("alpha grounded evidence", Some("Alpha beta."), vec![invalid.to_string()]),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_blocks_without_draft_text() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_write_eligibility_deterministic_and_path_free(
            &temp,
            grounding_request("alpha grounded evidence", Some("   "), vec![source_id]),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert!(result.blockers.iter().any(|blocker| blocker.kind == "draft_text_missing"));
        assert!(result.summary.contains("blocked"));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_blocks_without_evidence_candidates() {
        let temp = tempfile::tempdir().unwrap();
        let source_path = temp.path().join("note-no-index.md");
        fs::write(&source_path, "alpha beta\n").unwrap();
        let authority = crate::corpus_authority::CorpusAuthority::new(temp.path());
        let source = authority
            .register_source(
                &source_path,
                crate::source_metadata::SourceMetadataInput {
                    title: "Notes".to_string(),
                    source_type: crate::source_metadata::SourceType::MarkdownNote,
                    discipline: "psychology".to_string(),
                    subdiscipline: Some("statistics".to_string()),
                    language: "en".to_string(),
                    tags: vec!["study".to_string()],
                    reliability_notes: None,
                },
            )
            .unwrap();
        crate::extraction::ExtractionService::new(temp.path())
            .extract_source(&source.source_id)
            .unwrap();
        crate::chunking::ChunkingService::new(temp.path())
            .chunk_source(&source.source_id)
            .unwrap();

        let result = assert_grounded_answer_write_eligibility_deterministic_and_path_free(
            &temp,
            grounding_request(
                "alpha grounded evidence",
                Some("Alpha beta."),
                vec![source.source_id.clone()],
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::Blocked);
        assert_eq!(result.candidate_statement_count, 0);
        assert_eq!(result.evidence_candidate_count, 0);
        assert!(result
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "needs_evidence_candidates"));
        assert!(result
            .next_required_actions
            .iter()
            .any(|action| action.contains("Add local evidence candidates")));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_needs_review_when_candidate_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let result = assert_grounded_answer_write_eligibility_deterministic_and_path_free(
            &temp,
            grounding_request(
                "alpha grounded evidence",
                Some("The alpha."),
                vec![source_id],
            ),
        );
        assert_eq!(result.status, ScholarChatGroundedAnswerWriteEligibilityStatus::NeedsReview);
        assert_eq!(result.candidate_status, ScholarChatGroundedAnswerCandidateStatus::NeedsReview);
        assert_eq!(result.candidate_statement_count, 0);
        assert_eq!(result.supported_item_count, 0);
        assert_eq!(result.weakly_supported_item_count, 1);
        assert!(result
            .warnings
            .iter()
            .any(|warning| warning.kind == "needs_review"));
        assert!(result
            .eligibility_reasons
            .iter()
            .any(|reason| reason.contains("Weakly supported or unsupported")));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_is_ready_only_when_candidate_ready_later_and_statements_exist() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let request = grounding_request(
            "alpha grounded evidence",
            Some("Alpha beta. Alpha beta gamma."),
            vec![source_id.clone()],
        );
        let candidate_preview = assert_grounded_answer_candidate_deterministic_and_path_free(&temp, request.clone());
        let write_eligibility_preview = assert_grounded_answer_write_eligibility_deterministic_and_path_free(&temp, request);
        assert_eq!(candidate_preview.status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(write_eligibility_preview.status, ScholarChatGroundedAnswerWriteEligibilityStatus::WriteEligibleLater);
        assert_eq!(write_eligibility_preview.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert!(write_eligibility_preview.candidate_statement_count > 0);
        assert_eq!(write_eligibility_preview.candidate_statement_count, candidate_preview.candidate_statement_count);
        assert_eq!(write_eligibility_preview.selected_source_count, candidate_preview.selected_source_count);
        assert_eq!(write_eligibility_preview.evidence_candidate_count, candidate_preview.evidence_candidate_count);
        assert_eq!(write_eligibility_preview.inspected_item_count, candidate_preview.inspected_item_count);
        assert_eq!(write_eligibility_preview.supported_item_count, candidate_preview.supported_item_count);
        assert_eq!(write_eligibility_preview.weakly_supported_item_count, candidate_preview.weakly_supported_item_count);
        assert_eq!(write_eligibility_preview.unsupported_item_count, candidate_preview.unsupported_item_count);
        assert!(write_eligibility_preview
            .eligibility_reasons
            .iter()
            .any(|reason| reason.contains("All inspected items were supported by local evidence")));
        assert!(write_eligibility_preview
            .next_required_actions
            .iter()
            .any(|action| action.contains("A GroundedAnswer write implementation can be added later")));
    }

    #[test]
    fn scholar_chat_grounded_answer_write_eligibility_rejects_zero_candidate_statements_even_when_candidate_status_is_ready_later() {
        let candidate_preview = ScholarChatGroundedAnswerCandidatePreview {
            status: ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater,
            normalized_prompt: "alpha grounded evidence".to_string(),
            selected_source_count: 1,
            evidence_candidate_count: 1,
            inspected_item_count: 1,
            supported_item_count: 1,
            weakly_supported_item_count: 0,
            unsupported_item_count: 0,
            candidate_statement_count: 0,
            summary: "candidate preview".to_string(),
            candidate_items: Vec::new(),
            preview_only: true,
            not_answer_draft: true,
            not_grounded_answer: true,
            not_final_answer: true,
            no_answer_artifact_created: true,
            no_evidence_pack_built: true,
            no_persistence: true,
            no_llm_call: true,
            no_runtime_execution: true,
            blockers: Vec::new(),
            warnings: Vec::new(),
            next_required_actions: Vec::new(),
        };
        let preview = grounded_answer_write_eligibility_preview_from_candidate_preview(candidate_preview);
        assert_eq!(preview.status, ScholarChatGroundedAnswerWriteEligibilityStatus::Blocked);
        assert_eq!(preview.candidate_status, ScholarChatGroundedAnswerCandidateStatus::CandidateReadyLater);
        assert_eq!(preview.candidate_statement_count, 0);
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "candidate_statements_missing"));
        assert!(preview
            .eligibility_reasons
            .iter()
            .any(|reason| reason.contains("No candidate statements were available")));
        assert_grounded_answer_write_eligibility_boundary_fields(&preview);
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_rejects_invalid_source_ids_before_filesystem_access() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        for invalid in ["..", "../evil", "evil/source", "evil\\source"] {
            let result = preview_scholar_chat_runtime_diagnostic_bridge(
                temp.path(),
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec![invalid.to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
            );
            assert!(matches!(result, Err(AegisError::ScholarChatInvalidSourceId)));
            assert!(!temp.path().join(".aegis").exists());
        }
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_blocks_when_prompt_is_blank() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_bridge_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_bridge_request(
                "   ",
                vec!["src_demo".to_string()],
                Some(helper.to_string_lossy().as_ref()),
                Some(model_path.to_string_lossy().as_ref()),
                true,
                true,
                true,
                Some("Diagnostic smoke prompt."),
                Some(128),
                Some(1_500),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked);
        assert_eq!(preview.selected_source_count, 1);
        assert_eq!(preview.normalized_prompt, "");
        assert_eq!(preview.smoke_execution_plan_status, LocalRuntimeSmokeExecutionPlanStatus::PlanReadyLater);
        assert_eq!(preview.smoke_readiness_status, LocalRuntimeSmokeReadinessStatus::SmokeReadyLater);
        assert_eq!(preview.capability_status, LocalRuntimeCapabilityStatus::CapabilityReadyLater);
        assert_eq!(preview.probe_readiness_status, LocalRuntimeProbeReadinessStatus::ProbeReadyLater);
        assert_eq!(preview.validation_status, LocalRuntimeValidationStatus::ValidationReadyLater);
        assert_eq!(preview.adapter_contract_status, LocalRuntimeAdapterContractStatus::ContractReadyLater);
        assert_eq!(preview.adapter_kind, LocalRuntimeAdapterKind::LlamaCpp);
        assert_eq!(preview.version_probe_status, LocalRuntimeVersionProbeStatus::ProbeSucceeded);
        assert!(preview
            .runtime_diagnostic_reasons
            .iter()
            .any(|reason| reason.contains("Scholar Chat prompt is blank")));
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "scholar_chat_prompt_missing"));
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "2");
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_blocks_when_no_sources_selected() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_bridge_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_bridge_request(
                "Bridge preview prompt.",
                vec![],
                Some(helper.to_string_lossy().as_ref()),
                Some(model_path.to_string_lossy().as_ref()),
                true,
                true,
                true,
                Some("Diagnostic smoke prompt."),
                Some(128),
                Some(1_500),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked);
        assert_eq!(preview.selected_source_count, 0);
        assert!(preview
            .runtime_diagnostic_reasons
            .iter()
            .any(|reason| reason.contains("No Scholar Chat sources are selected")));
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "scholar_chat_sources_missing"));
        assert_eq!(preview.probe_readiness_status, LocalRuntimeProbeReadinessStatus::ProbeReadyLater);
        assert_eq!(preview.validation_status, LocalRuntimeValidationStatus::ValidationReadyLater);
        assert_eq!(preview.adapter_contract_status, LocalRuntimeAdapterContractStatus::ContractReadyLater);
        assert_eq!(preview.adapter_kind, LocalRuntimeAdapterKind::LlamaCpp);
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "2");
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_blocks_when_smoke_execution_plan_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_bridge_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_bridge_request(
                "Bridge preview prompt.",
                vec!["src_demo".to_string()],
                Some(helper.to_string_lossy().as_ref()),
                Some(model_path.to_string_lossy().as_ref()),
                true,
                false,
                true,
                Some("Diagnostic smoke prompt."),
                Some(128),
                Some(1_500),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked);
        assert_eq!(preview.smoke_execution_plan_status, LocalRuntimeSmokeExecutionPlanStatus::Blocked);
        assert_eq!(preview.smoke_readiness_status, LocalRuntimeSmokeReadinessStatus::Blocked);
        assert_eq!(preview.capability_status, LocalRuntimeCapabilityStatus::Blocked);
        assert_eq!(preview.probe_readiness_status, LocalRuntimeProbeReadinessStatus::ProbeReadyLater);
        assert_eq!(preview.validation_status, LocalRuntimeValidationStatus::ValidationReadyLater);
        assert_eq!(preview.adapter_contract_status, LocalRuntimeAdapterContractStatus::ContractReadyLater);
        assert_eq!(preview.adapter_kind, LocalRuntimeAdapterKind::LlamaCpp);
        assert_eq!(preview.version_probe_status, LocalRuntimeVersionProbeStatus::Blocked);
        assert!(preview
            .runtime_diagnostic_reasons
            .iter()
            .any(|reason| reason.contains("smoke execution plan is not ready later")));
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.kind == "probe_execution_not_allowed"));
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "0");
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_needs_review_when_smoke_execution_plan_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_fail.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_bridge_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_bridge_request(
                "Bridge preview prompt.",
                vec!["src_demo".to_string()],
                Some(helper.to_string_lossy().as_ref()),
                Some(model_path.to_string_lossy().as_ref()),
                true,
                true,
                true,
                Some("Diagnostic smoke prompt."),
                Some(128),
                Some(1_500),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview);
        assert_eq!(preview.smoke_execution_plan_status, LocalRuntimeSmokeExecutionPlanStatus::NeedsReview);
        assert_eq!(preview.smoke_readiness_status, LocalRuntimeSmokeReadinessStatus::NeedsReview);
        assert_eq!(preview.capability_status, LocalRuntimeCapabilityStatus::NeedsReview);
        assert_eq!(preview.probe_readiness_status, LocalRuntimeProbeReadinessStatus::ProbeReadyLater);
        assert_eq!(preview.validation_status, LocalRuntimeValidationStatus::ValidationReadyLater);
        assert_eq!(preview.adapter_contract_status, LocalRuntimeAdapterContractStatus::ContractReadyLater);
        assert_eq!(preview.adapter_kind, LocalRuntimeAdapterKind::LlamaCpp);
        assert_eq!(preview.version_probe_status, LocalRuntimeVersionProbeStatus::ProbeFailed);
        assert!(preview
            .runtime_diagnostic_reasons
            .iter()
            .any(|reason| reason.contains("smoke execution plan still needs review")));
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "2");
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_bridge_is_ready_later_when_prompt_sources_and_smoke_execution_plan_are_ready_later() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let before_entries = count_entries_recursively(temp.path());
        let preview = preview_scholar_chat_runtime_diagnostic_bridge(
            temp.path(),
            runtime_diagnostic_bridge_request(
                "Bridge preview prompt.",
                vec!["src_demo".to_string()],
                Some(helper.to_string_lossy().as_ref()),
                Some(model_path.to_string_lossy().as_ref()),
                true,
                true,
                true,
                Some("Diagnostic smoke prompt."),
                Some(128),
                Some(1_500),
            ),
        )
        .unwrap();
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater);
        assert_eq!(preview.smoke_execution_plan_status, LocalRuntimeSmokeExecutionPlanStatus::PlanReadyLater);
        assert_eq!(preview.smoke_readiness_status, LocalRuntimeSmokeReadinessStatus::SmokeReadyLater);
        assert_eq!(preview.capability_status, LocalRuntimeCapabilityStatus::CapabilityReadyLater);
        assert_eq!(preview.probe_readiness_status, LocalRuntimeProbeReadinessStatus::ProbeReadyLater);
        assert_eq!(preview.validation_status, LocalRuntimeValidationStatus::ValidationReadyLater);
        assert_eq!(preview.adapter_contract_status, LocalRuntimeAdapterContractStatus::ContractReadyLater);
        assert_eq!(preview.adapter_kind, LocalRuntimeAdapterKind::LlamaCpp);
        assert_eq!(preview.version_probe_status, LocalRuntimeVersionProbeStatus::ProbeSucceeded);
        assert!(preview
            .runtime_diagnostic_reasons
            .iter()
            .any(|reason| reason.contains("runtime diagnostic bridge is ready later")));
        assert_eq!(preview.normalized_prompt, "Bridge preview prompt.");
        assert_eq!(preview.selected_source_count, 1);
        assert_eq!(preview.normalized_model_format, "gguf");
        assert_eq!(preview.safe_model_file_name.as_deref(), Some("runtime-diagnostic-bridge-model.gguf"));
        assert_eq!(preview.safe_executable_file_name.as_deref(), Some("runtime_diagnostic_bridge_ready.exe"));
        assert_eq!(preview.diagnostic_prompt_char_count, "Diagnostic smoke prompt.".chars().count());
        assert_eq!(preview.max_output_tokens, 32);
        assert_eq!(preview.timeout_ms, 1_500);
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "1");
        assert!(!temp.path().join(".aegis").exists());
        assert_runtime_diagnostic_bridge_boundary_fields(&preview);
        assert_eq!(before_entries, count_entries_recursively(temp.path()));
        assert_eq!(
            fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt"))
                .unwrap(),
            ""
        );
        let debug = format!("{preview:?}");
        let json = serde_json::to_string(&preview).unwrap();
        let temp_path = temp.path().to_string_lossy();
        assert!(!debug.contains(temp_path.as_ref()));
        assert!(!json.contains(temp_path.as_ref()));
        assert!(!debug.contains(model_path.to_string_lossy().as_ref()));
        assert!(!json.contains(model_path.to_string_lossy().as_ref()));
        assert!(!debug.contains(helper.to_string_lossy().as_ref()));
        assert!(!json.contains(helper.to_string_lossy().as_ref()));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_blocks_when_bridge_preview_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "   ",
                    vec![],
                    None,
                    None,
                    false,
                    false,
                    false,
                    None,
                    None,
                    None,
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                    "stdout ok",
                    "stderr ok",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::Blocked);
        assert_eq!(preview.bridge_status, ScholarChatRuntimeDiagnosticBridgeStatus::Blocked);
        assert!(preview
            .runtime_result_reasons
            .iter()
            .any(|reason| reason.contains("bridge preview is blocked")));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_blocks_when_bridge_preview_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_fail.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                    "stdout ok",
                    "stderr ok",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::NeedsReview);
        assert_eq!(preview.bridge_status, ScholarChatRuntimeDiagnosticBridgeStatus::NeedsReview);
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_blocks_when_smoke_diagnostic_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::Blocked,
                    "",
                    "",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::Blocked);
        assert_eq!(preview.smoke_diagnostic_status, LocalRuntimeSmokeDiagnosticStatus::Blocked);
        assert!(preview
            .runtime_result_reasons
            .iter()
            .any(|reason| reason.contains("smoke diagnostic preview is blocked")));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_fails_when_smoke_diagnostic_fails() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::SmokeFailed,
                    "stdout failed",
                    "stderr failed",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed);
        assert_eq!(preview.smoke_diagnostic_status, LocalRuntimeSmokeDiagnosticStatus::SmokeFailed);
        assert!(preview.runtime_result_reasons.iter().any(|reason| reason.contains("failed")));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_fails_when_smoke_diagnostic_times_out() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::TimedOut,
                    "stdout timeout",
                    "stderr timeout",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed);
        assert_eq!(preview.smoke_diagnostic_status, LocalRuntimeSmokeDiagnosticStatus::TimedOut);
        assert!(preview.runtime_result_reasons.iter().any(|reason| reason.contains("timed out")));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_succeeds_later_when_bridge_and_diagnostic_match() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let before_entries = count_entries_recursively(temp.path());
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                runtime_diagnostic_preview_like_bridge(
                    LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                    "stdout ok",
                    "stderr ok",
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticSucceededLater);
        assert_eq!(preview.bridge_status, ScholarChatRuntimeDiagnosticBridgeStatus::RuntimeDiagnosticReadyLater);
        assert_eq!(preview.smoke_diagnostic_status, LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded);
        assert_eq!(preview.normalized_model_format, "gguf");
        assert_eq!(preview.safe_model_file_name.as_deref(), Some("runtime-diagnostic-bridge-model.gguf"));
        assert_eq!(preview.safe_executable_file_name.as_deref(), Some("runtime_diagnostic_bridge_ready.exe"));
        assert_eq!(preview.stdout_preview, "stdout ok");
        assert_eq!(preview.stderr_preview, "stderr ok");
        assert_eq!(before_entries, count_entries_recursively(temp.path()));
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_version_probe_count.txt")).unwrap().trim(), "2");
        assert_eq!(fs::read_to_string(temp.path().join("runtime_diagnostic_bridge_unexpected_call.txt")).unwrap(), "");
        assert!(preview.runtime_result_reasons.iter().any(|reason| reason.contains("succeeded")));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_needs_review_on_metadata_mismatch() {
        let temp = tempfile::tempdir().unwrap();
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let mut diagnostic_preview = runtime_diagnostic_preview_like_bridge(
            LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
            "stdout ok",
            "stderr ok",
        );
        diagnostic_preview.normalized_model_format = "ggml".to_string();
        let preview = assert_runtime_diagnostic_result_deterministic_and_path_free(
            &temp,
            runtime_diagnostic_result_request(
                runtime_diagnostic_bridge_request(
                    "Bridge preview prompt.",
                    vec!["src_demo".to_string()],
                    Some(helper.to_string_lossy().as_ref()),
                    Some(model_path.to_string_lossy().as_ref()),
                    true,
                    true,
                    true,
                    Some("Diagnostic smoke prompt."),
                    Some(128),
                    Some(1_500),
                ),
                diagnostic_preview,
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeDiagnosticResultStatus::NeedsReview);
        assert!(preview
            .runtime_result_reasons
            .iter()
            .any(|reason| reason.contains("metadata mismatch")));
        assert!(preview
            .warnings
            .iter()
            .any(|warning| warning.message.contains("metadata mismatch")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_blocks_when_prompt_is_blank() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "   ",
                    Some("Alpha beta."),
                    vec!["src_demo".to_string()],
                    Some("draft-1"),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "   ",
                        vec!["src_demo".to_string()],
                        None,
                        None,
                        false,
                        false,
                        false,
                        None,
                        None,
                        None,
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                        "stdout ok",
                        "stderr ok",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked);
        assert_eq!(preview.selected_source_count, 1);
        assert_eq!(preview.grounded_answer_execution_plan_status, ScholarChatGroundedAnswerExecutionPlanStatus::Blocked);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::Blocked);
        assert!(preview
            .pipeline_gate_reasons
            .iter()
            .any(|reason| reason.contains("prompt is nonblank")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_blocks_when_no_sources_are_selected() {
        let temp = tempfile::tempdir().unwrap();
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "Bridge preview prompt.",
                    Some("Alpha beta."),
                    vec![],
                    Some("draft-1"),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "Bridge preview prompt.",
                        vec![],
                        None,
                        None,
                        false,
                        false,
                        false,
                        None,
                        None,
                        None,
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                        "stdout ok",
                        "stderr ok",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked);
        assert_eq!(preview.selected_source_count, 0);
        assert!(preview
            .pipeline_gate_reasons
            .iter()
            .any(|reason| reason.contains("at least one Scholar Chat source")));
        assert!(preview
            .blockers
            .iter()
            .any(|blocker| blocker.contains("scholar_chat_sources_missing")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_blocks_when_grounded_plan_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "   ",
                    Some("The alpha."),
                    vec![source_id.clone()],
                    Some(&answer_draft_id),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "   ",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                        "stdout ok",
                        "stderr ok",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked);
        assert_eq!(preview.grounded_answer_execution_plan_status, ScholarChatGroundedAnswerExecutionPlanStatus::Blocked);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::Blocked);
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_blocks_when_runtime_diagnostic_result_is_blocked() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "Bridge preview prompt.",
                    Some("Alpha beta. Alpha beta gamma."),
                    vec![source_id.clone()],
                    Some(&answer_draft_id),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "Bridge preview prompt.",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::Blocked,
                        "",
                        "",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::Blocked);
        assert!(preview
            .pipeline_gate_reasons
            .iter()
            .any(|reason| reason.contains("provided smoke diagnostic preview is blocked")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_blocks_when_runtime_diagnostic_result_failed() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "  alpha grounded evidence  ",
                    Some("  Alpha beta. Alpha beta gamma.  "),
                    vec![format!("  {source_id}  ")],
                    Some(&format!("  {answer_draft_id}  ")),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "  alpha grounded evidence  ",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeFailed,
                        "stdout failed",
                        "stderr failed",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::Blocked);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticFailed);
        assert!(preview
            .pipeline_gate_reasons
            .iter()
            .any(|reason| reason.contains("provided smoke diagnostic preview failed")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_needs_review_when_grounded_plan_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let source_id = build_source_with_index(&temp, "alpha beta gamma\nalpha beta delta\n");
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "alpha grounded evidence",
                    Some("The alpha."),
                    vec![source_id.clone()],
                    Some("draft-1"),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "alpha grounded evidence",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                        "stdout ok",
                        "stderr ok",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview);
        assert_eq!(preview.grounded_answer_execution_plan_status, ScholarChatGroundedAnswerExecutionPlanStatus::NeedsReview);
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_needs_review_when_runtime_diagnostic_result_needs_review() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let mut diagnostic_preview = runtime_diagnostic_preview_like_bridge(
            LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
            "stdout ok",
            "stderr ok",
        );
        diagnostic_preview.normalized_model_format = "ggml".to_string();
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "  alpha grounded evidence  ",
                    Some("  Alpha beta. Alpha beta gamma.  "),
                    vec![format!("  {source_id}  ")],
                    Some(&format!("  {answer_draft_id}  ")),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "  alpha grounded evidence  ",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    diagnostic_preview,
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::NeedsReview);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::NeedsReview);
        assert!(preview
            .pipeline_gate_reasons
            .iter()
            .any(|reason| reason.contains("metadata mismatch")));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_is_ready_later_only_when_plan_and_result_are_ready_later() {
        let temp = tempfile::tempdir().unwrap();
        let (source_id, answer_draft_id, _version_id, _claim_count) = build_readable_answer_draft_fixture(&temp);
        let helper = runtime_diagnostic_bridge_helper_executable(&temp, "runtime_diagnostic_bridge_ready.exe");
        let model_path = temp.path().join("runtime-diagnostic-bridge-model.gguf");
        fs::write(&model_path, "gguf placeholder").unwrap();
        prepare_runtime_diagnostic_bridge_spies(&temp);
        let preview = assert_runtime_answer_pipeline_gate_deterministic_and_path_free(
            &temp,
            runtime_answer_pipeline_gate_request(
                execution_plan_request(
                    "  alpha grounded evidence  ",
                    Some("  Alpha beta. Alpha beta gamma.  "),
                    vec![format!("  {source_id}  ")],
                    Some(&format!("  {answer_draft_id}  ")),
                    true,
                    true,
                ),
                runtime_diagnostic_result_request(
                    runtime_diagnostic_bridge_request(
                        "  alpha grounded evidence  ",
                        vec![source_id],
                        Some(helper.to_string_lossy().as_ref()),
                        Some(model_path.to_string_lossy().as_ref()),
                        true,
                        true,
                        true,
                        Some("Diagnostic smoke prompt."),
                        Some(128),
                        Some(1_500),
                    ),
                    runtime_diagnostic_preview_like_bridge(
                        LocalRuntimeSmokeDiagnosticStatus::SmokeSucceeded,
                        "stdout ok",
                        "stderr ok",
                    ),
                ),
            ),
        );
        assert_eq!(preview.status, ScholarChatRuntimeAnswerPipelineGateStatus::ReadyLater);
        assert_eq!(preview.grounded_answer_execution_plan_status, ScholarChatGroundedAnswerExecutionPlanStatus::PlanReadyLater);
        assert_eq!(preview.runtime_diagnostic_result_status, ScholarChatRuntimeDiagnosticResultStatus::RuntimeDiagnosticSucceededLater);
        assert!(preview.next_required_actions.iter().any(|action| action.contains("A future Scholar Chat runtime answer pipeline step can be added later")));
        assert!(preview.summary.contains("ready later"));
    }

    #[test]
    fn scholar_chat_runtime_answer_pipeline_gate_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_runtime_answer_pipeline_gate")
            .unwrap();
        let end = source[start..]
            .find("fn runtime_diagnostic_result_metadata_mismatch_fields")
            .unwrap();
        let body = &source[start..start + end];
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("Command::new"));
        assert!(!body.contains("build_answer_draft"));
        assert!(!body.contains("build_grounded_answer"));
        assert!(!body.contains("build_final_answer"));
        assert!(!body.contains("build_evidence_pack"));
        assert!(!body.contains("export_answer_artifacts"));
    }

    #[test]
    fn scholar_chat_runtime_diagnostic_result_body_does_not_call_execution_functions() {
        let source = include_str!(concat!(env!("CARGO_MANIFEST_DIR"), "/src/scholar_chat.rs"));
        let start = source
            .find("pub fn preview_scholar_chat_runtime_diagnostic_result")
            .unwrap();
        let end = source[start..]
            .find("fn grounded_answer_build_preflight_required_inputs")
            .unwrap();
        let body = &source[start..start + end];
        assert!(!body.contains("run_llama_runtime_smoke_diagnostic"));
        assert!(!body.contains("smoke_test_local_runtime_inference"));
        assert!(!body.contains("run_smoke_inference_probe"));
        assert!(!body.contains("Command::new"));
    }
}
